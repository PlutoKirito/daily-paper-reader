---
title: Multi-scale Image Representation Compression
title_zh: 多尺度图像表示压缩
authors: "Tianhao Peng, Ho Man Kwan, Fan Zhang, Shan Liu, David Bull"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.04274v1"
tags: ["query:entropy-coding"]
score: 6.0
evidence: 过拟合图像编解码器联合优化量化与率失真并引入多尺度合成
tldr: "过拟合图像编解码器（如Cool-chic）解码复杂度低，但其网络权重训练时保持全精度、量化参数需后处理选取，且合成仅单尺度，未充分优化率失真并忽略跨尺度冗余。MIRC将隐变量、合成网络和熵模型等所有编码组件在统一率失真目标下量化与熵编码，并引入跨阶段参数共享的多尺度表示以提升编码效率。在CLIC2020专业验证集上，MIRC相比VVC（VTM 22.0）取得10.5% BD-rate节省，并提供1.2至2.9 kMAC/像素的配置，可按部署目标灵活选择解码预算。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.04274v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 931, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2609.04274v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 787}, {\"url\": \"assets/figures/arxiv/2609.04274v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 542}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.04274v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 917, \"height\": 279}, {\"url\": \"assets/tables/arxiv/2609.04274v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 263}]"
motivation: 现有过拟合图像编解码器未完全面向率失真优化：网络权重训练时保持全精度，量化参数在训练后单独选取，且单尺度合成忽略跨尺度冗余。
method: MIRC采用NVRC端到端压缩流程，将隐变量、合成网络和熵模型等所有编码组件在统一率失真目标下量化与熵编码，并引入跨阶段参数共享的多尺度表示。
result: "在CLIC2020专业验证集上，MIRC相比VVC（VTM 22.0）取得10.5% BD-rate节省，并提供1.2至2.9 kMAC/像素的配置，解码预算可按部署目标选择。"
conclusion: MIRC表明，将全部编码组件联合率失真优化并结合多尺度参数共享，可在低解码复杂度下显著优于VVC，为过拟合图像编解码提供灵活配置。
---

## 摘要
过拟合编解码器在图像和视频压缩方面已展现出有前景的性能。特别是在图像压缩方面，Cool-chic 系列模型相较于与场景无关的模型表现出具有竞争力的性能，其解码复杂度低数个数量级，代价是更长的过拟合过程。然而，这些过拟合图像编解码器并未完全针对率失真目标进行优化：其网络权重在训练期间保持全精度，而相关的量化参数则在单独的后训练阶段选取。此外，其合成过程在单一尺度上进行，忽略了跨尺度冗余。本文提出 MIRC，一种过拟合图像编解码器，其中每个被编码的组件，包括潜在变量、合成网络和熵模型，都在单一率失真目标下进行量化和熵编码，并采用神经视频表示编解码器 NVRC 的端到端压缩流程。我们进一步引入具有跨阶段参数共享的多尺度表示，以较小的传输开销提升编码效率。在 CLIC2020 专业验证集上，MIRC 相较于 VVC（VTM 22.0）实现了 10.5% 的 BD-rate 节省。此外，MIRC 提供了一系列配置，覆盖每像素 1.2 至 2.9 kMAC，因此可以根据部署目标选择解码预算。

## Abstract
Overfitted codecs have demonstrated promising performance for image and video compression. In particular, for image compression, the Cool-chic family of models has shown competitive performance against scene-agnostic models, with orders of magnitude lower decoding complexity at the cost of a longer overfitting process. However, these overfitted image codecs are not fully optimized toward the rate-distortion objective: their network weights remain in full precision during training, and the associated quantization parameters are selected in a separate post-training stage. Furthermore, their synthesis operates at a single scale, which overlooks cross-scale redundancy. In this paper, we propose MIRC, an overfitted image codec in which every coded component, including the latents, the synthesis network, and the entropy models, is quantized and entropy coded under a single rate-distortion objective, adopting the end-to-end compression pipeline of the neural video representation codec NVRC. We further introduce a multi-scale representation with cross-stage parameter sharing, which improves coding efficiency at a small transmitted overhead. On the CLIC2020 professional validation set, MIRC achieves a 10.5% BD-rate saving against VVC (VTM 22.0). Moreover, MIRC offers a family of configurations spanning 1.2 to 2.9 kMAC per pixel, so the decoding budget can be selected to match the deployment target.