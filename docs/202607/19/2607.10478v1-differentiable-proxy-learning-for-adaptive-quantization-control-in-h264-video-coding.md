---
title: Differentiable Proxy Learning for Adaptive Quantization Control in H.264 Video Coding
title_zh: 可微分代理学习用于H.264视频编码中的自适应量化控制
authors: "Qihan Xu, Ivan V. Bajić"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10478v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 视频编码中的可微代理自适应QP控制
tldr: "H.264编码参数优化因不可微而困难。本文提出可微代理学习方法，基于可变速率学习压缩模型，通过软索引机制使代理对量化参数可微，训练其近似H.264率失真行为。该代理准确拟合目标编码器，基于它的自适应量化框架在感知优化和机器视觉任务中显著提升率任务权衡，BD-rate降低最高17.12%。为视频编码自适应控制提供了可微优化方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 915, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1601, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 714, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 697, \"height\": 1054, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 785, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 684, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 689, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1415, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 645, \"height\": 639, \"label\": \"Figure\"}]"
motivation: H.264编码参数优化因标准编解码器不可微而困难，现有代理方法缺乏对保真度的显式刻画。
method: 基于可变速率学习压缩模型，通过软索引机制构建对量化参数可微的代理，训练其近似H.264全局和空间率失真行为。
result: "代理准确近似H.264率失真；自适应量化框架在语义分割和MS-SSIM上分别降低BD-rate 17.12%和15.30%。"
conclusion: 所提可微代理学习有效实现自适应量化控制，优于固定量化参数基线。
---

## 摘要
H.264由于其相对简单、高效以及软硬件实现的广泛可用性，在过去二十年中一直是使用最广泛的视频编码格式。然而，由于标准视频编码器的不可微性质，为特定目标（如感知质量或机器视觉任务）优化编码参数（如量化参数QP）具有挑战性。尽管最近已使用可微分代理来对标准编码器进行基于梯度的优化，但它们对目标编码器的保真度很少被明确表征。在本文中，我们提出了一种用于H.264帧内编码器的可微分代理学习方法，以实现自适应量化控制。基于可变速率学习压缩模型，所提出的代理通过软索引机制使其对编码器QP可微。然后训练它来近似H.264在两种量化设置下的率失真行为：全局QP（每幅图像使用一个QP）和空间QP（在宏块级别分配QP）。利用冻结的已训练代理，我们开发了一个基于代理的自适应量化（AQ）框架，用于感知优化和机器视觉任务。实验结果表明，所提出的代理紧密近似了H.264帧内编码器的率失真行为。由此产生的基于代理的AQ框架在固定QP的H.264基线上持续改善了率任务权衡，对于语义分割实现了高达17.12%的BD率降低，对于MS-SSIM实现了15.30%的BD率降低。

## Abstract
H.264 has been the most widely used video coding format for the past two decades due to its relative simplicity, efficiency, and wide availability of software and hardware implementations. However, optimizing codec parameters such as the quantization parameter (QP) for specific objectives (e.g., perceptual quality or machine vision tasks) is challenging due to the non-differentiable nature of standard video codecs. While differentiable proxies have recently been used to enable gradient-based optimization around standard codecs, their fidelity to the target codec is rarely explicitly characterized. In this paper, we propose a differentiable proxy learning method for H.264 intra codec to enable adaptive quantization control. Built upon a variable-rate learned compression model, the proposed proxy is made differentiable with respect to codec QP through a soft-indexing mechanism. It is then trained to approximate the rate-distortion behavior of H.264 under two quantization settings: global-QP, which uses one QP per image, and spatial-QP, which assigns QPs at the macroblock level. Using the frozen trained proxy, we develop a proxy-based adaptive quantization (AQ) framework for both perceptual optimization and machine vision tasks. Experimental results demonstrate that the proposed proxies closely approximate the rate-distortion behavior of H.264 intra codec. The resulting proxy-based AQ framework consistently improves rate-task trade-offs over fixed-QP H.264 baselines, achieving BD-rate reduction of up to 17.12% for semantic segmentation and 15.30% for MS-SSIM.

---

## 论文详细总结（自动生成）

# 可微分代理学习用于H.264视频编码中的自适应量化控制——详细论文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：H.264作为使用最广泛的视频编码格式，其编码参数（特别是量化参数QP）的优化对于提升感知质量或机器视觉任务性能至关重要。然而，标准视频编码器（如H.264）的不可微性质导致无法直接进行梯度反向传播，阻碍了端到端优化。
- **核心问题**：现有可微分代理方法多作为辅助模块用于优化预处理或后处理，鲜有明确评估代理对目标编码器的保真度。本文聚焦于构建高保真的可微分代理，并基于此实现自适应量化控制。
- **整体含义**：通过代理学习使不可微编码器可微，从而支持基于梯度的QP优化，显著改善率-任务权衡（如目标检测、语义分割、感知质量），为视频编码自适应性提供可微优化方案。

## 2. 论文提出的方法论

### 核心思想
基于可变速率学习压缩模型（DCVC-FM帧内编码器），通过**软索引机制**使代理对H.264的量化参数可微，训练代理近似H.264帧内编码器在全局QP和空间QP两种设置下的率失真行为。训练完成后，冻结代理，用于训练自适应量化网络（AQ网络），该网络针对下游任务优化QP配置。

### 关键技术细节
1. **代理模型构建**：
   - 基模型：DCVC-FM帧内编码器（可变速率神经压缩模型，使用可学习量化缩放）。
   - 将DCVC-FM的离散质量索引映射到H.264的QP值：通过最小化BPP-PSNR欧氏距离建立映射表（基于Kodak数据集统计）。
2. **软索引机制**（使量化缩放可微）：
   - 对于每个宏块位置$(u,v)$，计算软分配权重：
     $$w_{u,v,i} = \frac{\exp\left(-\frac{(q_{u,v} - i)^2}{\tau}\right)}{\sum_{j=1}^L \exp\left(-\frac{(q_{u,v} - j)^2}{\tau}\right)}$$
     其中$\tau$控制锐度，$i$为索引，$L$为缩放因子个数。
   - 最终缩放因子：$s_{u,v} = \sum_{i=1}^L w_{u,v,i} s_i$。
   - 全局QP设置下退化到单个全局缩放因子；空间QP下每个宏块独立计算。
3. **代理训练**：
   - 输入图像X和QP配置Q（全局标量或空间图），分别输入H.264帧内编码器和代理，获取重建图像和码率。
   - 训练损失：$L_p = D_R(R_c, R_p) + \lambda D_X(\hat{X}_c, \hat{X}_p)$（码率绝对差 + 重建MSE）。
   - $\lambda$由映射后的代理控制参数q计算（使用DCVC-FM的指数公式），并引入经验乘子$\alpha$。
   - 空间QP设置下，$\bar{\lambda}$为所有宏块$\lambda(q_{u,v})$的平均值。
4. **自适应量化控制**：
   - **全局QP控制**：轻量CNN（3个步长卷积+全局平均池化+全连接）预测单幅图像QP值。
   - **空间QP控制**：全卷积编码器，输入包括图像和扩展后的$\beta$图（率-任务权衡参数），通过sigmoid和缩放输出宏块级QP图。$\beta$通过$s(\beta)=1+\alpha\tanh(w\beta+b)$调制logits。
   - 优化目标：$\phi^* = \arg\min_\phi \mathbb{E}[R_p + \beta L_{\text{task}}(\hat{X}_p)]$。

## 3. 实验设计

- **数据集**：
  - 代理训练：Vimeo-90k（仅第一帧）。
  - 代理评估：Kodak数据集。
  - AQ训练和测试：COCO（目标检测）、Pascal VOC（语义分割）、Vimeo-90k（感知优化）、Kodak（感知质量评估）。
- **基准**：H.264参考模型JM 19.0（固定QP作为基线）。
- **对比方法**：
  - 全局QP自适应 vs 固定QP。
  - 空间QP自适应 vs 固定QP。
  - 全局QP与空间QP自适应之间的对比（在语义分割任务中）。
- **评价指标**：BD率、PSNR、MS-SSIM、YOLOv5的mAP、DeepLabv3的mIoU。
- **实验配置**：
  - 全局QP锚点集合：{35,40,45,51}。
  - 空间QP锚点分布：5个均匀分布U[20,30], U[25,35], U[30,40], U[35,45], U[40,51]。
  - 机器视觉任务：YOLOv5（S/M/L三种尺寸）；语义分割：DeepLabv3 ResNet-50。
  - 感知优化：MS-SSIM为目标。

## 4. 资源与算力

- 文中仅提及：模型在PyTorch实现，使用Adam优化器（学习率1e-5，batch size=4），训练在单张NVIDIA 2080 Ti GPU上进行。
- **未明确说明**：
  - 训练总时长、GPU数量、训练epoch数、推理时间等细节。因此**算力信息不完整**，但可推测实验规模较小（单卡2080 Ti）。

## 5. 实验数量与充分性

- **实验组数**：
  - 代理保真度评估：全局QP和空间QP各一组（BD率、PSNR、MS-SSIM曲线、定性视觉对比）。
  - 自适应全局QP：3种YOLOv5大小的COCO目标检测（每组5个β值，共约15个实验点）+ 1组Kodak MS-SSIM感知。
  - 自适应空间QP：Pascal VOC语义分割（与全局QP对比）+ Vimeo-90k感知MS-SSIM。
  - 定性结果：多幅示例图（Fig.5、Fig.8、Fig.10）。
- **充分性分析**：
  - **优点**：覆盖了目标检测、语义分割、感知质量三种任务；对比了全局与空间两种控制粒度；报告了BD率等主流指标。
  - **不足**：
    - 缺少消融实验（如不同$\tau$值、不同锚点数量、代理训练中$\lambda$的敏感性）。
    - 未与其他自适应量化方法（如传统速率控制算法、其他可微代理方法）对比。
    - 仅在YOLOv5和DeepLabv3上测试，未涵盖更多视觉任务（如实例分割、视频任务）。
    - 未在更大的数据集（如ImageNet）或更多视频编码标准（H.265/HEVC）上验证。

## 6. 论文的主要结论与发现

1. **代理保真度**：全局QP代理的BD率为-4.75%（PSNR）和-2.56%（MS-SSIM），空间QP代理的BD率为-15.01%（PSNR）和-12.91%（MS-SSIM），表明代理能近似H.264率失真行为，但空间QP下偏差更大。
2. **自适应全局QP**：在COCO目标检测上，相比固定QP基线，BD率降低8.96%~9.50%（YOLOv5 S/M/L）；在Kodak感知MS-SSIM上降低3.75%。
3. **自适应空间QP**：在Pascal VOC语义分割上BD率降低17.12%（全局QP仅-11.99%），在感知MS-SSIM上降低15.30%。
4. 空间QP控制优于全局QP控制，因其能实现更细粒度的区域级比特分配。
5. 代理的高保真并非必要：即使代理重建不完全精确，仍可通过优化实现显著的率-任务提升。

## 7. 优点

1. **方法创新**：首次将软索引机制引入H.264代理学习，使代理对QP可微，支持全局和空间两种量化设置。
2. **保真度明确评估**：显式量化了代理的率-失真匹配误差（BD率），填补了先前工作的空白。
3. **应用广泛**：同时支持感知质量和机器视觉任务，验证了代理的多功能性。
4. **实用性强**：训练完成后代理冻结，仅用于梯度回传，推理时仍使用实际H.264编码器，无额外推理开销。
5. **实验设计合理**：对比了不同控制粒度，展示了空间QP对任务性能的额外增益。

## 8. 不足与局限

1. **实验覆盖有限**：
   - 仅测试H.264帧内模式，未扩展至帧间预测（作者在结论中承认）。
   - 仅评估了YOLOv5和DeepLabv3，未涉及更多现代视觉架构或视频分析任务。
   - 未与最新的可微代理方法（如基于神经模拟的）或传统AQ算法（如H.264内置AQ）对比。
2. **偏差风险**：
   - 代理训练基于Vimeo-90k，评估在Kodak和COCO等，数据集领域差异可能影响保真度。
   - 空间QP训练仅依赖5种均匀分布锚点，可能无法覆盖真实场景中的复杂QP分布。
3. **消融不足**：
   - 未分析软索引中$\tau$的影响。
   - 未分析代理训练中$\lambda$权重$\alpha$的影响。
   - 未比较不同代理网络架构（如使用更简单的MLP替代DCVC-FM）的效果。
4. **计算开销**：代理本身是大型网络（DCVC-FM），训练需要GPU，但文中未提供训练时间或参数量。
5. **可复现性**：超参数（如$\tau$、$\alpha$、$\lambda$范围）部分未明确给出，可能影响复现。

（完）
