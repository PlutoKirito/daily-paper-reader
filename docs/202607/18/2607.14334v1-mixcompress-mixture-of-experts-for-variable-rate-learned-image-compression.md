---
title: "MixCompress: Mixture of Experts for Variable Rate Learned Image Compression"
title_zh: MixCompress：面向可变码率学习图像压缩的专家混合模型
authors: "Calvin-Khang Ta, Praneet Singh, Tong Shao, Peng Yin"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14334v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 使用混合专家模型解决可变速率图像压缩中的梯度冲突问题
tldr: 传统学习图像压缩需为每个码率点独立存模型，现有变比特率方法用密集调制但共享骨干导致特征纠缠。MixCompress基于稀疏混合专家路由缓解梯度冲突，并引入混合深度动态缩放容量，结合条件辅助变换调制子带能量。结果匹配甚至超越单速率基线，建立了计算高效编码的新帕累托前沿。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14334v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 799, \"height\": 330, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1014, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 857, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 876, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 887, \"height\": 733, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1232, \"height\": 259, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14334v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1259, \"height\": 555, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14334v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 996, \"height\": 585, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14334v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1040, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14334v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 822, \"height\": 293, \"label\": \"Table\"}]"
motivation: 现有变比特率方法因共享骨干导致低码率平滑与高频细节冲突，性能次优。
method: 采用稀疏门控MoE缓解梯度冲突，引入MoD动态缩放容量，并结合CAT进行子带能量调制。
result: MixCompress匹配甚至超越单独优化的单速率基线，达到新的帕累托最优。
conclusion: 实现了计算高效且性能优越的统一变比特率图像压缩。
---

## 摘要
学习图像压缩（LIC）因需要为每个率失真工作点存储独立模型而受限于开销。现有的可变码率（VBR）方法旨在通过密集参数调制来减少此开销，但强制共享主干网络逼近不同映射会导致严重的特征纠缠。具体而言，低码率平滑梯度与高频纹理细节的保留存在固有冲突，从而导致次优性能。为解决此问题，我们提出MixCompress，一种基于稀疏结构特化的统一VBR框架。稀疏门控的专家混合（MoE）路由成功缓解了梯度冲突，但其计算预算固定。为应对更高码率带来的更大表示需求，我们引入深度混合（MoD）扩展以动态缩放模型容量。结合用于动态子带能量调制的条件辅助变换（CAT），我们的分层框架有效实现了动态容量缩放。大量实验表明，MixCompress不仅能够匹配甚至超越独立优化的单码率基线，为计算高效的图像编码建立了新的帕累托前沿。

## Abstract
Learned image compression (LIC) is bottlenecked by the need to store independent models for each rate-distortion operating point. Existing variable bit-rate (VBR) methods aim to reduce this overhead via dense parameter modulation, but forcing a shared backbone to approximate divergent mappings causes severe feature entanglement. Specifically, low-rate smoothing gradients inherently conflict with the preservation of high-frequency textural details, leading to sub-optimal performance. To resolve this, we propose MixCompress, a unified VBR framework based on sparse structural specialization. While sparsely gated Mixture-of-Experts (MoE) routing successfully mitigates gradient conflict, it operates on a fixed computational budget. To address the increased representational demands of higher bit-rates we introduce a Mixture-of-Depths (MoD) extension to dynamically scale model capacity. Combined with Conditional Auxiliary Transforms (CAT) for dynamic sub-band energy modulation, our hierarchical framework effectively dynamically scales capacity. Extensive evaluations demonstrate that MixCompress not only matches individually optimized single-rate baselines but can even surpass them, establishing a new Pareto frontier for computationally efficient image coding.

---

## 论文详细总结（自动生成）

### 论文总结：MixCompress：面向可变码率学习图像压缩的专家混合模型

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的学习图像压缩（LIC）方法通常需要为每个率失真（R-D）工作点（即每个目标码率）独立存储和训练一个模型，导致巨大的存储、训练和部署开销。虽然已有可变码率（VBR）方法试图通过在一个共享主干网络上使用密集参数调制（如条件卷积或潜在缩放）来统一模型，但这会引发严重的**特征纠缠**和**梯度冲突**。
*   **背景与动机**：低码率优化倾向于平滑梯度，而高码率优化需要保留高频纹理细节。将这两个冲突目标强加于共享骨干网络的同一组参数上，会导致次优的折衷，性能远不如各个码率点独立优化的单码率模型。因此，本文旨在设计一种能够有效解耦不同码率优化目标、在不牺牲性能的前提下实现统一VBR模型的框架。

#### 2. 论文提出的方法论：核心思想、关键技术细节
*   **核心思想**：通过**稀疏结构特化**替代传统的**密集参数调制**，从根本上缓解梯度冲突。具体而言，引入稀疏门控的专家混合（MoE）和深度混合（MoD）模块，为不同码率点分配不同的网络路径和计算容量。
*   **关键技术细节**：
    *   **稀疏门控混合专家（MoE）**：
        *   **结构**：在编码器、解码器和超先验网络的关键瓶颈处，用MixCompress块替换原有层。该块包含一个始终激活的**共享专家**和K个从N个专家中通过**稀疏门控**选出的**路由专家**。
        *   **路由机制**：门控网络仅根据码率嵌入`λe`（与输入图像无关）生成路由分布，通过Top-K选择激活专家。这使得专家选择独立于输入数据，推理时可预计算并缓存，实现**零路由开销**。
        *   **梯度冲突缓解**：将参数分割为共享和路由部分。路由专家的梯度更新仅作用于被激活的专家，有效隔离了不同码率点的优化轨迹。公式(7)证明，路由子空间的梯度内积仅与重叠的专家有关，减少了冲突。实验（图4）表明，该方法将梯度余弦相似度分布从负峰区间移向正交区间（0附近）。
    *   **深度混合（MoD）扩展**：
        *   **动机**：MoE的参数分配是固定的，而高码率需要更多的表示能力。
        *   **实现**：将MoE中的专家设计为**渐进深度**的结构。例如，第i个专家由i个嵌套的变换块（如Bi-RWKV块）组成。高码率样本被路由到更深、容量更大的专家，从而动态扩展模型容量。
    *   **条件辅助变换（CAT）**：
        *   **动机**：传统辅助变换（如AuxT）使用静态的子带缩放，不适合可变码率场景。
        *   **实现**：在编码器和解码器侧引入CAT/iCAT模块，其子带能量调制参数由码率嵌入`λe`通过一个FiLM网络动态生成。这使得模型能够根据目标码率调整频域能量压缩程度（低码率时更强压缩，高码率时保留更多细节），且不影响主变换和熵模型。
*   **优化目标**：公式(2)定义了VBR的联合优化目标，在训练时均匀采样不同的`λ`，最小化期望损失。

#### 3. 实验设计：数据集、基准与对比方法
*   **数据集**：
    *   **训练集**：OpenImages数据集的前40万张高分辨率图像（随机裁剪成256x256或512x512）。
    *   **测试集**：Kodak（24张，768x512）、CLIC Professional验证/测试集（41/60张，高至2K分辨率）、Tecnick（100张，1200x1200）。
*   **基准**：传统编码器VTM-23.1作为BD-rate计算的锚点。
*   **对比方法**：
    *   **单码率基线**：Hyperprior, Minnen18, Cheng20, ELIC, TCM, FAT, LALIC等。
    *   **可变码率（VBR）基线**：QRAF（量化感知变速率框架）和Conditional Convolutions（条件卷积），并在LALIC骨干网络上重新实现以确保公平对比。
    *   **本文方法**：MixCompress的两个变体（MoE和MoD），集成在LALIC和LIC-TCM两个单码率骨干网络上。

#### 4. 资源与算力
*   **训练配置**：所有模型均在**单个NVIDIA H100 GPU**上完成训练。
*   **训练策略**：采用与单码率基线LALIC相同的总训练时长和Adam优化器（batch size=8）。VBR模型在同一个训练过程中联合学习所有码率点，每个码率点看到的迭代次数仅为独立训练一个单码率模型的1/6。训练分为三个阶段（40个epoch + 4个epoch + 4个epoch微调）。

#### 5. 实验数量与充分性
*   **实验数量**：
    *   **主性能对比**：在四个标准数据集（Kodak, CLIC-V, CLIC-T, Tecnick）上，与多个单码率和VBR基线进行了BD-rate（PSNR）指标的对比（表1）。
    *   **消融实验**：在LALIC骨干上系统地评估了每个组件（CondConv, AuxT, CAT, MoE, MoD）的贡献（表3）。
    *   **机制验证实验**：对梯度冲突缓解（图4）、通道利用率（图7）、专家利用率和路由稳定性（补充材料）进行了可视化或统计分析。
    *   **计算与部署效率分析**：比较了推理时间、内存占用、FLOPs和参数数量（表2）。
    *   **视觉质量比较**：提供了特定图像的视觉对比（图6）。
    *   **消融实验的充分性**：实验设计非常全面，从性能、成本、内在机制（梯度、容量分配）等多个角度验证了方法的有效性。控制了变量，确保了与基线方法对比的公平性（如对VBR基线使用相同的骨干网络和训练设置）。
*   **客观性与公平性**：结果表格清晰展示了BD-rate提升，并明确指出优劣。对VBR基线（QRAF, CondConv）进行了重新实现，并承认了其在特定骨干网络（LALIC/Bi-RWKV）上的特殊失败模式（LayerNorm削弱了QRAF的缩放信号），体现了严谨性。

#### 6. 论文的主要结论与发现
*   MixCompress（特别是MoD变体）在几乎所有基准数据集上，其BD-rate性能不仅匹配了独立优化的**单码率**基线，甚至**显著超越**了它们（例如，在CLIC测试集上比LALIC的BD-rate降低-2.28%）。
*   这证明了，通过稀疏结构特化（MoE/MoD）来隔离梯度冲突，相较于传统的密集参数调制（QRAF, CondConv），是一种更有效的VBR图像压缩方法。
*   MoD版本（混合深度专家）优于MoE版本（同质专家），表明动态缩放计算容量对适应不同码率需求至关重要。
*   MixCompress在获得性能提升的同时，大幅降低了部署和存储的开销（一个模型代替6个）。

#### 7. 优点：方法或实验设计上的亮点
*   **方法论亮点**：
    *   **首次**将稀疏门控MoE系统性地应用于层次化变换的学习图像压缩编码中的VBR问题。
    *   提出了**梯度冲突缓解**的明确动机和理论框架（公式6, 7），并有实证验证（图4），逻辑链完整。
    *   **组合创新**：MoE/MoD + CAT的设计并非简单堆砌，而是分别从**结构隔离**和**频域能量调制**两个互补角度解决了VBR的核心挑战。
    *   **推理零开销**：门控和CAT参数仅依赖`λe`，可预计算缓存，实际推理时几乎不增加计算量。
*   **实验设计亮点**：
    *   对VBR基线进行了公平的**重新实现**，并深入分析了其失败的**根本原因**（LayerNorm兼容性问题），增强了结论的说服力。
    *   提供了清晰的**资源与算力**描述。
    *   不仅报告了平均BD-rate，还通过**视觉对比**和**梯度分布分析**直观展示了方法的优越性。

#### 8. 不足与局限
*   **实验覆盖的局限**：虽然对比了多个基线，但主要限于基于CNN和Transformer的经典框架，未与最新的基于扩散模型的VBR方法或超大规模模型对比。
*   **算法复杂性**：虽然没有额外的推理开销，但MoE/MoD模型的**总参数量**（特别是MoD版本）显著大于单码率模型，总内存占用仍较高（表2），这对于资源严格受限的设备可能仍是负担。
*   **消融研究的深度**：虽然分析了梯度冲突，但在消融中未深入探讨不同`K`值（激活专家数）和`N`值（专家总数）对性能与专家利用率的具体影响，仅提及在补充材料中分析。
*   **特定骨干网络的依赖**：研究发现QRAF在LALIC（使用Bi-RWKV和LayerNorm）骨干上表现特别差，这表明所提出的方法（MoE等）可能对底层网络架构的特性有一定的依赖性，其泛化性在不同架构上的表现需要更多验证。
*   **额外的计算开销**：虽然路由无开销，但MoD中`多个嵌套专家`的设计意味着整个模型的计算图更复杂，训练时的计算量可能更大，文中未对比训练总耗时。

（完）
