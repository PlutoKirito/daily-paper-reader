---
title: Differentiable Proxy Learning for Adaptive Quantization Control in H.264 Video Coding
title_zh: 用于H.264视频编码中自适应量化控制的可微分代理学习
authors: "Qihan Xu, Ivan V. Bajić"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10478v1"
tags: ["query:train-trick"]
score: 9.0
evidence: H.264自适应量化控制的可微分代理学习
tldr: "H.264编码中量化参数优化困难，因编解码器不可微。本文提出可微分代理学习法，基于可变率学习模型和软索引机制构建H.264帧内编码的代理，使其对QP可微，并训练以逼近全局QP和空间QP的率失真行为。利用冻结代理实现自适应量化框架，在感知优化和机器视觉任务中优于固定QP基线，BD-rate降低最高17.12%（语义分割）和15.30%（MS-SSIM）。该方法实现了端到端梯度优化与标准编码器的结合，显著提升率任务权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 915, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1601, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 714, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 697, \"height\": 1054, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 785, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 684, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 689, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1415, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 645, \"height\": 639, \"label\": \"Figure\"}]"
motivation: H.264量化参数优化因编解码器不可微而困难，现有可微分代理缺乏对目标编解码器的保真度刻画。
method: 基于可变率学习模型构建H.264帧内编码的可微分代理，通过软索引机制使其对QP可微，并训练近似全局QP和空间QP的率失真行为。
result: "代理精确逼近H.264率失真行为；自适应量化框架在语义分割和MS-SSIM上分别实现17.12%和15.30%的BD-rate降低。"
conclusion: 可微分代理学习有效实现自适应量化控制，为编码优化提供新范式。
---

## 摘要
H.264凭借其相对简单、高效以及广泛可用的软硬件实现，在过去二十年中一直是最广泛使用的视频编码格式。然而，由于标准视频编解码器的不可微分性质，为特定目标（如感知质量或机器视觉任务）优化编解码器参数（如量化参数QP）具有挑战性。尽管最近已使用可微分代理实现围绕标准编解码器的梯度优化，但它们对目标编解码器的保真度很少被明确表征。在本文中，我们提出了一种用于H.264帧内编解码器的可微分代理学习方法，以实现自适应量化控制。该代理基于可变速率学习压缩模型，通过软索引机制使其相对于编解码器QP可微分。然后训练该代理近似H.264在两种量化设置下的率失真行为：全局QP（每幅图像使用一个QP）和空间QP（在宏块级别分配QP）。利用冻结的训练代理，我们开发了一个基于代理的自适应量化（AQ）框架，用于感知优化和机器视觉任务。实验结果表明，所提出的代理紧密近似了H.264帧内编解码器的率失真行为。由此产生的基于代理的AQ框架在固定QP的H.264基线基础上持续改善了率-任务权衡，在语义分割上实现了高达17.12%的BD率降低，在MS-SSIM上实现了15.30%的降低。

## Abstract
H.264 has been the most widely used video coding format for the past two decades due to its relative simplicity, efficiency, and wide availability of software and hardware implementations. However, optimizing codec parameters such as the quantization parameter (QP) for specific objectives (e.g., perceptual quality or machine vision tasks) is challenging due to the non-differentiable nature of standard video codecs. While differentiable proxies have recently been used to enable gradient-based optimization around standard codecs, their fidelity to the target codec is rarely explicitly characterized. In this paper, we propose a differentiable proxy learning method for H.264 intra codec to enable adaptive quantization control. Built upon a variable-rate learned compression model, the proposed proxy is made differentiable with respect to codec QP through a soft-indexing mechanism. It is then trained to approximate the rate-distortion behavior of H.264 under two quantization settings: global-QP, which uses one QP per image, and spatial-QP, which assigns QPs at the macroblock level. Using the frozen trained proxy, we develop a proxy-based adaptive quantization (AQ) framework for both perceptual optimization and machine vision tasks. Experimental results demonstrate that the proposed proxies closely approximate the rate-distortion behavior of H.264 intra codec. The resulting proxy-based AQ framework consistently improves rate-task trade-offs over fixed-QP H.264 baselines, achieving BD-rate reduction of up to 17.12% for semantic segmentation and 15.30% for MS-SSIM.

---

## 论文详细总结（自动生成）

### 论文总结：用于H.264视频编码中自适应量化控制的可微分代理学习

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：H.264是使用最广泛的视频编码标准，但量化参数（QP）的优化（如为感知质量或机器视觉任务）受到标准编解码器非微分性质的严重制约，无法端到端优化QP分配。现有可微分代理方法缺少对代理保真度的明确评估，难以确保代理在梯度优化中的可靠性。
- **整体含义**：本文旨在构建高保真的可微分代理，用于替代H.264帧内编解码器，从而实现基于梯度的自适应量化控制，在保持与H.264兼容的前提下提升率-任务（rate-task）权衡。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：利用可变速率学习压缩模型（DCVC-FM）作为基础，通过软索引机制将离散的QP映射为连续的量化缩放因子，使代理相对于QP可微分；训练代理精确拟合H.264在全局QP和空间QP两类设置下的率失真行为；冻结该代理后，将其作为可微分模拟器，训练自适应量化网络（AQ Network）以优化特定下游任务。
- **关键技术细节**：
  - **可变率模型自适应**：将DCVC-FM的离散质量索引替换为通过软赋值（soft indexing）得到的连续缩放因子。软赋值公式为：
    \[
    w_{u,v,i} = \frac{\exp(- (q_{u,v} - i)^2 / \tau)}{\sum_j \exp(- (q_{u,v} - j)^2 / \tau)}, \quad s_{u,v} = \sum_i w_{u,v,i} s_i
    \]
    其中 \( q_{u,v} \) 为代理控制参数，\( \tau \) 控制赋值锐度，\( s_i \) 为可学习缩放因子集合。
  - **QP映射**：通过最小化BPP-PSNR空间中的欧氏距离将H.264的QP值映射到代理索引，保证操作点对齐。
  - **代理训练**：联合优化率匹配和失真匹配损失：
    \[
    \mathcal{L}_p = D_R(R_c, R_p) + \lambda D_X(\hat{X}_c, \hat{X}_p)
    \]
    其中 \( D_R \) 为绝对差值，\( D_X \) 为MSE，\( \lambda \) 由代理控制参数计算。
  - **空间QP设置**：训练时从预定义的分布集合中采样宏块级QP图，且损失中的拉格朗日乘子 \( \bar{\lambda} \) 取所有宏块 \( \lambda(q_{u,v}) \) 的平均值。
  - **自适应量化（AQ）网络**：
    - 全局QP控制：轻量级CNN（3个带步长卷积层+全局平均池化+全连接层），输出单个QP。
    - 空间QP控制：全卷积编码器，输入图像和归一化的β图，通过β依赖缩放因子 \( s(\beta) = 1 + 0.5\tanh(w\beta + b) \) 调节QP logits，输出宏块级QP图。
  - **优化目标**：
    \[
    \phi^* = \arg\min_\phi \mathbb{E}[R_p + \beta \mathcal{L}_{\text{task}}(\hat{X}_p)]
    \]
    其中 \( \hat{X}_p, R_p \) 来自冻结代理，\( \mathcal{L}_{\text{task}} \) 为下游任务损失。

#### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：
  - 代理训练：Vimeo-90k（仅第一帧）。
  - 代理评估：Kodak数据集（率失真曲线）。
  - AQ网络训练与评估：
    - 目标检测：COCO（YOLOv5 S/M/L）。
    - 语义分割：Pascal VOC（DeepLabv3 ResNet-50）。
    - 感知优化：Vimeo-90k（训练），Kodak（测试MS-SSIM）。
- **基准（Benchmark）**：H.264帧内编解码器参考软件JM 19.0，固定QP基线（全局固定或均匀分布）。
- **对比方法**：
  - 全局QP设置：固定QP的H.264。
  - 空间QP设置：固定QP的H.264（全局固定QP或随机QP图）。
  - 未与其他可微分代理方法（如直接替换非可微模块的代理）对比，重点对比自身基线。

#### 4. 资源与算力
- **文中说明**：代理训练使用1块NVIDIA 2080 Ti GPU，batch size=4，学习率1×10⁻⁵，Adam优化器。未明确训练迭代次数和时长。
- **未明确说明**：AQ网络训练的具体GPU资源、推理时间、总计算量等细节。

#### 5. 实验数量与充分性
- **实验组数**：
  - **代理保真度评估**：全局QP和空间QP下各测试了多个操作点（QP值或分布），报告了BD-rate（PSNR和MS-SSIM）以及视觉对比。
  - **自适应量化实验**：
    - 全局QP：3种YOLOv5模型尺寸（S/M/L），各选取6个β值 → 共18条率-精度曲线；感知优化（Kodak）1组。
    - 空间QP：语义分割1组（Pascal VOC DeepLabv3）、感知优化1组（Vimeo-90k → Kodak）。
  - **消融实验**：未明确设计消融实验（如移除软索引、不同τ的影响、不同映射策略等），但通过全局QP vs 空间QP的对比间接体现了粒度的重要性。
- **充分性评估**：
  - 覆盖了两种主流下游任务（检测、分割）和感知指标，系统性强。
  - 对比组数足够表明方法优势，BD-rate降低显著（17.12%和15.30%）。
  - 缺点：未与近期其他可微分代理方法（如基于替换非可微模块的代理）做公平对比；未在更多编码器（如H.265/HEVC）或真实视频（帧间编码）上验证；缺少对代理与H.264之间的PSNR/SSIM差异的统计显著性检验；视觉对比仅展示少量示例。

#### 6. 论文的主要结论与发现
- 所提出的可微分代理能够紧密近似H.264帧内编解码器的率失真行为（全局QP下BD-rate -4.75% PSNR，空间QP下-15.01% PSNR）。
- 基于冻结代理的自适应量化框架在所有测试任务中均一致优于固定QP的H.264基线：
  - 目标检测（YOLOv5）：BD-rate节省8.19%~9.50%。
  - 语义分割（DeepLabv3）：空间QP节省17.12%（全局QP节省11.99%）。
  - 感知优化（MS-SSIM）：空间QP节省15.30%。
- 即使代理保真度不完全精确（空间QP代理偏差较大），仍能获得有效的任务优化，表明代理的近似能力足以支持梯度优化。
- 空间QP控制比全局QP控制带来额外的性能增益，表明细粒度比特分配的重要性。

#### 7. 优点
- **方法创新性**：首次在H.264上实现同时支持全局QP和空间QP的可微分代理，且明确评估了保真度。
- **软索引设计**：使QC索引连续可微，同时保留离散缩放因子的表达能力，简洁有效。
- **统一框架**：代理训练后可直接用于多种下游任务（感知、检测、分割），无需重复训练代码。
- **实践价值**：无需修改H.264标准，即可实现端到端优化的自适应量化，易于集成到现有编码管道。
- **实验充分性**：涵盖多个任务、模型尺寸、量化设置，结果具有说服力。

#### 8. 不足与局限
- **代理保真度限制**：空间QP代理的BD-rate偏差较大（-15.01%），在极端低码率下视觉平滑，可能丢失压缩伪影细节，影响下游任务对伪影的适应性。
- **实验覆盖**：
  - 仅使用帧内编码，未扩展到帧间编码（论文也指出为未来方向）。
  - 仅测试了H.264，未验证框架是否可迁移到HEVC、VVC等更先进编码器。
  - 未与已有的可微分代理方法（如直接替代非可微操作）进行定量比较。
- **消融分析不足**：未提供软索引中τ的影响、QP映射策略的权衡、λ计算中α的选择依据等消融实验。
- **训练数据单一**：代理训练只用Vimeo-90k，可能影响对真实世界多样图像的泛化能力（虽然测试用了Kodak）。
- **计算开销**：代理训练和AQ网络训练需要额外成本，推理时需先运行一次网络再调用标准编码器，实时性未评估。
- **公开可重复性**：未提供代码，文献引用部分版本可能无法完全复现。

（完）
