---
title: "Hybrid Compression: Integrating Pruning and Quantization for Optimized Neural Networks"
title_zh: 混合压缩：融合剪枝与量化以优化神经网络
authors: "Minh-Loi Nguyen, Long-Bao Nguyen, Van-Hieu Huynh, Minh-Triet Tran, Trung-Nghia Le"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22935v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 模型剪枝与量化压缩，适用于轻量级视频编解码器
tldr: 深度神经网络在嵌入式和边缘设备上面临内存和计算资源限制，部署困难。本文提出一种两阶段混合压缩方法，先通过剪枝和量化大幅减小模型体积，再引入混合专家路由技术动态组合多个压缩模型，在保持推理效率的同时提升性能。在多个基准数据集上对CNN模型进行实验，实现了FLOPs和参数数量的显著降低，而精度损失可忽略。该方法有效地平衡了模型大小与性能，为资源受限场景下的模型部署提供了实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1003, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1240, \"height\": 793, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1262, \"height\": 316, \"label\": \"Table\"}]"
motivation: 克服深度神经网络在资源受限设备上的部署瓶颈，需在保持性能的前提下大幅压缩模型体积。
method: 先对模型进行剪枝和量化以压缩大小，再使用混合专家架构路由多个压缩模型来提升性能。
result: 在多个基准数据集上，CNN模型的FLOPs和参数量显著减少，准确率几乎无下降。
conclusion: 提出的混合压缩方法有效平衡了模型压缩率和性能，适用于资源受限环境。
---

## 摘要
深度神经网络近年来取得了显著进展，并已成为各种应用中不可或缺的一部分。然而，伴随这些发展，在嵌入式设备和边缘设备上训练和部署神经网络模型面临内存和计算资源有限的重大挑战。这些问题可以通过深度神经网络压缩来解决，这需要在模型大小和性能之间进行权衡。本文提出了一种通过两个阶段进行模型压缩的新方法。首先，我们利用模型压缩技术（如剪枝和量化）显著减小模型大小。然后，我们使用混合专家（MoE）路由先前压缩的模型，以增强性能，同时保持推理效率的平衡。MoE由多个中等大小且性能稳定的专家模型（即压缩模型）组成。在多个基准数据集上的实验结果表明，我们的方法成功压缩了CNN模型，在FLOPs和参数方面实现了大幅减少，而准确率下降可忽略不计。

## Abstract
Deep neural networks have witnessed remarkable advancements in recent years and have become integral to various applications. However, alongside these developments, training and deployment of neural network models on embedding and edge devices face significant challenges due to limited memory and computational resources. These problems can be addressed with deep neural network compression, which involves a trade-off between model size and performance. In this paper, we propose a novel method for model compression through two phases. First, we utilize model compression techniques, such as pruning and quantization, to significantly reduce the model size. Then, we use Mixture of Experts to route the previously compressed models to enhance performance while maintaining a balance in inference efficiency. MoEs consist of multiple expert models (i.e., compressed models) that are moderately sized and deliver stable performance. Experimental results on several benchmark datasets show that our method successfully compresses CNN models which achieves substantial reductions in FLOPs and parameters with a negligible accuracy drop.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：深度神经网络（DNN）在嵌入式设备和边缘设备上面临内存和计算资源严重受限的问题，难以直接部署复杂模型（如 CNN）。传统模型压缩方法（剪枝、量化）虽然能减小模型尺寸，但会导致性能下降，且缺乏有效的性能恢复机制。
- **整体含义**：本文提出一种两阶段混合压缩方法，**先通过剪枝和量化大幅压缩模型，再引入混合专家（MoE）架构动态路由多个压缩模型**，旨在在保持推理效率的同时恢复甚至提升压缩后的性能，从而为资源受限场景提供可行的部署方案。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将硬压缩（剪枝+量化）与软集成（MoE）结合，利用 MoE 的稀疏激活机制在多个压缩“专家”间动态分配输入，以低计算开销换取性能提升。
- **关键技术细节**：
  - **剪枝阶段**：
    - 采用 **自动渐进剪枝**（AGP），使用立方函数逐步增加稀疏度，避免一次性高稀疏导致层坍塌。公式：
      \( s_t = s_f + (s_i - s_f) \left(1 - \frac{t - t_0}{n \Delta t}\right)^3 \)。
    - 应用 **幅度剪枝** 全局移除不重要连接，并在每个训练步骤后通过再训练恢复。
    - 使用 **Model Speedup** 移除被剪枝的特征图，实现实际推理加速（而非仅掩码）。
  - **量化阶段**：
    - 采用 **量化感知训练**（QAT），通过“伪量化”模拟低比特精度（如 8-bit）下的前向传播，同时保持梯度反向传播（使用直通估计器 STE）。
    - 定标和零点通过校准数据初始化，损失函数加入量化损失项：\( L_{total} = L_{task} + \lambda L_{quant} \)。
  - **MoE 集成阶段**：
    - 将多个经过剪枝+量化+微调的压缩模型作为专家，配合 **Top-k 带噪门控**（Top-k Noisy Gating）路由函数，在门控 logits 中加入高斯噪声以促进专家平衡，避免专家坍塌。
    - 最终输出为 Top-k 专家输出加权和。

## 3. 实验设计
- **数据集与场景**：
  - **CIFAR-10**：图像分类（10 类，60,000 张 32×32 彩色图像）。
  - **BloodMNIST**：医学图像分类（8 类，17,092 张正常细胞图像）。
- **Benchmark 与基线**：
  - 原始未压缩的 CNN 模型：VGG16、ResNet18、InceptionV3、DenseNet121。
  - 对比指标：参数量、FLOPs、推理时间（秒）、准确率。
- **对比方法**：主要对比的是原始模型与压缩后模型的性能，以及**最终 MoE 集成后的整体准确率**。未与其他压缩方法（如单独剪枝、单独量化或现有压缩框架）进行横向对比。

## 4. 资源与算力
- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练时长等硬件细节。仅提到使用 PyTorch 和 NNI 框架实现。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验（表 1）在 **2 个数据集**上测试了 **4 种 CNN 架构**，共 **8 组压缩对比**（每模型原始 vs 压缩），外加 MoE 集成结果（1 组）。
  - 未提供消融实验（如单独剪枝、单独量化、不同剪枝率/量化位宽的影响）。
  - 未提供 MoE 内部专家数量、Top-k 值、门控参数等超参数分析。
- **充分性与公平性**：
  - 实验覆盖了经典 CNN 家族（VGG、ResNet、Inception、DenseNet），具有一定代表性。
  - 但缺少与已有压缩方法（如仅剪枝、仅量化、蒸馏等）的定量比较，且未报告推理速度的真实加速比（如延迟对比仅在绝对时间上给出，未归一化）。
  - MoE 集成后仅报告准确率，未报告其 FLOPs、参数量、推理时间，导致无法评估效率-性能折中。
  - 实验结论存在一定偏差：部分模型（InceptionV3、DenseNet121）压缩后准确率反而提升，作者解释为“压缩可能去除噪声”，但未提供理论或消融支持。

## 6. 主要结论与发现
- 所提混合压缩方法能在 **10.5×~11× FLOPs 压缩**、**10.5× 参数减少**的同时，使准确率下降可忽略（0.5%–4.5%），甚至在某些模型上略有提升。
- 引入 MoE 后，整体准确率（CIFAR-10：92.1%，BloodMNIST：96.9%）超越了所有单个压缩模型，接近甚至超过原始模型水平。
- 验证了“硬压缩 + MoE 软集成”在资源受限场景下的可行性与有效性。

## 7. 优点
1. **方法新颖性**：将剪枝、量化与 MoE 有机融合，利用稀疏门控激活多个压缩模型，既保留了硬压缩的低资源消耗，又通过专家多样性恢复了性能。
2. **实际部署价值**：显著降低 FLOPs 和参数量，且 MoE 的稀疏路由（Top-k）进一步控制推理成本，适合 IoT、移动端等环境。
3. **自动化实现**：借助 NNI 框架自动执行剪枝与量化流程，降低了方法复现门槛。

## 8. 不足与局限
1. **实验覆盖不足**：仅使用图像分类（两个数据集），未验证在目标检测、语义分割等其他视觉任务或 NLP 领域的泛化性。
2. **对比不充分**：缺乏与已有压缩方法（如结构化剪枝、知识蒸馏、低秩分解等）的横向比较，无法突出本方法相对于纯剪枝或纯量化的优势。
3. **MoE 评估不完整**：MoE 集成后的计算开销（FLOPs、参数量、延迟）未报告，无法判断其是否真正保持了“高效率”。
4. **超参数敏感性未分析**：未讨论剪枝稀疏度、量化位宽、专家数量、Top-k 值等的影响，缺乏消融研究。
5. **可复现性问题**：未提供代码或详细的训练配置（学习率、轮数、优化器等），且无随机种子设定，结果可能不稳定。
6. **性能提升存在疑点**：部分模型压缩后准确率反而提升，可能由于初始模型未完全收敛或数据集较小，作者未给出充分解释。

（完）
