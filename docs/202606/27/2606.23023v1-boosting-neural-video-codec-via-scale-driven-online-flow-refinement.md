---
title: Boosting Neural Video Codec via Scale-Driven Online Flow Refinement
title_zh: 通过尺度驱动的在线光流细化提升神经视频编解码器
authors: "Tiange Zhang, Rongqun Lin, Haocheng Tang, Xiandong Meng, Weijia Jiang, Zhimeng Huang, Siwei Ma"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23023v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 尺度驱动的在线流精炼用于神经视频编解码运动补偿
tldr: "现有神经视频编码器在处理复杂运动时泛化性差，且在线微调代价高昂。本文提出无训练的自适应流优化模块SOFR，通过粗-细尺度动态融合运动信息并根据扭曲精度修正估计误差，同时引入率感知策略和可靠性检查。在USTC-TD数据集上，SOFR为DCVC-FM平均节省2.84% PSNR和4.05% MS-SSIM码率，计算开销极小，代码已开源。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23023v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1804, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23023v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 727, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23023v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1755, \"height\": 626, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23023v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23023v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1598, \"height\": 379, \"label\": \"Table\"}]"
motivation: 现有神经视频编码器对未见过的复杂运动模式泛化能力有限，且在线微调代价高，需要一种轻量级、无需训练的修正方法。
method: 提出Training-Free Scale-Driven Online Flow Refinement (SOFR)，利用粗-细尺度的运动信息动态融合，根据扭曲精度修正运动估计误差，并设计率感知策略和基于扭曲误差的可靠性检查。
result: "在USTC-TD数据集上，SOFR为DCVC-FM模型分别带来2.84%（PSNR）和4.05%（MS-SSIM）的平均码率节省，编码时间增加可忽略。"
conclusion: SOFR作为即插即用模块，可显著提升多种神经视频编码器的压缩性能，且无需重新训练，具有良好的泛化性和实用性。
---

## 摘要
尽管最先进的神经视频编解码器（NVC）已取得显著性能，但在处理训练中未见过的复杂运动模式时，其泛化能力有限。为弥补这一领域差距且避免在线微调的高昂成本，我们提出了一种无需训练的尺度驱动在线光流细化（SOFR）方法。作为即插即用模块，SOFR整合了粗尺度和细尺度的运动信息，并根据扭曲精度动态融合这些信息，以可忽略的计算开销有效修正运动估计误差。此外，我们设计了一种率感知策略，根据比特率模式选择不同的动态融合策略，并采用基于扭曲误差的可靠性检查来确保鲁棒性。在USTC-TD数据集上的大量实验验证了SOFR在多种NVC框架（包括DCVC-SDD、DCVC-FM和EHVC）中的有效性和泛化能力。值得注意的是，它在DCVC-FM上分别带来了平均2.84%和4.05%的PSNR和MS-SSIM比特率节省，且编码时间增加可忽略不计。我们的代码可在https://github.com/SunnyMass/SOFR获取。

## Abstract
Although state-of-the-art neural video codecs (NVCs) have achieved remarkable performance, they suffer from limited generalization when encountering complex motion patterns unseen during training. To bridge this domain gap without the expensive cost of online fine-tuning, we propose a Training-Free Scale-Driven Online Flow Refinement (SOFR) method. Serving as a plug-and-play module, SOFR integrates motion information from coarse and fine scales and dynamically fuses them according to warping accuracy, effectively rectifying motion estimation errors with negligible computational overhead. Furthermore, we design a rate-aware strategy that selects different dynamic fusion strategies according to bitrate modes, and employs a reliability check based on warping error to ensure robustness. Extensive experiments on the USTC-TD dataset verify the effectiveness and generalization of SOFR across various NVC frameworks, including DCVC-SDD, DCVC-FM, and EHVC. Notably, it brings an average of 2.84% and 4.05% bitrate savings in terms of PSNR and MS-SSIM, respectively, to DCVC-FM with negligible coding time increase. Our code is available at https://github.com/SunnyMass/SOFR.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：现有神经视频编解码器（NVC）在训练时未见过复杂运动模式时泛化能力差，运动估计不准确导致压缩效率下降。传统的在线微调方法（更新网络参数）计算开销大、延迟高，不适合实际部署。
- **整体含义**：本文旨在以极低的计算代价（无需训练、即插即用）在线修正运动估计误差，提升NVC在域外场景下的率失真性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用粗、细两种尺度的光流互补性，动态融合生成更精确的运动场。粗尺度提供结构稳定性，细尺度保留局部分辨细节。
- **关键技术细节**（SOFR模块，训练无关）：
  - **双粒度光流生成**：
    - 细粒度流 \( \mathbf{v}_{\text{fine}} \)：在原分辨率下用预训练FlowNet计算。
    - 粗粒度流 \( \mathbf{v}_{\text{coarse}} \)：先将帧下采样（尺度 \( s \) 由率感知模块决定），计算光流后上采样回去并缩放幅度。
  - **率感知控制**：
    - 根据量化参数（Q-Index）划分高/低比特率模式：
      - 高比特率：\( s=2 \)，偏置 \( b=0.005 \)，倾向保留细节。
      - 低比特率：\( s=4 \)，偏置 \( b=0.03 \)，倾向使用平滑粗流以节省运动向量比特。
  - **可靠性检查**：计算两种候选流的全局平均绝对扭曲误差 \( \bar{E}_{\text{fine}} \) 和 \( \bar{E}_{\text{coarse}} \)，若粗流误差更大则退化为细流，避免劣化。
  - **软融合掩码**：基于像素级扭曲误差差值和偏置 \( b \) 生成Sigmoid掩码 \( M \)，最终输出 \( \mathbf{v}_t = M \odot \mathbf{v}_{\text{fine}} + (1-M) \odot \tilde{\mathbf{v}}_{\text{coarse}} \)。
- **流程**：作为即插即用模块插入NVC的运动估计分支，推理时仅一次前向计算，无需反向传播。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：USTC-TD（2020年代高质量基准），包含10个1920×1080序列（Badminton, BasketDrill, BasketPass, BicycleDriving, Dancing, FourPeople, ParkWalking, Running, ShakeHands, Snooker），运动模式多样。全部转换为RGB（BT.709），前96帧测试。
- **基准**：三个SOTA NVC框架：
  - DCVC-SDD（IP=32）
  - DCVC-FM（IP=-1）
  - EHVC（IP=-1）
- **对比方法**：将SOFR集成到每个基线中，比较原始基线和加SOFR后的BD-Rate（PSNR和MS-SSIM）。无其他对比方法（如在线微调方法），因为本文主打训练无关且低计算开销。

## 4. 资源与算力

- **GPU**：NVIDIA A100（40GB）用于评估。
- **训练**：本文方法无需训练，因此没有训练时长。
- **推理开销**：SOFR使编码时间增加约2.4%（1.0240×），解码时间不变。文中明确指出“negligible computational overhead”。

## 5. 实验数量与充分性

- **主要实验**（表1）：在三个NVC框架、10个序列上报告BD-Rate（PSNR和MS-SSIM），共3×10=30个数据点，展示了方法一致有效。
- **消融实验**（表2）：使用DCVC-FM为锚点，设置了五个变体：
  - 固定尺度 \( s=2,4,8 \) 对比动态尺度
  - 是否带率感知控制
  - 是否带可靠性检查
- **视觉对比**（图3）：三个框架各选一帧进行重建质量和光流可视化。
- **充分性与公平性**：实验覆盖了主流NVC架构（基于条件编码的DCVC系列和基于分层参考的EHVC），序列运动类型多样，消融完整，公平性较好。但未与在线微调方法或传统代码标准作直接性能比较，也未在多个不同数据集上验证泛化。

## 6. 主要结论与发现

- SOFR作为无训练模块，能显著提升多种NVC的压缩效率。
- 在DCVC-FM上平均节省PSNR BD-Rate 2.84%，MS-SSIM BD-Rate 4.05%，复杂运动序列（如BasketDrill）节省可达9.65%/13.31%。
- 计算开销极小（编码时间仅增加~2.4%），适合实际部署。
- 双粒度融合必须结合动态尺度选择和可靠性检查，否则固定过大下采样（如s=8）反而导致性能下降（BD-Rate +30%）。

## 7. 优点

- **训练无关**：无需任何参数更新或微调，直接利用预训练光流网络，即插即用。
- **自适应强**：率感知策略根据比特率自动调节尺度与融合偏好，在不同码率下保持鲁棒。
- **可靠性检查**：避免粗流在简单场景中引入噪声，属于细粒度保护机制。
- **低计算开销**：前向设计，无迭代优化，实际编码时间几乎不变。
- **通用性**：验证在三种不同SOTA NVC框架上均有效，说明方法具有框架无关性。

## 8. 不足与局限

- **实验覆盖偏少**：
  - 仅使用USTC-TD一个测试集，未在标准测试集（如UVG, MCL-JCV, HEVC-Class B等）上验证。
  - 未与其他在线适应方法（如参数微调、适配器模块）进行性能或计算量对比，仅以自身基线为基准。
- **率感知策略手工设计**：下采样尺度 \( s \) 和偏置 \( b \) 根据Q-Index分为两个固定值（2或4），可能不是最优；缺乏自适应学习机制。
- **融合掩码的锐度因子 α 未说明如何设定**：文中仅写“α is a sharpness factor”，未分析其影响。
- **可靠性检查基于全局MAD**：全局平均可能无法捕捉局部区域差异，更细致的区域级或像素级门控可能更好。
- **未讨论对传统编码器（如VVC）的适用性**：仅针对NVC，但方法思想可迁移。
- **潜在偏差风险**：所有实验在相同数据集上报告，可能方法对USTC-TD的分布有偏向；需要更多域外数据验证泛化。

（完）
