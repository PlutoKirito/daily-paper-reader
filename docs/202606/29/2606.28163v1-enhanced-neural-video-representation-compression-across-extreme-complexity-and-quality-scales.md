---
title: Enhanced Neural Video Representation Compression across Extreme Complexity and Quality Scales
title_zh: 跨极端复杂度和质量尺度的增强型神经视频表示压缩
authors: "Ho Man Kwan, Tianhao Peng, Fan Zhang, Mike Nilsson, Andrew Gower, David Bull"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28163v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 基于INR的可扩展神经视频压缩
tldr: 现有神经视频编码器难以平衡复杂度与可扩展性，轻量模型在变码率下性能下降，高性能模型复杂度随质量增加。为此提出NVRC++，基于轻量隐式神经表示与多分辨率特征网格，通过高效过拟合优化和高级熵模型，实现四种复杂度级别（7k-360k MACs/像素），覆盖宽码率范围，支持实时解码。解码速度比SOTA的NVRC快7.6倍且性能相当，解决了复杂度和可扩展性的矛盾。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28163v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1227, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1251, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 387, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28163v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 556, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 646, \"height\": 304, \"label\": \"Table\"}]"
motivation: 现有INR视频编码器无法在保持复杂度一致的前提下支持宽码率范围，限制了实际部署。
method: 采用轻量INR与多高分辨率特征网格，结合高效优化框架和高级熵模型压缩网格参数。
result: 提供四种复杂度级别，解码速度相比SOTA提升7.6倍，性能相当。
conclusion: NVRC++实现了复杂度和可扩展性的统一，适合多样化场景部署。
---

## 摘要
隐式神经表示（INRs）最近成为视频压缩的一种有前途的方法，在快速解码的同时提供了有竞争力的率失真性能。然而，现有的神经视频编解码器难以平衡复杂度和可扩展性。轻量级模型在扩展到不同比特率/质量水平时，压缩性能往往会下降，而高性能模型的可扩展性有限，因为其模型复杂度通常随着质量提高而增加。缺乏能够在广泛比特率范围内保持一致复杂度的统一架构，严重限制了它们在现实世界中的多样化部署。为了解决这些挑战，我们引入了NVRC++，一种基于INR的新型视频编解码器，它利用具有多个高分辨率特征网格的轻量级INR，在任意给定的复杂度水平上提供高可扩展性。这结合了一个优化框架，能够对长视频序列的高分辨率网格进行高效过拟合，从而利用时空冗余，而无需过高的计算或内存开销。此外，设计了一种先进的熵模型，用于有效压缩高维网格参数。因此，NVRC++提供了四个复杂度水平（从7kMACs/像素到360kMACs/像素），每个水平覆盖广泛的比特率和质量范围，同时支持实时解码。实验结果表明，与最先进的基于INR的视频编解码器NVRC相比，NVRC++提供了更快的解码速度（最高7.6倍），同时提供了相当的性能。

## Abstract
Implicit neural representations (INRs) have recently emerged as a promising approach to video compression, delivering competitive rate-distortion performance alongside rapid decoding. However, existing neural video codecs struggle to balance complexity and scalability. Lightweight models often suffer from degraded compression performance when scaled to different bitrate/quality levels, whereas high-performance models exhibit limited scalability, as their model complexity typically increases with quality. This lack of a unified architecture capable of maintaining consistent complexity across a wide range of bitrates severely limits their diverse real-world deployment. To address these challenges, we introduce NVRC++, a novel INR-based video codec that utilizes a lightweight INR with multiple high-resolution feature grids, providing high scalability at any given complexity level. This is paired with an optimization framework that enables efficient overfitting on high-resolution grids for long video sequences, thereby exploiting spatio-temporal redundancies without prohibitive computational or memory overhead. Additionally, an advanced entropy model is designed for efficiently compressing the high-dimensional grid parameters. As a result, NVRC++ provides four complexity levels (from 7kMACs/pixel to 360kMACs/pixel), each spanning wide bitrate and quality ranges while supporting real-time decoding. The experimental results show that NVRC++ offers a much faster decoding speed (up to 7.6x) compared to the SOTA INR-based video codec, NVRC, while delivering comparable performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：隐式神经表示（INR）在视频压缩中展现出有竞争力的率失真性能和快速解码能力，但现有INR视频编解码器存在两个关键矛盾：轻量模型在扩展到不同比特率/质量水平时压缩性能大幅下降；高性能模型（如NeRV、NVRC）的模型复杂度随目标质量增加而线性增长，缺乏可扩展性。这种“复杂度与可扩展性不可兼得”的现状限制了INR编解码器在实际应用中的部署。
- **整体含义**：本文旨在设计一个统一的INR框架，能够在保持固定解码复杂度的前提下，覆盖从低码率到高码率的宽质量范围，从而适应多样化的硬件和业务场景。

### 2. 论文提出的方法论

- **核心思想**：采用轻量INR（HiNeRV++）结合多个高分辨率特征网格，将表示负担从网络层转移到网格参数上，从而解耦模型复杂度与重建质量；通过精心设计的优化框架和熵模型，使高维网格的压缩和训练可行。
- **关键技术细节**：
  - **HiNeRV++ 网络**：比HiNeRV使用更高分辨率的特征网格，并在前三个网络阶段分别注入多分辨率网格特征；采用**多参考输入**（multiple reference inputs）——不仅使用插值特征，还直接使用邻近的原始网格切片作为解码器输入；移除最高层级的ConvNeXt块，改用Pixel Shuffle层作为输出，降低计算量。
  - **In-parameter Coding Structure**：将特征网格参数按时间切片（temporal slices），采用**分层B帧结构**进行依赖建模，类似于传统视频编码的预测结构。但实际优化中，去除了依赖前帧的量化偏移（使用非条件量化），并采用**随机采样**（random sampling）进行率失真优化，使得仅需处理部分参数即可计算梯度，从而在24GB内存的消费级GPU上处理600帧1080p视频的长序列过拟合。
  - **Feature Grid Masking**：在训练初期，对高分辨率网格特征施加随机掩码（类似Dropout），迫使模型先利用低分辨率网格捕捉全局冗余，后期逐步解除掩码，从而改善低码率下的压缩性能。
  - **多维度网格熵模型**：利用**尺度先验**（低分辨率网格作为高分辨率网格的条件）、**时间先验**（已编码的参考切片）和**空间先验**（多步掩码的上下文模型）来压缩网格参数；使用1-4步编码，兼顾性能和速度。
- **优化过程**：端到端联合优化网格参数、层参数和熵模型参数，遵循率失真损失 \( \arg\min L = R(\hat{\theta}) + \lambda D(V, \hat{V}) \)。

### 3. 实验设计

- **数据集**：UVG（7个序列，300-600帧，1920×1080），MCL-JCV（30个序列，120-150帧，1920×1080）。
- **基准方法**：
  - 传统编码器：x265 (veryslow)、HM-18.0 (random access)、VTM-20.0 (random access)
  - 自编码器神经编码器：DCVC-FM、DCVC-RT
  - INR-based 编码器：HiNeRV、NVRC、C3
- **评价指标**：PSNR、MS-SSIM、BD-rate（相对x265），解码复杂度（kMACs/像素）、解码速度（FPS）。
- **可比性**：NVRC/NVRC++的FPS在NVIDIA RTX 4090上测量，其他方法在不同平台（如A100）上测量，但作者已指出平台差异；编码时间单独报告。

### 4. 资源与算力

- 文中未给出完整的训练资源细节（如GPU数量、总训练时长），仅提及：最大模型（S4）包含超过1亿网格参数，可在24GB显存的消费级GPU上对600帧1080p视频进行编码。
- 解码测试平台为NVIDIA RTX 4090（表1脚注）。
- 训练过程基于NVRC和HiNeRV的框架，但具体超参数和训练策略未在正文中详述（可能见补充材料），因此无法精确评估训练成本。

### 5. 实验数量与充分性

- **主要实验**：表1给出四个复杂度等级（S1-S4）与所有基准方法的BD-rate对比；图4展示RD曲线。
- **消融实验**：表2包含7个变体（低分辨率网格、无掩码、无空间先验、无时间先验、无尺度先验、2D stem、3D stem），验证各组件贡献。
- **额外分析**：表3比较HiNeRV与HiNeRV++的参数量、复杂度、收敛速度和最终PSNR；图4右对比NVRC++与NVRC的可扩展性。
- **充分性**：实验覆盖两个主流全高清数据集，对比方法全面（传统、神经、多个INR基线），消融实验完整。但是，仅测试了1080p分辨率，缺少4K、低分辨率或不同帧率的评估；且未与最新发表的基于Transformer的神经编码器（如ECVC、BIC等）对比（论文发表于2026年，可能已有更新方法）。总体而言，实验设计客观，在现有条件下具备说服力。

### 6. 论文的主要结论与发现

- NVRC++提供了四个复杂度等级（S1: 7.3kMACs/px, S2: 25.1k, S3: 92.5k, S4: 357.7k），每个等级均可覆盖宽码率范围，且解码复杂度固定。
- 在UVG数据集上，S3/S4达到与SOTA INR编码器NVRC相当的性能，但解码速度提升最高7.6倍（S3 vs NVRC）；S2性能优于HM，接近DCVC-RT；S1以极低复杂度显著超越x265和C3。
- 解码速度达到实时（S3为73.4 FPS），编解码速度-性能权衡优于所有对比方法。
- 消融实验证实了高分辨率网格、掩码策略、多维度先验和多参考输入的各自贡献。

### 7. 优点

- **高可扩展性**：首次实现使用单一模型配置即可覆盖从低到高码率，且复杂度恒定，极大便利了软硬件实现。
- **优化技巧创新**：in-parameter coding structure + 非条件量化 + 随机采样，使得长序列的高维网格过拟合在有限内存中可行。
- **高效熵模型**：结合尺度、时间、空间多维度先验，不仅提升压缩效率，且编码/解码速度较快（多步编码最多4步）。
- **轻量解码器**：HiNeRV++通过移除高分辨率层、使用Pixel Shuffle和多参考输入，在保持质量的同时大幅降低计算量和提高解码速度。
- **实验全面**：对比了传统和神经方法的多个SOTA，消融实验覆盖了主要设计选择。

### 8. 不足与局限

- **数据集局限**：仅评估两个1080p数据集，未测试4K、低分辨率或不同帧率/动态范围的视频，泛化性需更多验证。
- **训练成本不透明**：未提供详细的训练时间、GPU数量或能耗，对于实际部署的能效评估不够充分。
- **编码时间较长**：NVRC++仍采用instance overfitting方式，编码阶段需要较长时间训练（虽然解码快），文中未与其他方法的编码时间做公平对比。
- **与最新方法对比不足**：论文发表于2026年，但对比的自编码器方法（DCVC-FM、DCVC-RT）可能已被较新工作超越；且未包含近年来基于Transformer的轻量自编码器（如ECVC、BIC）的比较。
- **解码速度测量平台不一致**：不同方法的FPS在不同GPU上测得，虽然作者已说明，但仍然影响直接比较的公平性。
- **未分析运动剧烈场景的鲁棒性**：缺少对高动态、快速运动视频的专门分析。
- **潜在偏差**：消融实验仅在S2复杂度上进行，未在S1/S3/S4上重复验证，可能无法完全反映各组件在不同复杂度下的贡献。

（完）
