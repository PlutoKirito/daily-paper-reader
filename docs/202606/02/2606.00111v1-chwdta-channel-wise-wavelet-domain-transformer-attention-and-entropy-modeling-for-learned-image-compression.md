---
title: "ChWDTA: Channel-wise Wavelet-Domain Transformer Attention and Entropy Modeling for Learned Image Compression"
title_zh: "ChWDTA: 通道级小波域Transformer注意力与熵模型用于学习图像压缩"
authors: "Haisheng Fu, Runyu Yang, Feng Ding, Siyu Zhu, Jie Liang, Xiaoxiao Li, Zhenman Fang, Jingning Han"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2606.00111v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 通道级小波域注意力与熵建模的端到端图像压缩方法
tldr: "现有基于CNN-Transformer混合架构的端到端图像压缩方法在率失真性能上仍有提升空间。本文提出通道级小波域Transformer注意力（ChWDTA），在保持窗口空间自注意力的同时，对通道级小波变换后的特征计算Q/K/V投影，再用逆变换恢复，从而稀疏化通道协方差。在熵编码阶段引入通道级小波包分解，生成四个相等子带，适配通道切片自回归熵模型。在Kodak、CLIC和Tecnick数据集上获得BD-rate降低17.82%-22.56%。结果表明小波变换在混合架构中的优势。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00111v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1792, \"height\": 859, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 699, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1831, \"height\": 882, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1851, \"height\": 803, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1857, \"height\": 796, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1884, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00111v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1873, \"height\": 484, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00111v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 508, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00111v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 764, \"height\": 160, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00111v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 869, \"height\": 127, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00111v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 765, \"height\": 129, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00111v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 896, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00111v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 864, \"height\": 128, \"label\": \"Table\"}]"
motivation: 现有混合CNN-Transformer压缩方法中，通道协方差冗余限制了注意力机制的效率，且熵编码子带划分不均衡。
method: 提出通道级小波域Transformer块，在窗口注意力中对小波变换后的特征做Q/K/V投影；并用通道级小波包分解为四个等大子带进行切片熵编码。
result: "在Kodak、CLIC和Tecnick集上BD-rate分别降低17.82%、19.15%、22.56%；单切片配置仍保持大部分增益。"
conclusion: 将小波变换引入CNN-Transformer混合压缩架构可显著提升率失真性能，且可通过牺牲少量增益简化熵编码。
---

## 摘要
最先进的学习图像压缩（LIC）方案越来越多地基于混合CNN-Transformer架构。为了进一步提高率失真性能，我们将通道级小波变换引入到Transformer和熵编码组件中。首先，我们提出了一种通道级小波域Transformer注意力（ChWDTA）机制。ChWDTA保留了现代LIC骨干网络中使用的有效窗口化空间自注意力，但在通过逆变换将注意力输出映射回来之前，在通道级小波变换的特征上计算Q/K/V投影。由此产生的通道级小波域Transformer块（ChWDTB）因此保留了窗口化注意力的空间标记化模式，同时稀疏了注意力投影所见的通道协方差。其次，在熵编码阶段，我们引入了一种通道级小波包（ChWP）分解，产生四个大小相等的子带，这更适合基于通道级切片的自回归熵建模。当每个通道级子带被分成两个切片时，我们使用八个切片进行熵编码。通过这种配置，所提出的方案在Kodak、CLIC专业验证和Tecnick测试集上分别获得了-17.82%、-19.15%和-22.56%的BD-rate降低。即使每个通道级子带被编码为单个切片，该方案仍保留了大部分编码增益，且复杂度较低。结果证实了在基于CNN-Transformer的LIC方案中引入小波变换的优势。

## Abstract
State-of-the-art learned image compression (LIC) schemes are increasingly based on hybrid CNN-transformer architectures. To further improve rate-distortion performance, we introduce channel-wise wavelet transforms into both the transformer and entropy-coding components. First, we propose a channel-wise wavelet-domain transformer attention (ChWDTA) mechanism. ChWDTA keeps the efficient windowed spatial self-attention used in modern LIC backbones, but computes the Q/K/V projections on channel-wise wavelet-transformed features before mapping the attention output back with the inverse transform. The resulting Channel-wise Wavelet-Domain Transformer Block (ChWDTB) therefore preserves the spatial tokenization pattern of windowed attention while sparsifying the channel covariance seen by the attention projections. Second, in the entropy-coding stage, we introduce a channel-wise wavelet packet (ChWP) decomposition that produces four equal-sized subbands, which better fit channel-wise slice-based autoregressive entropy modeling. When each channel-wise subband is divided into two slices, we use eight slices for entropy coding. With this configuration, the proposed scheme obtains BD-rate reductions of -17.82%, -19.15%, and -22.56% on the Kodak, CLIC Professional Validation, and Tecnick test sets, respectively. Even when each channel-wise subband is coded as a single slice, the scheme still retains most of the coding gains with lower complexity. The results confirm the advantage of introducing wavelet transform in CNN-transformer-based LIC schemes.

---

## 论文详细总结（自动生成）

# ChWDTA论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：  
现有基于CNN-Transformer混合架构的端到端学习图像压缩（LIC）方法在率失真性能上仍有提升空间。尽管此类方法已超越H.266/VVC，但其依赖的注意力机制和熵模型面临通道协方差冗余问题：  
- 窗口空间注意力在原生特征通道上计算Q/K/V时，通道间的扩散耦合限制了注意力核的容量。  
- 基于通道级切片的自回归熵模型（ChARM）难以处理切片间和切片内非对角协方差，导致率失真次优。  

**整体含义**：  
将经典变换编码中的小波分解思想重新引入现代LIC，作为一种可逆的通道级重参数化手段，同时改进注意力骨干和熵编码模块，以结构化稀疏通道协方差，提升有限容量模型对统计依赖的拟合效率。

## 2. 论文提出的方法论

### 核心思想
在窗口空间注意力中，先对输入特征沿通道维度施加一维小波变换（WT_c），在变换后的通道坐标系中计算Q/K/V投影，再通过逆小波变换（IWT_c）恢复原始通道坐标；在熵编码阶段，对量化前潜变量执行两级通道级小波包分解（ChWP），生成四个等大子带，适配ChARM切片编码。

### 关键技术细节

#### 2.1 通道级小波域Transformer块（ChWDTB）
- 结构：逐层堆叠，每个ChWDTB包含归一化 → WT_c → 多头窗口空间自注意力（ChWDMSA_sp） → IWT_c → 前馈网络。  
- 数学表示：  
  \( (S, D) = \text{WT}_c(F_{in}) \) → \( T = \text{concat}(S, D) \) → \( A = \text{ChWDMSA}_{sp}(T) \) → \( F_{out} = \text{IWT}_c(A) \)。  
- 协方差稀疏化解释：  
  设原始通道协方差为 \( \Sigma_f \)，小波变换后 \( \Sigma_u = W\Sigma_f W^\top \)。定义跨分支耦合比 \( r_{off} = (\|\Delta_{LH}\|_F + \|\Delta_{HL}\|_F) / (\|\Sigma_{LL}\|_F + \|\Sigma_{HH}\|_F) \)。小波变换使得 \( r_{off} \) 降低，从而减少有限容量注意力核需建模的跨分支交互上界（Cauchy-Schwarz不等式）。

#### 2.2 通道级小波包分解（ChWP）与切片熵编码
- 两级分解：  
  第一级将潜变量 \( y \) 分为 \( L_1, H_1 \)；第二级分别对 \( L_1 \) 和 \( H_1 \) 再做分解，得到 \( LL, LH, HL, HH \) 四个等大子带（通道维度各占1/4）。  
- 切片划分：默认每个子带再均分为2个通道切片，共8切片顺序编码（8-slice）；也可直接4切片编码（4-slice）。  
- 编码模型：条件高斯似然，每切片内对角，切片间顺序依赖，使用注意力网络（含ChWDTB）估计分布参数，并加入潜在残差预测（LRP）补偿量化误差。

#### 2.3 可学习提升小波
- 采用CDF 9/7拓扑的Lifting实现，含四个预测/更新步骤和一对缩放。参数 \( (\alpha, \beta, \gamma, \delta, K) \) 可初始化自CDF 9/7并微调，每个小波模块仅增加5个标量参数。

## 3. 实验设计

### 数据集与评估场景
- **训练集**：OpenImages子集（约300k张图像）。  
- **测试集**：Kodak（24张，768×512）、CLIC Professional Validation（41张，约2048×1440）、Tecnick（100张，1200×1200）。  
- **评估指标**：bpp vs. PSNR / MS-SSIM，BD-rate（以VTM-9.1 VVC-Intra 4:4:4为锚点）。  
- **对比方法**：  
  - 经典编码器：VVC (VTM-9.1)  
  - 学习编码器：ELIC (CVPR'22)、STF (CVPR'22)、TCM (CVPR'23)、FAT (ICLR'24)、MLIC++ (ICML'23)、WeConvene (ECCV'24)、LALIC (CVPR'25)、DCAE (CVPR'25)。

### 实验设置
- 损失函数：MSE加权（λ∈{0.0025,0.0035,0.0067,0.0130,0.0250,0.0500}）和MS-SSIM加权（λ∈{3,5,8,16,32,64}）。  
- 训练配置：Adam优化器，三阶段调度（批次8，图像尺寸256→512，学习率1e-4→1e-5，总70 epochs）。  
- 网络配置：骨干通道数(96,144,256,192,192)，潜变量320维，超先验192维；窗口大小8×8（骨干）和4×4（超先验）。

## 4. 资源与算力

- **GPU型号**：论文仅在测量编解码延迟时明确使用了**NVIDIA RTX 4090**（单batch，768×512输入）。  
- **训练算力**：未明确说明训练使用的GPU数量/型号；仅提及采用Adam优化器和70 epochs训练，约300k图像，批次8。  
- **算力信息完整性**：训练具体算力细节未公开，但消融实验和最终模型均在同一协议下完成，可复现性较好。

## 5. 实验数量与充分性

### 实验组数
- **主对比实验**：3个数据集×2个指标（PSNR, MS-SSIM）的率失真曲线，以及一张包含5个指标（BD-rate、编码/解码时延、kMACs/pixel、参数量）的综合对比表（Table I）。  
- **消融实验**：  
  - 移除WT_c/IWT_c和ChWP（Table II，1次）  
  - 不同切片数（4-slice vs 8-slice，Table III）  
  - 不同WT级别（1级 vs 2级，Table IV）  
  - 不同小波类型（Haar、固定bior4.4、学习）在ChWDTB和ChWP中的效果（Table V，6次）  
  - 不同注意力网络用于熵模型（Table VI，1次）  
- **可视化分析**：图7（通道相关性矩阵）、图8（ChWP切片协方差与率分布）  
- **复杂度-率失真权衡**：图1（延迟 vs BD-rate散点图）

### 充分性与公平性
- 消融实验严格保持其他组件不变，对比合理。  
- 对比方法使用其公开模型或论文报告数据，且BD-rate基于同一VTM-9.1锚点计算。  
- 覆盖低、中、高码率，自然图像和高分辨率图像。  
- 局限性：未进行交叉验证或统计学显著性检验；未在视频或医学图像上评估。

## 6. 论文的主要结论与发现

1. **性能领先**：默认8-slice模型在Kodak、CLIC Pro Valid、Tecnick上BD-rate分别达-17.82%、-19.15%、-22.56%，优于所有对比方法（如DCAE分别高出0.84、2.17、2.43个百分点）。  
2. **复杂度可控**：4-slice变体以更低参数和MACs获得接近性能（-17.24%, -18.42%, -21.71%），在低延迟场景下仍优于DCAE。  
3. **模块有效性**：  
   - 移除WT_c/IWT_c（仅保留ChWP）导致BD-rate退化1.06个百分点；移除ChWP再退化1.26个百分点（Table II）。  
   - 8-slice比4-slice提升0.58个百分点，但代价是参数量+67%、延迟+20%。  
   - 学习小波优于固定CDF 9/7，且ChWP中小波学习收益更大。  
4. **协方差稀疏化验证**：图7显示通道相关性在小波变换后显著降低，跨分支耦合比下降23.96%；图8显示ChWP后的切片间和切片内协方差更接近对角假设。

## 7. 优点

- **方法创新性**：将可逆通道级小波变换作为注意力预处理器和熵编码组织工具，而非简单代替空间注意力或卷积。提供了协方差稀疏化的理论解释。  
- **实验彻底性**：包含多种消融（小波类型、级别、切片数、模块禁用），可视化支撑理论分析。  
- **性能领先**：在多个高分辨率数据集上刷新SOTA，且提供4-slice/8-slice两种复杂度选项，实用性强。  
- **可复现性**：开源代码（github仓库），训练协议详细，便于复现。

## 8. 不足与局限

- **理论局限**：协方差稀疏化分析基于均值场近似（跨位置协方差等于边际协方差），实际偏差未量化。  
- **实验覆盖**：仅评估自然图像，未涉及视频、医学或遥感图像；仅使用MSE和MS-SSIM损失，未探索感知优化。  
- **场景限制**：未实现内容自适应或可变码率；固定小波基（即使可学习）可能不是最适应特定图像结构的变换。  
- **算力消耗**：训练需70 epochs且逐步增大图像尺寸，训练时间较长；Kodak上8-slice总延迟225 ms（编码+解码），虽优于部分方法但仍不满足实时需求。  
- **低码率改进有限**：Kodak上低码率（<0.3 bpp）增益较小（约0.1 dB），高码率改进更明显，说明协方差稀疏化在低码率下可能受量化噪声主导。  
- **统计显著性未报告**：未提供多次实验的标准差或置信区间，单次实验结果可能受随机种子影响。

（完）
