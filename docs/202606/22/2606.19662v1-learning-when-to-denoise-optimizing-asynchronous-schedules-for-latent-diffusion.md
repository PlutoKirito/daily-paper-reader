---
title: "Learning When to Denoise: Optimizing Asynchronous Schedules for Latent Diffusion"
title_zh: 学习何时去噪：优化潜空间扩散的异步调度
authors: "Bingshuo Qian, Xiang Cheng"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19662v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 直接改进潜在扩散模型通过学习去噪调度
tldr: "多表示扩散模型依赖于异步调度决定各表示的去噪时机，但固定调度限制了性能。本文提出通过异步流匹配和调度校正目标学习最优调度，采用凸单调参数化类联合探测，计算开销低于1%。在ImageNet 256x256上，学习调度使200 epoch模型达到FID 1.05，较800 epoch SFD-XL基线训练加速4倍；600 epoch进一步降至FID 1.02，超越1B参数SFD-XXL。无引导下200 epoch FID 2.37优于基线，600 epoch达2.14。该方法有效提升训练效率与生成质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19662v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 681, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 683, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1573, \"height\": 797, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1574, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1575, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1574, \"height\": 1055, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1574, \"height\": 1056, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1573, \"height\": 1057, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19662v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1572, \"height\": 1055, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19662v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 617, \"height\": 812, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19662v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 945, \"height\": 781, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19662v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 818, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19662v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 744, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19662v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1031, \"height\": 2048, \"label\": \"Table\"}]"
motivation: 多表示扩散模型中异步调度决定各表示去噪时机，固定调度难以最优，需学习自适应调度以提升性能。
method: "提出异步流匹配框架，引入调度校正目标保持噪化权重固定，使用凸单调参数化调度类，通过联合探测快速学习，附加计算开销小于1%。"
result: ImageNet 256x256上，学习调度使200 epoch模型FID 1.05（4倍加速），600 epoch达FID 1.02超越1B参数SFD-XXL；无引导下200 epoch FID 2.37优于基线。
conclusion: 通过学习最优异步调度，显著加速扩散模型训练并提升生成质量，以更少资源超越更大模型性能。
---

## 摘要
多表征扩散模型可以通过对图像的互补视图进行去噪来改进视觉合成，但其性能关键取决于决定每个表征何时去噪的异步调度。我们提出学习这种调度。我们的方法在多表征空间上制定异步流匹配，并使用调度校正目标，随着调度变化保持每个表征的局部加噪时间权重固定。我们用一个灵活的、通过构造保证凸且单调的参数类实例化调度，并通过快速联合探测来学习它，额外训练计算量不到1%。在ImageNet 256x256上，学习到的调度在匹配的675M参数XL骨干下显著提高了收敛速度和最终质量。使用AutoGuidance，我们的200轮模型达到了FID 1.05，与800轮SFD-XL基线相当，训练量减少4倍。训练到600轮进一步提升到FID 1.02，优于使用更大模型的1B参数SFD-XXL结果（FID 1.04）。在无引导设置下，我们的200轮模型达到了FID 2.37，已经低于最佳800轮SFD-XL结果（2.54），训练量减少4倍，并在600轮时提升到FID 2.14。代码可在https://github.com/bsq532087/LWD获取。

## Abstract
Multi-representation diffusion models can improve visual synthesis by denoising complementary views of an image, but their performance depends critically on the asynchronous schedule that determines when each representation is denoised. We propose to learn this schedule. Our method formulates asynchronous flow matching over multiple representation spaces and uses a schedule-corrected objective that keeps each representation's local noising-time weights fixed as the schedule changes. We instantiate the schedule with a flexible parametric class that is convex and monotone by construction, and learn it using a fast joint probe with less than 1% additional training compute. On ImageNet 256x256, the learned schedule substantially improves both convergence speed and final quality under a matched 675M-parameter XL backbone. With AutoGuidance, our 200-epoch model reaches FID 1.05, matching the 800-epoch SFD-XL baseline with 4x less training. Training to 600 epochs further improves to FID 1.02, outperforming the 1B-parameter SFD-XXL result of FID 1.04 while using a smaller model. In the unguided setting, our 200-epoch model reaches FID 2.37, already below the best 800-epoch SFD-XL result (2.54) at 4x less training, and improves to FID 2.14 at 600 epochs. Code is available at https://github.com/bsq532087/LWD

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多表示扩散模型**中的异步去噪调度问题。现有方法（如 SFD）手动或通过低维网格搜索确定不同表示（如语义潜变量和纹理潜变量）的噪声水平随时间变化的速率，但这种固定调度限制了模型的表达能力和效率。**核心挑战**是：如何自动学习一个最优的异步调度，使得在不同表示空间中去噪的时机协调一致，从而加速收敛并提升生成质量。该研究动机在于：好的调度需要在表示之间平衡信息揭示顺序，同时保持流匹配的易训练性，而这依赖于具体模型、表示和训练目标，手动选择变得困难。

## 2. 论文提出的方法论

### 核心思想
- 将异步调度参数化为一个**可学习的语义-纹理局部时间函数**，其中语义时间被固定为全局时间（`t_sem(τ) = τ`），纹理时间 `t_tex(τ; ρ)` 由参数 ρ 决定且满足语义优先约束（`t_tex ≤ τ`）。
- 提出**调度校正目标**（Schedule-Corrected Objective），通过雅可比校正（Jacobian correction）确保当调度变化时，每个表示空间的局部噪化时间边际权重保持不变，避免调度通过偷换训练时域来欺骗损失。
- 设计一个**凸单调的参数化调度类**（多项式导数），保证语义领先特性，无需额外惩罚项。
- 采用**两阶段联合优化**：短期联合探测（约 1% 训练预算）学习调度参数，冻结后从头训练最终去噪器。

### 关键技术细节
- **异步流匹配**：将复合潜变量 `z(τ) = [x_tex(t_tex(τ)), x_sem(τ)]` 的全局时间导数表示为局部时间导数的加权和（`dz/dτ = [t'_tex · u_tex, u_sem]`）。理想局部流场通过条件期望定义。
- **调度校正权重**：纹理分支的时间权重 `ω_tex(τ,ρ) = w_tex · t'_tex(τ;ρ)`（stop-gradient），语义分支 `ω_sem = w_sem`，使得局部时间均匀采样不变。
- **动能正则化**：`R_kin = E[ t'_tex² ||v_tex||² + ||v_sem||² ]`，惩罚纹理在全局时间上过快变化，提高离散化稳定性。
- **调度参数化**：`t'_tex(τ;ρ) = (1/Z_ρ) Σ a_m τ^m`，`a_m = softplus(ρ_m)`，归一化保证端点条件，积分得 `t_tex`，自动凸且单调，满足 `t_tex ≤ τ`。
- **两阶段算法**：
  - **阶段一**：联合优化临时去噪器 `θ_probe` 和调度 ρ（约 10K 步，5K 烧入后平均参数），使用目标 `L_probe = L_flow + λ R_kin`。
  - **阶段二**：冻结 ρ，从头训练最终去噪器 θ（使用标准 SFD 辅助损失 + REPA）。

### 公式/算法流程（文字说明）
- 公式 (35) 给出了带校正权重的流匹配损失 `L_flow(θ,ρ)`。
- 公式 (38) 定义了调度学习目标 `L_ρ`，结合动能正则化。
- 算法 1 详细描述了探测和固定训练的两个阶段。

## 3. 实验设计

### 数据集和场景
- **单一数据集**：ImageNet 256×256 类别条件生成。
- **场景**：无引导（unguided）和有引导（AutoGuidance）设置。

### Benchmark
- 主要对比基线为 **SFD**（Semantic-First Diffusion），使用相同架构（LightningDiT-XL/1, 675M 参数）、相同潜表示、相同辅助损失和采样器。
- 同时对比 **DiT-XL/2, SiT-XL, MaskDiT, REPA, REPA-E, VA-VAE** 等代表性方法。

### 对比方法
- **直接对比**：SFD-XL 和 SFD-XXL。
- **更广泛的 675M 参数模型**：DiT-XL, MaskDiT, SiT-XL, FasterDiT, MDT, MDTv2, DDT; VA-VAE, REPA, REPA-E, ReDi, REG, RAE。
- **消融实验**：不同语义表示（SemVAE, DINO-PCA, CLIP-PCA）以及不同动能正则化系数 λ。

## 4. 资源与算力
- **训练硬件**：NVIDIA B200（1M 迭代及以下）和 H200（2M/3M 迭代）。
- **计算量**：
  - 主训练：2 块 B200，吞吐量 4.6 steps/s，1M 迭代约 60 小时。
  - 调度探测：10K 步，约 36 分钟（不到主训练 1%）。
- **模型规模**：所有主实验使用 675M 参数（LightningDiT-XL/1），对比实验中包含 1.0B 参数 SFD-XXL。

## 5. 实验数量与充分性
- **主要实验**：
  - 无引导 FID 收敛表（表 1）：7 个训练预算（70K 到 4M 迭代），与 SFD 同步比较。
  - 引导下系统级比较（表 2）：80/200/600 epoch，与 11 种方法对比，含 sFID、IS、Precision、Recall。
  - 鲁棒性实验（表 3）：三种语义特征（SemVAE, DINO-PCA, CLIP-PCA），对比 SFD。
- **消融实验**：
  - 动能正则化 λ 扫掠（图 2），展示从坍塌到稳定区域的过渡。
  - 不同语义编码器的调度形状（图 3）。
- **充分性**：实验覆盖了不同训练预算、不同引导条件、不同语义表示，且与多个 SOTA 方法公平比较（保持相同骨干、采样器等）。但仅在一个数据集（ImageNet 256×256）上验证，未扩展至其他模态或更高分辨率，实验充分性有局限。

## 6. 论文的主要结论与发现
- 学习到的异步调度显著加速收敛：在 400K 迭代时无引导 FID 从 3.53 (SFD) 降至 2.87，800K 时已匹配 SFD 的 4M 迭代结果（FID 2.53 vs 2.54），训练效率提升约 5 倍。
- 最终质量提升：200 epoch 引导下 FID 1.05（与 800 epoch SFD-XL 相同），600 epoch 达 FID 1.02，超越 1B 参数 SFD-XXL 的 FID 1.04，且召回率更高（0.72 vs 0.66）。
- 调度学习方法对语义表示具有鲁棒性：在 SemVAE、DINO-PCA、CLIP-PCA 上均优于相应固定调度（表 3）。
- 调度形状随语义编码器信息丰度变化：信息越丰富，纹理延迟越小。

## 7. 优点
- **轻量高效**：调度学习附加计算开销<1%，无需大量超参数搜索。
- **理论保证**：调度校正目标确保局部时间权重不变，避免训练损失被调度扭曲；凸单调参数化自动满足语义领先约束。
- **即插即用**：学习到的调度可冻结后用于标准训练，不改变推理接口。
- **实验控制严谨**：所有与 SFD 的比较使用完全相同的骨干、表示、采样器和辅助损失，仅改变调度，结果可信。
- **通用性**：在多种语义特征上有效，表明并非针对特定编码器的技巧。

## 8. 不足与局限
- **实验范围有限**：仅评测 ImageNet 256×256 的类别条件生成，未在文本-图像、视频、音频上验证，也未测试更高分辨率。
- **模态与规模**：未尝试超过两个表示组（如纹理+语义+深度），也未扩展至更大骨干（如 3B 参数）。
- **引导交互未深入**：仅使用 AutoGuidance 单一设置，未研究学习调度如何影响 CFG 或其他引导策略，以及引导权重随 τ 变化的可能性。
- **调度探测超参数依赖**：动能正则化 λ 需要针对每个语义编码器进行一维扫掠（虽然很小），增加了部署成本。
- **调度参数化简单**：仅使用 4 阶多项式，可能无法表达更复杂的形状；未比较其他单调族（样条、CONN）。
- **潜在偏差**：仅考虑语义领先约束（纹理滞后），对于其他表示组合可能不适用。

（完）
