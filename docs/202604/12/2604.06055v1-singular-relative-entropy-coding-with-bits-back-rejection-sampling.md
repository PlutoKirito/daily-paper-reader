---
title: Singular Relative Entropy Coding with Bits-Back Rejection Sampling
title_zh: 基于 Bits-Back 拒绝采样的奇异相对熵编码
authors: "Gergely Flamich, Spencer Hill"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06055v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 针对奇异信道的相对熵编码与Bits-back拒绝采样
tldr: 本文针对相对熵编码（REC）中奇异信道的效率问题，提出了一种名为“Bits-Back 拒绝采样器”（BBRS）的新型编码方法。REC 旨在以接近互信息的比特数编码随机样本，但在奇异信道下通常存在对数级冗余。BBRS 结合了 bits-back 编码与贪婪拒绝采样技术，不仅在理论上实现了与前人研究一致的亚对数渐近冗余，且具有更简单的分析过程和更优的常数项，最重要的是其具备实际可操作性。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的奇异信道相对熵编码方法虽然能降低冗余，但其算法过于复杂且无法在实际中部署。
method: 提出 BBRS 算法，通过融合 bits-back 编码思想与贪婪拒绝采样机制来构建高效的相对熵代码。
result: BBRS 在奇异信道上达到了亚对数级的渐近冗余，且相比前人工作具有更简单的分析逻辑和更优的性能常数。
conclusion: BBRS 为奇异信道下的相对熵编码提供了一个兼具理论优越性和实践可行性的新方案。
---

## 摘要
针对信源 $X \sim P_X$ 的相对熵编码是一种随机编码，旨在以尽可能少的比特数对来自指定分布 $P_{Y \mid X}$ 的随机样本进行编码。作为熵编码的一种推广，一个标准结论是实现该目标所需的最小比特数至少为互信息 $I[X\,\Vert\,Y]$。然而，与熵编码相比，相对熵编码一个特别引人注目的特征是，通常情况下，该下界只能在增加一个额外对数因子的范围内实现。因此，一个重要的研究方向是识别能够缩小这一差距的信道。Sriramu 和 Wagner 通过为所谓的奇异信道展示一种具有亚对数渐近冗余的相对熵编码，取得了此类成功。然而，他们的编码非常复杂，且遗憾的是无法在实践中实现。在本文中，我们构建了 Bits-Back 拒绝采样器（BBRS），这是一种结合了 Bits-Back 编码和（贪婪）拒绝采样思想的相对熵编码。我们对 BBRS 的分析表明，该算法达到了与 Sriramu 和 Wagner 采样器相同的渐近效率，但分析更简单且常数项更优。此外，BBRS 可以使用标准的相对熵编码方法来实现。

## Abstract
A relative entropy code for a source $X \sim P_X$ is a stochastic code that encodes random samples from a prescribed $P_{Y \mid X}$ using as few bits as possible. A generalisation of entropy coding, it is a standard result that the minimum number of bits required to achieve this is at least the mutual information $I[X\,\Vert\,Y]$. However, a particularly fascinating feature of relative entropy coding compared to entropy coding is that, in general, this lower bound is only achievable to within an additional logarithmic factor. As such, an important research direction is to identify channels where we can reduce this gap.   Sriramu and Wagner achieved such success by exhibiting a relative entropy code for so-called singular channels with sub-logarithmic asymptotic redundancy. However, their code is quite involved and, sadly, cannot be implemented in practice.   In this paper, we construct the bits-back rejection sampler (BBRS), a relative entropy code that combines ideas from bits-back coding and (greedy) rejection sampling. Our analysis of BBRS reveals that the algorithm achieves the same asymptotic efficiency as Sriramu and Wagner's sampler, but with much simpler analysis and better constants. Moreover, BBRS can be implemented using standard relative entropy coding methods.

---

## 论文详细总结（自动生成）

### 论文总结：基于 Bits-Back 拒绝采样的奇异相对熵编码

#### 1. 论文的核心问题与整体含义
*   **研究背景**：相对熵编码（Relative Entropy Coding, REC）是熵编码的推广，旨在以接近互信息 $I[X;Y]$ 的比特数模拟信道 $X \to Y$。
*   **核心痛点**：理论证明，REC 的平均码长通常比下界 $I[X;Y]$ 高出一个对数级冗余（$\log I$）。虽然 Sriramu 和 Wagner (2024) 发现对于“奇异信道”（Singular Channels），这种冗余可以降至亚对数级，但其提出的算法极其复杂，且在实际中不可计算。
*   **研究动机**：开发一种既能达到理论最优渐近冗余，又具有实际可操作性、分析更简单的奇异信道编码方案。

#### 2. 论文提出的方法论
论文提出了一种名为 **Bits-Back 拒绝采样器（BBRS）** 的新型随机编码算法。
*   **核心思想**：利用奇异信道的特性（即输出 $Y$ 的似然比仅取决于其支持集），将其视为一种“纠错机制”。结合 **Bits-Back 编码** 思想，将量化密度比带来的额外开销通过“回收”比特的方式抵消。
*   **关键技术细节**：
    1.  **量化密度比**：对对数密度比进行 $\Delta$ 精度的量化，得到离散变量 $\Gamma$。
    2.  **两阶段编码流程**：
        *   **第一阶段**：使用“贪婪拒绝采样”（GRS）编码一个中间样本 $Y'$。
        *   **第二阶段（Bits-Back 过程）**：由于信道是奇异的，解码器可以从 $Y'$ 中通过预定义的函数 $g(Y')$ 完美恢复 $\Gamma$。利用这一特性，编码器可以从现有的消息流中“解码”出 GRS 的接受决策比特，从而缩短总码长。
    3.  **最终采样**：使用简单的拒绝采样，以 $P_{Y|\Gamma}$ 为提议分布，编码最终符合 $P_{Y|X,\Gamma}$ 的样本。
*   **算法优势**：通过 Bits-Back 机制，原本用于告知解码器密度比信息的比特被有效回收，从而消除了对数级冗余。

#### 3. 实验设计
*   **研究类型**：本文属于理论信息论与算法构建研究，侧重于数学证明。
*   **场景/数据集**：主要针对**奇异信道**模型，如加性均匀信道（$Y = X + U$）和二进制擦除信道。
*   **Benchmark（基准）**：Sriramu-Wagner (SW) 采样器。
*   **对比方法**：对比了 SW 采样器在渐近对数冗余（Asymptotic Logarithmic Redundancy）上的表现，以及单次编码（One-shot）的速率界限。

#### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何关于 GPU、CPU 型号或训练时长的具体算力消耗。
*   **原因**：该研究侧重于编码协议的逻辑构建和概率论证明，不涉及深度学习模型的训练或大规模数据处理。

#### 5. 实验数量与充分性
*   **实验规模**：论文未提供大规模的数值实证实验，而是提供了详尽的数学推导和伪代码实现。
*   **充分性评价**：
    *   **理论层面**：非常充分。论文给出了单次速率的严格上界证明，并推导了渐近情况下的冗余度。
    *   **实证层面**：缺乏在真实压缩任务（如神经网络权重压缩）中的性能评测。但作为一篇解决“可实现性”问题的理论论文，其逻辑自洽性已足够。

#### 6. 主要结论与发现
*   **零渐近冗余**：证明了 BBRS 在奇异信道下可以实现零渐近对数冗余（$R_{log} = 0$），达到了理论极限。
*   **效率提升**：BBRS 的单次速率常数项比 SW 算法更小，且分析过程更直观。
*   **可实现性**：BBRS 可以直接结合非对称数字系统（ANS）等现代流编码器使用，解决了奇异信道 REC 无法落地的难题。

#### 7. 优点
*   **理论创新**：首次将 Bits-Back 编码与奇异信道的 REC 联系起来，为消除对数冗余提供了清晰的物理直觉。
*   **实用性强**：算法流程清晰（见论文算法 2 和 3），避开了 SW 算法中不可计算的项。
*   **性能优越**：在保证渐近最优的同时，显著改善了单次编码的效率。

#### 8. 不足与局限
*   **量化依赖**：算法仍需对密度比进行人为的 $\Delta$ 量化，虽然 $\Delta$ 可以很小，但如何选择最优量化步长仍需进一步研究。
*   **适用范围限制**：该算法专门针对“奇异信道”设计。对于非奇异信道，虽然论文在附录中给出了改进方案，但其冗余度无法达到零，且优势不明显。
*   **缺乏实测数据**：未提供实际软件运行的吞吐量（Throughput）或在具体应用场景下的比特率对比。

（完）
