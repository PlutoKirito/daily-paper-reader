---
title: Lossless Compression via Chained Lightweight Neural Predictors with Information Inheritance
title_zh: 基于信息继承的链式轻量级神经预测器无损压缩
authors: "Yuriy Kim, Evgeny Belyaev"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15472v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 基于神经预测器的无损压缩概率估计架构
tldr: 本研究提出了一种基于链式轻量级神经预测器的无损数据压缩架构。通过将多个极小规模的神经网络单元串联，并引入信息继承机制，使低阶单元的概率估计能辅助高阶单元，从而根据数据统计特性优化权重使用。该方法在保持与最先进PAC压缩器相当的压缩率的同时，显著提升了在消费级GPU上的编解码吞吐量，实现了高效的概率估计与数据压缩。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15472v1/fig-001.webp\", \"caption\": \"TABLE I: Description of the datasets used in the experiments\", \"page\": 5, \"index\": 1, \"width\": 540, \"height\": 292}]"
motivation: 旨在解决现有神经网络压缩方法在概率估计时计算开销大、权重冗余的问题，以提升编解码速度。
method: 提出一种由最小权重单元组成的链式神经预测器架构，并结合信息继承机制，将低阶预测结果传递给高阶单元。
result: 实验表明该方法在压缩率接近PAC的同时，编码速度提升了1.2至6.3倍，解码速度提升了2.8至12.3倍。
conclusion: 链式轻量级神经预测器结合信息继承机制，能有效平衡压缩效率与计算性能，是实现高性能无损压缩的有效方案。
---

## 摘要
本文致力于研究利用神经网络进行概率估计的无损数据压缩。首先，我们提出了一种基于链式神经预测器的概率估计架构，其中链中的每个单元都被定义为具有最小可能权重数量的神经网络，这足以对给定阶数的马尔可夫源生成的数据进行高效压缩。我们表明，该架构允许我们根据输入数据的统计特性，最小化参与概率估计过程的总权重数量。其次，为了提高压缩效率，我们引入了一种信息继承机制，即低阶单元获得的概率估计被用于下一个高阶单元。实验结果表明，所提出的配备链式概率估计架构的无损数据压缩器提供的压缩比接近最先进的 PAC 压缩器。同时，在消费级 GPU 上，其编码吞吐量优于 PAC 1.2 到 6.3 倍，解码吞吐量优于 PAC 2.8 到 12.3 倍。

## Abstract
This paper is dedicated to lossless data compression with probability estimation using neural networks. First, we propose a probability estimation architecture based on a chain of neural predictors, so that each unit of the chain is defined as a neural network with the minimum possible number of weights, which is sufficient for efficient compression of data generated by Markov sources of a given order. We show that this architecture allows us to minimize the overall number of weights participating in the probability estimation process depending on the statistical properties of the input data. Second, in order to improve compression efficiency, we introduce an information inheritance mechanism, where the probability estimate obtained by a low-order unit is used at the next higher-order unit. Experimental results show that the proposed lossless data compressor equipped with the chained probability estimation architecture provides compression ratios close to the state-of-the-art PAC compressor. At the same time, it outperforms PAC by a factor of 1.2 to 6.3 in encoding throughput and by a factor of 2.8 to 12.3 in decoding throughput on a consumer GPU.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“基于信息继承的链式轻量级神经预测器”的无损压缩方法，旨在解决现有神经压缩算法计算开销过大的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的神经无损压缩器（如 PAC、TRACE）虽然压缩率优于传统算法（如 Gzip、7z），但其**编解码吞吐量极低**（通常仅为几十 KB/s），且往往对所有类型的数据使用统一的大型模型，导致在处理统计特性简单的源时存在严重的计算资源浪费。
*   **研究动机**：
    *   不同数据的马尔可夫阶数（依赖关系长度）差异巨大，应根据数据特性动态调整模型复杂度。
    *   减少解码时的计算负担（如避免自适应方法中必需的反向传播），提升实用性。

### 2. 方法论
该方法的核心思想是构建一个由多个轻量级神经单元组成的“链条”，并引入信息传递机制。
*   **链式轻量级预测器**：设计了 6 个针对不同马尔可夫阶数（$s=1, 2, 3, 4, 8, 16$）优化的单元。通过架构搜索发现，不同阶数下表现最优的架构不同（如低阶用 CNN，高阶用 GRU）。
*   **信息继承机制**：高阶单元在进行概率估计时，会通过线性加权的方式继承低阶单元生成的 Logits（未归一化的概率得分），公式为 $l_i = \alpha_i \cdot l'_i + \beta_i \cdot l'_{i-1}$。这使得模型能结合不同长度上下文的预测结果。
*   **自适应禁用准则**：引入拉格朗日乘子 $\lambda$ 来平衡压缩率和计算时间。如果增加一个高阶单元带来的压缩收益不足以抵消其增加的时间成本，则停止链条，从而实现复杂度自适应。
*   **半自适应方案**：在编码端针对特定数据训练模型，将量化后的权重放入压缩包头部。解码端直接加载权重进行推理，无需反向传播，显著提升了解码速度。
*   **数据驱动分词（Tokenization）**：使用字节对编码（BPE）减少序列长度，从而减少神经预测器的调用次数。

### 3. 实验设计
*   **数据集**：涵盖了异构数据（Backup）、文本（Enwik8/9、Books）、图像（Image100）、音频（Sound100）、浮点数（Spitzer）和基因序列（AnCa、GaGa），以及随机数据（Rand）。
*   **Benchmark（基准）**：
    *   **传统算法**：Gzip (-9)、7z (mx=9)、Zstd (-22)。
    *   **神经压缩器**：PAC（当前 SOTA）、TRACE。
*   **对比指标**：BPS（每符号比特数，越低越好）、编码吞吐量（KB/s）、解码吞吐量（KB/s）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i5-12400F CPU，**NVIDIA GeForce RTX 4060 Ti GPU** (消费级)，64 GB RAM。
*   **训练细节**：每个单元训练 3 个 Epoch（1 个无继承预训练，2 个带继承联合优化）。Batch size 为 4096。
*   **算力说明**：文中提供了各单元的参数量（0.02M 到 0.82M）和推理 FLOPs，但未明确给出总的训练时长，仅以吞吐量形式体现了整体效率。

### 5. 实验数量与充分性
*   **实验规模**：在 14 个不同规模和类型的测试集上进行了完整测试。
*   **消融实验**：
    *   验证了**信息继承**的有效性（在某些数据集上可降低高达 13.9% 的 BPS）。
    *   测试了**分词器**对性能的影响。
    *   研究了**验证集比例**对早停机制和最终压缩率的影响。
*   **评价**：实验设计较为全面，覆盖了多种模态的数据，对比对象包含了目前最强的神经压缩模型，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **压缩率**：提出的方法在压缩性能上非常接近 PAC，且在大型数据集上甚至略有超越。
*   **速度优势**：在 GPU 上，编码速度比 PAC 快 **1.2 到 6.3 倍**，解码速度快 **2.8 到 12.3 倍**。
*   **复杂度自适应**：对于随机数据，系统能自动识别并仅使用最简单的单元（Unit 1），避免无效计算。
*   **架构发现**：证明了混合使用 CNN、GRU 和 Transformer 单元比单一架构在处理多阶马尔可夫源时更有效。

### 7. 优点
*   **高效性**：通过轻量级单元链和半自适应模式，极大地缓解了神经压缩“跑不动”的痛点。
*   **灵活性**：信息继承机制允许模型在不增加过多参数的情况下提升预测精度。
*   **实用性**：解码端无需训练，使其在消费级硬件上具备了初步的实用价值。

### 8. 不足与局限
*   **头部开销**：作为半自适应方法，必须在压缩文件中存储模型权重。对于极小文件，权重带来的额外比特数可能会抵消压缩收益。
*   **速度仍慢于传统算法**：尽管比 PAC 快很多，但百 KB/s 级别的速度与传统算法（MB/s 甚至 GB/s 级别）相比仍有巨大差距，限制了其在大规模实时系统中的应用。
*   **硬件依赖**：性能高度依赖 GPU，在纯 CPU 环境下的表现（文中未详细讨论）可能会大幅下降。

（完）
