---
title: "Uni-ViGU: Towards Unified Video Generation and Understanding via A Diffusion-Based Video Generator"
title_zh: Uni-ViGU：通过基于扩散的视频生成器实现统一的视频生成与理解
authors: "Luozheng Qin, Jia Gong, Qian Qiao, Tianjiao Li, Li Xu, Haoyu Pan, Chao Qu, Zhiyu Tan, Hao Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08121v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 基于扩散模型的视频生成器，用于统一生成与理解
tldr: 本研究提出Uni-ViGU框架，旨在通过扩展视频生成器来实现视频生成与理解的统一。针对视频生成计算成本远高于理解的挑战，该框架反转了传统以理解为中心的范式，引入统一流方法处理多模态生成，并采用基于MoE的架构和双阶段双向训练机制（知识召回与能力精炼）。实验证明，Uni-ViGU在生成和理解任务上均表现出色，为构建统一多模态智能提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08121v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of Uni-ViGU framework. We formulate unified multimodal generation via a uniflow process, where video generation is modeled with continuous flow matching in the latent space, while text generation is modeled with discrete flow matching over token embeddings. Both modalities are jointly learned within a single Transformer backbone: self- and cross-attention layers are shared to enable cross-modal interaction and alignment, whereas modality-specific FFN branches are employed to capture domain-specific knowledge for video and text, respectively. This design allows the model to unify generation and understanding while preserving modality-specialized capacity.\", \"page\": 4, \"index\": 1, \"width\": 820, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.08121v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative Results on Video-Text Joint Generation\", \"page\": 8, \"index\": 2, \"width\": 824, \"height\": 197}]"
motivation: 针对视频生成比理解计算成本更高的问题，探索以生成器为基础而非以理解模型为基础的统一多模态框架。
method: 提出统一流方法处理多模态生成，并结合模态驱动的MoE架构与双阶段双向训练机制来复用生成知识进行理解。
result: Uni-ViGU在视频生成和视频理解任务上均取得了具有竞争力的性能表现。
conclusion: 实验验证了以生成为中心的架构是实现统一多模态智能的一条具有扩展性的可行路径。
---

## 摘要
集成视觉理解与生成的统一多模态模型面临着一个根本性挑战：视觉生成的计算成本远高于理解，在视频领域尤为如此。这种不平衡促使我们反转传统范式：我们没有扩展以理解为中心的多模态大语言模型（MLLMs）来支持生成，而是提出了 Uni-ViGU，这是一个以视频生成器为基础进行扩展，从而统一视频生成与理解的框架。我们引入了一种统一流（unified flow）方法，在单一过程中对视频执行连续流匹配（continuous flow matching），对文本执行离散流匹配（discrete flow matching），从而实现连贯的多模态生成。我们进一步提出了一种基于模态驱动的混合专家（MoE）框架，通过轻量级层增强 Transformer 块以进行文本生成，同时保留生成先验。为了将生成知识重新用于理解，我们设计了一个包含两个阶段的双向训练机制：“知识召回”（Knowledge Recall）阶段通过重构输入提示词来利用已学习的文本-视频对应关系，而“能力精炼”（Capability Refinement）阶段则在详细描述上进行微调，以建立具有判别性的共享表示。实验表明，Uni-ViGU 在视频生成和理解方面均取得了具有竞争力的性能，验证了以生成为中心的架构是通往统一多模态智能的一条可扩展路径。项目主页与代码：https://fr0zencrane.github.io/uni-vigu-page/。

## Abstract
Unified multimodal models integrating visual understanding and generation face a fundamental challenge: visual generation incurs substantially higher computational costs than understanding, particularly for video. This imbalance motivates us to invert the conventional paradigm: rather than extending understanding-centric MLLMs to support generation, we propose Uni-ViGU, a framework that unifies video generation and understanding by extending a video generator as the foundation. We introduce a unified flow method that performs continuous flow matching for video and discrete flow matching for text within a single process, enabling coherent multimodal generation. We further propose a modality-driven MoE-based framework that augments Transformer blocks with lightweight layers for text generation while preserving generative priors. To repurpose generation knowledge for understanding, we design a bidirectional training mechanism with two stages: Knowledge Recall reconstructs input prompts to leverage learned text-video correspondences, while Capability Refinement fine-tunes on detailed captions to establish discriminative shared representations. Experiments demonstrate that Uni-ViGU achieves competitive performance on both video generation and understanding, validating generation-centric architectures as a scalable path toward unified multimodal intelligence. Project Page and Code: https://fr0zencrane.github.io/uni-vigu-page/.

---

## 论文详细总结（自动生成）

### Uni-ViGU：通过基于扩散的视频生成器实现统一的视频生成与理解

#### 1. 核心问题与研究动机
*   **核心问题**：如何在单一模型中高效统一视频的“生成”与“理解”任务。
*   **研究动机**：
    *   **计算成本不平衡**：传统的统一模型（如 MLLMs）通常以理解为中心，通过扩展来支持生成。然而，视频生成的计算开销（涉及数百万个 token 的迭代去噪）远高于理解。
    *   **范式反转**：作者认为与其让理解模型去适配高昂的生成成本，不如反过来，以强大的视频生成器为基础，将其能力扩展到理解领域。
    *   **生物学启发**：人类在学会用语言表达（理解/描述）之前，先具备了视觉感知能力，这暗示生成先验可能是统一智能的更自然基础。

#### 2. 方法论
Uni-ViGU 框架的核心思想是将视频生成器改造为一个能够同时处理视频和文本生成的统一扩散模型。
*   **统一流方法 (Uni-Flow)**：
    *   在同一个生成过程中，对视频采用**连续流匹配 (Continuous Flow Matching)**，在潜空间（Latent Space）进行去噪。
    *   对文本采用**离散流匹配 (Discrete Flow Matching)**，在 token 嵌入空间进行操作。
    *   通过独立采样视频和文本的时间步（$\tau_v, \tau_t$），使模型能学习跨模态的依赖关系。
*   **模态驱动的 MoE 架构**：
    *   基于 WAN2.1 视频生成器，保留其 Transformer 块中的**共享自注意力层**（用于跨模态对齐）。
    *   设计**模态特定的 FFN 分支**：保留预训练的视频 FFN 以维持生成能力，新增轻量级的文本 FFN 用于语言生成。
*   **双阶段双向训练机制**：
    *   **阶段 1：知识召回 (Knowledge Recall)**：让模型根据视频重构原始提示词（Prompt）。通过对提示词施加高概率丢弃（Dropout），强迫模型利用预训练的“文本-视频”对应关系来反向推导文本。
    *   **阶段 2：能力精炼 (Capability Refinement)**：使用详细的视频描述（Detailed Captions）进行微调。由于详细描述包含提示词中没有的视觉细节，模型必须深度理解视频内容才能完成生成。

#### 3. 实验设计
*   **数据集**：
    *   **阶段 1**：10,000 组视频-提示词对。
    *   **阶段 2**：10,000 组视频-提示词-详细描述三元组。
    *   数据构建：使用 SOTA 视频生成器合成视频，并利用 LLM 生成比原始提示词更丰富的详细描述。
*   **对比与基准**：
    *   主要展示了**联合视频-文本生成**的定性结果，即模型从噪声中同时生成视频及其对应的详细描述。
    *   作为预览版论文，实验侧重于验证“生成中心化”路径的可行性，而非在所有传统理解榜单上刷榜。

#### 4. 资源与算力
*   **算力消耗**：使用了 **16 台 H800 GPU**。
*   **训练时长**：总计训练时间为 **1 周**。
*   **初始化**：模型初始化自预训练的 WAN2.1 视频生成器。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了两个阶段的训练过程及多组定性生成案例（如海边小屋、木盒苹果、护士漫步等）。
*   **充分性评价**：
    *   **定性分析充分**：展示了模型在联合生成任务中，文本与视频内容的高度一致性。
    *   **定量分析略显不足**：作为一篇探索性论文，缺乏与主流视频理解模型（如 Video-LLaVA）在标准 Benchmark（如 MSR-VTT, MSVD）上的大规模量化对比。
    *   **消融实验**：对 MoE 架构设计和训练阶段的必要性进行了逻辑论证，但缺乏详细的消融数据表。

#### 6. 主要结论与发现
*   **生成中心化路径可行**：证明了扩展视频生成器是实现统一多模态智能的一条具有扩展性且原则上正确的路径。
*   **知识可逆性**：视频生成器中隐含的视觉-语义对应关系可以通过简单的双向训练被激活，从而转化为理解能力。
*   **协同进化**：在联合生成过程中，文本流和视频流可以通过共享注意力机制相互引导，产生比原始提示词更丰富的描述。

#### 7. 优点
*   **范式创新**：反转了“理解带动生成”的思维定式，解决了视频生成 token 爆炸带来的计算不平衡问题。
*   **架构优雅**：通过统一流匹配和模态驱动 MoE，在不破坏预训练生成先验的前提下，以极小的参数代价（仅新增文本 FFN）实现了功能扩展。
*   **训练高效**：仅需 2 万条数据和一周的 H800 训练即可实现初步的统一能力。

#### 8. 不足与局限
*   **数据规模较小**：2 万条数据的规模对于构建通用多模态大模型来说仍处于起步阶段。
*   **评估维度单一**：目前主要依赖视觉观察和定性案例，缺乏在复杂视频问答（VideoQA）等硬核理解任务上的表现数据。
*   **文本生成范式限制**：基于扩散的文本生成在逻辑推理和长文本连贯性上，目前通常被认为略逊于自回归（Autoregressive）模型。

（完）
