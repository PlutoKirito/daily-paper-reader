---
title: Lossless Compression via Chained Lightweight Neural Predictors with Information Inheritance
title_zh: 基于信息继承的链式轻量级神经预测器无损压缩
authors: "Yuriy Kim, Evgeny Belyaev"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15472v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 基于神经预测器链的概率估计架构
tldr: 本研究针对神经网络无损数据压缩中的概率估计问题，提出了一种基于链式轻量级神经预测器的架构。该架构通过最小化权重的链式单元处理马尔可夫源数据，并引入信息继承机制，使高阶单元能利用低阶单元的估计结果。实验表明，该方法在保持接近SOTA（如PAC）压缩率的同时，显著提升了GPU上的编解码吞吐量，实现了效率与性能的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15472v1/fig-001.webp\", \"caption\": \"TABLE I: Description of the datasets used in the experiments\", \"page\": 5, \"index\": 1, \"width\": 540, \"height\": 292}]"
motivation: 旨在解决现有神经网络压缩模型计算开销大、推理速度慢的问题，同时保持高效的概率估计能力。
method: 提出一种由极简权重单元组成的链式神经预测器架构，并结合信息继承机制，将低阶概率估计传递至高阶单元。
result: 在GPU上，该方法的压缩率接近PAC，但编码速度提升了1.2至6.3倍，解码速度提升了2.8至12.3倍。
conclusion: 链式轻量级神经预测器结合信息继承机制，为高效、快速的神经网络无损压缩提供了一种极具竞争力的方案。
---

## 摘要
本文致力于研究利用神经网络进行概率估计的无损数据压缩。首先，我们提出了一种基于链式神经预测器的概率估计架构，其中链中的每个单元都被定义为具有最小可能权重数量的神经网络，这足以对给定阶数的马尔可夫源生成的数据进行高效压缩。我们表明，该架构允许我们根据输入数据的统计特性，最小化参与概率估计过程的总权重数量。其次，为了提高压缩效率，我们引入了一种信息继承机制，即低阶单元获得的概率估计被用于下一个高阶单元。实验结果表明，所提出的配备链式概率估计架构的无损数据压缩器提供的压缩比接近最先进的 PAC 压缩器。同时，在消费级 GPU 上，其编码吞吐量优于 PAC 1.2 到 6.3 倍，解码吞吐量优于 PAC 2.8 到 12.3 倍。

## Abstract
This paper is dedicated to lossless data compression with probability estimation using neural networks. First, we propose a probability estimation architecture based on a chain of neural predictors, so that each unit of the chain is defined as a neural network with the minimum possible number of weights, which is sufficient for efficient compression of data generated by Markov sources of a given order. We show that this architecture allows us to minimize the overall number of weights participating in the probability estimation process depending on the statistical properties of the input data. Second, in order to improve compression efficiency, we introduce an information inheritance mechanism, where the probability estimate obtained by a low-order unit is used at the next higher-order unit. Experimental results show that the proposed lossless data compressor equipped with the chained probability estimation architecture provides compression ratios close to the state-of-the-art PAC compressor. At the same time, it outperforms PAC by a factor of 1.2 to 6.3 in encoding throughput and by a factor of 2.8 to 12.3 in decoding throughput on a consumer GPU.

---

## 论文详细总结（自动生成）

### 论文总结：基于信息继承的链式轻量级神经预测器无损压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着全球数据量爆炸式增长，传统的无损压缩算法（如 LZ77、PPM）在处理复杂高阶依赖关系时达到瓶颈。近年来，基于神经网络的压缩器（如 PAC、TRACE）虽然在压缩率上超越了传统算法，但面临严重的**计算效率问题**：
*   **计算冗余**：现有模型（如 PAC）对所有类型的数据使用统一的大型架构，导致在处理简单数据源时浪费算力。
*   **解码缓慢**：自适应方法通常需要在解码端进行昂贵的反向传播训练。
*   **吞吐量低**：在消费级 GPU 上，现有神经压缩器的速度通常仅为几十 KB/s，难以满足实际应用需求。

本研究旨在设计一种**复杂度可自适应**的神经概率估计架构，在保持高压缩率的同时，显著提升编解码吞吐量。

#### 2. 方法论
论文提出了一个由**链式轻量级神经预测器**组成的架构，核心思想包括：
*   **链式轻量级单元**：根据马尔可夫源的阶数（$s=1, 2, 3, 4, 8, 16$）构建一系列极小规模的神经单元（包含 MLP、CNN、GRU、Transformer 等不同类型）。每个单元仅负责捕捉特定阶数的统计依赖。
*   **信息继承机制（Information Inheritance）**：受传统 PPMII 算法启发，高阶单元在预测时会通过线性加权方式继承低阶单元生成的 Logits（预测结果），公式表示为：$l_i = \alpha_i \cdot l'_i + \beta_i \cdot l'_{i-1}$。这增强了预测的准确性。
*   **自适应禁用准则**：引入拉格朗日乘子 $\lambda$，在压缩率（BPS）和编码时间之间做权衡。如果增加一个高阶单元带来的压缩收益不足以抵消时间成本，则自动停止链条，从而最小化参与计算的权重总数。
*   **半自适应方案**：在编码端针对输入数据训练模型，将量化后的权重放入压缩包头部。解码端直接进行推理而无需训练（反向传播），从而大幅提升解码速度。
*   **数据驱动的分词（Tokenization）**：使用字节对编码（BPE）减少序列长度，进一步提升处理速度。

#### 3. 实验设计
*   **数据集**：涵盖了异构数据（Backup）、文本（Enwik8/9、Books）、图像（Image100）、音频（Sound100）、科学数据（Spitzer100）、基因序列（AnCa、GaGa）以及随机数据（Rand）。
*   **Benchmark（基准）**：
    *   **传统算法**：Gzip (-9)、7z (-mx=9)、Zstd (-22)。
    *   **神经压缩器**：TRACE、PAC（当前 SOTA）。
*   **对比指标**：每符号比特数（BPS）、编码吞吐量（KB/s）、解码吞吐量（KB/s）。

#### 4. 资源与算力
*   **硬件环境**：Intel Core i5-12400F CPU，**NVIDIA GeForce RTX 4060 Ti GPU** (8GB)，64GB RAM。
*   **训练细节**：每个单元训练 3 个 Epoch（1 个不带继承，2 个带继承）。Batch Size 为 4096。
*   **模型规模**：单个单元参数量极小（0.02M 到 0.82M 不等），远小于 PAC 的 19.74M。

#### 5. 实验数量与充分性
*   **实验规模**：在 14 个不同规模和类型的测试集上进行了完整对比。
*   **消融实验**：
    *   验证了**信息继承**的有效性（在某些数据集上可降低高达 13.9% 的 BPS）。
    *   测试了不同**分词器字典大小**对性能的影响。
    *   分析了**验证集比例**对早停策略的影响。
*   **评价**：实验覆盖了多种模态，对比对象包括了最新的神经压缩模型，实验设计较为客观、公平，充分展示了模型在不同复杂度数据下的自适应能力。

#### 6. 主要结论与发现
*   **压缩率**：提出的方法在平均压缩率上与 PAC 非常接近（如在大型数据集上均为 4.12 BPS 左右），显著优于传统算法和 TRACE。
*   **速度优势**：
    *   **编码**：比 PAC 快 1.2 到 6.3 倍。
    *   **解码**：比 PAC 快 2.8 到 12.3 倍。
*   **自适应性**：对于随机数据，模型能自动识别并仅使用最简单的 Unit 1，避免无效计算。

#### 7. 优点
*   **复杂度可伸缩**：通过链式架构实现了“按需分配算力”，解决了神经压缩器在简单任务上过度计算的问题。
*   **解码效率高**：半自适应设计避开了解码端的训练开销，使其更接近实用化。
*   **架构搜索创新**：发现不同阶数的马尔可夫源适合不同的神经架构（如低阶适合 CNN，高阶适合 GRU），并据此优化了单元组成。

#### 8. 不足与局限
*   **绝对速度仍受限**：尽管比 PAC 快得多，但数百 KB/s 的速度与传统算法（如 Zstd 可达数百 MB/s）相比仍有数量级的差距。
*   **头部开销**：作为半自适应方法，需要存储模型权重，对于极小文件的压缩效率可能会受到权重体积的影响。
*   **硬件依赖**：性能高度依赖 GPU 的并行计算能力，在纯 CPU 环境下的表现未做详细讨论。

（完）
