---
title: "Neural Dynamic GI: Random-Access Neural Compression for Temporal Lightmaps in Dynamic Lighting Environments"
title_zh: Neural Dynamic GI：动态光照环境下时序光照贴图的随机存取神经压缩
authors: "Jianhui Wu, Jian Zhou, Zhi Zhou, Zhangjin Huang, Chao Li"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12625v1"
tags: ["query:vcodec"]
score: 8.5
evidence: 动态环境下时间光照贴图集的神经压缩技术
tldr: 本研究针对动态光照环境下预计算全局光照贴图存储开销巨大的挑战，提出了Neural Dynamic GI (NDGI) 压缩技术。该方法通过多维特征图和轻量级神经网络整合时域信息，取代了显式存储多套贴图的传统方式。此外，引入块压缩模拟策略进一步提升压缩率，并结合虚拟纹理系统实现高效实时解压。NDGI在极低存储占用下实现了高质量动态GI，为实时渲染提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12625v1/fig-001.webp\", \"caption\": \"Figure 5. Representative examples from our datasets. (a) FarmLand, sourced from “Arena Breakout: Infinite”. (b) An indoor environment with time-varying skylight and sunlight. (c) A city scene where streetlights toggle on and off at specific times. (d) An yard with multiple local light sources.\", \"page\": 6, \"index\": 1, \"width\": 496, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2604.12625v1/fig-002.webp\", \"caption\": \"Figure 6. Quality and storage comparison showing PSNR scores for lightmap tiles in the “FarmLand” scene. Compared with traditional texture compression methods, our approach achieves higher compression ratios and better reconstruction quality. Compared to NTC, NDGI yields obviously less noise with a much smaller decoder, leading to superior decompression performance.\", \"page\": 7, \"index\": 2, \"width\": 1014, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.12625v1/fig-003.webp\", \"caption\": \"Figure 11. Additional comparisons of rendering quality. Compared to PRT [23], our method better captures global illumination tone variations, such as multi-bounce interreflections. Compared to NTC [27], it reconstructs lightmaps with higher fidelity and noticeably less noise.\", \"page\": 14, \"index\": 3, \"width\": 1022, \"height\": 1204}, {\"url\": \"assets/figures/arxiv/2604.12625v1/fig-004.webp\", \"caption\": \"Figure 7. Comparison of rendered results. The “Lit Scene” displays the final rendered image. Notably, PRT exhibits color tone deviation, ASTC displays obvious blocky artifacts, and NTC produces noticeable noise. Compared with previous methods, NDGI delivers higherquality GI effects.\", \"page\": 8, \"index\": 4, \"width\": 1026, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.12625v1/fig-005.webp\", \"caption\": \"Figure 3. Method overview. Our method samples feature maps of different structures and feeds the features together with encoded time into an MLP to reconstruct the corresponding lighting (Section 3.3). During training, we introduce random noise to simulate the quantization and use weights and endpoints to emulate BC compression (Section 3.4). This approach allows us to further compress the feature map using quantization and BC7 [15]. The compressed feature maps and the MLP serve as the final parameters of our model. At runtime, they are streamed and decompressed on demand via a virtual texturing system to deliver high-quality dynamic global illumination (Section 3.5).\", \"page\": 4, \"index\": 5, \"width\": 1036, \"height\": 361}]"
motivation: 旨在解决动态光照环境下预计算多组全局光照贴图所带来的巨大存储与内存负担。
method: 采用多维特征图与轻量级神经网络融合时域信息，并结合块压缩模拟训练及虚拟纹理系统。
result: 实现了极高的压缩比和低内存占用，同时保持了高质量的动态全局光照渲染效果。
conclusion: NDGI 是一种高效的动态光照贴图压缩方案，在渲染质量、存储效率与实时性能之间取得了良好平衡。
---

## 摘要
实时渲染中的高质量全局光照（GI）通常使用预计算光照技术实现，其中光照贴图（lightmap）是标准选择。为了支持动态光照环境下静态物体的全局光照，需要预计算不同光照条件下的多组光照贴图，这会产生巨大的存储和内存开销。为了克服这一限制，我们提出了 Neural Dynamic GI (NDGI)，这是一种专门为时序光照贴图集设计的创新压缩技术。我们的方法利用多维特征图和轻量级神经网络来整合时序信息，而不是显式地存储多组贴图，从而显著减小了光照贴图的存储大小。此外，我们在训练过程中引入了块压缩（BC）模拟策略，使得最终生成的特征图能够进行 BC 压缩，进一步提高了压缩率。为了实现高效的实时解压缩，我们还将虚拟纹理（VT）系统与我们的神经表示相结合。与现有方法相比，我们的方法在保持极低存储和内存需求的同时，实现了高质量的动态全局光照，且实时解压缩开销较小。为了促进该方向的进一步研究，我们将发布在包含多种时序变化的多个场景中预计算的时序光照贴图数据集。

## Abstract
High-quality global illumination (GI) in real-time rendering is commonly achieved using precomputed lighting techniques, with lightmap as the standard choice. To support GI for static objects in dynamic lighting environments, multiple lightmaps at different lighting conditions need to be precomputed, which incurs substantial storage and memory overhead.   To overcome this limitation, we propose Neural Dynamic GI (NDGI), a novel compression technique specifically designed for temporal lightmap sets. Our method utilizes multi-dimensional feature maps and lightweight neural networks to integrate the temporal information instead of storing multiple sets explicitly, which significantly reduces the storage size of lightmaps. Additionally, we introduce a block compression (BC) simulation strategy during the training process, which enables BC compression on the final generated feature maps and further improves the compression ratio. To enable efficient real-time decompression, we also integrate a virtual texturing (VT) system with our neural representation.   Compared with prior methods, our approach achieves high-quality dynamic GI while maintaining remarkably low storage and memory requirements, with only modest real-time decompression overhead. To facilitate further research in this direction, we will release our temporal lightmap dataset precomputed in multiple scenes featuring diverse temporal variations.

---

## 论文详细总结（自动生成）

以下是对论文《Neural Dynamic GI: Random-Access Neural Compression for Temporal Lightmaps in Dynamic Lighting Environments》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在实时渲染中，预计算光照贴图（Lightmaps）是实现高质量全局光照（GI）的标准手段。然而，为了支持动态光照（如昼夜交替、灯光开关），必须预计算并存储多套对应不同时间点的贴图。
*   **核心痛点**：显式存储大量时序贴图会导致巨大的磁盘空间占用和运行时显存压力，这限制了动态 GI 在大规模游戏场景中的应用。
*   **研究目标**：提出一种名为 **NDGI** 的神经压缩框架，旨在以极低的存储成本实现支持随机存取的动态光照贴图重建。

### 2. 方法论
*   **核心思想**：不再直接存储多帧图像，而是将时序光照信息编码进一组**混合特征图（Hybrid Feature Maps）**中，并利用轻量级 **MLP 解码器**在运行时根据坐标 $(u, v)$ 和时间 $t$ 实时重建光照值。
*   **关键技术细节**：
    *   **混合特征表示**：结合了 2D 空间特征图（捕捉静态细节）、3D 时空特征图（捕捉亮度随时间的变化）以及三平面（Triplane）投影特征（$F_{ut}, F_{vt}$），以高效处理高频时空信号。
    *   **块压缩（BC）模拟策略**：在训练阶段引入模拟硬件 BC7 压缩的端点和权重插值逻辑。通过在训练中加入噪声和量化模拟，确保生成的特征图在后续应用硬件压缩时不会出现明显的质量退化。
    *   **虚拟纹理（VT）集成**：采用分块（Tile-based）训练模式，将 NDGI 嵌入 VT 系统。运行时仅对当前视口可见的贴图块进行神经解码，并利用 VT 的缓存机制避免重复计算。
*   **算法流程**：输入 $(u, v, t) \rightarrow$ 采样混合特征图 $\rightarrow$ 拼接时间频率编码 $\rightarrow$ 轻量级 MLP 解码 $\rightarrow$ 输出 RGB 光照值。

### 3. 实验设计
*   **实验场景**：使用了四个代表性场景：**FarmLand**（复杂户外）、**Room**（室内昼夜）、**City**（城市灯光切换）、**Yard**（多局部光源）。
*   **对比基准（Benchmark）**：
    *   **传统硬件压缩**：BC6H（HDR）、BC7（LDR）、ASTC（多种比特率）。
    *   **神经压缩前沿**：NTC（Neural Texture Compression）。
    *   **经典动态技术**：PRT（Precomputed Radiance Transfer，预计算辐射传输）。
*   **评价指标**：PSNR、SSIM、LPIPS（感知质量）以及 BPP（每像素位数/压缩率）。

### 4. 资源与算力
*   **算力设备**：推理性能在 **NVIDIA RTX 4060 GPU** 上进行了测试。
*   **训练细节**：使用 PyTorch 框架，采用 Adam 优化器。论文提到为了提高效率使用了批量矩阵运算（batched matrix operations）进行并行训练。
*   **训练时长**：文中未明确给出单个场景的总训练小时数，但指出由于每个 Tile 需要独立训练，在大规模场景下可能涉及数千个模型的并行训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同类型的光照环境进行了详尽测试，涵盖了平滑的时间变化（太阳移动）和突发的光照变化（灯光开关）。
*   **消融实验**：做了多组消融实验，分别验证了“混合特征图结构”对比“纯 2D/3D 特征”、“有无 BC 模拟策略”以及“三平面表示”的有效性。
*   **客观性**：实验对比了不同比特率（BPP）下的质量曲线，证明了 NDGI 在同等存储占用下显著优于现有技术，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **高压缩比**：NDGI 在比特率低于 1.0 BPP 时，重建质量（PSNR）显著超过了 BC7 和 ASTC。
*   **视觉优势**：相比 NTC，NDGI 产生的噪声更少；相比 PRT，NDGI 能更准确地还原多跳间接光和复杂的阴影过渡。
*   **实时可行性**：在 RTX 4060 上，1024x1024 贴图的解码延迟仅为 0.2ms 左右，完全满足实时渲染（>60 FPS）的需求。

### 7. 优点
*   **时空整合**：首次将神经压缩应用于“时序”光照贴图，解决了动态 GI 的存储瓶颈。
*   **硬件友好**：通过 BC 模拟策略，使神经特征图能直接利用 GPU 硬件纹理压缩单元，进一步压低了存储开销。
*   **随机存取**：支持对贴图任意位置、任意时间的即时访问，无需解压整个序列。

### 8. 不足与局限
*   **训练开销**：虽然推理很快，但预计算阶段需要为每个贴图块训练神经网络，对于超大规模场景，离线处理时间可能较长。
*   **更新延迟**：在极端的瞬时光照切换下，由于 VT 系统的流式加载机制，可能会存在极短的视觉更新延迟。
*   **应用限制**：目前主要针对静态几何体，对于全动态物体的支持仍需结合其他探针技术。

（完）
