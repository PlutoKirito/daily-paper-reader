---
title: Generative Semantic Communication via Alternating Dual-Domain Posterior Sampling
title_zh: 基于交替双域后验采样的生成式语义通信
authors: "Shunpu Tang, Qianqian Yang"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16796v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 利用基于扩散的先验进行图像传输的生成式语义通信
tldr: 本研究针对生成式语义通信中现有方法无法保持数据分布且感知质量受限的问题，提出了一种基于交替双域后验采样（ADDPS）的扩散模型接收机。该方法将语义解码建模为贝叶斯逆问题，通过在采样过程中交替执行潜空间和图像域的一致性约束，有效克服了信道噪声敏感性和解码器偏差，显著提升了无线图像传输的感知质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16796v1/fig-001.webp\", \"caption\": \"Fig. 1: Illustration of the proposed ADDPS method, where the reverse diffusion process alternates between Z-domain and X-domain guidance to enforce consistency in both domains while avoiding noise amplification in Z-domain guidance.\", \"page\": 3, \"index\": 1, \"width\": 712, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2604.16796v1/fig-002.webp\", \"caption\": \"TABLE I: Comparison of different methods at SNR = 1 dB.\", \"page\": 5, \"index\": 2, \"width\": 936, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2604.16796v1/fig-003.webp\", \"caption\": \"TABLE II: Ablation Study of the Proposed Method\", \"page\": 6, \"index\": 3, \"width\": 1018, \"height\": 200}]"
motivation: 现有的生成式语义通信接收机因采用最大后验估计而无法保持数据分布，且单域引导方法存在噪声敏感或解码器偏差等局限。
method: 提出ADDPS算法，通过在扩散采样过程中交替应用潜域和图像域的一致性引导，将复杂的联合后验采样分解为更简单的子问题。
result: 在FFHQ数据集上的实验表明，ADDPS在感知质量方面优于现有的生成式语义通信方法。
conclusion: 证明了后验采样在保持数据分布方面的优越性，并验证了交替双域策略在提升语义通信感知性能上的有效性。
---

## 摘要
生成式语义通信（SemCom）利用预训练的生成先验来提高无线图像传输的感知质量。然而，现有的生成式 SemCom 接收机依赖于最大后验（MAP）估计，这在根本上无法保持数据分布，从而限制了可实现的感知质量。此外，目前使用单域引导的基于扩散的方法面临重大局限：潜域引导对信道噪声敏感，而图像域引导则继承了解码器偏差。简单地同时结合这两个域会产生过度自信的伪后验。在本文中，我们将语义解码建模为一个贝叶斯逆问题，并证明后验采样通过保持数据分布实现了最优的感知质量。基于这一见解，我们提出了交替双域后验采样（ADDPS），这是一种基于扩散的 SemCom 接收机，在采样过程中交替强制执行潜域和图像域的一致性。这种交替策略将联合后验采样分解为更简单的子问题，在保留两个域互补优势的同时避免了梯度冲突。在 FFHQ 数据集上的实验表明，与现有方法相比，所提出的 ADDPS 实现了更优的感知质量。

## Abstract
Generative semantic communication (SemCom) harnesses pretrained generative priors to improve the perceptual quality of wireless image transmission. Existing generative SemCom receivers, however, rely on maximum a posteriori (MAP) estimation, which fundamentally cannot preserve the data distribution and thus limits achievable perceptual quality. Moreover, current diffusion-based approaches using single-domain guidance face significant limitations: latent-domain guidance is sensitive to channel noise, while image-domain guidance inherits decoder bias. Simply combining both domains simultaneously yields an overconfident pseudo-posterior. In this paper, we formulate semantic decoding as a Bayesian inverse problem and prove that posterior sampling achieves optimal perceptual quality by preserving the data distribution. Building on this insight, we propose alternating dual-domain posterior sampling (ADDPS), a diffusion-based SemCom receiver that alternately enforces latent-domain and image-domain consistency during the sampling process. This alternating strategy decomposes joint posterior sampling into simpler subproblems, avoiding gradient conflicts while retaining the complementary strengths of both domains. Experiments on FFHQ demonstrate that the proposed ADDPS achieves superior perceptual quality compared with existing methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ADDPS（交替双域后验采样）** 的生成式语义通信框架，旨在解决无线图像传输中感知质量与失真之间的权衡问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：语义通信（SemCom）通过传输任务相关的语义信息而非原始比特，极大地提高了通信效率。生成式语义通信利用预训练的生成模型（如扩散模型）作为先验，能够显著提升重建图像的视觉效果。
*   **核心问题**：
    1.  **MAP 估计的局限性**：现有方法多采用最大后验概率（MAP）估计，这在数学上会导致重建图像的分布向均值收缩，造成图像模糊，无法保持原始数据的分布特性。
    2.  **单域引导的缺陷**：在扩散模型采样过程中，仅使用潜域（Z-domain）引导容易受到信道噪声干扰；仅使用图像域（X-domain）引导则会继承解码器的偏差并丢失部分语义细节。
    3.  **简单融合的问题**：同时使用双域引导会导致“过度自信”的伪后验，产生梯度冲突，影响采样稳定性。

### 2. 方法论
*   **核心思想**：将语义解码建模为**贝叶斯逆问题**，并证明**后验采样**（而非 MAP）在理论上能实现最优的感知质量（即重建分布与原始分布一致）。
*   **关键技术细节**：
    *   **双域一致性建模**：同时考虑潜域似然（确保接收信号与编码特征一致）和图像域似然（利用预训练解码器的鲁棒性）。
    *   **交替引导策略（ADDPS）**：在扩散模型的反向采样过程中，不采用同时叠加梯度的方法，而是**交替**应用潜域梯度和图像域梯度。
    *   **算法流程**：
        1.  使用语义解码器获得初始重建图像 $\tilde{x}$。
        2.  在扩散采样的每一步，利用 Tweedie 公式预测清晰图像 $\hat{x}_0$。
        3.  **偶数步**：计算潜域一致性梯度，纠正语义偏差。
        4.  **奇数步**：计算图像域一致性梯度，利用解码器的鲁棒性抑制噪声。
        5.  通过这种交替迭代，逐步细化生成结果。

### 3. 实验设计
*   **数据集**：FFHQ（人脸数据集），分辨率固定为 $256 \times 256$。
*   **通信场景**：AWGN（加性高斯白噪声）信道，设定在极具挑战性的低信噪比（SNR = 1 dB）和高带宽压缩比（BCR = 1/192）条件下。
*   **对比方法（Benchmark）**：
    *   **DeepJSCC**：基于 MSE 训练的经典深度联合源信道编码。
    *   **DeepJSCC-LPIPS**：引入感知损失训练的 DeepJSCC。
    *   **InverseJSCC**：基于 GAN 逆映射的生成式方法。
    *   **HiFi-DiffCom**：目前最先进的基于扩散模型的语义通信方法（SOTA）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   **实现细节**：提到使用了预训练的扩散模型和语义编解码器骨干网络，这表明该方法在推理阶段（接收端）具有一定的计算开销，因为需要进行多次扩散迭代（T=300 或 T=1000）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比，涵盖了失真指标（PSNR, MS-SSIM）和感知指标（FID, PIEAPP, DISTS, LPIPS）。
    *   **消融实验**：对比了“仅图像域”、“仅潜域”、“双域同时引导”以及“ADDPS 交替引导”四种变体。
*   **充分性评价**：实验设计较为客观，特别是在极低信噪比（-1 dB 和 1 dB）下的测试证明了算法的鲁棒性。消融实验有力地支撑了“交替引导”优于“同时引导”的论点。但实验目前仅局限于人脸数据集（FFHQ），缺乏在更通用数据集（如 ImageNet）上的验证。

### 6. 主要结论与发现
*   **感知质量最优**：ADDPS 在 FID 指标上表现最佳（56.94），显著优于 HiFi-DiffCom 和其他基准方法，证明了后验采样的有效性。
*   **分布保持**：理论证明和实验结果均证实，后验采样比 MAP 估计更能保持图像的统计分布，减少模糊感。
*   **交替策略的优越性**：消融实验显示，简单的双域相加（X+Z）在感知指标上甚至不如单域引导，而交替策略成功融合了潜域的忠实度和图像域的鲁棒性。

### 7. 优点
*   **理论支撑扎实**：从贝叶斯逆问题的角度重新审视语义解码，并给出了关于分布保持的数学证明。
*   **即插即用**：作为一种接收端算法，它可以直接配合预训练的扩散模型使用，无需对发送端进行大规模重训。
*   **创新性引导方案**：提出的交替引导策略巧妙地避开了梯度冲突和伪后验问题，为扩散模型处理多约束逆问题提供了新思路。

### 8. 不足与局限
*   **计算复杂度**：扩散模型需要多次迭代（如 1000 步），这在实时通信系统中可能存在较高的延迟挑战。
*   **泛化性验证不足**：实验仅在 FFHQ 人脸数据集上进行，人脸具有极强的结构先验，该方法在背景复杂、类别多样的通用图像上的表现尚待观察。
*   **算力信息缺失**：未提供训练和推理的具体资源消耗，难以评估其在移动设备上的部署可行性。

（完）
