---
title: Singular Relative Entropy Coding with Bits-Back Rejection Sampling
title_zh: 基于 Bits-Back 拒绝采样的奇异相对熵编码
authors: "Gergely Flamich, Spencer Hill"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06055v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 相对熵编码和源样本的随机编码
tldr: 本文针对相对熵编码（REC）中奇异信道的效率问题，提出了一种名为“Bits-Back 拒绝采样器”（BBRS）的新型编码方法。REC 旨在以接近互信息的比特数编码随机样本，但在奇异信道下通常存在对数级冗余。BBRS 结合了 bits-back 编码与贪婪拒绝采样技术，不仅在理论上实现了与前人研究一致的亚对数渐近冗余，且具有更简单的分析过程和更优的常数项，最重要的是其具备实际可操作性。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的奇异信道相对熵编码方法虽然能降低冗余，但其算法过于复杂且无法在实际中部署。
method: 提出 BBRS 算法，通过融合 bits-back 编码思想与贪婪拒绝采样机制来构建高效的相对熵代码。
result: BBRS 在奇异信道上达到了亚对数级的渐近冗余，且相比前人工作具有更简单的分析逻辑和更优的性能常数。
conclusion: BBRS 为奇异信道下的相对熵编码提供了一个兼具理论优越性和实践可行性的新方案。
---

## 摘要
针对信源 $X \sim P_X$ 的相对熵编码是一种随机编码，旨在以尽可能少的比特数对来自指定分布 $P_{Y \mid X}$ 的随机样本进行编码。作为熵编码的一种推广，一个标准结论是实现该目标所需的最小比特数至少为互信息 $I[X\,\Vert\,Y]$。然而，与熵编码相比，相对熵编码的一个特别引人注目的特征是，通常情况下，该下界只能在增加一个额外对数因子的范围内实现。因此，一个重要的研究方向是识别能够缩小这一差距的信道。Sriramu 和 Wagner 通过为所谓的奇异信道展示一种具有亚对数渐近冗余的相对熵编码，取得了此类成功。然而，他们的编码非常复杂，且遗憾的是无法在实践中实现。在本文中，我们构建了 Bits-Back 拒绝采样器（BBRS），这是一种结合了 Bits-Back 编码和（贪婪）拒绝采样思想的相对熵编码。我们对 BBRS 的分析表明，该算法达到了与 Sriramu 和 Wagner 采样器相同的渐近效率，但分析更简单且常数项更优。此外，BBRS 可以使用标准的相对熵编码方法来实现。

## Abstract
A relative entropy code for a source $X \sim P_X$ is a stochastic code that encodes random samples from a prescribed $P_{Y \mid X}$ using as few bits as possible. A generalisation of entropy coding, it is a standard result that the minimum number of bits required to achieve this is at least the mutual information $I[X\,\Vert\,Y]$. However, a particularly fascinating feature of relative entropy coding compared to entropy coding is that, in general, this lower bound is only achievable to within an additional logarithmic factor. As such, an important research direction is to identify channels where we can reduce this gap.   Sriramu and Wagner achieved such success by exhibiting a relative entropy code for so-called singular channels with sub-logarithmic asymptotic redundancy. However, their code is quite involved and, sadly, cannot be implemented in practice.   In this paper, we construct the bits-back rejection sampler (BBRS), a relative entropy code that combines ideas from bits-back coding and (greedy) rejection sampling. Our analysis of BBRS reveals that the algorithm achieves the same asymptotic efficiency as Sriramu and Wagner's sampler, but with much simpler analysis and better constants. Moreover, BBRS can be implemented using standard relative entropy coding methods.

---

## 论文详细总结（自动生成）

这篇论文由伦敦帝国理工学院和女王大学的研究人员撰写，提出了一种针对“奇异信道”（Singular Channels）的高效相对熵编码方案。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在信息论中，相对熵编码（Relative Entropy Coding, REC）的目标是使用尽可能少的比特数来模拟一个给定输入 $X$ 的条件分布 $P_{Y|X}$。虽然理论下界是互信息 $I[X;Y]$，但通常存在一个无法消除的对数级冗余（即 $I + \log I$）。
*   **研究动机**：Sriramu 和 Wagner (2024) 证明了对于“奇异信道”，这种渐近对数冗余可以降低到零。然而，他们提出的原始算法极其复杂，且在实际中无法计算和部署。
*   **整体含义**：本文旨在提出一种名为 **Bits-Back 拒绝采样器（BBRS）** 的新算法，它不仅在理论上达到了最优的零渐近对数冗余，而且结构简单、常数项更小，并且可以使用现有的流编码技术（如 ANS）进行实际实现。

### 2. 论文提出的方法论：核心思想与关键技术
BBRS 结合了 **Bits-Back 编码**、**贪婪拒绝采样（GRS）** 和 **奇异信道** 的特性。其核心流程如下：
*   **奇异信道定义**：信道 $X \to Y$ 被称为奇异的，如果其似然比 $dP_{Y|X}/dP_Y$ 仅取决于输出 $Y$（即存在可测函数 $g$ 使得似然比等于 $g(Y)$）。
*   **核心思想**：利用奇异信道的特性，从输出 $Y$ 中可以完全恢复量化后的对数密度比 $\Gamma$。这使得发送方可以利用 Bits-Back 机制，在编码过程中“回收”一部分用于选择样本的比特。
*   **算法流程**：
    1.  **量化**：对对数密度比进行 $\Delta$ 精度的量化，得到 $\Gamma$。
    2.  **第一阶段采样（GRS）**：使用贪婪拒绝采样从 $P_{Y|\Gamma}$ 中抽取一个样本 $Y'$。
    3.  **Bits-Back 机制**：由于信道是奇异的，解码器在获得 $Y'$ 后可以重新计算出 $\Gamma$。因此，发送方可以从消息流中解码（回收）出 GRS 过程中的接受/拒绝决策比特。
    4.  **第二阶段采样（拒绝采样）**：使用简单的拒绝采样从 $P_{Y|X,\Gamma}$ 中抽取最终样本 $Y$。
*   **关键公式**：单次编码的期望长度满足 $E[|enc(X, Z)|] \leq I[X;Y] + \log(H[\Gamma]+1) + 2\Delta + 5$。

### 3. 实验设计
*   **场景与基准**：本论文主要是一篇**理论证明型**论文。其“实验”主要体现在对算法速率（Rate）的数学推导和渐近分析上。
*   **对比对象**：主要的对比基准是 **Sriramu-Wagner (SW) 编码**。
*   **对比维度**：
    *   **渐近冗余**：两者在奇异信道下均能达到 $R_{log} = 0$。
    *   **实现复杂度**：SW 编码依赖于难以计算的理论量，而 BBRS 使用标准的 GRS 和拒绝采样，具备实践可行性。
    *   **常数项**：BBRS 具有更紧凑的单次编码速率界限。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何关于 GPU、显存或具体训练时长的信息。
*   **原因**：该研究属于信息论基础研究，侧重于算法的概率性质证明和编码效率的理论推导，不涉及深度学习模型的训练。

### 5. 实验数量与充分性
*   **分析**：作为理论论文，其充分性体现在数学证明的严密性上。
*   **证明内容**：论文详细推导了 BBRS 的单次编码速率（Theorem 1），并利用中心极限定理证明了在 $n$ 次独立重复实验下，其对数冗余随 $n$ 增长的速率（Corollary 3）。
*   **客观性**：作者客观地指出了该方法在非奇异信道下的局限性，并承认算法仍依赖于对数密度比的任意离散化（量化）。

### 6. 论文的主要结论与发现
*   **结论一**：BBRS 算法在奇异信道下实现了零渐近对数冗余，验证了奇异信道在相对熵编码中的特殊地位。
*   **结论二**：通过 Bits-Back 机制，可以将原本复杂的奇异信道模拟问题转化为一个简单的“错误纠正”过程（从 $Y$ 恢复 $\Gamma$）。
*   **结论三**：BBRS 的单次编码效率优于之前的 SW 方案，且更易于在现代压缩框架（如 ANS）中实现。

### 7. 优点
*   **理论简洁性**：将 Bits-Back 这一经典压缩思想巧妙地引入相对熵编码，解释了为什么奇异信道能达到更低的冗余。
*   **实用性**：相比于之前纯理论的构造，BBRS 提供了伪代码（Algorithm 2 & 3），为实际开发相对熵编码器提供了路线图。
*   **性能优化**：通过更精细的分析，降低了编码速率中的常数项。

### 8. 不足与局限
*   **应用范围限制**：该算法的优越性（零对数冗余）仅限于“奇异信道”。对于非奇异信道，其效率并不比现有方法高。
*   **量化依赖**：算法性能受限于量化精度 $\Delta$ 的选择，论文尚未完全消除这一超参数的影响。
*   **缺乏大规模实测**：虽然理论上可实现，但论文未提供在真实数据集（如图像或视频压缩）上的性能评测数据。

（完）
