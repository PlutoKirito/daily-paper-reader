---
title: "Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs"
title_zh: 别浪费比特！面向轻量级端侧大语言模型的自适应 KV 缓存量化
authors: "Sayed Pedram Haeri Boroujeni, Niloufar Mehrabi, Patrick Woods, Gabriel Hillesheim, Abolfazl Razi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04722v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 受哈夫曼编码原理启发的自适应量化
tldr: 针对端侧设备上大语言模型推理受限于KV缓存内存和带宽的问题，本文提出了一种自适应KV缓存量化方法。受哈夫曼编码启发，该方法通过学习到的策略动态分配不同位宽（2/4/8-bit或FP16），将更多比特分配给重要性高的Token。实验表明，该方法在SmolLM系列模型上显著降低了内存占用和延迟，同时保持了接近FP16的推理精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04722v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2048, \"height\": 1116}, {\"url\": \"assets/figures/arxiv/2604.04722v1/fig-002.webp\", \"caption\": \"\", \"page\": 1, \"index\": 2, \"width\": 2048, \"height\": 1569}]"
motivation: 现有的KV缓存量化方案多采用固定精度或手工启发式规则，导致低影响Token浪费比特而关键Token压缩过度，影响了端侧部署的效率与精度。
method: 提出一种基于数据驱动的轻量级控制器，提取Token频率、质量得分、注意力方差等特征，动态决定每个Token的KV缓存量化精度。
result: "在SmolLM-360M模型上，相比静态量化，该方法在HellaSwag任务中延迟降低了17.75%，准确率提升了7.60个百分点，且与FP16精度差距极小。"
conclusion: 自适应KV缓存量化策略能有效优化端侧LLM的准确率与延迟权衡，是实现轻量化、高性能端侧推理的有效途径。
---

## 摘要
大语言模型（LLMs）在推理、生成和决策任务中取得了显著进展，但在移动、嵌入式和边缘设备上部署它们仍然面临巨大挑战。端侧 LLM 推理受到键值（KV）缓存的内存和带宽开销的严重限制，该开销随上下文长度线性增长，且通常在解码成本中占据主导地位。现有的 KV 缓存量化方案通常依赖于固定精度或手工设计的启发式方法，从而在低影响的 token 上浪费比特，同时对信息量大的 token 过度压缩，导致了本可避免的精度下降。受哈夫曼编码（Huffman coding）变长分配原则的启发，我们提出了自适应 KV 缓存量化，这是一种学习到的策略，根据 token 的重要性按比例分配位宽，在不牺牲竞争性精度的前提下，最小化预期的内存占用和延迟。我们的框架提取轻量级的 token 级特征，包括 token 频率、质量得分、注意力方差和基于熵的不确定性，并将它们输入到一个紧凑的数据驱动控制器中，该控制器在解码过程中动态地从 {2-bit, 4-bit, 8-bit, FP16} 中选择 KV 精度。与静态 KV 量化和基于规则的基准方法相比，这种自适应精度策略减少了 KV 内存占用和延迟，同时提高了精度，并在标准 LLM 基准测试中保持了接近 FP16 推理的竞争性精度。在 SmolLM-135M、SmolLM-360M 和 SmolLM-1.7B 上进行的多个常识推理基准测试的大量实验表明，我们的控制器持续改进了精度与延迟之间的权衡。例如，在 HellaSwag 基准测试中使用 SmolLM-360M 时，我们的方法相对于静态 KV 量化将解码延迟（ms/token）降低了 17.75%，精度提高了 7.60 个百分点，且与 FP16 推理的差距仅在 0.30 个百分点以内。

## Abstract
Large Language Models (LLMs) have achieved remarkable progress across reasoning, generation, and decision-making tasks, yet deploying them on mobile, embedded, and edge devices remains particularly challenging. On-device LLM inference is heavily constrained by the memory and bandwidth overhead of the key-value (KV) cache, which grows linearly with context length and often dominates decoding cost. Existing KV-cache quantization schemes typically rely on fixed precision or hand-crafted heuristics, thereby wasting bits on low-impact tokens while over-compressing informative ones, leading to avoidable accuracy degradation. Inspired by Huffman coding's principle of variable-length allocation, we propose adaptive KV-cache quantization, a learned policy that assigns bit-width proportional to token importance, minimizing expected memory and latency without sacrificing competitive accuracy. Our framework extracts lightweight token-level features, including token frequency, quality score, attention variance, and entropy-based uncertainty, and feeds them into a compact data-driven controller that dynamically selects KV precision from {2-bit, 4-bit, 8-bit, FP16} during decoding. This adaptive precision policy reduces KV memory footprint and latency while improving accuracy compared to static KV quantization and rule-based baselines, and maintaining competitive accuracy close to FP16 inference across standard LLM benchmarks. Extensive experiments across multiple commonsense reasoning benchmarks using SmolLM-135M, SmolLM-360M, and SmolLM-1.7B demonstrate that our controller consistently improves the accuracy-latency trade-off. For instance, with SmolLM-360M on HellaSwag, our method reduces decoding latency (ms/token) by 17.75% relative to static KV quantization, improves accuracy by 7.60 points, and remains within only 0.30 points of FP16 inference.

---

## 论文详细总结（自动生成）

这篇论文《Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs》提出了一种针对端侧大语言模型（LLM）推理优化的自适应KV缓存量化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心瓶颈**：在端侧设备（如手机、嵌入式设备）上，LLM推理的主要障碍是**KV缓存（Key-Value Cache）**。它随上下文长度线性增长，占据了大量的内存和带宽。
*   **现有方案缺陷**：目前的KV缓存量化多采用**静态精度**（如统一4-bit）或**手工启发式规则**。这导致了“比特浪费”：对不重要的Token分配了过多比特，而对关键Token压缩过度，从而引发严重的推理精度下降。
*   **研究动机**：受哈夫曼编码（变长编码）启发，作者认为不同的Token对未来生成的贡献不同，应当根据Token的重要性动态分配量化比特数。

### 2. 方法论
*   **核心思想**：引入一个数据驱动的轻量级控制器，在解码过程中实时预测每个Token的重要性，并从{2, 4, 8, 16}位中选择最合适的量化精度。
*   **关键技术细节**：
    *   **特征提取（Saliency Features）**：提取4维轻量级特征：
        1.  **熵（Entropy）**：衡量预测分布的不确定性。
        2.  **稀有度（Rarity）**：基于Token频率的自信息量，识别罕见但关键的语义信息。
        3.  **注意力方差（Attention Variance）**：反映注意力分布的尖锐程度。
        4.  **置信度（Confidence）**：模型对当前预测的确定程度。
    *   **控制器架构**：采用一个极轻量的**三层MLP**（隐藏层维度128），确保预测精度的开销远小于量化带来的收益。
    *   **训练目标**：损失函数由三部分组成：分类交叉熵（预测准确性）+ 延迟惩罚（鼓励低比特）+ 质量惩罚（保护高精度需求）。
*   **算法流程**：在生成每个新Token时，提取其特征 -> MLP控制器输出精度类别 -> 对该Token的K和V向量进行相应位宽量化 -> 存入KV缓存。

### 3. 实验设计
*   **模型背书**：使用了HuggingFace的 **SmolLM系列**（135M, 360M, 1.7B参数量）。
*   **数据集/基准测试**：
    *   **HellaSwag**（常识推理）
    *   **OpenBookQA (OBQA)**（科学问答）
    *   **ARC-Challenge**（挑战性科学推理）
*   **对比方法**：
    *   **FP16**（全精度基准）
    *   **Static 4-bit KV**（静态量化）
    *   **Rule-based Dynamic KV**（基于手工规则的动态量化）
    *   **其他LLM家族**（如OPT, Pythia, Galactica等）作为横向性能参考。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 4090 GPU (24 GB)** 上完成。
*   **训练细节**：针对不同规模的模型设置了不同的学习率和Batch Size（如1.7B模型使用2M BS）。虽然论文未明确给出总训练时长，但强调了控制器训练是基于提取的特征样本进行的，属于轻量级训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从135M到1.7B三个量级的模型，并在三个主流基准上进行了零样本（Zero-shot）评估。
*   **充分性评价**：实验设计较为充分。作者不仅对比了量化基准，还通过与OPT、Pythia等同量级模型的横向对比，证明了该方法结合SmolLM后在端侧模型中的领先地位。实验结果同时涵盖了**精度（Accuracy）**和**延迟（Latency）**两个核心维度，体现了客观性。

### 6. 主要结论与发现
*   **性能提升**：在SmolLM-360M上，该方法相比静态4-bit量化，在HellaSwag任务中**延迟降低了17.75%**，同时**精度提升了7.60个百分点**。
*   **逼近上限**：自适应量化后的模型精度非常接近FP16（差距通常在0.5%以内），但内存占用和延迟显著降低。
*   **帕累托优化**：证明了通过动态分配比特，可以在相同的内存预算下获得比静态量化更高的推理质量。

### 7. 优点
*   **理论优雅**：将哈夫曼编码思想引入KV缓存管理，逻辑自洽且符合直觉。
*   **极低开销**：控制器仅为微型MLP，特征提取也基于已有的Logits和Attention，几乎不增加推理负担。
*   **即插即用**：不需要对原始LLM进行微调或架构修改，具有很强的通用性。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在1.7B以下的小模型，尚未验证在7B、14B或更大规模模型上的扩展性。
*   **长文本验证不足**：虽然针对KV缓存，但测试集多为短文本推理，未深入探讨在数万Token超长文本下的累积误差或稳定性。
*   **硬件实测缺失**：实验在4090 GPU上完成，虽然模拟了延迟，但未在真实的移动端SoC（如骁龙或苹果A系列芯片）上进行底层算子加速的实测。

（完）
