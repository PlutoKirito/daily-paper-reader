---
title: "Prune-Quantize-Distill: An Ordered Pipeline for Efficient Neural Network Compression"
title_zh: 剪枝-量化-蒸馏：一种用于高效神经网络压缩的有序流水线
authors: "Longsheng Zhou, Yu Shen"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04988v1"
tags: ["query:train-trick"]
score: 9.0
evidence: 剪枝、量化和蒸馏的有序流水线
tldr: 本研究针对模型压缩中参数量与实际推理延迟不匹配的问题，提出了一种有序的压缩流水线：非结构化剪枝、INT8量化感知训练（QAT）和知识蒸馏（KD）。实验表明，该组合在保持高精度的同时显著降低了CPU延迟和模型体积。通过在CIFAR数据集上的多种架构验证，证明了该顺序在优化精度-尺寸-延迟平衡点上的优越性，为边缘设备部署提供了实用指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04988v1/fig-001.webp\", \"caption\": \"Fig. 2: Synergy of the ordered hybrid method. Left: limitations of single-step pruning-only, quantization-only, and KD-only strategies, each optimizing mainly one axis (accuracy or efficiency). Right: our three-stage ordering, where pruning shrinks the active set, quantization converts sparsity into real efficiency gains, and KD recovers accuracy in the compressed space, moving the model toward the Pareto front in the accuracy–efficiency plane.\", \"page\": 5, \"index\": 1, \"width\": 1022, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.04988v1/fig-002.webp\", \"caption\": \"Fig. 4: Global analysis on ResNet-18/CIFAR-10: ROC/PR curves and the 3D accuracy–size–latency trade-off.\", \"page\": 7, \"index\": 2, \"width\": 1076, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.04988v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of the proposed hybrid compression pipeline. A dense FP32 teacher with weights W(0) is first pruned into a sparse FP32 student (W(p) = M⊙W(0)), then optimized under INT8 quantization (PTQ/QAT) to obtain a sparse INT8 student W(q) = Q8(W (p)), and finally refined via knowledge distillation to produce the hybrid student W(h) in the compressed space.\", \"page\": 3, \"index\": 3, \"width\": 1022, \"height\": 320}]"
motivation: 传统的压缩指标（如参数量和FLOPs）无法准确反映实际推理时间，且非结构化剪枝在CPU上往往难以实现真正的加速。
method: 提出一种按序执行的压缩流程，先通过剪枝降低模型容量，再进行INT8量化感知训练以获得主要加速，最后利用知识蒸馏恢复精度。
result: 在ResNet和VGG等模型上实现了0.99-1.42毫秒的极低CPU延迟，且在精度-尺寸-延迟的权衡上优于任何单一压缩技术。
conclusion: 压缩技术的执行顺序对最终性能至关重要，边缘部署应以实测延迟而非代理指标作为评估核心。
---

## 摘要
现代部署通常需要在严格的 CPU 和内存限制下权衡准确率与效率，然而常见的压缩代理指标（如参数量或 FLOPs）并不能可靠地预测实际推理时间。特别是，非结构化稀疏虽然可以减少模型存储，但由于不规则的内存访问和稀疏算子开销，往往无法加速（有时甚至会略微减慢）标准的 CPU 执行。受这种压缩与加速之间差距的启发，我们研究了一种以实测延迟为目标的实用有序流水线，结合了三种广泛使用的技术：非结构化剪枝、INT8 量化感知训练 (QAT) 和知识蒸馏 (KD)。实验表明，INT8 QAT 提供了主要的运行时间收益，而剪枝主要作为一种降低容量的预处理器，提高了后续低精度优化的鲁棒性；最后应用的 KD 在不改变部署形式的情况下，在已经受限的稀疏 INT8 状态下恢复了准确率。我们在 CIFAR-10/100 上使用三种骨干网络（ResNet-18、WRN-28-10 和 VGG-16-BN）进行了评估。在所有设置中，该有序流水线比任何单一技术都实现了更强的准确率-尺寸-延迟前沿，在具有竞争力的准确率和紧凑的检查点下，达到了 0.99-1.42 毫秒的 CPU 延迟。采用固定 20/40/40 轮次分配的受控顺序消融实验进一步证实，阶段顺序至关重要，所提出的顺序在测试的排列组合中通常表现最佳。总之，我们的结果为边缘部署提供了一个简单的指导方针：在准确率-尺寸-延迟的联合空间中，使用实测运行时间而非仅凭代理指标来评估压缩选择。

## Abstract
Modern deployment often requires trading accuracy for efficiency under tight CPU and memory constraints, yet common compression proxies such as parameter count or FLOPs do not reliably predict wall-clock inference time. In particular, unstructured sparsity can reduce model storage while failing to accelerate (and sometimes slightly slowing down) standard CPU execution due to irregular memory access and sparse kernel overhead. Motivated by this gap between compression and acceleration, we study a practical, ordered pipeline that targets measured latency by combining three widely used techniques: unstructured pruning, INT8 quantization-aware training (QAT), and knowledge distillation (KD). Empirically, INT8 QAT provides the dominant runtime benefit, while pruning mainly acts as a capacity-reduction pre-conditioner that improves the robustness of subsequent low-precision optimization; KD, applied last, recovers accuracy within the already constrained sparse INT8 regime without changing the deployment form. We evaluate on CIFAR-10/100 using three backbones (ResNet-18, WRN-28-10, and VGG-16-BN). Across all settings, the ordered pipeline achieves a stronger accuracy-size-latency frontier than any single technique alone, reaching 0.99-1.42 ms CPU latency with competitive accuracy and compact checkpoints. Controlled ordering ablations with a fixed 20/40/40 epoch allocation further confirm that stage order is consequential, with the proposed ordering generally performing best among the tested permutations. Overall, our results provide a simple guideline for edge deployment: evaluate compression choices in the joint accuracy-size-latency space using measured runtime, rather than proxy metrics alone.

---

## 论文详细总结（自动生成）

### 论文总结：Prune-Quantize-Distill (PQD) 有序压缩流水线

#### 1. 核心问题与整体含义（研究动机和背景）
在深度学习模型部署中，开发者常面临准确率与效率的权衡。传统的压缩评估指标（如参数量、FLOPs）往往无法真实反映模型在硬件上的**实际推理延迟（Wall-clock Latency）**。例如，非结构化剪枝虽然能大幅减少参数，但由于内存访问不规则，在标准 CPU 上可能反而变慢。
本研究的动机在于弥合“压缩指标”与“实际加速”之间的鸿沟。论文提出了一种简单且可复现的有序流水线，旨在通过组合剪枝、量化和蒸馏，在 CPU 部署环境下实现真正的性能增益。

#### 2. 方法论：核心思想与技术细节
论文提出了一个三阶段的有序流水线（**Prune → QAT → KD**），其核心思想是让不同技术各司其职：

*   **阶段 I：全局非结构化剪枝 (Global Unstructured Pruning)**
    *   **操作**：基于权重量级（Magnitude）进行全局剪枝。
    *   **作用**：并非为了直接加速，而是作为“预处理器”降低模型容量，减少活跃参数量，从而为后续的低精度优化（量化）提供更稳定的起点。
*   **阶段 II：INT8 量化感知训练 (QAT)**
    *   **操作**：使用均匀仿射量化（Uniform Affine Quantization）和直通估计器（STE）进行训练。
    *   **作用**：这是**推理加速的核心来源**。通过将权重和激活值转为 INT8 格式，利用硬件的整数运算单元实现真正的延迟降低。
*   **阶段 III：知识蒸馏 (KD)**
    *   **操作**：在已经完成剪枝和量化的受限空间内，使用原始 FP32 教师模型对稀疏 INT8 学生模型进行逻辑值（Logits）蒸馏。
    *   **作用**：作为“修复器”，在不改变模型部署尺寸和延迟的前提下，恢复因压缩导致的精度损失。

#### 3. 实验设计
*   **数据集**：CIFAR-10 和 CIFAR-100。
*   **骨干网络 (Backbones)**：ResNet-18、WideResNet-28-10 (WRN-28-10)、VGG-16-BN。此外，为了与现有文献对比，还使用了 ResNet-20。
*   **Benchmark 与对比方法**：
    *   **单项技术基准**：仅剪枝 (Prune-only)、仅量化 (QAT-only)、仅蒸馏 (KD-only)。
    *   **顺序消融实验**：对三个阶段的执行顺序进行全排列测试（如 QAT→KD→Prune 等）。
    *   **文献对比**：与 SQL、QST、GETA 等混合压缩框架在 BOPs（二进制操作数）指标上进行对比。
*   **评估指标**：准确率 (Acc)、模型序列化大小 (MB)、实测 CPU 延迟 (ms)、加速比 (Speedup)。

#### 4. 资源与算力
*   **推理测试环境**：单台 Intel Xeon (Skylake) CPU 服务器，使用 PyTorch 的 `fbgemm` 后端，固定 10 个 CPU 线程。
*   **训练配置**：总预算固定为 100 个 Epoch。在混合流水线中，按 20（剪枝）/ 40（量化）/ 40（蒸馏）的比例分配。
*   **算力说明**：论文**未明确提及**具体使用的 GPU 型号和数量，但由于实验基于 CIFAR 数据集且模型规模适中，预计单张主流商用 GPU（如 RTX 3090 或 A100）即可完成训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三种不同风格的架构（残差网络、宽残差网络、VGG）上验证了通用性，并针对 ResNet-18 做了详尽的诊断性分析。
*   **消融实验**：通过固定总训练时长（100 Epochs）并改变阶段顺序，客观地证明了“顺序”本身对最终结果的影响，排除了因训练时长不同导致的偏差。
*   **多维度评估**：引入了 3D 可视化（精度-尺寸-延迟），比单一指标更全面地展示了 Pareto 前沿，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **顺序至关重要**：**Prune → QAT → KD** 是最优顺序。先剪枝能稳定量化过程，最后蒸馏能最大化精度恢复。
*   **加速来源明确**：INT8 量化是 CPU 推理加速的主要贡献者；非结构化剪枝在没有特殊算子支持下对 CPU 加速贡献微乎其微，但有助于压缩存储。
*   **协同效应**：有序流水线在所有测试设置中均优于任何单一压缩技术，实现了更优的“精度-延迟”平衡点（CPU 延迟降至 0.99-1.42 ms）。
*   **代理指标误导性**：再次强调了 FLOPs 等指标不能完全代表实际运行时间，部署决策应基于实测延迟。

#### 7. 优点
*   **实用性强**：不依赖复杂的自定义算子或混合精度搜索，仅使用标准组件，易于在工业界复现和集成。
*   **角色分工明确**：清晰地界定了剪枝（预处理）、量化（加速）、蒸馏（修复）在流水线中的功能。
*   **部署导向**：直接在 CPU 环境下测量端到端延迟，对边缘计算部署具有直接指导意义。

#### 8. 不足与局限
*   **硬件覆盖有限**：实验主要集中在 CPU (Intel Xeon) 上，未测试在 ARM 架构（移动端）或专用加速器（NPU/DSP）上的表现。
*   **数据集规模**：主要在 CIFAR 等小型数据集上验证，缺乏在 ImageNet 等大规模视觉任务或 Transformer 等非 CNN 架构上的泛化证据。
*   **剪枝类型限制**：主要讨论非结构化剪枝，虽然提到了结构化剪枝，但未深入探讨其与量化结合时的硬件友好性差异。

（完）
