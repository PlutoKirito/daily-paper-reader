---
title: A Noise Constrained Diffusion (NC-Diffusion) Framework for High Fidelity Image Compression
title_zh: 一种用于高保真图像压缩的噪声约束扩散（NC-Diffusion）框架
authors: "Zhenyu Du, Yanbo Gao, Shuai Li, Yiyang Li, Hui Yuan, Mao Ye"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06568v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 基于扩散模型的图像压缩框架
tldr: 针对扩散模型在图像压缩中因随机噪声导致重建偏差的问题，本文提出NC-Diffusion框架。该框架创新性地将量化噪声建模为扩散的前向过程，建立了从原图到初始压缩结果的约束扩散路径，解决了噪声不匹配问题并提升了推理效率。结合自适应频域滤波和零样本引导增强，实现了极高保真度的图像压缩，在多个基准数据集上达到领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06568v1/fig-001.webp\", \"caption\": \"Fig. 14. Visual comparison between the method without the Lhigh loss term and the method with the Lhigh loss term (β is set to 0.3) on the reconstructed image kodim23 from the Kodak dataset.\", \"page\": 11, \"index\": 1, \"width\": 1004, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.06568v1/fig-002.webp\", \"caption\": \"Fig. 12. The visual comparison between our method and the baseline on the reconstructed image kodim07 from Kodak dataset.\", \"page\": 10, \"index\": 2, \"width\": 1013, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.06568v1/fig-003.webp\", \"caption\": \"Fig. 13. Quantitative comparison of the results obtained with different inference steps on the Kodak dataset.\", \"page\": 10, \"index\": 3, \"width\": 445, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2604.06568v1/fig-004.webp\", \"caption\": \"Fig. 15. Visual comparison of the results obtained without (left) and with (right) sample-guided perceptual enhancement. It can be observed that the left image exhibits blurring artifacts in the shoe’s ventilation holes area, while the right image demonstrates better perceptual quality.\", \"page\": 12, \"index\": 4, \"width\": 494, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2604.06568v1/fig-005.webp\", \"caption\": \"Fig. 11. Comparison results of the ablation experiments on the Kodak dataset.\", \"page\": 9, \"index\": 5, \"width\": 393, \"height\": 342}]"
motivation: 现有基于扩散的压缩方法因引入随机高斯噪声导致重建图像与原图存在偏差，且推理效率较低。
method: 将量化噪声表述为扩散前向过程的噪声，构建噪声约束扩散过程，并结合频域滤波增强细节及零样本引导增强技术。
result: 在多个基准数据集上的实验证明，该方法在保持高保真度方面优于现有所有先进方法。
conclusion: NC-Diffusion通过统一量化与扩散噪声，有效解决了扩散模型在图像压缩中的保真度与效率瓶颈。
---

## 摘要
随着扩散模型在图像生成领域取得巨大成功，基于扩散的图像压缩正引起越来越多的关注。然而，由于扩散学习中引入了随机噪声，这些方法生成的重建图像通常与原始图像存在偏差，导致压缩结果不尽如人意。为解决这一问题，本文提出了一种用于高保真图像压缩的噪声约束扩散（NC-Diffusion）框架。与现有的添加随机高斯噪声并将噪声直接引入图像空间的扩散压缩方法不同，所提出的 NC-Diffusion 将学习型图像压缩中原始添加的量化噪声建模为扩散前向过程中的噪声。随后，构建了一个从地面真值（ground-truth）图像到由量化噪声生成的初始压缩结果的噪声约束扩散过程。NC-Diffusion 克服了压缩与扩散之间的噪声不匹配问题，显著提高了推理效率。此外，开发了一个自适应频域滤波模块来增强基于 U-Net 的扩散架构中的跳跃连接，以强化高频细节。此外，还设计了一种零样本样本引导增强方法，以进一步提高图像的保真度。在多个基准数据集上的实验表明，与现有方法相比，我们的方法能够达到最佳性能。

## Abstract
With the great success of diffusion models in image generation, diffusion-based image compression is attracting increasing interests. However, due to the random noise introduced in the diffusion learning, they usually produce reconstructions with deviation from the original images, leading to suboptimal compression results. To address this problem, in this paper, we propose a Noise Constrained Diffusion (NC-Diffusion) framework for high fidelity image compression. Unlike existing diffusion-based compression methods that add random Gaussian noise and direct the noise into the image space, the proposed NC-Diffusion formulates the quantization noise originally added in the learned image compression as the noise in the forward process of diffusion. Then a noise constrained diffusion process is constructed from the ground-truth image to the initial compression result generated with quantization noise. The NC-Diffusion overcomes the problem of noise mismatch between compression and diffusion, significantly improving the inference efficiency. In addition, an adaptive frequency-domain filtering module is developed to enhance the skip connections in the U-Net based diffusion architecture, in order to enhance high-frequency details. Moreover, a zero-shot sample-guided enhancement method is designed to further improve the fidelity of the image. Experiments on multiple benchmark datasets demonstrate that our method can achieve the best performance compared with existing methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Noise Constrained Diffusion (NC-Diffusion) Framework for High Fidelity Image Compression》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：基于扩散模型（Diffusion Models）的图像压缩因其强大的生成能力而备受关注，能有效缓解传统方法在低比特率下产生的模糊问题。
*   **核心问题**：
    1.  **噪声不匹配**：现有方法通常在推理时从随机高斯噪声开始，而图像压缩引入的是“量化噪声”。这种分布差异导致重建图像容易偏离原图，产生虚假细节（保真度低）。
    2.  **推理效率低**：从纯高斯噪声恢复图像通常需要大量的迭代步数。
*   **研究动机**：探索如何将扩散过程与压缩过程中的量化噪声结合，实现既有高感知质量又有高保真度的图像重建。

### 2. 方法论
*   **核心思想**：提出 **NC-Diffusion** 框架，将学习型压缩中的“量化噪声”建模为扩散模型的前向过程噪声。
*   **关键技术细节**：
    *   **噪声约束扩散过程**：不再使用随机高斯噪声，而是构建从地面真值（$I_0$）到带量化噪声的初始压缩结果（$I_T$）的扩散路径。前向过程通过在 $I_0$ 上逐步添加缩放后的量化噪声 $\epsilon_n$ 来实现。
    *   **确定性采样**：推理时直接从初始解码图像 $I_T$ 开始进行去噪，避免了随机性，确保了重建结果对原图的忠实度。
    *   **自适应频域滤波（AFF）**：在 U-Net 的跳跃连接（Skip Connections）中加入 AFF 模块，通过傅里叶变换在频域增强高频特征，补偿压缩丢失的细节。
    *   **高频细节保持损失（$L_{high}$）**：利用多级小波变换（DWT）计算高频子带的 MSE 损失，强化纹理恢复。
    *   **零样本样本引导增强**：在测试阶段，利用 CLIP 图像编码器计算重建图与初始图之间的感知损失，通过梯度引导进一步提升保真度。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Flickr2W（约 2.07 万张图像）。
    *   **测试集**：CLIC2020（428 张高分辨率图像）、Kodak（24 张图像）。
*   **Benchmark 与对比方法**：
    *   **传统方法**：BPG。
    *   **生成式/扩散式方法**：HiFiC, MS-ILLM, CDC, DIRAC, HFD, DiffEIC, OneDC。
    *   **基础模型**：ELIC（作为该框架的 Backbone）。
*   **评价指标**：PSNR（失真度）、MS-SSIM、LPIPS、FID（感知质量/分布对齐）。

### 4. 资源与算力
*   **训练配置**：使用 Adam 优化器，学习率 $8 \times 10^{-5}$，训练迭代次数为 80k 次，Batch Size 为 1。
*   **推理配置**：使用 DDIM 加速采样。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到了在处理高分辨率图像时采用了重叠分块（Overlapping approach）策略以节省显存。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集上进行了完整的率失真（RD）和率感知（RP）曲线对比。
*   **消融实验**：非常充分，涵盖了：
    1.  NC-Diffusion 机制的有效性（对比随机高斯噪声）。
    2.  推理步数（1 步 vs 10 步）对性能的影响。
    3.  AFF 模块与 $L_{high}$ 损失的增益。
    4.  高频损失权重 $\beta$ 的敏感度分析。
    5.  样本引导增强的效果可视化。
*   **客观性**：对比了多种最新的扩散压缩模型，并同时报告了客观指标（PSNR）和感知指标（FID/LPIPS），评价维度全面。

### 6. 主要结论与发现
*   **性能领先**：NC-Diffusion 在保持高 PSNR 的同时，感知指标（FID）显著优于现有生成式压缩方法。
*   **保真度突破**：相比于 HiFiC 或 MS-ILLM，该方法能更准确地还原微小纹理（如文字、特定细节），不会产生虚假的伪影。
*   **效率提升**：由于从初始压缩图开始推理，该方法在 1-10 步内即可达到极佳效果，解码速度远快于需要数百步的传统扩散压缩模型（如 CDC）。

### 7. 优点与亮点
*   **理论创新**：巧妙地统一了“量化噪声”与“扩散噪声”的概念，解决了扩散模型在压缩任务中的本质矛盾。
*   **即插即用**：AFF 模块和 NC 框架可以适配任何基于 U-Net 的扩散架构。
*   **平衡性好**：在失真（Distortion）和感知（Perception）之间找到了极佳的平衡点，克服了生成式模型常见的“改写内容”风险。

### 8. 不足与局限
*   **依赖基础模型**：该框架的效果高度依赖于底层图像编解码器（如 ELIC）的质量，若基础模型产生的伪影过于严重，扩散层可能难以完全修复。
*   **算力开销**：尽管比传统扩散模型快，但相比于纯 CNN 编解码器（如 ELIC 原型），增加扩散模块仍会带来额外的计算延迟（解码时间从 0.063s 增至 0.102s）。
*   **极端低码率挑战**：在极低码率下，虽然感知质量好，但 PSNR 仍会有所下降，对于追求绝对像素一致性的场景可能不适用。

（完）
