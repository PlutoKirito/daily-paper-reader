---
title: Singular Relative Entropy Coding with Bits-Back Rejection Sampling
title_zh: 结合 Bits-Back 拒绝采样的奇异相对熵编码
authors: "Gergely Flamich, Spencer Hill"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06055v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 相对熵编码与Bits-back拒绝采样
tldr: 本研究针对奇异信道下的相对熵编码（REC）问题，提出了一种名为位回馈拒绝采样（BBRS）的新型随机编码算法。传统REC在编码时通常存在对数级冗余，而前人针对奇异信道的低冗余方案过于复杂且难以实现。BBRS通过结合位回馈编码与贪婪拒绝采样，不仅在理论上实现了亚对数级的渐近冗余，还简化了分析过程并优化了常数项。更重要的是，该算法可利用现有REC方法实现，具有极强的实用价值。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决奇异信道下相对熵编码渐近冗余较高，且现有低冗余理论方案过于复杂、无法实际应用的问题。
method: 提出了一种结合位回馈编码（Bits-Back Coding）和贪婪拒绝采样（Rejection Sampling）的新型编码框架 BBRS。
result: BBRS 在奇异信道上实现了与现有最先进技术相同的亚对数渐近效率，同时具备更简单的理论分析和更优的常数性能。
conclusion: 该研究为奇异信道提供了一种高效、易于分析且可实际部署的相对熵编码方案，显著提升了编码效率与实用性。
---

## 摘要
针对信源 $X \sim P_X$ 的相对熵编码是一种随机编码，它使用尽可能少的比特对来自给定 $P_{Y \mid X}$ 的随机样本进行编码。作为熵编码的一种推广，一个标准结论是，实现这一目标所需的最小比特数至少为互信息 $I[X\,\Vert\,Y]$。然而，与熵编码相比，相对熵编码的一个特别引人注目的特征是，通常情况下，该下界只能在增加一个对数因子的范围内实现。因此，一个重要的研究方向是识别可以缩小这一差距的信道。Sriramu 和 Wagner 通过为所谓的奇异信道展示一种具有次对数渐近冗余的相对熵编码，取得了这一成功。然而，他们的编码非常复杂，遗憾的是，无法在实践中实现。在本文中，我们构建了 Bits-Back 拒绝采样器 (BBRS)，这是一种结合了 Bits-Back 编码和（贪婪）拒绝采样思想的相对熵编码。我们对 BBRS 的分析表明，该算法达到了与 Sriramu 和 Wagner 采样器相同的渐近效率，但分析更简单且常数项更优。此外，BBRS 可以使用标准的相对熵编码方法来实现。

## Abstract
A relative entropy code for a source $X \sim P_X$ is a stochastic code that encodes random samples from a prescribed $P_{Y \mid X}$ using as few bits as possible. A generalisation of entropy coding, it is a standard result that the minimum number of bits required to achieve this is at least the mutual information $I[X\,\Vert\,Y]$. However, a particularly fascinating feature of relative entropy coding compared to entropy coding is that, in general, this lower bound is only achievable to within an additional logarithmic factor. As such, an important research direction is to identify channels where we can reduce this gap.   Sriramu and Wagner achieved such success by exhibiting a relative entropy code for so-called singular channels with sub-logarithmic asymptotic redundancy. However, their code is quite involved and, sadly, cannot be implemented in practice.   In this paper, we construct the bits-back rejection sampler (BBRS), a relative entropy code that combines ideas from bits-back coding and (greedy) rejection sampling. Our analysis of BBRS reveals that the algorithm achieves the same asymptotic efficiency as Sriramu and Wagner's sampler, but with much simpler analysis and better constants. Moreover, BBRS can be implemented using standard relative entropy coding methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Singular Relative Entropy Coding with Bits-Back Rejection Sampling》的结构化分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：相对熵编码（Relative Entropy Coding, REC）旨在以接近互信息 $I[X; Y]$ 的代价，通过随机编码模拟给定信道 $X \to Y$。虽然理论下界是 $I[X; Y]$，但通用算法通常存在 $O(\log I)$ 的额外冗余。
*   **研究动机**：Sriramu 和 Wagner (2024) 证明了对于**奇异信道**（Singular Channels，即输出的似然性仅受输入支持集影响，而不受具体似然值影响的信道），渐近对数冗余可以降至 0。然而，前人的实现方案在计算上是不可行的（Intractable），且常数项较大。
*   **整体含义**：本文提出了一种名为**位回馈拒绝采样（Bits-Back Rejection Sampling, BBRS）**的新算法，旨在为奇异信道提供一个理论分析更简单、常数项更优且在实践中可部署的编码方案。

### 2. 论文提出的方法论
BBRS 结合了**贪婪拒绝采样（GRS）**、**位回馈编码（Bits-Back Coding）**和**可逆采样**技术。其核心步骤如下：
*   **量化密度比**：首先对对数密度比进行 $\Delta$ 精度的量化，得到离散变量 $\Gamma = Q_\Delta(\log \frac{dP_{Y|X}}{dP_Y})$。
*   **两阶段编码策略**：
    1.  **第一阶段（GRS 采样）**：使用基于 $P_Y$ 的共享随机性，通过贪婪拒绝采样生成一个中间样本 $Y'$。
    2.  **第二阶段（位回馈机制）**：利用奇异信道的特性——存在一个可测函数 $g$ 使得从 $Y$ 可以恢复 $\Gamma$。编码器模拟解码器恢复 $\Gamma$ 的过程，并通过位回馈（Bits-Back）从消息流中“回收”用于编码 $\Gamma$ 的比特。
    3.  **第三阶段（简单拒绝采样）**：在已知 $\Gamma$ 的条件下，使用简单的拒绝采样对最终样本 $Y$ 进行编码。
*   **关键公式**：论文证明了 BBRS 的单次期望码长满足：
    $$E[|enc(X, Z)|] \leq I[X; Y] + \log(H[\Gamma] + 1) + 2\Delta + 5$$
    在渐近情况下（$n \to \infty$），该式导出的对数冗余项为 0。

### 3. 实验设计
*   **研究性质**：本文属于**理论信息论**研究，侧重于算法构建与数学证明，而非大规模工程实验。
*   **场景与 Benchmark**：
    *   **场景**：主要针对奇异信道，如加性均匀信道（$Y = X + U$）和二元擦除信道。
    *   **Benchmark**：对比对象是 **Sriramu-Wagner (SW) 编码**。
*   **对比维度**：对比了渐近对数冗余（Asymptotic Logarithmic Redundancy）、单次编码效率（One-shot efficiency）以及算法的可实现性（Practicability）。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何具体的算力资源（如 GPU 型号、数量或训练时长）。
*   **原因**：该研究关注的是编码协议的率失真理论分析和算法复杂度，不涉及深度学习模型的训练，其计算开销主要集中在拒绝采样的迭代次数上。

### 5. 实验数量与充分性
*   **理论证明**：论文提供了详尽的数学推导（见附录 A 和 B），证明了算法的正确性和率界限。
*   **实验充分性**：从理论分析角度看，论文通过引理和定理完整地覆盖了从单次率到渐近率的推导，逻辑严密。但从实证角度看，论文缺乏在真实数据集上的数值仿真实验，这在纯理论信息论论文中是常见的，但对于追求工程落地的读者来说可能稍显不足。

### 6. 论文的主要结论与发现
*   **零渐近冗余**：证明了 BBRS 在奇异信道下实现了 $R_{log} = 0$，验证了奇异信道在相对熵编码中的特殊地位。
*   **性能提升**：相比 SW 编码，BBRS 具有更小的常数项和更简单的结构。
*   **可实现性**：BBRS 可以利用现有的非对称数字系统（ANS）等流编码器实现，解决了前人方案无法实际操作的问题。
*   **非奇异信道扩展**：虽然 BBRS 主要针对奇异信道，但论文也探讨了其在非奇异信道下的局限性，并提供了一个改进的泊松泛函表示（PFR）方案作为补充。

### 7. 优点
*   **创新性**：巧妙地将位回馈（Bits-Back）这一通常用于变分自编码器压缩的技术引入到拒绝采样中，解决了冗余消除问题。
*   **理论优雅**：利用奇异信道的数学定义（函数 $g$ 的存在性）直接对应位回馈中的“错误恢复”步骤，物理含义清晰。
*   **实用潜力**：算法流程清晰（见 Algorithm 2 & 3），为未来开发高性能的神经压缩插件提供了理论基础。

### 8. 不足与局限
*   **依赖量化**：算法性能高度依赖于对数密度比的量化精度 $\Delta$，论文尚未给出如何自动选择最优 $\Delta$ 的准则。
*   **信道限制**：该算法的优势仅限于“奇异信道”。对于一般的非奇异信道，其效率并不优于现有的 GRS 或 PFR 方法。
*   **缺乏实测数据**：未在实际的图像或视频压缩任务中测试其作为熵编码替代方案的具体表现（如吞吐量、实际码率等）。

（完）
