---
title: "GeoHCC: Local Geometry-Aware Hierarchical Context Compression for 3D Gaussian Splatting"
title_zh: GeoHCC：面向 3D 高斯泼溅的局部几何感知分层上下文压缩
authors: "Xuan Deng, Xiandong Meng, Hengyu Man, Qiang Zhu, Tiange Zhang, Debin Zhao, Xiaopeng Fan"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28431v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 具有锚点剪枝和熵编码的几何感知压缩框架
tldr: 针对3D高斯泼溅（3DGS）存储开销巨大的问题，本文提出GeoHCC压缩框架。该方法通过引入局部几何感知机制，利用锚点间的几何相关性优化压缩过程。主要贡献包括：提出了邻域感知锚点剪枝（NAAP）以保持结构一致性，以及基于几何引导卷积（GG-Conv）的分层熵编码方案，实现了高效的空间自适应上下文建模。实验证明GeoHCC在保持渲染质量的同时显著提升了压缩性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28431v1/fig-001.webp\", \"caption\": \"Figure 6: Rate-Distortion (RD) performance curves. Comparison with advanced compression techniques (HAC, HAC++, ContextGS, CAT-3DGS) on the Mip-NeRF360, BungeeNeRF, and Tank&Temples datasets. GeoHCC demonstrates robust coding efficiency, maintaining the upper envelope across a wide range of bitrates.\", \"page\": 8, \"index\": 1, \"width\": 1045, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2603.28431v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the proposed 3DGS compression framework. Starting with Colmap initialization, anchors first undergo densification to capture fine-grained scene details. To optimize the resulting structure, we introduce Neighborhood-Aware Anchor Pruning (NAAP), which constructs a local graph to evaluate geometric importance. Unlike simple removal, NAAP adaptively merges the attributes of redundant anchors into their salient neighbors, producing a compact yet locally geometryconsistent representation. Subsequently, the preserved anchors are organized into Hierarchical Geometry-Guided Context Modeling module, where we employ a Local Geometry-Guided Context to guide entropy coding. Finally, the decoded attributes (e.g., colors, opacitys, scalings, rotations) generate 3D Gaussians via learnable offsets for high-fidelity rasterization.\", \"page\": 4, \"index\": 2, \"width\": 1042, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2603.28431v1/fig-003.webp\", \"caption\": \"Figure 4: This module exploits cross-level correlations bymapping Level 1 attributes to Level 2 query anchors via a deterministic mappingM to obtain the preliminary attributes A (2) 𝑝𝑟𝑒 . Then, a 𝑘-NN graph is constructed over A (2) 𝑝𝑟𝑒 to form the graph-based geometric prior G (2) 𝑔𝑒𝑜 . Within the GG-Conv (dashed box): (1) Geometry Branch: Relative spatial offsets Δp𝑖 𝑗 between the query anchor and its neighbors are used to query a learnable 3D kernel (Weight Look-up Table) via trilinear interpolation, generating dynamic geometric weightsw𝑖 𝑗 that explicitly encode the local spatial layout. (2) Feature Branch: This branch extracts semanticgeometric embeddings e𝑖 𝑗 by transforming the concatenated residual features Δf (1) 𝑖 𝑗 and relative offsets Δp𝑖 𝑗 through an MLP 𝜙 . Finally, the dynamic weights modulate these embeddings to produce the refined geometry-aware feature h𝑖 , which provides a structured foundation for deriving the final local geometry-guided context a𝑖 (𝑐𝑡𝑥 ) for Level 2 attribute entropy coding.\", \"page\": 5, \"index\": 3, \"width\": 1020, \"height\": 250}]"
motivation: 现有的基于锚点的3DGS压缩方案忽略了显式的几何依赖关系，导致结构退化和率失真性能不佳。
method: 提出邻域感知锚点剪枝（NAAP）和基于几何引导卷积（GG-Conv）的分层熵编码方案，利用局部几何相关性进行紧凑表示。
result: 实验结果表明，GeoHCC在保持几何完整性和渲染保真度方面优于现有的最先进锚点压缩方法。
conclusion: GeoHCC通过整合局部几何信息，有效解决了3DGS压缩中的结构保持瓶颈，实现了更优的率失真平衡。
---

## 摘要
尽管 3D 高斯泼溅 (3DGS) 能够实现高保真实时渲染，但其高昂的存储开销严重阻碍了实际部署。近期基于锚点 (anchor-based) 的 3DGS 压缩方案通过上下文建模减少冗余，但忽视了显式的几何依赖关系，导致结构退化和次优的率失真性能。在本文中，我们提出了 GeoHCC，这是一个几何感知的 3DGS 压缩框架，它将锚点间的几何相关性纳入锚点剪枝和熵编码中，以实现紧凑表示。我们首先引入了邻域感知锚点剪枝 (NAAP)，该方法通过加权邻域特征聚合评估锚点重要性，并将冗余锚点合并到显著邻居中，从而产生一个紧凑且几何一致的锚点集。在此优化结构的基础上，我们进一步开发了一种分层熵编码方案，其中通过轻量级几何引导卷积 (GG-Conv) 算子利用从粗到细的先验，以实现空间自适应上下文建模和率失真优化。大量实验表明，GeoHCC 有效解决了结构保持瓶颈，在保持优越的几何完整性和渲染保真度方面优于最先进的基于锚点的方法。

## Abstract
Although 3D Gaussian Splatting (3DGS) enables high-fidelity real-time rendering, its prohibitive storage overhead severely hinders practical deployment. Recent anchor-based 3DGS compression schemes reduce redundancy through context modeling, yet overlook explicit geometric dependencies, leading to structural degradation and suboptimal rate-distortion performance. In this paper, we propose GeoHCC, a geometry-aware 3DGS compression framework that incorporates inter-anchor geometric correlations into anchor pruning and entropy coding for compact representation. We first introduce Neighborhood-Aware Anchor Pruning (NAAP), which evaluates anchor importance via weighted neighborhood feature aggregation and merges redundant anchors into salient neighbors, yielding a compact yet geometry-consistent anchor set. Building upon this optimized structure, we further develop a hierarchical entropy coding scheme, in which coarse-to-fine priors are exploited through a lightweight Geometry-Guided Convolution (GG-Conv) operator to enable spatially adaptive context modeling and rate-distortion optimization. Extensive experiments demonstrate that GeoHCC effectively resolves the structure preservation bottleneck, maintaining superior geometric integrity and rendering fidelity over state-of-the-art anchor-based approaches.

---

## 论文详细总结（自动生成）

以下是对论文《GeoHCC: Local Geometry-Aware Hierarchical Context Compression for 3D Gaussian Splatting》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 高斯泼溅（3DGS）虽然实现了高保真的实时渲染，但其数百万个高斯基元的存储开销巨大。
*   **研究动机**：现有的压缩方法（如基于锚点的 Scaffold-GS 或基于哈希网格的 HAC）往往将锚点视为孤立实体，忽略了锚点之间显式的**局部几何相关性**。这导致在低比特率压缩时会出现结构退化、几何不连续以及熵建模效率低下的问题。

### 2. 方法论：GeoHCC 框架
论文提出了一个局部几何感知的分层上下文压缩框架，包含两个核心模块：

*   **邻域感知锚点剪枝 (NAAP)**：
    *   **核心思想**：改变传统的基于单一阈值（如不透明度）直接删除锚点的做法，改为基于局部图结构评估重要性。
    *   **技术细节**：首先构建 $K$ 近邻图，计算邻域加权重要性分数 $\xi_i$（结合了锚点自身不透明度和邻域平滑后的上下文）。
    *   **属性合并**：对于分数较低的冗余锚点，不直接丢弃，而是通过“加权属性转换器”将其属性（偏移量、缩放、不透明度）合并到最近的显著邻居中，从而在减少数量的同时保持几何完整性。
*   **分层几何引导上下文建模 (GG-Conv)**：
    *   **核心思想**：利用粗层级（Level 1）的几何信息作为先验，指导细层级（Level 2）属性的熵编码。
    *   **几何引导卷积 (GG-Conv)**：设计了一个轻量级算子。它包含两个分支：
        1.  **几何分支**：利用相对空间偏移量在可学习的 3D 核中进行三线性插值，生成动态几何权重。
        2.  **特征分支**：提取语义-几何嵌入。
    *   **结果**：通过动态权重调制特征，生成空间自适应的上下文先验，从而更准确地估计概率分布参数（均值 $\mu$、标准差 $\sigma$），提升压缩率。

### 3. 实验设计
*   **数据集**：在四个标准真实场景基准上进行了评估：Mip-NeRF360（全部 9 个场景）、BungeeNeRF、DeepBlending 和 Tanks&Temples。
*   **对比方法 (Benchmarks)**：
    *   **基础模型**：原始 3DGS、Scaffold-GS。
    *   **压缩 SOTA**：HAC、HAC++、Context-GS、CAT-3DGS、Compact3DGS、Compressed3D 等。
*   **评价指标**：存储大小 (MB)、PSNR、SSIM、LPIPS 以及渲染速度 (FPS)。

### 4. 资源与算力
*   **训练细节**：基于 PyTorch 实现，在 Scaffold-GS 官方代码库基础上构建。
*   **时长**：模型训练迭代次数为 35,000 次。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但根据 3DGS 类论文的惯例，通常使用单张或多张 NVIDIA RTX 3090/4090 级别的显卡。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从室内到室外、从近景到超大规模场景（BungeeNeRF）的多种测试。
*   **消融实验**：针对 NAAP 模块、GG-Conv 模块以及分层级数（两层 vs 三层）做了详细的对比实验。
*   **充分性评价**：实验设计较为充分，提供了完整的率失真（RD）曲线对比，证明了在不同比特率下该方法的稳健性。对比实验涵盖了 2024-2025 年最新的顶会论文方法，具有较高的时效性和公平性。

### 6. 主要结论与发现
*   **压缩效率**：GeoHCC 相比 Scaffold-GS 实现了超过 **21 倍** 的存储压缩，且渲染质量几乎无损。
*   **性能领先**：在相同的存储预算下，GeoHCC 的 PSNR 和 SSIM 普遍优于 HAC++ 和 Context-GS，尤其在处理复杂几何结构时表现更优。
*   **结构保持**：NAAP 模块能有效抑制“漂浮物”伪影，GG-Conv 能够提取更精准的上下文，使特征熵编码的比特分配更具针对性。

### 7. 优点与亮点
*   **几何感知**：将 3D 数据的几何特性（图结构）引入熵建模，比单纯的哈希网格或线性 MLP 更有物理意义。
*   **属性合并策略**：NAAP 的“合并而非简单删除”策略是处理非结构化点云/高斯数据的一个亮点，有效缓解了剪枝导致的空洞问题。
*   **轻量化设计**：GG-Conv 算子设计精巧，在提升压缩性能的同时，对渲染 FPS 的负面影响较小。

### 8. 不足与局限
*   **计算开销**：虽然 GG-Conv 是轻量级的，但构建 $K$ 近邻图和进行三线性插值在锚点数量极多时仍会增加预处理或解码的计算负担。
*   **超参数敏感性**：NAAP 中的平衡因子 $\lambda$ 和合并权重 $\gamma$ 可能需要针对不同类型的场景进行微调。
*   **应用限制**：该方法目前主要针对基于锚点（Anchor-based）的 3DGS 变体，对于原始的无结构 3DGS 压缩的直接适用性有待进一步验证。

（完）
