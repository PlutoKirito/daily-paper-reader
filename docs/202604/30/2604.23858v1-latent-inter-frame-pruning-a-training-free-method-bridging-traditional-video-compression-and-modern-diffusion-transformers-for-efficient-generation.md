---
title: "Latent Inter-Frame Pruning: A Training-Free Method Bridging Traditional Video Compression and Modern Diffusion Transformers for Efficient Generation"
title_zh: 潜空间帧间剪枝：一种连接传统视频压缩与现代扩散 Transformer 以实现高效生成的免训练方法
authors: "Dennis Menn, Chih-Hsien Chou"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23858v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 通过潜变量帧间剪枝将传统视频压缩与现代扩散Transformer结合
tldr: 针对视频生成计算开销大、速度慢的问题，本文提出了一种无需训练的潜在帧间剪枝（Latent Inter-frame Pruning）框架。该方法借鉴传统视频压缩技术，通过识别并跳过潜在空间中冗余块的重复计算来提升效率。为解决剪枝带来的视觉伪影，引入了注意力恢复机制以弥合训练与推理间的差异。该方法在保持视频质量的前提下，将生成吞吐量提升了1.44倍，为高效视频生成提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23858v1/fig-001.webp\", \"caption\": \"Figure 2. Illustration on approximation of the pruned tokens with full length token sequence.\", \"page\": 4, \"index\": 1, \"width\": 932, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-002.webp\", \"caption\": \"Figure 3. Noise-aware unpruning.e\", \"page\": 4, \"index\": 2, \"width\": 416, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-003.webp\", \"caption\": \"Figure 4. Inference speed comparisons between baseline and our pruning method.\", \"page\": 5, \"index\": 3, \"width\": 519, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-004.webp\", \"caption\": \"Figure 1. Framework overview: The proposed framework consists of three stages.\", \"page\": 3, \"index\": 4, \"width\": 932, \"height\": 332}]"
motivation: 视频生成模型计算极其耗时，且在潜在扩散模型的潜在空间中存在显著的时间轴冗余。
method: 提出一种无需训练的剪枝框架，通过跳过重复潜在块的计算并利用注意力恢复机制消除视觉伪影。
result: 在NVIDIA RTX 6000上实现了1.44倍的吞吐量提升，达到12.44 FPS，且未损失视频质量。
conclusion: 该工作成功将传统视频压缩理念应用于现代扩散变换器，显著提高了视频生成的推理效率。
---

## 摘要
视频生成虽然能够生成逼真的视频，但计算成本高昂且速度缓慢，阻碍了实时应用。在本文中，我们观察到在潜扩散模型（LDM）框架下通过自编码器编码的视频潜变量在时间轴上存在冗余。类似于传统视频压缩算法避免传输冗余帧数据的方式，我们提出了潜空间帧间剪枝（Latent Inter-frame Pruning）框架，用于剪枝（跳过重复计算）重复的潜空间补丁（latent patches），从而减轻计算负担并提高吞吐量。然而，由于全序列训练与剪枝推理之间的差异，直接剪枝会导致视觉伪影。为了解决这些伪影，我们提出了一种注意力恢复（Attention Recovery）机制来弥合训练与推理之间的差距。通过我们提出的方法，我们将视频编辑吞吐量提高了 1.44 倍，在 NVIDIA RTX 6000 上达到了 12.44 FPS，同时保持了视频质量。我们希望我们的工作能启发更多关于将传统视频压缩方法与现代视频生成流水线相结合的研究。这项工作是关于带有注意力恢复的免训练潜空间帧间剪枝的初步研究。

## Abstract
Video generation, while capable of generating realistic videos, is computationally expensive and slow, prohibiting real-time applications. In this paper, we observe that video latents encoded via an autoencoder under the Latent Diffusion Model (LDM) framework contain redundancy along the temporal axis. Analogous to how traditional video compression algorithms avoid transmitting redundant frame data, we propose the Latent Inter-frame Pruning framework to prune (skip the re-computation of) duplicated latent patches, thereby reducing computational burden and increasing throughput. However, direct pruning results in visual artifacts due to the discrepancy between full-sequence training and pruned inference. To resolve these artifacts, we propose an Attention Recovery mechanism to bridge the train-inference gap. With our proposed method, we increase video editing throughput by 1.44$\times$, achieving 12.44 FPS on an NVIDIA RTX 6000 while maintaining video quality. We hope our work inspires further research into integrating traditional video compression methods with modern video generation pipelines. This work is a preliminary work on Training-free Latent Inter-Frame Pruning with Attention Recovery.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Latent Inter-Frame Pruning (LIP)** 的方法，旨在通过借鉴传统视频压缩技术来加速现代扩散变换器（Diffusion Transformers, DiT）的视频生成过程。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频生成模型（如 LDM）计算开销巨大且推理速度慢，难以满足实时应用需求。
*   **研究动机**：作者观察到，虽然视频已通过自编码器压缩到潜空间（Latent Space），但在时间轴上仍存在大量冗余。传统视频编码（如 MPEG）通过跳过重复帧数据来节省带宽，作者希望将这一理念引入生成式 AI，通过剪枝潜空间中的冗余补丁（Patches）来减少计算量。
*   **挑战**：直接剪枝会导致推理与训练阶段的分布不一致（训练时是全序列），从而产生视觉伪影。

### 2. 方法论
该方法是一个**免训练（Training-free）**的框架，包含三个核心阶段：
*   **潜空间帧间剪枝 (LIP)**：在 Tokenization 之前，通过计算相邻帧相同空间位置补丁的 L1 距离。若距离小于阈值 $\theta$，则判定为冗余并跳过该补丁的生成计算。
*   **注意力恢复机制 (Attention Recovery)**：
    *   **原理**：为了弥合剪枝后的推理与全序列训练之间的差距，通过数学近似方法调整自注意力层的计算，使其在缺少 Token 的情况下仍能模拟完整序列的注意力得分。
    *   **噪声感知复制 (Noise-Aware Duplication)**：直接复制 Token 会导致噪声分量重复，违反扩散模型的 i.i.d. 噪声假设并产生高频伪影。该机制通过从 KV-Cache 中提取“干净”的内容来近似冗余 Token，确保视频结构的连贯性而不放大噪声。
*   **Token 恢复**：在解码前，通过参考前一帧的清洁 Token 将剪枝掉的位点进行上采样还原，以满足 VAE 解码器的固定维度要求。

### 3. 实验设计
*   **数据集**：使用 **DAVIS 数据集** 中的 51 组视频-文本对。
*   **基准模型 (Benchmark)**：以 **Self-forcing 模型**（一种微调过的因果视频扩散模型）作为 Baseline。
*   **对比指标**：
    *   **质量评估**：使用 VBench 指标（包括主体一致性、背景一致性、运动平滑度、审美质量等）。
    *   **效率评估**：推理吞吐量（FPS）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 6000** GPU 上运行。
*   **算力表现**：
    *   推理速度达到了 **12.44 FPS**。
    *   相比 Baseline，实现了约 **1.44 倍** 的吞吐量提升（加速约 44.8%）。
*   **训练时长**：由于是免训练方法，文中未涉及模型训练耗时。

### 5. 实验数量与充分性
*   **实验规模**：基于 51 个视频样本进行了定量分析，并提供了定性视觉对比。
*   **充分性评价**：作为一项初步研究（Preliminary work），实验涵盖了主流的视频质量评价体系（VBench），证明了该方法在大幅提速的同时几乎不损失画质。但实验主要集中在视频编辑场景，且样本量相对较小，缺乏在更大规模数据集或不同 DiT 架构上的广泛验证。

### 6. 主要结论与发现
*   **潜空间冗余性**：证实了扩散模型的潜空间保留了像素空间的空时局部性，使得传统压缩算法的逻辑可以迁移。
*   **加速效果显著**：在不进行任何重训练的情况下，通过减少进入 Transformer 层的 Token 数量，实现了线性的计算加速。
*   **注意力恢复的必要性**：证明了通过数学手段补偿缺失的注意力权重是消除剪枝伪影的关键。

### 7. 优点
*   **免训练且即插即用**：无需昂贵的重训练过程，可直接集成到现有的 DiT 框架中。
*   **软硬件友好**：减少 Token 数量直接降低了内存占用和计算复杂度，且与 FlashAttention 等硬件加速库兼容。
*   **理论结合紧密**：巧妙地解决了扩散模型中特有的噪声相关性问题（噪声感知复制）。

### 8. 不足与局限
*   **应用场景受限**：目前主要在视频编辑（Video Editing）任务上验证，尚未证明在从零开始的文本转视频（T2V）任务中的表现。
*   **剪枝维度单一**：目前仅探索了时间轴剪枝，未涉及空间维度的自适应剪枝。
*   **策略固定**：剪枝阈值是预设的，缺乏根据视频内容复杂度动态调整剪枝率的自适应策略。

（完）
