Title: Semi-Supervised Goal-Oriented Semantic Communication Framework for Foreground Classification

URL Source: https://arxiv.org/pdf/2604.10870v1

Published Time: Tue, 14 Apr 2026 01:33:12 GMT

Number of Pages: 7

Markdown Content:
# Semi-Supervised Goal-Oriented Semantic Communication Framework for Foreground Classification 

Zhitong Ni ∗, Yansha Deng †, and Jinhong Yuan ∗∗ School of Electrical Engineering and Telecommunications, University of New South Wales 

† Department of Engineering, King’s College London Emails: ∗ {zhitong.ni, j.yuan}@unsw.edu.au, † yansha.deng@kcl.ac.uk 

Abstract—Wireless goal-oriented semantic communication (GSC) has emerged as a promising paradigm by directly opti-mizing task performance. However, existing GSC frameworks typically operate on entire images and rely on labeled data for classification tasks, which can limit their compression efficiency and increase the risk of overfitting. This paper proposes a novel semi-supervised wireless GSC framework for the unla-beled image foreground classification task. In our proposed framework, a foreground-aware masked autoencoder (MAE) is developed to prioritize semantically important foreground objects, thereby reducing transmission overhead. To enable accurate reconstruction and classification under a limited data size, we further propose a semi-supervised autoencoder (SSAE) that decodes the semantic latent tensor and refines image details by leveraging three complementary information sources, followed by fine-tuning a pre-trained image classification model. The entire pipeline, from foreground masking to classification, is trained in a semi-supervised manner to significantly reduce the need for manual labeling. Simulation results validate that the proposed GSC framework achieves over 90% image classification accuracy while reducing the original image data size by 95%, and demonstrate its strong potential for practical tasks in resource-constrained wireless scenarios. Index Terms—semantic communication, image classification, semi-supervised learning, Transformer 

I. Introduction With the advancement of machine learning technology in wireless communications, semantic communication (SC) has emerged as a pivotal paradigm for next-generation networks [1], particularly for bandwidth-intensive appli-cations such as underwater acoustics and satellite com-munication systems [2]. Unlike traditional communication systems that focus on the precise transmission of original bits and symbols, SC aims to convey the meaningful source data, thereby offering a compressed data size and enhancing the spectral efficiency of scenarios under stochastic wireless channels with limited resources [3]. The existing work on SC can be broadly classified into two categories: data-oriented [4] and goal-oriented approaches [5] [6]. The data-oriented SC, such as deep joint source-channel coding (D-JSCC) [7], focuses on extracting latent representations from inputs and reconstructing out-puts to be as close to the original inputs as possible. Data-oriented SC frameworks typically employ auto-encoders to learn compact latent representations that are transmitted over noisy channels and reconstructed at the receiver with high fidelity [8]. In contrast, goal-oriented SC (GSC) prioritizes the successful execution of a downstream task at the receiver, such as classification or detection, and preserves only the task-relevant semantic representation [9]. Existing GSC frameworks typically compress the entire image (or other media), implicitly assuming that all regions contribute equally to classification performance. However, in practice, different regions of an image con-tribute unequally, and some may even negatively impact performance [10]. This observation motivates foreground extraction to enhance GSC performance. Another limita-tion of current GSC frameworks is their reliance on labeled datasets to train the underlying neural networks. In many practical scenarios, however, obtaining comprehensive la-bels is time-consuming, labor-intensive, and sometimes infeasible. To address these challenges, we develop a semi-supervised GSC framework for image foreground clas-sification. The transmitter is designed to identify and compress foreground objects in the images during the en-coding and transmission stages. Then, the receiver obtains the reconstructed images for classifications. The entire framework is trained in a semi-supervised manner, where the foreground extraction and reconstruction models are trained in a fully unsupervised manner while only the final image classification stage requires a limited amount of labeled data. The contributions of this paper are:  

> •

At the transmitter, we develop a foreground-aware masked autoencoder (MAE) for semantic information extraction. Unlike conventional masking strategies based on random masking or label-driven masking, our proposed MAE explicitly prioritizes semantically important regions in unlabeled image data. By selec-tively masking less informative background patches, the MAE is encouraged to learn latent representations that are highly concentrated on foreground content, thereby enabling goal-oriented data transmission.  

> •

At the receiver, we propose a semi-supervised auto-encoder (SSAE) that jointly performs reconstruction and classification of images. Our SSAE reconstructs 

> arXiv:2604.10870v1 [eess.IV] 13 Apr 2026

VisionTransformerBlock Patchify 

> LN
> MSA
> LN
> FFN
> ...
> [CLS]token
> C × 1
> Patchtoken
> ...
> C × T

Image Semanticawaremask 

> Wirreless Channel

SemanticEncoding 

> CNNk3n16s1
> CNNk3n32s1
> Down
> Down

... 

> RefineDetails
> CNNk3n4s1
> Channel Encoding
> Channel Decoding
> RefineDetails
> CNNk3n32s1
> CNNk3n3s1
> Up
> Up

... 

> CNNk3n16s1

SemanticDecoding  

> Classification Fig. 1. Illustration of our proposed image classification framework, where the image first goes through a ViT-based model to detect the semantically meaningful foreground, then goes through a semantic encoder for the image’s foreground reconstruction, and the encoded outputs are transmitted in the wireless channel. At the receiver, the image foreground is extracted using a symmetric model and then passed through a classification model in a semi-supervised manner.

high-quality foreground images with a given limited data size. Meanwhile, the overall classification perfor-mance is improved through the combined contribu-tions of MAE and SSAE.  

> •

We introduce a semi-supervised training paradigm for the GSC systems. The foreground masking strategy learns semantic representations without requiring an-notations; the foreground information is compressed and reconstructed in a label-free setting; and a pre-trained image classification model is fine-tuned using a limited amount of labeled data. This training paradigm significantly reduces the reliance on exten-sive manual labeling and facilitates the practical GSC deployment in real-world scenarios. Notations: a denotes a vector, [a]i denotes the i-th entry of a. A denotes a matrix or tensor, italic English letters like N and lower-case Greek letters like α are scalars. 

∥A∥ is the Frobenius norm of a matrix or a tensor. Operator ◦ denotes the Hadamard product. E(·) obtains the expectation of a random scalar. II. System Model In this section, we introduce the architecture of our proposed GSC system. The main goal of this system is to reconstruct the image of interest and classify the images within limited categories. The information source is an image tensor I of di-mension 3 × H × W , where the 3 channels correspond to red/green/blue (RGB) color channels, and H and W

represent the height and width of the images. As shown in Fig. 1, the transmitter extracts the image foreground. Several modules sequentially process the foreground to produce the semantically encoded latent representation, S.This latent is then channel-encoded and transmitted over the wireless channel. At the receiver, the recovered latent is decoded for image reconstruction and classification. Firstly, we aim to obtain the foreground using the vision Transformer (ViT)-based MAE, of which details will be given in Section III-A. Secondly, after obtaining the semantic mask, denoted as M, the image tensor is masked to maintain the foreground only, and goes through the semantic encoding, i.e., SSAE, to further reduce the data size while maintaining high fidelity. Finally, the data stream to be transmitted after the channel encoding is 

x = C(S) = C(E(M(3) ◦ I)) , (1) where C(·) denotes the conventional channel encoding operation, E(·) is the SSAE as will be detailed in Section III-B, and M(3) is a 3-dimensional (3D) tensor with each channel being M.For wireless communications, we aim to use the additive white Gaussian noise (AWGN) channel, which is widely adopted for justifying the SC systems. The received data symbols can be given by 

y = x + n, (2) where n is the AWGN of zero mean and variance of σ2.At the receiver, the received signal is first processed by the channel decoder to correct bit errors and reconstruct the transmitted foreground for image classifications. The reconstructed image is given by 

ˆI = E−1(C−1(y)) , (3) where C−1(·) is the channel decoder and E−1(·) is the semantic decoder. An image classification model is trained to identify C′ classes, indexed by c = {1, · · · , C ′}. Given the reconstructed image ˆI, the classification model outputs probability distribution p = {p1, · · · , p C′ }, where pc

represents the probability of recognizing ˆI as class c.III. Semi-Supervised GSC Framework In this section, we illustrate the framework of semi-supervised GSC that extracts the foreground of the image, obtains the compressed image latent representation, and classifies the reconstructed image. A. MAE We aim to obtain a semantic mask that removes the background of the image since the background generally provides no or negative contributions to classify the specific class. Due to the unavailability of image labels, we patchify the image and leverage the inherent and coherent semantic information of each separated patch of the image. Each patch has the size of 3 × P × P , where P is the patch size, H and W should be integer multiples of P , and the number of patches is T = HW/P 2.Each image patch can be transformed into a meaningful token that describes partial information about the image. To transform the image patches into the tokens, we adopt the ViT architecture as in [11], where the images are sequentially processed by convolutional neural network (CNN) modules and multiple Transformer blocks. It is well known that the Transformer can capture long-range dependencies between image patches through its self-attention mechanism, enabling more effective feature representations. The output of the final Transformer block is a (T +1) ×C

token matrix T that consists of one classification to-ken, commonly referred to as the “[CLS]” token, and 

T patch tokens that correspond to each image patch. The dimension of the token is C × 1, which determines the semantic representation capability of the token. Since position embedding is adopted for the patch tokens to represent their specific image region, it introduces spatial bias into patch tokens, which can interfere with learning position-invariant semantic representations that are es-sential for foreground extraction. The [CLS] token that has no position embedding aggregates global semantic information from the entire image. This unique token can serve as a robust semantic query about the foreground object. Now, supposing that the [CLS] token represents the global semantic information regarding the entire image by using a pre-trained ViT. The problem is to remove the background information from the [CLS] token. We project the [CLS] token produced by the final Transformer block into a high-dimensional latent space. This is achieved by appending a multi-layer perceptron (MLP) projection head to the [CLS] token. The embedding dimension is thus increased from C to K. The output of the MLP is then normalized by a temperature-scaled softmax function to make the output become a probability distribution function (pdf), i.e., 

qk = eQk /ϵ K∑

> k=1

eQk /ϵ 

, (4) where Qk, Qk ≥ 0, is the output of the MLP and ϵ is a temperature scaler. The entire neural network mapping from the input image I to the softmax-normalized output 

qk forms the entire neural network architecture of the MAE, denoted as N .The pdf qk is determined by the weights of N and the input I. To generate a foreground-aware output in a supervised manner, we need to process the image into a perfectly masked input, i.e., I ◦ M(3) , and obtain the corresponding output as labels, and then train the network N . However, this is not a practical solution since a perfectly masked input would need extremely intense labor. This motivates us to generate a semantic mask in a self-supervised learning manner that is inspired by the distillation with no label (DINO) framework [11]. Specifically, two differently augmented views of the same image I are constructed, denoted as Is and It, where It is a weakly augmented image and Is conducts additional data augmentation on It. In our paper, It is obtained using a pretrained DINO teacher network, denoted as Nt(·),to generate a coarse image mask on I; Is is obtained by conducting additional random cropping and color jittering on It. The cropping is realized by randomly masking 

X image patches on It. The color jittering is done by adjusting brightness, contrast, and saturation by ξ times on It. Is is processed by a student network that will be trained, denoted as Ns(·).The training objective is to minimize the cross-entropy loss between the outputs of Nt(It) and Ns(Is). Hence, the loss function of the MAE is 

L = −

> K

∑

> k=1

qk,t log qk,s , (5) where qk,t and qk,s are the outputs of Nt(It) and Ns(Is),respectively. The gradients are propagated only through the student network, while the Nt is kept fixed during backpropagation. After training, the student network is expected to produce outputs similar to those of the teacher network, since the cross-entropy loss is minimized when qk,s = qk,t 

for a given qk,t . However, the student network has access to less information than the teacher network. Therefore, to match the teacher’s output distribution, the student must learn to extract the most task-relevant features from the limited input by leveraging the Transformer’s attention mechanism. As a result, the student network tends to focus more on the biased foreground information while suppressing background details. When a fully detailed image I is fed into the student network, the produced [CLS] token therefore contains less redundant background information. Recall that each patch token corresponds to a specific spatial region. By leveraging both the [CLS] token and the patch tokens, we can generate the final semantic mask M

through the self-attention weights, as detailed in Appendix A. B. SSAE This subsection develops a novel SSAE that reconstructs the image from limited data bits. To enable reliable image classification at the receiver, conventional image compression methods such as JPEG can be employed to reconstruct the image with sufficient fidelity under a limited data size. However, our proposed SSAE has better flexibility and image fidelity, as the JPEG format can be corrupted under high bit error rates (BERs) due to its variable-length Huffman coding. The primary objective of SSAE is to learn a highly compact latent representation that enables effective image reconstruction while preserving the core semantic informa-tion essential for image classification or human perception. Given the strong capability of CNNs in modeling spatial correlations and reconstructing high-dimensional signals, we are inspired by the architecture in [12] and [7], and propose an encoder setup as shown in the “Semantic Encoding” in Fig. 1, where ‘k’ denotes the kernel size, ‘n’ denotes the output channel, and ‘s’ denotes the padding size. The “Down” modules consist of a 2 × 2 max-pooling layer followed by a CNN with a kernel size of 3 and a stride of 1, which halves the spatial dimensions and maintains the channel dimension. Each CNN or Down module connects a batch normalization layer and a rectified linear unit (ReLU), which are omitted in the figure. The decoder is at the receiver side and has symmetric setups. The semantic encoder in Fig. 1 progressively transforms the input image into a low-dimensional latent tensor at the bottleneck, which serves as a compressed representation of the image. Comparing the dimension of the input tensor and that of the final CNN module, the total dimension is dropped from 3HW to Co H 

> 2D
> W
> 2D

, where Co is the final CNN’s output channel dimension and D is the number of Down modules. Hence, the compression ratio of the entire CNN architecture is Co/(4 D · 3) .By carefully controlling Co and D at this stage, we can generate a semantically rich latent tensor at the bottleneck that will be received at the receiver for image reconstruc-tion. For wireless communication systems, the continuous-valued latent representation must be quantized before transmission. Hence, we apply element-wise quantization to each entry of the latent tensor with N -bit resolution. At the receiver side, the decoder reconstructs the image from the quantized latent. For training of the entire SSAE, we omit the wireless channel noise and connect the encoder and decoder directly for offline training. This is because the channel coding is used to mitigate the noise influences. The loss function is ∥(I − ˆI) ◦ M∥ and backpropagation is adopted to train the whole network. The CNN-based image reconstruction achieves perfor-mance gains under a limited data size. However, the weights and structures of the CNN model are fixed after training, preventing it from adaptively adjusting its fidelity level. As a result, the encoder lacks flexibility in enhancing fine-grained image details, particularly when such details are crucial for image classification or human perception. To address this issue, recalling that image I is parti-tioned into T non-overlapping patches, we need to identify 

T ′ patches that require refinement. We note that different patches have different attention weights according to Appendix A. Hence, we first select the patches whose attention weights are larger than a threshold ψ, ψ ≥ ρ.Then, among the selected M non-masked patches, we directly identify the first T ′ patches that have the largest reconstruction errors. A compact binary sequence can be used for representing each patch, where 0 indicates that the patch should be reconstructed solely using the CNN decoded output, and 1 indicates that the patch requires per-pixel refinement. We define the refinement ratio as 

η = T ′/M. (6) When the η is 0, the receiver fully uses the CNN’s latent to reconstruct the image. Otherwise, we use the traditional K-means method to refine the T ′ patches: The K-means method can obtain a palette of F representative cluster centers among M patches, where each center corresponds to a 3-byte representative color. Each pixel in the refined patch is then replaced by the index of its nearest cluster center (an integer in {0, · · · , F −1}), effectively quantizing the patch using the learned palette. The indices of the refined patches are further compressed via run-length encoding (RLE) of run length L, leveraging the spatial redundancy inherent in palette-based representations. The transmitter thus needs to send three key compo-nents to the receiver:  

> •

The quantized latent tensor from the main semantic encoder that contains core semantic information.  

> •

The short binary mask sequence of length T indicat-ing which patches need refinement.  

> •

The color index for the refined patches followed by the palette. The receiver first receives the quantized latent tensor to reconstruct the full image using the CNN decoder. Then, for patches flagged as 1 in the binary mask sequence, each Fig. 2. Illustration of the MAE, where two pictures are a comparison pair, with the left one and right one being the original picture and foreground, respectively. 

refined patch is reconstructed in the spatial domain. Finally, we fine-tune a pre-trained ViT-based classifi-cation model to classify the reconstructed images. The adopted classification model shares the same architecture as the MAE used in our framework, but with different network weights. In this model, the [CLS] token is pro-jected onto the C′ categories instead of the original K-dimensional embedding space, followed by using the cross-entropy loss function. Since fine-tuning requires labeled data, we use a small portion of labeled data to train the classification model. Consequently, only a little human la-beling is required at the final stage of image classification. This significantly reduces manual labeling effort, making our semi-supervised learning approach avoid extensive human intervention. IV. Simulations In this section, we present numerical results to illustrate the performance of our semi-supervised GSC framework. The experiments are conducted on the STL-10 dataset, a widely used benchmark for image reconstruction and representation learning tasks. The dataset consists of 100,000 unlabeled natural images collected from 10 object categories without manual labeling, 5,000 labeled images for supervised learning, and 8,000 labeled images for testing. This unlabeled split is adopted for our self-supervised learning of foreground extraction and image reconstruction, and the labeled images are used for fine-tuning the image classification models. Fig. 2 shows representative image pairs illustrating the foreground extraction capability of our proposed MAE. The image spatial dimension is 96 × 96 and the patch 10 -5 10 -4 10 -3 10 -2 10 -1    

> BER
> 40 50 60 70 80 90 100
> Classification Accuracy (%)
> Orignal Proposed JPEG (Q=5) Standard MAE D-JSCC
> Original Proposed JPEG Standard MAE D-JSCC
> Data Size (KB)
> 0510 15 20 25 30
> 0.9 0.9 0.9 0.6
> Proposed JPEG Standard MAE D-JSCC
> 00.2 0.4 0.6 0.8 11.2
> Fig. 3. Classification accuracy of different image reconstruction models. The data size refers to the number of bit streams before channel encoding.

size is P = 8 , thus the image is divided into T = 144 

patches. The extensions of channels are C = 384 and 

K = 65536 . The number of Transformer blocks is U = 12 .The number of multi-head attentions is 5. Other param-eters are the same as in the DINO network. We set 

ϵ = 0 .1, X = 10 , and ξ ∈ [0 .9, 1.1] for our MAE. The results are illustrated in pairs with the original input image (left) and its corresponding masked output (right). The masking process preserves both fine- and large-scale semantic components, such as animal contours and main bodies. These qualitative examples demonstrate that our proposed MAE effectively captures high-level contextual information while maintaining visually meaningful con-tent across diverse object categories, scales, and scene complexities. We further measure the proportion of non-masked pixels in the entire dataset. Approximately 49% of the pixels remain non-masked, indicating that nearly half of the image can be removed, which is beneficial for subsequent compression and transmission efficiency. Fig. 3 illustrates the classification accuracy of the reconstructed images versus the BER, where the BER refers to the bit error rate in the reconstructed image tensors. We compare our methods with the JPEG format with a quality of 5 (Q=5), the standard MAE with JPEG compression [13], and the D-JSCC [7]. We adopt a pre-trained ViT classification model originally trained on Ima-geNet with 1000 classes, and modify its classification head to accommodate our 10 object categories. The training data is based on the STL-10 training dataset. The data size is constrained between 0.6 KB and 0.9 KB, as different models require different latent representations. In our proposed MAE+SSAE, we adjust the model parameters to achieve the highest classification accuracy under the constrained data size, which are shown as follows: We let 

Co = 4 , D = 3 , and N = 8 , and set η = 0 .5, ψ = 5 × 10 −3,TABLE I Performance of Image Reconstructions Under AWGN 

Models PSNR(dB) SNR 20 dB 15 dB 10 dB Proposed 25.4 24.0 20.7 Standard MAE [13] 16.0 NaN NaN D-JSCC [7] 21.4 20.0 16.8 JPEG (Q=5) 22.0 NaN NaN Original ∞ ∞ 25.8 

F = 8 , and L = 4 , respectively. As shown in the figure, the classification model is robust to the BER below 10 −2. The original STL-10 test images achieve the highest accuracy of around 94% when the BER is below 10 −3, with accuracy degrading as the BER increases. Our proposed method achieves significantly higher accuracy than the baselines under similar data-size constraints. Note that the original images require 96 × 96 × 3/1024 = 27 KB. In contrast, our proposed framework reduces the data size to 0.9 KB, which saves around 95% of the transmission costs, while the classification accuracy is only 4%-5% lower than that of the original one. Table I illustrates the performance of image reconstruc-tions against AWGN. The performance metric is the peak-signal-to-noise ratio (PSNR), which is defined as 

10 log 10 



> H,W, 3

∑

> i,j,c =1 ,1,1

13P 2M

(

Ii,j,c − ˆIi,j,c 

)2

Mi,j 



> −1

, (7) where Ii,j,c is the (i, j )-th original pixel in the c-th color channel, ˆIi,j,c is the corresponding estimated pixel, and 

Mi,j is the masking coefficient. The signal-to-noise ratio (SNR) is given by E(|x|2)/σ 2. A low-density parity-check code with a code rate of 0.5 and a block length of 2048 is adopted for channel encoding. From the table, we see that the original image has no reconstruction error at an SNR above 15 dB. JPEG bit streams are highly fragile to noise due to variable-length Huffman coding. This often results in complete decoding failure or severe image corruption. For standard MAE and D-JSCC, their performance remains lower than that of our proposed MAE+SSAE. Overall, our proposed method achieves the highest reconstruction quality among the baseline methods and demonstrates strong robustness to noise. V. Conclusions In this paper, we proposed a novel GSC framework for image transmission and classification over wireless channels, incorporating foreground extraction, compres-sion, reconstruction, and classification. By prioritizing semantically important foreground regions using a ViT-based MAE, our proposed framework effectively removes background information. Our proposed SSAE enables highly compact yet semantically informative latent repre-sentations for unlabeled image datasets and outperforms the state-of-the-art methods in terms of classification accuracy and PSNR. In summary, our proposed approach offers a practical and efficient solution for joint image reconstruction and classification in bandwidth-constrained scenarios. Appendix A Generations of Attention Maps For a token block T of size (T + 1) × C, we obtain the multi-head self-attention (MSA) matrix as 

MSA(LN( T)) , (8) where LN( ·) is the layer normalization of T, such that each row of T is normalized to have zero mean and variance of 1. The MSA operation involves linear projections, scaled dot-product attention calculations, and concatenation. For the MSA mechanism, the input T is transformed into Query and Key subblocks. For each attention head m,

m ∈ { 1, · · · , M ′}, the Query (Q) and Key (K) subblocks are 

Qm = TW Qm, Km = TW Km , (9) where WQm and WKm are C ×C/M ′ matrices whose weights are given by the last Transform block in Fig. 1. Then, the 

mth MSA matrix is 

Sm = softmax (QmKTm

) /√C/M ′, (10) where softmax( ·) is the softmax function. Then, we obtain the first row that corresponds to the attention from the CLS token to each patch token, as shown as 

am = [ Sm]1,2:( T +1) , (11) The attention map is a H/P × W/P matrix that is reshaped from the 1 × T vector am, denoted as Am. By concatenating all M ′ MSA heads, the multi-head attention map is A = cat( A1, · · · , AM ′ ) that has the dimension of 

M ′ × H/P × W/P . We set the entries larger than ρ to be 1s and all others to be 0s. To obtain the pixel-wise semantic mask M, we directly sum the multi-head attention maps across the head dimension, followed by upsampling to the original image size H ×W using a simple bilinear interpolation operation. References          

> [1] P. Zhang, W. Xu, H. Gao, K. Niu, X. Xu, X. Qin, C. Yuan, Z. Qin, H. Zhao, J. Wei et al., “Toward wisdom-evolutionary and primitive-concise 6G: A new paradigm of semantic commu-nication networks,” Engineering, vol. 8, pp. 60–73, 2022. [2] W. Chen, W. Xu, H. Chen, X. Zhang, Z. Qin, Y. Zhang, and Z. Han, “Semantic communication based on large language model for underwater image transmission,” IEEE Transactions on Mobile Computing, vol. 25, no. 2, pp. 2060–2075, Feb. 2026.

[3] D. Gündüz, Z. Qin, I. E. Aguerri, H. S. Dhillon, Z. Yang, A. Yener, K. K. Wong, and C.-B. Chae, “Beyond transmitting bits: Context, semantics, and task-oriented communications,” IEEE Journal on Selected Areas in Communications, vol. 41, no. 1, pp. 5–41, Jan. 2022. [4] P. Jiang, C.-K. Wen, S. Jin, and G. Y. Li, “Deep source-channel coding for sentence semantic transmission with HARQ,” IEEE transactions on communications, vol. 70, no. 8, pp. 5225–5240, Aug. 2022. [5] N. Li, Y. Deng, and D. Niyato, “Goal-oriented semantic com-munication for wireless video transmission via generative AI,” IEEE Transactions on Wireless Communications, vol. 25, pp. 10 841–10 854, 2026. [6] J. Shao, Y. Mao, and J. Zhang, “Learning task-oriented com-munication for edge inference: An information bottleneck ap-proach,” IEEE Journal on Selected Areas in Communications, vol. 40, no. 1, pp. 197–211, Jan. 2021. [7] E. Bourtsoulatze, D. B. Kurka, and D. Gündüz, “Deep joint source-channel coding for wireless image transmission,” IEEE Transactions on Cognitive Communications and Networking, vol. 5, no. 3, pp. 567–579, Sep. 2019. [8] Q. Luo, Y. Guo, A. Zheng, Z. Ni, F. R. Yu, and V. C. Leung, “Wireless semantic communication based on probability distribution: An initial work,” in 2025 IEEE Wireless Commu-nications and Networking Conference (WCNC), 2025, pp. 1–6. [9] S. Liu, N. Li, Y. Deng, and T. Q. S. Quek, “Goal-oriented se-mantic communication for wireless visual question answering,” IEEE Journal on Selected Areas in Communications, vol. 43, no. 12, pp. 4247–4261, Dec. 2025. [10] W. Wu, M. Qiu, Y. Deng, and J. Yuan, “Guaranteed image classification via goal-oriented joint semantic source and channel coding,” arXiv preprint arXiv:2603.01872, 2026. [11] M. Caron, H. Touvron, I. Misra, H. Jégou, J. Mairal, P. Bo-janowski, and A. Joulin, “Emerging properties in self-supervised vision transformers,” in Proceedings of the IEEE/CVF interna-tional conference on computer vision, 2021, pp. 9650–9660. [12] P. Wang, H. Zhang, F. Zhou, and Z. Jiang, “Unsupervised remote sensing image super-resolution using cycle CNN,” in IGARSS 2019 - 2019 IEEE International Geoscience and Re-mote Sensing Symposium, 2019, pp. 3117–3120. [13] K. He, X. Chen, S. Xie, Y. Li, P. Dollár, and R. Girshick, “Masked autoencoders are scalable vision learners,” in Pro-ceedings of the IEEE/CVF conference on computer vision and pattern recognition, 2022, pp. 16 000–16 009.