---
title: Lossless Compression via Chained Lightweight Neural Predictors with Information Inheritance
title_zh: 基于信息继承的链式轻量级神经网络预测器实现无损压缩
authors: "Yuriy Kim, Evgeny Belyaev"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15472v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 基于神经预测器链的概率估计无损压缩架构
tldr: 本文提出一种基于链式轻量级神经预测器的无损压缩架构，通过极小权重的神经网络单元串联并引入信息继承机制，实现了高效的概率估计。该方法在保证压缩率接近最先进水平的同时，利用轻量化设计大幅提升了编解码速度，有效解决了传统神经压缩模型计算复杂度高的问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15472v1/fig-001.webp\", \"caption\": \"TABLE I: Description of the datasets used in the experiments\", \"page\": 5, \"index\": 1, \"width\": 540, \"height\": 292}]"
motivation: 针对现有神经网络无损压缩模型计算开销大、难以在保证压缩率的同时实现高吞吐量的问题。
method: 采用由极简权重单元构成的链式架构，并引入信息继承机制，将低阶单元的概率估计传递给高阶单元以增强预测。
result: 在GPU上，该方法的压缩率接近SOTA水平，且编解码吞吐量分别比PAC压缩器提升了1.2至6.3倍和2.8至12.3倍。
conclusion: 链式轻量级神经预测器结合信息继承，在维持高压缩性能的同时显著优化了推理效率，为高效无损压缩提供了新方案。
---

## 摘要
本文致力于研究利用神经网络进行概率估计的无损数据压缩。首先，我们提出了一种基于链式神经网络预测器的概率估计架构，其中链中的每个单元都被定义为具有最小可能权重数量的神经网络，这足以对给定阶数的马尔可夫源生成的数据进行高效压缩。我们表明，该架构允许我们根据输入数据的统计特性，最小化参与概率估计过程的总权重数量。其次，为了提高压缩效率，我们引入了一种信息继承机制，即低阶单元获得的概率估计被用于下一个高阶单元。实验结果表明，所提出的配备链式概率估计架构的无损数据压缩器提供的压缩比接近最先进的 PAC 压缩器。同时，在消费级 GPU 上，其编码吞吐量优于 PAC 1.2 到 6.3 倍，解码吞吐量优于 PAC 2.8 到 12.3 倍。

## Abstract
This paper is dedicated to lossless data compression with probability estimation using neural networks. First, we propose a probability estimation architecture based on a chain of neural predictors, so that each unit of the chain is defined as a neural network with the minimum possible number of weights, which is sufficient for efficient compression of data generated by Markov sources of a given order. We show that this architecture allows us to minimize the overall number of weights participating in the probability estimation process depending on the statistical properties of the input data. Second, in order to improve compression efficiency, we introduce an information inheritance mechanism, where the probability estimate obtained by a low-order unit is used at the next higher-order unit. Experimental results show that the proposed lossless data compressor equipped with the chained probability estimation architecture provides compression ratios close to the state-of-the-art PAC compressor. At the same time, it outperforms PAC by a factor of 1.2 to 6.3 in encoding throughput and by a factor of 2.8 to 12.3 in decoding throughput on a consumer GPU.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“基于信息继承的链式轻量级神经预测器”的无损压缩方法，旨在解决现有神经压缩算法计算开销过大、编解码速度慢的问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的神经无损压缩器（如 PAC、TRACE）虽然在压缩率上优于传统算法（如 Gzip、7z），但其计算复杂度极高，编解码吞吐量通常仅为几十 KB/s，难以满足实际应用需求。
*   **研究动机**：
    *   **计算冗余**：现有模型（如 PAC）对所有类型的数据使用统一的大型架构，但在处理统计特性简单的源（如随机数据或低阶马尔可夫源）时，这种设计会造成严重的算力浪费。
    *   **解码延迟**：自适应神经压缩器需要在解码端进行耗时的反向传播（训练），这极大地限制了吞吐量。
    *   **目标**：设计一种能够根据数据复杂度自适应调整计算量，且无需在解码端进行训练的高效神经压缩架构。

### 2. 方法论
*   **核心思想**：将概率估计任务分解为一系列由轻量级神经单元组成的“链条”，每个单元对应不同阶数的马尔可夫源建模，并引入信息继承机制。
*   **关键技术细节**：
    *   **链式轻量级预测器**：构建 6 个单元（Unit 1-6），分别针对 1, 2, 3, 4, 8, 16 阶马尔可夫源。通过神经架构搜索（NAS）为不同阶数选择最合适的轻量级模型（包括 MLP、CNN、GRU 和 Transformer）。
    *   **信息继承机制**：借鉴传统 PPM 算法的思想，高阶单元在预测时会线性融合低阶单元生成的 Logits（概率分布特征），公式为：$l_i = \alpha_i \cdot l'_i + \beta_i \cdot l'_{i-1}$。
    *   **半自适应方案**：在编码端针对输入数据训练模型，将量化后的权重（通过剪枝和矢量量化压缩）放入压缩包头部。解码端直接加载权重进行推理，避免了反向传播。
    *   **自适应单元禁用**：引入拉格朗日乘子 $\lambda$ 平衡压缩率与耗时。如果增加一个高阶单元带来的压缩收益不足以抵消其增加的时间开销，则自动停止使用更高阶的单元。
    *   **数据驱动分词（Tokenization）**：使用字节对编码（BPE）减少序列长度，提升处理速度。

### 3. 实验设计
*   **数据集**：涵盖了多种模态，包括文本（Enwik8/9, Books）、图像（Image100）、音频（Sound100）、基因序列（AnCa, GaGa）、浮点数数据（Spitzer）以及随机数据（Rand）。
*   **Benchmark（基准）**：
    *   **传统方法**：Gzip (-9), 7z (LZMA2), Zstd (-22)。
    *   **神经方法**：PAC（当时的最先进水平 SOTA）、TRACE。
*   **对比指标**：每符号比特数（BPS，越低越好）、编码吞吐量（KB/s）、解码吞吐量（KB/s）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i5-12400F CPU，**NVIDIA GeForce RTX 4060 Ti GPU** (消费级显卡)，64GB RAM。
*   **训练细节**：Batch size 为 4096；每个单元训练 3 个 Epoch（1 个无继承预训练，2 个带继承联合优化）；使用 Adam 优化器。
*   **算力开销**：文中给出了各单元的参数量（0.02M 到 0.82M）和推理 FLOPs，相比 PAC（19.74M 参数）显著更轻量。

### 5. 实验数量与充分性
*   **实验规模**：在 13 个不同规模和类型的数据集上进行了测试，涵盖了从 100MB 到 1.2GB 的数据量。
*   **消融实验**：
    *   验证了**信息继承**的有效性（在某些数据集上可降低高达 13.9% 的 BPS）。
    *   测试了**分词器（Tokenizer）**字典大小对性能的影响。
    *   研究了**验证集比例**对早停策略和最终压缩率的影响。
*   **评价**：实验设计较为全面，对比了不同领域的多种数据类型，且直接在消费级硬件上测试吞吐量，具有较高的客观性和实际参考价值。

### 6. 主要结论与发现
*   **压缩率**：提出的方法在平均 BPS 上与 SOTA 的 PAC 非常接近（在大型数据集上甚至略优），显著优于传统算法和 TRACE。
*   **速度优势**：在 GPU 上，该方法的编码速度是 PAC 的 1.2 到 6.3 倍，**解码速度更是达到了 PAC 的 2.8 到 12.3 倍**。
*   **自适应性**：对于简单数据（如随机数据），系统能自动识别并仅使用最简单的 Unit 1，从而极大地节省计算资源。

### 7. 优点
*   **解码高效**：通过半自适应设计和轻量化架构，解决了神经压缩器解码过慢的痛点。
*   **架构灵活**：链式结构允许在压缩效率和计算成本之间进行精细的权衡。
*   **信息融合**：创新的信息继承机制证明了低阶统计信息对高阶神经预测有显著辅助作用。

### 8. 不足与局限
*   **绝对速度仍受限**：尽管比 PAC 快很多，但几百 KB/s 的速度与传统算法（如 Zstd 可达数百 MB/s）相比仍有数量级的差距。
*   **头部开销**：由于需要存储模型权重，对于极小文件的压缩可能不划算（尽管文中主要针对 100MB+ 的数据）。
*   **模型限制**：目前链条长度固定为 6，且马尔可夫阶数是预设的，可能无法完美匹配所有特殊分布的数据。

（完）
