---
title: "Generation Is Compression: Zero-Shot Video Coding via Stochastic Rectified Flow"
title_zh: 生成即压缩：基于随机修正流的零样本视频编码
authors: "Ziyue Zeng, Xun Su, Haoyuan Liu, Bingyu Lu, Yui Tatsumi, Hiroshi Watanabe"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26571v1"
tags: ["query:vcodec"]
score: 10.0
evidence: 基于随机修正流和零样本视频编码的生成式视频编解码器
tldr: 该研究提出Generative Video Codec (GVC)，一种无需重新训练的零样本视频压缩框架。它将预训练视频生成模型直接转化为编解码器，通过将确定性修正流ODE转换为等效SDE，在推理时引入随机注入点进行码本驱动压缩。GVC支持图像到视频、文本到视频及首尾帧到视频三种策略，在极低比特率（<0.002 bpp）下实现了高质量视频重建，并在保真度与效率间取得了良好平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26571v1/fig-001.webp\", \"caption\": \"Figure 2: Hyperparameter sweeps on UVG Beauty (T2V-1.3B, 720p). Blue: PSNR (left axis, ↑). Red: LPIPS (right axis, ↓). Purple: encoding time (where shown). Stars: selected defaults. (a) M . (b) K. (c) T . (d) gscale. (e) GOP length.\", \"page\": 7, \"index\": 1, \"width\": 706, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2603.26571v1/fig-002.webp\", \"caption\": \"Table 3: Default hyperparameters for GVC (720p / 1080p).\", \"page\": 9, \"index\": 2, \"width\": 586, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.26571v1/fig-003.webp\", \"caption\": \"Table 2: State-of-the-art comparison on UVG 1080p at three bitrate tiers corresponding to the three GVC variants. Baseline results are interpolated to matched BPP from published RD curves or our measurements. Bold: best LPIPS per tier. Underline: second best.\", \"page\": 9, \"index\": 3, \"width\": 714, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2603.26571v1/fig-004.webp\", \"caption\": \"Figure 1: GVC produces perceptually superior reconstruction at ultra-low bitrates. Left: diagonal split comparison between DCVC-RT (0.0017 bpp) and our GVC-T2V (0.0016 bpp) on the UVG Jockey sequence. GVC recovers sharp textures and coherent details while DCVC-RT exhibits severe oversmoothing. Middle: zoomed-in crops comparing DCVC-RT, GNVC-VD, and GVC. Right: LPIPS comparison shows GVC achieves a 70.3% reduction over DCVC-RT at comparable bitrate; a user study confirms that GVC is preferred over DCVC-RT in 97% and over GNVC-VD in 88% of pairwise comparisons.\", \"page\": 2, \"index\": 4, \"width\": 829, \"height\": 234}]"
motivation: 现有生成式视频压缩方法仅将生成模型作为后处理模块，未能直接利用生成模型本身的轨迹进行端到端编码。
method: 通过将修正流ODE转换为等效SDE，在推理阶段引入随机注入点进行码本驱动压缩，并结合I2V、T2V和FLF2V三种条件策略实现时空控制。
result: 实验表明GVC在低于0.002 bpp的极低比特率下仍能保持高质量重建，并可通过单一超参数实现灵活的比特率调节。
conclusion: 该框架证明了预训练视频生成模型可直接作为高效的零样本编解码器，开辟了“生成即压缩”的新路径。
---

## 摘要
现有的生成式视频压缩方法仅将生成模型作为传统编解码器之上的事后重建模块。我们提出了生成式视频编解码器（Generative Video Codec, GVC），这是一个零样本框架，可将预训练的视频生成模型直接转化为编解码器本身：传输的比特流直接指定生成式解码轨迹，无需任何重新训练。为了实现这一点，我们在推理时将现代视频基础模型的确定性修正流 ODE 转换为等效的 SDE，从而为基于码本的压缩解锁了每一步的随机注入点。基于这一统一骨干网络，我们实例化了三种互补的条件策略：具有自适应尾帧原子分配的“图像到视频”（I2V）、作为纯生成先验且在近乎零辅助信息下运行的“文本到视频”（T2V），以及具有边界共享 GOP 链式结构以实现双锚点时间控制的“首尾帧到视频”（FLF2V）。这些变体共同构成了一个在空间保真度、时间连贯性和压缩效率之间进行权衡的原则性空间。在标准基准测试上的实验表明，GVC 在低于 0.002 bpp 的码率下实现了高质量重建，同时支持通过单个超参数进行灵活的比特率控制。

## Abstract
Existing generative video compression methods use generative models only as post-hoc reconstruction modules atop conventional codecs. We propose \emph{Generative Video Codec} (GVC), a zero-shot framework that turns a pretrained video generative model into the codec itself: the transmitted bitstream directly specifies the generative decoding trajectory, with no retraining required. To enable this, we convert the deterministic rectified-flow ODE of modern video foundation models into an equivalent SDE at inference time, unlocking per-step stochastic injection points for codebook-driven compression. Building on this unified backbone, we instantiate three complementary conditioning strategies -- \emph{Image-to-Video} (I2V) with adaptive tail-frame atom allocation, \emph{Text-to-Video} (T2V) operating at near-zero side information as a pure generative prior, and \emph{First-Last-Frame-to-Video} (FLF2V) with boundary-sharing GOP chaining for dual-anchor temporal control. Together, these variants span a principled trade-off space between spatial fidelity, temporal coherence, and compression efficiency. Experiments on standard benchmarks show that GVC achieves high-quality reconstruction below 0.002\,bpp while supporting flexible bitrate control through a single hyperparameter.

---

## 论文详细总结（自动生成）

以下是对论文《Generation Is Compression: Zero-Shot Video Coding via Stochastic Rectified Flow》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在极低比特率下，传统视频编解码器（如 HEVC/VVC）和现有的深度学习编解码器往往会出现严重的细节丢失和过度平滑。
*   **核心问题**：现有的生成式视频压缩方法大多将生成模型视为“事后”的增强模块，而非编解码器本身。论文探讨了是否可以直接利用预训练的视频生成模型作为编解码器，使传输的比特流直接控制生成轨迹。
*   **整体含义**：提出 **GVC (Generative Video Codec)** 框架，实现“生成即压缩”。这是一种**零样本（Zero-shot）**方案，无需对预训练模型进行任何重新训练，通过控制生成过程中的随机性来实现高效压缩。

### 2. 方法论
*   **核心思想**：将视频生成模型的确定性推理轨迹转变为可由码本（Codebook）驱动的随机轨迹。
*   **关键技术细节**：
    *   **ODE 到 SDE 的转换**：现代视频模型（如 Wan 2.1）基于修正流（Rectified Flow），其推理是确定性的 ODE 过程。GVC 利用 Score-SDE 理论，在推理时将其转换为等效的随机微分方程（SDE），从而引入每一步的随机注入点。
    *   **码本驱动采样**：在 SDE 的每一步中，不使用随机噪声，而是从编码器和解码器共享的码本中选择与残差最匹配的“原子”（Atoms）。编码器只需传输这些原子的索引和符号。
    *   **三种条件策略**：
        1.  **T2V (Text-to-Video)**：纯生成先验，不传输参考帧，比特率极低。
        2.  **I2V (Image-to-Video)**：自回归 GOP 链式结构，利用首帧作为锚点，并引入“尾帧原子分配”和“尾部潜变量残差校正”来抑制误差累积。
        3.  **FLF2V (First-Last-Frame-to-Video)**：双锚点控制，每组 GOP 传输首尾两帧，并采用“边界共享 GOP 链”技术，使相邻 GOP 共享边界帧，减少 50% 的参考帧开销。

### 3. 实验设计
*   **数据集**：标准基准数据集 UVG（720p 和 1080p 分辨率），以及用于验证泛化性的自拍视频。
*   **对比方法（Benchmark）**：
    *   传统编解码器：HEVC, VVC。
    *   学习型编解码器：DCVC-FM, DCVC-RT。
    *   生成式编解码器：GLC-Video, GNVC-VD（目前最先进的生成式方法）。
*   **评价指标**：PSNR、MS-SSIM（像素保真度）、LPIPS（感知质量）、BPP（比特率）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 6000 Ada (48 GB)** 上运行。
*   **训练时长**：由于是**零样本**方法，该框架**不需要任何重新训练**，直接加载预训练的 Wan 2.1 权重即可使用。
*   **推理开销**：文中通过消融实验展示了不同码本大小（K）和采样步数（T）对耗时的影响（例如 K=65536 时的编码时间是 K=1024 的 2.6 倍），但未给出完整的端到端实时性数据。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的**超参数扫描**（包括原子数 M、码本大小 K、步数 T、扩散缩放因子 $g_{scale}$ 和 GOP 长度）。
    *   在 UVG 全集上进行了跨变体（T2V vs I2V vs FLF2V）的对比。
    *   针对三个不同的比特率层级（Tier 1~3）与 SOTA 方法进行了性能对齐比较。
*   **充分性与公平性**：实验设计较为全面，包含了主观用户调研（User Study），结果显示 GVC 在 97% 的情况下优于 DCVC-RT。通过插值法对齐比特率进行对比，保证了客观指标比较的公平性。

### 6. 主要结论与发现
*   **极低码率下的卓越表现**：GVC 在低于 **0.002 bpp** 的码率下，感知质量（LPIPS）显著优于传统和学习型编解码器（相比 DCVC-RT 降低了 70.3% 的感知失真）。
*   **策略权衡**：FLF2V 在边际效率上表现最佳，能以极小的比特率代价换取接近 I2V 的保真度。
*   **灵活性**：通过调整单一超参数（原子数 M），即可实现对比特率的精细控制。

### 7. 优点
*   **无需训练**：直接复用强大的视频基础模型，降低了开发和部署成本。
*   **真正的生成式编码**：比特流直接定义生成轨迹，而非简单的后处理，充分挖掘了生成模型的先验能力。
*   **创新的 GOP 机制**：边界共享和自适应尾帧分配有效解决了长视频序列中的时间漂移和不连续问题。

### 8. 不足与局限
*   **计算复杂度**：基于扩散/流模型的推理过程涉及多次网络前向计算，编解码速度远慢于传统方法，难以满足实时应用需求。
*   **像素保真度挑战**：虽然感知质量极高，但在 PSNR 等传统指标上可能仍逊色于专门优化的学习型编解码器（这是生成式压缩的共性问题）。
*   **模型依赖性**：压缩效果高度依赖于底层预训练视频模型的质量，若模型存在生成伪影，会直接反映在解码视频中。

（完）
