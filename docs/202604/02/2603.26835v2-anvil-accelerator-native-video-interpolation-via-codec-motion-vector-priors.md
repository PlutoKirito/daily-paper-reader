---
title: "ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors"
title_zh: ANVIL：基于编解码器运动矢量先验的加速器原生视频插帧
authors: Shibo Liu
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26835v2"
tags: ["query:train-trick"]
score: 8.0
evidence: 复用编解码器运动矢量进行插帧与NPU优化
tldr: 针对移动端NPU上实时视频插帧面临的硬件适配和量化性能下降问题，本文提出ANVIL。该方法通过复用H.264/AVC解码器的运动矢量进行预对齐，消除了传统方法中耗时的光流计算和空间采样算子。ANVIL采用纯卷积网络进行残差细化，在Snapdragon 8 Gen 3上实现了12.8ms的1080p推理速度，并解决了8位整数化后的精度崩溃问题，为移动端实时视频插帧提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26835v2/fig-001.webp\", \"caption\": \"TABLE IV PER-OPERATOR INSTRUMENTED INT8 QUANTIZATION. COSSIM MEASURED AGAINST FP32 OUTPUT. THE SAME CAUSAL PATTERN HOLDS FOR BOTH METHODS: CONV INTRODUCES INITIAL ERROR, PRELU HAS ZERO EFFECT, AND ADD TRIGGERS THE COLLAPSE. FULL W8A8 ≈ CONV+ADD.\", \"page\": 7, \"index\": 1, \"width\": 505, \"height\": 405}, {\"url\": \"assets/figures/arxiv/2603.26835v2/fig-002.webp\", \"caption\": \"TABLE II CROSS-VENDOR OPERATOR COMPATIBILITY FOR 17 VFI OPERATORS AT 1080P. LATENCY NORMALIZED TO CONV 3×3 = 1.0×. “FREQ”: METHODS USING THE OPERATOR (OUT OF 9 SURVEYED). GROUPS: (A) UNIVERSAL, (B) PARTIAL, (C) LIMITED, (D) ITERATIVE PATTERNS.\", \"page\": 7, \"index\": 2, \"width\": 810, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2603.26835v2/fig-003.webp\", \"caption\": \"TABLE V QUALITY AND DEPLOYMENT STATUS ON VIMEO90K AND XIPH 1080P. BOLD: BEST AMONG DEPLOYABLE METHODS. FOR CONTEXT, NON-DEPLOYABLE METHODS REACH 35–36 DB ON VIMEO90K (IFRNET [2] 35.80, AMT-S [3] 35.72, EMA-VFI [6] 36.11).\", \"page\": 8, \"index\": 3, \"width\": 840, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2603.26835v2/fig-004.webp\", \"caption\": \"Fig. 4. Quality scaling with model capacity. Basic prealignment (dashed) and smoothed prealignment (solid) series both scale without saturation. Dashed horizontal lines show RIFE HDv3 (3.04M) and NAFNet ceiling (17.1M, smoothed prealignment retrained) as reference points.\", \"page\": 9, \"index\": 4, \"width\": 498, \"height\": 394}]"
motivation: 传统基于光流的插帧方法在移动端NPU上存在算子不支持、8位量化精度差以及内存受限等部署障碍。
method: 利用H.264/AVC解码器自带的运动矢量预对齐输入帧，并使用仅包含计算密集型卷积算子的网络进行残差细化。
result: 在Snapdragon 8 Gen 3上，ANVIL实现了12.8ms的1080p 8位整数推理，且在30分钟持续播放中保持了极低的端到端延迟。
conclusion: 通过复用编解码器先验信息并优化算子结构，ANVIL成功克服了移动端NPU部署视频插帧的结构性障碍，实现了高性能的实时插帧。
---

## 摘要
在移动端神经网络处理器（NPU）上实现 30 到 60 fps 的实时视频帧插值，要求每帧合成时间在 33.3 毫秒以内。我们指出，主流的基于流（flow-based）的视频帧插值在移动端 NPU 上面临三个结构性部署障碍：空间采样算子超出时间预算或缺乏硬件支持；迭代流细化在 8 位整数训练后量化下会失效；以及受内存限制的算子主导了推理图。ANVIL 通过复用 H.264/AVC 解码器中的运动矢量来预对齐输入帧，从而解决了这些障碍，并从加速器计算图中移除了学习型光流、空间采样和迭代累加。剩余的残差由一个以卷积为主的网络进行细化，该网络几乎完全由受计算限制的算子组成。在骁龙 8 Gen 3 设备上，ANVIL 在 8 位整数精度下实现了 12.8 毫秒的 1080p 推理；一个开源 Android 播放器在 30 分钟的连续播放中保持了 28.4 毫秒的中值端到端延迟。逐算子因果分析表明，循环流状态上的量化累加是迭代方法中整数量化失败的关键机制。当前设计针对具有解码器暴露运动矢量的 H.264/AVC 播放。

## Abstract
Real-time 30-to-60 fps video frame interpolation on mobile neural processing units (NPUs) requires each synthesized frame within 33.3 ms. We show that mainstream flow-based video frame interpolation faces three structural deployment barriers on mobile NPUs: spatial sampling operators exceed the frame budget or lack hardware support, iterative flow refinement collapses under 8-bit integer post-training quantization, and memory-bound operators dominate the inference graph. ANVIL addresses these barriers by reusing motion vectors from the H.264/AVC decoder to prealign input frames, removing learned optical flow, spatial sampling, and iterative accumulation from the accelerator graph. The remaining residual is refined by a convolution-dominated network composed almost entirely of compute-bound operators. On a Snapdragon 8 Gen 3 device, ANVIL achieves 12.8 ms 1080p inference at 8-bit integer precision; an open-source Android player sustains 28.4 ms median end-to-end latency over 30-minute continuous playback. Per-operator causal analysis identifies quantized accumulation on recurrent flow states as a key mechanism behind integer quantization failure in iterative methods. The current design targets H.264/AVC playback with decoder-exposed motion vectors.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors》** 的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于在**移动端神经处理单元（NPU）**上实现实时 1080p（30fps 补帧至 60fps）视频插帧（VFI）的落地难题。
*   **核心障碍：** 作者指出主流基于光流（Flow-based）的方法在移动端部署面临三大“结构性屏障”：
    1.  **算子不兼容：** 核心的 `grid_sample`（双线性采样）算子在 NPU 上极慢或不被支持。
    2.  **量化崩溃：** 移动端 NPU 偏好 INT8 量化，但迭代式流细化结构在 INT8 下会产生严重的精度衰减（PSNR 剧降）。
    3.  **内存受限：** 推理图中充斥着大量非计算密集型算子（如 Resize、Concat），导致无法充分发挥 NPU 的算力。
*   **研究意义：** 通过解决这些问题，可以将插帧任务从云端卸载到移动终端，降低带宽成本并保护隐私。

### 2. 论文提出的方法论
**核心思想：** 既然 NPU 跑不好光流计算和空间采样，那就把这两部分移出 NPU。ANVIL 利用 H.264 视频解码器中现成的**运动矢量（Motion Vectors, MVs）**作为零成本的运动先验，将插帧任务分解为“编解码器端预对齐”和“NPU 端残差细化”。

**关键技术细节：**
*   **MV 提取与平滑：** 从 H.264 P 帧中提取块级 MV，通过 CPU 进行零阶保持填充，再利用 GPU 进行空间平滑（5x5 中值滤波 + 高斯模糊），生成稠密流场。
*   **三处理器协同流水线：**
    *   **CPU：** 负责 MV 提取、下采样与数据打包。
    *   **GPU (Vulkan)：** 负责流场平滑、图像 Warp（预对齐）及量化预处理。
    *   **NPU (HTP/APU)：** 运行纯卷积构成的残差网络（UNet-v3b），预测预对齐帧与真实帧之间的微小残差。
*   **NPU 原生架构优化：**
    *   **算子约束：** 仅使用 3x3 卷积、ReLU、加法和步长卷积，避开 LayerNorm、GELU 等 NPU 弱势算子。
    *   **部署优化：** 采用加法跳跃连接（替代 Concat 以节省内存）和部署时 Batch Normalization 融合。

### 3. 实验设计
*   **数据集：** 训练使用 **Vimeo90K**；评估使用 Vimeo90K 测试集和 **Xiph 1080p**（用于验证高分辨率泛化性）。
*   **Benchmark 与对比方法：**
    *   **对比模型：** RIFE (HDv3)、IFRNet、AMT-S、EMA-VFI 等 9 种主流方法。
    *   **对比维度：** 跨平台算子兼容性、INT8 量化后的 PSNR 损失、1080p 推理延迟。
*   **硬件平台：** 骁龙 8 Gen 2/3 (Qualcomm)、天玑 9300/9400+ (MediaTek)。

### 4. 资源与算力
*   **训练配置：** 使用 AdamW 优化器，学习率 2e-4，Batch Size 为 16，采用 AMP bfloat16 混合精度训练。
*   **算力说明：** 文中未明确给出训练所用的具体 GPU 型号（如 A100/H100）或总训练时长，但强调了推理端的性能：在骁龙 8 Gen 3 上，ANVIL-S 的 NPU 推理仅需 **12.8ms**。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了系统性的算子级基准测试（覆盖 17 种算子在不同 NPU 上的表现），并对 54,623 个连续帧对进行了长达 30 分钟的端到端稳定性测试。
*   **消融实验：** 针对预对齐平滑策略、模型容量缩放、跳跃连接方式等进行了详细消融。
*   **客观性与公平性：** 作者通过“因果分析”定位了量化崩溃的根源（循环状态下的 Add 算子），并在两个不同厂商的 NPU 栈上验证了结论，实验设计严谨且具有高度的工程参考价值。

### 6. 论文的主要结论与发现
*   **量化失效机制：** 发现迭代流模型在 INT8 下崩溃是因为循环流状态上的加法操作不断放大误差。
*   **性能突破：** ANVIL 是首个在公开移动 NPU 栈上实现 1080p 30->60fps 稳定运行（端到端中值延迟 28.4ms）的深度学习插帧方案。
*   **算子 affinity：** 证明了在移动端，算子与加速器的契合度（Affinity）比单纯的 FLOPs 或参数量更能决定实际性能。

### 7. 优点
*   **极高的工程实用性：** 避开了学术界常用但工业界难部署的复杂算子，直接利用编解码器副产物。
*   **系统级视角：** 不仅仅关注模型指标，还考虑了热节流（Thermal Throttling）、内存带宽和多处理器协同。
*   **深度诊断：** 对 INT8 量化失败的底层原因分析透彻，为后续移动端模型设计提供了指导。

### 8. 不足与局限
*   **感知质量损失：** 由于采用了残差预测而非子像素级学习采样，其感知指标（LPIPS）和细节保留能力略逊于非实时、非量化的复杂流模型（如 RIFE 原版）。
*   **编解码器依赖：** 目前高度依赖 H.264 的 MV 导出。虽然理论上支持 HEVC/AV1，但目前主流移动端解码 API（如 Android MediaCodec）尚未公开暴露这些底层 MV 数据，限制了通用性。
*   **B 帧限制：** 在处理包含复杂 B 帧结构的视频流时，由于 MV 引用距离增加，插帧质量会有所下降。

（完）
