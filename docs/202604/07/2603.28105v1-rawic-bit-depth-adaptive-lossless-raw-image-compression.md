---
title: "RAWIC: Bit-Depth Adaptive Lossless Raw Image Compression"
title_zh: RAWIC：位深自适应的无损 Raw 图像压缩
authors: "Chunhang Zheng, Tongda Xu, Mingli Xie, Yan Wang, Dou Li"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28105v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 学习型无损压缩框架
tldr: "针对Raw图像存储挑战，本文提出RAWIC，一种位深自适应的深度学习无损压缩框架。该方法将Bayer图像转为RGGB格式并分块，利用分块位深作为辅助信息引导熵模型，实现了单一模型对不同相机和位深的兼容。实验表明，RAWIC在压缩性能上显著优于传统编解码器，比JPEG-XL平均降低了7.7%的比特率，具有极高的实用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28105v1/fig-001.webp\", \"caption\": \"Fig. 1. The comparison between previous bit-depth-specific lossless image compression methods and our proposed bit-depth adaptive lossless raw image compression method. (a) Exiting methods train the model a specific bit depth and can only compress raw images with the same bit depth. (b) Our proposed method uses a single model to compress raw images across different bit depths.\", \"page\": 1, \"index\": 1, \"width\": 522, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2603.28105v1/fig-002.webp\", \"caption\": \"Fig. 2. Visualization of raw image and corresponding pixel-wise bit depth map. The pixel-wise bit depth is computed as ⌈log2(value + 1)⌉. It can be observed that different regions in the raw image exhibit varying bit depths.\", \"page\": 2, \"index\": 2, \"width\": 480, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2603.28105v1/fig-003.webp\", \"caption\": \"Fig. 3. The overall architecture of RAWIC. The input raw image is first converted to an RGGB four-channel format and split into patches. Each patch is then processed by a bit-depth adaptive entropy model that estimates the pixel distribution conditioned on the bit depth of the patch. Q denotes the quantization operation. AE and AD represent arithmetic encoder and arithmetic decoder, respectively.\", \"page\": 3, \"index\": 3, \"width\": 1068, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2603.28105v1/fig-004.webp\", \"caption\": \"TABLE I LOSSLESS IMAGE COMPRESSION PERFORMANCE (BPP) OF OUR PROPOSED METHOD COMPARED TO OTHER LOSSLESS IMAGE CODEC CANON 1DS MKIII, CANON 600D, OLYMPUS EPL6, PANASONIC GX1, SAMSUNG NX2000 AND RAISE. THE BEST RESULTS ARE HIGHLIGHTED IN BOLD. WE SHOW THE DIFFERENCE (%) BETWEEN OUR METHOD AND EACH TRADITIONAL CODEC IN GREEN COLOR. NOTE THAT OUR METHOD INCLUDES THE BITRATES FOR STORING BIT DEPTHS.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2603.28105v1/fig-005.webp\", \"caption\": \"TABLE IV AVERAGE RUNTIME (SEC.) OF RAWIC COMPARED TO OTHER CODECS (ENCODING/DECODING TIME). OOM: OUT OF MEMORY.\", \"page\": 5, \"index\": 5, \"width\": 490, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.28105v1/fig-006.webp\", \"caption\": \"TABLE II LOSSLESS RGB IMAGE COMPRESSION PERFORMANCE (BPP) COMPARED TO OTHER LEARNED LOSSLESS IMAGE CODECS.\", \"page\": 5, \"index\": 6, \"width\": 535, \"height\": 280}]"
motivation: 现有无损压缩方法多针对8位sRGB图像，难以处理Raw图像文件大、位深多变且传感器依赖性强的存储难题。
method: 将Bayer数据转为四通道RGGB并分块，设计位深自适应熵模型，利用分块位深信息动态引导概率分布估计。
result: "RAWIC在多种相机和位深设置下均优于传统编解码器，相比JPEG-XL实现了平均7.7%的比特率节省。"
conclusion: 该框架通过位深自适应机制，为不同来源的Raw图像提供了一种高效、通用且性能领先的无损压缩方案。
---

## 摘要
Raw 图像保留了对高级视觉任务和摄影应用至关重要的线性传感器测量值和高位深信息，但由于文件体积大、位深各异以及传感器相关的特性，其存储仍面临挑战。现有的基于学习的无损压缩方法主要针对 8 位 sRGB 图像，而 Raw 重建方法本质上是有损的，且依赖于特定相机的假设。为了应对这些挑战，我们提出了 RAWIC，这是一种针对 Bayer 模式 Raw 图像的位深自适应学习型无损压缩框架。我们首先将单通道 Bayer 数据转换为四通道 RGGB 格式，并将其划分为图像块。对于每个图像块，我们计算其位深，并将其作为辅助输入来引导压缩。随后，我们设计了一个位深自适应熵模型，用于在给定其位深的条件下估计图像块的分布。该架构使单个模型能够处理来自不同相机和不同位深的 Raw 图像。实验表明，RAWIC 始终优于传统的无损编解码器，相比 JPEG-XL 平均降低了 7.7% 的比特率。我们的代码可在 https://github.com/chunbaobao/RAWIC 获取。

## Abstract
Raw images preserve linear sensor measurements and high bit-depth information crucial for advanced vision tasks and photography applications, yet their storage remains challenging due to large file sizes, varying bit depths, and sensor-dependent characteristics. Existing learned lossless compression methods mainly target 8-bit sRGB images, while raw reconstruction approaches are inherently lossy and rely on camera-specific assumptions. To address these challenges, we introduce RAWIC, a bit-depth-adaptive learned lossless compression framework for Bayer-pattern raw images. We first convert single-channel Bayer data into a four-channel RGGB format and partition it into patches. For each patch, we compute its bit depth and use it as auxiliary input to guide compression. A bit-depth-adaptive entropy model is then designed to estimate patch distributions conditioned on their bit depths. This architecture enables a single model to handle raw images from diverse cameras and bit depths. Experiments show that RAWIC consistently surpasses traditional lossless codecs, achieving an average 7.7% bitrate reduction over JPEG-XL. Our code is available at https://github.com/chunbaobao/RAWIC.

---

## 论文详细总结（自动生成）

以下是对论文《RAWIC: Bit-Depth Adaptive Lossless Raw Image Compression》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：Raw 图像直接记录传感器的线性测量值，保留了高位深信息，对去噪、超分辨率等视觉任务至关重要。
*   **核心问题**：
    1.  **存储压力大**：Raw 图像文件体积巨大。
    2.  **位深多样性**：不同相机传感器的位深通常在 10 到 14 位之间波动，且同一图像内不同区域的有效位深也可能不同。
    3.  **现有方案局限**：传统的学习型无损压缩多针对 8 位 sRGB 图像；而基于重建的方法（从 sRGB 恢复 Raw）本质上有损且依赖特定相机参数。
*   **研究动机**：开发一种能够自适应不同位深、兼容多种相机硬件、且完全无损的 Raw 图像压缩框架。

### 2. 方法论
*   **核心思想**：提出 **RAWIC** 框架，通过将位深信息作为辅助输入嵌入到熵模型中，实现单一模型对多位深、多相机数据的自适应无损压缩。
*   **关键技术细节**：
    *   **预处理**：将单通道 Bayer 图像转换为四通道 RGGB 格式，并划分为非重叠的图像块（Patches）。
    *   **位深嵌入**：计算每个图像块的位深 $b_i$，通过嵌入层（Embedding Layer）将其转化为特征向量，引导分析变换和熵模型。
    *   **骨干架构**：采用基于超先验（Hyper-prior）的 ELIC 框架。
    *   **位深自适应熵模型**：
        *   **概率掩码与重归一化**：在估计像素概率分布时，利用指示函数屏蔽掉超出当前位深范围（$[0, 2^{b_i}-1]$）的无效值，并对概率质量函数（PMF）进行重归一化。
        *   **离散逻辑混合似然（DLM）**：扩展 DLM 以处理多通道 Raw 数据，并引入通道自回归建模来捕捉 RGGB 通道间的依赖关系。
    *   **端到端训练**：通过模拟均匀噪声来实现量化过程的微分，优化目标是最小化总比特率（潜在表示比特 + 像素比特）。

### 3. 实验设计
*   **数据集**：
    *   **NUS 数据集**：包含 Canon 1Ds MkIII (14-bit), Canon 600D (14-bit), Olympus EPL6 (12-bit), Panasonic GX1 (12-bit), Samsung NX2000 (12-bit)。
    *   **RAISE 数据集**：用于增强训练数据的多样性。
    *   **sRGB 扩展实验**：使用 DIV2K, CLIC, Kodak 数据集验证其在 8 位图像上的通用性。
*   **Benchmark（对比方法）**：
    *   **传统编解码器**：QOI, PNG, WebP, FLIF, JPEG2000, JPEG-LS, JPEG-XL。
    *   **学习型无损压缩（sRGB 场景）**：L3C, RC, SReC, iVPF, iFlow, DLPR 等。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU** 进行训练。
*   **训练参数**：
    *   训练 200 个 Epoch。
    *   使用 Adam 优化器，初始学习率 $1 \times 10^{-4}$。
    *   Batch size 为 128。
    *   基于 PyTorch 的 CompressAI 库实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同品牌相机、3 种不同位深（12/14/8 bit）的广泛测试。
*   **消融实验**：
    1.  **位深自适应 vs. 固定位深**：证明了自适应模型在低位深数据集上增益巨大（最高提升 75.7%）。
    2.  **全能模型 vs. 特定相机模型**：证明了单一模型在处理多相机数据时甚至优于专门为某一相机训练的模型，体现了强大的泛化性。
*   **充分性评价**：实验设计客观、公平，不仅对比了传统标准，还跨领域对比了最新的深度学习压缩模型，验证了从 Raw 到 sRGB 的全覆盖能力。

### 6. 主要结论与发现
*   **性能领先**：RAWIC 在所有 Raw 测试集上均显著优于传统方法，相比目前最强的传统工具 **JPEG-XL，平均比特率降低了 7.7%**。
*   **通用性强**：通过位深嵌入机制，单个模型即可高效处理来自不同传感器的 Raw 数据，解决了模型碎片化问题。
*   **sRGB 表现**：在 8 位 sRGB 图像压缩任务中，RAWIC 同样达到了 SOTA 水平，优于 DLPR 和 iFlow 等模型。

### 7. 优点
*   **首创性**：第一个直接针对单通道 Bayer 模式 Raw 图像设计的学习型无损压缩框架。
*   **位深感知**：巧妙地利用位深信息作为先验，解决了 Raw 图像动态范围大导致的比特冗余问题。
*   **架构优雅**：将复杂的传感器特性简化为位深特征，实现了“一个模型走天下”。

### 8. 不足与局限
*   **计算复杂度**：作为深度学习模型，其编码和解码时间（如 Canon 600D 需 45s/119s）远高于传统方法（如 PNG 仅需 4.5s/0.6s），难以满足实时处理需求。
*   **内存开销**：文中提到在处理高分辨率图像时，部分对比模型会出现显存溢出（OOM），虽然 RAWIC 表现较好，但其显存占用仍高于传统工具。
*   **应用限制**：目前主要针对 Bayer 模式，对于其他传感器阵列（如 X-Trans）的适用性尚未讨论。

（完）
