---
title: "NeuralLVC: Neural Lossless Video Compression via Masked Diffusion with Temporal Conditioning"
title_zh: NeuralLVC：基于带有时序调节的掩码扩散的神经无损视频压缩
authors: "Tiberio Uricchio, Marco Bertini"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03353v1"
tags: ["query:vcodec"]
score: 10.0
evidence: 通过带有时空调节的掩码扩散进行神经无损视频压缩
tldr: NeuralLVC 是一种基于掩码扩散模型和 I/P 帧架构的神经无损视频压缩方案。它通过双射线性标记化确保 I 帧的精确重建，并利用轻量级参考嵌入对 P 帧进行时间条件建模以消除冗余。该方法支持可控的解码速度与压缩率权衡，在 Xiph 数据集上显著优于 H.264 和 H.265 无损模式，为神经无损视频压缩提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03353v1/fig-001.webp\", \"caption\": \"Table 1: Lossless video compression rates (%, lower is better) on Xiph CIF, YUV420, all frames. Best truly lossless result in bold.\", \"page\": 6, \"index\": 1, \"width\": 586, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.03353v1/fig-002.webp\", \"caption\": \"Figure 1: High-level overview of NeuralLVC. The first frame is coded independently, while later frames are coded from the current frame together with the previous decoded frame. Both branches use the same masked-diffusion entropy-modeling backbone and are finally compressed with arithmetic coding into a single lossless bitstream. Exact token mappings and the group-wise decoding strategy are described in the method section rather than embedded in the figure.\", \"page\": 3, \"index\": 2, \"width\": 1053, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2604.03353v1/fig-003.webp\", \"caption\": \"Figure 4: Per-frame compression rate on two CIF sequences with differentmotion levels (YUV420, all 300 frames, sampled every 5). Our method (solid) produces stable P-frame rates that consistently outperform H.265 lossless (dashed). H.265 exhibits large per-frame fluctuations due to its B-frame GOP structure (235 B, 63 P, 2 I frames), while our codec maintains near-constant rates without error accumulation.\", \"page\": 8, \"index\": 3, \"width\": 466, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2604.03353v1/fig-004.webp\", \"caption\": \"Figure 6: Compression rate vs. encoding speed on Xiph CIF (YUV420, native data). All speeds measured on NVIDIA GH200. ∗VVC QP=0 is near-lossless.\", \"page\": 8, \"index\": 4, \"width\": 457, \"height\": 343}]"
motivation: 尽管神经无损图像压缩已取得进展，但神经无损视频压缩领域仍处于起步阶段，缺乏高效利用时间冗余的方法。
method: 提出 NeuralLVC 架构，结合掩码扩散模型、双射线性标记化以及基于前一帧条件的轻量级时间参考嵌入。
result: 在 9 个 Xiph CIF 序列上的实验表明，NeuralLVC 的压缩性能显著超过了 H.264 和 H.265 的无损压缩标准。
conclusion: 采用时间条件的掩码扩散模型是实现高性能神经无损视频压缩的一种极具前景的技术路径。
---

## 摘要
虽然利用学习型熵模型的神经无损图像压缩已取得显著进展，但在神经领域，无损视频压缩在很大程度上仍未得到充分探索。我们提出了 NeuralLVC，这是一种神经无损视频编解码器，它结合了掩码扩散（masked diffusion）与 I/P 帧架构，以利用时序冗余。我们的 I 帧模型使用双射线性分词（bijective linear tokenization）压缩单个帧，从而保证精确的像素重建。P 帧模型压缩连续帧之间的时序差异，并通过轻量级参考嵌入（仅增加 1.3% 的可训练参数）以先前解码的帧为条件。分组解码实现了可控的速度与压缩率之间的权衡。我们的编解码器在输入域是无损的：对于视频，它能精确重建 YUV420 平面；对于图像评估，它能精确重建 RGB 通道。在 9 个 Xiph CIF 序列上的实验表明，NeuralLVC 的性能显著优于 H.264 和 H.265 无损模式。我们通过结合算术编码的端到端编解码测试验证了精确重建。这些结果表明，带有时序调节的掩码扩散是神经无损视频压缩的一个有前景的方向。

## Abstract
While neural lossless image compression has advanced significantly with learned entropy models, lossless video compression remains largely unexplored in the neural setting. We present NeuralLVC, a neural lossless video codec that combines masked diffusion with an I/P-frame architecture for exploiting temporal redundancy. Our I-frame model compresses individual frames using bijective linear tokenization that guarantees exact pixel reconstruction. The P-frame model compresses temporal differences between consecutive frames, conditioned on the previous decoded frame via a lightweight reference embedding that adds only 1.3% trainable parameters. Group-wise decoding enables controllable speed-compression trade-offs. Our codec is lossless in the input domain: for video, it reconstructs YUV420 planes exactly; for image evaluation, RGB channels are reconstructed exactly. Experiments on 9 Xiph CIF sequences show that NeuralLVC outperforms H.264 and H.265 lossless by a significant margin. We verify exact reconstruction through end-to-end encode-decode testing with arithmetic coding. These results suggest that masked diffusion with temporal conditioning is a promising direction for neural lossless video compression.

---

## 论文详细总结（自动生成）

以下是对论文《NeuralLVC: Neural Lossless Video Compression via Masked Diffusion with Temporal Conditioning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在神经压缩框架下实现**精确无损**的视频压缩，并有效利用视频帧间的时间冗余。
*   **研究动机**：
    *   **应用需求**：医疗影像、影视后期制作及数字存档等领域对“像素级精确”有严格要求，任何有损压缩带来的伪影都是不可接受的。
    *   **技术空白**：虽然神经有损视频压缩（如 DCVC 系列）和神经无损图像压缩（如 HPAC、LLM-based）已取得长足进步，但专门针对**无损视频**的神经编解码器研究较少，现有方法多将视频帧视为独立图像处理，忽略了巨大的时间冗余。

### 2. 方法论
NeuralLVC 结合了掩码扩散模型（Masked Diffusion）与经典的 I/P 帧架构：
*   **核心思想**：利用双射线性标记化将像素映射为 Token，通过带有时序调节的掩码扩散模型预测 Token 的概率分布，最后配合算术编码实现无损压缩。
*   **关键技术细节**：
    *   **双射线性标记化（Bijective Linear Tokenization）**：
        *   **I 帧**：将像素值 $x \in [0, 255]$ 映射为偶数 Token $2x$，确保逆映射唯一且精确。
        *   **P 帧**：计算当前帧与前一帧的差值 $(x_t - x_{t-1}) + 255$，映射到 $[0, 510]$ 范围。
    *   **掩码扩散熵模型**：基于 LLaDA 架构，采用双向注意力机制。训练时随机掩码部分 Token，模型学习预测被掩码位置的概率分布。相比自回归模型，它能同时利用空间四周的上下文。
    *   **时序调节（Temporal Conditioning）**：在 P 帧模型中引入轻量级的“参考嵌入层”（Reference Embedding），将前一帧已解码的像素作为空间位置的额外输入，仅增加 1.3% 的参数量，却能极大地辅助预测当前帧。
    *   **分组并行解码（Group-wise Parallelism）**：借鉴 HPAC 方法，通过参数 $\delta$ 控制每步解码的像素组数，在解码速度和压缩率之间取得平衡。

### 3. 实验设计
*   **数据集**：
    *   **视频**：9 个 Xiph CIF 序列（YUV420 格式，共 2300 帧）；3 个 720p 高清序列用于扩展性测试。
    *   **图像**：Kodak 数据集（用于评估 I 帧模型的空间压缩能力）。
*   **Benchmark（对比方法）**：
    *   **传统无损/近无损**：PNG、FFV1、H.264 Lossless、H.265 Lossless、VVC (QP=0, 近无损)。
    *   **神经图像方法**：L3C、HPAC、ArIB-BPS 等。
    *   **基准线**：简单的帧差+PNG 压缩（diff+PNG）。

### 4. 资源与算力
*   **硬件**：实验运行在 **NVIDIA GH200** GPU 上。
*   **训练细节**：
    *   I 帧模型在 ImageNet 上训练 20 个 epoch。
    *   P 帧模型在 Vimeo-90k 上训练 15 个 epoch。
    *   模型参数量约为 **15.18M**。
*   **推理速度**：在 CIF 分辨率下，端到端速度约为 **0.06 FPS**（与 VVC 的 slower 预设处于同一数量级）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低运动（akiyo）到高运动（mobile）的多种视频场景，并进行了长达 300 帧的连续测试以验证稳定性。
*   **消融实验**：对比了“仅 I 帧”、“I+P（仅差分）”和“I+P（带参考嵌入）”三种配置，清晰展示了时序调节的贡献。
*   **客观性**：通过实际的算术编码器进行了端到端测试，验证了重建结果与原始 YUV 数据 0 位误差，确保了“无损”定义的严谨性。

### 6. 主要结论与发现
*   **性能领先**：在 Xiph CIF 序列上，NeuralLVC 比 H.265 无损模式平均节省了 **18.3%** 的比特率，比 H.264 无损节省了 **19.2%**。
*   **时序增益**：引入时序调节后，压缩率从仅 I 帧的 49.56% 显著下降至 29.71%，证明了时间冗余消除是无损视频压缩的关键。
*   **稳定性**：与 H.265 周期性的比特率波动不同，NeuralLVC 的 P 帧压缩率在长序列中表现得非常平稳，无误差累积。

### 7. 优点
*   **架构创新**：首次将掩码扩散模型应用于时序调节的无损视频压缩。
*   **精确保证**：通过双射标记化解决了神经模型中常见的量化误差问题，实现了真正的像素级无损。
*   **灵活性**：分组解码机制允许用户根据硬件能力调整解码步长（速度与性能的权衡）。

### 8. 不足与局限
*   **计算开销**：0.06 FPS 的速度限制了其在实时场景的应用，目前更适合离线存档。
*   **上下文限制**：模型以 32x32 的 Patch 为单位独立处理，忽略了跨 Patch 的长程空间相关性。
*   **结构简单**：目前仅采用了简单的 I/P 帧结构，尚未引入更复杂的 B 帧（双向预测）或场景切换检测机制。

（完）
