---
title: "ClariCodec: Optimising Neural Speech Codes for 200bps Communication using Reinforcement Learning"
title_zh: ClariCodec：利用强化学习优化 200bps 通信的神经语音编码
authors: "Junyi Wang, Chi Zhang, Jing Qian, Haifeng Luo, Hao Wang, Zengrui Jin, Chao Zhang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14654v1"
tags: ["query:entropy-coding"]
score: 7.5
evidence: 利用强化学习优化超低比特率下的神经语音编解码器
tldr: 针对卫星和水下通信等极低带宽场景，本研究提出了ClariCodec神经语音编解码器。在200bps的极低比特率下，传统编解码器往往因追求感知细节而牺牲可懂度。ClariCodec将量化过程建模为随机策略，利用强化学习（RL）并以字错率（WER）作为奖励函数对编码器进行微调。该方法在保持感知质量的同时显著提升了语音的可懂度，为超低速率通信提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14654v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the two-stage training framework of ClariCodec. In Stage 1, the full codec is trained end-to-end using a combination of L1 mel reconstruction loss, adversarial loss, and feature matching loss to ensure high-fidelity speech reconstruction. In Stage 2, all modules excpet the encoder are frozen, and the encoder is fine-tuned using an RL objective where the reward signal is derived from a pretrained ASR model, explicitly optimizing for speech intelligibility. An L1 mel reconstruction loss is used for preventing perceptual degradation during RL optimization.\", \"page\": 3, \"index\": 1, \"width\": 1004, \"height\": 488}, {\"url\": \"assets/figures/arxiv/2604.14654v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison of ClariCodec and baseline neural speech codecs on LibriSpeech test-clean and test-other. “#Param” and “#hours” denote the number of model parameters and the size of the training data, respectively. Acoustic quality is evaluated using STOI, PESQ, UTMOS [54], and SIM, while speech intelligibility is measured by WER. Bold indicates the best result in each column. Despite operating at the lowest bitrate, ClariCodec maintains competitive acoustic quality and intelligibility, with further improvements in intelligibility after RL fine-tuning.\", \"page\": 4, \"index\": 2, \"width\": 1000, \"height\": 289}]"
motivation: 在极低比特率通信中，传统语音编解码器因过度关注感知细节而导致语音可懂度（字错率）大幅下降。
method: 将量化过程重构为随机策略，并利用强化学习以字错率驱动的奖励函数对编码器进行微调。
result: "在200bps速率下，ClariCodec将LibriSpeech测试集的字错率降至3.20%，实现了13%的相对降幅。"
conclusion: 强化学习优化能有效提升超低比特率语音编解码器的可懂度，且不损失感知质量。
---

## 摘要
在卫星和水下信道等带宽受限的通信中，语音通常必须以极低比特率传输，此时可懂度是首要目标。在如此极端的压缩水平下，使用声学重建损失训练的编解码器往往会将比特分配给感知细节，从而导致词错率（WER）显著恶化。本文提出了 ClariCodec，一种运行速率为 200 bps 的神经语音编解码器，它将量化重新表述为随机策略，从而实现了基于强化学习（RL）的可懂度优化。具体而言，编码器使用以 WER 为驱动的奖励进行微调，而声学重建流水线保持冻结。即使不使用强化学习，ClariCodec 在 200 bps 速率下在 LibriSpeech test-clean 数据集上也能达到 3.68% 的 WER，与更高比特率下的编解码器相比已具竞争力。进一步的强化学习微调将 test-clean 的 WER 降低至 3.20%，test-other 的 WER 降低至 8.93%，在保持感知质量的同时实现了 13% 的相对降幅。

## Abstract
In bandwidth-constrained communication such as satellite and underwater channels, speech must often be transmitted at ultra-low bitrates where intelligibility is the primary objective. At such extreme compression levels, codecs trained with acoustic reconstruction losses tend to allocate bits to perceptual detail, leading to substantial degradation in word error rate (WER). This paper proposes ClariCodec, a neural speech codec operating at 200 bit per second (bps) that reformulates quantisation as a stochastic policy, enabling reinforcement learning (RL)-based optimisation of intelligibility. Specifically, the encoder is fine-tuned using WER-driven rewards while the acoustic reconstruction pipeline remains frozen. Even without RL, ClariCodec achieves 3.68% WER on the LibriSpeech test-clean set at 200 bps, already competitive with codecs operating at higher bitrates. Further RL fine-tuning reduces WER to 3.20% on test-clean and 8.93% on test-other, corresponding to a 13% relative reduction while preserving perceptual quality.