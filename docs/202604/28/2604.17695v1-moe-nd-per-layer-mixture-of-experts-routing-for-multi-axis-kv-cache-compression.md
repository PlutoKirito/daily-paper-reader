---
title: "MoE-nD: Per-Layer Mixture-of-Experts Routing for Multi-Axis KV Cache Compression"
title_zh: MoE-nD：用于多轴 KV 缓存压缩的逐层混合专家路由
authors: "Libo Sun, Peixiong He, Po-Wei Harn, Xiao Qin"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17695v1"
tags: ["query:train-trick"]
score: 7.0
evidence: KV缓存压缩的逐层剔除与量化混合策略
tldr: 引入了MoE-nD，通过逐层混合专家路由实现多轴KV缓存压缩。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17695v1/fig-001.webp\", \"caption\": \"Table 3. LongBench per task (n = 50). Tasks: HQA (HotpotQA), NQA (NarrativeQA), PRE (PassageRetrieval-en), TREC. Metrics: QA-F1 (HQA, NQA), retrieval-score (PRE), exact-match (TREC). Columns are nominal eviction budget b; per-method actual memory is in Table 2 and is the same across tasks. full is budget-invariant.\", \"page\": 6, \"index\": 1, \"width\": 379, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2604.17695v1/fig-002.webp\", \"caption\": \"Figure 2. Per-layer sensitivity landscape. (A) Full 28×9 heatmap of predicted attention-output L2 error when applying each compression operation to a single layer, measured offline against the uncompressed reference (log-scale color; only headline cells are annotated to keep the panel readable). The top strip shows per-operation maxℓ/minℓ ratio, directly summarizing the heterogeneity numbers in Table 1: aggressive eviction (evict75, evict90) spans 500–700× across layers while V-quant spans only 1.2–1.6×. (B) Per-op error distribution across layers as box plots: V-quant boxes are nearly flat (uniform-OK) while evict75/90 sprawl across three orders of magnitude. (C) Per-layer evict-vs-quant preference at equal memory cost (evict50 vs K-quant-4, both ≈1/2 of full precision). Positive bars: eviction damages more than quant on this layer (prefer quant); negative bars: quant damages more (prefer eviction). L00 is the lone layer preferring eviction; the routing solver must discover this without a uniform heuristic. (D) L00 vs L06 head-to-head: L2 error across all 9 ops, with the equal-memory pair (evict50 vs k4) shaded — concrete proof of the “two layers, opposite preferences” phenomenon cited in the text.\", \"page\": 3, \"index\": 2, \"width\": 962, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2604.17695v1/fig-003.webp\", \"caption\": \"Figure 3. MoE-nD pipeline. (A) Offline, per-layer sensitivity to each compression operation is measured against an uncompressed reference; the heatmap shows the actual calibration table for DeepSeek-R1-Distill-Qwen-7B. (B) A greedy budget solver picks the per-layer (keep, kbits, vbits) tuple that greedily reduces predicted total sensitivity (attention-output L2 error) subject to a global memory budget M . (C) At inference time, an attention patch applies the routed eviction and quantization to each layer jointly, with per-layer RoPE re-inversion. Routing colors in (B) and keep-patterns in (C) are illustrative; the heatmap in (A) is real data.\", \"page\": 5, \"index\": 3, \"width\": 953, \"height\": 333}]"
motivation: KV缓存压缩的逐层剔除与量化混合策略。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
KV 缓存内存是长上下文大语言模型（LLM）推理的主要瓶颈。现有的压缩方法分别作用于四维 KV 张量的单个轴——令牌剔除（序列）、量化（精度）、低秩投影（头维度）或跨层共享——但对每一层都采用相同的方案。我们表明，这种同质化牺牲了准确性：不同层对每种压缩操作的反应截然不同，且逐层剔除与量化的最优组合远非均匀。我们提出了 MoE-nD，这是一个混合专家框架，在全局内存预算下将每一层路由到其专属的（剔除率、K-bits、V-bits）元组。一个离线校准的贪婪求解器选择最小化预测质量损失的路由；在推理时，通过单个注意力补丁共同应用逐层异构的剔除和量化。在 LongBench-v1 的 4 任务子集上（16k 输入，每个任务 n=50，采用适配的推理模型协议），MoE-nD 的异构变体在 14 倍压缩（136 MB）下达到了与 1.9 GB 未压缩基准相当的效果，而我们测试的其他所有在相当或更小内存下的压缩基准（1d、2d_uniform、2d）得分均低于 8/100。这些增益在 AIME 推理基准测试中依然成立（在八种配置下，比最强的逐层量化基准高出 +6 到 +27 分）。两个无效结果——MATH-500 和 LongBench 的 TREC——具有一个共同的原理性原因（输入较短，求解器在大多数层上选择保留率 keep=1.0），这清晰地刻画了逐层剔除路由何时具有提升空间。

## Abstract
KV cache memory is the dominant bottleneck for long-context LLM inference. Existing compression methods each act on a single axis of the four-dimensional KV tensor -- token eviction (sequence), quantization (precision), low-rank projection (head dimension), or cross-layer sharing -- but apply the same recipe to every layer. We show that this homogeneity leaves accuracy on the table: different layers respond very differently to each compression operation, and the optimal per-layer mix of eviction and quantization is far from uniform. We propose MoE-nD, a mixture-of-experts framework that routes each layer to its own (eviction-ratio, K-bits, V-bits) tuple under a global memory budget. An offline-calibrated greedy solver chooses the routing that minimizes predicted quality loss; at inference time, per-layer heterogeneous eviction and quantization are applied jointly through a single attention patch. On a 4-task subset of LongBench-v1 (16k inputs, n=50 per task, adapted reasoning-model protocol; see section Experiments), MoE-nD's hetero variant matches our uncompressed 1.9~GB baseline at 14x compression (136~MB) while every other compressed baseline we tested (1d, 2d_uniform, 2d) at comparable or smaller memory stays under 8/100. The gains hold on AIME reasoning benchmarks (+6 to +27 pts over the strongest per-layer-quantization baseline across eight configurations). Two null results -- MATH-500 and LongBench's TREC -- share a principled cause (short inputs, solver picks keep=1.0 on most layers), cleanly characterizing when per-layer eviction routing has headroom to help.