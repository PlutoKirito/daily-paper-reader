---
title: "JLT: Clean-Latent Prediction in Latent Diffusion Transformers"
title_zh: JLT：潜在扩散变换器中的干净潜在预测
authors: "Funing Fu, Tenghui Wang, Junyong Cen, Qichao Zhu, Guanyu Zhou"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27102v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 潜在扩散变换器中的干净潜在预测
tldr: 潜在扩散模型中预测目标的选择至关重要。本文提出JLT，一个130M参数的潜在扩散Transformer，采用干净潜在预测而非速度预测。实验表明，在FLUX.2 VAE的潜在空间上，干净预测相比速度预测在ImageNet 256x256上取得了显著更优的FID分数（2.50）。这揭示了预测目标在潜在扩散中本质上是表示依赖的几何选择，而非可互换的代数参数化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27102v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1574, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27102v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27102v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27102v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 265, \"label\": \"Table\"}]"
motivation: 探讨在已知压缩潜在空间后，干净数据预测是否仍比速度预测更有效。
method: 构建130M参数JLT模型，使用冻结FLUX.2 VAE，比较干净潜在预测与速度预测DiT。
result: JLT-B/1在ImageNet 256x256上FID-50K 2.50，显著优于速度预测。
conclusion: 预测目标的选择在潜在扩散中是表示依赖的几何选择，而非简单代数变换。
---

## 摘要
基于干净数据预测的流匹配表明，回归干净点比预测环境噪声量能更有效地利用低维结构。我们探究在图像被映射到学习到的潜在空间后，这一原理是否仍然有效，因为压缩已经消除了原始像素的大部分变异性。我们引入了JLT，一个在冻结的FLUX.2 VAE编码上训练的1.3亿参数潜在扩散变换器，并在相同的表示、主干网络和训练设置下，将干净潜在预测与匹配的速度预测DiT进行比较。尽管对于固定的损坏时间，变量x、epsilon和v可线性转换，但局部高斯分析显示，速度回归继承了一个各向同性的目标协方差下限，并放大了低方差潜在方向，而干净预测则抑制了它们。在ImageNet 256×256上，使用无分类器引导的JLT-B/1获得了FID-50K 2.50，与速度预测相比有较大的匹配目标差距。这些结果表明，潜在扩散中的预测目标是依赖于表示的几何选择，而不是可互换的代数参数化。

## Abstract
Flow matching with clean-data prediction has shown that regressing the clean point can exploit low-dimensional structure more effectively than predicting an ambient noised quantity. We ask whether this principle remains useful after images are mapped into a learned latent space, where compression has already removed much of the raw pixel variability. We introduce JLT, a 130M latent diffusion Transformer over frozen FLUX.2 VAE codes, and compare clean-latent prediction with a matched velocity-prediction DiT under the same representation, backbone, and training settings. Although the three variables x, epsilon, and v are linearly convertible for a fixed corruption time, a local Gaussian analysis shows that velocity regression inherits an isotropic target-covariance floor and amplifies low-variance latent directions, while clean prediction damps them. On ImageNet 256 x 256, JLT-B/1 obtains FID-50K 2.50 with classifier-free guidance, with a large matched-target gap over velocity prediction. These results suggest that prediction targets in latent diffusion are representation-dependent geometric choices, rather than interchangeable algebraic parameterizations.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在扩散模型中，预测目标（如干净数据、噪声、速度）的选择虽在代数上等价，但对有限容量网络的学习难度有显著影响。先前工作（如JiT）在像素空间证明干净数据预测优于速度预测，因为干净数据集中在低维流形上。本文进一步追问：当模型已在压缩的潜在空间（如VAE）中运行时，这一原则是否仍然成立？潜在压缩已移除大部分像素变异性，但预测目标的选择是否仍能带来显著差异？
- **整体含义**：通过在一个固定的FLUX.2 VAE潜在空间中，对干净潜在预测与速度预测进行受控比较，发现预测目标本质上是表示依赖的几何选择，而不是可互换的代数参数化。这揭示了即使在压缩空间中，目标几何对生成质量仍有决定性影响。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：使用干净潜在预测（直接回归潜空间中的干净编码x）而非速度预测（v = x - epsilon），在相同架构、相同VAE表示、相同训练设置下，可以更有效地利用低维结构，避免速度预测引入的各向同性协方差下限和对低方差方向的放大。
- **关键技术细节**：
  - 采用线性腐蚀路径：`zt = t x + (1 - t) epsilon, t in [0,1]`。
  - 三种常见直接预测目标：`y_x = x`（干净预测）、`y_epsilon = epsilon`（噪声预测）、`y_v = x - epsilon`（速度预测）。代数上可相互转换，但直接回归损失不同。
  - 局部高斯分析：假设局部数据服从`x ~ N(0, Sigma)`，噪声独立。比较边缘协方差：`Cov(y_x) = Sigma`，`Cov(y_v) = Sigma + I`。速度预测给所有方向加上单位协方差地板，放大低方差方向；干净预测则衰减它们。
  - 条件残差方差分析：`Var(v_i | z_i) = (1/(1-t)^2) Var(x_i | z_i)`，速度目标条件模糊性更大。
  - 贝叶斯估计器显示：当特征值`lambda_i -> 0`时，干净预测系数趋于0，而速度预测系数趋于`-1/(1-t)`，从而放大低方差方向。
- **算法流程**：训练一个Base-scale的潜在DiT（JLT），输入为FLUX.2 VAE编码的潜在块，直接监督为干净潜在x；对比组（DiT-B）使用相同架构但监督为v。优化器、学习率、批大小等均固定。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：ImageNet 256×256，类条件生成。
- **Benchmark**：FID-50K（弗雷歇初始距离，50K样本）、IS（初始分数）。同时报告有无分类器引导（CFG）的结果。
- **对比方法**：
  - 主要对比：JLT-B/1（干净预测，VAE grid patch /1） vs DiT-B/1（速度预测）；JLT-B/2 vs DiT-B/2（更高聚合的patch）。
  - 外部基线：LDM（潜在扩散模型）、JiT-B/16、JiT-L/16（像素空间干净预测）。注意这些基线不是严格受控比较，仅用于上下文。

## 4. 资源与算力

- **论文明确说明**：模型为130M参数的Base-scale Transformer（12个Transformer块，隐藏维度768，12注意力头，128维瓶颈块嵌入）。训练250K步（200 epoch），有效批大小1024，使用AdamW优化器（无权重衰减，学习率2×10^{-4}）。
- **未明确说明**：使用的GPU型号、数量、训练时长等具体硬件信息未在文本中给出。

## 5. 实验数量与充分性

- **主要实验**：
  - 核心消融：两个patch size（/1和/2）下，干净预测 vs 速度预测（共4个模型）。
  - 训练曲线跟踪（图2）：每40 epoch评估FID和IS，显示贯穿训练的优势。
  - 最终JLT-B/1结果与若干基线对比（表2）。
- **充分性与公平性**：
  - 实验设计非常受控：在同一表示（FLUX.2 VAE）、同一架构（Base DiT）、同一训练和采样设置下，仅改变预测目标。这隔离了目标几何效应，避免了其他变量的混淆。
  - 但仅使用了ImageNet 256×256一个数据集、一种VAE tokenizer（FLUX.2）。未在其他数据集或不同潜在表示上验证，泛化性有限。
  - 未进行更广泛的消融如不同损失加权、不同噪声调度、不同采样器。论文承认这些是未来工作。

## 6. 论文的主要结论与发现

- 在固定潜在空间内，干净潜在预测显著优于速度预测。JLT-B/1在CFG引导下FID-50K为2.50，而DiT-B/1为6.56；JLT-B/2为14.81 vs DiT-B/2为28.71。
- 局部高斯分析提供了机制解释：速度预测引入各向同性协方差地板并放大低方差潜在方向，干净预测则衰减它们，从而降低回归难度。
- 预测目标在潜在扩散中是表示依赖的几何选择，而非简单代数参数化。这一结论在不同patch size下均成立。

## 7. 优点：方法或实验设计上的亮点

- **高度受控的实验设计**：在同一表示空间、同一架构、同一训练设置下进行唯一变量（预测目标）的比较，因果推断清晰。
- **理论机制分析**：通过局部高斯模型推导条件方差、贝叶斯估计器系数等，为实验差距提供了数学解释，不止停留于经验观察。
- **简洁有效**：未引入复杂辅助目标（如分类损失）或额外对齐技术，直接展现了目标几何的重要性。
- **结果显著**：FID从6.56降至2.50（/1）和从28.71降至14.81（/2），提升幅度大且一致。

## 8. 不足与局限

- **实验覆盖有限**：仅使用ImageNet 256×256一个数据集、一个VAE tokenizer（FLUX.2），未在其他数据集（如LSUN、COCO）或其他潜在表示（如KL-VAE、VQ-VAE）上验证。
- **未探索多种配置**：未比较不同噪声调度、损失加权、采样步数、采样器（如EDM、DPM-Solver）下的表现；理论分析只针对局部高斯假设，实际非高斯分布可能产生偏差。
- **资源细节缺失**：未说明使用的GPU型号、数量、训练时间，不利于复现和资源评估。
- **局限性明确承认**：论文指出当前结果应解释为受控潜在设置下的目标几何效应，并非对所有潜在扩散目标的完全表征。也指出理论不保证全局最优性。

（完）
