---
title: "TMPDiff: Temporal Mixed-Precision for Diffusion Models"
title_zh: TMPDiff：扩散模型的时间混合精度
authors: "Basile Lewandowski, Simon Kurz, Aditya Shankar, Robert Birke, Jian-Jia Chen, Lydia Y. Chen"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14062v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 扩散模型的时间混合精度量化
tldr: 扩散模型推理延迟高，量化是常用加速手段但通常采用固定精度。本文提出 TMPDiff 框架，通过为不同去噪时间步分配不同数值精度来实现时间混合精度。基于量化误差随时间步加性累积的发现，开发了线性复杂度的自适应二分算法。实验表明，在相同加速比下，TMPDiff 显著提升了感知质量，在 FLUX.1-dev 上实现了 2.5 倍加速并保持高保真度。
selection_source: fresh_fetch
motivation: 现有的扩散模型量化方法在所有时间步使用固定精度，忽略了不同阶段对精度敏感度的差异。
method: 提出一种基于自适应二分法的算法，根据量化误差累积特性为每个时间步分配最优精度。
result: "在多个模型和数据集上，TMPDiff 在相同加速比下比统一精度基准提升了 10% 到 20% 的感知质量。"
conclusion: 时间混合精度是优化扩散模型推理效率的有效维度，能在大幅提升速度的同时保持生成质量。
---

## 摘要
扩散模型是文本生成图像的主流方法，但其迭代去噪过程具有较高的推理延迟。量化通过使用较低的位宽来减少计算时间，但在所有去噪时间步中采用固定精度，这使得一个完整的优化维度尚未得到探索。我们提出了 TMPDiff，这是一个用于扩散模型的时间混合精度框架，它为不同的去噪时间步分配不同的数值精度。我们假设量化误差在时间步之间呈加性累积，并随后通过实验验证了这一假设。基于观察结果，我们开发了一种基于自适应二分法的算法，该算法以线性评估复杂度分配每步精度，从而简化了原本属于指数级的搜索问题。在四种最先进的扩散模型和三个数据集上，TMPDiff 在相同加速比下始终优于均匀精度基准，在感知质量上实现了 10% 到 20% 的提升。在 FLUX.1-dev 上，TMPDiff 在相对于 16 位推理实现 2.5 倍加速的情况下，达到了全精度模型 90% 的 SSIM。

## Abstract
Diffusion models are the go-to method for Text-to-Image generation, but their iterative denoising processes has high inference latency. Quantization reduces compute time by using lower bitwidths, but applies a fixed precision across all denoising timesteps, leaving an entire optimization axis unexplored. We propose TMPDiff, a temporal mixed-precision framework for diffusion models that assigns different numeric precision to different denoising timesteps. We hypothesize that quantization errors accumulate additively across timesteps, which we then validate experimentally. Based on our observations, we develop an adaptive bisectioning-based algorithm, which assigns per-step precisions with linear evaluation complexity, reducing an otherwise exponential search problem. Across four state-of-the-art diffusion models and three datasets, TMPDiff consistently outperforms uniform-precision baselines at matched speedup, achieving 10 to 20% improvement in perceptual quality. On FLUX.1-dev, TMPDiff achieves 90% SSIM relative to the full-precision model at a speedup of 2.5x over 16-bit inference.

---

## 论文详细总结（自动生成）

以下是对论文《TMPDiff: Temporal Mixed-Precision for Diffusion Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：扩散模型（如 FLUX, SDXL）在图像生成领域表现卓越，但其迭代去噪过程导致推理延迟极高。
*   **核心问题**：现有的量化加速技术通常采用“空间量化”，即在所有去噪时间步（timesteps）中使用统一的固定精度（如全 4-bit 或全 8-bit）。这种做法忽略了扩散模型在不同去噪阶段对精度敏感度的显著差异。
*   **研究动机**：探索“时间混合精度”（Temporal Mixed-Precision），通过在不同时间步动态切换数值精度，在保持高推理速度的同时，最大限度地恢复图像质量。

### 2. 方法论
*   **核心思想**：建立并验证“加性误差模型”（Additive Error Model），证明扩散模型最终的潜空间偏差是各时间步局部量化误差的加权累积。
*   **关键技术细节**：
    *   **误差建模**：通过一阶泰勒展开线性化去噪过程，推导出最终偏差 $\delta_0$ 的闭式表达式，证明了各步误差对结果的影响具有近似线性的独立性。
    *   **敏感度评分**：定义“上采样增益”（Upcasting Gain）和“下采样损失”（Downcasting Loss），用于衡量将某一时间步从量化恢复为全精度（或反之）对最终图像质量的贡献。
    *   **自适应二分算法（Adaptive Bisectioning）**：为了避免对所有 $T$ 个时间步进行穷举校准，算法利用敏感度曲线的平滑性，从首、中、尾锚点开始，迭代采样增益最高区域的中点，以线性复杂度重建完整的敏感度曲线。
    *   **调度策略**：根据阿姆达尔定律（Amdahl's Law）和目标加速比计算可用的全精度步数 $K$，贪婪地将这 $K$ 个名额分配给敏感度最高的时间步。

### 3. 实验设计
*   **数据集**：使用了三个主流基准数据集：COCO (Microsoft Common Objects in Context)、DCI (Densely Captioned Images) 和 MJHQ (MidJourney HQ)。
*   **模型（Benchmark）**：涵盖了四种最先进的扩散模型：
    *   **Transformer 架构**：FLUX.1-dev (12B), PixArt-Σ (0.6B), Sana (1.6B)。
    *   **CNN 架构**：Stable Diffusion XL (3.5B)。
*   **对比方法**：
    *   **均匀精度量化**：如全 W4A4 (SVDQuant) 或全 Int8/FP8 (TensorRT)。
    *   **等效加速的全精度模型**：通过减少去噪步数来匹配 TMPDiff 的速度。
    *   **模型混合启发式方法**：如 DDSM（用于模型大小混合的进化搜索方法）。
*   **评估指标**：SSIM、PSNR、LPIPS（以全精度模型输出为参考），以及 ImageReward、CLIP Score 和 FID。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **Nvidia RTX 4090 GPU** 上运行，配备 Intel Core i7-14700KF CPU。
*   **软件环境**：Ubuntu 22.04, PyTorch 2.7, CUDA 12.6。
*   **校准成本**：校准过程非常高效，仅需 128 个样本，且通过自适应二分法，通常只需少于 10 次前向评估即可确定最优调度方案。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个模型和 3 个数据集上进行了交叉验证，每组实验通常包含 1024 个生成样本。
*   **充分性验证**：
    *   **消融实验**：测试了不同校准样本量（64-512）的影响，证明 128 个样本已足够稳定。
    *   **分布外（OOD）验证**：在未参与校准的数据集上验证了调度方案的通用性。
    *   **线性相关性分析**：通过 Pearson 和 Spearman 系数验证了加性误差模型的准确性（相关性 > 0.98）。
*   **客观性**：实验通过调整对比方法的步数来确保在“相同加速比”下进行质量对比，保证了实验的公平性。

### 6. 主要结论与发现
*   **质量提升**：在相同加速比下，TMPDiff 相比均匀精度基准在感知质量指标（SSIM, LPIPS）上提升了 **10% 到 20%**。
*   **加速表现**：在 FLUX.1-dev 上实现了 **2.5 倍**的加速（相对于 16 位推理），同时保持了 90% 的 SSIM。
*   **帕累托优化**：TMPDiff 成功在“高保真度”和“高速度”之间找到了更优的平衡点，填补了现有方法的空白。
*   **通用性发现**：时间敏感度评分不仅适用于量化，也适用于不同规模模型的混合调度，且效果优于复杂的进化算法。

### 7. 优点
*   **理论创新**：首次系统性地提出并验证了扩散模型量化误差的时间加性模型。
*   **算法高效**：将原本指数级的组合搜索空间简化为线性复杂度的贪婪搜索，极大地降低了校准成本。
*   **高度兼容**：作为一种正交优化方案，它可以与现有的空间量化（如 SVDQuant）和激活缓存（如 DeepCache）技术无缝结合。

### 8. 不足与局限
*   **内存开销**：在推理时可能需要同时在显存中保留全精度和量化两套模型权重，这增加了显存占用（论文提到了权重卸载作为潜在解决方案）。
*   **工程集成**：目前尚未在底层算子（Kernel）级别实现自动化的精度切换，实际部署时可能存在微小的系统级切换开销。
*   **校准依赖**：虽然校准很快，但针对不同的模型架构或总步数设置，仍需进行一次性的预校准。

（完）
