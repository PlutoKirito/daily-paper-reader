---
title: "RPiAE: A Representation-Pivoted Autoencoder Enhancing Both Image Generation and Editing"
title_zh: RPiAE：一种增强图像生成与编辑的表征枢轴自编码器
authors: "Yue Gong, Hongyu Li, Shanyuan Liu, Bo Cheng, Yuhang Ma, Liebucha Wu, Xiaoyu Wu, Manyuan Zhang, Dawei Leng, Yuhui Yin, Lijun Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19206v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于潜空间扩散模型的表征枢轴自编码器
tldr: 针对扩散模型中预训练表征分词器重建保真度低和潜空间维度过高的问题，本文提出 RPiAE。通过表征枢轴正则化微调编码器以保留语义结构，并利用变分桥压缩潜空间。该方法采用解耦的阶段式训练，在提升文本生成图像和编辑性能的同时，实现了表征类分词器中最佳的重建效果。
selection_source: fresh_fetch
motivation: 现有的基于预训练表征的分词器因编码器冻结导致重建保真度差，且高维潜空间增加了扩散建模的难度。
method: 提出表征枢轴正则化以微调编码器，并结合变分桥压缩空间，采用目标解耦的阶段式训练策略。
result: 实验证明 RPiAE 在文本生成图像和图像编辑任务上优于其他分词器，并达到了同类模型中最高的重建保真度。
conclusion: RPiAE 成功平衡了语义保留与重建精度，为高效且高质量的图像生成与编辑提供了更优的潜空间表征。
---

## 摘要
扩散模型已成为图像生成和编辑的主导范式，潜扩散模型为了效率和可扩展性，将去噪过程转移到了紧凑的潜空间。最近利用预训练视觉表征模型作为分词器先验的尝试，要么将扩散特征与表征特征对齐，要么直接将表征编码器作为冻结的分词器重用。尽管这些方法可以提高生成指标，但由于编码器被冻结，它们往往面临重建保真度有限的问题，进而降低了编辑质量，且过高维度的潜变量也增加了扩散建模的难度。为了解决这些局限性，我们提出了表征枢轴自编码器（Representation-Pivoted AutoEncoder，RPiAE），这是一种基于表征的分词器，能够同时提升生成和编辑效果。我们引入了表征枢轴正则化（Representation-Pivot Regularization），这是一种训练策略，允许对表征初始化的编码器进行重建微调，同时保留预训练表征空间的语义结构，随后通过变分桥将潜空间压缩为紧凑空间，以实现更好的扩散建模。我们采用了一种目标解耦的分阶段训练策略，依次优化生成易处理性和重建保真度目标。这些组件共同构成了一个分词器，它保留了强大的语义，能够忠实地重建，并产生降低了扩散建模复杂度的潜变量。实验表明，RPiAE 在文本到图像生成和图像编辑方面优于其他视觉分词器，同时在基于表征的分词器中提供了最佳的重建保真度。

## Abstract
Diffusion models have become the dominant paradigm for image generation and editing, with latent diffusion models shifting denoising to a compact latent space for efficiency and scalability. Recent attempts to leverage pretrained visual representation models as tokenizer priors either align diffusion features to representation features or directly reuse representation encoders as frozen tokenizers. Although such approaches can improve generation metrics, they often suffer from limited reconstruction fidelity due to frozen encoders, which in turn degrades editing quality, as well as overly high-dimensional latents that make diffusion modeling difficult. To address these limitations, We propose Representation-Pivoted AutoEncoder, a representation-based tokenizer that improves both generation and editing. We introduce Representation-Pivot Regularization, a training strategy that enables a representation-initialized encoder to be fine-tuned for reconstruction while preserving the semantic structure of the pretrained representation space, followed by a variational bridge which compress latent space into a compact one for better diffusion modeling. We adopt an objective-decoupled stage-wise training strategy that sequentially optimizes generative tractability and reconstruction-fidelity objectives. Together, these components yield a tokenizer that preserves strong semantics, reconstructs faithfully, and produces latents with reduced diffusion modeling complexity. Experiments demonstrate that RPiAE outperforms other visual tokenizers on text-to-image generation and image editing, while delivering the best reconstruction fidelity among representation-based tokenizers.

---

## 论文详细总结（自动生成）

这是一份关于论文《RPiAE: A Representation-Pivoted Autoencoder Enhancing Both Image Generation and Editing》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在潜扩散模型（LDM）中，分词器（Tokenizer）定义的潜空间面临“生成易处理性（Generative Tractability）”与“重建保真度（Reconstruction Fidelity）”之间的固有矛盾。
*   **研究动机**：
    *   **传统 VAE**：潜空间缺乏语义结构，导致扩散模型训练困难，且为了易于训练通常采用低维潜变量，限制了重建质量。
    *   **现有基于表征的方法**：直接重用预训练视觉模型（如 DINOv2）作为冻结编码器。虽然语义强、生成质量高，但由于编码器不可调，重建精度差（不利于编辑），且表征维度过高（如 768 维），增加了扩散建模的复杂度。
*   **整体含义**：论文旨在设计一种既能保留预训练模型强大语义（利于生成），又能通过微调实现高保真重建（利于编辑），同时具备紧凑潜空间的分词器。

### 2. 方法论
RPiAE 的核心思想是通过“表征枢轴”机制平衡语义保留与重建优化。
*   **核心架构**：
    1.  **RM 编码器 ($E_\theta$)**：由预训练表征模型（如 DINOv2-B）初始化，且在训练中可更新。
    2.  **变分桥（Variational Bridge, VB）**：由 Transformer 构成的编码器-解码器对，将高维表征特征压缩为低维（如 64 维）、符合 KL 正则化的紧凑潜变量 $z$。
    3.  **像素解码器 ($D_\phi$)**：基于 ViT-XL 架构，负责从特征恢复像素图像。
*   **关键技术：表征枢轴正则化（Representation-Pivot Regularization）**：
    *   引入一个冻结的**枢轴副本编码器（Pivot Replica Encoder）**作为语义锚点。
    *   通过枢轴损失 $L_{piv}$（计算可训练编码器特征与冻结副本特征的 $\ell_2$ 距离）约束微调过程，防止编码器在追求重建精度时丢失预训练的语义几何结构。
    *   使用**自适应权重 $\lambda_{piv}$** 动态平衡重建损失与枢轴损失。
*   **目标解耦的阶段式训练策略**：
    *   **阶段 I**：微调编码器和解码器，重点在于通过枢轴正则化提升重建上限。
    *   **阶段 II**：冻结编解码器，训练变分桥（VB），实现潜空间压缩和 KL 正则化。
    *   **阶段 III**：固定潜空间，专门微调解码器以进一步消除重建伪影。

### 3. 实验设计
*   **数据集**：
    *   **图像重建与类条件生成**：ImageNet-1K (256x256)。
    *   **文本到图像生成**：CC12M-LLaVA-Next。
    *   **图像编辑**：OmniEdit。
*   **Benchmark 与对比方法**：
    *   **评测指标**：rFID（重建）、gFID（生成）、PSNR、LPIPS、GenEval（文本对齐）、DPG-Bench、GEdit-Bench-EN（编辑质量）。
    *   **对比对象**：包括传统 VAE（SD-VAE、Flux-VAE）、外部对齐方法（REPA、VA-VAE）以及内部重用方法（RAE、FAE、SVG）。

### 4. 资源与算力
*   **训练配置**：
    *   **Batch Size**：重建阶段为 512，扩散模型训练阶段为 1024。
    *   **训练时长**：分词器训练各阶段约 16-32 epoch；扩散模型训练 60-80 epoch（ImageNet）或 200K 迭代（T2I）。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**，但从其使用的模型规模（ViT-XL 解码器、0.5B 参数的 LLM 骨干）和迭代次数来看，属于中大规模算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础重建、类条件生成到复杂的文本生成和指令驱动编辑等多个维度。
*   **消融实验**：非常详尽，包括：
    *   枢轴损失函数的变体（$\ell_2$ vs Norm-$\ell_2$）。
    *   自适应权重的有效性。
    *   三阶段训练策略的必要性。
    *   潜空间维度（32, 64, 128）的影响。
    *   不同解码器架构（CNN vs ViT）的泛化性。
*   **客观性**：通过引入第三方评估指标（如 EditScore-Qwen3VL）和多种 Benchmark，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **重建与生成的双赢**：RPiAE 在保持高生成质量的同时，实现了基于表征的分词器中最高的重建保真度（rFID 0.50）。
*   **编辑能力的提升**：由于编码器经过微调且保留了语义，RPiAE 在图像编辑任务（GEdit-Bench）上显著优于冻结编码器的 RAE 和传统 VAE。
*   **语义保留**：线性探测（Linear Probing）实验证明，经过微调后的编码器在 ImageNet 分类准确率上仅下降约 0.4%，说明语义结构得到了极好的保留。

### 7. 优点（亮点）
*   **创新的正则化机制**：通过“枢轴副本”解决了预训练模型微调时的“灾难性遗忘”或语义漂移问题。
*   **解耦训练流程**：将语义保持、空间压缩和感知优化分阶段处理，降低了多目标优化的难度。
*   **高效的潜空间**：将 768 维特征压缩至 64 维，既保留了足够的信息，又降低了下游扩散模型的计算负担。

### 8. 不足与局限
*   **训练流程复杂**：三阶段训练增加了实现的复杂度，且每阶段的超参数（如权重、学习率）需要精细调优。
*   **训练开销**：在训练期间需要同时运行可训练编码器和冻结的枢轴编码器，增加了显存占用（尽管推理时不需要副本）。
*   **应用限制**：目前主要在 256x256 分辨率上验证，对于更高分辨率（如 1024x1024）或视频生成任务的扩展性有待进一步观察。

（完）
