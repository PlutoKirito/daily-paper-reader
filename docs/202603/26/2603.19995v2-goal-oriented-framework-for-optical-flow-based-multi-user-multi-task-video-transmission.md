---
title: Goal-Oriented Framework for Optical Flow-based Multi-User Multi-Task Video Transmission
title_zh: 面向目标的基于光流的多用户多任务视频传输框架
authors: "Yujie Xu, Shutong Chen, Nan Li, Yansha Deng, Jinhong Yuan, Robert Schober"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19995v2"
tags: ["query:vcodec"]
score: 8.0
evidence: 基于光流的语义编码器与运动提取器用于视频传输
tldr: 针对无线通信中多用户多任务视频传输的资源消耗问题，本文提出了一种基于光流的面向目标语义通信框架（OF-GSC）。该框架在发送端利用运动提取和块级光流语义表示来识别关键信息，在接收端通过Transformer解码器实现视频重建与分类，并引入DDPG算法优化带宽分配。实验表明，该方案在提升视频质量和分类精度的同时，显著降低了传输延迟和数据量。
selection_source: fresh_fetch
motivation: 旨在降低多用户多任务视频传输中的通信负担并优化无线资源利用率。
method: 提出OF-GSC框架，结合光流语义提取、Transformer解码及基于DDPG的带宽分配算法。
result: "视频重建SSIM提升13.47%，分类任务仅需25%数据即可超越VideoMAE，且传输时间缩短25.97%。"
conclusion: 该框架通过语义驱动的特征提取与资源优化，有效提升了多任务视频传输的效率与性能。
---

## 摘要
高效的多用户多任务视频传输是当前无线通信系统领域的一个重要研究课题。为了减轻传输负担并节省通信资源，我们提出了一种面向目标的基于光流的多用户多任务视频传输语义通信框架（OF-GSC）。在发送端，我们设计了一个语义编码器，由运动提取器和基于块级光流的语义表示提取器组成，以有效地识别和选择重要的语义表示。在接收端，我们设计了一个基于 Transformer 的语义解码器，用于高质量视频重建和视频分类任务。为了最小化通信时间，我们开发了一种基于深度确定性策略梯度（DDPG）的多用户传输带宽分配算法。对于视频重建任务，我们的 OF-GSC 框架在接收视频质量方面取得了显著提升，与 DeepJSCC 相比，结构相似性指数（SSIM）得分提高了 13.47%。对于视频分类任务，在 0.3 的相同掩码率下，OF-GSC 仅需 25% 的数据即可实现略高于 VideoMAE 性能的 Top-1 准确率。在带宽分配优化方面，与基准等带宽分配方案相比，我们基于 DDPG 的算法将最大传输时间缩短了 25.97%。

## Abstract
Efficient multi-user multi-task video transmission is an important research topic within the realm of current wireless communication systems. To reduce the transmission burden and save communication resources, we propose a goal-oriented semantic communication framework for optical flow-based multi-user multi-task video transmission (OF-GSC). At the transmitter, we design a semantic encoder that consists of a motion extractor and a patch-level optical flow-based semantic representation extractor to effectively identify and select important semantic representations. At the receiver, we design a transformer-based semantic decoder for high-quality video reconstruction and video classification tasks. To minimize the communication time, we develop a deep deterministic policy gradient (DDPG)-based bandwidth allocation algorithm for multi-user transmission. For video reconstruction tasks, our OF-GSC framework achieves a significant improvement in the received video quality, as evidenced by a 13.47% increase in the structural similarity index measure (SSIM) score in comparison to DeepJSCC. For video classification tasks, OF-GSC achieves a Top-1 accuracy slightly surpassing the performance of VideoMAE with only 25% required data under the same mask ratio of 0.3. For bandwidth allocation optimization, our DDPG-based algorithm reduces the maximum transmission time by 25.97% compared with the baseline equal-bandwidth allocation scheme.

---

## 论文详细总结（自动生成）

这是一份关于论文《Goal-Oriented Framework for Optical Flow-based Multi-User Multi-Task Video Transmission》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无线通信资源受限的环境下，实现高效的多用户、多任务（视频重建与视频分类）视频传输。
*   **研究动机**：
    *   **传统局限**：H.264/H.265 等传统编解码器基于比特级优化，忽略了语义信息，在低信噪比（SNR）下表现不佳。
    *   **现有语义通信不足**：现有的视频语义通信大多将视频视为独立图像序列，未能充分利用帧间时空相关性，且较少同时考虑多用户带宽分配和多任务并行处理。
    *   **目标导向**：通过提取“光流”作为核心语义特征，仅传输对任务关键的运动补丁（Patches），以极低的数据量满足高质量重建和精准分类的需求。

### 2. 方法论
该论文提出了 **OF-GSC 框架**，核心流程如下：
*   **发送端（语义编码器）**：
    *   **运动提取器**：基于轻量级 SPyNet 架构，提取连续帧之间的像素级光流。
    *   **语义表示提取器**：将光流划分为非重叠补丁。提供两种算法：
        1.  **数学运动检测算法**：利用 RANSAC 和最小二乘法（LSRE）估计背景运动，通过阈值筛选出显著的前景运动补丁。
        2.  **注意力机制模型**：利用多头注意力机制（MHA）根据任务目标（重建或分类）自动为补丁评分并掩码。
    *   **联合源信道编码（JSCC）**：对选定的光流补丁进行压缩和功率归一化，以适应无线信道。
*   **接收端（语义解码器）**：
    *   **Transformer 架构**：使用 12 层 Transformer 块捕捉时空相关性。
    *   **多任务分支**：共享特征提取层后，接一个 4 层 Transformer 解码器用于视频重建，或接一个线性层用于视频分类。
    *   **视频质量增强**：引入可选的 U-Net 模块，消除补丁边界的不连续性。
*   **带宽分配**：
    *   采用 **DDPG（深度确定性策略梯度）** 强化学习算法，根据各用户的传输负载和地理位置动态分配带宽，目标是最小化多用户中的最大传输延迟。

### 3. 实验设计
*   **数据集**：
    *   **UCF-101**：用于动作分类和视频重建训练。
    *   **MPI Sintel**：专门用于光流网络训练。
*   **基准方法（Benchmarks）**：
    *   **视频重建**：对比了 DeepJSCC（基于图像的语义通信）和 M-JPEG（传统压缩）。
    *   **视频分类**：对比了 VideoMAE（先进的掩码自编码器）。
    *   **带宽分配**：对比了等带宽分配（Equal-bandwidth）基准方案。
*   **评价指标**：SSIM、PSNR、VMAF（重建质量）；Top-1 Accuracy（分类精度）；传输时间（系统效率）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 40G GPU**。
*   **软件环境**：Python 3.10.11, PyTorch 2.3.0。
*   **训练细节**：论文提到采用了分阶段训练策略（先训练重建，再冻结编码器微调分类头），但未明确给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了不同信噪比（SNR 0-12dB）、不同掩码率（0.0-0.9）下的性能测试。
    *   进行了**消融实验**，验证了视频增强模块（Augmentation）的作用以及运动区域占比（MAP）对重建质量的影响。
    *   针对多用户场景模拟了 5 种不同的带宽分配方案。
*   **充分性评价**：实验设计较为全面，对比了传统方法、深度学习方法及最新的语义通信方法。通过 SSIM、PSNR 和 VMAF 多维度衡量视频质量，客观性较强。

### 6. 主要结论与发现
*   **重建性能**：在低 SNR 环境下，OF-GSC 的 SSIM 比 DeepJSCC 提升了 **13.47%**。在相同传输负载下，其表现远优于 M-JPEG。
*   **分类性能**：在 0.3 的掩码率下，OF-GSC 仅需 VideoMAE **25% 的数据量**即可达到甚至略微超过其 Top-1 准确率。
*   **系统效率**：基于 DDPG 的带宽分配算法比等带宽方案减少了 **25.97%** 的最大传输时间。
*   **关键发现**：光流作为语义特征在视频传输中具有极高的压缩潜力，且通过注意力机制筛选的补丁能有效平衡传输成本与任务精度。

### 7. 优点与亮点
*   **高效性**：通过补丁级光流掩码，极大地降低了空间和时间冗余。
*   **多任务统一**：一个框架同时支持重建和分类，且共享大部分网络参数，降低了端侧部署压力。
*   **鲁棒性**：针对无线信道损伤设计了端到端的 JSCC，在极低 SNR 下仍能保持视频连贯性。
*   **动态优化**：引入强化学习解决多用户资源竞争问题，提升了系统的实用性。

### 8. 不足与局限
*   **计算复杂度**：虽然传输负载降低了，但发送端需要运行光流提取（SPyNet）和 RANSAC/注意力计算，对移动设备的实时算力有一定要求。
*   **任务局限性**：目前仅验证了重建和分类任务，对于更复杂的视频任务（如目标检测、语义分割）的泛化能力尚待证明。
*   **背景建模简化**：数学运动检测算法假设背景运动符合二次函数模型，在极度复杂的动态背景（如波浪、森林）下可能失效。

（完）
