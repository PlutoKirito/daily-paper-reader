---
title: Joint Degradation-Aware Arbitrary-Scale Super-Resolution for Variable-Rate Extreme Image Compression
title_zh: 联合退化感知的任意尺度超分辨率用于可变速率极端图像压缩
authors: "Xinning Chai, Zhengxue Cheng, Xin Li, Rong Xie, Li Song"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17408v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 基于扩散模型的极端图像压缩，结合联合超分辨率实现多速率编码
tldr: 针对极低比特率图像压缩中多模型训练开销大及固定超分倍率不灵活的问题，本文提出ASSR-EIC框架。该框架在编码端引入任意尺度下采样实现可控码率，在解码端利用基于扩散模型的退化感知任意尺度超分技术，通过单一模型实现码率自适应重建。通过全局适配器和局部调制器平衡生成质量与保真度，并结合双语义增强设计，在实现极低码率压缩的同时保证了高保真度和高真实感的图像恢复。
selection_source: fresh_fetch
motivation: 现有的极低比特率图像压缩方法通常需要为不同码率训练独立模型，且固定比例的超分辨率难以灵活适应多变的码率需求。
method: 提出一种结合任意尺度下采样与扩散模型退化感知超分的框架，利用全局适配器和局部调制器实现细粒度的码率自适应图像重建。
result: 实验表明，该方法在极低比特率下达到了最先进的性能，并能通过单一模型支持灵活的码率控制和高质量的图像恢复。
conclusion: ASSR-EIC 有效解决了极低码率压缩中的灵活性与效率问题，为变码率图像压缩与超分辨率的联合优化提供了新思路。
---

## 摘要
最近基于扩散模型的极端图像压缩方法在超低比特率下表现出了卓越的性能。然而，大多数方法需要为每个目标比特率训练单独的扩散模型，导致了巨大的计算开销并阻碍了实际部署。同时，最近的研究表明，联合超分辨率可以作为增强低比特率重建的有效方法。然而，在迈向超低比特率领域时，由于严重的信息丢失，这些方法面临困难，且其对固定超分辨率尺度的依赖阻止了在不同比特率下的灵活适配。为了解决这些局限性，我们提出了 ASSR-EIC，这是一种利用任意尺度超分辨率（ASSR）来支持可变速率极端图像压缩（EIC）的新型图像压缩框架。在编码器端引入了一个任意尺度下采样模块以提供可控的速率降低，而基于扩散模型的联合退化感知 ASSR 解码器则能够在单个模型中实现速率自适应重建。我们利用压缩和缩放感知的扩散先验来引导重建，在不同的压缩和缩放设置下实现了高保真度和高真实感的修复。具体而言，我们设计了一个全局压缩-缩放适配器，为速率自适应提供整体引导，以及一个局部压缩-缩放调制器，动态平衡生成式和保真度导向的行为，以实现细粒度的、比特率自适应的细节修复。为了进一步提高重建质量，我们引入了双重语义增强设计。广泛的实验表明，ASSR-EIC 在极端图像压缩中提供了最先进的性能，同时支持灵活的比特率控制和自适应的速率相关重建。

## Abstract
Recent diffusion-based extreme image compression methods have demonstrated remarkable performance at ultra-low bitrates. However, most approaches require training separate diffusion models for each target bitrate, resulting in substantial computational overhead and hindering practical deployment. Meanwhile, recent studies have shown that joint super-resolution can serve as an effective approach for enhancing low-bitrate reconstruction. However, when moving toward ultra-low bitrate regimes, these methods struggle due to severe information loss, and their reliance on fixed super-resolution scales prevents flexible adaptation across diverse bitrates.   To address these limitations, we propose ASSR-EIC, a novel image compression framework that leverages arbitrary-scale super-resolution (ASSR) to support variable-rate extreme image compression (EIC). An arbitrary-scale downsampling module is introduced at the encoder side to provide controllable rate reduction, while a diffusion-based, joint degradation-aware ASSR decoder enables rate-adaptive reconstruction within a single model. We exploit the compression- and rescaling-aware diffusion prior to guide the reconstruction, yielding high fidelity and high realism restoration across diverse compression and rescaling settings. Specifically, we design a global compression-rescaling adaptor that offers holistic guidance for rate adaptation, and a local compression-rescaling modulator that dynamically balances generative and fidelity-oriented behaviors to achieve fine-grained, bitrate-adaptive detail restoration. To further enhance reconstruction quality, we introduce a dual semantic-enhanced design.   Extensive experiments demonstrate that ASSR-EIC delivers state-of-the-art performance in extreme image compression while simultaneously supporting flexible bitrate control and adaptive rate-dependent reconstruction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Joint Degradation-Aware Arbitrary-Scale Super-Resolution for Variable-Rate Extreme Image Compression》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在带宽极度受限的场景（如无人机、深海探测）中，超低比特率（<0.1 bpp）图像压缩至关重要。
*   **核心痛点**：
    1.  **部署开销大**：现有的基于扩散模型的极端图像压缩（EIC）方法通常需要为每个目标比特率训练独立模型，难以实际应用。
    2.  **灵活性差**：传统的“下采样+压缩+超分”方案多采用固定倍率（如2x、4x），无法根据带宽精细调整码率。
    3.  **信息丢失严重**：在极低码率下，图像结构和纹理损失殆尽，传统回归模型重建效果模糊。
*   **研究动机**：开发一个统一的框架，通过“任意尺度缩放”与“扩散模型先验”相结合，实现单模型下的可变码率极端压缩。

### 2. 论文提出的方法论
该论文提出了 **ASSR-EIC** 框架，其核心思想是将变码率重建建模为一个**退化感知的生成式问题**。

*   **核心流程**：
    1.  **编码端**：引入任意尺度下采样模块（因子 $s$ 可调），配合锚点编码器（如 VVC 或 MS-ILLM）实现灵活的码率削减。
    2.  **解码端**：使用基于 Stable Diffusion 的联合退化感知 ASSR 解码器进行高分辨率重建。
*   **关键技术细节**：
    *   **退化感知嵌入**：将编码器类型（$\chi_{CT}$）、质量参数（$\chi_{QP}$）和缩放因子（$s$）转化为特征嵌入（Embedding），显式引导扩散过程。
    *   **全局压缩-缩放适配器（Global Adaptor）**：在 U-Net 的 ResNet 块中注入退化信息，调节整体扩散动力学。
    *   **局部压缩-缩放调制器（Local Modulator）**：在保真度模块（Fidelity Module）与主干网络之间进行通道级增益和偏置调节，实现细粒度的细节修复。
    *   **双语义增强设计**：结合文本提示（Caption）提供全局语义，以及利用 SAM-Tiny 提取的软语义特征（Soft Semantic Prompt）增强结构保真度。
*   **损失函数**：由潜空间扩散损失（$L_{diff}$）和领域对齐损失（$L_A$）组成，后者用于确保退化图像的特征能映射到高质量扩散先验空间。

### 3. 实验设计
*   **数据集**：训练集采用 OpenImagesV6（10万张）；测试集包括 **Kodak**、**CLIC2020** 和 **MS-COCO**。
*   **Benchmark（基准）**：
    *   传统标准：VVC (VTM), HEVC (BPG)。
    *   学习型压缩：ELIC, MS-ILLM。
    *   极端压缩 SOTA：PerCo, DiffEIC, Text+Sketch。
*   **评价指标**：MS-SSIM（保真度）、LPIPS/DISTS/FID（感知质量）、CLIPScore（语义一致性）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 GPU** 进行训练。
*   **训练参数**：Batch size 为 12，学习率为 $5 \times 10^{-5}$，使用 Adam 优化器。
*   **训练时长**：总计进行 **100k 次迭代**，耗时约 **2 天**。
*   **推理开销**：默认采用 50 步扩散采样，MACs 约为 65634G（ASSR-EIC-S50）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了完整的率失真（R-D）曲线对比。
    *   针对 VVC 和 MS-ILLM 两种不同类型的锚点编码器分别验证了增益。
    *   **消融实验**：分别验证了全局适配器、局部调制器、文本引导和语义注意力的有效性。
    *   **泛化性实验**：在未见过的编码器（HEVC, ELIC）上直接测试，验证了框架的通用性。
*   **充分性评价**：实验设计非常充分，涵盖了客观指标、主观视觉对比、复杂度分析及跨模型泛化测试，能够客观证明该方法在变码率场景下的优越性。

### 6. 论文的主要结论与发现
*   **性能领先**：ASSR-EIC 在 FID 和 CLIPScore 等感知指标上全面超越了 PerCo 和 DiffEIC，同时在 MS-SSIM 上保持了与传统方法相当的保真度。
*   **码率节省**：相比 VVC 锚点，在相同感知质量下可节省约 **93.26%** 的比特率（以 FID-BPP 衡量）。
*   **单模型多码率**：通过调节缩放因子 $s$，单个模型即可覆盖从极低到常规的多个码率区间，无需重复训练。
*   **退化感知的重要性**：显式引入压缩参数和缩放因子能显著提升模型在不同退化程度下的适应能力。

### 7. 优点（亮点）
*   **兼容性极强**：采用插件式设计，不改变现有编码器结构，可直接增强 VVC、HEVC 或任何神经编码器。
*   **任意尺度适配**：突破了固定倍率超分的限制，实现了真正意义上的连续码率控制。
*   **保真度与真实感的平衡**：通过局部调制器动态调整生成强度，缓解了扩散模型常见的“过度生成”或“结构扭曲”问题。

### 8. 不足与局限
*   **解码延迟**：作为基于扩散模型的方法，其解码速度受限于迭代采样步数（如 50 步需约 6.96 秒），难以满足实时通信需求。
*   **计算复杂度**：MACs 远高于非扩散模型方法（如 MS-ILLM），对终端设备的算力有一定要求。
*   **潜在的伪影**：在极高缩放因子（如 $s=2$）下，虽然视觉真实感高，但可能出现与原图不符的细微纹理（如羽毛过锐）。

（完）
