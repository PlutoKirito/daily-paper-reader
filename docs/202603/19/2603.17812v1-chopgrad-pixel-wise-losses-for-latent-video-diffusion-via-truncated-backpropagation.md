---
title: "ChopGrad: Pixel-Wise Losses for Latent Video Diffusion via Truncated Backpropagation"
title_zh: ChopGrad：通过截断反向传播实现潜在视频扩散的像素级损失
authors: "Dmitriy Rivkin, Parker Ewen, Lili Gao, Julian Ost, Stefanie Walz, Rasika Kangutkar, Mario Bijelic, Felix Heide"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17812v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 用于潜空间视频扩散训练的截断反向传播及像素级损失
tldr: 针对视频扩散模型在像素域训练时因递归机制导致的内存开销巨大问题，本文提出了 ChopGrad。该方法通过截断反向传播技术，将梯度计算限制在局部帧窗口内，从而将训练内存需求从随帧数线性增长降低为常数级别。这使得在长视频或高分辨率视频上进行像素级损失微调变得可行。实验证明，ChopGrad 在视频超分、修复及受控驾驶视频生成等任务中表现优异，兼顾了效率与生成质量。
selection_source: fresh_fetch
motivation: 视频扩散模型的递归生成机制导致在像素域进行全序列反向传播时内存消耗极高，限制了长视频和高分辨率视频的微调。
method: 提出 ChopGrad 方案，通过在视频解码过程中采用截断反向传播，将梯度计算限制在局部窗口以实现常数级内存占用。
result: 实验表明 ChopGrad 在视频超分辨率、修复、增强和受控驾驶视频生成等多种任务中，其性能优于现有的最先进模型。
conclusion: ChopGrad 有效解决了视频扩散模型像素级微调的内存瓶颈，为高效训练高质量长视频生成模型提供了有力支持。
---

## 摘要
最近的视频扩散模型通过循环帧处理实现高质量生成，其中每一帧的生成都依赖于之前的帧。然而，这种循环机制意味着在像素域训练此类模型会产生极高的内存成本，因为激活值会在整个视频序列中累积。这一根本限制也使得对于长视频或高分辨率视频，使用像素级损失对这些模型进行微调在计算上是不可行的。本文介绍了 ChopGrad，这是一种用于视频解码的截断反向传播方案，它将梯度计算限制在局部帧窗口内，同时保持全局一致性。我们对这种近似进行了理论分析，并证明它能够利用帧级损失进行高效微调。ChopGrad 将训练内存从随视频帧数线性增长（全反向传播）降低到常数内存，并且在一系列带有像素级损失的条件视频生成任务中（包括视频超分辨率、视频修复、神经渲染场景的视频增强以及受控驾驶视频生成），其表现优于现有的最先进视频扩散模型。

## Abstract
Recent video diffusion models achieve high-quality generation through recurrent frame processing where each frame generation depends on previous frames. However, this recurrent mechanism means that training such models in the pixel domain incurs prohibitive memory costs, as activations accumulate across the entire video sequence. This fundamental limitation also makes fine-tuning these models with pixel-wise losses computationally intractable for long or high-resolution videos. This paper introduces ChopGrad, a truncated backpropagation scheme for video decoding, limiting gradient computation to local frame windows while maintaining global consistency. We provide a theoretical analysis of this approximation and show that it enables efficient fine-tuning with frame-wise losses. ChopGrad reduces training memory from scaling linearly with the number of video frames (full backpropagation) to constant memory, and compares favorably to existing state-of-the-art video diffusion models across a suite of conditional video generation tasks with pixel-wise losses, including video super-resolution, video inpainting, video enhancement of neural-rendered scenes, and controlled driving video generation.

---

## 论文详细总结（自动生成）

### ChopGrad：通过截断反向传播实现潜在视频扩散的像素级损失 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代潜在视频扩散模型（LVDM）通常使用带有“因果缓存（Causal Caching）”机制的3D变分自编码器（VAE）来保证视频的时间一致性。这种机制引入了递归结构，导致在进行像素级损失（如LPIPS、MSE）训练时，梯度必须通过整个视频序列回传，激活值随视频长度线性累积，造成内存开销巨大。
*   **研究动机**：由于内存限制，现有的长视频或高分辨率视频模型无法在像素域进行有效微调。这限制了单步模型蒸馏、视频超分辨率、神经渲染增强等依赖像素级感知损失的应用。
*   **整体含义**：本文提出了 **ChopGrad**，通过在视频解码过程中引入“截断反向传播（Truncated Backpropagation）”，将梯度计算限制在局部时间窗口内，从而将内存需求从线性增长降低为常数级别，使长视频的像素级微调变得可行。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用视频自编码器的**时间局部性（Temporal Locality）**。作者通过理论分析证明，前序帧对当前帧梯度的影响随时间距离呈指数级衰减。
*   **关键技术细节**：
    *   **截断机制**：在反向传播时，梯度仅回传至预设的 $D_{trunc}$ 个前序帧组，之后便切断计算图。
    *   **缓存分离**：在每一组帧的解码前，将来自前一组的因果缓存进行 `detach` 处理，防止梯度无限回传。
    *   **空间分块（Spatial Chunking）**：除了时间截断，还结合了空间分块技术，进一步降低单次计算的内存峰值。
*   **算法流程**：
    1. 将视频潜变量按帧组输入解码器。
    2. 解码器利用前一组的缓存生成当前像素帧。
    3. 计算当前帧的像素级损失。
    4. 执行反向传播，但梯度流仅覆盖当前帧及之前的 $D_{trunc}$ 个缓存步骤。
    5. 释放已完成截断范围的计算图，保持内存占用恒定。

#### 3. 实验设计
*   **应用场景与数据集**：
    *   **视频超分辨率**：使用 HQ-VSR 数据集，在 UDM10、SPMCS、YouHQ40 等多个 benchmark 上评估。
    *   **神经渲染增强（NVS）**：使用 DL3DV-Benchmark，针对 3D 高斯泼溅（3DGS）产生的伪影进行修复。
    *   **视频修复（Inpainting）**：在 DL3DV、Waymo Open Dataset 和 ROVI 数据集上测试。
    *   **受控驾驶视频生成**：基于 Waymo 数据集，模拟车辆插入与光影协调。
*   **对比方法**：包括 DOVE、VACE、MVSplat-360、Difix、Mirage 等各领域的 SOTA 模型。

#### 4. 资源与算力
*   **硬件设备**：使用了 **4 到 8 张 NVIDIA A100 (80GB) GPU**。
*   **训练时长**：
    *   神经渲染增强：约 3-4 小时。
    *   视频超分辨率：约 8 小时。
    *   视频修复：根据数据集大小，耗时 1.5 小时至 18 小时不等。
    *   驾驶视频生成：约 10 小时。
*   **效率提升**：相比全量反向传播，ChopGrad 实现了常数级内存占用，且在推理时支持单步生成（相比 VACE 等模型提速 50 倍）。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 4 种不同的下游任务，使用了超过 6 个主流数据集。
*   **消融实验**：
    *   分析了不同截断距离（$D_{trunc}$）对梯度误差、内存和计算时间的影响。
    *   对比了仅使用潜空间损失（Latent MSE）与引入 ChopGrad 像素损失的效果差异。
*   **客观性与公平性**：引入了用户偏好研究（34名参与者，ChopGrad 胜率达 95.6%）以及 VBench 等综合性视频质量评估框架，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **内存效率**：ChopGrad 成功将训练内存从随帧数线性增长转变为常数占用，支持处理极长视频。
*   **感知质量**：引入像素级损失显著提升了视频的纹理细节（如毛发、云朵）和时间一致性，减少了运动模糊。
*   **梯度近似有效性**：实验证实，即使截断距离很小（如 $D_{trunc}=1$），梯度的方向（余弦相似度）依然与全量回传高度一致，足以支持模型收敛。

#### 7. 优点
*   **理论支撑扎实**：通过数学推导证明了时间局部性和误差边界，而非纯经验性尝试。
*   **通用性强**：可无缝集成到多种主流视频扩散架构（如 Wan2.1, CogVideoX）中。
*   **实用价值高**：极大地降低了微调长视频模型的门槛，使得在有限算力下训练高质量视频模型成为可能。

#### 8. 不足与局限
*   **截断误差**：虽然误差随距离指数衰减，但在极少数需要极长程依赖的场景下，截断可能导致微小的全局不一致。
*   **架构依赖**：该方法主要针对具有因果缓存结构的循环解码器，对于非循环结构的自编码器提升有限。
*   **超参数敏感性**：虽然 $D_{trunc}=1$ 在多数场景有效，但针对不同复杂度的视频，可能仍需微调截断距离以平衡效率与精度。

（完）
