---
title: "D-Compress: Detail-Preserving LiDAR Range Image Compression for Real-Time Streaming on Resource-Constrained Robots"
title_zh: D-Compress：面向资源受限机器人实时流传输的保细节激光雷达距离图像压缩
authors: "Shengqian Wang, Chang Tu, He Chen"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13699v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 激光雷达距离图像压缩的率失真优化
tldr: 针对资源受限机器人的实时LiDAR点云传输，本文提出D-Compress框架。该方法将点云转换为距离图像，通过帧内/帧间预测与自适应离散小波变换（DWT）保留几何细节，并引入新型RDO率失真控制算法。实验证明，在压缩比超过100倍时，其几何精度和下游任务表现优于现有SOTA方法，且支持动态带宽下的实时运行。
selection_source: fresh_fetch
motivation: 传统图像编解码器因侧重人类视觉感知而导致LiDAR几何细节损失，且在动态带宽下缺乏有效的率失真优化。
method: 集成帧内/帧间预测与自适应离散小波变换进行残差压缩，并引入基于新型率失真建模的速率控制算法。
result: 在压缩比超过100倍时，该方法在几何精度和下游任务性能上均优于现有SOTA，且能在受限硬件上实时运行。
conclusion: D-Compress为边缘辅助机器人系统提供了一种高效、保真且具备鲁棒带宽自适应能力的实时点云压缩方案。
---

## 摘要
高效的三维激光雷达点云压缩（LPCC）和流传输对于边缘服务器辅助的机器人系统至关重要，能够通过紧凑的数据表示实现实时通信。一种被广泛采用的方法是将激光雷达点云表示为距离图像（range images），从而可以直接使用成熟的图像和视频压缩编解码器。然而，由于这些编解码器是针对人类视觉感知设计的，它们往往会牺牲几何细节，从而降低了建图和目标检测等下游机器人任务的性能。此外，在动态带宽条件下，基于率失真优化（RDO）的距离图像压缩（RIC）速率控制在很大程度上仍未得到充分探索。为了解决这些局限性，我们提出了 D-Compress，这是一个专为实时流传输量身定制的新型保细节且快速的 RIC 框架。D-Compress 将帧内和帧间预测与自适应离散小波变换方法相结合，以实现精确的残差压缩。此外，我们通过新的率失真建模，为 RIC 引入了一种新的基于 RDO 的速率控制算法。在各种数据集上的广泛评估证明了 D-Compress 的优越性，它在几何精度和下游任务性能方面均优于最先进的（SOTA）压缩方法，特别是在压缩率超过 100 倍的情况下，同时在资源受限的硬件上保持实时执行。此外，在动态带宽条件下的评估验证了其速率控制机制的鲁棒性。

## Abstract
Efficient 3D LiDAR point cloud compression (LPCC) and streaming are critical for edge server-assisted robotic systems, enabling real-time communication with compact data representations. A widely adopted approach represents LiDAR point clouds as range images, enabling the direct use of mature image and video compression codecs. However, because these codecs are designed with human visual perception in mind, they often compromise geometric details, which downgrades the performance of downstream robotic tasks such as mapping and object detection. Furthermore, rate-distortion optimization (RDO)-based rate control remains largely underexplored for range image compression (RIC) under dynamic bandwidth conditions. To address these limitations, we propose D-Compress, a new detail-preserving and fast RIC framework tailored for real-time streaming. D-Compress integrates both intra- and inter-frame prediction with an adaptive discrete wavelet transform approach for precise residual compression. Additionally, we introduce a new RDO-based rate control algorithm for RIC through new rate-distortion modeling. Extensive evaluations on various datasets demonstrate the superiority of D-Compress, which outperforms state-of-the-art (SOTA) compression methods in both geometric accuracy and downstream task performance, particularly at compression ratios exceeding 100x, while maintaining real-time execution on resource-constrained hardware. Moreover, evaluations under dynamic bandwidth conditions validate the robustness of its rate control mechanism.

---

## 论文详细总结（自动生成）

以下是对论文《D-Compress: Detail-Preserving LiDAR Range Image Compression for Real-Time Streaming on Resource-Constrained Robots》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的机器人平台上，实现激光雷达（LiDAR）点云的高压缩比、高几何精度且实时的流式传输。
*   **研究背景**：
    *   **现有方法局限**：传统的几何压缩（G-PCC）计算复杂度高，难以实时；而将点云转为距离图像并使用传统视频编解码器（如H.265）的方法，因其设计初衷是满足人类视觉（过滤高频细节），会导致机器人下游任务（如建图、检测）所需的几何精度大幅下降。
    *   **带宽波动**：在动态网络环境下，缺乏针对距离图像压缩（RIC）的率失真优化（RDO）速率控制机制。

### 2. 方法论
D-Compress 框架的核心思想是结合高效的预测机制与保细节的残差压缩。
*   **关键技术细节**：
    1.  **自适应预测**：
        *   **帧内预测**：提出基于梯度的预测方法。通过计算宏块内的梯度方向，沿相干方向进行差分编码（Delta Coding），有效利用空间冗余。
        *   **帧间预测**：利用刚体变换（R, t）建模运动。若有IMU则直接使用，若无则通过Link3D特征匹配和ICP算法估计位姿，计算预测残差。
    2.  **自适应离散小波变换（a-DWT）**：
        *   针对残差图像，采用3层Haar小波变换。
        *   **核心逻辑**：根据高频子带与低频子带的能量比例，动态分配量化步长。能量越高（细节越丰富）的子带分配越精细的量化，从而优先保护机器处理所需的几何细节。
    3.  **RDO速率控制算法**：
        *   **建模**：首次建立了RIC专用的率失真模型。验证发现失真-量化（D-Q）符合二次函数模型，速率-量化（R-Q）符合双曲函数模型。
        *   **优化**：通过自适应最小均方（LMS）算法动态更新模型参数，确保在带宽波动时精准控制输出比特率。

### 3. 实验设计
*   **数据集**：KITTI、nuScenes、Waymo Perception。
*   **Benchmark（基准）**：
    *   **几何精度**：PSNR、MSE。
    *   **下游任务**：LiDAR建图误差（A-LOAM）、3D目标检测精度（PointPillars的AP值）。
    *   **传输性能**：每秒帧数（FPS）、比特率误差（BE）。
*   **对比方法**：MPEG G-PCC、Google Draco、H.265、JPEG2000、WebP、RT-ST（实时时空压缩）。

### 4. 资源与算力
*   **硬件平台**：低成本 Mini PC（Intel Core i5-7260U CPU @ 2.20GHz，16 GB RAM）。
*   **算力需求**：该方法完全基于 CPU 运行，无需 GPU。
*   **运行效率**：
    *   有 IMU 辅助时：总耗时约 39.9ms（>20 FPS）。
    *   无 IMU 辅助时：总耗时约 79.4ms（>10 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流自动驾驶数据集，进行了数千帧的连续序列测试。
*   **实验类型**：
    *   不同压缩比下的率失真曲线对比。
    *   建图与定位误差的端到端评估。
    *   动态带宽下的速率控制稳定性测试。
    *   针对 a-DWT 模块的消融实验。
*   **评价**：实验设计全面，不仅关注传统的压缩指标，还深入探讨了对机器人实际应用的影响，对比实验涵盖了从传统标准到最新学术成果，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **精度领先**：在压缩比超过 100 倍时，D-Compress 的 PSNR 和几何精度显著优于 H.265 和 G-PCC。
*   **任务保真度**：在 176 倍压缩下，建图误差仅为 2.7%；在 108 倍压缩下，3D 检测 AP 保持在 48.9%，远高于其他方法。
*   **速率控制**：在动态带宽测试中，其比特率误差（BE）仅为 2.9%，而对比方法高达 9.8% 以上，证明了其 R-D 模型的准确性。
*   **实时性**：在资源受限的 CPU 平台上实现了实时处理，满足机器人闭环控制的需求。

### 7. 优点（亮点）
*   **机器视觉导向**：不同于传统编解码器，a-DWT 策略专门为保留几何细节设计，更符合机器人任务需求。
*   **首创 RIC 率失真模型**：填补了距离图像压缩领域缺乏 RDO 速率控制的空白。
*   **轻量化与高效**：无需昂贵的 GPU，在低功耗嵌入式设备上表现优异。

### 8. 不足与局限
*   **运动估计依赖**：在无 IMU 且环境特征极度缺失（如开阔广场）的情况下，基于 ICP 的帧间预测可能会失效或变慢。
*   **硬件加速潜力未完全挖掘**：目前主要基于 CPU，未来若引入 FPGA 或专用指令集加速，性能可能进一步提升。
*   **极端动态环境**：论文未深入讨论在极高旋转速度或剧烈震动环境下的压缩鲁棒性。

（完）
