---
title: Next-Frame Decoding for Ultra-Low-Bitrate Image Compression with Video Diffusion Priors
title_zh: 基于视频扩散先验的超低比特率图像压缩下一帧解码
authors: "Yunuo Chen, Chuqin Zhou, Jiangchuan Li, Xiaoyue Ling, Bing He, Jincheng Dai, Li Song, Guo Lu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15129v2"
tags: ["query:vcodec"]
score: 9.0
evidence: 利用视频扩散先验进行生成式图像压缩
tldr: 本文提出一种超低比特率图像压缩（ULB-IC）新范式，利用视频扩散模型（VDM）的“时间”演化特性进行解码。研究者定义了一个保留几何与语义的紧凑锚点帧，并将解码过程重新解释为从锚点到最终图像的虚拟时间转换，即下一帧预测任务。相比传统的图像扩散模型，该方法在提升重建保真度与真实感的同时，显著提高了解码速度，为生成式图像压缩提供了高效的新路径。
selection_source: fresh_fetch
motivation: 旨在解决超低比特率下生成式图像压缩保真度不足及解码速度慢的问题，探索利用视频扩散模型的时间先验来增强重建效果。
method: 通过定义一个紧凑的锚点帧作为初始状态，利用预训练视频扩散模型将解码过程转化为从锚点到目标图像的下一帧预测任务。
result: "在CLIC2020数据集上，相比DiffC在多个感知指标上节省了超过50%的比特率，且解码速度提升了高达5倍。"
conclusion: 将图像解码建模为基于视频先验的虚拟时间演化过程，能有效平衡超低比特率下的图像质量与计算效率。
---

## 摘要
我们提出了一种用于超低比特率图像压缩（ULB-IC）的新范式，该范式利用了生成式图像压缩中的“时间”演化。具体而言，我们在解码过程中定义了一个显式的中间状态：一个紧凑的锚点帧（anchor frame），它在丢弃高频细节的同时保留了场景几何和语义布局。然后，我们将生成式解码重新解释为从该锚点到最终重建图像的虚拟时间转换。为了对这一过程进行建模，我们利用预训练的视频扩散模型（VDM）作为时间先验：锚点帧作为初始帧，原始图像作为目标帧，从而将解码过程转化为下一帧预测任务。与基于图像扩散的 ULB-IC 模型相比，我们的解码过程从一个可见的、语义忠实的锚点开始，这提高了感知图像压缩的保真度和逼真度。大量实验表明，我们的方法在客观和主观性能上均表现优异。在 CLIC2020 测试集上，与 DiffC 相比，我们的方法在 LPIPS、DISTS、FID 和 KID 指标上实现了超过 50% 的比特率节省，同时还实现了高达 5 倍的显著解码加速。代码将随后发布。

## Abstract
We present a novel paradigm for ultra-low-bitrate image compression (ULB-IC) that exploits the "temporal" evolution in generative image compression. Specifically, we define an explicit intermediate state during decoding: a compact anchor frame, which preserves the scene geometry and semantic layout while discarding high-frequency details. We then reinterpret generative decoding as a virtual temporal transition from this anchor to the final reconstructed image.To model this progression, we leverage a pretrained video diffusion model (VDM) as temporal priors: the anchor frame serves as the initial frame and the original image as the target frame, transforming the decoding process into a next-frame prediction task.In contrast to image diffusion-based ULB-IC models, our decoding proceeds from a visible, semantically faithful anchor, which improves both fidelity and realism for perceptual image compression. Extensive experiments demonstrate that our method achieves superior objective and subjective performance. On the CLIC2020 test set, our method achieves over 50% bitrate savings across LPIPS, DISTS, FID, and KID compared to DiffC, while also delivering a significant decoding speedup of up to $\times$5. Code will be released later.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next-Frame Decoding for Ultra-Low-Bitrate Image Compression with Video Diffusion Priors》的结构化技术总结：

### 1. 核心问题与整体含义
*   **研究动机**：在超低比特率（ULB）下，传统的图像压缩方法难以平衡“真实感”（Realism）、“保真度”（Faithfulness）和“推理效率”。现有的基于图像扩散模型（IDM）的方法虽能生成逼真细节，但常面临语义偏移（生成的图像与原图内容不符）和解码延迟高（需多次迭代采样）的问题。
*   **核心思想**：论文提出将图像解码视为一种**虚拟的时间演化过程**。通过定义一个紧凑的“锚点帧”（Anchor Frame）来保留场景几何和语义布局，并利用预训练**视频扩散模型（VDM）**的“下一帧预测”能力，将锚点帧演化为高保真度的重建图像。

### 2. 方法论
该方法名为 **NeFIC**，其核心流程分为两个阶段：
*   **虚拟两帧视频建模**：将压缩过程抽象为两帧视频，第一帧是极低比特率的锚点帧（由 Anchor Codec 生成），第二帧是目标重建图像。
*   **第一阶段：下一帧解码适配（Adaptation）**：
    *   利用预训练的 CogVideoX-1.5 模型，通过 LoRA 适配器进行微调。
    *   输入包含：文本提示词、锚点帧 Token 和带噪的目标帧 Token。
    *   利用 VDM 内置的“从模糊到清晰”的时间先验（类似于视频中的对焦过程）来合成细节。
*   **第二阶段：一步生成旁路（One-Step Generative Bypass）**：
    *   **条件锚点编码**：使锚点编码器感知视频 VAE 的潜空间，减少两者间的域差异。
    *   **旁路细化（Bypass Refinement）**：引入 Transformer 模块，将压缩特征映射为生成潜变量，替代随机高斯噪声作为初始状态。
    *   **单步采样**：通过 $v$-prediction 参数化，将多步扩散压缩为单步生成，大幅提升速度。
*   **损失函数**：结合了噪声预测损失、锚点辅助损失（MSE+LPIPS）、CLIP 判别器引导的 GAN 损失以及比特率惩罚。

### 3. 实验设计
*   **数据集**：训练使用 Flickr2W；测试评估使用 **Kodak**、**CLIC2020** 测试集和 **DIV2K**。
*   **Benchmark 指标**：
    *   感知/真实感：LPIPS、DISTS、FID、KID。
    *   客观失真：PSNR、MS-SSIM。
    *   效率：编码/解码耗时（Runtime）。
*   **对比方法**：包括 GAN 类（MS-ILLM, GLC）、图像扩散类（DiffC, PerCo, DiffEIC, ResULIC, StableCodec）以及最新的 DLF 等 SOTA 模型。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 GPU**。
*   **训练时长**：两阶段训练总计约 **3 天**。
    *   第一阶段：10k 步，学习率 1e-4。
    *   第二阶段：50k 步，在 35k 步引入 GAN 损失，后期学习率降至 1e-5。
*   **模型规模**：基于 CogVideoX-5B 基础模型。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了详尽的定量对比，并提供了大量的定性视觉对比图。
*   **消融实验**：非常充分，涵盖了：
    *   两阶段训练的必要性。
    *   条件编码与旁路细化模块的作用。
    *   辅助损失与 GAN 损失的影响。
    *   LoRA 参数配置（Rank/Alpha）。
    *   **范式对比**：专门对比了“视频扩散范式”与“图像扩散范式（如 FLUX-12B）”，证明了视频先验在结构一致性上的优越性。
*   **客观性**：使用了 BD-rate 衡量比特率节省，对比基准统一，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：在 CLIC2020 上，相比 DiffC，在 LPIPS、FID 等感知指标上实现了 **50% 以上的比特率节省**。
*   **保真度高**：在超低比特率下，NeFIC 能更准确地重建纹理（如玻璃杯棱角、墙面腐蚀），且 PSNR 等客观指标也优于多数生成式方法。
*   **速度极快**：通过一步生成技术，解码速度比 DiffEIC 快 5.5-8.6 倍，比 StableCodec 快 6 倍（且无需切片处理）。
*   **视频先验优势**：VDM 的 3D 注意力机制和时间一致性先验比 IDM 的通道拼接方式更能有效利用上下文信息。

### 7. 优点
*   **创新范式**：首次将图像压缩建模为视频的“下一帧预测”，巧妙利用了视频模型处理帧间演化的能力。
*   **效率与质量并重**：通过“一步生成旁路”解决了扩散模型解码慢的顽疾，同时利用锚点帧解决了生成式压缩常见的语义漂移问题。
*   **结构化引导**：锚点帧作为显式的中间状态，为生成过程提供了坚实的几何约束。

### 8. 不足与局限
*   **模型体积**：虽然推理步数减少了，但基础模型（CogVideoX-5B）参数量巨大，对显存有一定要求，可能不适用于移动端。
*   **训练复杂性**：采用两阶段训练且涉及多个 VAE 潜空间的对齐，训练流程相对复杂。
*   **应用限制**：主要针对超低比特率场景，在追求极高 PSNR 的高比特率场景下，其优势可能不如传统的端到端压缩模型明显。

（完）
