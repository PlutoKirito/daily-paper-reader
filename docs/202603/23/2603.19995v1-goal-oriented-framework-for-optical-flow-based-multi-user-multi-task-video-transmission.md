---
title: Goal-Oriented Framework for Optical Flow-based Multi-User Multi-Task Video Transmission
title_zh: 面向光流的多用户多任务视频传输的目标导向框架
authors: "Yujie Xu, Shutong Chen, Nan Li, Yansha Deng, Jinhong Yuan, Robert Schober"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19995v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 基于光流的语义表示与视频传输
tldr: 针对无线通信中多用户多任务视频传输的资源压力，本文提出一种基于光流的面向目标语义通信框架（OF-GSC）。该框架在发送端通过提取运动和补丁级光流语义特征来识别关键信息，在接收端利用Transformer解码器执行视频重建与分类任务，并结合DDPG算法优化带宽分配，显著提升了传输效率和任务性能。
selection_source: fresh_fetch
motivation: 旨在解决当前无线通信系统中多用户多任务视频传输带来的巨大通信负担和资源消耗问题。
method: 设计了包含运动提取与补丁级光流语义表示的编码器、Transformer解码器以及基于DDPG的带宽分配算法。
result: "相比DeepJSCC，视频重建SSIM提升13.47%；分类任务仅需25%数据即可比肩VideoMAE；传输时间较等带宽分配减少25.97%。"
conclusion: OF-GSC框架通过语义特征提取与智能资源调度，为高效的多用户多任务视频通信提供了有效的解决方案。
---

## 摘要
高效的多用户多任务视频传输是当前无线通信系统领域的一个重要研究课题。为了减轻传输负担并节省通信资源，我们提出了一种基于光流的多用户多任务视频传输目标导向语义通信框架（OF-GSC）。在发送端，我们设计了一个语义编码器，由运动提取器和基于块级光流的语义表示提取器组成，以有效识别和选择重要的语义表示。在接收端，我们设计了一个基于 Transformer 的语义解码器，用于高质量视频重建和视频分类任务。为了最小化通信时间，我们针对多用户传输开发了一种基于深度确定性策略梯度（DDPG）的带宽分配算法。对于视频重建任务，我们的 OF-GSC 框架在接收视频质量方面取得了显著提升，与 DeepJSCC 相比，结构相似性指数（SSIM）得分提高了 13.47%。对于视频分类任务，在相同的掩码率 0.3 下，OF-GSC 仅需 25% 的数据即可达到略高于 VideoMAE 的 Top-1 准确率。在带宽分配优化方面，与基准等带宽分配方案相比，我们基于 DDPG 的算法将最大传输时间缩短了 25.97%。

## Abstract
Efficient multi-user multi-task video transmission is an important research topic within the realm of current wireless communication systems. To reduce the transmission burden and save communication resources, we propose a goal-oriented semantic communication framework for optical flow-based multi-user multi-task video transmission (OF-GSC). At the transmitter, we design a semantic encoder that consists of a motion extractor and a patch-level optical flow-based semantic representation extractor to effectively identify and select important semantic representations. At the receiver, we design a transformer-based semantic decoder for high-quality video reconstruction and video classification tasks. To minimize the communication time, we develop a deep deterministic policy gradient (DDPG)-based bandwidth allocation algorithm for multi-user transmission. For video reconstruction tasks, our OF-GSC framework achieves a significant improvement in the received video quality, as evidenced by a 13.47% increase in the structural similarity index measure (SSIM) score in comparison to DeepJSCC. For video classification tasks, OF-GSC achieves a Top-1 accuracy slightly surpassing the performance of VideoMAE with only 25% required data under the same mask ratio of 0.3. For bandwidth allocation optimization, our DDPG-based algorithm reduces the maximum transmission time by 25.97% compared with the baseline equal-bandwidth allocation scheme.

---

## 论文详细总结（自动生成）

### 论文总结：面向光流的多用户多任务视频传输的目标导向框架 (OF-GSC)

#### 1. 核心问题与研究动机
随着 5G/6G 时代智能设备和视频应用（如无人机监控、视频会议）的激增，无线网络面临巨大的数据传输压力。传统视频编解码器（如 H.264/H.265）基于香农通信框架，侧重于比特级优化，难以在资源受限的无线环境下高效支持高质量视频传输。
**核心问题**：如何在减少通信资源消耗的同时，满足多用户场景下视频重建（视觉质量）和视频分类（机器视觉任务）的双重需求？
**研究动机**：利用语义通信（GSC）提取与任务相关的关键特征，并通过光流（Optical Flow）显式捕捉帧间运动，以消除时空冗余。

#### 2. 方法论
论文提出了一种名为 **OF-GSC** 的框架，主要包含以下核心组件：
*   **发送端（语义编码器）**：
    *   **运动提取器**：采用轻量级的 SPyNet 架构，通过多尺度金字塔提取像素级光流特征。
    *   **语义表示提取器**：将光流数据划分为非重叠补丁（Patches）。提出两种算法：
        1.  **数学运动检测算法**：利用 RANSAC 和最小二乘回归（LSRE）估计背景运动，通过阈值筛选出显著的运动补丁。
        2.  **注意力机制模型**：利用多头注意力机制动态评估补丁的重要性，实现任务感知的特征选择。
*   **接收端（语义解码器）**：
    *   基于 **Transformer** 架构。共享一个深层编码器来捕捉时空相关性。
    *   **多任务分支**：重建任务使用浅层 Transformer 解码器恢复视频；分类任务使用轻量级线性层。
*   **视频质量增强**：设计了一个基于 U-Net 的双路径增强网络，用于消除补丁边界的不连续性。
*   **带宽分配**：针对多用户场景，开发了基于 **DDPG（深度确定性策略梯度）** 的强化学习算法，动态优化各用户的带宽分配，以最小化最大传输延迟。

#### 3. 实验设计
*   **数据集**：
    *   **UCF-101**：用于视频分类和重建任务的训练与评估。
    *   **MPI Sintel**：用于光流提取网络的训练。
*   **基准方法 (Benchmarks)**：
    *   **视频重建**：对比了 DeepJSCC（深度联合源信道编码）和传统 M-JPEG。
    *   **视频分类**：对比了 VideoMAE（掩码自编码器）。
    *   **带宽分配**：对比了等带宽分配（Equal-bandwidth allocation）方案。
*   **评估指标**：SSIM（结构相似性）、PSNR、VMAF、Top-1 准确率、传输时间。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 40G GPU**。
*   **软件环境**：Python 3.10.11, PyTorch 2.3.0。
*   **训练细节**：论文提到了针对不同掩码率（0.0 到 0.9）进行了模型训练，并采用了两阶段训练策略（先优化 MSE，再微调 SSIM/分类损失）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了不同信噪比（SNR）、不同掩码率（Mask Ratio）、不同运动面积占比（MAP）的多组实验。
*   **消融实验**：验证了“有无增强模块”以及“不同语义提取算法（数学 vs 注意力）”对性能的影响。
*   **客观性**：实验对比了理想信道与瑞利衰落信道，考虑了传输负载与视觉质量的权衡，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **重建性能**：在低 SNR 环境下，OF-GSC 的 SSIM 比 DeepJSCC 提升了 **13.47%**（约 0.1 的绝对值提升）。
*   **分类效率**：在 0.3 的掩码率下，OF-GSC 仅需 VideoMAE **25%** 的数据量即可达到甚至略微超过其 Top-1 准确率。
*   **传输效率**：相比 DeepJSCC，OF-GSC 在保持同等 SSIM 的情况下，传输数据量减少了 **43.77%**。
*   **资源优化**：DDPG 算法相比基准方案，将多用户场景下的最大传输时间缩短了 **25.97%**。

#### 7. 优点与亮点
*   **显式运动建模**：通过光流而非单纯的像素补丁来处理时间冗余，显著提高了视频语义压缩的效率。
*   **模块化多任务设计**：一个框架同时支持人类视觉（重建）和机器视觉（分类），且共享大部分网络权重，降低了部署成本。
*   **端到端鲁棒性**：考虑了无线信道损伤（噪声和衰落），比纯源编码方法在实际通信场景中更稳健。
*   **智能资源调度**：结合强化学习解决多用户干扰和带宽分配问题，具有较强的系统级实用性。

#### 8. 不足与局限
*   **计算复杂度**：光流提取和 Transformer 编码器对移动端设备的算力要求较高，论文未详细讨论实时处理的延迟（Latency）。
*   **背景依赖性**：数学运动检测算法在极度复杂的动态背景下（如森林中晃动的树叶）可能难以准确区分前景运动。
*   **首帧依赖**：框架高度依赖第一帧（关键帧）的传输质量，若首帧在信道中严重受损，后续基于光流的重建可能会出现误差累积。

（完）
