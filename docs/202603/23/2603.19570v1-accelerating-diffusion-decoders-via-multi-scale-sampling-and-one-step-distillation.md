---
title: Accelerating Diffusion Decoders via Multi-Scale Sampling and One-Step Distillation
title_zh: 通过多尺度采样和单步蒸馏加速扩散解码器
authors: "Chuhan Wang, Hao Chen"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19570v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 通过一步蒸馏加速扩散解码器
tldr: 针对扩散解码器在图像标记化中推理速度慢的问题，本文提出了一种两阶段加速框架。该框架首先采用多尺度采样策略，从低分辨率逐步细化至高分辨率，实现理论上的对数级加速；随后通过单步蒸馏技术，将各尺度的扩散过程简化为单次前向传播。实验表明，该方法在保持高感知保真度的同时，将解码速度提升了一个数量级，为高效图像标记化提供了新路径。
selection_source: fresh_fetch
motivation: 扩散解码器虽然能实现高保真图像重建，但其迭代采样过程带来的高延迟限制了其实际应用。
method: 结合了从粗到精的多尺度采样策略与针对各尺度的单步去噪模型蒸馏技术。
result: 在几乎不损失输出质量的前提下，实现了图像解码速度的数量级提升。
conclusion: 该研究为构建高效且具有表现力的视觉标记器奠定了基础，有助于推动下游生成任务的发展。
---

## 摘要
图像标记化在现代生成建模中起着核心作用，它将视觉输入映射为紧凑的表示，作为像素与生成模型之间的中间信号。基于扩散的解码器最近被应用于图像标记化，以从潜表示中重建具有高感知保真度的图像。与用于下游生成的扩散模型不同，这些解码器致力于忠实重建而非内容生成。然而，它们的迭代采样过程引入了显著的延迟，使其在实时或大规模应用中不切实际。在这项工作中，我们引入了一个两阶段加速框架来解决这一效率低下的问题。首先，我们提出了一种多尺度采样策略，解码从粗分辨率开始，并通过在每个阶段将分辨率翻倍来逐步细化输出，与标准的全分辨率采样相比，实现了 $\mathcal{O}(\log n)$ 的理论加速。其次，我们将每个尺度的扩散解码器蒸馏为单步去噪模型，从而在每个尺度的一次前向传递中实现快速且高质量的重建。这些技术共同使解码时间减少了一个数量级，且输出质量几乎没有下降。我们的方法为构建高效且具有表现力的图像标记器提供了一条实用路径。我们希望它能为未来在高效视觉标记化和下游生成方面的工作奠定基础。

## Abstract
Image tokenization plays a central role in modern generative modeling by mapping visual inputs into compact representations that serve as an intermediate signal between pixels and generative models. Diffusion-based decoders have recently been adopted in image tokenization to reconstruct images from latent representations with high perceptual fidelity. In contrast to diffusion models used for downstream generation, these decoders are dedicated to faithful reconstruction rather than content generation. However, their iterative sampling process introduces significant latency, making them impractical for real-time or large-scale applications. In this work, we introduce a two-stage acceleration framework to address this inefficiency. First, we propose a multi-scale sampling strategy, where decoding begins at a coarse resolution and progressively refines the output by doubling the resolution at each stage, achieving a theoretical speedup of $\mathcal{O}(\log n)$ compared to standard full-resolution sampling. Second, we distill the diffusion decoder at each scale into a single-step denoising model, enabling fast and high-quality reconstructions in a single forward pass per scale. Together, these techniques yield an order-of-magnitude reduction in decoding time with little degradation in output quality. Our approach provides a practical pathway toward efficient yet expressive image tokenizers. We hope it serves as a foundation for future work in efficient visual tokenization and downstream generation.

---

## 论文详细总结（自动生成）

### 论文总结：通过多尺度采样和单步蒸馏加速扩散解码器

#### 1. 核心问题与整体含义（研究动机和背景）
现代生成模型（如自回归模型或扩散模型）依赖**图像标记化（Image Tokenization）**将像素映射为紧凑的潜表示。传统的自编码器（如 VAE/VQGAN）虽然速度快，但在处理精细纹理和复杂结构时容易模糊。**扩散自编码器（Diffusion Autoencoders）**通过迭代去噪过程实现了极高的重建保真度，但其致命缺点是**推理延迟高**：生成单张图像通常需要数十次甚至上百次前向计算，这限制了其在实时任务和大规模生产中的应用。本文旨在解决扩散解码器“高质量但低效率”的矛盾。

#### 2. 方法论：核心思想与关键技术
论文提出了一个**两阶段加速框架**，结合了空间复杂度的优化和时间步长的压缩：

*   **核心思想**：将全分辨率的迭代去噪分解为“由粗到精”的多尺度过程，并利用蒸馏技术将每个尺度的多步迭代压缩为单步。
*   **关键技术细节**：
    *   **多尺度采样策略（Multi-Scale Sampling）**：解码不再直接在全分辨率（如 256x256）上进行，而是从 32x32 开始，逐级翻倍分辨率（32→64→128→256）。每一级利用前一级的输出作为起始点并加入少量噪声进行细化。这种策略将计算复杂度从 $O(n)$ 优化至 $O(\log n)$。
    *   **单步蒸馏（One-Step Distillation）**：借鉴对抗扩散蒸馏（ADD）的思想，将训练好的多步扩散解码器作为“教师”，训练一个“学生”模型。学生模型在每个分辨率尺度上仅需一次前向传递即可完成去噪。
    *   **模型架构**：采用基于 Transformer 的 **MMDiT（多模态扩散 Transformer）**。编码器将图像压缩为 128 个潜标记（Tokens），解码器通过交叉注意力机制结合这些标记进行重建。
    *   **损失函数**：结合了多尺度重建损失（MSE）、感知损失（LPIPS）和基于 DINO 特征的对抗损失（Adversarial Loss），以确保在极少步数下仍能保持视觉真实感。

#### 3. 实验设计
*   **数据集**：主要在 **ImageNet-1K** 验证集上进行评估，分辨率统一为 256x256。
*   **Benchmark（基准指标）**：重建 FID (rFID)、PSNR、SSIM 以及吞吐量（Throughput，每秒处理图像数）。
*   **对比方法**：
    *   **矢量量化标记器**：TiTok, LlamaGen, Cosmos DI, OpenMagViT-V2, ViT-VQGAN。
    *   **连续潜变量标记器**：DC-AE, Cosmos CI, SD-VAE。
    *   **扩散标记器**：DiTo, FlowMo（当前最先进的扩散标记器）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练时长**：
    *   **第一阶段（教师模型训练）**：联合优化编码器和解码器，训练 **200 个 epoch**，耗时较长。
    *   **第二阶段（蒸馏）**：冻结编码器，仅蒸馏解码器，仅需 **1 个 epoch** 即可完成。
*   **配置**：使用 AdamW 优化器，bfloat16 精度，Batch Size 为 1024。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了主对比实验、消融实验及超参数敏感性分析。
*   **消融实验**：
    *   对比了不同尺度数量（3阶 vs 4阶）对速度和质量的影响。
    *   分析了无分类器引导（CFG）强度对重建效果的作用。
    *   探讨了感知损失权重 $\lambda_{perc}$ 的调节。
*   **充分性评价**：实验设计较为全面，对比了从传统 VQ 到最新扩散标记器的多种路径。通过 rFID 与吞吐量的散点图清晰展示了该方法在“帕累托前沿”上的优势。不过，实验主要集中在 ImageNet 这一通用数据集上，缺乏在特定领域（如医学影像或遥感）的泛化性验证。

#### 6. 主要结论与发现
*   **速度飞跃**：在相同 128 标记的预算下，该方法达到了 **87.16 img/s** 的吞吐量，比之前的扩散标记器 DiTo 快了 **459 倍**，比 FlowMo 快了 **60 倍**。
*   **性能持平**：其推理速度已追平甚至超过了非扩散的连续标记器（如 SD-VAE 的 63.65 img/s），同时保持了扩散模型特有的高感知质量。
*   **质量损失极小**：蒸馏后的 rFID 为 1.09，相比教师模型的 0.91 仅轻微上升，但仍显著优于大多数传统标记器。

#### 7. 优点（亮点）
*   **复杂度优化**：通过多尺度设计实现了理论上的对数级加速，这是对扩散模型推理瓶颈的有效突破。
*   **实用性强**：两阶段框架（先训练强教师，再快速蒸馏）使得该方法易于迁移到其他扩散架构中。
*   **实时性**：首次将扩散解码器的速度提升到足以支持实时视频生成或大规模图像处理的水平。

#### 8. 不足与局限
*   **训练成本**：虽然推理很快，但第一阶段需要 200 epoch 的全量训练，算力消耗依然可观。
*   **超参数敏感**：实验显示 CFG 和感知损失权重的微小变动会对最终 rFID 产生明显影响，调参过程可能较复杂。
*   **应用范围限制**：目前仅验证了图像重建任务，尚未深入探讨该标记器在下游生成任务（如 Text-to-Image）中的实际表现提升。

（完）
