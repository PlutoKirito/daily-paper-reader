---
title: "MosaicMem: Hybrid Spatial Memory for Controllable Video World Models"
title_zh: MosaicMem：用于可控视频世界模型的混合空间记忆
authors: "Wei Yu, Runjia Qian, Yumeng Li, Liquan Wang, Songheng Yin, Sri Siddarth Chakaravarthy P, Dennis Anthony, Yang Ye, Yidi Li, Weiwei Wan, Animesh Garg"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17117v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 视频扩散模型与保持一致性的空间记忆
tldr: 针对视频扩散模型在长视频生成中空间一致性不足的问题，本文提出 MosaicMem 混合空间存储方案。它将图像块提升至 3D 空间进行定位与检索，结合补丁组合接口与 PRoPE 相机调节技术，在保持模型生成能力的同时，实现了精准的相机运动控制与动态物体建模，支持长达数分钟的场景导航与编辑。
selection_source: fresh_fetch
motivation: 现有的显式 3D 结构难以处理动态物体，而隐式存储在相机运动控制上不够精确，限制了视频世界模型的一致性。
method: 提出 MosaicMem 混合存储，通过将图像块映射到 3D 空间进行检索，并利用补丁组合接口与相机调节技术实现空间对齐的视频生成。
result: 实验证明 MosaicMem 在姿态遵循度上优于隐式存储，在动态建模能力上超过了显式基准模型。
conclusion: MosaicMem 为构建可控且具有长效空间记忆的视频世界模型提供了一种有效的混合存储架构。
---

## 摘要
视频扩散模型正从生成简短、合理的片段转向构建世界模拟器，这些模拟器必须在相机运动、重访和干预下保持一致性。然而，空间记忆仍然是一个关键瓶颈：显式 3D 结构可以提高基于重投影的一致性，但难以描绘运动物体；而隐式记忆即使在位姿正确的情况下，也往往会产生不准确的相机运动。我们提出了 Mosaic Memory (MosaicMem)，这是一种混合空间记忆，它将 patch 提升到 3D 空间以实现可靠的定位和针对性检索，同时利用模型的原生条件化（native conditioning）来保持遵循提示词的生成能力。MosaicMem 通过 patch-and-compose 接口在查询视图中合成空间对齐的 patch，在保留应持久存在的内容的同时，允许模型对应该演变的内容进行修复（inpaint）。结合 PRoPE 相机条件化和两种新的记忆对齐方法，实验表明，与隐式记忆相比，MosaicMem 提高了位姿遵循度，且比显式基准模型具有更强的动态建模能力。MosaicMem 进一步实现了分钟级的导航、基于记忆的场景编辑和自回归推演（autoregressive rollout）。

## Abstract
Video diffusion models are moving beyond short, plausible clips toward world simulators that must remain consistent under camera motion, revisits, and intervention. Yet spatial memory remains a key bottleneck: explicit 3D structures can improve reprojection-based consistency but struggle to depict moving objects, while implicit memory often produces inaccurate camera motion even with correct poses. We propose Mosaic Memory (MosaicMem), a hybrid spatial memory that lifts patches into 3D for reliable localization and targeted retrieval, while exploiting the model's native conditioning to preserve prompt-following generation. MosaicMem composes spatially aligned patches in the queried view via a patch-and-compose interface, preserving what should persist while allowing the model to inpaint what should evolve. With PRoPE camera conditioning and two new memory alignment methods, experiments show improved pose adherence compared to implicit memory and stronger dynamic modeling than explicit baselines. MosaicMem further enables minute-level navigation, memory-based scene editing, and autoregressive rollout.

---

## 论文详细总结（自动生成）

以下是对论文《MosaicMem: Hybrid Spatial Memory for Controllable Video World Models》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视频扩散模型正从生成短视频向构建“视频世界模型”（World Models）演进，要求在相机运动、场景重访和交互干预下保持高度的空间一致性。
*   **核心瓶颈**：现有的空间记忆方案存在两极化问题：
    *   **显式记忆**（如 3D 点云/高斯散射）：几何一致性好，但难以处理动态物体，且生成内容往往过于静态。
    *   **隐式记忆**（如 存储历史帧特征）：能处理动态变化，但相机运动控制不精确，长序列生成时容易产生空间漂移（Drift）且计算冗余度高。
*   **整体含义**：本文提出 **MosaicMem**，一种混合空间记忆机制，旨在结合显式几何定位的精确性和隐式特征调节的灵活性，实现可控、长效且一致的视频生成。

### 2. 论文提出的方法论
*   **核心思想**：采用“**图像块（Patch）**”作为记忆的基本单元。将 Patch 提升（Lift）到 3D 空间进行定位，在生成新视角时，像拼贴“马赛克”一样检索并对齐相关的 Patch，由模型决定哪些部分保持不变，哪些部分随动力学演化。
*   **关键技术细节**：
    *   **Mosaic Memory 检索**：利用 3D 估计器（深度和位姿）将历史 Patch 投影到当前查询视角，作为 DiT（Diffusion Transformer）的上下文输入。
    *   **记忆对齐机制**：
        *   **Warped RoPE（扭曲旋转位置编码）**：根据几何投影关系重新计算 Patch 的位置编码，确保特征在空间上精确对齐。
        *   **Warped Latent（扭曲潜变量）**：在特征空间对检索到的 Patch 进行双线性插值采样，补偿 VAE 压缩带来的空间模糊。
    *   **PRoPE 相机控制**：引入投影位置编码（Projective RoPE），将相对相机截锥体几何信息直接注入自注意力机制，并针对 VAE 的时间压缩（4x）进行了适配。
    *   **Patch-and-Compose 接口**：模型通过原生注意力机制整合检索到的 Patch，实现“静态背景靠记忆，动态物体靠生成”的有机结合。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **MosaicMem-World**：作者收集的一个新基准，包含 Unreal Engine 5 模拟场景、商业游戏（《赛博朋克 2077》）、真实世界第一视角视频以及 Sekai 数据集。
    *   **特点**：专门设计了包含大量“重访（Revisit）”情节的轨迹，用于测试长效记忆。
*   **对比方法**：
    *   **显式记忆基准**：GEN3C, SEVA, VMem, VWM。
    *   **隐式记忆基准**：WorldMem, Context-as-Memory (CaM)。
*   **评估指标**：FID/FVD（质量）、RotErr/TransErr（相机精度）、Dynamic Score（动态强度）、Consistency Score（记忆检索一致性，含 SSIM/PSNR/LPIPS）。

### 4. 资源与算力
*   **基础模型**：基于开源的 **Wan 2.2 5B** 视频生成模型进行微调。
*   **算力投入**：
    *   **硬件**：使用 **8 × H100 GPU** 集群。
    *   **训练参数**：学习率 $1 \times 10^{-5}$，有效 Batch Size 为 64。
    *   **时长**：训练了 **250k 个 Step**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（表 1），涵盖显式、隐式及消融变体。
    *   **消融实验**：详细对比了 PRoPE、Warped RoPE、Warped Latent 各组件对性能的影响。
    *   **长视频测试**：生成了长达 **2 分钟**的连续导航视频。
    *   **自回归对比**：在实时生成场景下与 RELIC 和 Matrix-Game 2.0 进行了对比（表 2）。
*   **充分性评价**：实验设计较为全面，不仅覆盖了标准质量指标，还针对“空间记忆”这一核心贡献设计了专门的重访一致性测试，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **混合架构的优越性**：MosaicMem 在相机位姿遵循度上显著优于隐式记忆，在动态物体建模上显著优于显式记忆。
*   **对齐的重要性**：Warped RoPE 和 Warped Latent 的结合能有效解决 VAE 压缩带来的对齐误差，防止长序列生成中的伪影累积。
*   **功能扩展**：该模型不仅能生成长视频，还支持通过直接操作 Mosaic 记忆空间进行场景编辑（如拼接不同风格的场景、创建“盗梦空间”式的垂直空间连接）。

### 7. 优点
*   **创新性**：提出以 Patch 为单位的混合存储，巧妙解决了 3D 一致性与动态生成之间的矛盾。
*   **实用性**：支持自回归生成（Mosaic Forcing），在 H100 上可达到 16 FPS 的实时生成速度。
*   **灵活性**：通过 Patch 级的操作，为视频场景编辑提供了一种直观且具有几何约束的新手段。

### 8. 不足与局限
*   **依赖外部工具**：性能高度依赖于预训练的 3D 估计器（如 Depth Anything V3），如果深度或位姿估计出错，会直接导致记忆检索失效。
*   **计算开销**：虽然 Patch 检索减少了冗余，但 3D 提升和扭曲变换（Warping）在推理时仍增加了额外的几何计算负担。
*   **边界效应**：在极端相机运动下，新旧 Patch 的衔接处可能仍存在细微的视觉不连续性（尽管文中提到 Compose 接口能缓解此问题）。

（完）
