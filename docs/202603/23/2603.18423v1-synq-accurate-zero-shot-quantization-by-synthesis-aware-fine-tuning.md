---
title: "SynQ: Accurate Zero-shot Quantization by Synthesis-aware Fine-tuning"
title_zh: SynQ：通过合成感知微调实现精确的零样本量化
authors: "Minjun Kim, Jongjin Kim, U Kang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18423v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 适用于资源受限边缘设备的零样本量化框架
tldr: SynQ 是一种针对零样本量化（ZSQ）的创新框架，旨在解决合成数据中的噪声、非目标模式预测及错误硬标签引导等挑战。该方法通过引入低通滤波器减少合成样本噪声，利用类激活图（CAM）对齐提升模型准确性，并对困难样本采用软标签策略。实验证明，SynQ 在无原始数据的情况下实现了 SOTA 的量化精度，有效提升了边缘设备的部署性能。
selection_source: fresh_fetch
motivation: 现有的零样本量化方法受限于合成数据的噪声、错误的模式关注以及预训练模型硬标签的误导。
method: 提出 SynQ 框架，通过低通滤波器降噪、类激活图对齐以及针对困难样本的软标签微调来优化量化模型。
result: 在多个基准测试中，SynQ 的量化精度超越了现有的零样本量化方法，达到了领域内领先水平。
conclusion: SynQ 通过感知合成过程的微调策略，有效解决了零样本量化中的核心痛点，为隐私敏感场景下的模型部署提供了高效方案。
---

## 摘要
如何在没有任何数据的情况下精确地量化预训练模型？量化算法被广泛用于在资源受限的边缘设备上部署神经网络。零样本量化 (ZSQ) 解决了由于隐私或安全原因无法获取训练数据的关键且实际的场景。然而，三个重大挑战阻碍了现有 ZSQ 方法的性能：1) 合成数据集中的噪声，2) 基于非目标模式的预测，以及 3) 错误硬标签的误导。在本文中，我们提出了 SynQ（用于零样本量化的合成感知微调），这是一个精心设计的 ZSQ 框架，旨在克服现有方法的局限性。SynQ 通过利用低通滤波器来最小化生成样本中的噪声。然后，SynQ 通过将其类激活图与预训练模型对齐来训练量化模型，以提高准确性。此外，SynQ 通过仅对困难样本利用软标签，减轻了来自预训练模型错误的误导。大量的实验表明，SynQ 提供了优于现有 ZSQ 方法的最先进准确率。

## Abstract
How can we accurately quantize a pre-trained model without any data? Quantization algorithms are widely used for deploying neural networks on resource-constrained edge devices. Zero-shot Quantization (ZSQ) addresses the crucial and practical scenario where training data are inaccessible for privacy or security reasons. However, three significant challenges hinder the performance of existing ZSQ methods: 1) noise in the synthetic dataset, 2) predictions based on off-target patterns, and the 3) misguidance by erroneous hard labels. In this paper, we propose SynQ (Synthesis-aware Fine-tuning for Zero-shot Quantization), a carefully designed ZSQ framework to overcome the limitations of existing methods. SynQ minimizes the noise from the generated samples by exploiting a low-pass filter. Then, SynQ trains the quantized model to improve accuracy by aligning its class activation map with the pre-trained model. Furthermore, SynQ mitigates misguidance from the pre-trained model's error by leveraging only soft labels for difficult samples. Extensive experiments show that SynQ provides the state-of-the-art accuracy, over existing ZSQ methods.

---

## 论文详细总结（自动生成）

以下是对论文《SynQ: Accurate Zero-shot Quantization by Synthesis-aware Fine-tuning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：为了在资源受限的边缘设备上部署深度学习模型，量化技术至关重要。但在隐私或安全敏感的场景下，原始训练数据往往不可获取，这催生了**零样本量化（Zero-shot Quantization, ZSQ）**。
*   **核心问题**：现有的 ZSQ 方法主要通过预训练模型合成数据并进行微调，但面临三大挑战：
    1.  **合成数据噪声**：合成图像与真实图像在频域分布上存在显著差异，含有大量高频噪声。
    2.  **预测模式偏移**：量化模型往往关注图像中的非目标区域（Off-target patterns）。
    3.  **错误标签误导**：预训练模型对合成的“困难样本”给出的硬标签（Hard Labels）往往是错误的，误导了微调过程。

### 2. 方法论
SynQ 提出了“合成感知微调”框架，包含三个核心技术组件：
*   **低通滤波器（Low-pass Filter）**：在频域对合成样本应用高斯低通滤波器 $G$，公式为 $x^F_i = \mathcal{F}^{-1}(G \odot \mathcal{F}(x_i))$。该操作去除了合成图像中的高频噪声，使其振幅分布更接近真实图像。
*   **类激活图对齐（CAM Alignment）**：引入 $L_{CAM}$ 损失函数，通过最小化预训练模型与量化模型之间 Grad-CAM 显著图的均方误差（MSE），强制量化模型学习预训练模型的物体定位知识，确保其关注正确的图像区域。
*   **困难样本软标签策略**：定义样本难度 $\delta = 1 - q_{y_i}$。当样本难度超过阈值 $\tau$ 时，微调过程舍弃硬标签的交叉熵损失（CE），仅使用预训练模型输出的概率分布（软标签）进行 KL 散度蒸馏，以避免错误硬标签的负面影响。
*   **总目标函数**：结合了 KL 散度、有条件的 CE 损失以及 CAM 对齐损失。

### 3. 实验设计
*   **数据集**：CIFAR-10、CIFAR-100 和 ImageNet (ILSVRC 2012)。
*   **模型架构**：
    *   **CNN**：ResNet-18、ResNet-20、ResNet-50、MobileNetV2。
    *   **ViT**：DeiT-Tiny/Small、Swin-Tiny/Small。
*   **Benchmark 与对比方法**：对比了包括 GDFQ、ARC、Qimera、IntraQ、AdaSG、HAST、TexQ 和 PLF 在内的 10 余种主流 ZSQ 方法。
*   **量化设置**：主要针对极低比特场景（如 W3A3 和 W4A4，即权重和激活均为 3/4 bit）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Xeon Silver 4214 CPU** 和 **NVIDIA RTX 3090 GPU** 的工作站上完成。
*   **训练规模**：
    *   合成数据集大小：仅 5,120 张图像（Batch size 256）。
    *   微调周期：100 个 Epoch。
*   **效率说明**：论文指出 SynQ 相比于需要训练生成器的方法（如 AdaSG）更快，且时间复杂度随模型层数线性增长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括不同位宽（3/4 bit）、不同模型架构（CNN 和 Transformer）以及多个数据集。
*   **消融实验**：详细验证了低通滤波、CAM 对齐和软标签策略各自的贡献，证明三个组件缺一不可。
*   **鲁棒性分析**：对超参数（阈值 $\tau$、滤波频率 $D_0$、损失权重 $\lambda$）进行了敏感性分析，并测试了对不同类型噪声（高斯、椒盐等）的鲁棒性。
*   **评价**：实验设计全面，对比了最新的 SOTA 方法（如 2024 年的 PLF），且提供了标准差以示结果的稳定性，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：SynQ 在所有测试设置下均优于现有方法，在 ImageNet 上的 ResNet-18 (3bit) 准确率比之前最好的方法提升了约 1.74%。
*   **频域重要性**：发现合成数据的噪声主要集中在高频段，简单的低通滤波能显著提升量化模型的泛化能力。
*   **注意力一致性**：通过 CAM 对齐，量化模型能够复现预训练模型的决策逻辑，减少了因量化导致的特征偏移。
*   **通用性**：SynQ 可以作为插件集成到现有的 ZSQ 框架（如基于生成器或基于噪声优化的方法）中，并带来一致的性能提升。

### 7. 优点
*   **即插即用**：不改变现有的数据合成逻辑，仅通过改进微调阶段的损失函数和数据预处理即可生效。
*   **视角独特**：从频域分布和空间注意力两个维度切入，解决了 ZSQ 中长期被忽视的合成数据质量问题。
*   **高效性**：相比于生成数百万张图像的方法，SynQ 仅需数千张图像即可达到更高精度。

### 8. 不足与局限
*   **任务覆盖**：实验主要集中在图像分类任务，尚未在目标检测、语义分割等更复杂的下游任务中验证。
*   **超参数调节**：虽然具有一定的鲁棒性，但针对不同数据集（如 CIFAR 与 ImageNet），最佳的滤波频率 $D_0$ 和难度阈值 $\tau$ 仍需手动调整。
*   **应用限制**：目前主要针对均匀量化，对于非均匀量化或更复杂的混合精度量化场景讨论较少。

（完）
