---
title: End-to-End Training for Unified Tokenization and Latent Denoising
title_zh: 统一分词与潜空间去噪的端到端训练
authors: "Shivam Duggal, Xingjian Bai, Zongze Wu, Richard Zhang, Eli Shechtman, Antonio Torralba, Phillip Isola, William T. Freeman"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22283v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 统一令牌化与潜空间扩散训练
tldr: 传统的潜扩散模型（LDM）需要复杂的两阶段训练，即先训练分词器再训练扩散模型。本文提出UNITE架构，通过共享权重的生成编码器将图像分词与潜变量生成统一。该方法将两者视为不同条件下的潜变量推理问题，通过单阶段联合训练，使模型在图像和分子模态上均达到接近SOTA的性能，且无需对抗损失或预训练编码器，简化了训练流程并提升了效率。
selection_source: fresh_fetch
motivation: 旨在解决潜扩散模型中分词器与扩散模型必须分阶段训练导致的流程复杂且潜空间不统一的问题。
method: 提出UNITE架构，利用共享权重的生成编码器通过单阶段两次前向传播同时优化分词与生成任务。
result: 在ImageNet 256x256上取得了FID 2.12和1.73的优异成绩，且无需对抗损失或外部预训练编码器。
conclusion: 证明了从零开始对分词与生成进行单阶段联合训练是可行且高效的，为统一生成模型提供了新思路。
---

## 摘要
潜扩散模型（LDMs）通过在学习到的潜空间中运行，实现了高保真度的合成。然而，训练最先进的 LDM 需要复杂的阶段划分：必须先训练一个分词器（tokenizer），然后才能在冻结的潜空间中训练扩散模型。我们提出了 UNITE——一种用于统一分词和潜扩散的自动编码器架构。UNITE 由一个生成式编码器（Generative Encoder）组成，通过权重共享，它既可以作为图像分词器，也可以作为潜空间生成器。我们的核心见解是，分词和生成可以被视为不同条件机制下的同一种潜空间推理问题：分词是从完全观测到的图像中推理潜变量，而生成则是从噪声以及文本或类别条件中推理潜变量。受此启发，我们引入了一种单阶段训练程序，通过对同一个生成式编码器进行两次前向传递，来联合优化这两个任务。共享参数使得梯度能够共同塑造潜空间，从而促进形成一种“通用潜语言”。在图像和分子模态上，UNITE 在不使用对抗损失或预训练编码器（如 DINO）的情况下，实现了接近最先进水平的性能，在 ImageNet 256 x 256 数据集上，Base 和 Large 模型的 FID 分别达到 2.12 和 1.73。我们还从表示对齐和压缩的角度进一步分析了生成式编码器。这些结果表明，从头开始对分词和生成进行单阶段联合训练是可行的。

## Abstract
Latent diffusion models (LDMs) enable high-fidelity synthesis by operating in learned latent spaces. However, training state-of-the-art LDMs requires complex staging: a tokenizer must be trained first, before the diffusion model can be trained in the frozen latent space. We propose UNITE - an autoencoder architecture for unified tokenization and latent diffusion. UNITE consists of a Generative Encoder that serves as both image tokenizer and latent generator via weight sharing. Our key insight is that tokenization and generation can be viewed as the same latent inference problem under different conditioning regimes: tokenization infers latents from fully observed images, whereas generation infers them from noise together with text or class conditioning. Motivated by this, we introduce a single-stage training procedure that jointly optimizes both tasks via two forward passes through the same Generative Encoder. The shared parameters enable gradients to jointly shape the latent space, encouraging a "common latent language". Across image and molecule modalities, UNITE achieves near state of the art performance without adversarial losses or pretrained encoders (e.g., DINO), reaching FID 2.12 and 1.73 for Base and Large models on ImageNet 256 x 256. We further analyze the Generative Encoder through the lenses of representation alignment and compression. These results show that single stage joint training of tokenization & generation from scratch is feasible.

---

## 论文详细总结（自动生成）

### 论文总结：UNITE —— 统一分词与潜空间去噪的端到端训练

#### 1. 核心问题与整体含义（研究动机和背景）
现代潜扩散模型（LDM）通常采用**多阶段训练方案**：首先训练一个自动编码器（如 VAE/VQ-GAN）作为分词器，将其冻结后，再在诱导的潜空间上训练扩散模型。这种分离设计虽然方便，但存在两个问题：
*   **流程复杂**：需要多次训练和存储不同的模型。
*   **潜空间不统一**：分词器的潜空间仅针对重建优化，未必是最适合生成任务的结构。
论文的研究动机在于探索：**分词（Tokenization）与生成（Generation）是否可以合二为一？** 作者认为两者本质上是同一种潜变量推理问题，只是观测条件不同（分词是强观测，生成是弱观测/噪声观测）。

#### 2. 方法论：核心思想与技术细节
论文提出了 **UNITE** 架构，其核心组件是**生成编码器（Generative Encoder, GE）**：
*   **核心思想**：通过权重共享，让同一个 ViT 编码器同时承担“图像分词”和“潜变量去噪”两个角色。
*   **双路径前向传播**：
    1.  **分词路径（重建）**：输入图像补丁（Patches）和一组可学习的寄存器令牌（Registers）。通过自注意力机制，寄存器吸收图像信息并输出潜变量 $z_0$。
    2.  **生成路径（去噪）**：输入加噪后的潜变量 $z_t$。GE 预测原始潜变量 $\hat{z}_0$（采用 Rectified Flow/流匹配框架）。
*   **关键技术细节**：
    *   **停止梯度（Stop-gradient）**：在生成路径中，对来自路径 1 的 $z_0$ 进行截断，防止去噪梯度直接崩溃潜空间。
    *   **共享参数**：除了归一化层（Normalization）有微小差异外，注意力层和 MLP 层完全共享，促使模型学习一种“通用潜语言”。
    *   **单阶段损失**：总损失 $L = L_{recon} + L_{flow}$，其中重建损失包含像素级 $L_1$ 和感知损失 LPIPS。

#### 3. 实验设计
*   **数据集**：
    *   **图像**：ImageNet 256×256（主要 Benchmark）。
    *   **科学领域**：QM9 分子数据集、MP20 晶体数据集（用于验证在缺乏预训练编码器领域的效果）。
*   **对比方法**：
    *   **单阶段模型**：JiT（像素级去噪）、SiD2、PixelFlow。
    *   **两阶段模型**：DiT、SiT（标准 LDM 流程）。
    *   **带外部监督模型**：REPA、RAE（利用 DINOv2 等预训练模型作为教师）。
*   **评估指标**：gFID（生成质量）、rFID（重建质量）、IS（分类分数）。

#### 4. 资源与算力
*   **训练配置**：使用 AdamW 和 Muon 优化器，全局 Batch Size 为 1024。
*   **算力消耗**：
    *   UNITE-B 在 ImageNet 上训练 120 个 Epoch 约需 $6.7 \times 10^{20}$ FLOPs。
    *   论文指出，相比于依赖 DINOv2 预训练特征的方法（如 RAE），UNITE 的总算力成本低约 **15 倍**（因为省去了昂贵的自监督预训练阶段）。
    *   具体 GPU 型号和数量未在正文中详列，但提到了使用混合精度 BF16 和梯度检查点技术。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 Base (217M) 到 XL (806M) 的模型缩放实验。
*   **消融实验**：非常充分。作者对比了“权重共享 vs. 独立网络”、“有无停止梯度”、“不同重建噪声水平”以及“噪声调度偏移”等关键变量。
*   **客观性**：通过在分子和晶体生成任务上的实验，证明了该方法在没有视觉预训练模型（如 DINO）的领域依然有效，增强了结论的普适性。

#### 6. 主要结论与发现
*   **单阶段可行性**：证明了从零开始联合训练分词与生成是可行的，且性能优异。
*   **性能领先**：UNITE-L 在 ImageNet 上达到 **FID 1.73**，优于两阶段的 DiT-XL/2 (2.27) 和 SiT-XL/2 (2.06)。
*   **权重共享的红利**：参数共享不仅减少了模型体积，还通过生成目标的正则化提升了重建质量（rFID 从 1.38 降至 1.01）。
*   **表示对齐**：分析显示，分词和去噪任务在内部特征表示上具有高度的一致性（CKA 分析）。

#### 7. 优点（亮点）
*   **架构极简**：取消了复杂的两阶段流水线，实现了真正的端到端学习。
*   **无需对抗损失**：在不使用 GAN 损失的情况下即可获得高质量生成结果（虽然加入 GAN 损失可进一步提升重建）。
*   **去中心化/独立性**：不依赖 DINOv2 等外部预训练模型，这对于数据稀缺或非视觉领域（如生物医药）至关重要。

#### 8. 不足与局限
*   **判别能力有限**：虽然生成和重建很强，但生成编码器的线性探测（Linear Probing）准确率仅约 30%，说明其潜变量的语义判别性尚不及专门的自监督模型。
*   **感知损失依赖**：图像实验仍依赖预训练的 VGG 网络计算 LPIPS 损失（尽管作者认为 VGG 训练成本极低）。
*   **训练动态**：联合训练过程中重建和去噪损失存在一定的“对抗性”波动，可能需要更精细的超参数调优。

（完）
