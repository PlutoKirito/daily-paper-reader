---
title: "DUO-VSR: Dual-Stream Distillation for One-Step Video Super-Resolution"
title_zh: DUO-VSR：用于一步视频超分辨率的双流蒸馏
authors: "Zhengyao Lv, Menghan Xia, Xintao Wang, Kwan-Yee K. Wong"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22271v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 利用扩散模型进行单步视频超分辨率的双流蒸馏
tldr: 针对基于扩散模型的视频超分辨率（VSR）采样成本高的问题，本文提出DUO-VSR框架。该框架采用三阶段策略，通过双流蒸馏结合分布匹配与对抗监督，解决了单步生成中的训练不稳定和监督不足问题。实验证明，DUO-VSR在保持高效单步生成的同时，实现了优于现有方法的视觉质量和推理效率。
selection_source: fresh_fetch
motivation: 现有的扩散模型VSR采样速度慢，且直接应用分布匹配蒸馏进行单步加速时存在训练不稳定和监督质量下降的问题。
method: 提出包含渐进式引导蒸馏初始化、结合DMD与RFS-GAN的双流蒸馏以及偏好引导细化的三阶段框架。
result: DUO-VSR在单步视频超分辨率任务上取得了比以往方法更优的视觉效果和更高的推理效率。
conclusion: 该研究通过创新的双流蒸馏策略，成功实现了高质量且高效的单步视频超分辨率生成。
---

## 摘要
基于扩散的视频超分辨率（VSR）最近取得了显著的保真度，但仍面临极高的采样成本。虽然分布匹配蒸馏（DMD）可以将扩散模型加速至一步生成，但将其直接应用于 VSR 往往会导致训练不稳定，以及监督信号退化和不足。为了解决这些问题，我们提出了 DUO-VSR，这是一个基于双流蒸馏策略的三阶段框架，为一步 VSR 统一了分布匹配和对抗监督。首先，采用渐进式引导蒸馏初始化，通过轨迹保持蒸馏来稳定后续训练。接下来，双流蒸馏联合优化 DMD 和真假评分特征 GAN（RFS-GAN）流，后者利用来自真假评分模型的判别性特征提供补充的对抗监督。最后，偏好引导的细化阶段进一步使学生模型与感知质量偏好保持一致。大量实验表明，与以往的一步 VSR 方法相比，DUO-VSR 实现了卓越的视觉质量和效率。

## Abstract
Diffusion-based video super-resolution (VSR) has recently achieved remarkable fidelity but still suffers from prohibitive sampling costs. While distribution matching distillation (DMD) can accelerate diffusion models toward one-step generation, directly applying it to VSR often results in training instability alongside degraded and insufficient supervision. To address these issues, we propose DUO-VSR, a three-stage framework built upon a Dual-Stream Distillation strategy that unifies distribution matching and adversarial supervision for one-step VSR. Firstly, a Progressive Guided Distillation Initialization is employed to stabilize subsequent training through trajectory-preserving distillation. Next, the Dual-Stream Distillation jointly optimizes the DMD and Real-Fake Score Feature GAN (RFS-GAN) streams, with the latter providing complementary adversarial supervision leveraging discriminative features from both real and fake score models. Finally, a Preference-Guided Refinement stage further aligns the student with perceptual quality preferences. Extensive experiments demonstrate that DUO-VSR achieves superior visual quality and efficiency over previous one-step VSR approaches.

---

## 论文详细总结（自动生成）

### DUO-VSR：用于一步视频超分辨率的双流蒸馏论文总结

#### 1. 核心问题与整体含义
*   **研究动机**：基于扩散模型的视频超分辨率（VSR）虽然能生成高保真细节，但由于需要数十次迭代采样，推理速度极慢，难以实际应用。
*   **核心挑战**：现有的加速技术如分布匹配蒸馏（DMD）在应用于 VSR 时存在三大问题：
    1.  **训练不稳定**：直接从多步模型初始化单步学生模型会导致分布偏差过大。
    2.  **监督信号退化**：冻结的教师模型（Real Score Model）未见过学生生成的噪声输出，可能产生空间偏移或伪影。
    3.  **监督信号不足**：学生模型受限于教师模型的质量上限，无法达到真实高清视频的水平。
*   **整体含义**：本文提出 **DUO-VSR** 框架，旨在通过一种三阶段的蒸馏策略，在实现“一步生成”极速推理的同时，突破教师模型的质量限制，提升视频的感知质量和时间一致性。

#### 2. 方法论
论文提出了一个三阶段的训练框架：
*   **阶段 I：渐进式引导蒸馏初始化（Progressive Guided Distillation）**
    *   首先进行无分类器引导（CFG）蒸馏，使单一模型匹配条件与无条件分支的组合。
    *   随后进行渐进式蒸馏，将多步轨迹逐步压缩至一步，为后续训练提供稳定的起点。
*   **阶段 II：双流蒸馏策略（Dual-Stream Distillation）—— 核心创新**
    *   **DMD 流**：通过最小化 KL 散度，使学生模型分布对齐教师模型。
    *   **RFS-GAN 流**：引入“真假评分特征 GAN”。它不使用额外的判别器，而是利用**真实评分模型**和**伪评分模型**的中间层特征作为判别器骨干，添加轻量级卷积头进行对抗训练。
    *   **联合优化**：交替更新学生模型、伪评分模型和判别器头。这种设计利用真实视频的对抗信号弥补了 DMD 监督的不足，并纠正了教师模型的偏差。
*   **阶段 III：偏好引导细化（Preference-Guided Refinement）**
    *   利用视频质量评估模型对生成的多个候选样本进行排序，构建偏好数据集。
    *   采用直接偏好优化（DPO）算法微调学生模型，使其更符合人类视觉审美。

#### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：SPMCS、UDM10、YouHQ40（用于评估保真度）。
    *   **真实世界数据集**：VideoLQ。
    *   **AIGC 数据集**：自建 AIGC60（包含 60 个 AI 生成的视频场景）。
*   **Benchmark 与对比方法**：
    *   对比了 8 种 SOTA 方法，包括多步扩散模型（VEnhancer, MGLD, STAR 等）和单步模型（DLoRAL, DOVE, SeedVR2）。
*   **评估指标**：
    *   保真度：PSNR, SSIM, LPIPS。
    *   感知质量：NIQE, MUSIQ, CLIP-IQA, DOVER。
    *   时间一致性：$E^*_{warp}$（流翘曲误差）。

#### 4. 资源与算力
*   **模型规模**：基础 VSR 模型约为 13 亿（1.3B）参数。
*   **训练细节**：
    *   在 83 万个样本上预训练基础模型（10k iterations）。
    *   蒸馏阶段：CFG 蒸馏 500 次，双流蒸馏 2000 次，DPO 细化 1000 次。
    *   Batch Size 为 32 或 64。
*   **算力说明**：文中未明确给出训练所用的 GPU 具体型号和数量，但提到推理测试是在**单张 GPU** 上完成的。对于 21 帧 1080P 视频，推理仅需 **11.3 秒**，比 SeedVR-7B 快约 50 倍，比多步模型 MGLD 快近 90 倍。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成、真实、AI 生成三大类场景，实验设计较为全面。
*   **消融实验**：针对三阶段框架、双流组件（DMD vs GAN）、优化策略（联合 vs 顺序）以及判别器特征来源做了详细对比。
*   **主观评估**：进行了 20 人参与的盲测用户研究（GSB 测试），从视觉保真度、视觉质量和整体质量三个维度进行评分。
*   **充分性评价**：实验设计客观、公平，通过定量指标、定性视觉对比和主观用户研究多维度验证了方法的有效性。

#### 6. 主要结论与发现
*   **性能领先**：DUO-VSR 在感知指标（如 NIQE, MUSIQ, DOVER）上一致优于现有的一步生成方法。
*   **效率极高**：实现了真正的一步推理，显著降低了扩散模型在视频任务中的计算开销。
*   **双流协同**：证明了将分布匹配（DMD）与对抗学习（GAN）结合能有效打破教师模型的性能天花板，并解决训练不稳定的问题。

#### 7. 优点
*   **创新的判别器设计**：RFS-GAN 复用了评分模型的特征，既节省了显存，又提供了更强的判别信号。
*   **三阶段流程科学**：从稳定初始化到双流增强，再到偏好对齐，逻辑严密，解决了单步蒸馏的痛点。
*   **平衡性好**：在极速推理的前提下，较好地平衡了细节生成与时间一致性。

#### 8. 不足与局限
*   **VAE 瓶颈**：推理过程中，视频 VAE 的解码占用了超过 90% 的时间，模型整体速度受限于 VAE 而非扩散模型本身。
*   **极细微细节丢失**：由于在潜空间（Latent Space）训练且 VAE 存在时空压缩，对于极小的文字等细节恢复能力有限。
*   **训练复杂性**：三阶段训练流程相对复杂，对超参数和数据质量有一定依赖。

（完）
