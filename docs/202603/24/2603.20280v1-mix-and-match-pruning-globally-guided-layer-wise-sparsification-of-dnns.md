---
title: "Mix-and-Match Pruning: Globally Guided Layer-Wise Sparsification of DNNs"
title_zh: Mix-and-Match 剪枝：深度神经网络的全局引导逐层稀疏化框架
authors: "Danial Monachan, Samira Nazari, Mahdi Taheri, Ali Azarpeyvand, Milos Krstic, Michael Huebner, Christian Herglotz"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.20280v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 针对边缘设备部署的分层稀疏化与剪枝框架
tldr: 针对深度神经网络在边缘设备部署时的压缩需求，本文提出了一种名为 Mix-and-Match Pruning 的全局引导逐层稀疏化框架。该框架通过结合敏感度评分和架构感知规则，为不同层生成多样化的高质量剪枝配置，解决了单一策略无法适应不同层特性的问题。实验表明，该方法在 CNN 和 ViT 上均达到了帕累托最优，显著降低了精度损失，证明了协调现有剪枝信号比开发新标准更有效。
selection_source: fresh_fetch
motivation: 传统的单一剪枝策略无法有效应对不同网络层和架构对稀疏化的差异化响应，导致压缩效果受限。
method: 提出一种全局引导的逐层剪枝框架，利用架构感知规则和敏感度信号采样生成多种剪枝配置，实现灵活的精度-稀疏度权衡。
result: "在 Swin-Tiny 等模型上，该方法相比标准单准则剪枝将精度下降减少了 40%，并在多种架构上实现了帕累托最优。"
conclusion: 通过系统性地协调现有的剪枝信号和架构规则，可以比引入复杂的新剪枝标准更高效地生成高性能的压缩模型。
---

## 摘要
在边缘设备上部署深度神经网络 (DNN) 需要在保持最小精度损失的同时进行强力压缩。本文介绍了 Mix-and-Match 剪枝，这是一种全局引导的逐层稀疏化框架，它利用敏感度得分和简单的架构规则来生成多样化、高质量的剪枝配置。该框架解决了不同层和架构对剪枝反应不同这一关键局限性，使得单一策略方法往往表现欠佳。Mix-and-Match 推导出感知架构的稀疏度范围（例如，在保留归一化层的同时更激进地剪枝分类器），并系统地对这些范围进行采样，从而为每个敏感度信号（量级、梯度或其组合）生成十种策略。这消除了重复的剪枝运行，同时提供了可直接部署的精度-稀疏度权衡。在 CNN 和 Vision Transformer 上的实验证明了帕累托最优结果，与标准的单准则剪枝相比，Mix-and-Match 将 Swin-Tiny 的精度下降减少了 40%。这些研究结果表明，与引入新准则相比，协调现有的剪枝信号可以实现更可靠、更高效的压缩模型。

## Abstract
Deploying deep neural networks (DNNs) on edge devices requires strong compression with minimal accuracy loss. This paper introduces Mix-and-Match Pruning, a globally guided, layer-wise sparsification framework that leverages sensitivity scores and simple architectural rules to generate diverse, high-quality pruning configurations. The framework addresses a key limitation that different layers and architectures respond differently to pruning, making single-strategy approaches suboptimal. Mix-and-Match derives architecture-aware sparsity ranges, e.g., preserving normalization layers while pruning classifiers more aggressively, and systematically samples these ranges to produce ten strategies per sensitivity signal (magnitude, gradient, or their combination). This eliminates repeated pruning runs while offering deployment-ready accuracy-sparsity trade-offs. Experiments on CNNs and Vision Transformers demonstrate Pareto-optimal results, with Mix-and-Match reducing accuracy degradation on Swin-Tiny by 40% relative to standard single-criterion pruning. These findings show that coordinating existing pruning signals enables more reliable and efficient compressed models than introducing new criteria.

---

## 论文详细总结（自动生成）

### 论文总结：Mix-and-Match Pruning (混合匹配剪枝)

#### 1. 核心问题与研究动机
*   **研究背景**：深度神经网络（DNN）和视觉 Transformer（ViT）参数量巨大（通常在 100MB 到 500MB 以上），难以直接部署在资源受限的边缘设备上。
*   **核心问题**：现有的剪枝方法（如 MAG、SNIP 等）通常针对固定的稀疏度目标生成单一模型，且往往忽略了不同网络层和架构组件对剪枝的敏感度差异。
*   **研究动机**：当部署约束发生变化时，传统方法需要重新运行整个剪枝流程。本文旨在开发一个能够系统探索“精度-稀疏度”权衡空间、且具备架构感知能力的统一框架。

#### 2. 方法论
Mix-and-Match 框架分为三个阶段，旨在通过一次敏感度分析生成多个候选模型：
*   **阶段 1：敏感度分析与范围分配**
    *   计算权重敏感度得分 $S_i^{(l)}$，支持三种准则：权重绝对值（Magnitude）、梯度（Gradient）或两者的乘积。
    *   **架构感知规则**：为不同类型的层设定稀疏度范围 $[\rho_{min}, \rho_{max}]$。例如：归一化层不剪枝（0%）；参数量小于 10K 的小层限制在 [0%, 10%]；全连接层则允许更激进的剪枝。
*   **阶段 2：策略构建（Strategy Generation）**
    *   在设定的范围内采样生成 **10 种不同的剪枝策略**。
    *   策略包括：保守型（Min）、激进型（Max）、平衡型（Midpoint）、参数比例型（根据层大小缩放）以及结构感知型（针对分类器或特征提取层进行侧重）。
*   **阶段 3：剪枝执行与微调**
    *   根据选定的敏感度得分对权重排序并应用二进制掩码（Mask）。
    *   在微调过程中强制执行掩码，确保被剪掉的权重保持为零。

#### 3. 实验设计
*   **数据集与场景**：
    *   **VGG-11**：使用 CIFAR-10 数据集。
    *   **ResNet-18**：使用 GTSRB（交通标志识别）数据集。
    *   **LeViT-384**：使用 CIFAR-10 数据集。
    *   **Swin-Tiny**：使用 CIFAR-100 数据集。
*   **对比基准（Benchmarks）**：
    *   经典方法：**MAG**（量级剪枝）、**SNIP**（单次网络剪枝）、**GraSP**（梯度信号保留）。
    *   针对性方法：**LPViT**（针对 ViT 的半结构化剪枝）、**GETA**（联合剪枝与量化）。

#### 4. 资源与算力
*   **硬件平台**：使用 **NVIDIA A100 GPU**。
*   **耗时分析**：
    *   **阶段 1（敏感度计算）**：VGG-11 耗时小于 2 分钟。
    *   **阶段 3（微调）**：每个策略约 8-10 分钟（以 20 个 epoch 为例）。
    *   **总耗时**：生成 10 个模型总计约 85 分钟，而传统基准方法若要生成 10 个不同配置的模型则需约 100 分钟以上，且缺乏系统性的超参数搜索。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种代表性架构（卷积、残差、混合、分层 Transformer）和 3 个主流视觉数据集。
*   **多样性**：每种架构都测试了 10 种不同的稀疏化策略，并绘制了帕累托（Pareto）前沿曲线。
*   **客观性**：所有方法（包括基准方法）均采用统一的优化协议（如 Adam/AdamW 优化器、相同的学习率和微调轮数），确保了对比的公平性。

#### 6. 主要结论与发现
*   **帕累托最优**：Mix-and-Match 在所有测试架构上均达到了帕累托最优或极具竞争力的结果。
*   **精度提升**：在 Swin-Tiny 模型上，相比标准的单准则剪枝，该框架将精度下降减少了 **40%**。
*   **架构差异性**：实验发现 VGG-11 最适合“量级+梯度”组合准则，而 ResNet-18 和 Swin-Tiny 更适合纯量级准则，LeViT-384 则在纯梯度准则下表现最好。
*   **压缩效果**：成功将 VGG-11 压缩至约 10MB，ResNet-18 压缩至约 4.5MB，且精度损失极小。

#### 7. 优点与亮点
*   **高效性**：通过一次敏感度计算复用多个策略，大幅降低了探索“精度-压缩比”权衡空间的计算成本。
*   **架构感知**：引入了基于层类型和参数量的硬性约束（如保护归一化层），有效防止了过度剪枝导致的模型崩溃。
*   **灵活性**：能够根据部署环境的实时资源限制，从生成的 10 个模型中快速选择最合适的一个。

#### 8. 不足与局限
*   **微调成本**：虽然敏感度分析是单次的，但每个生成的策略仍需独立的微调过程，对于超大规模模型（如 LLM），这部分开销依然显著。
*   **非结构化限制**：目前主要侧重于非结构化剪枝，虽然减少了参数量，但在不支持稀疏加速的常规硬件上可能无法获得显著的推理加速。
*   **架构规则依赖**：框架中的某些稀疏度范围（如 [15%, 30%] 用于 Patch Embedding）是基于经验设定的，可能需要针对全新架构进行手动调整。

（完）
