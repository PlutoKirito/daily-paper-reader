---
title: Repurposing Geometric Foundation Models for Multi-view Diffusion
title_zh: 将几何基础模型重新用于多视图扩散
authors: "Wooseok Jang, Seonghu Jeon, Jisang Han, Jinhyeok Choi, Minkyung Kwon, Seungryong Kim, Saining Xie, Sainan Liu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22275v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于多视图合成的几何潜扩散
tldr: 针对新视角合成（NVS）中几何一致性不足的问题，本文提出几何潜扩散（GLD）框架。该方法摒弃了传统的独立VAE潜空间，转而利用几何基础模型的特征空间作为扩散模型的潜空间。GLD不仅实现了高保真图像重建，还通过跨视角几何对应关系显著提升了3D一致性，在训练效率和生成质量上均优于现有方法，且无需大规模文本到图像预训练即可达到领先水平。
selection_source: fresh_fetch
motivation: 现有的多视图生成方法多采用视角独立的VAE潜空间，难以保证生成结果在几何上的高度一致性。
method: 提出GLD框架，将几何基础模型的特征空间重新利用为多视图扩散的潜空间，以利用其内在的几何对应关系。
result: GLD在图像质量和3D一致性上超越了VAE和RAE，训练速度提升4.4倍以上，且在无预训练情况下仍具竞争力。
conclusion: 几何基础模型的特征空间是进行多视图扩散生成的理想潜空间，能有效平衡生成质量与几何一致性。
---

## 摘要
虽然生成式潜空间的最新进展推动了单图像生成的重大进步，但用于新视角合成（NVS）的最佳潜空间在很大程度上仍未被探索。特别是，NVS 要求在不同视角之间实现几何一致的生成，但现有方法通常在与视角无关的 VAE 潜空间中运行。在本文中，我们提出了几何潜扩散（Geometric Latent Diffusion, GLD），这是一个将几何基础模型的几何一致特征空间重新用作多视图扩散潜空间的框架。我们表明，这些特征不仅支持高保真 RGB 重建，还编码了强大的跨视图几何对应关系，为 NVS 提供了一个非常合适的潜空间。我们的实验表明，GLD 在 2D 图像质量和 3D 一致性指标上均优于 VAE 和 RAE，同时与 VAE 潜空间相比，训练速度提高了 4.4 倍以上。值得注意的是，尽管 GLD 在没有此类生成式预训练的情况下从头开始训练其扩散模型，但它与利用大规模文本到图像预训练的最新方法相比仍具有竞争力。

## Abstract
While recent advances in generative latent spaces have driven substantial progress in single-image generation, the optimal latent space for novel view synthesis (NVS) remains largely unexplored. In particular, NVS requires geometrically consistent generation across viewpoints, but existing approaches typically operate in a view-independent VAE latent space. In this paper, we propose Geometric Latent Diffusion (GLD), a framework that repurposes the geometrically consistent feature space of geometric foundation models as the latent space for multi-view diffusion. We show that these features not only support high-fidelity RGB reconstruction but also encode strong cross-view geometric correspondences, providing a well-suited latent space for NVS. Our experiments demonstrate that GLD outperforms both VAE and RAE on 2D image quality and 3D consistency metrics, while accelerating training by more than 4.4x compared to the VAE latent space. Notably, GLD remains competitive with state-of-the-art methods that leverage large-scale text-to-image pretraining, despite training its diffusion model from scratch without such generative pretraining.

---

## 论文详细总结（自动生成）

以下是对论文《Repurposing Geometric Foundation Models for Multi-view Diffusion》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多视图生成（如新视角合成 NVS）方法大多沿用为 2D 图像生成设计的 VAE（变分自编码器）潜空间。这种潜空间是“视角无关”的，缺乏内在的几何约束，导致生成的多视图在 3D 一致性上表现不佳，且训练效率低下。
*   **研究动机**：作者提出，与其在缺乏几何信息的空间中强行注入几何条件，不如直接利用**几何基础模型**（如 Depth Anything 3, VGGT）预训练好的特征空间。这些空间天然编码了跨视角的几何对应关系，是进行几何感知生成的理想“土壤”。

### 2. 方法论
*   **核心思想**：提出**几何潜扩散（GLD）**框架，将扩散过程从传统的 VAE 潜空间转移到几何基础模型的特征空间中。
*   **关键技术细节**：
    *   **特征空间选择**：选用 Depth Anything 3 (DA3) 作为主干。DA3 提取 4 个层级的特征。
    *   **边界层确定（Boundary Layer）**：通过实验发现，第 1 层（Level 1）特征在几何对应关系（3D 结构）和光度保真度（图像细节）之间达到了最佳平衡。
    *   **三阶段流水线**：
        1.  **RGB 解码器训练**：训练一个 ViT 解码器，证明几何特征可以高质量重建 RGB 图像。
        2.  **多视图扩散**：在 Level 1 特征空间进行扩散，使用 DiT 架构，并引入 3D 自注意力机制和 Plücker 射线嵌入来强化视角一致性。
        3.  **级联生成与传播**：显式合成 Level 1 特征；通过级联模型合成更浅的 Level 0 特征以保证对齐；更深的 Level 2/3 特征则通过冻结的 DA3 编码器直接传播获得。
    *   **零成本几何预测**：由于在几何特征空间运行，生成的潜变量可以直接通过原生的几何解码器输出深度图和相机参数。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：RealEstate10K (Re10K), DL3DV, HyperSim, TartanAir。
    *   **评估集**：Re10K, DL3DV（域内）；Mip-NeRF 360（域外/零样本泛化）；ETH3D（深度评估）。
*   **Benchmark 与对比方法**：
    *   **潜空间基准**：对比了基于 VAE 和 DINOv2 特征空间的扩散模型。
    *   **SOTA 方法**：对比了 MVGenMaster, Matrix3D, CAMEO, NVComposer, CAT3D 等利用大规模文本到图像（T2I）预训练权重的先进模型。
*   **评估指标**：2D 指标（PSNR, SSIM, LPIPS）和 3D 指标（ATE 相机误差, RPE 相对位姿误差, 重投影误差, MEt3R 一致性指标）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA B200 GPU**。
*   **训练时长**：
    *   扩散模型训练了 **175k 次迭代**。
    *   RGB 解码器训练了约 **170k 步**。
*   **效率提升**：相比于 VAE 潜空间，GLD 的收敛速度快了 **4.4 倍**以上。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 1 到 4 个源视图的多种配置，并在域内和域外数据集上进行了广泛测试。
*   **消融实验**：详细探讨了边界层选择（k=0 到 3）、级联生成 vs 独立生成的差异、以及不同源视图数量对性能的影响。
*   **充分性评价**：实验设计非常全面且客观。作者不仅对比了图像质量，还深入分析了内部注意力机制的几何对应关系（PCK 指标），并验证了不同几何主干（如 VGGT）的通用性，证明了结论的稳健性。

### 6. 主要结论与发现
*   **几何特征更优**：几何基础模型的特征空间显著优于通用的 VAE 或语义（DINO）空间，尤其在维持 3D 结构一致性方面。
*   **无需大规模预训练**：即使 GLD 是从头开始训练（不依赖 Stable Diffusion 等 T2I 权重），其表现依然能与经过大规模预训练微调的 SOTA 方法持平甚至更优。
*   **层级特性**：中间层特征（Level 1）是生成任务的“甜点区”，兼顾了结构与纹理。

### 7. 优点
*   **创新性强**：首次系统性地将几何基础模型特征作为扩散潜空间，解决了 NVS 任务中几何一致性的根本痛点。
*   **效率极高**：大幅缩短了训练收敛时间，且推理时能同步生成高质量深度图。
*   **3D 一致性卓越**：生成的视角在几何上高度对齐，减少了物体漂移或结构扭曲。

### 8. 不足与局限
*   **推理延迟**：由于采用了两阶段采样（Level 1 和 Level 0），推理速度慢于单阶段的 VAE 扩散模型（GLD 约 66s vs VAE 约 28s）。
*   **极端场景挑战**：在极度稀疏的输入或严重的遮挡情况下，模型仍可能产生幻觉伪影。
*   **依赖主干质量**：生成质量在一定程度上受限于所选几何基础模型（如 DA3）的表征能力。

（完）
