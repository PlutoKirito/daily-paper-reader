---
title: "Adaptive Video Distillation: Mitigating Oversaturation and Temporal Collapse in Few-Step Generation"
title_zh: 自适应视频蒸馏：缓解少步生成中的过饱和与时间崩溃
authors: "Yuyang You, Yongzhi Li, Jiahui Li, Yadong Mu, Quan Chen, Peng Jiang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21864v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 专为视频扩散模型设计的蒸馏框架，以减轻时间崩溃
tldr: 针对视频扩散模型在少步生成中存在的过度饱和与时间崩溃问题，本文提出了一种自适应视频蒸馏框架。该框架通过引入自适应回归损失以动态调整空间监督权重，结合时间正则化损失来确保采样轨迹的平滑性，并利用推理阶段的帧插值策略降低计算开销。实验证明，该方法在VBench等基准测试中显著提升了视频的感知保真度和运动真实感，优于现有的蒸馏基准。
selection_source: fresh_fetch
motivation: 现有的图像蒸馏技术直接应用于视频模型时，容易导致画面过度饱和、时间不一致及模式崩溃等严重伪影。
method: 提出了由自适应回归损失、时间正则化损失和推理时帧插值组成的视频专用蒸馏框架。
result: 在VBench和VBench2基准测试中，该方法实现了高质量的少步视频合成，在多项指标上均优于现有蒸馏基准。
conclusion: 本研究通过针对性的优化策略有效解决了视频蒸馏中的关键挑战，为实现高效且高质量的视频生成提供了新方案。
---

## 摘要
视频生成近期已成为生成式人工智能领域的核心任务。然而，视频合成固有的巨大计算成本使得模型蒸馏成为高效部署的关键技术。尽管其具有重要意义，但专门针对视频扩散模型设计的方法仍然匮乏。现有方法通常直接沿用图像蒸馏技术，这往往会导致过饱和、时间不一致和模式崩溃等伪影。为了应对这些挑战，我们提出了一种专门为视频扩散模型量身定制的新型蒸馏框架。其核心创新包括：(1) 一种自适应回归损失，通过动态调整空间监督权重来防止由过度分布偏移引起的伪影；(2) 一种时间正则化损失，用于对抗时间崩溃，促进平滑且符合物理规律的采样轨迹；以及 (3) 一种推理阶段的帧插值策略，在保持感知质量的同时减少采样开销。在 VBench 和 VBench2 基准测试上的广泛实验和消融研究表明，我们的方法实现了稳定的少步视频合成，显著增强了感知保真度和运动真实感。在多项指标上，该方法始终优于现有的蒸馏基准模型。

## Abstract
Video generation has recently emerged as a central task in the field of generative AI. However, the substantial computational cost inherent in video synthesis makes model distillation a critical technique for efficient deployment. Despite its significance, there is a scarcity of methods specifically designed for video diffusion models. Prevailing approaches often directly adapt image distillation techniques, which frequently lead to artifacts such as oversaturation, temporal inconsistency, and mode collapse. To address these challenges, we propose a novel distillation framework tailored specifically for video diffusion models. Its core innovations include: (1) an adaptive regression loss that dynamically adjusts spatial supervision weights to prevent artifacts arising from excessive distribution shifts; (2) a temporal regularization loss to counteract temporal collapse, promoting smooth and physically plausible sampling trajectories; and (3) an inference-time frame interpolation strategy that reduces sampling overhead while preserving perceptual quality. Extensive experiments and ablation studies on the VBench and VBench2 benchmarks demonstrate that our method achieves stable few-step video synthesis, significantly enhancing perceptual fidelity and motion realism. It consistently outperforms existing distillation baselines across multiple metrics.

---

## 论文详细总结（自动生成）

### 论文总结：Adaptive Video Distillation (自适应视频蒸馏)

#### 1. 核心问题与整体含义（研究动机和背景）
视频扩散模型（Video Diffusion Models）虽然生成效果卓越，但推理时的计算开销巨大，限制了实时应用。现有的加速技术主要依赖蒸馏（Distillation），但直接将图像蒸馏技术（如 DMD）迁移至视频领域会引发三大问题：
*   **过饱和（Oversaturation）：** 蒸馏过程中的误差累积导致视频颜色异常鲜艳、失真。
*   **时间崩溃（Temporal Collapse）：** 模型倾向于生成静态或动作幅度极小的视频（模式崩溃在时间维度的体现）。
*   **伪影与撕裂：** 强行对齐分布时，若监督信号不一致，会导致物体变形或画面撕裂。
本文旨在开发一种专为视频设计的蒸馏框架，在实现 4 步（Few-step）极速生成的条件下，保持甚至超越原教师模型的动态效果和视觉质量。

#### 2. 方法论：核心思想与关键技术
该框架基于分布匹配蒸馏（DMD），并引入了三个核心组件：
*   **自适应回归损失（Adaptive Regression Loss）：**
    *   **核心思想：** 在蒸馏损失之外引入真实数据的回归监督，但为了防止数据分布差异过大导致的“画面撕裂”，采用动态权重。
    *   **技术细节：** 维护一个“损失均值缓存”（Loss Mean Cache），通过指数移动平均（EMA）计算每个时间步的预期损失。利用 Sigmoid 函数根据当前样本损失与均值的偏差动态调整权重：偏差越大，权重越低，从而过滤掉可能导致伪影的异常梯度。
*   **时间正则化损失（Temporal Regularization Loss）：**
    *   **核心思想：** 显式惩罚时间维度上的低方差输出，强制模型生成具有动态变化的视频。
    *   **技术细节：** 计算生成视频在时间轴上的方差，并取负对数作为损失。为了防止过度补偿导致画面闪烁（幻觉），该损失在收敛到一定阈值后会被截断（Truncated）。
*   **推理时帧插值策略（Frame-interpolation Inference）：**
    *   **核心思想：** 利用扩散模型“高噪声阶段决定语义，低噪声阶段决定细节”的特性。
    *   **技术细节：** 在前两个高噪声步中以半帧率推理，随后使用一个轻量级的预训练 UNet 模块在 Latent 空间进行插值还原帧率，最后两个低噪声步在全帧率下精修细节。

#### 3. 实验设计
*   **基础模型（Teacher）：** 采用了先进的开源视频模型 Wan 2.1（包含 1.3B 和 14B 两个版本）。
*   **数据集：** 
    *   蒸馏训练：混合了开源和私有视频数据集的文本标注。
    *   回归监督：精心筛选的 15 万条高质量视频子集（经过分辨率、色彩熵、光流、美学评分等多重过滤）。
*   **Benchmark：** VBench 和 VBench2（涵盖语义质量、感知保真度、运动真实感等维度）。
*   **对比方法：** DMD、LCM（潜变量一致性模型）、PCM、DCM、rCM 等主流蒸馏/加速基准。

#### 4. 资源与算力
*   **硬件：** 论文明确指出所有实验均在 **24 台 GPU** 上进行。
*   **型号与时长：** 文中未明确说明具体的 GPU 型号（如 A100 或 H100）以及总训练时长。
*   **参数量：** 针对 1.3B 和 14B 两种规模的模型均进行了蒸馏验证。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 1.3B 到 14B 的模型缩放实验，并在 VBench1/2 两个权威榜单上进行了全面评测。
*   **消融实验：** 针对自适应权重（k 值影响）、时间正则化（截断阈值影响）、帧插值模块的效果分别进行了详细对比。
*   **主观评价：** 组织了 12 名专业标注员进行人类偏好研究（User Study），对比了 180 组视频样本。
*   **充分性评价：** 实验设计较为全面，不仅有定量指标，还有丰富的定性视觉对比和下游任务（如风格迁移）的适配性验证，客观性较强。

#### 6. 主要结论与发现
*   **性能领先：** 该方法在 4 步生成下，VBench 总分优于所有现有的蒸馏基准，甚至在某些维度（如运动真实感）上超过了 50 步推理的教师模型。
*   **解决过饱和：** 自适应回归损失有效校准了颜色分布，消除了 DMD 常见的过饱和现象。
*   **增强动态性：** 时间正则化成功解决了蒸馏模型“画面变静止”的顽疾。
*   **效率提升：** 结合帧插值策略，推理速度进一步提升约 30%，且感知质量几乎无损。

#### 7. 优点与亮点
*   **自适应机制：** 巧妙地利用 EMA 缓存来动态调整监督权重，平衡了“学习真实分布”与“保持生成稳定性”之间的矛盾。
*   **任务融合：** 该框架支持在蒸馏的同时进行有监督微调（SFT），例如直接将模型蒸馏为特定的动画风格，极具实用价值。
*   **推理优化：** 帧插值策略充分利用了扩散模型不同阶段的特性，而非盲目加速，设计非常优雅。

#### 8. 不足与局限
*   **算力细节缺失：** 未公布具体的训练耗时和 GPU 型号，不便于其他研究者评估复现成本。
*   **正则化风险：** 虽然引入了截断机制，但时间正则化权重若设置不当，仍可能导致视频出现不符合物理规律的剧烈抖动（幻觉）。
*   **应用限制：** 帧插值模块需要额外预训练一个轻量级 UNet，虽然推理快，但增加了训练管线的复杂性。

（完）
