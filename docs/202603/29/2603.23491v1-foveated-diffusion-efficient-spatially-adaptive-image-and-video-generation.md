---
title: "Foveated Diffusion: Efficient Spatially Adaptive Image and Video Generation"
title_zh: 注视点扩散：高效的空间自适应图像与视频生成
authors: "Brian Chao, Lior Yariv, Howard Xiao, Gordon Wetzstein"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23491v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 基于扩散模型的高效空间自适应图像与视频生成
tldr: 该研究针对扩散模型在高分辨率图像和视频生成中计算开销巨大的问题，提出了一种受人类视觉特性启发的“注视点扩散”（Foveated Diffusion）方法。通过结合眼动追踪技术，在用户注视区域分配高密度标记，而在边缘区域分配低密度标记，实现了非均匀的标记分配。该方法支持从现有模型进行后训练，在显著降低计算量和生成时间的同时，保持了与全分辨率生成一致的视觉感知效果。
selection_source: fresh_fetch
motivation: 扩散模型在处理高分辨率和长视频时，计算复杂度随标记数量呈二次方增长，导致生成效率低下。
method: 利用人类视觉的偏心率依赖特性，通过掩码在注视点区域分配高密度标记，在边缘区域分配低密度标记，构建混合分辨率生成机制。
result: 实验和用户研究表明，该方法在大幅减少标记数量和缩短生成时间的同时，达到了与全分辨率生成在感知上不可区分的效果。
conclusion: 注视点生成为高效、可扩展的图像和视频生成提供了一个实用且具有竞争力的优化维度。
---

## 摘要
扩散模型和流匹配模型为创意内容创作解锁了前所未有的能力，例如交互式图像和流式视频生成。然而，对更高分辨率、帧率和上下文长度日益增长的需求，使得高效生成变得越来越具有挑战性，因为计算复杂度随生成的 token 数量呈二次方增长。我们的工作旨在优化用户注视位置已知或可被估计（例如通过眼动追踪）的场景下的生成过程效率。在这些场景中，我们利用了人类视觉中依赖于离心率的敏锐度：虽然用户在注视位置周围的一个小区域（中央凹区域）内感知到极高分辨率的视觉信息，但在视野外周，分辨细节的能力会迅速下降。我们的方法首先使用一个模拟注视点分辨率的掩码来非均匀地分配 token，为中央凹区域分配较高的 token 密度，为外周区域分配较低的密度。图像或视频在混合分辨率的 token 设置下生成，产生的结果在感知上与全分辨率生成无异，同时大幅减少了 token 数量和生成时间。为此，我们开发了一种从高分辨率数据直接构建混合分辨率 token 的原则性机制，使得注视点扩散模型能够从现有的基础模型进行后训练，同时保持跨分辨率的内容一致性。我们通过广泛的分析和精心设计的用户研究验证了我们的方法，证明了注视点化作为高效生成的一种实用且可扩展的维度是有效的。

## Abstract
Diffusion and flow matching models have unlocked unprecedented capabilities for creative content creation, such as interactive image and streaming video generation. The growing demand for higher resolutions, frame rates, and context lengths, however, makes efficient generation increasingly challenging, as computational complexity grows quadratically with the number of generated tokens. Our work seeks to optimize the efficiency of the generation process in settings where the user's gaze location is known or can be estimated, for example, by using eye tracking. In these settings, we leverage the eccentricity-dependent acuity of human vision: while a user perceives very high-resolution visual information in a small region around their gaze location (the foveal region), the ability to resolve detail quickly degrades in the periphery of the visual field. Our approach starts with a mask modeling the foveated resolution to allocate tokens non-uniformly, assigning higher token density to foveal regions and lower density to peripheral regions. An image or video is generated in a mixed-resolution token setting, yielding results perceptually indistinguishable from full-resolution generation, while drastically reducing the token count and generation time. To this end, we develop a principled mechanism for constructing mixed-resolution tokens directly from high-resolution data, allowing a foveated diffusion model to be post-trained from an existing base model while maintaining content consistency across resolutions. We validate our approach through extensive analysis and a carefully designed user study, demonstrating the efficacy of foveation as a practical and scalable axis for efficient generation.

---

## 论文详细总结（自动生成）

以下是对论文《Foveated Diffusion: Efficient Spatially Adaptive Image and Video Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代扩散模型（如 DiT）的计算复杂度随生成的 token 数量呈二次方增长。随着用户对高分辨率、高帧率和长上下文视频的需求增加，实时生成变得极其困难。
*   **研究动机**：人类视觉系统具有“注视点”特性，即只有注视中心（中央凹区域）具有极高分辨率，而边缘视觉的细节分辨能力随离心率增加而迅速下降。论文旨在利用这一生理特性，通过空间自适应的生成方式，在不损失感知质量的前提下大幅提升生成效率。

### 2. 方法论
*   **核心思想**：引入“注视点扩散”（Foveated Diffusion）框架，根据注视点掩码（Foveation Mask）非均匀地分配 token。在注视区域保持高密度 token，在边缘区域使用低密度 token（如 2×2 区域合并为一个 token）。
*   **关键技术细节**：
    *   **注视点标记化（Foveated Tokenization）**：将潜在空间划分为混合分辨率。高分辨率区域保留原始 token，边缘区域则通过下采样减少 token 数量，从而缩短序列长度 $L$。
    *   **混合分辨率 RoPE（旋转位置嵌入）**：为了处理非均匀网格，采用了“相位对齐”（Phase-Aligned）的 RoPE 调整方案，确保不同分辨率的 token 在注意力机制中能够正确对齐空间位置。
    *   **注视点后训练（Foveated Training）**：直接在混合分辨率 token 空间进行微调。通过 VAE 编码器分别获取高、低分辨率的潜在表示，并根据掩码合并（Merge）成训练目标，使用流匹配（Flow Matching）目标函数进行训练。
    *   **推理流程**：在缩短的 token 序列上进行去噪，得到结果后将其拆分回高/低分辨率网格，分别通过 VAE 解码，最后根据掩码进行 alpha 混合。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像生成**：使用 Aesthetic-Train-V2 数据集，基于 FLUX.2 Klein 4B 模型进行微调。
    *   **视频生成**：使用 Vchitect-T2V-Dataverse 数据集，基于 Wan2.1 1.3B 模型进行微调。
*   **对比基准（Benchmark）**：
    *   **Full high-res**：全分辨率生成（基准上限）。
    *   **Naïve mixed-res**：未经训练直接应用混合分辨率去噪（对比基线）。
*   **评估指标**：FID、Precision、CLIP Score、HPSv2.1（人类偏好得分）、VBench（视频质量指标）以及 2AFC（双选迫选）用户研究。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA H100 GPU 集群。
*   **训练细节**：
    *   采用 LoRA（低秩自适应）技术，秩（rank）设为 32。
    *   图像和视频模型各训练 10,000 步。
    *   有效批大小（Batch Size）为 8。
    *   图像生成分辨率为 1024×1024，视频为 480p（并展示了 720p 的泛化能力）。

### 5. 实验数量与充分性
*   **实验规模**：定量评估涵盖了 10,000 张图像和 200 个视频样本。
*   **消融与变体**：测试了多种掩码策略，包括随机圆掩码、显著性引导掩码（Saliency-guided）和边界框掩码（Bounding-box）。
*   **用户研究**：邀请 11 名受试者进行了 660 次试验，模拟了眼动追踪环境。
*   **充分性评价**：实验设计较为全面，不仅涵盖了图像和视频两个维度，还通过用户研究弥补了传统指标（如 FID）在评估注视点渲染时的局限性，客观地证明了该方法在感知上的一致性。

### 6. 主要结论与发现
*   **显著加速**：在保持感知质量的前提下，图像生成实现了约 **2倍** 的加速，视频生成实现了约 **4倍** 的加速。
*   **训练的必要性**：未经训练的混合分辨率生成（Naïve）会导致严重的结构不一致和比例失调（如物体重复），而经过后训练的模型能完美保持跨分辨率的内容一致性。
*   **感知等效性**：用户研究表明，在注视点观看条件下，该方法生成的内容与全分辨率内容在感知上不可区分。
*   **功能扩展**：该模型能很好地泛化到未见过的掩码形状和位置，并支持显著性引导的生成。

### 7. 优点（亮点）
*   **感知驱动**：将计算机图形学中成熟的注视点渲染概念引入扩散模型，符合人类视觉原理。
*   **高效性**：直接在缩短的 token 序列上操作，从根本上解决了 DiT 的计算瓶颈。
*   **兼容性强**：作为一种后训练方案，可以轻松适配现有的预训练扩散模型，无需从头训练。
*   **内容一致性**：通过原则性的 token 合并和 RoPE 调整，解决了混合分辨率带来的伪影问题。

### 8. 不足与局限
*   **边界伪影**：在注视区域与边缘区域的交界处，由于 VAE 分别解码后再混合，偶尔会出现颜色不连续或轻微伪影。
*   **VAE 限制**：目前的 VAE 并非为混合分辨率设计，未来若能开发直接处理混合分辨率 token 的 VAE，效果会更佳。
*   **分辨率层级**：目前仅实现了两级分辨率（1x 和 1/4x），尚未探索更多层级的渐进式过渡。
*   **依赖注视信息**：该方法的优势高度依赖于准确的注视点输入（如眼动追踪硬件），在普通显示设备上的应用受限。

（完）
