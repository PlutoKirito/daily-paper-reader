---
title: "ViBe: Ultra-High-Resolution Video Synthesis Born from Pure Images"
title_zh: ViBe：源自纯图像的超高分辨率视频合成
authors: "Yunfeng Wu, Hongying Cheng, Zihao He, Songhua Liu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23326v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 视频扩散Transformer与端到端训练
tldr: 针对视频扩散模型在超高分辨率训练中面临的计算复杂度高、成本昂贵的问题，本文提出了ViBe框架。该框架通过纯图像适配技术，将预训练视频模型升级为超高分辨率合成模型。核心采用Relay LoRA两阶段策略解决模态对齐与空间外推问题，并引入高频感知训练目标增强细节。实验证明，ViBe在无需视频训练数据的情况下，生成质量超越了现有SOTA模型。
selection_source: fresh_fetch
motivation: 视频扩散模型在超高分辨率下的端到端训练因计算复杂度过高而难以实现。
method: 提出Relay LoRA两阶段适配策略（模态对齐与空间外推）及高频感知训练目标，仅利用图像数据提升视频分辨率。
result: 在不使用任何视频训练数据的情况下，ViBe在VBench基准测试中比现有SOTA模型高出0.8分。
conclusion: 证明了通过纯图像适配可以有效实现超高分辨率视频合成，显著降低了训练成本并提升了视觉细节。
---

## 摘要
基于 Transformer 的视频扩散模型依赖于空间和时间 token 上的 3D 注意力机制，这导致了二次方的时间和内存复杂度，使得超高分辨率视频的端到端训练成本极高。为了克服这一瓶颈，我们提出了一个纯图像适配框架，将预训练的视频扩散 Transformer 升级以合成更高分辨率的视频。然而，由于图像-视频模态差异，仅使用高分辨率图像进行朴素微调通常会引入明显的噪声。为了解决这个问题，我们将学习目标解耦，分别处理模态对齐和空间外推。我们方法的核心是 Relay LoRA，这是一种两阶段适配策略。在第一阶段，利用低分辨率图像将视频扩散模型适配到图像域，以弥合模态差异。在第二阶段，使用高分辨率图像进一步适配模型，以获取空间外推能力。在推理过程中，仅保留高分辨率适配部分，以在保持视频生成模态的同时实现高分辨率视频合成。为了增强细粒度细节的合成，我们进一步提出了高频感知训练目标，通过专门的重构损失显式地鼓励模型从退化的潜变量表示中恢复高频分量。广泛的实验表明，我们的方法在不需要任何视频训练数据的情况下，能够生成具有丰富视觉细节的超高分辨率视频，在 VBench 基准测试中甚至比在超高分辨率视频上训练的先前最先进模型高出 0.8 分。代码可在 https://github.com/WillWu111/ViBe 获取。

## Abstract
Transformer-based video diffusion models rely on 3D attention over spatial and temporal tokens, which incurs quadratic time and memory complexity and makes end-to-end training for ultra-high-resolution videos prohibitively expensive. To overcome this bottleneck, we propose a pure image adaptation framework that upgrades a video Diffusion Transformer pre-trained at its native scale to synthesize higher-resolution videos. Unfortunately, naively fine-tuning with high-resolution images alone often introduces noticeable noise due to the image-video modality gap. To address this, we decouple the learning objective to separately handle modality alignment and spatial extrapolation. At the core of our approach is Relay LoRA, a two-stage adaptation strategy. In the first stage, the video diffusion model is adapted to the image domain using low-resolution images to bridge the modality gap. In the second stage, the model is further adapted with high-resolution images to acquire spatial extrapolation capability. During inference, only the high-resolution adaptation is retained to preserve the video generation modality while enabling high-resolution video synthesis. To enhance fine-grained detail synthesis, we further propose a High-Frequency-Awareness-Training-Objective, which explicitly encourages the model to recover high-frequency components from degraded latent representations via a dedicated reconstruction loss. Extensive experiments demonstrate that our method produces ultra-high-resolution videos with rich visual details without requiring any video training data, even outperforming previous state-of-the-art models trained on high-resolution videos by 0.8 on the VBench benchmark. Code will be available at https://github.com/WillWu111/ViBe.

---

## 论文详细总结（自动生成）

### 论文总结：ViBe - 基于纯图像训练的超高分辨率视频合成

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于 Transformer 的视频扩散模型（DiT）在处理超高分辨率（如 4K）视频时，面临 3D 注意力机制带来的二次方计算复杂度和显存瓶颈，导致端到端训练成本极高。
*   **研究动机**：现有的视频生成模型大多在低分辨率（如 480P）下预训练。研究者希望探索：**能否仅利用静态图像数据进行微调，使低分辨率预训练视频模型具备生成 4K 超高清视频的能力？**
*   **挑战**：直接用高分辨率图像微调视频模型会产生严重的“图像-视频模态鸿沟”，导致视频推理时出现明显的噪声和伪影。

#### 2. 论文提出的方法论
ViBe 提出了一个纯图像适配框架，核心包含以下三个关键技术：
*   **Relay LoRA（中继 LoRA）**：
    *   **阶段 1（模态对齐）**：使用低分辨率图像训练 LoRA 1，使视频模型适应图像模态。
    *   **阶段 2（空间外推）**：在 LoRA 1 基础上，使用高分辨率图像训练 LoRA 2，赋予模型处理高分辨率空间结构的能力。
    *   **推理阶段**：仅保留 LoRA 2 并挂载到原始视频模型上。这样既保留了原始模型的视频动力学（时间维度），又获得了 LoRA 2 的高分辨率生成能力，有效消除了模态差异带来的噪声。
*   **GCLFA（全局粗糙-局部精细注意力）**：
    *   为了平衡计算开销与全局一致性，将注意力分为两路：**局部精细分支**采用向内滑动窗口注意力捕捉细节；**全局粗糙分支**通过池化操作生成压缩的全局 token，确保长程语义一致。
*   **HFATO（高频感知训练目标）**：
    *   在训练中引入“退化-重构”机制。先对潜变量进行下采样再上采样的退化处理（丢失高频信息），然后要求模型预测流（Flow）并重构原始清晰潜变量 $x_0$。
    *   **公式**：$L = \|\hat{x}_0 - x_0\|^2$，通过显式的重构损失强制模型学习恢复丢失的高频纹理和边缘。

#### 3. 实验设计
*   **基础模型**：基于 SOTA 视频模型 **Wan2.2**（5B 和 14B 版本）。
*   **数据集**：使用 FLUX 1.1 Pro 生成的 **2.3K 张** 高质量超高清图像（2752 × 1536 分辨率）。
*   **Benchmark**：**VBench**（涵盖审美质量、成像质量、语义一致性等维度）。
*   **对比方法**：
    *   **超分模型**：Real-ESRGAN, Upscale-A-Video。
    *   **免训练高分辨率生成**：I-Max, HiFlow。
    *   **基于视频训练的方法**：CineScale, T3-Video。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **训练时长**：两阶段训练总计约 **1 天**（每个阶段各 3000 次迭代）。
*   **推理开销**：在 4K 分辨率下，推理显存占用约为 74.82 GB。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量评估**：在 1080P 和 4K 两个分辨率下进行了详尽的 VBench 测试。
    *   **消融实验**：针对 Relay LoRA、GCLFA、HFATO 以及下采样比例（DR）分别进行了定性和定量分析，验证了各组件的必要性。
    *   **用户研究**：邀请 35 名参与者对文本对齐、审美和视频质量进行主观评分。
    *   **泛化性测试**：验证了该方法在 4 步快速采样模型、图生视频（I2V）以及视频风格迁移任务中的表现。
*   **充分性评价**：实验设计非常全面，不仅对比了同类图像适配方法，还挑战了需要大量视频数据训练的 SOTA 模型，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **纯图像训练可行性**：证明了通过合理的解耦策略（Relay LoRA），仅靠图像数据就能让视频模型实现高质量的空间外推。
*   **性能超越**：ViBe 在 VBench 上的综合得分超过了使用真实高分辨率视频训练的 CineScale 等模型（高出约 0.8 分）。
*   **细节增强**：HFATO 机制显著提升了 4K 视频中的皮肤纹理、毛发等细粒度特征。

#### 7. 优点
*   **极低成本**：无需昂贵的视频数据采集和超大规模算力集群，单卡即可完成适配。
*   **即插即用**：作为 LoRA 模块，可以轻松集成到现有的 DiT 架构和下游任务（如 I2V、快速采样）中。
*   **理论创新**：Relay LoRA 的两阶段解耦思想为解决跨模态微调中的噪声问题提供了新思路。

#### 8. 不足与局限
*   **推理显存要求高**：虽然训练成本低，但 4K 视频推理仍需 A100 等大显存显卡，普通消费级显卡难以运行。
*   **时间一致性依赖原模型**：由于训练不涉及视频数据，模型的时间连贯性完全依赖于预训练的低分辨率视频模型，无法通过该方法修正原模型可能存在的动作畸变。
*   **数据偏差**：依赖于合成图像（FLUX 生成），可能存在合成数据的分布偏差。

（完）
