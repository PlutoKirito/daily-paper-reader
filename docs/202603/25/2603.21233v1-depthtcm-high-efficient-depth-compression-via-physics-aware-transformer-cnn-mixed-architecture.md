---
title: "DepthTCM: High Efficient Depth Compression via Physics-aware Transformer-CNN Mixed Architecture"
title_zh: DepthTCM：基于物理感知的 Transformer-CNN 混合架构的高效深度图压缩
authors: "Young-Seo Chang, Yatong An, Jae-Sang Hyun"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21233v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 使用学习型编解码器的端到端深度图压缩框架
tldr: 本研究提出DepthTCM，一种结合物理感知与Transformer-CNN混合架构的高效深度图压缩框架。该方法通过多波长深度（MWD）编码将高位深度图无损转换为三通道图像，并利用4位量化显著降低熵值，最后通过结合卷积与Transformer优势的神经网络进行压缩。实验表明，该方法在保持极高精度的同时大幅降低了比特率，且具备极高的推理效率，适用于实时RGB-D应用场景。
selection_source: fresh_fetch
motivation: 旨在解决高位深度图在压缩过程中面临的比特率过高、精度损失严重以及推理效率不足的挑战。
method: 利用受物理启发的MWD编码将深度图转为三通道表示，并结合4位量化与Transformer-CNN混合架构进行端到端压缩。
result: "在Middlebury数据集上以0.307 bpp实现了99.38%的精度，且在移动端设备上表现出极高的推理速度。"
conclusion: DepthTCM证明了物理感知编码与混合神经网络架构在提升深度图压缩率失真性能和实用性方面的巨大潜力。
---

## 摘要
我们提出了 DepthTCM，这是一个用于深度图压缩的物理感知端到端框架。在 DepthTCM 框架中，受物理正弦条纹投影轮廓测量系统的启发，高位深度图首先被无损地转换为常规的三通道图像表示，随后该三通道彩色图像由近期开发的 Transformer-CNN 混合神经网络架构进行编码和解码。具体而言，DepthTCM 使用多波长深度（MWD）编码将深度映射为平滑的三通道表示，然后将 MWD 编码后的表示全局量化为每通道 4 位以降低熵，最后使用结合了卷积层和 Transformer 层的学习型编解码器进行压缩。实验结果证明了所提方法的优势。在 Middlebury 2014 数据集上，DepthTCM 在保持 99.38% 准确率的同时达到了 0.307 bpp 的比特率，其保真度与无损 PNG 相当。我们还展示了实际的效率和可扩展性，在 ScanNet++ iPhone RGB-D 子集上报告的平均端到端推理时间分别为 41.48 毫秒（编码器）和 47.45 毫秒（解码器）。消融实验验证了我们的设计选择：相对于 8 位量化，4 位量化在保持相当的重建质量的同时，将比特率降低了 66%，PSNR 变化仅为 0.68 dB，准确率差异仅为 0.04%。此外，相比于纯 CNN 架构，Transformer-CNN 模块进一步将 PSNR 提升了高达 0.75 dB。

## Abstract
We propose DepthTCM, a physics-aware end-to-end framework for depth map compression. In our framework of DepthTCM, the high-bit depth map is first converted to a conventional 3-channel image representation losslessly using a method inspired by a physical sinusoidal fringe pattern based profiliometry system, then the 3-channel color image is encoded and decoded by a recently developed Transformer-CNN mixed neural network architecture. Specifically, DepthTCM maps depth to a smooth 3-channel using multiwavelength depth (MWD) encoding, then globally quantized the MWD encoded representation to 4 bits per channel to reduce entropy, and finally is compressed using a learned codec that combines convolutional and Transformer layers. Experiment results demonstrate the advantage of our proposed method. On Middlebury 2014, DepthTCM reaches 0.307 bpp while preserving 99.38% accuracy, a level of fidelity commensurate with lossless PNG. We additionally demonstrate practical efficiency and scalability, reporting average end-to-end inference times of 41.48 ms (encoder) and 47.45 ms (decoder) on the ScanNet++ iPhone RGB-D subset. Ablations validate our design choices: relative to 8-bit quantization, 4-bit quantization reduces bitrate by 66% while maintaining comparable reconstruction quality, with only a marginal 0.68 dB PSNR change and a 0.04% accuracy difference. In addition, Transformer--CNN blocks further improve PSNR by up to 0.75 dB over CNN-only architectures.

---

## 论文详细总结（自动生成）

以下是对论文《DepthTCM: High Efficient Depth Compression via Physics-aware Transformer-CNN Mixed Architecture》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：高质量深度图包含精确的几何细节，但数据量巨大，给存储和实时传输带来了挑战。
*   **研究背景**：
    *   **传统编解码器局限性**：JPEG、PNG 等传统工具无法有效处理深度图中“分段平滑但边缘锐利”的特性，导致比特率过高或结构细节丢失。
    *   **现有学习型方法不足**：直接将针对自然图像（RGB）设计的深度学习压缩模型应用于原始深度图效果不佳，因为深度图的统计特性（如大面积同质区域和边界主导的信号）与自然图像迥异。
    *   **混合方法潜力未尽**：现有的混合方法（如 N-DEPTH）仍依赖传统编解码器（如 H.264），未能充分利用深度学习在熵建模方面的潜力。

### 2. 论文提出的方法论
DepthTCM 结合了**物理感知编码**与**混合神经网络架构**，构建了一个端到端的深度压缩框架。
*   **核心思想**：利用物理启发的多波长深度（MWD）编码将深度图转换为更易压缩的相位表示，并结合 Transformer-CNN 混合架构进行高效熵编码。
*   **关键技术细节**：
    1.  **MWD 编码**：受结构光测量系统启发，将单通道深度图转换为 3 通道图像。红/绿通道存储正弦/余弦包装相位（捕捉高频细节），蓝通道存储归一化深度（作为绝对深度参考）。
    2.  **全局 4 位量化**：在压缩前将 MWD 表示量化为每通道 4 位。实验发现，利用相位的结构冗余，4 位量化能显著降低熵（比特率下降约 66%），而对几何精度的影响微乎其微。
    3.  **Transformer-CNN 混合架构 (TCM)**：采用混合自编码器。分析变换（Encoder）和合成变换（Decoder）交替使用卷积层（提取局部特征）和 Transformer 块（捕捉全局上下文），以更好地重建边缘。
    4.  **损失函数**：联合优化 MSE（重建误差）、Bpp（比特率估计）、置信度加权损失（针对挑战区域）和全变分（TV）损失（保持平滑性）。

### 3. 实验设计
*   **数据集与场景**：
    *   **DIODE**：室内/室外密集深度图。
    *   **Middlebury 2014**：高分辨率立体匹配深度图。
    *   **ScanNet v2**：大规模室内场景。
    *   **ScanNet++ (iPhone RGB-D)**：移动端 LiDAR 采集的低分辨率数据。
    *   **UnrealStereo4K**：4K 高分辨率合成数据。
    *   **KITTI**：室外自动驾驶稀疏深度图。
*   **Benchmark 与对比方法**：
    *   对比了传统方法：PNG、JPEG 90、JPEG-LS。
    *   对比了相关研究：MWD 原型方法、N-DEPTH（基于 JPEG/H.264 的混合方法）。
    *   对比了消融基准：Only-TCM（直接压缩原始深度图）、纯 CNN 架构。

### 4. 资源与算力
*   **硬件设备**：使用单张 **NVIDIA GeForce RTX 4080 SUPER** GPU。
*   **训练细节**：在 ScanNet v2 的 25,000 帧子集上训练，运行 **100 个 epoch**，batch size 为 4。
*   **推理效率**：在 ScanNet++ (256x192) 上，编码耗时 41.48ms，解码耗时 47.45ms，接近实时处理速度（约 21-24 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个不同来源、不同分辨率、不同稀疏程度的数据集上进行了测试，验证了泛化性。
*   **消融实验**：
    *   **量化位数**：对比了 2-bit 到 8-bit 的性能，证明 4-bit 是最佳平衡点。
    *   **架构对比**：对比了 Transformer-CNN 混合架构与纯 CNN 架构，证明前者在 PSNR 上有 0.75 dB 的提升。
    *   **量化策略**：对比了全局固定量化与自适应块量化，证明全局量化能更好地保持 MWD 的相位连续性。
*   **评价**：实验设计全面且客观，通过跨数据集验证和与 SOTA 方法的直接对比，充分证明了该框架的有效性。

### 6. 论文的主要结论与发现
*   **性能卓越**：在 Middlebury 数据集上，以 0.307 bpp 的极低比特率实现了 99.38% 的准确率，性能媲美无损 PNG，但压缩率提升了约 44 倍。
*   **物理感知的优势**：MWD 编码改变了深度信号的熵特性，使得通用图像压缩模型能高效处理几何数据。直接压缩原始深度图（Only-TCM）效果极差。
*   **低位量化可行性**：4 位量化在深度压缩中表现出极高的性价比，大幅降低比特率的同时几乎不损失几何保真度。

### 7. 优点
*   **物理与 AI 结合**：巧妙利用物理光学中的相位编码原理来预处理数据，降低了神经网络的学习难度。
*   **无需辅助信息**：与某些需要 RGB 图像引导的方法不同，DepthTCM 仅依赖深度图本身，适用性更广。
*   **高效且可扩展**：推理速度快，且能处理从低分辨率到 4K 的多种数据。
*   **端到端优化**：整个流程（编码、量化、压缩、解码）是全微分的，支持联合优化。

### 8. 不足与局限
*   **动态场景限制**：目前主要针对单帧深度图压缩，未充分利用视频序列中的时间冗余。
*   **极端稀疏性挑战**：虽然在 KITTI 上表现尚可，但在极度稀疏的 LiDAR 数据上，MWD 编码的连续性假设可能会受到挑战。
*   **硬件部署**：虽然推理速度较快，但在嵌入式或移动端低功耗芯片上的实际部署表现（如算子支持情况）仍需进一步验证。

（完）
