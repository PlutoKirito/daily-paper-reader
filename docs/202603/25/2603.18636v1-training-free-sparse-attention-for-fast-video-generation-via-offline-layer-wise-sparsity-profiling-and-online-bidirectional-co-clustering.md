---
title: Training-Free Sparse Attention for Fast Video Generation via Offline Layer-Wise Sparsity Profiling and Online Bidirectional Co-Clustering
title_zh: 通过离线逐层稀疏度分析与在线双向协同聚类实现快速视频生成的免训练稀疏注意力
authors: "Jiayi Luo, Jiayu Chen, Jiankun Wang, Cong Wang, Hanxin Zhu, Qingyun Sun, Chen Gao, Zhibo Chen, Jianxin Li"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18636v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于快速视频生成的稀疏注意力与逐层稀疏分析
tldr: 针对视频生成模型（DiT）推理成本高的问题，本文提出了一种无需训练的稀疏注意力框架SVOO。该方法基于“注意力稀疏性是层固有属性”的发现，通过离线层级敏感度分析确定各层剪枝比例，并结合在线双向协同聚类算法实现块级稀疏注意力。在Wan2.1等7个主流模型上的实验表明，SVOO在保持高质量生成的同时，实现了高达1.93倍的加速，显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有稀疏注意力方法忽略了层间异构性和查询-键（Q-K）耦合，限制了视频生成中质量与速度的平衡。
method: 提出SVOO框架，包含离线层级稀疏度分析以确定固有剪枝水平，以及在线双向协同聚类算法以优化块级注意力计算。
result: 在7个主流视频生成模型上验证，SVOO实现了最高1.93倍的加速，且在Wan2.1上保持了29 dB的高PSNR。
conclusion: SVOO通过结合层级特性与双向聚类，为高效、高质量的视频生成提供了一种无需训练的通用优化方案。
---

## 摘要
扩散 Transformer (DiTs) 在视频生成质量上表现出色，但由于密集的 3D 注意力机制导致推理成本高昂，这促使了旨在提高效率的稀疏注意力技术的发展。然而，现有的视频生成免训练稀疏注意力方法仍面临两个尚未解决的局限性：在注意力剪枝中忽略了层间异质性，以及在分块过程中忽略了查询-键（query-key）耦合，这阻碍了更好的质量-加速权衡。在这项工作中，我们揭示了一个关键见解：每一层的注意力稀疏性是其固有属性，受不同输入的影响较小。受此启发，我们提出了 SVOO，这是一个通过离线逐层稀疏度分析和在线双向协同聚类实现快速视频生成的免训练稀疏注意力框架。具体而言，SVOO 采用两阶段范式：(i) 离线逐层敏感度分析，以推导各层固有的剪枝水平；(ii) 通过一种新型的双向协同聚类算法实现在线块状稀疏注意力。在七个广泛使用的视频生成模型上进行的广泛实验表明，SVOO 相比现有最先进方法实现了更优的质量-加速权衡，在 Wan2.1 上实现了高达 1.93 倍的加速，同时保持了高达 29 dB 的 PSNR。

## Abstract
Diffusion Transformers (DiTs) achieve strong video generation quality but suffer from high inference cost due to dense 3D attention, leading to the development of sparse attention technologies to improve efficiency. However, existing training-free sparse attention methods in video generation still face two unresolved limitations: ignoring layer heterogeneity in attention pruning and ignoring query-key coupling in block partitioning, which hinder a better quality-speedup trade-off. In this work, we uncover a critical insight that the attention sparsity of each layer is its intrinsic property, with minor effects across different inputs. Motivated by this, we propose SVOO, a training-free Sparse attention framework for fast Video generation via Offline layer-wise sparsity profiling and Online bidirectional co-clustering. Specifically, SVOO adopts a two-stage paradigm: (i) offline layer-wise sensitivity profiling to derive intrinsic per-layer pruning levels, and (ii) online block-wise sparse attention via a novel bidirectional co-clustering algorithm. Extensive experiments on seven widely used video generation models demonstrate that SVOO achieves a superior quality-speedup trade-off over state-of-the-art methods, delivering up to $1.93\times$ speedup while maintaining a PSNR of up to 29 dB on Wan2.1.

---

## 论文详细总结（自动生成）

### 论文总结：SVOO——基于离线层级分析与在线双向协同聚类的免训练稀疏注意力框架

#### 1. 核心问题与研究动机
*   **核心问题**：基于扩散 Transformer (DiT) 的视频生成模型虽然质量高，但其密集的 3D 自注意力机制具有随 Token 数量平方增长的计算复杂度，导致推理成本极高。
*   **研究动机**：
    *   **层间异质性（Layer Heterogeneity）**：现有方法通常对所有层采用统一的稀疏比例，忽略了不同层对剪枝的敏感度差异。
    *   **Q-K 耦合性（Q-K Coupling）**：现有分块方法独立地对查询（Query）和键（Key）进行分块，导致语义关联的 Q-K 对可能被划分到不匹配的块中，降低了生成质量。
*   **关键见解**：每一层的注意力稀疏性是其**固有属性**，在不同输入下表现出高度的稳定性（Layer-wise Stability）。

#### 2. 方法论
SVOO 采用两阶段范式来实现高效的稀疏注意力：
*   **离线阶段：逐层稀疏度分析（Offline Layer-Wise Sparsity Profiling）**
    *   **核心思想**：通过少量随机输入量化每层的剪枝容忍度。
    *   **技术细节**：计算预 Softmax 注意力 Logit 的行方差作为稀疏度代理。对每一层和每一个注意力头，拟合单变量高斯分布，取其上分位数作为该层的保守稀疏度估计，从而生成定制化的“稀疏度表”。
*   **在线阶段：双向协同聚类（Online Bidirectional Co-Clustering）**
    *   **核心思想**：联合优化 Q 和 K 的分块，使块内 Token 具有相似的注意力偏好。
    *   **算法流程**：交替进行“查询感知的键聚类”和“键感知的查询聚类”。通过计算 Token 与另一侧质心的亲和力向量进行分配并迭代更新，产生对齐的 Q-K 块。
    *   **优化技术**：利用聚类结果在扩散步骤间的稳定性，每隔 $N$ 步重新聚类以降低开销；使用 Triton 实现定制化的 FlashInfer 内核。

#### 3. 实验设计
*   **测试模型**：涵盖 7 个主流视频生成模型，包括 Wan2.1 (1.3B/14B T2V, 14B I2V)、Wan2.2 (A14B T2V/I2V) 以及 HunyuanVideo (T2V/I2V)。
*   **数据集/场景**：使用 VBench 和 VBench++ 基准测试集，涵盖文本生成视频 (T2V) 和图像生成视频 (I2V) 任务。
*   **对比方法**：SpargeAttention, SVG, SVG2, Radial 等最先进的免训练稀疏注意力方法。
*   **评估指标**：PSNR, SSIM, LPIPS（质量相似度）；VBench 综合评分（图像质量、美学、一致性等）；推理延迟与加速比。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在单台 **NVIDIA H200 GPU** 上完成。
*   **生成配置**：测试分辨率为 720p (720×1280)，Wan 系列生成 81 帧，HunyuanVideo 系列生成 129 帧。
*   **训练时长**：由于是免训练（Training-Free）方法，无模型训练开销。离线分析阶段仅需极少量随机输入（如 10 个），计算成本可忽略不计。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同规模和架构的模型上进行了验证，涵盖了从 1.3B 到 14B 以及 MoE 架构。
*   **消融实验**：针对离线分析阶段和在线聚类阶段分别做了消融，验证了各组件对速度和质量的贡献。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了传统的图像质量指标（PSNR），还引入了专门的视频生成基准（VBench），并探讨了聚类重用间隔、块数量、预热步数等超参数的影响，提供了完整的质量-速度权衡曲线。

#### 6. 主要结论与发现
*   **性能领先**：SVOO 在所有测试模型上均实现了最优的质量-速度权衡。例如在 Wan2.1-1.3B 上实现 **1.93倍** 加速，在 HunyuanVideo 上实现 **2.17倍** 加速。
*   **质量保持**：在大幅加速的同时，SVOO 能够保持极高的生成保真度（如 Wan2.1 上 PSNR 接近 30dB），视觉效果与原始密集模型几乎无异。
*   **理论验证**：通过数学证明和实验观察，确认了 DiT 层级稀疏性的稳定性，为离线分析提供了理论支撑。

#### 7. 优点
*   **免训练且通用**：无需昂贵的重训练过程，可直接应用于现有的各种 DiT 架构。
*   **层级感知**：通过离线分析避开了“一刀切”的剪枝策略，保护了敏感层，挖掘了冗余层的潜力。
*   **耦合优化**：双向协同聚类解决了 Q-K 错位问题，显著提升了块选择的准确性（Attention Recall 提升约 2.5%-4.5%）。

#### 8. 不足与局限
*   **聚类开销**：尽管有重用机制，但在线聚类仍会带来一定的额外计算负担，在极小模型或极短序列上加速效果可能受限。
*   **超参数依赖**：稀疏度表的生成依赖于离线分析的校准输入，虽然实验证明其具有稳定性，但在极端分布的输入下是否存在鲁棒性问题仍需进一步验证。
*   **硬件依赖**：高性能的实现依赖于 Triton 和特定的 GPU 内核优化，在非 NVIDIA 硬件上的移植性可能存在挑战。

（完）
