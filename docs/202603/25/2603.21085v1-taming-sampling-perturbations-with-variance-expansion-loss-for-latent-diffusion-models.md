---
title: Taming Sampling Perturbations with Variance Expansion Loss for Latent Diffusion Models
title_zh: 利用方差膨胀损失缓解潜在扩散模型中的采样扰动
authors: "Qifan Li, Xingyu Zhou, Jinhua Zhang, Weiyi You, Shuhang Gu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21085v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 潜在扩散模型中采样扰动的鲁棒性
tldr: 本文针对潜扩散模型（LDM）中潜空间过于紧凑导致对采样扰动敏感的问题，提出了一种方差膨胀损失（Variance Expansion loss）。该方法通过在VAE训练中引入方差膨胀机制，有效对抗了方差塌陷，增强了潜空间对扩散采样过程中随机扰动的鲁棒性。实验证明，该方法在保持高重构精度的同时，显著提升了多种LDM架构的图像生成质量和稳定性。
selection_source: fresh_fetch
motivation: 发现常用的β-VAE分词器生成的潜空间过于紧凑，导致模型对扩散采样中的随机扰动极度敏感，从而引发视觉质量退化。
method: 提出方差膨胀损失，利用重构精度与方差膨胀之间的对抗博弈，构建一个既能保持高保真重构又对采样扰动具有鲁棒性的潜空间。
result: 广泛的实验表明，该方法在多种潜扩散架构上均能一致地提高生成质量，有效缓解了采样过程中的不稳定性。
conclusion: 潜空间的鲁棒性是实现稳定且高质量扩散采样的关键要素，通过方差膨胀损失可以有效弥补这一现有模型的缺陷。
---

## 摘要
潜在扩散模型因其在紧凑潜在空间中学习扩散过程的能力，已成为高保真、高效图像生成的主流框架。然而，尽管以往的研究主要集中在潜在空间的重建精度和语义对齐上，我们观察到另一个关键因素——对采样扰动的鲁棒性，在决定生成质量方面也起着至关重要的作用。通过实证和理论分析，我们表明潜在扩散模型中常用的基于 $eta$-VAE 的分词器往往会产生过于紧凑的潜在流形，这些流形对扩散采样过程中的随机扰动高度敏感，从而导致视觉质量下降。为了解决这一问题，我们提出了一种简单而有效的解决方案，在保持强大重建保真度的同时，构建一个对采样扰动具有鲁棒性的潜在空间。这是通过引入方差膨胀损失来实现的，该损失可以抵消方差崩塌，并利用重建与方差膨胀之间的对抗性相互作用来实现自适应平衡，在保持重建精度的同时提高对随机采样的鲁棒性。广泛的实验表明，我们的方法在不同的潜在扩散架构中一致地提升了生成质量，证实了潜在空间的鲁棒性是实现稳定且忠实扩散采样的关键缺失要素。

## Abstract
Latent diffusion models have emerged as the dominant framework for high-fidelity and efficient image generation, owing to their ability to learn diffusion processes in compact latent spaces. However, while previous research has focused primarily on reconstruction accuracy and semantic alignment of the latent space, we observe that another critical factor, robustness to sampling perturbations, also plays a crucial role in determining generation quality. Through empirical and theoretical analyses, we show that the commonly used $β$-VAE-based tokenizers in latent diffusion models, tend to produce overly compact latent manifolds that are highly sensitive to stochastic perturbations during diffusion sampling, leading to visual degradation. To address this issue, we propose a simple yet effective solution that constructs a latent space robust to sampling perturbations while maintaining strong reconstruction fidelity. This is achieved by introducing a Variance Expansion loss that counteracts variance collapse and leverages the adversarial interplay between reconstruction and variance expansion to achieve an adaptive balance that preserves reconstruction accuracy while improving robustness to stochastic sampling. Extensive experiments demonstrate that our approach consistently enhances generation quality across different latent diffusion architectures, confirming that robustness in latent space is a key missing ingredient for stable and faithful diffusion sampling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Taming Sampling Perturbations with Variance Expansion Loss for Latent Diffusion Models》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在潜在扩散模型（LDM）中，分词器（Tokenizer，通常是 VAE）的重构精度高并不直接等同于生成质量好。
*   **研究动机**：作者发现常用的 $\beta$-VAE 分词器为了追求极高的重构精度，往往会导致潜在空间（Latent Space）的流形过于“紧凑”或“塌陷”（Variance Collapse）。在扩散模型的采样过程中，微小的随机扰动（Sampling Perturbations）极易将潜在变量推离这个狭窄的流形，导致解码失败或视觉伪影。
*   **整体含义**：论文强调了潜在空间对采样扰动的**鲁棒性**是决定生成质量的关键因素，而不仅仅是重构精度或语义对齐。

### 2. 论文提出的方法论
*   **核心思想**：舍弃传统的 KL 散度正则项，引入**方差膨胀损失（Variance Expansion Loss, VE Loss）**，通过主动扩大潜在分布的方差 $\sigma^2$ 来构建一个更宽阔、更具鲁棒性的潜在流形。
*   **关键技术细节**：
    *   **方差塌陷分析**：理论证明重构损失 $L_{rec}$ 会诱导解码器雅可比矩阵的敏感性，迫使编码器输出的方差 $\sigma$ 趋向于零。
    *   **VE Loss 公式**：提出反比例形式的损失函数 $L_{var}(\sigma) = \frac{1}{\sigma^2 + \delta}$。当方差过小时，该项会产生巨大的梯度推动方差增大。
    *   **正则化项**：为了防止潜在变量数值爆炸，引入了经验正则项 $L_{reg} = e^{|z| - \tau}$。
    *   **自适应平衡**：总损失 $L = L_{rec} + \lambda_1 L_{var} + \lambda_2 L_{reg}$。利用重构损失（趋向压缩）与 VE 损失（趋向膨胀）之间的对抗博弈，使模型根据解码器的局部敏感度自适应地调整方差大小。
*   **算法流程**：在训练 VAE 时，不再强制潜在分布对齐标准高斯分布（KL=0），而是通过 VE Loss 确保每个潜在点都有足够的“厚度”来吸收采样噪声。

### 3. 实验设计
*   **数据集**：主要在 **ImageNet 256×256** 上进行验证。
*   **Benchmark 与对比方法**：
    *   **分词器基准**：LDM (Vanilla VAE)、VA-VAE。
    *   **生成模型基准**：DiT (Diffusion Transformer)、SiT、LightningDiT。
    *   **对比指标**：重构指标（rFID, PSNR, LPIPS, SSIM）；生成指标（FID-50K/10K, Inception Score, Precision, Recall）。
    *   **消融实验**：对比了不同 $\beta$ 值的 KL 散度正则化效果，证明单纯调整 KL 权重无法解决鲁棒性问题。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **分词器训练**：使用 8 台 **NVIDIA RTX 4090** GPU，Global Batch Size 为 64。
    *   **消融实验（生成模型）**：使用 8 台 **NVIDIA RTX 4090**，Batch Size 为 1024。
    *   **SOTA 模型训练**：使用 4 台 **NVIDIA RTX Pro 6000** GPU，Batch Size 为 768。
*   **训练时长**：分词器训练约 16 个 Epoch；生成模型训练最高达 530 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   包含了 2D 玩具数据集（Toy Example）的理论验证，直观展示了流形塌陷现象。
    *   在 ImageNet 上进行了系统级对比，涵盖了从 Base 到 XL 规模的多种模型架构。
    *   进行了详尽的消融实验，测试了不同损失权重和正则化策略。
*   **充分性与公平性**：实验设计较为充分，通过在相同的训练时长和架构下对比 VE Loss 与 KL Loss，客观地证明了方法的有效性。特别是引入了 Muon 优化器来解决长程训练的不稳定性，增强了实验结果的可信度。

### 6. 主要结论与发现
*   **鲁棒性至关重要**：潜在空间对采样扰动的抵抗力是高质量生成的“缺失环节”。
*   **KL 散度并非必须**：在 LDM 中，扩散过程本身会学习潜在空间的先验，因此 VAE 阶段强制高斯先验（KL 项）不仅不必要，反而会损害重构精度和鲁棒性。
*   **VE Loss 的优越性**：相比于固定噪声或强 KL 正则，VE Loss 提供的自适应方差能更好地平衡重构保真度与生成稳定性。

### 7. 优点
*   **理论与直觉结合**：通过 Taylor 展开分析了重构损失导致方差塌陷的根本原因，逻辑严密。
*   **简单且通用**：VE Loss 易于实现，且可以无缝集成到现有的各种 LDM 架构中，无需修改扩散模型本身的结构。
*   **性能提升显著**：在 ImageNet 256×256 上达到了 1.18 的 FID，超越了包括 RAE、VA-VAE 在内的多种 SOTA 方法。

### 8. 不足与局限
*   **重构权衡**：虽然论文声称保持了重构精度，但在严格对齐的实验中，增加方差膨胀仍会导致重构指标（如 PSNR）出现轻微下降。
*   **超参数敏感性**：$\lambda_1$ 和 $\lambda_2$ 的选择可能需要针对不同的数据集或压缩率进行微调。
*   **场景覆盖**：实验主要集中在 ImageNet 图像生成上，尚未在视频生成或更大规模的文本到图像（T2I）任务中验证其泛化能力。

（完）
