---
title: "ForestPrune: High-ratio Visual Token Compression for Video Multimodal Large Language Models via Spatial-Temporal Forest Modeling"
title_zh: ForestPrune：通过时空森林建模实现视频多模态大语言模型的高比例视觉 Token 压缩
authors: "Shaobo Ju, Baiyang Song, Tao Chen, Jiapeng Zhang, Qiong Wu, Chao Chang, HuaiXi Wang, Yiyi Zhou, Rongrong Ji"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22911v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 视频模型的视觉令牌压缩与剪枝
tldr: "针对视频多模态大模型在处理长视频时计算开销巨大的挑战，本文提出了一种无需训练的Token剪枝方法ForestPrune。该方法通过构建跨帧的时空森林模型，利用语义、空间和时间约束对视频Token进行全局建模，并依据树深度和节点角色评估重要性，实现了极高比例的压缩。实验证明，该方法在减少90% Token的情况下仍能保持95.8%的精度，且在效率和性能上显著优于现有方法。"
selection_source: fresh_fetch
motivation: 现有Token压缩方法在视频任务中难以实现高比例压缩，主要原因是缺乏对视频时空连续性和动态内容的充分建模。
method: 提出ForestPrune方法，通过构建时空Token森林并基于树深度和节点角色进行全局重要性评估，实现高效的Token剪枝。
result: "在LLaVA-OneVision等模型上，该方法在减少90% Token的同时保留了95.8%的平均精度，且剪枝速度比FrameFusion快81.4%。"
conclusion: ForestPrune为视频MLLM提供了一种高效、高压缩比且无需重新训练的Token管理方案，显著提升了长视频理解的推理效率。
---

## 摘要
由于在节省计算和内存开销方面的巨大优势，Token 压缩已成为多模态大语言模型（MLLMs）的研究热点，并在图像-语言任务中取得了显著进展。然而，对于视频而言，现有方法在实现高比例 Token 压缩方面仍力有不足。我们将这一缺陷归因于对视频内容的时间维度和连续性建模不足，并为视频 MLLMs 提出了一种名为 ForestPrune 的新型免训练 Token 剪枝方法，该方法通过时空森林建模实现了高效且高比例的剪枝。在实践中，ForestPrune 根据语义、空间和时间约束跨视频帧构建 Token 森林，从而实现对视频的全面理解。随后，ForestPrune 根据树的深度和节点角色评估 Token 树及节点的重要性，从而获得全局最优的剪枝决策。为了验证 ForestPrune，我们将其应用于两个代表性的视频 MLLMs（即 LLaVA-Video 和 LLaVA-OneVision），并在多个视频基准测试上进行了广泛实验。实验结果不仅展示了其在视频 MLLMs 上的巨大效能（例如，在 LLaVA-OneVision 上减少 90% Token 的同时保留了 95.8% 的平均准确率），还证明了其相比于其他 Token 压缩方法具有更优越的性能和效率（例如，在 MLVU 上准确率提升 10.1%，且在 LLaVA-Video 上比 FrameFusion 减少了 81.4% 的剪枝时间）。

## Abstract
Due to the great saving of computation and memory overhead, token compression has become a research hot-spot for MLLMs and achieved remarkable progress in image-language tasks. However, for the video, existing methods still fall short of high-ratio token compression. We attribute this shortcoming to the insufficient modeling of temporal and continual video content, and propose a novel and training-free token pruning method for video MLLMs, termed ForestPrune, which achieves effective and high-ratio pruning via Spatial-temporal Forest Modeling. In practice, ForestPrune construct token forests across video frames based on the semantic, spatial and temporal constraints, making an overall comprehension of videos. Afterwards, ForestPrune evaluates the importance of token trees and nodes based on tree depth and node roles, thereby obtaining a globally optimal pruning decision. To validate ForestPrune, we apply it to two representative video MLLMs, namely LLaVA-Video and LLaVA-OneVision, and conduct extensive experiments on a bunch of video benchmarks. The experimental results not only show the great effectiveness for video MLLMs, e.g., retaining 95.8% average accuracy while reducing 90% tokens for LLaVA-OneVision, but also show its superior performance and efficiency than the compared token compression methods, e.g., +10.1% accuracy on MLVU and -81.4% pruning time than FrameFusion on LLaVA-Video.

---

## 论文详细总结（自动生成）

以下是对论文《ForestPrune: High-ratio Visual Token Compression for Video Multimodal Large Language Models via Spatial-Temporal Forest Modeling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视频多模态大语言模型（Video MLLMs）在处理长视频时，由于输入帧数多，视觉 Token 数量呈爆炸式增长，导致计算开销（FLOPs）和内存占用巨大。
*   **研究动机**：现有的 Token 压缩方法（如剪枝或合并）多源于图像任务，在处理视频时往往只关注单帧内的重要性，缺乏对视频时空连续性的全局建模。这导致在高压缩比（如 90% 以上）下，模型性能会剧烈下降，且保留的 Token 在相邻帧间存在大量冗余。
*   **整体含义**：本文提出了一种名为 **ForestPrune** 的免训练剪枝方法，通过构建“时空森林”来识别并保留跨帧的最具代表性信息，旨在实现极高比例压缩下的性能保持。

### 2. 方法论：核心思想与技术细节
ForestPrune 的核心思想是将视频 Token 建模为跨越时间和空间的森林结构，通过树的深度和节点角色来评估 Token 的全局重要性。
*   **候选节点选择**：首先从每帧中提取代表性 Token 作为潜在的树节点（可采用随机采样或现有的轻量级剪枝法）。
*   **时空森林构建**：
    *   **约束条件**：基于**语义相似度**（余弦相似度 $\tau_s$）、**空间距离**（坐标欧氏距离 $\tau_p$）和**时间顺序**（前帧指向后帧）建立节点间的连接。
    *   **树生成**：计算入度为 0 的节点作为根节点（Root），将相似且位置接近的后续帧 Token 挂载为子节点。
*   **全局剪枝策略**：
    *   **重要性排序**：树的深度反映了该视觉特征在视频中持续的时间和稳定性。深度越深、越靠近根部（Root）或主干（Trunk）的节点越重要。
    *   **迭代剪枝**：根据预设的压缩预算，优先剪掉森林中的叶子节点（Leaf）和尾部节点（Tail），直到达到目标 Token 数量。
*   **算法优势**：该方法是**免训练（Training-free）**的，可直接插入现有的推理流程。

### 3. 实验设计
*   **基础模型**：LLaVA-Video-7B 和 LLaVA-OneVision-7B。
*   **数据集/基准测试**：涵盖了 5 个极具竞争力的视频理解基准：
    *   **NExT-QA**（时间动作解释）、**MVBench**（多能力短视频理解）、**VideoMME**（全方位评估）、**MLVU**（长视频理解）、**LongVideoBench**（长上下文理解）。
*   **对比方法**：
    *   图像类：FastV, VisionZip, G-Prune。
    *   视频类：STTM, FrameFusion。
*   **压缩比例**：重点测试了 70%、80% 和 90% 的高压缩率场景。

### 4. 资源与算力
*   **算力消耗**：论文对比了不同方法的 TFLOPS、GPU 显存占用和预填充（Pre-filling）时间。
*   **具体表现**：在 90% 压缩率下，ForestPrune 显著降低了显存峰值（从约 35G 降至 23G 左右），计算量从 80.9 TFLOPS 降至 6.5 TFLOPS。
*   **训练时长**：由于是**免训练方法**，不涉及训练时长。剪枝过程本身的计算开销极低，比 FrameFusion 快 81.4%。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个主流模型和 5 个不同维度的基准上进行了测试，实验覆盖面广。
*   **消融实验**：针对候选节点选择方式（随机 vs. 剪枝）、节点保留比例、语义/空间阈值（$\tau_s, \tau_p$）进行了详细的消融研究。
*   **客观性**：通过可视化分析（展示了时空树的构建过程和不同方法保留 Token 的差异）增强了结果的可信度。实验结果显示，在 90% 压缩率下，ForestPrune 的平均准确率保留率高达 95.8%，显著优于同类方法。

### 6. 主要结论与发现
*   **高压缩比下的鲁棒性**：ForestPrune 在极高压缩比下表现远超现有方法，证明了时空建模对减少视频冗余的关键作用。
*   **性能提升潜力**：通过 ForestPrune 压缩 Token，可以在保持相同计算预算的前提下，将输入帧数从 64 帧扩展到 512 帧，从而在长视频任务（如 MLVU）上达到 SOTA 性能（72.5 分）。
*   **效率优势**：该方法在推理速度和显存节省上具有明显优势，适合部署在资源受限的设备上。

### 7. 优点
*   **全局视野**：不同于逐帧处理的方法，ForestPrune 能够识别跨帧的重复信息，实现真正的全局最优剪枝。
*   **即插即用**：无需重新训练模型，兼容性强。
*   **时空结合**：巧妙地利用了视频的空间局部性和时间连续性先验。
*   **可解释性**：通过森林结构直观地展示了视频内容的演变和 Token 的重要性分布。

### 8. 不足与局限
*   **超参数依赖**：虽然消融实验显示其对阈值有一定鲁棒性，但在极端动态或场景切换极快的视频中，可能需要精细调整语义和空间阈值。
*   **精细特征丢失风险**：在极高压缩比下，虽然能保留主干信息，但可能会丢失某些微小的、仅出现一次的关键视觉细节（如快速闪过的文字）。
*   **应用限制**：目前主要针对基于 Transformer 架构的 MLLMs，对于其他架构的适用性尚未探讨。

（完）
