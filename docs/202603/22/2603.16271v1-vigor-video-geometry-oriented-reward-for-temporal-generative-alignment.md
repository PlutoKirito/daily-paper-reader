---
title: "VIGOR: VIdeo Geometry-Oriented Reward for Temporal Generative Alignment"
title_zh: VIGOR：面向视频几何的奖励模型，用于时序生成对齐
authors: "Tengjiao Yin, Jinglei Shi, Heng Guo, Xi Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16271v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 视频扩散模型与基于几何奖励的时间生成对齐
tldr: 针对视频扩散模型缺乏几何监督导致的变形和漂移问题，本文提出VIGOR，一种基于几何的奖励模型。该模型利用预训练几何基础模型，通过跨帧重投影误差评估多视图一致性，并引入几何感知采样策略以增强鲁棒性。通过后训练对齐和推理侧优化两种路径，VIGOR显著提升了视频生成的几何一致性，为增强开源模型提供了高效且实用的方案。
selection_source: fresh_fetch
motivation: 视频扩散模型在训练中缺乏显式几何监督，导致生成视频常出现物体变形、空间漂移和深度冲突等不一致现象。
method: 提出一种基于点对点跨帧重投影误差的几何奖励模型，并结合几何感知采样策略，通过后训练对齐和推理时路径验证来优化视频生成。
result: 实验证明该几何奖励模型比现有指标更具鲁棒性，能有效减少生成视频中的几何伪影并提升视觉质量。
conclusion: VIGOR为提升视频生成模型的几何一致性提供了一种无需大规模重训的实用且高效的对齐方案。
---

## 摘要
视频扩散模型在训练过程中缺乏显式的几何监督，导致生成的视频中出现物体变形、空间漂移和深度违背等不一致伪影。为了解决这一局限性，我们提出了一种基于几何的奖励模型，该模型利用预训练的几何基础模型，通过跨帧重投影误差来评估多视图一致性。与以往在像素空间测量不一致性的几何指标不同（像素强度可能会引入额外的噪声），我们的方法以逐点方式进行误差计算，从而产生更具物理依据且更鲁棒的误差度量。此外，我们引入了一种几何感知采样策略，过滤掉低纹理和非语义区域，将评估重点放在具有可靠对应关系的几何意义区域，以提高鲁棒性。我们将该奖励模型应用于视频扩散模型的对齐，通过两条互补路径：通过有监督微调（SFT）或强化学习对双向模型进行后期训练，以及通过将我们的奖励作为路径验证器进行测试时扩展，对因果视频模型（如流式视频生成器）进行推理时优化。实验结果验证了我们设计的有效性，表明与其它变体相比，我们基于几何的奖励提供了卓越的鲁棒性。通过实现高效的推理时扩展，我们的方法为增强开源视频模型提供了一种实用的解决方案，而无需大量的重新训练计算资源。

## Abstract
Video diffusion models lack explicit geometric supervision during training, leading to inconsistency artifacts such as object deformation, spatial drift, and depth violations in generated videos. To address this limitation, we propose a geometry-based reward model that leverages pretrained geometric foundation models to evaluate multi-view consistency through cross-frame reprojection error. Unlike previous geometric metrics that measure inconsistency in pixel space, where pixel intensity may introduce additional noise, our approach conducts error computation in a pointwise fashion, yielding a more physically grounded and robust error metric. Furthermore, we introduce a geometry-aware sampling strategy that filters out low-texture and non-semantic regions, focusing evaluation on geometrically meaningful areas with reliable correspondences to improve robustness. We apply this reward model to align video diffusion models through two complementary pathways: post-training of a bidirectional model via SFT or Reinforcement Learning and inference-time optimization of a Causal Video Model (e.g., Streaming video generator) via test-time scaling with our reward as a path verifier. Experimental results validate the effectiveness of our design, demonstrating that our geometry-based reward provides superior robustness compared to other variants. By enabling efficient inference-time scaling, our method offers a practical solution for enhancing open-source video models without requiring extensive computational resources for retraining.

---

## 论文详细总结（自动生成）

### 论文总结：VIGOR —— 面向视频几何的奖励模型，用于时序生成对齐

#### 1. 核心问题与整体含义（研究动机和背景）
当前的视频扩散模型（VDM）虽然在视觉逼真度上取得了巨大进步，但普遍面临一个核心缺陷：**在训练过程中缺乏显式的几何监督**。这导致生成的视频经常出现物体变形、空间漂移、闪烁、深度冲突以及物理上不合理的透视变化。虽然闭源大模型通过海量数据学习到了部分隐式几何先验，但对于开源模型或特定下游应用，获取大规模带几何标注（如深度图、相机位姿）的数据极具挑战。因此，本文旨在开发一种无需大规模重训、能够引导模型生成符合几何规律视频的奖励模型及对齐框架。

#### 2. 方法论：核心思想与关键技术
本文提出了 **VIGOR** 框架，其核心思想是利用预训练的几何基础模型（如 VGGT）来评估视频的 3D 一致性，并以此作为奖励信号。

*   **几何感知采样 (Geometry-Aware Sampling, GAS)：**
    利用 VGGT 的全局注意力机制识别几何关键区域（如具有纹理的背景），过滤掉无意义区域（如天空、平坦地面）。通过提取浅层注意力图并进行补丁筛选，确保评估集中在具有可靠对应关系的区域。
*   **逐点重投影误差 (Pointwise Reprojection Error)：**
    不同于以往在像素空间计算差异（易受光照、噪声干扰）的方法，VIGOR 采用物理意义更明确的逐点计算：
    1.  **反投影：** 将参考帧的 2D 像素点结合深度和相机参数还原为 3D 世界坐标。
    2.  **重投影：** 将 3D 点投影到目标帧，得到预测位置。
    3.  **误差计算：** 计算预测位置与跟踪器实际跟踪位置之间的 $L_2$ 距离。
*   **对齐路径：**
    *   **后期训练对齐 (Post-hoc Alignment)：** 构建 **GB3DV-25k** 偏好数据集（包含 25,600 对视频），通过有监督微调（SFT）和直接偏好优化（DPO）调整双向扩散模型。
    *   **测试时扩展 (Test-Time Scaling, TTS)：** 针对因果自回归模型（流式生成），将奖励模型作为路径验证器，提出三种搜索策略：**起始搜索 (SoS)**、**路径搜索 (SoP)** 和 **束搜索 (Beam Search)**，在推理阶段动态筛选高质量生成路径。

#### 3. 实验设计
*   **数据集与场景：** 使用 RealEstate10k（室内）和 GLDv2（室外）作为提示词来源，利用 Qwen3-VL 生成详细描述，涵盖多种相机运动和场景。
*   **基准模型：** 包括双向模型 CausVid、Wan2.1-T2V-1.3B，以及流式模型 Causal-Forcing。
*   **对比方法：** 
    *   **Epipolar：** 基于对极几何约束（Sampson 距离）的方法。
    *   **Reproj-Pix：** 基于像素级变形差异的方法（如 VideoGPA）。
*   **评估指标：** 
    *   **几何一致性：** 3D 重建质量（PSNR, SSIM, LPIPS）和多视图一致性得分（EPI, RPX, RPT）。
    *   **视频质量：** VBench 综合评分（涵盖主体一致性、背景一致性、运动平滑度等）。

#### 4. 资源与算力
*   论文提到该方法是一种“实用且高效”的方案，旨在减少对大规模重训算力的依赖。
*   **微调细节：** 使用了 LoRA（秩 $r=64$，$\alpha=128$）进行参数高效微调，应用于 DiT 结构的 $q, k, v, o$ 投影模块。
*   **具体算力：** 文中**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长，但强调了其在开源模型上的可移植性和低资源消耗特性。

#### 5. 实验数量与充分性
*   **实验规模：** 构建了包含 25,600 对视频的大型偏好数据集，涵盖了 2,560 个多样化的提示词。
*   **消融与对比：** 针对双向模型和流式模型分别进行了实验，并对 TTS 的搜索预算（Budget 1 到 16）进行了扩展性研究。
*   **充分性评价：** 实验设计较为全面，不仅对比了不同的奖励函数（对极几何 vs 像素重投影 vs 逐点重投影），还验证了不同对齐策略（SFT, DPO, TTS）的效果。通过 VBench 的多维度评分，证明了提升几何一致性的同时并未损害视频的审美和成像质量，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **逐点误差更优：** 相比于像素级误差，逐点重投影误差对噪声更鲁棒，能更准确地捕捉 3D 不一致性。
*   **TTS 的有效性：** 在流式视频模型中，通过推理侧的搜索（尤其是 Beam Search）可以显著提升几何稳定性，且呈现出明显的 Scaling 效应（预算越高，效果越好）。
*   **对齐增益：** DPO 结合 VIGOR 奖励在各项几何指标上均优于 SFT 和基准模型，证明了偏好对齐在视频几何修正中的潜力。

#### 7. 优点
*   **物理 grounded：** 奖励模型基于严谨的 3D 几何重投影，比纯数据驱动的黑盒奖励模型更具解释性。
*   **采样策略聪明：** 引入几何感知采样（GAS），有效解决了低纹理区域（如天空）对几何评估的干扰。
*   **适用性广：** 方案同时覆盖了双向和自回归两类主流视频生成架构，特别是为流式视频生成提供了推理侧优化的新思路。

#### 8. 不足与局限
*   **依赖基础模型：** VIGOR 的效果高度依赖于预训练几何模型（VGGT）和跟踪器的准确性，如果基础模型在某些极端场景下失效，奖励信号将不再可靠。
*   **推理延迟：** TTS 策略虽然避免了重训，但多次采样和搜索显著增加了推理时的计算开销和等待时间。
*   **动态物体挑战：** 尽管 GAS 试图聚焦背景，但在处理包含大量复杂形变动态物体的视频时，几何一致性的定义和测量依然存在模糊地带。

（完）
