---
title: Question-guided Visual Compression with Memory Feedback for Long-Term Video Understanding
title_zh: 基于记忆反馈的问题引导视觉压缩，用于长视频理解
authors: "Sosuke Yamao, Natsuki Miyahara, Yuankai Qi, Shun Takeuchi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15167v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于长期视频理解的带记忆反馈的问题引导视觉压缩
tldr: 针对长视频理解中独立压缩帧导致缺乏全局事件理解的问题，本文提出QViC-MF框架。该框架打破了从感知到记忆的单向模式，引入记忆反馈机制，通过问题引导的多模态选择性注意力（QMSA）提取与问题相关的当前及历史视觉信息。实验证明，该方法在MLVU、LVBench等多个长视频基准测试中显著超越了现有SOTA模型，有效提升了复杂时序任务的性能。
selection_source: fresh_fetch
motivation: 传统的长视频理解方法通常独立压缩每一帧，难以捕捉完整的事件逻辑和时序关系。
method: 提出QViC-MF框架，利用问题引导的多模态选择性注意力（QMSA）结合记忆反馈，迭代地从当前片段和历史记忆中提取关键信息。
result: "在MLVU、LVBench、VNBench Long和VideoMME Long等测试集上分别实现了6.1%至18.3%的显著性能提升。"
conclusion: 通过建立记忆反馈驱动的感知过程，QViC-MF有效增强了模型对长视频中复杂事件和时序任务的理解能力。
---

## 摘要
在利用大型多模态模型进行长视频理解的背景下，研究者们已经提出了许多框架。尽管基于 Transformer 的视觉压缩器和记忆增强方法常被用于处理长视频，但它们通常独立地压缩每一帧，因此在需要理解完整事件的任务（如 MLVU 和 VNBench 中的时间排序任务）上表现不佳。这促使我们重新思考从感知到记忆的传统单向方案，转而建立一种反馈驱动的过程，使存储在上下文记忆中的过去视觉上下文能够辅助当前的感知。为此，我们提出了一种用于长视频理解的框架——基于记忆反馈的问题引导视觉压缩（QViC-MF）。其核心是问题引导的多模态选择性注意力（QMSA），它学习从当前视频片段以及记忆中过去的相关帧中保留与给定问题相关的视觉信息。压缩器和记忆反馈对整个视频的每个片段进行迭代工作。这种简单而有效的设计在长视频理解任务上带来了巨大的性能提升。大量实验表明，我们的方法相比当前最先进的方法取得了显著改进，在 MLVU 测试集上提升了 6.1%，在 LVBench 上提升了 8.3%，在 VNBench Long 上提升了 18.3%，在 VideoMME Long 上提升了 3.7%。代码将公开。

## Abstract
In the context of long-term video understanding with large multimodal models, many frameworks have been proposed. Although transformer-based visual compressors and memory-augmented approaches are often used to process long videos, they usually compress each frame independently and therefore fail to achieve strong performance on tasks that require understanding complete events, such as temporal ordering tasks in MLVU and VNBench. This motivates us to rethink the conventional one-way scheme from perception to memory, and instead establish a feedbackdriven process in which past visual contexts stored in the context memory can benefit ongoing perception. To this end, we propose Question-guided Visual Compression with Memory Feedback (QViC-MF), a framework for long-term video understanding. At its core is a Question-guided Multimodal Selective Attention (QMSA), which learns to preserve visual information related to the given question from both the current clip and the past related frames from the memory. The compressor and memory feedback work iteratively for each clip of the entire video. This simple yet effective design yields large performance gains on longterm video understanding tasks. Extensive experiments show that our method achieves significant improvement over current state-of-the-art methods by 6.1% on MLVU test, 8.3% on LVBench, 18.3% on VNBench Long, and 3.7% on VideoMME Long. The code will be released publicly.

---

## 论文详细总结（自动生成）

以下是对论文《Question-guided Visual Compression with Memory Feedback for Long-Term Video Understanding》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：长视频理解面临计算成本高和上下文窗口有限的挑战。现有方法（如视觉压缩或记忆增强）通常采用“感知到记忆”的单向模式，即独立压缩每一帧并存入记忆。
*   **核心问题**：这种单向独立压缩忽略了帧间的时序逻辑，且感知过程无法根据已有的记忆或具体问题进行动态调整，导致在需要理解完整事件（如动作排序、长程依赖）的任务中表现不佳。
*   **整体含义**：本文提出了 **QViC-MF** 框架，旨在建立一种“反馈驱动”的感知过程，让过去的视觉上下文（记忆）和当前的问题共同引导当前的视觉压缩，从而保持时序事件的完整性。

### 2. 方法论
*   **核心思想**：通过迭代的“片段处理-记忆更新-反馈检索”流程，使模型在处理当前视频片段时，能够参考从记忆中检索出的历史相关帧。
*   **关键技术细节**：
    *   **QMSA（问题引导的多模态选择性注意力）**：这是视觉压缩器的核心，通过修改自注意力机制的 Logits 来实现：
        1.  **掩码（Masking, M）**：确保每个上下文 Token 仅关注其对应帧的视觉信息，维持帧级表示的独立性。
        2.  **阻断（Blocking, B）**：阻止上下文 Token 关注文本 Token，防止文本信息泄露导致“压缩幻觉”（即生成的视觉特征包含视频中不存在的文本语义）。
        3.  **引导（Guiding, G）**：将“文本到视觉”的注意力权重广播到“上下文到视觉”的注意力中，使压缩过程具备问题感知能力。
    *   **记忆反馈机制**：计算每帧与问题的相关性得分（基于注意力权重），在处理下一片段时，从记忆库中提取 Top-Kr 个最相关的历史帧与当前片段共同输入压缩器。
    *   **解码阶段**：将压缩后的轻量化上下文 Token（每帧仅 16 个）输入冻结的 LLM（如 Qwen2-7B）生成答案。

### 3. 实验设计
*   **数据集/场景**：
    *   **MLVU**：涵盖 3 分钟到 2 小时的视频，测试多任务长视频理解。
    *   **LVBench**：极端长视频基准（最高 2 小时）。
    *   **VideoMME**：多领域视频理解（11 秒到 1 小时）。
    *   **VNBench**：针对“大海捞针”（NIAH）场景的合成评估。
*   **对比方法**：包括闭源模型（GPT-4o, Gemini 1.5 Pro）和开源 SOTA 模型（LLaVA-Video, LongVA, Flash-VStream, Video-XL, MovieChat 等）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：基于 LLaVA-Video-7B-Qwen2 进行 LoRA 微调。训练数据采用了 LLaVA-Video-178K 数据集的 5% 子集（约 8.3 万样本）。
*   **推理效率**：在 H200 上构建上下文记忆的速度为 **28 FPS**。显存占用约为 30.7 GiB（若采用参数共享可优化至 16.4 GiB）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流长视频基准上进行了全面测试，并针对 MLVU 的 7 个子任务进行了详细对比。
*   **消融实验**：非常充分。作者分别验证了记忆反馈、QMSA 的三个组件（M, B, G）、采样率（1fps vs 2fps）、压缩率（Token 数量）、记忆容量等对性能的影响。
*   **客观性**：通过在相同 Token 预算下对比 SOTA 模型，并使用官方评估脚本，确保了实验的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：QViC-MF 在所有基准上均刷新了开源模型记录。在 VNBench Long 上提升了 **18.3%**，在 MLVU 测试集上提升了 **6.1%**。
*   **反馈的重要性**：消融实验证明，单纯增加记忆而不引入反馈（Feedback）对性能提升有限，反馈机制是捕捉时序逻辑的关键。
*   **高效压缩**：即使每帧仅压缩为 16 个 Token（远少于 LLaVA-Video 的 169 个），模型依然保持了极高的准确率，证明了 QMSA 的高效性。

### 7. 优点
*   **创新性反馈设计**：打破了传统的单向感知模式，实现了感知与记忆的闭环。
*   **极高的 Token 效率**：在极低的 Token 预算下实现了超越高预算模型的性能，极具实用价值。
*   **可解释性**：通过相关性得分可视化，可以清晰看到模型在关注哪些关键帧。

### 8. 不足与局限
*   **计数任务瓶颈**：在 MLVU 的 Action Count 和 VNBench 的计数任务上表现提升不明显。案例分析显示，模型能找准关键帧，但 LLM 后端在最终推理计数时仍会出错。
*   **全局总结能力**：对于需要全局概括（如 Summarization）的任务，反馈机制带来的增益小于对稀疏事件检索（如 Needle QA）的增益。
*   **推理架构**：目前视觉压缩器和解码器是独立实例，虽然可以参数共享，但当前实现方式的显存开销仍有优化空间。

（完）
