---
title: A Mamba-based Perceptual Loss Function for Learning-based UGC Transcoding
title_zh: 一种用于基于学习的 UGC 转码的基于 Mamba 的感知损失函数
authors: "Zihao Qi, Chen Feng, Fan Zhang, Xiaozhong Xu, Shan Liu, David Bull"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25566v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 视频转码的感知损失函数
tldr: "针对用户生成内容（UGC）转码中源视频质量不佳导致传统保真度优化效果受限的问题，本文提出一种基于Mamba的感知损失函数。该方法将参考视频视为上下文引导而非绝对像素基准，利用弱监督孪生排序策略训练轻量化Mamba质量模型，并将其集成至神经视频编解码器的率失真优化中。实验表明，该方法在DCVC和HiNeRV上分别实现了8.46%和12.89%的BD-rate节省，显著提升了感知质量。"
selection_source: fresh_fetch
motivation: 传统视频压缩优化保真度会导致编解码器复制源视频中的固有伪影，不适用于质量参差不齐的UGC内容。
method: 提出一种基于选择性结构化状态空间模型（Mamba）的轻量化感知损失函数，通过弱监督孪生排序策略进行训练。
result: "在DCVC和HiNeRV两种神经编解码器上，该方法分别实现了8.46%和12.89%的BD-rate性能提升。"
conclusion: 将感知引导的Mamba模型引入率失真优化过程，能有效提升UGC视频转码的视觉质量并降低码率。
---

## 摘要
在用户生成内容（UGC）转码中，源视频通常由于先前的压缩、编辑或次优的拍摄条件而遭受各种退化。因此，现有的仅针对相对于参考视频的保真度进行优化的视频压缩范式变得不再理想，因为它们迫使编解码器复制非原始源视频中固有的伪影。为了解决这一问题，我们为基于学习的 UGC 视频转码提出了一种新型的受感知启发的损失函数，它重新定义了参考视频的角色，将其从地面真值（ground-truth）像素锚点转变为信息丰富的上下文引导。具体而言，我们训练了一个基于选择性结构化状态空间模型（Mamba）的轻量级神经质量模型，并使用弱监督孪生（Siamese）排序策略进行优化。随后，该模型作为损失函数被集成到两种神经视频编解码器（DCVC 和 HiNeRV）的率失真优化（RDO）过程中，旨在生成具有更高感知质量的重建内容。我们的实验表明，该框架在基于自动编码器和基于隐式神经表示的基准模型上均实现了显著的编码增益，BD-rate 分别节省了 8.46% 和 12.89%。

## Abstract
In user-generated content (UGC) transcoding, source videos typically suffer various degradations due to prior compression, editing, or suboptimal capture conditions. Consequently, existing video compression paradigms that solely optimize for fidelity relative to the reference become suboptimal, as they force the codec to replicate the inherent artifacts of the non-pristine source. To address this, we propose a novel perceptually inspired loss function for learning-based UGC video transcoding that redefines the role of the reference video, shifting it from a ground-truth pixel anchor to an informative contextual guide. Specifically, we train a lightweight neural quality model based on a Selective Structured State-Space Model (Mamba) optimized using a weakly-supervised Siamese ranking strategy. The proposed model is then integrated into the rate-distortion optimization (RDO) process of two neural video codecs (DCVC and HiNeRV) as a loss function, aiming to generate reconstructed content with improved perceptual quality. Our experiments demonstrate that this framework achieves substantial coding gains over both autoencoder and implicit neural representation-based baselines, with 8.46% and 12.89% BD-rate savings, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于基于学习的 UGC 转码的基于 Mamba 的感知损失函数

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：用户生成内容（UGC）在互联网流量中占据主导地位。与专业内容（PGC）不同，UGC 在上传至平台前通常已在移动设备上经过了有损压缩或处理，导致平台接收到的“参考视频”本身就带有伪影。
*   **核心问题**：传统的视频压缩范式（包括神经编解码器）大多以“保真度”为导向，即最小化重建视频与参考视频之间的像素差异（如 PSNR）。在 UGC 转码场景下，这种策略会迫使编解码器去“精确复制”参考视频中的噪声和压缩伪影，导致最终视觉质量低下。
*   **研究目的**：将参考视频的角色从“像素级真值锚点”转变为“信息上下文引导”，通过感知损失函数引导编解码器偏离低质量参考，实现感知质量的提升。

#### 2. 方法论
*   **核心思想**：提出一种名为 **PT-Loss** 的感知损失函数，利用轻量化的 **Mamba（选择性结构化状态空间模型）** 架构来捕捉时空相关性，并通过弱监督学习使其具备识别感知退化的能力。
*   **关键技术细节**：
    *   **Mamba 架构**：相比传统的 Transformer，Mamba 具有线性计算复杂度 $O(L)$，能更高效地处理长程时间依赖，区分时间一致性与闪烁伪影。
    *   **特征提取与融合**：从参考块和失真块中提取多级特征，通过通道注意力机制进行融合，输出块级质量索引，最后通过可微池化层聚合为剪辑级分数。
    *   **弱监督训练策略**：采用**孪生排序（Siamese ranking）策略**。利用原始纯净源视频（S）与转码视频（D）之间的 VMAF 分数作为代理标签，训练模型在仅输入非纯净参考（R）和失真视频（D）的情况下预测感知质量。
*   **集成方式**：
    *   将 PT-Loss 集成到率失真优化（RDO）公式中：$L_{total} = (1 - \alpha)L_{fidelity} + \alpha L_{PT}$。
    *   应用于两种架构：基于自动编码器的 **DCVC** 和基于隐式神经表示（INR）的 **HiNeRV**。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用来自 BVI-DVC、CLIC 2022、YouTube-UGC 的 252 个纯净源序列，生成了超过 60 万个补丁对。
    *   **测试集**：BVI-UGC 数据集，涵盖 15 个类别，包含高中低三种质量的参考视频（QP 30, 37, 42）。
*   **Benchmark（基准）**：原始的 DCVC 和 HiNeRV 编解码器。
*   **对比方法**：除了与原始基准对比，还与基于 Swin Transformer 的质量模型（RankDVQA-UGC）进行了消融对比。
*   **评价指标**：以原始纯净源视频为基准计算 PSNR 和 VMAF 的 BD-rate。

#### 4. 资源与算力
*   **算力开销**：
    *   **PT-Loss 模型**：参数量为 0.8M，计算量为 1.4G MACs。
    *   **训练/编码影响**：对于 DCVC，由于是离线微调 P 帧模型，不增加在线推理复杂度；对于 HiNeRV，由于其过拟合特性，编码时间增加了 **21.3%**。
*   **硬件说明**：文中未明确指出具体的 GPU 型号、数量及具体训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：测试了 15 个代表性 UGC 序列，每个序列对应 3 种参考质量，共计 45 组转码实验。
*   **充分性**：实验覆盖了两种截然不同的神经编解码器架构（AE 和 INR），证明了方法的通用性。
*   **客观性**：通过消融实验验证了损失函数权重 $\alpha$ 的影响，并与现有的先进感知模型进行了复杂度与性能的对比，实验设计较为客观。

#### 6. 主要结论与发现
*   **性能提升**：在 BVI-UGC 数据集上，该方法使 DCVC 实现了 **8.46%** 的 BD-rate 节省，使 HiNeRV 实现了 **12.89%** 的节省。
*   **质量相关性**：输入参考视频的质量越低，PT-Loss 带来的增益越明显，证明了其在处理严重退化内容时的有效性。
*   **视觉效果**：定性分析显示，集成 PT-Loss 后，重建视频在文本边缘和结构细节上更加清晰，减少了伪影的残留。

#### 7. 优点
*   **架构创新**：首次将 Mamba 引入感知损失函数，利用其线性复杂度和长程建模能力，平衡了性能与计算开销。
*   **场景针对性强**：精准切中了 UGC 转码中“参考视频非纯净”的痛点，打破了盲目追求像素保真度的局限。
*   **模型无关性**：展示了良好的泛化能力，可轻易集成到不同类型的神经编解码器中。

#### 8. 不足与局限
*   **测试集规模**：虽然涵盖了 15 个类别，但 15 个视频序列的测试规模相对较小，可能无法完全代表极其多样化的 UGC 生态。
*   **代理标签依赖**：训练依赖于 VMAF 作为“真值”代理，若 VMAF 本身对某些特定 UGC 伪影不敏感，可能会限制 PT-Loss 的上限。
*   **编码延迟**：对于像 HiNeRV 这样需要在线训练的编解码器，21.3% 的额外编码耗时在实时性要求高的场景下仍是挑战。

（完）
