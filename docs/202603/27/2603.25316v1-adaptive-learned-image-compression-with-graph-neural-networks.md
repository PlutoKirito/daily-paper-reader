---
title: Adaptive Learned Image Compression with Graph Neural Networks
title_zh: 基于图神经网络的自适应学习型图像压缩
authors: "Yunuo Chen, Bing He, Zezheng Lyu, Hongwei Hu, Qunshan Gu, Yuan Tian, Guo Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25316v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 基于图神经网络的内容自适应图像压缩框架
tldr: 针对现有基于CNN和Transformer的图像压缩方法因感受野固定和连接模式静态而难以自适应捕捉空间冗余的问题，本文提出了一种基于图神经网络的自适应图像压缩框架（GLIC）。该方法通过构建双尺度图并引入动态调整邻居数量的自适应连接机制，实现了数据驱动的灵活感受野。实验表明，GLIC在多个标准数据集上均达到了SOTA性能，显著优于传统VTM-9.1标准。
selection_source: fresh_fetch
motivation: 传统的CNN和Transformer模型具有固定的感受野和静态连接，限制了其捕捉图像中随空间变化的全局和局部冗余的能力。
method: 提出GLIC框架，利用双尺度图和基于内容复杂度的动态邻居调整机制，构建灵活且内容自适应的图神经网络。
result: "在Kodak、Tecnick和CLIC数据集上，GLIC相比VTM-9.1分别实现了19.29%、21.69%和18.71%的BD-rate降低。"
conclusion: 通过引入图神经网络的灵活性，GLIC能够更有效地建模多样的冗余模式，为学习型图像压缩提供了更高效的自适应方案。
---

## 摘要
高效的图像压缩依赖于对局部和全局冗余的建模。大多数最先进的（SOTA）学习型图像压缩（LIC）方法基于 CNN 或 Transformer，它们本质上是僵化的。标准的 CNN 卷积核和基于窗口的注意力机制施加了固定的感受野和静态的连接模式，这可能仅仅因为欧几里得空间中的邻近性而将非冗余像素耦合在一起。这种僵化限制了模型自适应捕获图像中随空间变化的冗余的能力，特别是在全局层面。为了克服这些限制，我们提出了一种基于图神经网络（GNN）的内容自适应图像压缩框架。具体而言，我们的方法构建了双尺度图，从而实现了灵活的、数据驱动的感受野。此外，我们通过根据局部内容复杂度动态调整每个节点的邻居数量，引入了自适应连接性。这些创新使我们的基于图的学习型图像压缩（GLIC）模型能够有效地建模图像中多样的冗余模式，从而实现更高效、更具自适应性的压缩。实验表明，GLIC 达到了最先进的性能，在 Kodak、Tecnick 和 CLIC 数据集上相对于 VTM-9.1 分别实现了 19.29%、21.69% 和 18.71% 的 BD-rate 降低。代码将在 https://github.com/UnoC-727/GLIC 发布。

## Abstract
Efficient image compression relies on modeling both local and global redundancy. Most state-of-the-art (SOTA) learned image compression (LIC) methods are based on CNNs or Transformers, which are inherently rigid. Standard CNN kernels and window-based attention mechanisms impose fixed receptive fields and static connectivity patterns, which potentially couple non-redundant pixels simply due to their proximity in Euclidean space. This rigidity limits the model's ability to adaptively capture spatially varying redundancy across the image, particularly at the global level. To overcome these limitations, we propose a content-adaptive image compression framework based on Graph Neural Networks (GNNs). Specifically, our approach constructs dual-scale graphs that enable flexible, data-driven receptive fields. Furthermore, we introduce adaptive connectivity by dynamically adjusting the number of neighbors for each node based on local content complexity. These innovations empower our Graph-based Learned Image Compression (GLIC) model to effectively model diverse redundancy patterns across images, leading to more efficient and adaptive compression. Experiments demonstrate that GLIC achieves state-of-the-art performance, achieving BD-rate reductions of 19.29%, 21.69%, and 18.71% relative to VTM-9.1 on Kodak, Tecnick, and CLIC, respectively. Code will be released at https://github.com/UnoC-727/GLIC.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Learned Image Compression with Graph Neural Networks》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的学习型图像压缩（LIC）模型主要基于 CNN 或 Transformer。CNN 的卷积核和 Transformer 的窗口注意力机制具有**僵化性（Rigidity）**，即感受野固定且连接模式静态。
*   **研究动机**：
    *   **空间冗余不均匀**：图像中平滑区域冗余高，纹理区域信息量大，固定连接无法适应这种空间变化。
    *   **欧几里得限制**：传统方法仅根据像素在空间上的邻近性进行耦合，容易忽略远距离的相关性，或错误地耦合邻近但不相关的特征。
    *   **目标**：引入图神经网络（GNN）的动态、数据驱动连接特性，实现内容自适应的冗余消除。

### 2. 提出的方法论：GLIC 框架
核心思想是利用图神经网络构建**内容自适应的特征聚合（GFA）模块**，主要包含以下关键技术：
*   **双尺度图构建（Dual-scale Graphs）**：
    *   **稠密局部采样**：在固定窗口内采集候选节点，捕捉精细的局部结构。
    *   **稀疏全局采样**：采用网格采样（Mesh-grid sampling）生成跨越全图的候选节点，以近线性的复杂度获取全局感受野。
*   **复杂度感知邻居配额（Complexity-aware Neighbor Quota）**：
    *   使用 **RMS-Gradient (RMS-G)** 评分机制：通过 Sobel 算子计算梯度，并进行跨通道均方根（RMS）处理，识别结构复杂的区域。
    *   **动态分配**：为复杂区域分配更多的邻居配额（Degree），为平滑区域分配较少配额，实现计算资源的自适应分配。
*   **图构建与聚合算法**：
    *   **二分查找阈值**：通过二分法快速确定每个节点的余弦相似度阈值，以满足预设的邻居配额。
    *   **边条件聚合**：基于像素间的相似度进行加权特征更新，优先处理内容相关的冗余。

### 3. 实验设计
*   **数据集**：Kodak（24张图）、Tecnick（1200×1200分辨率）、CLIC2020 验证集（2K分辨率）。
*   **Benchmark（基准）**：
    *   **传统标准**：VTM-9.1 (VVC)。
    *   **SOTA 学习型方法**：ELIC, MLIC++, TCM-L, FTIC, CCA, WeConvene, HPCM, DCAE, LALIC, MambaIC 等。
*   **评估指标**：BD-rate（主要指标）、PSNR、MS-SSIM、bpp、FLOPs、参数量、解码延迟及峰值内存。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU** 和 AMD EPYC 7742 64核 CPU。
*   **训练时长**：采用了训练加速策略，总训练时间缩短至 **3天**。
*   **推理效率**：在 2K 图像上，图构建的额外开销低于 0.02s（占比小于 5%），显示了算法的工程可行性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低分辨率（Kodak）到高分辨率（CLIC 2K）的多个数据集，实验设计全面。
*   **消融实验**：
    *   验证了双尺度图（局部 vs 全局）的必要性。
    *   对比了不同的复杂度评分策略（Sobel 梯度 vs 局部熵 vs 残差）。
    *   对比了不同的通道池化方式（RMS vs Mean）。
*   **客观性**：对比了 2022 年至 2025 年间几乎所有主流的 SOTA 模型，并提供了有效感受野（ERF）的可视化分析，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：GLIC 在 Kodak、Tecnick 和 CLIC 上相比 VTM-9.1 分别实现了 **19.29%、21.69% 和 18.71% 的 BD-rate 降低**。
*   **效率优势**：相比 2025 年的最强基准 MambaIC，GLIC 在参数量减少 57.2% 的同时，解码延迟降低了 41.0%，峰值内存降低了 73.1%。
*   **自适应性**：可视化证明 GLIC 能根据内容（如背景 vs 前景）动态调整感受野，实现了真正的“内容自适应”压缩。

### 7. 优点与亮点
*   **打破僵化**：成功将 GNN 引入 LIC，解决了 CNN/Transformer 感受野固定的痛点。
*   **线性复杂度**：通过双尺度采样和二分查找，在保持全局连接能力的同时，避免了 $O(N^2)$ 的计算开销。
*   **性能与效率兼顾**：在达到 SOTA 压缩率的同时，显著优化了内存和速度，更具实用价值。

### 8. 不足与局限
*   **架构依赖**：虽然 GFA 模块很强，但模型整体仍依赖于标准的 VAE 骨干网络，未探索非 VAE 架构下的表现。
*   **超参数敏感性**：邻居配额（平均度数 64）和采样窗口大小等超参数可能需要针对不同类型的图像（如医学图像或遥感图像）进行微调。
*   **硬件优化**：尽管延迟较低，但 GNN 的稀疏操作在现有深度学习框架（如 PyTorch）中通常不如密集卷积优化得充分，可能需要专门的算子优化以进一步提升速度。

（完）
