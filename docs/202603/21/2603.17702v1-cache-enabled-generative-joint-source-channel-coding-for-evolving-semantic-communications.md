---
title: Cache-enabled Generative Joint Source-Channel Coding for Evolving Semantic Communications
title_zh: 面向演进式语义通信的支持缓存的生成式联合信源信道编码
authors: "Shunpu Tang, Qianqian Yang, Jihong Park, Zhaoyang Zhang, Kaibin Huang, Deniz Gunduz"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17702v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于语义通信的生成式联合信源信道编码
tldr: 针对语义通信中端到端训练开销大且难以利用传输冗余的问题，本文提出了一种基于信道感知生成对抗网络逆映射的联合源信道编码框架（CAGI-JSCC）。该框架利用预训练的SemanticStyleGAN实现免训练通信，并引入缓存使能的动态码本（CDC）来存储和复用语义组件。实验表明，该方法在极低带宽压缩比下仍能保持高感知质量，显著提升了动态环境下的传输效率。
selection_source: fresh_fetch
motivation: 现有的语义通信方法依赖大量端到端训练且缺乏灵活性，且未能有效利用多次传输中相似内容的语义冗余。
method: 提出一种结合信道感知的GAN逆映射框架，并引入缓存机制构建动态码本以复用已传输的语义组件。
result: 在图像传输实验中，该系统在平均带宽压缩比仅为1/224的情况下实现了优异的感知质量，显著优于传统基准。
conclusion: 该框架通过免训练的生成式编码和语义级缓存，为动态无线环境下的高效语义通信提供了一种灵活且低带宽的解决方案。
---

## 摘要
基于学习的语义通信（SemCom）最近已成为提高无线网络传输效率的一种极具前景的范式。然而，现有方法通常依赖于大量的端到端训练，这在动态无线环境中既缺乏灵活性，又具有较高的计算开销。此外，它们未能利用语义相似内容在多次传输中的冗余，限制了整体效率。为了克服这些局限性，我们提出了一种基于信道感知的生成对抗网络（GAN）逆映射的联合信源信道编码（CAGI-JSCC）框架，通过利用预训练的 SemanticStyleGAN 模型实现免训练的语义通信。通过将无线信道特性显式地融入 GAN 逆映射过程，CAGI-JSCC 无需额外训练即可适应变化的信道条件。此外，我们引入了一种支持缓存的动态码本（CDC），在发送端和接收端同时缓存解耦的语义分量，从而允许系统重用先前传输的内容。这种语义级缓存可以随着经验的积累持续减少冗余传输。在图像传输上的广泛实验证明了所提框架的有效性。特别是，我们的系统在平均带宽压缩比（BCR）为 1/224、单张图像低至 1/1024 的情况下，实现了相当的感知质量，显著优于 BCR 为 1/128 的基准方法。

## Abstract
Learning-based semantic communication (SemCom) has recently emerged as a promising paradigm for improving the transmission efficiency of wireless networks. However, existing methods typically rely on extensive end-to-end training, which is both inflexible and computationally expensive in dynamic wireless environments. Moreover, they fail to exploit redundancy across multiple transmissions of semantically similar content, limiting overall efficiency. To overcome these limitations, we propose a channel-aware generative adversarial network (GAN) inversion-based joint source-channel coding (CAGI-JSCC) framework that enables training-free SemCom by leveraging a pre-trained SemanticStyleGAN model. By explicitly incorporating wireless channel characteristics into the GAN inversion process, CAGI-JSCC adapts to varying channel conditions without additional training. Furthermore, we introduce a cache-enabled dynamic codebook (CDC) that caches disentangled semantic components at both the transmitter and receiver, allowing the system to reuse previously transmitted content. This semantic-level caching can continuously reduce redundant transmissions as experience accumulates. Extensive experiments on image transmission demonstrate the effectiveness of the proposed framework. In particular, our system achieves comparable perceptual quality with an average bandwidth compression ratio (BCR) of 1/224, and as low as 1/1024 for a single image, significantly outperforming baselines with a BCR of 1/128.

---

## 论文详细总结（自动生成）

以下是对论文《Cache-enabled Generative Joint Source-Channel Coding for Evolving Semantic Communications》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着6G网络中扩展现实（XR）、自动驾驶和数字孪生等应用的发展，对高保真、低延迟传输的需求日益增长。语义通信（SemCom）通过传输任务相关的语义信息而非原始比特，成为提升效率的关键。
*   **核心问题**：
    1.  **灵活性差**：现有的深度联合信源信道编码（DeepJSCC）通常需要针对特定信道条件进行端到端训练，难以适应动态变化的无线环境。
    2.  **计算开销高**：在线重新训练模型以适应新环境的成本巨大。
    3.  **冗余未利用**：现有系统在多次传输语义相似的内容（如不同人脸中相似的眼睛或嘴巴形状）时，未能有效利用这些跨传输的语义冗余。

### 2. 方法论
论文提出了一种名为 **CAGI-JSCC** 的框架，结合了信道感知和语义缓存技术：
*   **核心思想**：利用预训练的生成模型（SemanticStyleGAN）作为语义编解码器，通过“GAN逆映射（Inversion）”过程在发送端提取语义潜在代码，并引入信道感知机制确保传输鲁棒性。
*   **关键技术细节**：
    *   **信道感知 GAN 逆映射 (CAGI)**：在优化潜在代码 $y$ 的迭代过程中，显式地加入功率归一化和加性高斯白噪声（AWGN）建模。这使得生成的潜在代码在无需额外信道编码的情况下，天然具备抗噪声能力。
    *   **支持缓存的动态码本 (CDC)**：利用 SemanticStyleGAN 的解耦特性（不同维度的向量控制不同器官），在发送端和接收端建立同步缓存。
    *   **缓存匹配与索引传输**：发送端搜索本地缓存，若发现相似度高于阈值的语义组件（如相似的鼻部特征），则仅传输该组件在码本中的索引（数字传输），而非原始向量（模拟传输），从而大幅压缩带宽。
    *   **SNR 感知更新策略**：根据当前信道信噪比（SNR）动态更新缓存内容，确保缓存中始终存储最高质量的语义分量。
    *   **两阶段优化**：先进行常规逆映射，再结合缓存匹配结果进行微调，以补偿缓存近似带来的误差。

### 3. 实验设计
*   **数据集**：使用人脸图像数据集 **CelebAMask-HQ**（分辨率 512x512）。
*   **对比方法 (Benchmarks)**：
    *   **DeepJSCC**：经典的端到端学习 JSCC。
    *   **InverseJSCC / GenerativeJSCC**：现有的基于 GAN 的语义通信方法。
    *   **BPG + LDPC**：传统的信源信道分离编码方案。
    *   **消融实验**：包括无信道感知的 GAN 逆映射（GI）和基于学习的语义 JSCC（GI-SSCC）。
*   **评估指标**：
    *   失真指标：PSNR, MS-SSIM。
    *   感知/生成指标：LPIPS, PIEAPP, DISTS, FID（衡量图像真实感和分布相似度）。

### 4. 资源与算力
*   **算法参数**：使用 Adam 优化器，学习率 0.01，最大迭代次数 300 次。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练/推理时长。但由于 GAN 逆映射涉及数百次迭代优化，其单次推理的计算延迟通常高于传统的神经网络前向传播。

### 5. 实验数量与充分性
*   **实验规模**：在 0dB 到 5dB 的不同 SNR 条件下进行了广泛测试，并针对 300 张图像的连续传输评估了缓存的演进效果。
*   **充分性评价**：实验设计较为全面，涵盖了从像素级失真到人类视觉感知的多维度评估。通过设置不同的缓存阈值（$\gamma_A, \gamma_B$）和缓存大小（$N_C$），验证了系统在不同配置下的性能边界。实验对比了多种 SOTA 方法，结果具有较强的说服力。

### 6. 主要结论与发现
*   **极高压缩比**：在保持相当感知质量的前提下，平均带宽压缩比（BCR）达到 **1/224**，单张图像甚至可低至 **1/1024**，远优于基准方法的 1/128。
*   **免训练适应性**：CAGI-JSCC 无需针对新信道重新训练，仅通过调整逆映射过程中的噪声参数即可适应环境变化。
*   **演进式增益**：随着传输次数增加，缓存命中率提高，传输效率随之持续提升。
*   **感知质量优越**：在低 SNR 条件下，虽然 PSNR 略低于 DeepJSCC，但在 FID 和 LPIPS 等感知指标上显著领先，生成的图像更符合人类视觉审美。

### 7. 优点与亮点
*   **解耦语义复用**：首次将 GAN 的解耦特征控制与无线缓存结合，实现了细粒度的语义组件复用。
*   **信道感知优化**：将信道特性融入优化目标函数而非网络架构，增强了系统的灵活性和鲁棒性。
*   **演进式学习**：系统具备“经验积累”能力，能够随着使用时间的增加变得更加高效。

### 8. 不足与局限
*   **计算延迟**：GAN 逆映射是一个迭代优化过程，相比于 DeepJSCC 的单次前向计算，其实时性可能受限。
*   **领域局限性**：高度依赖预训练 GAN 的分布。如果输入图像超出预训练模型的领域（如用人脸模型传输风景），性能会大幅下降。
*   **缓存同步风险**：在极差信道下，若索引传输出现比特错误，可能导致发送端与接收端缓存失步，论文虽提到了回退策略，但在大规模网络中的稳定性仍待验证。

（完）
