---
title: Motion-Adaptive Temporal Attention for Lightweight Video Generation with Stable Diffusion
title_zh: 基于 Stable Diffusion 的轻量级视频生成的运动自适应时间注意力机制
authors: "Rui Hong, Shuxue Quan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17398v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于轻量化视频生成的运动自适应时间注意力
tldr: "本研究提出一种基于冻结Stable Diffusion的轻量化视频生成方法，核心是运动自适应时间注意力机制。该机制根据运动强度动态调整感受野：高运动序列采用局部注意力以保留细节，低运动序列采用全局注意力以维持一致性。通过在UNet中注入仅25.8M（占比2.9%）的可训练参数，在WebVid数据集上实现了高效且高质量的视频生成，并揭示了噪声相关性与运动幅度之间的权衡关系。"
selection_source: fresh_fetch
motivation: 旨在解决现有视频生成模型参数量巨大且难以在保持场景一致性的同时捕捉快速运动细节的问题。
method: 提出运动自适应时间注意力，在UNet不同层级联注入全局和局部注意力模块，并结合运动感知门控。
result: "仅需2.9%的新增参数即可在WebVid验证集上达到竞争性表现，且无需额外的显式时间一致性损失。"
conclusion: 运动自适应机制能有效平衡视频的细节保留与全局一致性，为轻量化视频生成提供了高效的解决方案。
---

## 摘要
我们提出了一种基于冻结的 Stable Diffusion 模型的运动自适应时间注意力机制，用于参数高效的视频生成。我们的方法并非统一处理所有视频内容，而是根据估计的运动内容动态调整时间注意力的感受野：高运动序列在帧间进行局部注意力以保留快速变化的细节，而低运动序列则进行全局注意力以增强场景一致性。我们通过级联策略将轻量级时间注意力模块注入所有 UNet Transformer 块中——在下采样和中间块中使用全局注意力以实现语义稳定，在上采样块中使用运动自适应注意力以进行细粒度细化。结合时间相关噪声初始化和运动感知门控，该系统仅增加了 25.8M 个可训练参数（占基础 UNet 的 2.9%），在 100K 视频上训练后，在 WebVid 验证集上取得了具有竞争力的结果。我们证明了仅标准去噪目标就能提供足够的隐式时间正则化，其表现优于添加显式时间一致性损失的方法。我们的消融研究揭示了噪声相关性与运动幅度之间明显的权衡，为多样化的生成行为提供了实用的推理阶段控制。

## Abstract
We present a motion-adaptive temporal attention mechanism for parameter-efficient video generation built upon frozen Stable Diffusion models. Rather than treating all video content uniformly, our method dynamically adjusts temporal attention receptive fields based on estimated motion content: high-motion sequences attend locally across frames to preserve rapidly changing details, while low-motion sequences attend globally to enforce scene consistency. We inject lightweight temporal attention modules into all UNet transformer blocks via a cascaded strategy -- global attention in down-sampling and middle blocks for semantic stabilization, motion-adaptive attention in up-sampling blocks for fine-grained refinement. Combined with temporally correlated noise initialization and motion-aware gating, the system adds only 25.8M trainable parameters (2.9\% of the base UNet) while achieving competitive results on WebVid validation when trained on 100K videos. We demonstrate that the standard denoising objective alone provides sufficient implicit temporal regularization, outperforming approaches that add explicit temporal consistency losses. Our ablation studies reveal a clear trade-off between noise correlation and motion amplitude, providing a practical inference-time control for diverse generation behaviors.

---

## 论文详细总结（自动生成）

以下是对论文《Motion-Adaptive Temporal Attention for Lightweight Video Generation with Stable Diffusion》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保持预训练图像扩散模型（如 Stable Diffusion）生成质量的同时，以极低的参数代价实现时序连贯且运动自然的视频生成。
*   **研究动机**：
    *   **现有方法的局限性**：全量微调视频模型计算成本极高；而现有的参数高效方法（如 AnimateDiff）对所有视频内容采用统一的时间注意力机制，忽略了不同运动强度对时序建模的不同需求（如快动作需要局部关注细节，慢动作需要全局关注一致性）。
    *   **效率需求**：旨在开发一种极轻量级的插件，仅需少量参数即可让冻结的 SD 模型具备视频生成能力。

### 2. 方法论：核心思想与关键技术
论文提出了 **Motion-Adaptive Temporal UNet**，其核心包含以下四个关键组件：
*   **运动自适应时间注意力（Motion-Adaptive Attention）**：
    *   引入**距离偏置矩阵** $b_m(i, j) = -\lambda(m) \cdot |i - j|$。其中 $m$ 是运动得分，$\lambda(m)$ 随运动强度增大。
    *   **逻辑**：高运动时，衰减系数大，注意力集中在相邻帧以保留细节；低运动时，衰减系数小，注意力跨越全局以增强一致性。
*   **运动感知门控（Motion-aware Gating）**：
    *   使用一个受运动强度 $m$ 调控的门控因子 $g(m)$ 来调节时间残差分支的贡献，初始权重设为 0（零初始化），确保训练初期不破坏原始模型。
*   **级联注入策略（Cascaded Injection Strategy）**：
    *   在 UNet 的**下采样和中间块**注入“全局时间注意力”，用于稳定宏观语义和背景。
    *   在**上采样块**注入“自适应时间注意力”，用于精细化处理受运动影响较大的局部细节。
*   **时间相关噪声初始化（Temporally Correlated Noise）**：
    *   采用自回归方式生成帧间相关的噪声：$\epsilon_f = \rho \cdot \epsilon_{f-1} + \sqrt{1-\rho^2} \cdot \xi_f$。
    *   $\rho$ 作为一个超参数，在推理阶段可直接控制视频的运动幅度与一致性之间的权衡。

### 3. 实验设计
*   **数据集**：从 WebVid-10M 中抽取 100K 个视频子集进行训练。
*   **Benchmark**：WebVid 验证集（随机抽取 400 个标题进行评估）。
*   **对比方法**：
    *   **AnimateDiff**：在相同 100K 数据集上重新训练的版本。
    *   **Base SD 1.5/2.1**：不加时间模块的原始模型（作为基准）。
*   **评估指标**：CLIP-SIM（文本对齐）、Frame Consistency（帧间一致性）、Motion Magnitude（运动幅度）、FID（单帧质量）、FVD（视频整体质量）。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU** 集群进行训练。
*   **训练配置**：Batch Size 为 55，训练 30 个 Epoch。
*   **参数量**：新增时间模块仅 **25.8M** 个可训练参数，仅占基础 UNet（860M）的 **2.9%**。相比之下，AnimateDiff 的运动模块约为 417M 参数。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主对比实验、消融实验以及定性可视化。
*   **消融实验**：
    1.  **损失函数**：对比了“仅去噪损失”与“去噪+显式时间一致性损失”，发现显式损失反而抑制了运动多样性。
    2.  **噪声相关性 $\rho$**：测试了不同 $\rho$ 值对生成效果的影响。
    3.  **基础模型**：验证了方法在 SD 1.5 和 SD 2.1 上的通用性。
*   **充分性评价**：实验设计较为客观，通过在相同数据量下重训对比方法确保了公平性。虽然 100K 数据量不算巨大，但对于验证“轻量级”和“参数高效”这一目标是充分的。

### 6. 主要结论与发现
*   **参数极度高效**：仅用 2.9% 的参数增量即可实现与参数量大得多的模型（如 AnimateDiff）相近的性能。
*   **隐式正则化更优**：标准的去噪目标（Denoising Loss）已足以提供隐式的时间约束，添加显式的时间一致性损失（Temporal Consistency Loss）反而会降低视频的运动感。
*   **推理可控性**：噪声相关系数 $\rho$ 是一个强大的推理控制杠杆，无需重新训练即可调节视频的动态程度。

### 7. 优点（亮点）
*   **动态适应性**：首次在轻量级插件中引入了根据运动强度动态调整注意力感受野的机制，解决了“一刀切”建模的问题。
*   **级联设计合理**：区分了语义稳定（全局）和细节细化（自适应）的不同层级功能。
*   **推理灵活性**：通过噪声相关性提供了一种简单有效的运动幅度调节手段。

### 8. 不足与局限
*   **生成规格较低**：目前仅支持生成 8 帧、256x256 分辨率的视频，与当前主流的 SORA 或更高分辨率的模型相比，在时长和清晰度上有明显差距。
*   **性能上限**：虽然参数效率极高，但在 FID 和 FVD 指标上仍略逊于参数量更大的 AnimateDiff，说明极度压缩参数可能存在性能天花板。
*   **运动估计依赖**：推理时需要根据提示词手动或自动估计运动得分，若估计不准可能影响生成效果。

（完）
