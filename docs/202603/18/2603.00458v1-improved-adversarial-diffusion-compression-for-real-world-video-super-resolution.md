---
title: Improved Adversarial Diffusion Compression for Real-World Video Super-Resolution
title_zh: 针对真实场景视频超分辨率的改进对抗性扩散压缩
authors: "Bin Chen, Weiqi Li, Shijie Zhao, Xuanyu Zhang, Junlin Li, Li Zhang, Jian Zhang"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00458v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 通过剪枝与蒸馏实现视频扩散模型的对抗压缩
tldr: 针对真实场景视频超分辨率中扩散模型推理慢且参数量大的问题，本文提出了一种改进的对抗扩散压缩方法。该方法通过将大型DiT教师模型蒸馏至轻量化的2D SD骨干网络，并引入1D时间卷积和双头对抗蒸馏方案，在显著降低计算开销的同时，有效平衡了视频的细节生成与时序一致性，实现了高效的高质量视频超分。
selection_source: carryover_cache
motivation: 现有的单步扩散模型参数量巨大且推理延迟高，且传统压缩方法在处理视频超分时难以兼顾空间细节和时间一致性。
method: 将具有3D时空注意力的DiT教师模型蒸馏至带有轻量化1D时间卷积的2D骨干网络，并采用像素与特征双域的对抗蒸馏方案。
result: "压缩后的AdcVSR模型相比教师模型减少了95%的参数量，实现了8倍的推理加速，并保持了优异的视频质量。"
conclusion: 该研究证明了通过改进的对抗蒸馏策略，可以在极低计算资源下实现高质量且时序稳定的实时视频超分辨率处理。
---

## 摘要
虽然许多扩散模型通过生成丰富且真实的细节在真实场景视频超分辨率（Real-VSR）中取得了显著成果，但它们对多步采样的依赖导致推理速度缓慢。SeedVR2、DOVE 和 DLoRAL 等单步网络通过将生成过程压缩为单一步骤来缓解这一问题，但它们仍然过于臃肿，拥有数十亿参数和数秒级的延迟。最近的对抗性扩散压缩（ADC）通过剪枝和蒸馏将这些模型转化为紧凑的 AdcSR 网络，提供了一条可行的路径，但由于缺乏时间感知以及标准对抗学习的局限性，直接将其应用于 Real-VSR 无法平衡空间细节和时间一致性。为了应对这些挑战，我们提出了一种针对 Real-VSR 的改进 ADC 方法。我们的方法将配备 3D 时空注意力的庞大扩散 Transformer (DiT) 教师模型 DOVE 蒸馏到经过剪枝的、基于 2D Stable Diffusion (SD) 的 AdcSR 骨干网络中，并辅以轻量级 1D 时间卷积，从而实现了显著更高的效率。此外，我们引入了一种双头对抗蒸馏方案，其中像素域和特征域的判别器显式地将细节和一致性的判别解耦到两个头中，使得两个目标都能得到有效优化，而无需相互权衡。实验表明，由此产生的压缩 AdcVSR 模型在参数量上降低了 95% 的复杂度，并比其 DiT 教师模型 DOVE 实现了 8 倍的加速，同时保持了具有竞争力的视频质量和效率。

## Abstract
While many diffusion models have achieved impressive results in real-world video super-resolution (Real-VSR) by generating rich and realistic details, their reliance on multi-step sampling leads to slow inference. One-step networks like SeedVR2, DOVE, and DLoRAL alleviate this through condensing generation into one single step, yet they remain heavy, with billions of parameters and multi-second latency. Recent adversarial diffusion compression (ADC) offers a promising path via pruning and distilling these models into a compact AdcSR network, but directly applying it to Real-VSR fails to balance spatial details and temporal consistency due to its lack of temporal awareness and the limitations of standard adversarial learning. To address these challenges, we propose an improved ADC method for Real-VSR. Our approach distills a large diffusion Transformer (DiT) teacher DOVE equipped with 3D spatio-temporal attentions, into a pruned 2D Stable Diffusion (SD)-based AdcSR backbone, augmented with lightweight 1D temporal convolutions, achieving significantly higher efficiency. In addition, we introduce a dual-head adversarial distillation scheme, in which discriminators in both pixel and feature domains explicitly disentangle the discrimination of details and consistency into two heads, enabling both objectives to be effectively optimized without sacrificing one for the other. Experiments demonstrate that the resulting compressed AdcVSR model reduces complexity by 95% in parameters and achieves an 8$\times$ acceleration over its DiT teacher DOVE, while maintaining competitive video quality and efficiency.

---

## 论文详细总结（自动生成）

### 针对真实场景视频超分辨率的改进对抗性扩散压缩 (AdcVSR) 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的基于扩散模型的真实场景视频超分辨率（Real-VSR）方法面临**计算开销巨大**（参数量达数十亿）和**推理速度缓慢**（多步采样或复杂的3D注意力机制）的问题。
*   **研究动机**：虽然单步扩散模型（如 DOVE）提高了速度，但体积依然庞大。现有的图像压缩技术（如 ADC）直接应用于视频时，由于缺乏时间感知能力，会导致严重的画面闪烁（即无法平衡空间细节与时间一致性）。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将庞大的 3D 扩散 Transformer（DiT）教师模型知识蒸馏到一个轻量化的“2D+1D”学生模型中，并通过改进的对抗蒸馏方案解决细节与一致性的冲突。
*   **关键技术细节**：
    *   **“2D + 1D”网络设计**：以剪枝后的 2D Stable Diffusion (SD 2.1) 为骨干网络负责生成空间细节，在每个模块后插入轻量级的 **1D 时间卷积层**来捕捉帧间依赖，从而替代昂贵的 3D 注意力。
    *   **双域蒸馏**：在**像素域**（输出图像）和**特征域**（VAE 解码器中间特征）同时进行蒸馏，使用 $L_1$ 损失和 DISTS 感知损失进行监督。
    *   **双头对抗蒸馏方案**：设计了两个判别器（像素级和特征级），每个判别器包含两个独立的预测头——**“细节头”**和**“一致性头”**。
    *   **解耦训练策略**：通过五种精心设计的数据类型（真实视频、时间打乱的视频、高质量图像、静态伪视频、随机图像序列）分别训练这两个头，强制模型在不牺牲细节的前提下优化时间稳定性。

#### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：UDM10, SPMCS, YouHQ40（使用 RealBasicVSR 的退化流水线合成）。
    *   **真实场景数据集**：RealVSR, MVSR4x, VideoLQ。
    *   **训练数据**：OpenVid-1M（视频）和 LSDIR（图像）。
*   **对比方法（Benchmark）**：
    *   非生成式：RealBasicVSR。
    *   多步扩散：Upscale-A-Video, MGLD-VSR, STAR。
    *   单步扩散：SeedVR2, DOVE（教师模型）, DLoRAL。
    *   图像超分模型（逐帧处理）：PiSA-SR, AdcSR, HYPIR。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU** (每台 96GB 显存)。
*   **训练时长**：整个训练过程分为两个阶段（纯蒸馏阶段和对抗蒸馏阶段），总计耗时约 **1 天**。
*   **推理效率**：在 H20 GPU 上处理 25 帧 512x512 视频仅需 **0.55 秒**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流数据集上进行了定量评估，涵盖了忠实度（PSNR/SSIM）、感知质量（LPIPS/DISTS/MUSIQ 等）和时间一致性（$E^*_{warp}$）等多个维度。
*   **消融实验**：针对“2D+1D”架构、双头判别器设计、不同通道划分比例以及训练数据类型做了详尽的消融研究。
*   **评价**：实验设计非常充分且客观，通过对比图像超分模型在视频上的表现，有力地证明了其在解决闪烁问题上的优越性。

#### 6. 主要结论与发现
*   **极致压缩**：AdcVSR 相比教师模型 DOVE 减少了 **95% 的参数量**（从 11B 降至约 0.57B）。
*   **显著加速**：实现了 **8 倍的推理加速**，且比其他单步视频扩散模型（如 DLoRAL）快 300 倍以上。
*   **性能平衡**：在保持与教师模型相当的细节生成能力的同时，获得了**最佳的时间一致性指标**（$E^*_{warp}$ 最低）。

#### 7. 优点与亮点
*   **架构创新**：证明了简单的“2D 骨干 + 1D 时间卷积”足以承载大型 3D DiT 的知识，极大地降低了视频生成的门槛。
*   **判别器设计**：创新的双头解耦设计巧妙地解决了生成模型中“细节丰富度”与“时间稳定性”此消彼长的矛盾。
*   **实用性强**：模型轻量且速度快，为移动端或实时视频修复提供了可行的技术路径。

#### 8. 不足与局限
*   **幻觉风险**：作为生成式模型，在处理极端复杂的纹理（如茂密的树叶或水面）时，仍可能产生不真实的细节或轻微的幻觉。
*   **教师依赖**：学生模型的上限受限于教师模型（DOVE）的预训练质量。
*   **极端退化处理**：在面对极度模糊或噪声极大的视频时，虽然一致性较好，但细节还原的准确度仍有提升空间。

（完）
