---
title: An FPGA Implementation of Displacement Vector Search for Intra Pattern Copy in JPEG XS
title_zh: JPEG XS 中帧内模式复制位移矢量搜索的 FPGA 实现
authors: "Qiyue Chen, Yao Li, Jie Tao, Song Chen, Li Li, Dong Liu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10671v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 针对低复杂度 JPEG XS 的位移矢量搜索 FPGA 实现
tldr: JPEG XS 是一种低延迟图像压缩标准，其 Intra Pattern Copy (IPC) 工具通过位移矢量 (DV) 搜索减少屏幕内容的冗余。由于 DV 搜索计算量巨大，本文提出了一种高效的流水线 FPGA 架构，通过优化内存组织和利用数据复用模式来提升性能。实验表明，该设计在 277 mW 功耗下实现了 38.3 Mpixels/s 的吞吐量，为 IPC 的硬件部署和 ASIC 开发奠定了基础。
selection_source: fresh_fetch
motivation: 针对 JPEG XS 中 IPC 工具的位移矢量搜索模块计算复杂度高、难以在硬件中高效实现的问题。
method: 提出一种高效的流水线 FPGA 架构，并结合 IPC 计算特性设计了优化的内存组织结构以增强数据复用。
result: 该架构在 FPGA 上实现了 38.3 Mpixels/s 的吞吐量，且功耗仅为 277 mW。
conclusion: 该设计证明了 IPC 硬件实现的切实可行性，为未来在 ASIC 上的部署提供了重要参考。
---

## 摘要
最近，针对 JPEG XS 的帧内模式复制（IPC）工具取得了进展，JPEG XS 是一种专为低延迟和低复杂度编码设计的图像压缩标准。IPC 执行小波域帧内补偿预测，以减少屏幕内容中的空间冗余。IPC 的一个关键模块是位移矢量（DV）搜索，其目的是求解最优预测参考偏移量。然而，DV 搜索过程计算密集，给实际硬件部署带来了挑战。在本文中，我们为 DV 搜索模块提出了一种高效的流水线 FPGA 架构设计，以促进 IPC 的实际部署。此外，还引入了优化的存储组织，利用 IPC 的计算特性和数据固有的重用模式来增强性能。实验结果表明，所提出的架构实现了 38.3 Mpixels/s 的吞吐量，功耗为 277 mW，证明了其在 IPC 和其他预测编码工具中进行实际硬件实现的可行性，并为 ASIC 部署奠定了良好的基础。

## Abstract
Recently, progress has been made on the Intra Pattern Copy (IPC) tool for JPEG XS, an image compression standard designed for low-latency and low-complexity coding. IPC performs wavelet-domain intra compensation predictions to reduce spatial redundancy in screen content. A key module of IPC is the displacement vector (DV) search, which aims to solve the optimal prediction reference offset. However, the DV search process is computationally intensive, posing challenges for practical hardware deployment. In this paper, we propose an efficient pipelined FPGA architecture design for the DV search module to promote the practical deployment of IPC. Optimized memory organization, which leverages the IPC computational characteristics and data inherent reuse patterns, is further introduced to enhance the performance. Experimental results show that our proposed architecture achieves a throughput of 38.3 Mpixels/s with a power consumption of 277 mW, demonstrating its feasibility for practical hardware implementation in IPC and other predictive coding tools, and providing a promising foundation for ASIC deployment.

---

## 论文详细总结（自动生成）

### 论文总结：JPEG XS 中帧内模式复制位移矢量搜索的 FPGA 实现

#### 1. 核心问题与研究动机
*   **背景**：JPEG XS 是一种针对低延迟、低复杂度应用（如远程桌面、KVM、5G 视频流）设计的图像压缩标准。
*   **核心问题**：为了提升屏幕内容编码效率，JPEG XS 引入了帧内模式复制（IPC）工具。IPC 的核心是位移矢量（DV）搜索，通过在小波域寻找最优匹配块来减少空间冗余。然而，DV 搜索涉及大量的候选位置遍历和残差计算，计算复杂度极高，成为制约 IPC 在实时硬件系统中部署的瓶颈。
*   **研究目的**：设计一种高效的 FPGA 架构，通过流水线设计和内存组织优化，实现低功耗、高吞吐量的 DV 搜索，为实际硬件部署和未来的 ASIC 设计奠定基础。

#### 2. 方法论
论文提出了一种四级流水线的 FPGA 架构，主要包含以下核心组件：
*   **顶层架构**：分为**残差计算引擎**和**DV 比较引擎**。
    *   **残差计算引擎**：从 DRAM 中读取原始和重建的小波系数，通过 `SIG MAG SUB` 模块并行计算符号-数值格式的残差。
    *   **DV 比较引擎**：采用四级流水线设计。
        *   **Stage 0**：加载残差并生成计算参数（如带宽、组大小）。
        *   **Stage 1**：执行位或（OR）掩码计算。
        *   **Stage 2**：计算 GCLI（广义码长指示）成本，即估计残差所需的比特平面数。
        *   **Stage 3**：比较当前成本与历史最小值，更新并输出最优 DV。
*   **内存组织优化（Method 1）**：
    *   针对小波系数在内存中分布零散的问题，提出“按 IPC 组对齐”的存储方式。
    *   将同一 IPC 单元内的子带块连续存储，支持单次基地址加偏移的突发读取（Burst Access）。
    *   引入片上**转换检测缓冲区（TLB）**来存储不同组的变长块信息，简化地址计算。

#### 3. 实验设计
*   **硬件平台**：Xilinx Artix-7 (XC7A35T) FPGA。
*   **时钟频率**：100 MHz。
*   **Benchmark（对比基准）**：
    *   **Method 0**：传统的按 Precinct（区域）对齐的内存组织方式（线性寻址，但访问零散）。
    *   **软件参考**：验证 FPGA 实现的率失真性能是否与 IPC 参考软件一致。
*   **评估指标**：吞吐量（Mpixels/s）、功耗（mW）、资源利用率（LUT, FF, DSP, BRAM）、功率效率（Mpixels/s/W）。

#### 4. 资源与算力
*   **算力/硬件资源**：
    *   **逻辑资源**：消耗约 12.89K LUTs 和 21.79K FFs。
    *   **计算资源**：使用 17 个 DSP 单元。
    *   **存储资源**：使用 15 个 BRAM。
*   **功耗**：总功耗为 277 mW。
*   **性能**：在 100MHz 下实现了 38.3 Mpixels/s 的吞吐量。

#### 5. 实验数量与充分性
*   **实验规模**：论文对比了两种内存组织方法（Method 0 vs Method 1）在同一硬件平台上的表现。
*   **充分性评价**：
    *   **硬件验证充分**：详细列出了各子模块（残差计算、GCLI 计算、DV 更新）的资源消耗，展示了流水线设计的效率。
    *   **对比客观**：通过对比优化前后的内存访问模式，量化了吞吐量和功率效率的提升（提升约 6.1%）。
    *   **局限性**：实验主要集中在硬件性能指标上，未详细展示不同视频序列下的率失真曲线（仅文字说明与软件一致）。

#### 6. 主要结论与发现
*   **可行性验证**：首次为 JPEG XS IPC 框架提出了 FPGA 架构，证明了在低功耗 FPGA 上实现复杂 DV 搜索的可行性。
*   **性能优异**：38.3 Mpixels/s 的吞吐量足以支持实时处理需求，且功耗极低（277 mW）。
*   **内存优化的重要性**：通过将内存组织从“区域对齐”改为“组对齐”，不仅提升了吞吐量，还减少了约 7.5% 的 LUT 和 8.4% 的 FF 资源消耗。

#### 7. 优点
*   **首创性**：填补了 JPEG XS IPC 硬件实现领域的空白。
*   **高效流水线**：四级流水线设计平衡了吞吐量和延迟，适合实时处理。
*   **内存设计巧妙**：利用 TLB 和组对齐存储解决了小波域数据访问零散的痛点，显著提高了 DRAM 带宽利用率。

#### 8. 不足与局限
*   **平台单一**：仅在 Xilinx Artix-7 这一款中低端 FPGA 上进行了测试，未在更高端的 FPGA 或 ASIC 流程中验证频率上限。
*   **应用限制**：该架构专门针对 JPEG XS 的小波域 IPC 设计，对于传统像素域（如 HEVC/VVC）的帧内块复制（IBC）工具的通用性未做讨论。
*   **实验覆盖**：缺乏针对不同搜索范围（Search Range）对资源消耗影响的消融实验。

（完）
