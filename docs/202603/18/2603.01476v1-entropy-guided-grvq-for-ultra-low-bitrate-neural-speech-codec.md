---
title: Entropy-Guided GRVQ for Ultra-Low Bitrate Neural Speech Codec
title_zh: 面向超低比特率神经语音编解码器的熵引导分组残差向量量化
authors: "Yanzhou Ren, Noboru Harada, Daiki Takeuchi, Siyu Chen, Wei Liu, Xiao Zhang, Liyuan Zhang, Takehiro Moriya, Shoji Makino"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01476v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 神经编解码器的熵引导分组与残差向量量化
tldr: 针对超低比特率下的神经语音编解码器，本文提出了一种熵引导的分组残差向量量化（EG-GRVQ）方法。该方法结合了语义分支以保留语言信息，并在声学分支中采用基于通道方差的熵引导分组策略，确保各组信息量均衡，从而提高码本效率并减少冗余。实验表明，该模型在极低带宽下显著提升了语音的感知质量和可懂度。
selection_source: carryover_cache
motivation: 在超低比特率约束下，现有的神经语音编解码器难以平衡准确的语义建模与高保真的音频重建。
method: 提出EG-GRVQ方法，利用通道方差作为信息量代理，将编码器输出划分为信息量均衡的组进行残差向量量化。
result: 在LibriTTS和VCTK数据集上的测试显示，该模型在极低比特率下显著改善了语音的感知质量和可懂度指标。
conclusion: 通过熵引导的均衡分组策略，可以有效提升码本效率并降低冗余，为超低带宽通信提供了高性能的编解码方案。
---

## 摘要
神经音频编解码器 (NAC) 对于重建高质量语音信号以及为下游语音语言模型生成离散表示至关重要。然而，在超低比特率约束下，确保准确的语义建模同时保持高保真重建仍然具有挑战性。我们为超低比特率神经语音编解码器提出了一种熵引导的分组残差向量量化 (EG-GRVQ)，该方法保留了用于语言信息的语义分支，并在声学分支中引入了熵引导的分组策略。假设通道激活近似服从高斯分布，每个通道的方差可以作为其信息含量的原则性代理。基于这一假设，我们对编码器输出进行划分，使得每个组携带总信息的相等份额。这种均衡分配提高了码本效率并减少了冗余。在 LibriTTS 和 VCTK 数据集上训练后，我们的模型在超低比特率条件下表现出感知质量和可懂度指标的提升，重点关注面向通信场景的编解码器级保真度。

## Abstract
Neural audio codec (NAC) is essential for reconstructing high-quality speech signals and generating discrete representations for downstream speech language models. However, ensuring accurate semantic modeling while maintaining high-fidelity reconstruction under ultra-low bitrate constraints remains challenging. We propose an entropy-guided group residual vector quantization (EG-GRVQ) for an ultra-low bitrate neural speech codec, which retains a semantic branch for linguistic information and incorporates an entropy-guided grouping strategy in the acoustic branch. Assuming that channel activations follow approximately Gaussian statistics, the variance of each channel can serve as a principled proxy for its information content. Based on this assumption, we partition the encoder output such that each group carries an equal share of the total information. This balanced allocation improves codebook efficiency and reduces redundancy. Trained on LibriTTS and VCTK, our model shows improvements in perceptual quality and intelligibility metrics under ultra-low bitrate conditions, with a focus on codec-level fidelity for communication-oriented scenarios.

---

## 论文详细总结（自动生成）

这篇论文针对超低比特率下的神经语音编解码器提出了**熵引导的分组残差向量量化（EG-GRVQ）**方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在超低比特率（如低于 1 kbps）约束下，神经语音编解码器（NAC）难以同时兼顾**语义建模**（确保说话内容清晰）和**高保真重建**（确保音质自然）。
*   **研究背景**：现有的方法如 RVQ（残差向量量化）在深层量化器中效率较低，而 GRVQ（分组残差向量量化）虽然通过分组提升了性能，但通常采用简单的均匀通道划分，忽略了不同通道间信息分布不均的问题。

### 2. 核心方法论
*   **核心思想**：利用信息论中“方差与熵”的正相关性，将编码器输出的通道按信息量（方差）进行非均匀分组，使每个组承载相等份额的信息，从而优化码本利用率。
*   **关键技术细节**：
    *   **双分支架构**：继承了 Mimi 编解码器的结构，分为**语义分支**（提取语言信息，通过 WavLM 蒸馏学习）和**声学分支**（保留信号细节）。
    *   **熵引导分组（EG-GRVQ）**：假设编码器通道激活服从高斯分布，则微分熵是方差的单调函数。通过计算训练集上各通道的方差 $\sigma^2$，寻找一个分割点 $k^*$，使得分割出的两个组的累积方差近似相等。
    *   **量化配置**：在声学分支中，将 512 个通道根据方差分布划分为两组（例如 237 和 275 个通道），每组使用 2 层残差量化器（2x2 结构）。
*   **算法流程**：先统计全局通道方差 -> 确定非均匀分组界限 -> 对各组分别进行残差向量量化 -> 合并语义与声学特征 -> 解码重建。

### 3. 实验设计
*   **数据集**：使用 LibriTTS（100, 360, 500 小时子集）和 VCTK 全集进行训练。测试集采用 LibriTTS 的 test-clean 子集。
*   **比特率**：固定在 **0.6875 kbps** 的极低水平。
*   **对比基准（Benchmark）**：
    *   **Mimi (official)**：官方预训练模型。
    *   **Mimi (retrain)**：在相同数据集上重训的 RVQ 版本。
    *   **Mimi (GRVQ)**：采用均匀分组（256/256 通道）的重训版本。
*   **评估指标**：客观指标包括 PESQ（感知质量）、STOI（可懂度）、ViSQOL、SDR 和 NMSE（归一化均方误差）；主观指标采用 MUSHRA 测试。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A6000 GPU**（每台 48GB 显存）。
*   **训练参数**：每台 GPU 的 Batch Size 为 12。
*   **时长**：文中未明确说明具体的训练总时长，但提到了多目标损失函数（对抗损失、特征匹配损失、承诺损失和语义蒸馏损失）的优化过程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   客观评估：随机抽取 200 个样本进行多指标测试。
    *   消融实验：对比了不同的分组配置（1x4, 4x1, 2x2）。
    *   主观评估：8 名具有专业背景的受试者对 8 个样本进行 MUSHRA 评分。
*   **充分性评价**：实验设计较为客观，通过对比“官方版”、“重训版”和“均匀分组版”，排除了数据差异带来的干扰，证明了“熵引导分组”这一核心变量的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 0.6875 kbps 下，EG-GRVQ 在 PESQ（1.881）和 STOI（0.890）上均优于所有基准模型。
*   **码本效率**：相比 RVQ 随层数增加而效率骤降的问题，EG-GRVQ 在所有层级保持了高且稳定的码本利用率。
*   **主观偏好**：MUSHRA 测试显示，该方法比官方 Mimi 提升了 21 分，比均匀分组的 GRVQ 提升了 11 分，具有显著的统计学优势。
*   **分组策略**：实验发现“少而深”的分组（2组x2层）比“多而浅”的分组（4组x1层）效果更好。

### 7. 优点与亮点
*   **理论支撑**：将抽象的信息论原则（熵与方差的关系）转化为具体的工程实现（通道分组），逻辑清晰。
*   **效率优化**：在不增加比特率的前提下，通过优化内部结构显著提升了码本的表达能力。
*   **实用性强**：基于因果、低延迟的 Mimi 架构，适合实时通信场景。

### 8. 不足与局限
*   **静态分组**：目前的通道分割点 $k^*$ 是基于全局统计固定的。论文提到虽然可以实现每帧动态调整，但为了节省信令开销未予采用，这可能限制了对极端语音片段的自适应能力。
*   **应用范围**：实验主要集中在语音（Speech），对于更复杂的通用音频（音乐、环境音）在超低比特率下的表现尚待验证。
*   **相位敏感度**：SDR 指标较低（-7.3 dB 左右），反映出模型在相位对齐方面仍存在神经编解码器的通病。

（完）
