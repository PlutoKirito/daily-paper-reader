---
title: "From Fewer Samples to Fewer Bits: Reframing Dataset Distillation as Joint Optimization of Precision and Compactness"
title_zh: 从更少的样本到更少的比特：将数据集蒸馏重新定义为精度与紧凑性的联合优化
authors: "My H. Dinh, Aditya Sant, Akshay Malhotra, Keya Patani, Shahab Hamidi-Rad"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02411v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 用于数据集压缩的可微分量化模块和率失真视角
tldr: 本研究针对数据集蒸馏（DD）仅关注样本数量减少而忽视数据精度的局限，提出了QuADD框架。该框架将DD重新定义为固定比特预算下精度与紧凑性的联合优化问题，通过引入可微分量化模块，实现了合成样本与量化参数的端到端协同优化。实验证明，QuADD在图像分类和通信任务中均显著提升了单位比特的训练效率，为构建信息高效的合成数据集提供了新范式。
selection_source: carryover_cache
motivation: 现有的数据集蒸馏方法主要关注减少样本数量，而忽视了数据精度对存储效率和模型性能的影响。
method: 提出QuADD框架，通过在蒸馏循环中集成可微分量化模块，实现合成样本与量化参数的端到端联合优化。
result: 在图像分类和3GPP波束管理任务中，QuADD在单位比特精度上均优于现有的数据集蒸馏和后量化基准。
conclusion: 该研究通过联合优化精度与紧凑性，为信息高效的数据集蒸馏建立了新的标准。
---

## 摘要
数据集蒸馏（DD）将大型数据集压缩为紧凑的合成数据集，同时保持训练性能。然而，当前的方法主要针对样本数量的减少，而很少考虑数据精度及其对效率的影响。我们提出了量化感知数据集蒸馏（QuADD），这是一个在固定比特预算下联合优化数据集紧凑性和精度的统一框架。QuADD 在蒸馏循环中集成了一个可微分量化模块，实现了合成样本和量化参数的端到端协同优化。在率失真视角的指导下，我们实证分析了样本数量与精度之间的比特分配如何影响学习性能。我们的框架支持均匀量化和自适应非均匀量化，后者可以从数据中学习量化阶，以更好地表示信息密集区域。在图像分类和 3GPP 波束管理任务上的实验表明，QuADD 在单位比特准确率方面超越了现有的 DD 和后量化基准，为信息高效的数据集蒸馏建立了新标准。

## Abstract
Dataset Distillation (DD) compresses large datasets into compact synthetic ones that maintain training performance. However, current methods mainly target sample reduction, with limited consideration of data precision and its impact on efficiency. We propose Quantization-aware Dataset Distillation (QuADD), a unified framework that jointly optimizes dataset compactness and precision under fixed bit budgets. QuADD integrates a differentiable quantization module within the distillation loop, enabling end-to-end co-optimization of synthetic samples and quantization parameters. Guided by the rate-distortion perspective, we empirically analyze how bit allocation between sample count and precision influences learning performance. Our framework supports both uniform and adaptive non-uniform quantization, where the latter learns quantization levels from data to represent information-dense regions better. Experiments on image classification and 3GPP beam management tasks show that QuADD surpasses existing DD and post-quantized baselines in accuracy per bit, establishing a new standard for information-efficient dataset distillation.

---

## 论文详细总结（自动生成）

### 论文总结：从更少的样本到更少的比特 (QuADD)

#### 1. 核心问题与整体含义（研究动机和背景）
传统的数据集蒸馏（Dataset Distillation, DD）主要关注如何减少合成数据集的**样本数量**（IPC），而忽略了每个数据元素的**数值精度**（Bit-depth）。在实际的分布式学习、边缘计算或物联网（IoT）场景中，存储和传输的真正瓶颈是**总比特数**（Total Bits）。
本论文的核心问题是：在给定的总比特预算下，如何平衡“样本数量”与“数据精度”以获得最佳的模型性能？研究者将 DD 重新定义为一个**精度与紧凑性联合优化**的问题，旨在实现从“更少样本”到“更少比特”的范式转变。

#### 2. 方法论：核心思想与关键技术
论文提出了 **QuADD (Quantization-aware Dataset Distillation)** 框架，其核心思想是将一个**可微分量化层**集成到蒸馏的闭环优化中。

*   **核心思想**：在蒸馏过程中，不仅优化合成样本的像素值，还同时优化量化器的参数（如缩放因子 $\alpha$），使合成数据能够主动适应低精度带来的失真。
*   **关键技术细节**：
    *   **可微分量化模块**：为了解决量化操作（舍入/截断）不可微的问题，采用了**直通估计器（STE）**或软松弛技术，允许梯度回传至合成样本和量化参数。
    *   **自适应非均匀量化（APoT）**：除了标准的均匀量化，QuADD 重点使用了自适应幂次和（Additive Powers-of-Two）量化。这种方法能根据数据分布自动调整量化阶，在信息密集区域分配更高分辨率。
    *   **率失真视角（Rate-Distortion Perspective）**：通过公式 $Budget = M \times D \times b$（样本数 $\times$ 维度 $\times$ 位宽）来量化存储成本，并寻找性能最优的 $(M, b)$ 组合。
    *   **量化引导初始化**：在蒸馏开始前，利用量化后的真实数据通过图割（Graph-cut）准则选择最具代表性的样本作为初始值。

#### 3. 实验设计
*   **数据集/场景**：
    *   **图像分类**：CIFAR-10, CIFAR-100, ImageNette。
    *   **无线通信（非视觉）**：3GPP 波束管理（Beam Management）任务，处理的是结构化的表格/矩阵数据。
*   **Benchmark 与对比方法**：
    *   **全精度基准**：DD, DM, TM, DATM 等主流蒸馏算法。
    *   **压缩基准**：FreD（频域压缩）、AutoPalette（颜色调色板量化）。
    *   **传统方法**：Coreset 样本选择（如 Herding, K-Center）。
*   **评估指标**：在相同比特预算（Total Storage）下的测试准确率。

#### 4. 资源与算力
*   **硬件设备**：论文明确指出实验使用了 **4 × NVIDIA V100 (32GB)** 或 **2 × NVIDIA H100 (80GB)** GPU。
*   **训练效率**：QuADD 在训练时间上具有优势。相比于 AutoPalette（需优化颜色空间）和 FreD（频域变换开销大），QuADD 的量化层非常轻量，在 IPC=50 时比全精度基准（DATM）快约 25-30%。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括不同 IPC（1, 10, 50）、不同位宽（1-bit 到 8-bit）、跨架构验证（AlexNet, VGG, ResNet）以及跨模态验证（图像 vs. 无线数据）。
*   **充分性与公平性**：实验设计较为客观。作者通过“率失真曲线”展示了在相同比特预算下不同方法的表现，并对比了后量化（Post-quantization）与量化感知训练的区别，证明了联合优化的必要性。跨架构实验证明了合成数据的泛化能力。

#### 6. 主要结论与发现
*   **“甜点区”发现**：在固定比特预算下，**“低精度 + 更多样本”**的组合通常优于“高精度 + 极少样本”。例如，使用 2-3 bits 精度配合更多样本往往能达到最高准确率。
*   **高效压缩**：QuADD 在图像任务上实现了约 **10倍** 的存储压缩，而准确率损失控制在 1% 以内；在 3GPP 任务上，甚至实现了高达 **183倍** 的压缩率。
*   **非均匀量化的优越性**：自适应非均匀量化（APoT）在极低位宽（如 1-3 bits）下显著优于均匀量化。

#### 7. 优点（亮点）
*   **通用性（Modality-agnostic）**：不同于以往仅针对图像颜色的压缩方法，QuADD 是通用的，能直接应用于无线通信等非视觉领域。
*   **端到端协同**：将量化参数纳入优化目标，使合成数据对量化噪声具有鲁棒性。
*   **效率高**：在提升存储效率的同时，并没有显著增加（甚至减少了）训练时间。

#### 8. 不足与局限
*   **超参数敏感性**：虽然量化层简单，但 clipping threshold ($\alpha$) 的初始化对收敛稳定性有一定影响，需要针对不同分布的数据进行预设（如论文提到的百分位数初始化）。
*   **任务覆盖**：虽然引入了无线通信任务，但大部分实验仍集中在分类任务上，对于生成任务或目标检测等更复杂场景的适用性尚待验证。
*   **理论深度**：论文主要基于实证分析（Empirical Analysis），对于“样本数”与“精度”之间最优分配比例的理论推导相对较少。

（完）
