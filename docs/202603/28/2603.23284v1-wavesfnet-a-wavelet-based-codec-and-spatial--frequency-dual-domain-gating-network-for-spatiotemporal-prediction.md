---
title: "WaveSFNet: A Wavelet-Based Codec and Spatial--Frequency Dual-Domain Gating Network for Spatiotemporal Prediction"
title_zh: WaveSFNet：一种用于时空预测的基于小波编解码器和空频双域门控网络
authors: "Xinyong Cai, Runming Xie, Hu Chen, Yuankai Wu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23284v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 用于时空预测和长程动态的基于小波的编解码器
tldr: 本研究针对时空预测中高频细节丢失和长程动态建模难的问题，提出了WaveSFNet框架。该框架结合了小波变换编解码器以保留下采样过程中的纹理信息，并设计了空间-频率双域门控翻译器。通过注入相邻帧差增强动态信息，并融合大卷积核空间建模与频域全局调制，实现了高效的特征交互。实验证明，该模型在多个基准数据集上达到了高精度且保持了低计算复杂度。
selection_source: fresh_fetch
motivation: 现有的时空预测框架在下采样时易丢失高频纹理，且难以在局部交互与全局传播之间取得平衡。
method: 提出WaveSFNet，利用小波编解码器保留细节，并通过双域门控翻译器结合空间局部建模与频域全局调制。
result: 在Moving MNIST、TaxiBJ和WeatherBench等数据集上表现优异，且计算开销较低。
conclusion: WaveSFNet通过小波变换与双域门控机制，有效提升了时空预测的清晰度与长程建模能力。
---

## 摘要
时空预测学习旨在以无监督的方式根据历史观测预测未来帧，这对于广泛的应用至关重要。关键挑战在于建模长程动力学，同时保留高频细节，以实现清晰的多步预测。现有的高效无循环框架通常依赖步长卷积或池化进行采样，这往往会丢弃纹理和边界，而纯空间算子通常难以平衡局部交互与全局传播。为了解决这些问题，我们提出了 WaveSFNet，这是一个将基于小波的编解码器与空频双域门控时空转换器相结合的高效框架。基于小波的编解码器在下采样和重建过程中保留了高频子带线索。同时，转换器首先注入相邻帧差异以显式增强动态信息，然后在空域大核局部建模与频域全局调制之间进行双域门控融合，并结合门控通道交互进行跨通道特征交换。广泛的实验表明，WaveSFNet 在 Moving MNIST、TaxiBJ 和 WeatherBench 上实现了具有竞争力的预测精度，同时保持了较低的计算复杂度。我们的代码可在 https://github.com/fhjdqaq/WaveSFNet 获取。

## Abstract
Spatiotemporal predictive learning aims to forecast future frames from historical observations in an unsupervised manner, and is critical to a wide range of applications. The key challenge is to model long-range dynamics while preserving high-frequency details for sharp multi-step predictions. Existing efficient recurrent-free frameworks typically rely on strided convolutions or pooling for sampling, which tends to discard textures and boundaries, while purely spatial operators often struggle to balance local interactions with global propagation. To address these issues, we propose WaveSFNet, an efficient framework that unifies a wavelet-based codec with a spatial--frequency dual-domain gated spatiotemporal translator. The wavelet-based codec preserves high-frequency subband cues during downsampling and reconstruction. Meanwhile, the translator first injects adjacent-frame differences to explicitly enhance dynamic information, and then performs dual-domain gated fusion between large-kernel spatial local modeling and frequency-domain global modulation, together with gated channel interaction for cross-channel feature exchange. Extensive experiments demonstrate that WaveSFNet achieves competitive prediction accuracy on Moving MNIST, TaxiBJ, and WeatherBench, while maintaining low computational complexity. Our code is available at https://github.com/fhjdqaq/WaveSFNet.

---

## 论文详细总结（自动生成）

以下是对论文《WaveSFNet: A Wavelet-Based Codec and Spatial–Frequency Dual-Domain Gating Network for Spatiotemporal Prediction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在时空预测（如天气、交通预测）中，如何在建模长程动态规律的同时，保留高频细节（如边缘和纹理），以防止多步预测中的误差累积和画面模糊。
*   **研究背景**：
    *   **循环模型（Recurrent-based）**：如 ConvLSTM、PredRNN，因串行计算导致并行化差、计算成本高。
    *   **无循环模型（Recurrent-free）**：如 SimVP，虽然并行效率高，但通常依赖步长卷积或池化进行下采样，这会导致高频信息丢失。
    *   **算子局限性**：纯空间算子难以平衡局部交互与全局传播。

### 2. 方法论：WaveSFNet 架构
WaveSFNet 采用“编码器-翻译器-解码器”的对称架构，核心创新点包括：
*   **基于小波的编解码器（Wavelet-based Codec）**：
    *   使用**离散小波变换（DWT）**代替传统的步长卷积或池化进行下采样。
    *   通过 Haar 小波将特征分解为四个子带（LL, HL, LH, HH），显式保留高频分量，并通过 $1 \times 1$ 卷积融合。
*   **时空差分注入（TDI Block）**：
    *   在潜空间中计算相邻帧的差异（$\Delta Z_t = Z_t - Z_{t-1}$），通过深度卷积和门控机制显式增强模型对快速运动变化的敏感度。
*   **双域门控时空翻译器（ST Block）**：
    *   **空间分支**：使用 $9 \times 9$ 大核深度卷积捕捉局部结构。
    *   **频域分支**：利用 **2D 快速傅里叶变换（FFT）**在谱域进行全局调制，捕捉长程依赖。
    *   **门控通道交互**：通过 SiLU 激活函数和元素级乘法实现非线性通道混合，促进空频特征交换。

### 3. 实验设计
*   **数据集与场景**：
    *   **Moving MNIST**：合成运动物体预测（2个移动数字）。
    *   **TaxiBJ**：城市人流/交通流预测（北京出租车流量）。
    *   **WeatherBench**：全球天气预报，包括单变量（温度 T2M、云量 TCC 等）和多变量（R, T, U, V 等）预测。
*   **对比方法（Benchmark）**：
    *   对比了超过 15 种主流方法，包括循环类（ConvLSTM, PredRNN, VMRNN 等）和无循环类（SimVP, TAU, MogaNet, Earthformer 等）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **效率指标**：论文详细列出了模型的参数量（Params）和浮点运算量（FLOPs）。例如在 TaxiBJ 数据集上，WaveSFNet 仅需 4.5M 参数和 1.1G FLOPs，显著低于 PredRNN 等传统模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成、交通、气象三大领域，实验设置符合 OpenSTL 标准协议。
*   **消融实验**：设计了 5 组变体实验，分别验证了空间分支、频率分支、TDI 模块、小波编解码器以及门控机制的有效性。
*   **客观性**：引入了**径向平均功率谱密度（RAPSD）分析**，从频谱角度客观证明了该模型在缓解高频功率衰减（即保持清晰度）方面的优势。

### 6. 主要结论与发现
*   **性能领先**：WaveSFNet 在所有测试数据集上均达到了 SOTA（最优）或极具竞争力的表现，尤其在 Moving MNIST 上取得了最低的 MSE（15.8）。
*   **空频互补**：实验证明空间局部建模与频域全局调制相结合，比单一域建模效果更好。
*   **小波优势**：相比传统卷积编解码器，小波变换能更有效地减少采样过程中的信息损失，支持更忠实的图像重建。

### 7. 优点与亮点
*   **高效性**：在保持高精度的同时，大幅降低了计算开销（见图 1 的气泡图对比）。
*   **细节保留**：通过小波变换和频域分支，有效解决了时空预测中常见的“画面变模糊”问题。
*   **即插即用**：其模块化设计（如 TDI 和 ST Block）具有较强的通用性。

### 8. 不足与局限
*   **长程预测策略**：对于超长序列预测，仍依赖自回归（Autoregressive）滚动策略，这可能导致长期的误差累积。
*   **小波基选择**：目前仅使用了 Haar 小波，未探讨其他更复杂的小波基（如 Daubechies）对性能的影响。
*   **硬件细节缺失**：缺乏具体的训练环境描述，不便于其他研究者评估实际的训练吞吐量。

（完）
