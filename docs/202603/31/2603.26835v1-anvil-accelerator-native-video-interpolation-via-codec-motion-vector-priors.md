---
title: "ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors"
title_zh: ANVIL：基于编解码器运动矢量先验的加速器原生视频插帧
authors: Shibo Liu
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26835v1"
tags: ["query:train-trick"]
score: 9.0
evidence: 复用编解码器运动矢量实现低复杂度插帧
tldr: 针对移动端高刷新率显示与低帧率视频不匹配的问题，本文提出ANVIL框架。它通过复用H.264解码器已有的运动矢量进行帧预对齐，彻底移除了传统方法中难以在移动加速器上高效运行的学得光流和空间采样算子。该方法在Snapdragon 8 Gen 3上实现了1080p视频的实时8位量化推理，显著降低了端到端延迟，为移动端实时视频插帧提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26835v1/fig-001.webp\", \"caption\": \"TABLE IV PER-OPERATOR INSTRUMENTED INT8 QUANTIZATION. COSSIM MEASURED AGAINST FP32 OUTPUT. THE SAME CAUSAL PATTERN HOLDS FOR BOTH METHODS: CONV INTRODUCES INITIAL ERROR, PRELU HAS ZERO EFFECT, AND ADD TRIGGERS THE COLLAPSE. FULL W8A8 ≈ CONV+ADD.\", \"page\": 7, \"index\": 1, \"width\": 505, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.26835v1/fig-002.webp\", \"caption\": \"TABLE II CROSS-VENDOR OPERATOR COMPATIBILITY FOR 17 VFI OPERATORS AT 1080P. LATENCY NORMALIZED TO CONV 3×3 = 1.0×. “FREQ”: METHODS USING THE OPERATOR (OUT OF 9 SURVEYED). GROUPS: (A) UNIVERSAL, (B) PARTIAL, (C) LIMITED, (D) ITERATIVE PATTERNS.\", \"page\": 7, \"index\": 2, \"width\": 942, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2603.26835v1/fig-003.webp\", \"caption\": \"Fig. 4. Quality scaling with model capacity. Basic prealignment (dashed) and smoothed prealignment (solid) series both scale without saturation. Dashed horizontal lines show RIFE HDv3 (3.04M) and NAFNet ceiling (17.1M, smoothed prealignment retrained) as reference points.\", \"page\": 10, \"index\": 3, \"width\": 498, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2603.26835v1/fig-004.webp\", \"caption\": \"TABLE V QUALITY AND DEPLOYMENT STATUS ON VIMEO90K AND XIPH 1080P. BOLD: BEST AMONG DEPLOYABLE METHODS. FOR CONTEXT, NON-DEPLOYABLE METHODS REACH 35–36 DB ON VIMEO90K (IFRNET [2] 35.80, AMT-S [3] 35.72, EMA-VFI [6] 36.11).\", \"page\": 8, \"index\": 4, \"width\": 840, \"height\": 363}]"
motivation: 传统基于流的视频插帧算法在移动加速器上存在算子不支持、量化精度损失严重及内存受限等部署障碍。
method: 利用H.264解码器提供的运动矢量预对齐输入帧，并采用纯卷积网络进行残差细化，避免了复杂的迭代流计算。
result: 在Snapdragon 8 Gen 3上，1080p 8位量化推理仅需12.8ms，端到端中值延迟为28.4ms，支持长时间稳定播放。
conclusion: ANVIL证明了通过复用编解码器先验信息可以有效克服移动端硬件限制，实现高性能、低功耗的实时视频插帧。
---

## 摘要
移动端显示器的刷新率通常为 90-120 Hz，但大多数视频以每秒 24-30 帧进行编码；实时帧率翻倍要求在移动端神经网络处理单元（NPU）上，每个合成帧的处理时间在 33.3 毫秒以内。我们展示了主流的基于光流的视频帧插值在移动加速器上面临三个结构性部署障碍：空间采样算子超出了帧预算或缺乏硬件支持，迭代光流细化在 8 位训练后量化下会崩溃，以及受内存限制的算子在推理图中占据主导地位。ANVIL 通过重用 H.264 解码器已经计算出的运动矢量来预对齐输入帧，从而解决了这些障碍，并从加速器图中移除了学习到的光流、空间采样和迭代累加。剩余的残差由一个以卷积为主的网络进行细化，该网络的推理图几乎完全由受计算限制的算子组成。在骁龙 8 Gen 3 设备上，ANVIL 在 8 位整数精度下实现了 12.8 毫秒的 1080p 网络推理；一个开源的 Android 播放器在 30 分钟连续播放期间，通过对 54,623 个连续记录的样本进行测试，实现了每对插值帧 28.4 毫秒的中值端到端延迟。逐算子因果分析表明，循环光流状态上的量化累加是迭代方法中整数量化失败的关键机制。当前设计针对具有解码器暴露运动矢量的 H.264 播放场景。

## Abstract
Mobile displays refresh at 90-120 Hz, yet most video is encoded at 24-30 frames per second; real-time frame-rate doubling requires each synthesized frame within 33.3 ms on mobile neural processing units. We show that mainstream flow-based video frame interpolation faces three structural deployment barriers on mobile accelerators: spatial sampling operators exceed the frame budget or lack hardware support, iterative flow refinement collapses under 8-bit post-training quantization, and memory-bound operators dominate the inference graph. ANVIL addresses these barriers by reusing motion vectors already computed by the H.264 decoder to prealign input frames, removing learned optical flow, spatial sampling, and iterative accumulation from the accelerator graph. The remaining residual is refined by a convolution-dominated network whose inference graph is composed almost entirely of compute-bound operators. On a Snapdragon 8 Gen 3 device, ANVIL achieves 12.8 ms 1080p network inference in 8-bit integer precision; an open-source Android player sustains 28.4 ms median end-to-end latency per interpolated frame pair over 54,623 consecutively logged samples during 30-minute continuous playback. Per-operator causal analysis identifies quantized accumulation on recurrent flow states as a key mechanism behind integer quantization failure in iterative methods. The current design targets H.264 playback scenarios with decoder-exposed motion vectors.

---

## 论文详细总结（自动生成）

这是一份关于论文《ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors》的结构化深度分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：尽管移动端显示器已普及 90-120Hz 高刷，但主流视频仍为 24-30fps。在移动端实现实时 1080p 视频插帧（VFI）面临三大硬件部署障碍：
    1.  **算子不兼容**：光流法核心的 `grid_sample` 算子在 NPU 上极慢或不支持。
    2.  **量化崩溃**：主流 VFI 的迭代流细化结构在 INT8 量化下精度严重下降。
    3.  **效率瓶颈**：推理图中充斥着大量受内存带宽限制（Memory-bound）而非计算限制（Compute-bound）的算子。
*   **核心目标**：设计一种“加速器原生”的插帧框架，绕过复杂的学得光流，利用硬件解码器已有的信息实现实时、低功耗的 1080p 插帧。

### 2. 方法论
ANVIL 将插帧任务分解为**编解码器端预对齐**和**NPU 端残差细化**两个阶段：
*   **核心思想**：复用 H.264 解码器在解码过程中产生的运动矢量（Motion Vectors, MVs）作为零参数的运动先验，替代昂贵的深度学习光流估计。
*   **关键技术细节**：
    1.  **MV 提取与平滑**：从 H.264 P 帧提取块级 MV，通过中值滤波（抑制离群点）和高斯模糊（平滑块边界）生成稠密流场。
    2.  **三处理器流水线**：
        *   **CPU**：负责 MV 的解包与下采样。
        *   **GPU (Vulkan)**：执行空间平滑、Warp（变形）预对齐及量化准备。
        *   **NPU (HTP/APU)**：运行纯卷积残差网络，预测预对齐帧之间的微小残差。
    3.  **NPU 友好型架构 (UNet-v3b)**：
        *   仅使用 3x3 卷积、ReLU、加法跳跃连接（取代 Concat 以节省内存）。
        *   部署时将 Batch Normalization (BN) 融合进卷积权重。
        *   非对称通道设计：高分辨率层通道窄（16），深层通道宽（64-96），以平衡带宽与计算。

### 3. 实验设计
*   **数据集**：Vimeo90K（训练与测试）、Xiph 1080p（用于验证高分辨率下的泛化性）。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流 VFI 方法（如 RIFE, IFRNet, AMT, EMA-VFI 等）。
    *   在多个移动平台测试：骁龙 8 Gen 3/Gen 2、骁龙 7+ Gen 2、天玑 9300/9400+。
*   **评估指标**：PSNR、SSIM、LPIPS（感知质量）、tOF（时间稳定性）以及 NPU 推理延迟。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 16，采用 AMP bfloat16 自动混合精度训练。
*   **硬件使用**：文中未明确说明训练所用的具体服务器 GPU 型号（如 A100 数量），但强调了推理端在骁龙 8 Gen 3 (HTP V75) 上达到了 12.8ms 的 1080p 推理速度。
*   **部署工具**：使用了 Qualcomm QNN SDK 和 MediaTek NeuroPilot SDK。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了系统的**算子级性能基准测试**（17 种算子在不同 NPU 上的延迟）。
    *   **因果分析实验**：通过逐算子量化，定位了迭代流方法在 INT8 下崩溃的根本原因是“循环状态上的量化累加”。
    *   **消融实验**：验证了 MV 平滑、模型容量缩放、跳跃连接方式对性能的影响。
    *   **系统级验证**：在 Android 播放器中进行了 **30 分钟连续播放测试**，记录了超过 5.4 万个帧对的延迟数据。
*   **评价**：实验设计非常客观且具有工业实战价值，不仅关注算法指标，更深入探讨了硬件底层的执行逻辑。

### 6. 主要结论与发现
*   **量化失效机制**：发现迭代式光流细化中的 `Add` 算子会放大每一层的量化误差，导致最终图像崩溃。
*   **性能突破**：ANVIL-S 在 1080p INT8 下的推理速度超过了 RIFE 在 360p 下的速度，证明了“算子亲和性”比 FLOPs 更重要。
*   **实时性**：在骁龙 8 Gen 3 上实现了中值 28.4ms 的端到端延迟，成功将 1080p 插帧功耗和速度控制在商用可行范围内。

### 7. 优点
*   **硬件原生优化**：针对 NPU 的整数运算特性和内存瓶颈进行了深度共向设计。
*   **部署鲁棒性**：通过移除 `grid_sample` 和迭代结构，解决了 VFI 领域长期存在的 INT8 量化难题。
*   **系统集成度高**：提供了完整的 Android 播放器集成方案，证明了三处理器（CPU/GPU/NPU）协同工作的可行性。

### 8. 不足与局限
*   **编解码器依赖**：目前高度依赖 H.264 的 MV 导出。由于 Android MediaCodec API 的限制，目前需使用软件解码提取 MV，增加了 CPU 负担。
*   **感知质量损失**：由于采用了残差预测而非纯学得光流，其 LPIPS（感知质量）指标弱于 RIFE 等复杂模型，画面倾向于平滑。
*   **复杂运动处理**：对于不符合 H.264 块运动模型的复杂非刚性运动（如流体、遮挡），表现可能受限。
*   **B 帧限制**：当前设计在处理包含复杂 B 帧结构的视频流时，插帧覆盖率会下降。

（完）
