---
title: Next-Frame Decoding for Ultra-Low-Bitrate Image Compression with Video Diffusion Priors
title_zh: 基于视频扩散先验的超低比特率图像压缩下一帧解码
authors: "Yunuo Chen, Chuqin Zhou, Jiangchuan Li, Xiaoyue Ling, Bing He, Jincheng Dai, Li Song, Guo Lu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15129v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 利用视频扩散先验和下一帧预测进行超低比特率图像压缩
tldr: 本文提出一种利用视频扩散先验进行超低码率图像压缩的新范式。研究者定义了一个保留几何与语义的紧凑“锚点帧”，并将解码过程视为从锚点到最终图像的虚拟时间演化，通过预训练视频扩散模型执行下一帧预测任务。该方法在提升图像保真度与现实感的同时，显著提高了解码速度，为感知图像压缩提供了高效的新路径。
selection_source: fresh_fetch
motivation: 现有的基于图像扩散的压缩模型在超低码率下难以兼顾语义一致性、视觉质量与解码效率。
method: 将解码建模为从包含基础语义的锚点帧到目标图像的虚拟时间转换，并利用视频扩散模型作为时间先验进行预测。
result: "在CLIC2020测试集上，该方法比DiffC节省了50%以上的码率，且解码速度提升了5倍。"
conclusion: 通过将图像解码转化为视频序列生成任务，该研究在超低码率图像压缩领域取得了性能与效率的双重突破。
---

## 摘要
我们提出了一种用于超低比特率图像压缩（ULB-IC）的新范式，该范式利用了生成式图像压缩中的“时间”演化。具体而言，我们在解码过程中定义了一个显式的中间状态：一个紧凑的锚帧（anchor frame），它在丢弃高频细节的同时保留了场景几何和语义布局。然后，我们将生成式解码重新解释为从该锚帧到最终重建图像的虚拟时间转换。为了建模这一过程，我们利用预训练的视频扩散模型（VDM）作为时间先验：锚帧作为初始帧，原始图像作为目标帧，从而将解码过程转化为下一帧预测任务。与基于图像扩散的 ULB-IC 模型相比，我们的解码过程从一个可见的、语义忠实的锚帧开始，这提高了感知图像压缩的保真度和逼真度。大量实验表明，我们的方法实现了卓越的主客观性能。在 CLIC2020 测试集上，与 DiffC 相比，我们的方法在 LPIPS、DISTS、FID 和 KID 指标上实现了超过 50% 的比特率节省，同时还实现了高达 5 倍的显著解码加速。代码将随后发布。

## Abstract
We present a novel paradigm for ultra-low-bitrate image compression (ULB-IC) that exploits the ``temporal'' evolution in generative image compression. Specifically, we define an explicit intermediate state during decoding: a compact anchor frame, which preserves the scene geometry and semantic layout while discarding high-frequency details. We then reinterpret generative decoding as a virtual temporal transition from this anchor to the final reconstructed image.To model this progression, we leverage a pretrained video diffusion model (VDM) as temporal priors: the anchor frame serves as the initial frame and the original image as the target frame, transforming the decoding process into a next-frame prediction task.In contrast to image diffusion-based ULB-IC models, our decoding proceeds from a visible, semantically faithful anchor, which improves both fidelity and realism for perceptual image compression. Extensive experiments demonstrate that our method achieves superior objective and subjective performance. On the CLIC2020 test set, our method achieves over \textbf{50\% bitrate savings} across LPIPS, DISTS, FID, and KID compared to DiffC, while also delivering a significant decoding speedup of up to $\times$5. Code will be released later.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next-Frame Decoding for Ultra-Low-Bitrate Image Compression with Video Diffusion Priors》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
该论文针对**超低比特率图像压缩（ULB-IC）**领域中的核心矛盾展开研究。现有的基于图像扩散模型（IDM）的生成式压缩方法虽然在极低码率下能产生逼真的纹理，但存在三大痛点：
*   **语义漂移（Semantic Drift）**：由于从纯高斯噪声开始解码，重建图像往往偏离原始内容的语义。
*   **保真度与现实感的失衡**：难以在保持视觉真实性的同时精确还原源图像的几何结构。
*   **解码效率低下**：扩散模型的迭代采样过程导致高延迟，难以实际应用。

**核心动机**：作者提出将单张图像的解码过程重新定义为一种**“虚拟的时间演化”**，利用视频扩散模型（VDM）中天然存在的“从模糊到清晰”的时间先验（如变焦过程）来引导图像重建。

### 2. 方法论：核心思想与关键技术
论文提出了名为 **NeFIC** 的新范式，其核心流程如下：

*   **核心思想**：将解码任务转化为“下一帧预测”。首先传输一个极度压缩的**锚点帧（Anchor Frame）**，它保留了场景的几何布局和粗略语义；然后利用 VDM 将该锚点帧作为“第一帧”，预测并生成作为“第二帧”的高保真目标图像。
*   **关键技术细节**：
    1.  **锚点编解码器（Anchor Codec）**：基于 VAE 结构，负责提取并传输紧凑的可见锚点。
    2.  **阶段 I：下一帧解码适配**：通过 LoRA 微调预训练的 VDM（如 CogVideoX），使其适应单步时间间隔的细节合成，抑制视频模型中常见的运动和闪烁伪影。
    3.  **阶段 II：一步生成旁路（One-Step Generative Bypass）**：
        *   **条件锚点编码**：使锚点编码器感知视频 VAE 的潜空间。
        *   **旁路精炼（Bypass Refinement）**：引入 Transformer 模块将压缩特征映射为生成潜变量，替代高斯噪声作为初始化。
        *   **一步解码**：将多步迭代压缩为单步生成，大幅提升速度。
*   **算法流程**：输入图像 $x \rightarrow$ 锚点编码 $\rightarrow$ 比特流 $\rightarrow$ 锚点解码 $\hat{x}_{anchor} \rightarrow$ 结合旁路潜变量输入 VDM $\rightarrow$ 单步预测得到最终重建图像 $\hat{x}$。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Flickr2W（约 20,000 张图像）。
    *   **测试集**：Kodak（经典基准）、CLIC2020 Test（专业压缩挑战赛）、DIV2K（高分辨率）。
*   **Benchmark（对比方法）**：
    *   **传统/生成式 VAE/GAN**：MS-ILLM, GLC。
    *   **扩散模型类**：PerCo, DiffC, ResULIC, StableCodec, DLF（ICCV 2025 领先方法）等。
*   **评估指标**：
    *   **失真度**：PSNR, MS-SSIM。
    *   **感知质量**：LPIPS, DISTS。
    *   **现实感**：FID, KID。
    *   **效率**：BD-rate（码率节省百分比）、推理时间。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 GPU** 进行训练和推理时间测试。
*   **训练时长**：
    *   阶段 I：10k 步（学习率 1e-4）。
    *   阶段 II：50k 步（包含 GAN 损失微调）。
    *   **总耗时**：整个两阶段训练过程大约需要 **3 天**。
*   **模型规模**：基于 CogVideoX-5B 骨干网络，使用 LoRA（Rank=256）进行高效微调。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了详尽的定量对比，并提供了丰富的定性视觉对比图。
*   **消融实验**：非常充分。作者分别验证了：
    1.  阶段 I 和阶段 II 的必要性。
    2.  条件编码与旁路精炼模块的作用。
    3.  辅助锚点损失（Aux Anchor Loss）的影响。
    4.  LoRA 参数配置的影响。
    5.  **范式对比**：专门对比了基于图像扩散模型（如 Flux-12B）的方案，证明了 VDM 架构在处理此类任务时的优越性。
*   **客观性**：使用了 BD-rate 这一标准衡量指标，并对比了 2024-2025 年最新的 SOTA 方法，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能突破**：在 CLIC2020 数据集上，NeFIC 相比 DiffC 在感知指标（LPIPS, FID 等）上实现了 **50% 以上的码率节省**。
*   **保真度提升**：相比于其他生成式方法，NeFIC 在 PSNR 和 MS-SSIM 等客观指标上也表现优异，证明了锚点帧对保持内容忠实度的重要性。
*   **效率飞跃**：通过一步生成技术，解码速度比 DiffEIC 快 **5.5-8.6 倍**，且在处理高分辨率图像时无需昂贵的切片（tiling）操作。
*   **VDM 优势**：VDM 的 3D 注意力机制和时间先验比 IDM 的通道拼接方式更能有效地利用参考信息。

### 7. 优点（亮点）
*   **范式创新**：首次将图像压缩建模为“虚拟视频演化”，巧妙利用了视频大模型中蕴含的动态先验。
*   **双域耦合**：通过旁路潜变量（潜空间）和锚点帧（像素空间）的双重约束，解决了生成式压缩中常见的语义漂移问题。
*   **实用性强**：一步生成技术解决了扩散模型落地最难的延迟问题，使其具备了实际应用的潜力。

### 8. 不足与局限
*   **模型依赖性**：性能高度依赖于预训练视频大模型（如 CogVideoX）的质量，模型体积较大（5B 参数），对显存有一定要求。
*   **超低码率极限**：虽然在超低码率下表现优异，但在极极端情况下（如 BPP < 0.01），锚点帧本身可能过于模糊，导致 VDM 产生幻觉。
*   **训练复杂性**：两阶段训练流程相对复杂，且需要平衡 GAN 损失、重建损失和码率损失等多个目标。

（完）
