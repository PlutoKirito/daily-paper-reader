---
title: Rejection Sampling is Optimal for Relative Entropy Coding
authors: "Spencer Hill, Fady Alajaji, Tamás Linder, Gergely Flamich"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23076v1"
tags: ["query:entropy-coding"]
score: 9.0
evidence: 相对熵编码的理论分析与编码方法
tldr: 该论文证明了相对熵编码中功能信息界的紧致性，并提出了ring toss编码方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23076v1/fig-001.webp\", \"caption\": \"Fig. 2. A visualization of the acceptance region in rejection sampling for an additive white Gaussian noise channel Y = X+N with X ∼ N (0, 0.75) and N ∼ N (0, 0.25), where X and Y are truncated to a large but finite range to ensure a bounded density ratio. Here, x = −0.5 and the two proposal samples are Y1 = 0.3, Y2 = −0.4 with heights L1 = 1.5 and L2 = 1.1, where we use the shorthand Li ≜ MUi. We see that x ̸∈ S1 but x ∈ S2, meaning Y1 is rejected and Y2 is accepted. The PX -probability of hitting the set Si is exactly wYi (Li).\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 233}]"
motivation: 相对熵编码的理论分析与编码方法。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
In relative entropy coding, a sender aims to design a stochastic code such that, on input $X \sim P_X$, the receiver can generate a sample $Y \sim P_{Y \mid X}$. It is a standard result that (1) this requires at least $I(X; Y)$ bits, (2) the lower bound is achievable within a logarithmic gap, and (3) this gap cannot be reduced in general. The necessity of the gap suggests that the mutual information is not the correct information measure to quantify the rate of relative entropy coding.   A potential alternative emerged in the work of Flamich et al. (2025), who proved a tighter lower bound of $I_F(X \to Y)$, a quantity we call the functional information. In this paper, we show that this lower bound is tight by constructing the ring toss code, an encoding method for rejection sampling which uses at most $I_F(X \to Y) + \log e$ bits. This demonstrates that rejection sampling is optimal for relative entropy coding. Our result implies that the classical mutual information lower bound is achievable within $\log(I(X; Y) + 1) + 2.45$ bits in general and within $1.45$ bits for singular channels, which are both the tightest bounds of their kind to date. Moreover, our one-shot result also recovers Sriramu and Wagner's asymptotic results on the second-order redundancy of relative entropy codes.

---

## 论文详细总结（自动生成）

这篇论文题为《拒绝采样在相对熵编码中是帕累托最优的》（Rejection Sampling is Optimal for Relative Entropy Coding），由来自加拿大女王大学和伦敦帝国理工学院的研究人员合作完成。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：相对熵编码（Relative Entropy Coding, REC），又称信道模拟，旨在让发送者在给定输入 $X$ 的情况下，通过发送有限比特的信息，使接收者能利用共享随机性生成符合条件分布 $Y \sim P_{Y|X}$ 的样本。
*   **核心问题**：传统理论认为 REC 的速率下界是互信息 $I(X; Y)$，但实际编码中总存在一个对数级的冗余（Gap），且证明了该冗余在一般情况下无法消除。这暗示互信息可能不是衡量 REC 速率的最优指标。
*   **研究动机**：验证 Flamich 等人提出的“功能信息”（Functional Information, $I_F$）是否为 REC 更本质的下界，并探索拒绝采样（Rejection Sampling）这一经典算法在 REC 中的性能极限。

### 2. 论文提出的方法论
*   **核心思想**：提出了 **“套圈编码”（Ring Toss Code）**。这是一种针对标准拒绝采样的改进编码方法。其核心在于：不再使用拒绝采样索引 $K$ 的无条件分布进行编码，而是利用发送者和接收者共享的随机性，计算 $K$ 的条件分布进行编码。
*   **关键技术细节**：
    *   **宽度函数（Width Function）**：利用 $w_Q(\ell) = Q(dP/dQ \geq \ell)$ 来刻画分布之间的差异。
    *   **编码流程**：发送者运行拒绝采样获得索引 $K$。编码器根据共享随机性 $Z$ 计算每一项被接受的概率（即宽度函数的值），构造一个前缀码。
    *   **数学证明**：论文证明了该编码方式的单次（One-shot）速率满足 $H(K|Z) \leq I_F(X \to Y) + \log e$。
*   **算法逻辑**：该方法将 REC 问题转化为在 $X$ 空间中的搜索问题。接收者“投掷”支持集（套圈），看是否能“套中”发送者的输入 $X$。

### 3. 实验设计
*   **研究性质**：本论文属于**纯理论信息论研究**，不涉及传统意义上的大规模数据集实验。
*   **对比基准（Benchmark）**：
    *   **互信息界**：$I(X; Y)$。
    *   **现有算法界**：如 Poisson 功能表示法（冗余为 $\log(I+1) + 4$）和贪婪拒绝采样。
*   **对比场景**：
    *   **一般信道**：证明了新算法的冗余为 $\log(I(X; Y) + 1) + 2.45$ 比特，是目前已知最紧的界。
    *   **奇异信道（Singular Channels）**：如二进制擦除信道或均匀加性噪声信道。证明了在此类信道下，$I_F = I$，冗余仅为常数（约 1.44 比特）。

### 4. 资源与算力
*   **算力说明**：文中**未提及**使用任何 GPU 或大规模计算集群。由于是数学证明和算法复杂度分析，其核心贡献在于理论推导而非数值模拟。

### 5. 实验数量与充分性
*   **充分性评价**：作为理论论文，其“实验”体现在数学证明的严密性和对不同信道类型的覆盖上。
*   **覆盖范围**：论文不仅给出了单次编码的界，还通过推导恢复了 Sriramu 和 Wagner 关于渐近二阶冗余的已知结果。证明过程涵盖了有限域加性噪声信道、奇异信道和一般信道，逻辑闭环且论证充分。

### 6. 论文的主要结论与发现
*   **拒绝采样的最优性**：证明了拒绝采样在 REC 中是帕累托最优的，其速率紧贴功能信息界 $I_F$。
*   **功能信息的地位**：确认了 $I_F$ 是比互信息更适合描述 REC 复杂度的度量指标。
*   **性能提升**：将一般信道的非渐近冗余常数从之前的 2.74 降低到了 2.45；对于奇异信道，实现了常数级的冗余。

### 7. 优点
*   **理论优雅性**：算法设计极其简单（仅改变了索引的编码方式），但证明了极强的理论结果。
*   **统一性**：一个简单的单次（One-shot）结果同时解释了奇异信道和非奇异信道的渐近行为。
*   **实用潜力**：为有损压缩、差异隐私和机器学习中的生成模型采样提供了更高效的理论框架。

### 8. 不足与局限
*   **有界性假设**：算法要求密度比（Radon-Nikodym derivative）必须有界（即 $dP_{Y|X}/dP_Y \leq M$）。虽然这是拒绝采样的标准假设，但在某些无限支撑集的连续分布中可能不成立。
*   **计算复杂度**：虽然通信比特数最优，但拒绝采样的期望运行时间与 $M$ 成正比。如果 $M$ 极大，虽然传输快，但搜索样本的计算开销会很高。
*   **缺乏实证验证**：虽然理论完备，但未在实际的神经网络压缩或生成任务中进行工程化落地测试。

（完）
