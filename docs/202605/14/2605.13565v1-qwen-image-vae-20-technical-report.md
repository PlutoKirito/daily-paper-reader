---
title: Qwen-Image-VAE-2.0 Technical Report
title_zh: Qwen-Image-VAE-2.0 技术报告
authors: "Zekai Zhang, Deqing Li, Kuan Cao, Yujia Wu, Chenfei Wu, Yu Wu, Liang Peng, Hao Meng, Jiahao Li, Jie Zhang, Kaiyuan Gao, Kun Yan, Lihan Jiang, Ningyuan Tang, Shengming Yin, Tianhe Wu, Xiao Xu, Xiaoyue Chen, Yan Shu, Yanran Zhang, Yilei Chen, Yixian Xu, Yuxiang Chen, Zhendong Wang, Zihao Liu, Zikai Zhou, Yiliang Gu, Yi Wang, Xiaoxiao Xu, Lin Qu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13565v1"
tags: ["query:vcodec"]
score: 8.5
evidence: 适用于扩散模型的高压缩VAE潜空间
tldr: Qwen-Image-VAE-2.0 是一套高压缩率的变分自编码器，旨在提升图像重建质量和扩散模型的训练效率。该研究通过引入全局跳跃连接、扩展潜空间通道以及非对称无注意力架构，解决了高压缩下的重建瓶颈。通过在数十亿张图像上训练并结合合成渲染引擎，模型在通用领域和富文本场景中均达到 SOTA 水平，并显著加速了下游 DiT 模型的收敛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13565v1/fig-001.webp\", \"caption\": \"Figure 2: OmniDoc-TokenBench, a curated collection of ∼3K text-rich images.\", \"page\": 6, \"index\": 1, \"width\": 950, \"height\": 667}, {\"url\": \"assets/figures/arxiv/2605.13565v1/fig-002.webp\", \"caption\": \"Figure 4: Selected image samples generated by SiT on ImageNet with Qwen-Image-VAE-2.0.\", \"page\": 11, \"index\": 2, \"width\": 819, \"height\": 1386}, {\"url\": \"assets/figures/arxiv/2605.13565v1/fig-003.webp\", \"caption\": \"Figure 1: Comparison of No Skip Connection (NSC), Local Skip Connection (LSC), and Global Skip Connection (GSC) on model architecture, reconstruction loss and PSNR performance. S2C is the abbreviation of Space to Channel module. The experiments are conducted on f 16c64 model training from scratch.\", \"page\": 3, \"index\": 3, \"width\": 948, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.13565v1/fig-004.webp\", \"caption\": \"Table 3: Comprehensive evaluation on OmniDoc-TokenBench (∼3K text-rich images, 256×256). Models are grouped by spatial compression factor and sorted by NED within each group. Our models are\", \"page\": 9, \"index\": 4, \"width\": 946, \"height\": 729}]"
motivation: 旨在解决高压缩率下图像重建保真度不足以及潜空间难以适配扩散模型训练的问题。
method: 采用带有全局跳跃连接和扩展潜通道的非对称无注意力架构，并结合语义对齐策略与大规模合成数据进行训练。
result: 在公共基准和新提出的 OmniDoc-TokenBench 上均取得 SOTA 重建性能，且在下游 DiT 实验中表现出极佳的扩散适配性。
conclusion: Qwen-Image-VAE-2.0 证明了通过架构优化和语义对齐，可以在保持高压缩率的同时实现卓越的重建质量和扩散效率。
---

## 摘要
我们推出了 Qwen-Image-VAE-2.0，这是一套高压缩率的变分自编码器（VAE），在重建保真度和可扩散性（diffusability）方面均取得了显著进展。为了解决高压缩率下的重建瓶颈，我们采用了一种改进的架构，其特点是引入了全局跳跃连接（GSC）并扩展了潜变量通道。此外，我们将训练规模扩大到数十亿张图像，并引入了合成渲染引擎，以提升在富文本场景下的性能。为了应对高维潜空间的收敛挑战，我们实施了一种增强的语义对齐策略，使潜空间非常适合扩散建模。为了优化计算效率，我们利用非对称且无注意力机制的编码器-解码器骨干网络，以最大限度地减少编码开销。我们在公开的重建基准测试上对 Qwen-Image-VAE-2.0 进行了全面评估。为了评估在富文本场景下的性能，我们提出了 OmniDoc-TokenBench，这是一个包含多种真实世界文档以及专门基于 OCR 的评估指标的新基准。Qwen-Image-VAE-2.0 实现了最先进的重建性能，展示了在高压缩率下处理通用领域和富文本场景的卓越能力。此外，下游 DiT 实验表明，我们的模型具有优越的可扩散性，与现有的高压缩基线相比，显著加速了收敛。这些成果确立了 Qwen-Image-VAE-2.0 作为一款具有高压缩率、卓越重建能力和出色可扩散性的领先模型地位。

## Abstract
We present Qwen-Image-VAE-2.0, a suite of high-compression Variational Autoencoders (VAEs) that achieve significant advances in both reconstruction fidelity and diffusability. To address the reconstruction bottlenecks of high compression, we adopt an improved architecture featuring Global Skip Connections (GSC) and expanded latent channels. Moreover, we scale training to billions of images and incorporate a synthetic rendering engine to improve performance in text-rich scenarios. To tackle the convergence challenges of high-dimensional latent space, we implement an enhanced semantic alignment strategy to make the latent space highly amenable to diffusion modeling. To optimize computational efficiency, we leverage an asymmetric and attention-free encoder-decoder backbone to minimize encoding overhead. We present a comprehensive evaluation of Qwen-Image-VAE-2.0 on public reconstruction benchmarks. To evaluate performance in text-rich scenarios, we propose OmniDoc-TokenBench, a new benchmark comprising a diverse collection of real-world documents coupled with specialized OCR-based evaluation metrics. Qwen-Image-VAE-2.0 achieves state-of-the-art reconstruction performance, demonstrating exceptional capabilities in both general domains and text-rich scenarios at high compression ratio. Furthermore, downstream DiT experiments reveal our models possess superior diffusability, significantly accelerating convergence compared to existing high-compression baselines. These establish Qwen-Image-VAE-2.0 as a leading model with high compression, superior reconstruction, and exceptional diffusability.

---

## 论文详细总结（自动生成）

这篇技术报告介绍了 **Qwen-Image-VAE-2.0**，这是由阿里巴巴 Qwen 团队开发的一套高压缩率变分自编码器（VAE）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在原生高分辨率图像生成中，扩散模型（如 DiT）的计算复杂度随序列长度呈平方级增长。传统的 $f8$ 空间压缩率已成为计算瓶颈。
*   **研究动机**：虽然提高压缩率（如 $f16, f32$）能显著提升效率，但会面临“三难困境”：**高压缩率、重建保真度（尤其是文本）以及潜空间的可扩散性（Diffusability）**之间的权衡。高压缩往往导致细节丢失，而增加通道数补偿信息又会导致潜空间分布复杂，使下游扩散模型难以收敛。

### 2. 方法论
Qwen-Image-VAE-2.0 通过架构优化、数据工程和训练策略的组合来解决上述困境：
*   **核心架构创新**：
    *   **全局跳跃连接（Global Skip Connection, GSC）**：通过“空间到通道”（Space-to-Channel）操作，建立从像素到潜空间的直接残差路径，绕过初始下采样阶段，保留高频细节。
    *   **扩展潜通道**：增加潜变量的通道维度 $C$（如 64, 128, 192），以补偿空间压缩带来的信息损失。
    *   **非对称与无注意力设计**：采用轻量级编码器（减少推理开销）和全卷积（Attention-Free）骨干网络，以支持超高分辨率输入并提高吞吐量。
*   **训练策略优化**：
    *   **移除 KL 和 GAN 损失**：发现 KL 散度会限制容量并干扰语义对齐，而大规模数据下 L1 和 LPIPS 损失足以保证清晰度，移除 GAN 损失提升了训练稳定性。
    *   **语义对齐（Semantic Alignment）**：引入 DINOv2 中间层特征作为引导。
    *   **损失函数**：结合了边缘余弦相似度损失（$L_{mcos}$）和边缘距离矩阵相似度损失（$L_{mdms}$），确保潜空间在保持重建质量的同时具备生成友好的语义结构。
*   **数据工程**：
    *   **规模化**：使用数十亿张图像进行训练。
    *   **富文本增强**：利用 OCR 过滤筛选高字符密度图像，并开发合成渲染引擎，生成 5-20 像素大小的微小字符图像进行监督。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用重建**：ImageNet (256p) 和 FFHQ (1K)。
    *   **富文本重建**：提出 **OmniDoc-TokenBench**，包含约 3000 张涵盖学术论文、海报、网页、财务报表等 9 类真实文档图像。
*   **基准指标**：
    *   传统指标：PSNR, SSIM, LPIPS, FID。
    *   文本专用指标：**归一化编辑距离（NED）**，通过对比原始图与重建图的 OCR 识别结果来衡量文本可读性。
*   **对比方法**：对比了 FLUX.1/2, HunyuanVideo, Cosmos-0.1, Wan2.1/2.2, LTX-Video 等主流开源 VAE 模型。
*   **下游任务**：在 ImageNet 上训练 SiT（Scalable Interpolant Transformers）以验证潜空间的可扩散性。

### 4. 资源与算力
*   **算力说明**：报告中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **效率体现**：文中强调了其架构在超高分辨率下的高吞吐量，以及轻量级编码器对下游 DiT 训练延迟的优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 $f16$ 到 $f32$、通道数从 64 到 192 的多个模型变体。
    *   进行了关于跳跃连接（NSC vs LSC vs GSC）、语义编码器选择（DINOv2, MAE 等）以及对齐层选择的消融实验。
*   **充分性与公平性**：实验对比非常广泛，涵盖了目前市面上几乎所有最先进的高压缩 VAE。通过引入 NED 指标，客观地揭示了传统像素指标在文本重建评估上的局限性。下游 DiT 实验在相同代码库和超参数下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能领先**：Qwen-Image-VAE-2.0 在 $f16$ 和 $f32$ 级别均达到 SOTA。其 $f16c128$ 模型的文本重建能力（NED）甚至超过了许多 $f8$ 模型。
*   **解决困境**：证明了通过增加通道维度并配合语义对齐，可以克服高压缩率带来的收敛难题。
*   **文本修复**：合成渲染数据的引入显著提升了模型在极小字符下的重建表现，解决了高压缩 VAE 常见的“字符模糊”问题。

### 7. 优点
*   **架构简洁高效**：GSC 的引入简单且有效，Attention-Free 设计极大地提升了处理大图的内存效率。
*   **基准贡献**：OmniDoc-TokenBench 和 NED 指标为视觉 Tokenizer 的评估提供了更具实际意义的维度（尤其是针对文档理解类模型）。
*   **可扩散性强**：通过语义对齐策略，使得高维潜空间在扩散模型训练中收敛更快、质量更高。

### 8. 不足与局限
*   **算力透明度**：缺乏具体的训练资源消耗数据，外部研究者难以评估其复现成本。
*   **OCR 依赖性**：NED 指标高度依赖于所使用的 OCR 模型（PP-OCRv5），虽然文中通过对比原始图 OCR 结果来抵消偏差，但 OCR 模型本身的识别上限可能限制评估的绝对精度。
*   **应用限制**：虽然在重建上表现优异，但在极端压缩（如 $f32$）下，尽管 NED 较高，视觉上仍可能存在微小伪影，这在追求极致写实的场景中可能仍需权衡。

（完）
