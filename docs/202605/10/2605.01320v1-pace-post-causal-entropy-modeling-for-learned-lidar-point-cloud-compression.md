---
title: "PACE: Post-Causal Entropy Modeling for Learned LiDAR Point Cloud Compression"
title_zh: PACE：用于学习型激光雷达点云压缩的后因果熵建模
authors: "Jiahao Zhu, Kang You, Dandan Ding, Zhan Ma"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01320v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 用于点云压缩的学习熵模型和非因果骨干网络
tldr: "针对LiDAR点云压缩中熵建模解码延迟高和权衡僵化的问题，本文提出PACE框架。通过将上下文聚合重构为非因果骨干网络，并将因果性限制在轻量级、阶段可扩展的预测器中，消除了重复计算。PACE在实现SOTA压缩效率的同时，将解码延迟降低了90%以上，且支持无缝的性能-延迟调节，极具实用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01320v1/fig-001.webp\", \"caption\": \"Figure 5. Comparison of rate-distortion (R-D) performance across SemanticKITTI, Ford, nuScenes, and QNX datasets. G-PCC (O) and G-PCC (P) represent the Octree and Predgeom configurations of G-PCC, respectively.\", \"page\": 6, \"index\": 1, \"width\": 1007, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2605.01320v1/fig-002.webp\", \"caption\": \"Figure 1. Visualization of samples in KITTI, Ford, nuScenes, and QNX LiDAR point cloud datasets.\", \"page\": 11, \"index\": 2, \"width\": 991, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.01320v1/fig-003.webp\", \"caption\": \"Figure 2. Framework of our proposed PACE. The LiDAR point cloud is first preprocessed for efficient octree construction, optionally using intrinsic-aware or spherical-style preprocessing depending on whether sensor intrinsics are available. Then, nodes are organized into windows for our post-causal processing: the non-causal backbone applies inter-level context aggregation, and the stage-scalable predictor exploits intra-level causality, generating the occupancy probabilities for subsequent arithmetic coding. GPE and ECE refer to the introduced graph-based positional encoding and elastic causal embedding, respectively.\", \"page\": 3, \"index\": 3, \"width\": 833, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2605.01320v1/fig-004.webp\", \"caption\": \"Figure 4. Stage-scalable predictor. The inter-level context yielded by the non-causal backbone is shared across all stages, and the intra-level context (O<s, s ∈ [1, S]) from previous stages is embedded and fused to predict the target stage Os. The autoregressive mode is presented as an illustrative example.\", \"page\": 5, \"index\": 4, \"width\": 960, \"height\": 1286}]"
motivation: 现有的八叉树熵建模因多阶段因果上下文聚合导致解码延迟巨大，且缺乏灵活的性能-延迟调节能力。
method: 将上下文聚合解耦为非因果骨干网络，并采用轻量级、阶段可扩展的预测器来处理因果概率预测。
result: "PACE在保持SOTA压缩性能的同时，将自回归模式下的解码延迟降低了90%以上，并实现了显著的BD-BR节省。"
conclusion: 该框架通过解耦设计有效解决了LiDAR压缩的实时性瓶颈，为自动驾驶等实际应用提供了高效灵活的熵建模方案。
---

## 摘要
激光雷达点云压缩对于自动驾驶系统处理来自高分辨率传感器的大规模数据至关重要。虽然基于八叉树结构的学习型熵建模具有较高的压缩增益，但它面临两个关键瓶颈：1) 因果式、多阶段上下文建模导致的极高延迟，尤其是在解码过程中；2) 僵化的性能-延迟权衡，使得单一模型无法适应不同的约束条件。这些局限性源于上下文聚合骨干网络与概率预测之间的紧密耦合。为了解决这一问题，我们提出了 PACE，这是一个将祖先上下文聚合重新表述为非因果骨干网络的新框架，并将因果性限制在轻量级、阶段可扩展的预测器中，从而消除了重复的骨干网络执行并降低了计算开销。该预测器支持任意数量的预测阶段，无需重新加载参数即可在多种性能-延迟权衡之间实现无缝切换。实验表明，PACE 在压缩效率方面达到了新的 SOTA 水平，实现了显著的 BD-BR 节省，并在自回归模式下将解码延迟降低了 90% 以上，在实际应用中极具吸引力。

## Abstract
LiDAR point cloud compression is vital for autonomous systems to handle massive data from high-resolution sensors. While learned entropy modeling built upon octree structures yields high compression gains, it faces two critical bottlenecks: 1) prohibitive latency, particularly during decoding, caused by causal, multi-stage context modeling; and 2) a rigid performance-latency trade-off, preventing a single model from adapting to varying constraints. These limitations stem from the tight coupling between context aggregation backbone and probability prediction. To address this, we propose PACE, a new framework that reformulates ancestral context aggregation as a non-causal backbone and confines causality to a lightweight, stage-scalable predictor, eliminating repetitive backbone executions and reducing computational overhead. The predictor supports an arbitrary number of prediction stages, supporting seamless adaptation across diverse performance-latency trade-offs without reloading parameters. Experiments demonstrate that PACE sets a new state-of-the-art in compression efficiency, achieving notable BD-BR savings and reducing decoding latency by over 90% in autoregressive mode, highly attractive for practical applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《PACE: Post-Causal Entropy Modeling for Learned LiDAR Point Cloud Compression》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
LiDAR（激光雷达）点云数据量巨大，高效的压缩技术（LPCC）对自动驾驶和机器人系统至关重要。目前基于八叉树（Octree）的学习型熵建模方法虽然压缩率高，但存在两个痛点：
*   **高解码延迟：** 现有的多阶段或自回归模型采用“全因果”架构，每预测一个阶段或一个节点，都需要重复执行沉重的骨干网络（Backbone），导致延迟随阶段数线性增长。
*   **权衡僵化：** 模型在训练后阶段数固定，无法根据实际带宽或算力需求动态调整性能与延迟之间的平衡。

**PACE 的核心动机**是实现一种“后因果（Post-Causal）”建模范式，将计算量大的上下文聚合与轻量级的概率预测解耦，从而在保持高压缩率的同时大幅降低延迟。

### 2. 方法论
PACE 框架主要由三个关键部分组成：
*   **内参感知预处理（Intrinsic-aware Preprocessing）：** 利用传感器的激光俯仰角和垂直偏移等内参，将笛卡尔坐标转换为混合圆柱-光束（Cylindrical-beam）表示，使八叉树结构更符合硬件扫描特性。
*   **非因果骨干网络（Non-Causal Backbone）：** 
    *   **核心思想：** 将层间（祖先节点）的上下文聚合定义为非因果过程。
    *   **技术细节：** 引入**基于图的分布编码（GPE）**来捕捉 3D 结构先验，并使用**无掩码注意力机制（Unmasked Attention）**在窗口内并行提取特征。由于不涉及层内因果性，骨干网络在解码时只需运行一次。
*   **阶段可扩展预测器（Stage-Scalable Predictor）：**
    *   **核心思想：** 将因果约束限制在轻量级预测器中。
    *   **技术细节：** 采用**弹性因果嵌入（ECE）**和**选择性状态空间模型（SSM，基于 Mamba）**。它支持从单阶段（全并行）到自回归（逐节点）的任意阶段划分，且无需重新加载参数即可在线切换。

### 3. 实验设计
*   **数据集：** 涵盖了四个代表性基准：
    1.  **SemanticKITTI：** 64线雷达，大规模室外场景。
    2.  **Ford：** 64线雷达，MPEG 标准测试序列。
    3.  **nuScenes：** 32线雷达，多模态自动驾驶数据。
    4.  **QNX：** 16线雷达，极稀疏场景。
*   **Benchmark 与对比方法：**
    *   **传统标准：** G-PCC (TMC13) 的 Octree 和 Predgeom 配置。
    *   **学习型方法：** 包括基于稀疏张量的（SparsePCGC, Unicorn, RENO）和基于八叉树的（OctAttention, TopNet, EHEM, ASRL, SCP, LPCM）。

### 4. 资源与算力
*   **硬件环境：** 使用了一台配备 **NVIDIA RTX 4090 GPU** 和 **Intel i9-13900K CPU** 的工作站。
*   **训练时长：** 
    *   SemanticKITTI 和 QNX：10 个 epoch。
    *   nuScenes：25 个 epoch。
    *   Ford：50 个 epoch。
*   **模型规模：** 参数量约为 10.57M，内存占用约 0.58GB（以 L=14 为例）。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个不同线数、不同稀疏度的公开数据集上进行了测试，覆盖了从低比特率到高比特率的完整区间。
*   **消融实验：** 针对预处理方式（笛卡尔 vs 圆柱 vs 内参感知）、建模范式（全因果 vs 后因果）、GPE 模块的有效性、以及阶段可扩展性（1-stage 到 AR 模式）做了详尽的对比。
*   **公平性：** 实验采用了 MPEG 共同测试条件（CTC），并与最新的 SOTA 方法（如 2025 年的 TopNet, RENO）进行了直接对比，评估指标包括 BD-BR、PSNR 和实际解码时间，实验设计客观且充分。

### 6. 主要结论与发现
*   **压缩效率：** PACE 在所有数据集上均达到 SOTA。相比 G-PCC，单阶段模式节省 39%~51% 的 BD-BR，多阶段/自回归模式节省 45%~55%。
*   **延迟优化：** 在自回归模式下，得益于后因果设计和 Mamba 的步进推理，PACE 的**解码延迟比 TopNet 等同类模型降低了 90% 以上**。
*   **灵活性：** 单一模型即可支持多种性能-延迟权衡，例如在 Ford 数据集上，通过将阶段从 1 增加到 16，BD-BR 节省从 50.19% 提升至 54.23%，而解码时间仅略微增加。

### 7. 优点
*   **架构创新：** 成功解耦了重型骨干网络与因果预测，解决了学习型熵建模长期存在的“高增益必高延迟”的矛盾。
*   **内参利用：** 相比于盲目投影，内参感知的预处理显著提升了对特定雷达扫描模式的压缩效率。
*   **通用性强：** 阶段可扩展预测器允许模型在不同算力约束的设备上无缝部署，具有极强的工程实用价值。

### 8. 不足与局限
*   **编码端延迟：** 虽然解码速度极快，但在自回归（AR）模式下，为了保证算术编码的位一致性，编码端目前仍采用串行处理，导致编码时间较长（约 10.89s），尚未实现完全的实时编码。
*   **极稀疏场景挑战：** 在 QNX 等极稀疏数据集上，虽然表现优异，但在某些高比特率点上略逊于基于预测树（Predtree）的规则方法，说明八叉树在极度稀疏时的上下文建模仍有提升空间。
*   **硬件依赖：** 性能提升部分依赖于 Mamba 算子的优化，在不支持相关算子的老旧硬件上可能无法发挥速度优势。

（完）
