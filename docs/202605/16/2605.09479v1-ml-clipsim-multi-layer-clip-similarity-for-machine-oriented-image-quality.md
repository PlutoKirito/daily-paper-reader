---
title: "ML-CLIPSim: Multi-Layer CLIP Similarity for Machine-Oriented Image Quality"
title_zh: ML-CLIPSim：面向机器图像质量的多层 CLIP 相似度
authors: "Feng Ding, Haisheng Fu, Jie Liang, Qihan Xu, Siyu Zhu, Jingning Han"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09479v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 面向机器视觉质量和学习型图像压缩的可微质量度量指标
tldr: 本研究探讨了面向机器任务的全参考图像质量评估，旨在衡量图像对下游模型信息的保留程度。为此，作者构建了PCMP数据集，并提出了ML-CLIPSim指标。该指标基于冻结的CLIP视觉编码器，通过聚合多层中间块令牌相似度和全局嵌入来评估质量。实验证明，ML-CLIPSim在机器偏好对齐上优于传统指标，且在人类质量预测和图像压缩任务中表现出色。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09479v1/fig-001.webp\", \"caption\": \"Figure 3: Rate–task curves across ImageNet, VOC, and COCO downstream tasks. MLCLIPSim consistently achieves the best trade-off across tasks.\", \"page\": 11, \"index\": 1, \"width\": 695, \"height\": 764}, {\"url\": \"assets/figures/arxiv/2605.09479v1/fig-002.webp\", \"caption\": \"Table 5: Ablation results on PCMP. Higher is better.\", \"page\": 14, \"index\": 2, \"width\": 652, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2605.09479v1/fig-003.webp\", \"caption\": \"Figure 4: VLM evaluation on SEEDBench and POPE using InternVL3-1B and Qwen2.5VL-3B-Instruct. ML-CLIPSim achieves the best rate–task performance across benchmarks.\", \"page\": 12, \"index\": 3, \"width\": 688, \"height\": 510}, {\"url\": \"assets/figures/arxiv/2605.09479v1/fig-004.webp\", \"caption\": \"Table 1: Performance comparison on the PCMP test set. Higher is better.\", \"page\": 9, \"index\": 4, \"width\": 733, \"height\": 313}]"
motivation: 传统的图像质量评估指标主要针对人类视觉，而忽略了图像在面向机器任务中的信息保留能力。
method: 提出了ML-CLIPSim指标，利用冻结的CLIP视觉编码器提取多层块令牌相似度和全局图像嵌入，并构建了基于预测一致性投票的PCMP数据集。
result: 实验结果显示，ML-CLIPSim在机器偏好基准上优于现有感知指标，同时在人类IQA数据集和学习型图像压缩任务中也表现出极强的竞争力。
conclusion: ML-CLIPSim是一种有效的面向机器的图像质量度量工具，能够显著提升下游任务的率失真性能，并兼顾人类感知质量。
---

## 摘要
我们从以机器为中心的视角研究全参考图像质量评估，其中图像的评估标准是其对下游模型信息的保留程度。我们将面向机器的质量公式化为一种潜在的机器效用，并通过成对的预测一致性比较来进行近似。为此，我们构建了 PCMP，这是一个由 PSNR 匹配的失真对组成的数据集，并由多个预训练模型的一致性投票进行标注。我们进一步提出了 ML-CLIPSim，这是一种基于冻结的 CLIP 视觉编码器构建的可微质量指标，它聚合了中间层补丁标记（patch-token）相似度和全局图像嵌入。在机器偏好基准、人类 IQA 数据集和学习型图像压缩上的实验表明，与传统的保真度和感知指标相比，ML-CLIPSim 更好地契合了面向机器的偏好，同时在人类质量预测方面也保持了竞争力。当作为压缩失真项使用时，它改善了多个下游任务中的速率-任务（rate-task）权衡。

## Abstract
We study full-reference image quality assessment from a machine-centric perspective, where images are evaluated by how well they preserve information for downstream models. We formulate machine-oriented quality as a latent machine utility and approximate it through pairwise predictive-consistency comparisons. To this end, we construct PCMP, a dataset of PSNR-matched distortion pairs labeled by consistency votes from multiple pretrained models. We further propose ML-CLIPSim, a differentiable quality metric built on a frozen CLIP visual encoder, which aggregates intermediate patch-token similarities and global image embeddings. Experiments on machine-preference benchmarks, human-IQA datasets, and learned image compression show that ML-CLIPSim better aligns with machine-oriented preferences than conventional fidelity and perceptual metrics, while remaining competitive for human quality prediction. Used as a compression distortion term, it improves rate--task trade-offs across multiple downstream tasks.

---

## 论文详细总结（自动生成）

### ML-CLIPSim: 面向机器图像质量的多层 CLIP 相似度论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的图像质量评估（IQA）指标（如 PSNR、SSIM）或感知指标（如 LPIPS、DISTS）主要针对人类视觉感知设计。然而，在现代视觉流水线中，图像的最终“消费者”往往是机器模型（如分类器、检测器、分割器）。
*   **核心矛盾**：具有相似保真度或感知得分的图像，在下游机器任务中的表现可能大相径庭。
*   **研究目标**：定义并量化“面向机器的图像质量”，即图像保留支持稳定机器推理的信息的能力（称为“潜在机器效用”），并开发一个可微的、任务无关的全参考度量指标。

#### 2. 方法论：核心思想与技术细节
论文提出了 **ML-CLIPSim** 指标及其配套的训练框架：
*   **潜在机器效用（Latent Machine Utility）**：将其定义为多个下游模型预测一致性的期望。由于该效用无法直接观测，作者通过成对比较（Pairwise Comparison）来近似。
*   **PCMP 数据集构建**：
    *   **PSNR 匹配对**：为了避免模型只学习到“失真越小质量越好”的简单逻辑，PCMP 专门挑选 PSNR 接近（差异 $\le 0.5$ dB）但内容/失真类型不同的图像对。
    *   **多模型投票**：利用分类（ResNet, ViT）、检测（YOLO, Faster R-CNN）和分割（DeepLab, Mask R-CNN）等 7 个模型对图像对进行预测一致性评分，生成软偏好标签（Vote Ratio）。
*   **ML-CLIPSim 架构**：
    *   **多层 Token 相似度**：提取冻结 CLIP (ViT-B/16) 视觉编码器中间层的 Patch-token，计算参考图与失真图之间的余弦相似度，捕捉局部和层次化的视觉变化。
    *   **全局相似度**：利用 CLIP 的全局图像嵌入（Global Embedding）捕捉高层语义一致性。
    *   **轻量级聚合**：通过可学习的分组层权重和门控机制，将多层 Token 相似度和全局相似度融合为一个标量得分。
*   **训练目标**：使用二元交叉熵（BCE）损失函数，根据 PCMP 的成对偏好标签训练聚合参数。

#### 3. 实验设计
*   **数据集**：
    *   **PCMP（自建）**：包含 327,241 个 PSNR 匹配的失真对，用于训练和基础评估。
    *   **MPD（外部基准）**：用于验证机器偏好的泛化能力。
    *   **TID2013 / LIVE**：用于评估与人类视觉感知的一致性。
    *   **下游任务**：ImageNet 分类、VOC/COCO 检测与分割、VLM 评估（SEEDBench, POPE）。
*   **对比方法（Benchmarks）**：
    *   传统指标：PSNR, MS-SSIM。
    *   感知指标：LPIPS, DISTS。
    *   机器/语义指标：CLIPScore, UG-ICM（强力任务无关基准）。
*   **应用场景**：将 ML-CLIPSim 作为失真项，训练学习型图像压缩模型（MLIC++ 架构）。

#### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 64，训练 5 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但由于 CLIP 骨干网络是冻结的，仅训练极少数聚合参数，预计度量指标本身的训练算力需求较低；而图像压缩模型的训练则涉及标准的大规模视觉任务算力。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   数据集涵盖了从基础视觉任务（分类、检测）到复杂的多模态大模型（VLM）评估。
    *   在图像压缩任务中，测试了多种比特率（$\lambda$ 取值范围广）和多种架构。
    *   进行了消融实验，验证了多层特征、分组权重和软标签的有效性。
*   **客观性与公平性**：通过 PSNR 匹配对进行训练，有效排除了低级保真度对实验结果的干扰。使用了多个第三方基准（如 MPD）进行交叉验证，实验设计较为严谨、充分。

#### 6. 主要结论与发现
*   **机器偏好对齐**：ML-CLIPSim 在 PCMP 和 MPD 基准上的相关性显著优于 LPIPS 和 CLIPScore。
*   **压缩性能提升**：在相同的比特率下，使用 ML-CLIPSim 优化的压缩模型在下游任务（如 ImageNet 分类）中实现了高达 **-76.1% 的 BD-Rate 增益**（相比 MSE 优化）。
*   **人类感知兼容性**：尽管是为机器设计的，ML-CLIPSim 在人类 IQA 数据集上依然保持了与 LPIPS 相当甚至更好的竞争力，证明了机器效用与人类感知之间存在部分重叠。

#### 7. 优点
*   **任务无关性**：不需要针对特定下游任务进行微调，具有很强的通用性。
*   **可微性**：可以直接集成到深度学习模型的损失函数中（如图像压缩、增强）。
*   **结构化特征利用**：相比只用 CLIP 全局特征，多层 Token 聚合能更细腻地捕捉影响机器推理的局部伪影。

#### 8. 不足与局限
*   **依赖预训练骨干**：指标的有效性高度依赖于 CLIP 预训练特征的质量，若 CLIP 未涵盖的特殊领域（如医学影像），效果可能受限。
*   **计算开销**：虽然聚合层轻量，但在推理时需要运行完整的 CLIP 视觉编码器，相比 PSNR 等传统指标，计算成本较高。
*   **失真覆盖偏差**：尽管 PCMP 包含多种失真，但仍可能无法完全覆盖未来新型生成模型或压缩算法产生的特有伪影。

（完）
