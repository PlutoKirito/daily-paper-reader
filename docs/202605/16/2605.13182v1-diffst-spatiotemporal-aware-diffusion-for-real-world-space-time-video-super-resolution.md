---
title: "DiffST: Spatiotemporal-Aware Diffusion for Real-World Space-Time Video Super-Resolution"
title_zh: DiffST：面向真实场景时空视频超分辨率的时空感知扩散模型
authors: "Zheng Chen, Ruofan Yang, Jin Han, Dehua Song, Zichen Zou, Chunming He, Yong Guo, Yulun Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13182v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 高效的时空感知视频扩散框架
tldr: 针对现有扩散模型在时空视频超分辨率（STVSR）中推理效率低且时空信息利用不足的问题，本文提出DiffST框架。该方法通过单步采样技术和全视频处理显著提升效率，并引入跨帧上下文聚合（CFCA）与视频表示引导（VRG）模块来增强时空特征融合。实验证明，DiffST在真实场景下表现优异，且推理速度比同类方法快约17倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13182v1/fig-001.webp\", \"caption\": \"Figure 7: Visual comparison on synthetic (UDM10 [37] and Vid4 [28]) datasets.\", \"page\": 15, \"index\": 1, \"width\": 833, \"height\": 1013}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-002.webp\", \"caption\": \"Figure 1: Performance comparison of STVSR methods. The right-side quantitative scores are reported on the real-world dataset (RealVSR [56]). Runtime is measured on an A100 GPU using an output video of resolution 33×720×1280. Compared with VEnhancer [14], DiffST achieves superior performance with an approximately 17× inference speedup.\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-003.webp\", \"caption\": \"Table 4: Quantitative results. The best and second best results are colored with red and blue.\", \"page\": 8, \"index\": 3, \"width\": 818, \"height\": 877}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the proposed DiffST. Built upon a pre-trained video generation diffusion model, DiffST performs one-step sampling to process the entire video directly. The cross-frame context aggregation (CFCA) module aggregates information from multiple frames to generate intermediate frames. The video representation guidance (VRG) module extracts a video-level representation to guide the restoration process. To better match real-world conditions, we adopt multiple spatial degradations, combined with frame subsampling (temporal).\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-005.webp\", \"caption\": \"Figure 5: Qualitative results on synthetic (UDM10 [37]) and real-world (MVSR4x [43] and RealVSR [56]) benchmarks. Our method achieves impressive performance.\", \"page\": 9, \"index\": 5, \"width\": 833, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-006.webp\", \"caption\": \"Figure 6: Consistency comparison with other STVSR methods. We stack the green dots on each frame along the temporal axis. Our method produces richer details and smoother transitions.\", \"page\": 9, \"index\": 6, \"width\": 824, \"height\": 180}, {\"url\": \"assets/figures/arxiv/2605.13182v1/fig-007.webp\", \"caption\": \"Figure 8: Visual comparison on real-world (MVSR4x [43] and RealVSR [56]) datasets.\", \"page\": 16, \"index\": 7, \"width\": 833, \"height\": 1012}]"
motivation: 现有的基于扩散模型的STVSR方法面临推理效率低下和时空信息利用不充分的挑战。
method: 采用单步采样扩散模型，并结合跨帧上下文聚合（CFCA）和视频表示引导（VRG）模块来优化时空特征提取。
result: DiffST在真实世界STVSR任务中取得了领先性能，且推理速度比之前的扩散模型方法快约17倍。
conclusion: 该研究为高效且高质量的真实场景时空视频超分辨率提供了一种极具竞争力的扩散模型解决方案。
---

## 摘要
基于扩散的模型在视频超分辨率（VSR）和视频插帧（VFI）中表现出了强大的性能。然而，它们在耦合的时空视频超分辨率（STVSR）设置中的作用仍然有限。现有的基于扩散的 STVSR 方法存在两个问题：（1）推理效率低；（2）时空信息利用不足。这些局限性阻碍了其部署。为了解决这些问题，我们引入了 DiffST，这是一个用于真实场景 STVSR 的高效时空感知视频扩散框架。为了提高效率，我们调整了预训练的扩散模型以进行单步采样，并直接处理整个视频，而不是对单个帧进行操作。此外，为了增强时空信息的利用，我们引入了跨帧上下文聚合（CFCA）和视频表示引导（VRG）。CFCA 模块聚合多个关键帧的信息以生成中间帧。VRG 模块提取视频级全局特征来引导扩散过程。大量实验表明，DiffST 在真实场景 STVSR 任务上取得了领先的结果。它还保持了较高的推理效率，运行速度比以前基于扩散的 STVSR 方法快约 17 倍。代码可在以下网址获取：https://github.com/zhengchen1999/DiffST。

## Abstract
Diffusion-based models have shown strong performance in video super-resolution (VSR) and video frame interpolation (VFI). However, their role in the coupled space-time video super-resolution (STVSR) setting remains limited. Existing diffusion-based STVSR approaches suffer from two issues: (1) low inference efficiency and (2) insufficient utilization of spatiotemporal information. These limitations impede deployment. To address these issues, we introduce DiffST, an efficient spatiotemporal-aware video diffusion framework for real-world STVSR. To improve efficiency, we adapt a pre-trained diffusion model for one-step sampling and process the entire video directly rather than operating on individual frames. Furthermore, to enhance spatiotemporal information utilization, we introduce cross-frame context aggregation (CFCA) and video representation guidance (VRG). The CFCA module aggregates information across multiple keyframes to produce intermediate frames. The VRG module extracts video-level global features to guide the diffusion process. Extensive experiments show that DiffST obtains leading results on real-world STVSR tasks. It also maintains high inference efficiency, running about 17$\times$ faster than previous diffusion-based STVSR methods. Code is available at: https://github.com/zhengchen1999/DiffST.

---

## 论文详细总结（自动生成）

以下是对论文《DiffST: Spatiotemporal-Aware Diffusion for Real-World Space-Time Video Super-Resolution》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：时空视频超分辨率（STVSR）旨在同时提升视频的空间分辨率和帧率。虽然基于扩散的模型在视频修复领域表现出色，但在 STVSR 任务中面临两大挑战。
*   **核心问题**：
    1.  **推理效率低下**：现有扩散模型通常需要多步迭代采样，且往往逐帧处理，导致计算成本随视频长度激增。
    2.  **时空信息利用不足**：许多方法仅利用相邻两帧（如传统插帧设置），无法捕获长程时空上下文，且对视频全局信息的利用较为隐式。
*   **研究动机**：开发一个既能保持扩散模型高生成质量，又能实现高效推理并充分利用多帧时空信息的统一框架。

### 2. 方法论
DiffST 基于预训练的视频生成模型（WAN），核心思想是将 STVSR 转化为一个**单步采样**的条件生成过程。
*   **单步采样架构**：不同于从纯噪声开始，DiffST 将初步处理后的视频潜变量作为起点，利用流匹配（Flow-matching）架构和 Euler ODE 求解器，仅需一步采样即可生成修复结果，极大提升了速度。
*   **跨帧上下文聚合 (CFCA)**：
    *   不再局限于两帧输入。通过估计前向和后向光流，将多帧信息进行时空传播与融合。
    *   利用学习到的融合网络（$\Psi_\theta$）整合原始输入、前向融合帧和后向融合帧，为后续扩散模块提供更准确的中间帧预测。
*   **视频表示引导 (VRG)**：
    *   从输入视频中均匀提取 $N_k$ 个关键帧，通过预训练图像编码器提取空间特征。
    *   使用多头交叉注意力（MHCA）将这些特征聚合成全局视频表示 $e_v$。
    *   将 $e_v$ 与文本嵌入融合，作为扩散过程的全局 Prompt，提供显式的时空一致性约束。
*   **训练目标**：结合了潜空间 MSE 损失、像素空间 MSE 损失、感知损失（LPIPS）以及双向时间一致性损失，以平衡保真度、视觉质量和运动平滑度。

### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 HQ-VSR 数据集，模拟真实世界的空间退化和时间下采样。
    *   **测试**：合成数据集（UDM10, Vid4）和真实场景数据集（MVSR4x, RealVSR）。
*   **Benchmark 与对比方法**：
    *   **两阶段流水线**：组合了多种 SOTA VSR 模型（STAR, SeedVR, SeedVR2）和 VFI 模型（BiM-VFI, MoMo, TLBVFI）。
    *   **单阶段模型**：对比了 VEnhancer（基于扩散的 STVSR）、Zooming Slow-Mo 和 BF-STVSR。
*   **评估指标**：涵盖保真度（PSNR/SSIM）、感知质量（LPIPS, CLIP-IQA, DOVER）和时间一致性（tOF, tLP, FloLPIPS）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：迭代次数为 10,000 次，Batch size 为 4，学习率为 $5 \times 10^{-5}$。
*   **推理效率**：处理一段 33 帧、720p 分辨率的视频，DiffST 仅需 **7.12 秒**，比 VEnhancer 快约 **17 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准数据集上进行了详尽的定量和定性对比。
*   **消融实验**：针对 CFCA 模块、VRG 模块以及不同的引导 Prompt 策略（纯文本 vs 纯视频 vs 视频+文本）分别做了分解实验。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了单阶段模型，还穷举了多种 VSR+VFI 的组合方案，并引入了专门针对视频一致性的评价指标，证明了其在效率和质量上的双重优势。

### 6. 主要结论与发现
*   **效率突破**：通过单步采样和全视频处理，DiffST 解决了扩散模型在视频任务中推理缓慢的顽疾。
*   **质量提升**：显式的多帧聚合（CFCA）比简单的双帧插值能提供更清晰的纹理；全局视频引导（VRG）显著减少了视频修复中的闪烁现象。
*   **领先性能**：在真实世界数据集（如 RealVSR）上，DiffST 在感知指标（DOVER）上大幅领先现有方法。

### 7. 优点
*   **极速推理**：17 倍的加速比使其在实际应用部署中极具竞争力。
*   **时空感知设计**：CFCA 和 VRG 模块有效地将视频的局部细节与全局上下文结合起来。
*   **端到端优化**：避免了 VSR 和 VFI 串联导致的误差累积和信息共享缺失。

### 8. 不足与局限
*   **极端退化限制**：在面对极度严重的模糊或极其复杂的非线性运动时，单步采样可能仍难以完美恢复所有细节。
*   **VAE 瓶颈**：虽然扩散过程是一步，但 VAE 的编码和解码过程在整体耗时中仍占有一定比例。
*   **模型依赖**：性能在很大程度上依赖于预训练视频生成模型（WAN）的先验知识，对于完全脱离该先验分布的特殊视频可能表现受限。

（完）
