---
title: Rejection Sampling is Optimal for Relative Entropy Coding
authors: "Spencer Hill, Fady Alajaji, Tamás Linder, Gergely Flamich"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23076v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 相对熵编码与函数信息下界
tldr: 本研究探讨了相对熵编码中互信息下界与实际比特率之间存在对数差距的问题。作者提出了一种基于拒绝采样的“环投编码”方法，证明了其比特率仅比功能信息下界多出约1.44比特。该成果证明了拒绝采样在相对熵编码中的最优性，并将互信息下界的差距缩小至目前最紧致的水平，同时恢复了二阶冗余的渐近结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23076v1/fig-001.webp\", \"caption\": \"Fig. 2. A visualization of the acceptance region in rejection sampling for an additive white Gaussian noise channel Y = X+N with X ∼ N (0, 0.75) and N ∼ N (0, 0.25), where X and Y are truncated to a large but finite range to ensure a bounded density ratio. Here, x = −0.5 and the two proposal samples are Y1 = 0.3, Y2 = −0.4 with heights L1 = 1.5 and L2 = 1.1, where we use the shorthand Li ≜ MUi. We see that x ̸∈ S1 but x ∈ S2, meaning Y1 is rejected and Y2 is accepted. The PX -probability of hitting the set Si is exactly wYi (Li).\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 233}]"
motivation: 旨在解决传统互信息下界在相对熵编码中不紧致的问题，寻找更准确的信息度量方式。
method: 构造了一种名为“环投编码”的拒绝采样编码方案，并基于功能信息进行理论分析。
result: 证明了该编码方案的比特率达到了功能信息下界的紧致性，在奇异信道下仅有1.45比特的差距。
conclusion: 拒绝采样是相对熵编码的最优策略，该研究为信息论中的编码效率提供了新的理论上限。
---

## Abstract
In relative entropy coding, a sender aims to design a stochastic code such that, on input $X \sim P_X$, the receiver can generate a sample $Y \sim P_{Y \mid X}$. It is a standard result that (1) this requires at least $I(X; Y)$ bits, (2) the lower bound is achievable within a logarithmic gap, and (3) this gap cannot be reduced in general. The necessity of the gap suggests that the mutual information is not the correct information measure to quantify the rate of relative entropy coding.   A potential alternative emerged in the work of Flamich et al. (2025), who proved a tighter lower bound of $I_F(X \to Y)$, a quantity we call the functional information. In this paper, we show that this lower bound is tight by constructing the ring toss code, an encoding method for rejection sampling which uses at most $I_F(X \to Y) + \log e$ bits. This demonstrates that rejection sampling is optimal for relative entropy coding. Our result implies that the classical mutual information lower bound is achievable within $\log(I(X; Y) + 1) + 2.45$ bits in general and within $1.45$ bits for singular channels, which are both the tightest bounds of their kind to date. Moreover, our one-shot result also recovers Sriramu and Wagner's asymptotic results on the second-order redundancy of relative entropy codes.

---

## 论文详细总结（自动生成）

这篇论文题为《Rejection Sampling is Optimal for Relative Entropy Coding》（拒绝采样在相对熵编码中是最优的），由来自女王大学和帝国理工学院的研究者共同完成。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在相对熵编码（Relative Entropy Coding, REC，又称信道模拟）中，发送者希望通过共享随机性，使接收者能根据输入 $X$ 生成样本 $Y \sim P_{Y|X}$。
*   **研究背景**：传统理论认为 REC 的比特率下界是互信息 $I(X; Y)$，但实际方案总存在一个对数级的冗余（Gap）。这暗示互信息可能不是衡量 REC 速率的最准确指标。
*   **动机**：近期研究提出了“功能信息”（Functional Information, $I_F$）作为更紧致的下界。本论文旨在证明该下界是可达的，从而确立拒绝采样在 REC 中的最优地位。

### 2. 方法论
论文提出了一种名为**环投编码（Ring Toss Code）**的新型编码方案，其核心是对标准拒绝采样（Rejection Sampling）的索引进行重新编码。
*   **核心思想**：传统的拒绝采样编码直接发送索引 $K$，这忽略了解码器已知的共享随机性信息。环投编码利用解码器能计算每个提议样本（Proposal Sample）被接受的概率这一特性，构建了一个更高效的条件分布进行编码。
*   **关键技术细节**：
    *   **宽度函数（Width Function）**：定义 $w_Q(\ell)$ 来衡量密度比的超水平集的大小，这是计算功能信息的基础。
    *   **编码分布 $Q_{K|Z}$**：发送者不直接使用几何分布，而是根据共享随机性 $Z$ 动态调整编码分布。
    *   **算法流程**：
        1.  发送者和接收者共享无限序列的随机样本。
        2.  发送者运行拒绝采样找到第一个满足条件的索引 $K$。
        3.  发送者使用基于宽度函数构建的算术编码（或前缀码）对 $K$ 进行编码。
        4.  解码器根据接收到的比特流和共享随机性恢复 $K$，进而得到样本 $Y$。

### 3. 实验设计与对比
由于这是一篇**理论信息论论文**，其“实验”主要体现为数学证明和针对特定信道模型的理论分析，而非大规模数据集上的工程实验。
*   **对比基准（Benchmark）**：
    *   **互信息下界**：$I(X; Y)$。
    *   **先前最优方案**：如 Poisson Functional Representation（冗余为 $\log(I+1) + 4$）和 Greedy Rejection Sampling。
*   **分析场景**：
    *   **奇异信道（Singular Channels）**：如二进制擦除信道、均匀加性噪声信道。
    *   **一般信道**：具有有界密度比的任意信道。
*   **对比结果**：环投编码将冗余从以往的约 2.74-4 比特降低到了 $\log e \approx 1.44$ 比特。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU 或大规模集群的使用。
*   **原因**：该研究属于纯理论证明，其结论通过数学推导得出，不依赖于深度学习训练或大规模数值模拟。

### 5. 实验数量与充分性
*   **理论覆盖面**：论文的证明适用于所有在波兰空间（Polish spaces）取值的随机变量，具有极高的通用性。
*   **充分性**：作者不仅证明了一次性（One-shot）情况下的最优性，还通过推导恢复了渐近情况下的二阶冗余结果，证明了该方法在不同尺度下的鲁棒性。

### 6. 主要结论与发现
*   **拒绝采样的最优性**：证明了拒绝采样配合适当的编码可以达到功能信息下界（误差在 $\log e$ 以内）。
*   **紧致的比特率界限**：
    *   一般信道：速率 $\leq I(X; Y) + \log(I(X; Y) + 1) + 2.45$ 比特。
    *   奇异信道：速率 $\leq I(X; Y) + 1.45$ 比特。
*   **功能信息的重要性**：确认了 $I_F$ 是比互信息更适合描述 REC 复杂度的度量。

### 7. 优点
*   **算法简洁**：不需要复杂的排序或复杂的随机过程转换，仅通过改变拒绝采样索引的编码方式即可实现。
*   **理论突破**：解决了 REC 领域长期存在的关于“对数冗余是否可消除”的部分争议，并给出了目前最紧致的常数项。
*   **统一性**：一个简单的单次采样结果同时解释了渐近理论中的多种现象。

### 8. 不足与局限
*   **有界性假设**：算法要求密度比 $dP_{Y|X}/dP_Y$ 必须有界（即存在 $M$）。对于某些重尾分布或无界支撑集的分布，该算法无法直接应用，需要进行截断处理。
*   **计算复杂度**：虽然比特率最优，但在高维空间或 $M$ 值极大时，拒绝采样的期望迭代次数（时间复杂度）可能非常高。
*   **工程实现**：论文侧重于信息论极限，未讨论在有限精度算术编码下的实际实现损耗。

（完）
