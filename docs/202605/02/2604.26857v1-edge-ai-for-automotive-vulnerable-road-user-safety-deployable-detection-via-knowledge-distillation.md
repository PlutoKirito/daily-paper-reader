---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 面向汽车弱势道路使用者安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 用于可部署边缘AI模型的知识蒸馏与量化
tldr: 本研究针对车载边缘硬件上弱势群体（VRU）检测的精度与计算限制矛盾，提出一种知识蒸馏（KD）框架。通过让轻量级YOLOv8-S学生模型学习大型YOLOv8-L教师模型，实现了3.9倍的模型压缩。实验表明，该方法显著提升了模型在INT8量化下的鲁棒性，在保持高精度的同时大幅减少了误报，为安全关键型VRU检测在边缘端的部署提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 旨在解决大型目标检测模型在边缘硬件INT8量化时精度大幅下降，而小型模型原生性能不足的问题。
method: 采用知识蒸馏框架，训练参数量仅为11.2M的YOLOv8-S学生模型去模仿43.7M参数的YOLOv8-L教师模型。
result: "在BDD100K数据集上，KD学生模型在INT8量化下的精度损失仅为5.6%，且其INT8精度甚至超过了教师模型的FP32精度。"
conclusion: 知识蒸馏是实现在边缘硬件上部署高精度、高鲁棒性且符合安全要求的VRU检测系统的关键技术。
---

## 摘要
在边缘硬件上部署用于弱势道路使用者（VRU）安全的高精度目标检测，需要平衡模型容量与计算约束。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，训练紧凑的 YOLOv8-S 学生模型（11.2M 参数）来模仿 YOLOv8-L 教师模型（43.7M 参数），在实现 3.9 倍压缩的同时保留了量化鲁棒性。我们在全规模 BDD100K 数据集（70,000 张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 下性能大幅下降（mAP 下降 23%），而经过 KD 的学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，KD 传递的是精度校准而非原始检测能力：在 INT8 下，KD 学生模型的精度达到 0.748，而直接训练的模型仅为 0.653，在等效召回率下提升了 14.5%，且相比性能崩溃的教师模型，误报率降低了 44%。在 INT8 量化下，KD 学生模型的精度（0.748）甚至超过了体积大 3.9 倍的教师模型的 FP32 精度（0.718）。这些发现表明，知识蒸馏是在边缘硬件上部署高精度、安全关键型 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

这篇论文针对自动驾驶中弱势道路使用者（VRU）检测的边缘部署问题，提出了一种结合知识蒸馏（KD）与量化技术的优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在计算资源受限的边缘硬件（如 NVIDIA Jetson）上，部署既能保持高精度又能抵御 INT8 量化损失的 VRU 检测模型。
*   **研究背景**：
    *   **VRU 安全性**：行人、骑行者等弱势群体占交通死亡人数的一半以上，实时、准确的检测至关重要。
    *   **量化困境**：大型模型（如 YOLOv8-L）在 FP32 下精度高，但在边缘部署所需的 INT8 量化下会出现严重的精度坍塌；小型模型（如 YOLOv8-S）虽然量化鲁棒性好，但原生检测能力不足。
    *   **研究空白**：此前研究尚未明确知识蒸馏是否能提升模型对 INT8 量化的鲁棒性，以及蒸馏究竟传递了哪种检测特性。

### 2. 核心方法论
论文提出了一个**任务削弱型知识蒸馏（Task-dampened KD）框架**：
*   **核心思想**：使用大型教师模型（YOLOv8-L，43.7M 参数）引导紧凑型学生模型（YOLOv8-S，11.2M 参数）的训练，使其在大幅压缩体积的同时，继承教师模型的预测特性。
*   **关键技术细节**：
    *   **损失函数组合**：总损失由任务损失（$L_{task}$）、逻辑蒸馏损失（$L_{logit}$，使用 KL 散度和温度缩放 $T=10$）和特征蒸馏损失（$L_{feat}$，使用 L2 距离对齐中间层）组成。
    *   **任务削弱策略**：将任务损失权重 $\alpha$ 设为 0.5（通常为 1.0），通过降低地面真值（Ground Truth）的监督强度，迫使学生模型更多地学习教师模型的软标签分布。
    *   **量化流程**：训练完成后，使用 TensorRT 的训练后量化（PTQ）技术将模型转换为 INT8 精度。

### 3. 实验设计
*   **数据集**：使用 **BDD100K**，这是目前最大的自动驾驶数据集之一（70K 训练集，10K 验证集），涵盖了严重的类别不平衡场景。
*   **Benchmark 与对比方法**：
    *   **教师模型**：直接训练的 YOLOv8-L。
    *   **直接学生模型**：直接在地面真值上训练的 YOLOv8-S（无蒸馏）。
    *   **KD 学生模型**：通过本文方法训练的 YOLOv8-S。
*   **评估指标**：mAP50、mAP50-95、精度（Precision）、召回率（Recall）、误报率（FAR）以及推理吞吐量（FPS）。

### 4. 资源与算力
*   **训练算力**：使用了云端 GPU 基础设施，具体型号为 **NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)**。
*   **训练时长**：教师模型耗时 7 小时，直接学生模型 2.7 小时，KD 学生模型因需运行教师推理，耗时 4.9 小时。
*   **推理测试**：在 **NVIDIA RTX 5070 (8GB VRAM)** 上进行，以模拟接近边缘端的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：在全规模 BDD100K 数据集上进行，样本量充足。
*   **消融实验**：针对任务损失权重 $\alpha$ 进行了对比实验（$\alpha=0.5$ vs $\alpha=1.0$），验证了“任务削弱”是实现精度校准转移的关键。
*   **针对性评估**：专门针对行人、自行车、摩托车三类 VRU 进行了细分性能分析。
*   **客观性**：通过固定随机种子和独立的量化校准集，确保了对比的公平性。

### 6. 主要结论与发现
*   **量化鲁棒性**：教师模型在 INT8 下 mAP 暴跌 **23.4%**，而 KD 学生模型仅下降 **5.6%**。
*   **精度校准转移**：KD 传递的是**精度校准（Precision Calibration）**而非原始检测容量。在 INT8 下，KD 学生模型的精度（0.748）远高于直接训练的学生模型（0.653），甚至超过了教师模型的 FP32 精度。
*   **误报率降低**：相比于量化后性能崩溃的教师模型，KD 学生模型在 INT8 下的误报率降低了 **44%**。
*   **部署优势**：实现了 **3.9 倍**的参数压缩和 **2.37 倍**的推理加速，且在 INT8 下依然保持了极高的安全性指标。

### 7. 优点与亮点
*   **发现新机制**：揭示了 KD 提升量化鲁棒性的内在机制在于“精度校准”的转移，而非单纯的召回率提升。
*   **实用性强**：直接面向工业界边缘部署痛点，证明了 KD 是安全关键型边缘 AI 部署的“刚需”。
*   **性能卓越**：小模型在量化后的精度表现优于大模型在全精度下的表现，极具说服力。

### 8. 不足与局限
*   **架构单一**：实验仅基于 YOLOv8 系列，尚未在 Transformer 类检测器或其他架构上验证通用性。
*   **硬件覆盖**：虽然模拟了边缘环境，但未在真实的嵌入式车载硬件（如 Jetson Orin）上进行功耗和热管理分析。
*   **量化方法**：仅使用了 PTQ（训练后量化），未探讨 QAT（量化感知训练）与 KD 结合可能带来的进一步提升。
*   **数据集局限**：主要基于 BDD100K，对于极端天气或夜间场景的泛化能力有待进一步分层分析。

（完）
