---
title: Neural Video Compression with Domain Transfer
title_zh: 结合领域迁移的神经视频压缩
authors: "Tiange Zhang, Rongqun Lin, Xiandong Meng, Haofeng Wang, Xing Tian, Qi Zhang, Siwei Ma"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13476v1"
tags: ["query:entropy-coding"]
score: 10.0
evidence: 具有动态率失真的领域迁移增强型神经视频压缩框架
tldr: 针对神经视频压缩中训练与测试数据分布不一致导致的性能下降，本文提出DCVC-DT框架。该框架引入轻量级在线领域迁移机制，在不修改编解码器参数的情况下，通过动态调整推理时的潜在表示来弥合领域差距。此外，结合帧级动态率失真调整方案，显著提升了模型在未知数据上的泛化能力并降低了码率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13476v1/fig-001.webp\", \"caption\": \"Fig. 3. R–D performance comparison on HEVC Class C and D datasets evaluated with PSNR under BT.709 color space.\", \"page\": 4, \"index\": 1, \"width\": 495, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.13476v1/fig-002.webp\", \"caption\": \"Fig. 2. Illustration of the proposed Online Latent Refinement and Frame-level Dynamic RD Adjustment modules. During inference, the encoder iteratively updates the latent representation yt via gradient descent to minimize RD loss. Simultaneously, the dynamic RD adjustment module regulates the rate weight βt based on the inter-frame quality fluctuation ∆Qt to effectively enhance RD performance. Notably, this entire process requires no network parameter updates.\", \"page\": 3, \"index\": 2, \"width\": 927, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.13476v1/fig-003.webp\", \"caption\": \"Fig. 1. Overall framework of DCVC-DT. We integrate the proposed Online Domain Transfer (DT) mechanism and Dynamic RD Adjustment strategy into the baseline NVC architecture. By optimizing latent features solely on the encoder side, our framework effectively bridges the domain gap between training and testing data and enhances generalization to diverse contents.\", \"page\": 2, \"index\": 3, \"width\": 830, \"height\": 456}]"
motivation: 解决神经视频压缩中由于训练和测试数据分布差异导致的性能退化问题。
method: 设计了轻量级在线领域迁移机制来动态调整潜在表示，并引入帧级动态率失真调节方案。
result: "相比基准模型DCVC-DC实现了最高6.21%的码率节省，并有效缓解了误差传播。"
conclusion: DCVC-DT通过无需修改参数的领域迁移技术，显著增强了神经视频编码器的泛化性能和压缩效率。
---

## 摘要
内容自适应压缩一直是神经视频编码（NVC）中的一个关键方向，旨在缓解训练数据与测试数据之间的领域差异。这种差异通常源于训练数据与推理数据之间的分布不一致，当测试内容偏离训练分布时，可能会导致明显的性能下降。为了应对这一挑战，我们提出了 DCVC-DT，一个增强了领域迁移能力的神经视频压缩框架。具体而言，我们设计了一种轻量级的在线领域迁移（DT）机制，在推理过程中动态调整编码的潜变量表示，从而在不修改编码器或解码器参数的情况下有效弥合领域差异。此外，我们开发了一种帧级动态 RD（率失真）调节方案，根据质量波动主动调节损失函数中 R 和 D 的比例，从而提高率失真性能。大量实验表明，DCVC-DT 相比基准模型 DCVC-DC 可实现高达 6.21% 的码率节省，同时显著增强了对未知测试数据的泛化能力并缓解了误差传播。我们的代码可在 https://github.com/SunnyMass/DCVC-DT 获取。

## Abstract
Content-adaptive compression has always been a key direction in neural video coding (NVC), aiming to mitigate the domain gap between training and testing data. Such gaps often arise from distributional discrepancies between training and inference data, which may cause noticeable performance degradation when the testing content differs from the training distribution. To tackle this challenge, we propose DCVC-DT, a domain transfer enhanced neural video compression framework. Specifically, we design a lightweight online domain transfer (DT) mechanism that dynamically adapts the encoded latent representation during inference, effectively bridging the domain gap without modifying the encoder or decoder parameters. In addition, we develop a frame-level dynamic RD (Rate and Distortion) adjustment scheme that actively regulates the ratio of R and D in the loss function based on quality fluctuation, thereby improving rate-distortion performance. Extensive experiments demonstrate that DCVC-DT achieves up to 6.21% bitrate savings over the baseline DCVC-DC, while significantly enhancing generalization to unseen testing data and alleviating error propagation. Our code is available at https://github.com/SunnyMass/DCVC-DT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neural Video Compression with Domain Transfer》（DCVC-DT）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：神经视频压缩（NVC）模型在面对与训练数据分布不一致的测试内容（如屏幕录制、动画、医学视频）时，会出现明显的性能退化，即“领域间隙”（Domain Gap）问题。
*   **研究动机**：现有的内容自适应方法通常需要微调网络参数或引入额外的模块，这增加了编解码器的复杂度和推理负担。本文旨在探索一种**仅在编码端优化潜变量（Latent Representation）**的方法，在不改变模型参数和解码时间的前提下，实现高效的内容自适应和领域迁移。

### 2. 方法论
DCVC-DT 框架基于 DCVC-DC 构建，核心包含两个关键技术：
*   **在线潜变量细化（Online Latent Refinement）**：
    *   **核心思想**：在推理阶段，固定编码器和解码器参数，通过梯度下降法迭代优化编码后的潜变量 $y_t$。
    *   **技术细节**：利用率失真（RD）损失函数 $L_t = R + w_t \cdot \lambda \cdot D$ 指导优化。为了解决量化不可导问题，采用了随机 Gumbel 退火量化（SGA-Q）技术。这种方法仅在编码端进行，解码端完全透明，不增加解码延迟。
*   **帧级动态 RD 调整（Frame-level Dynamic RD Adjustment）**：
    *   **核心思想**：根据相邻帧间的质量波动动态调节损失函数中码率（R）和失真（D）的权重比例。
    *   **算法流程**：计算当前帧与前一帧的失真差异 $\Delta Q_t$。引入动态权重因子 $\beta_t$，更新公式为 $\beta_t = \beta_0 - 0.2 \cdot \text{sign}(\Delta Q_t)$。若当前帧质量下降，则通过调整 $\beta_t$ 允许分配更多码率以提升质量，从而平滑质量波动并抑制误差传播。

### 3. 实验设计
*   **数据集**：采用了 HEVC 标准测试序列中的 Class C（WVGA 分辨率）和 Class D（WQVGA 分辨率）。
*   **Benchmark（基准）**：以 SOTA 模型 **DCVC-DC** 作为锚点（Anchor）。
*   **对比方法**：包括 DCVC 家族的代表性工作：DCVC-TCM、DCVC-HEM 以及基准 DCVC-DC。
*   **评价指标**：PSNR（峰值信噪比）和 BD-Rate（等质量下的码率节省）。

### 4. 资源与算力
*   **硬件设备**：实验使用了单张 **NVIDIA A100 (40G) GPU**。
*   **算力消耗**：
    *   在线优化迭代次数最高设为 1000 次。
    *   对于 HEVC Class D 序列，在单个码率点上的细化过程大约需要 **25 分钟**。
    *   **解码端**：由于未修改参数，解码速度与基准模型持平，无额外算力负担。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 HEVC Class C 和 D 的所有标准序列，进行了完整的 RD 曲线对比。
*   **消融实验**：针对“在线潜变量细化”和“动态 RD 调整”分别做了消融测试（Model A/B/C），验证了各模块的增益（分别贡献了 2.35% 和 1.26% 的码率节省）。
*   **客观性**：实验遵循了 HEVC 通用测试条件（CTC），使用固定的 Intra 周期（32 帧）和标准的 BT.709 色彩空间，对比过程较为公平、客观。

### 6. 主要结论与发现
*   **性能提升**：DCVC-DT 相比基准 DCVC-DC 平均实现了 **6.21%** 的 BD-Rate 码率节省。
*   **泛化能力**：通过在线领域迁移，模型能更好地适应未见过的视频内容。
*   **误差抑制**：实验观察到 PSNR 波动曲线更加平滑，证明该方法能有效缓解 NVC 中常见的误差传播（Error Propagation）问题，提升了长序列编码的稳定性。

### 7. 优点
*   **解码友好**：完全不需要修改解码器架构或参数，适合对解码延迟敏感的应用场景。
*   **轻量化迁移**：无需大规模重传模型权重，仅通过优化潜变量即可实现领域自适应。
*   **质量稳定**：动态 RD 调整机制有效地解决了视频帧间质量不均的问题。

### 8. 不足与局限
*   **编码复杂度高**：编码端的迭代优化（1000 次迭代需 25 分钟）极大限制了其在实时编码场景中的应用，目前更适用于离线压缩或点播视频转码。
*   **实验覆盖面**：虽然在 HEVC 数据集上表现良好，但未在更高分辨率（如 4K/8K）或更多样化的非自然视频（如纯医学影像、复杂合成动画）上提供详尽的数据支撑。
*   **超参数敏感性**：动态调整步长（如 0.2）是经验值，对于不同特性的视频可能需要进一步调优。

（完）
