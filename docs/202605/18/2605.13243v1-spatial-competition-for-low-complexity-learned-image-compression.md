---
title: Spatial Competition for Low-Complexity Learned Image Compression
title_zh: 面向低复杂度学习型图像压缩的空间竞争机制
authors: "Théophile Blard, Pierrick Philippe, Théo Ladune, Xiaoran Jiang, Olivier Déforges"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13243v1"
tags: ["query:train-trick"]
score: 9.0
evidence: 基于空间竞争的低复杂度学习型图像压缩框架
tldr: "本研究针对基于自编码器的图像压缩模型解码复杂度高的问题，提出了一种基于空间竞争的低复杂度学习型图像压缩框架。该方法通过在不同图像区域选择最合适的专用神经编解码器，并利用模式图引导重建，实现了在保持单模型解码复杂度的同时提升压缩效率。在CLIC 2020数据集上，该方法相比单一编解码器可降低14.5%的码率，并以较低的计算量达到HEVC级别的性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13243v1/fig-001.webp\", \"caption\": \"Fig. 1: Rate savings vs. HEVC on the CLIC 2020 [17] dataset as a function of decoding complexity. Negative values mean fewer bits than HEVC at equal quality. Circle area denotes encoding complexity. Values are reported in Table 2.\", \"page\": 1, \"index\": 1, \"width\": 503, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-002.webp\", \"caption\": \"Fig. 2: Mode selection. The input image is first partitioned into non-overlapping patches. For each patch, the M codecs are evaluated by their rate-distortion costs, and the codec minimizing this cost is selected, yielding the mode map m.\", \"page\": 2, \"index\": 2, \"width\": 508, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-003.webp\", \"caption\": \"Fig. 3: Spatial encoding process. Given the mode map m, each image patch is encoded using the selected analysis transform and entropy model. Patches sharing the same mode are processed continuously. AE denotes arithmetic encoding.\", \"page\": 3, \"index\": 3, \"width\": 508, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-004.webp\", \"caption\": \"Fig. 4: Visualization of mode maps for different numbers of modes. Mode maps are obtained with different numbers of competing modes (M ) at λ = 0.0002. Images are stefan-kunze-26931 and nomao-saeki-33553 from the CLIC 2020 dataset.\", \"page\": 4, \"index\": 4, \"width\": 1054, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-005.webp\", \"caption\": \"Fig. 5: BD-rate vs. encoding complexity on CLIC 2020. Following [15], encoding complexity for Cool-chic 4.0 is computed as 3× κdec× number of iterations.\", \"page\": 5, \"index\": 5, \"width\": 503, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-006.webp\", \"caption\": \"Fig. 6: Rate-distortion performance on CLIC 2020.\", \"page\": 5, \"index\": 6, \"width\": 475, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2605.13243v1/fig-007.webp\", \"caption\": \"Table 2: BD-rate vs. HEVC on CLIC 2020. Encoding and decoding complexity κenc and κdec are reported in kMAC/pixel.\", \"page\": 5, \"index\": 7, \"width\": 525, \"height\": 254}]"
motivation: 现有的基于自编码器的图像压缩模型虽然性能优异，但解码时的计算复杂度往往过高。
method: 提出一种空间竞争机制，根据率失真代价为每个图像区域选择最优的专用编解码器，并通过模式图告知解码器。
result: "在CLIC 2020数据集上实现了最高14.5%的码率节省，且解码复杂度仅为每像素1433次乘加运算。"
conclusion: 该框架通过多模型空间竞争实现了低复杂度下的高性能图像压缩，为实时神经图像编解码提供了有效方案。
---

## 摘要
基于自动编码器的图像编解码器实现了最先进的压缩性能，但通常会产生高计算复杂度，尤其是在解码阶段。本研究介绍了一种基于多个专用神经编解码器之间空间竞争的低复杂度学习型图像压缩框架。对于每个图像区域，编码器根据率失真代价选择与局部内容最匹配的编解码器。模式图（mode map）作为辅助信息传输，以指示每个区域的编解码器选择。在解码时，这种基于模式图的选择引导重建过程，同时保持了单个编解码器的复杂度。该设计实现了每张图像的自适应，具有低解码复杂度和快速编码的特点。在 CLIC 2020 数据集上，与单个编解码器相比，我们的方法实现了高达 -14.5% 的码率降低，并在每像素 1433 MACs 的解码复杂度下达到了 HEVC 级别的性能。

## Abstract
Autoencoder-based image codecs achieve state-of-the-art compression performance but often incur high computational complexity, particularly at decoding time. This work introduces a low-complexity learned image compression framework based on spatial competition between multiple specialized neural codecs. For each image region, the encoder selects the codec that best matches the local content according to a rate-distortion cost. A mode map is transmitted as side information to indicate the per-region codec selection. At decoding time, this mode map-based selection guides reconstruction while preserving the complexity of a single codec. This design enables per-image adaptation with low decoding complexity and fast encoding. On the CLIC 2020 dataset, our method achieves up to -14.5% rate reduction compared to a single codec and reaches HEVC-level performance with a decoding complexity of 1433 MACs per pixel.

---

## 论文详细总结（自动生成）

这是一份关于论文《Spatial Competition for Low-Complexity Learned Image Compression》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然基于自编码器的学习型图像压缩（LIC）在性能上已超越传统标准（如HEVC、VVC），但其**解码复杂度极高**（通常超过 $10^5$ MAC/像素），难以在资源受限的设备上部署。
*   **核心挑战**：如何在保持**快速编码**和**极低解码复杂度**的同时，通过内容自适应技术提升压缩效率。
*   **整体含义**：本文提出了一种空间竞争框架，通过在编码端让多个“专家化”的轻量级神经编解码器进行竞争，为每个图像区域选择最优模型，从而在不增加解码负担的前提下实现性能增益。

### 2. 方法论
*   **核心思想**：借鉴传统视频编码中的“模式选择”概念，预训练 $M$ 个专门针对不同纹理/内容类型的轻量级编解码器。
*   **关键技术细节**：
    *   **空间竞争（Mode Selection）**：将图像划分为 $P \times P$ 的非重叠块。编码器对每个块遍历 $M$ 个编解码器，计算率失真代价 $L = D + \lambda R$，选择代价最小的编解码器索引，生成“模式图（Mode Map）”。
    *   **模式图传输**：模式图作为辅助信息（Side Information）写入码流，由于块大小较大（如128x128），其开销极低（通常小于总码率的1%）。
    *   **解码流程**：解码器根据模式图，对每个块仅调用对应的轻量级合成变换和熵模型。**解码复杂度等同于单个轻量级模型**，与 $M$ 的数量无关。
*   **训练算法（专用化训练）**：采用类似聚类的迭代优化：
    1.  **分配步**：根据当前的 $M$ 个模型，将训练集中的图像块分配给 RD 代价最低的模型。
    2.  **更新步**：利用分配到的图像块，通过梯度下降更新每个模型的参数。
    3.  重复上述步骤直至收敛。

### 3. 实验设计
*   **数据集**：训练使用 Unsplash 数据集；测试使用 **Kodak**、**CLIC 2020** 和 **JPEG AI Test**。
*   **Benchmark（基准）**：传统标准 **HEVC (HM 16.20)** 和 **VVC (VTM 19.1)**。
*   **对比方法**：
    *   轻量级/过拟合模型：Cool-chic 4.0、N-O Cool-chic。
    *   经典学习型模型：Scale Hyperprior (Balle 2018)。
    *   低复杂度模型：Shallow-NTC、EVC-LS、DCVC-RT。

### 4. 资源与算力
*   **训练细节**：每个更新步包含 100k 次迭代，使用 Adam 优化器，Batch Size 为 64，采用余弦学习率衰减。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。但提到编码复杂度随 $M$ 线性增加（$M=8$ 时约为 171 kMAC/pixel），这在 LIC 领域属于典型范围。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 $M=1, 2, 4, 8$ 四种配置，验证了模型数量对性能的影响。
    *   覆盖了从低分辨率（Kodak）到高分辨率（JPEG AI）的多个数据集。
    *   对比了 6 种以上的代表性学习型编解码器。
*   **充分性与客观性**：实验设计较为充分，通过 BD-Rate 与复杂度的权衡曲线（RD-Complexity Curve）直观展示了该方法的优势。使用了标准的 Bjøntegaard Delta 评估方法，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：在 CLIC 2020 数据集上，相比单一模型（$M=1$），$M=8$ 的配置实现了 **-14.5% 的码率节省**。
*   **复杂度优势**：在仅有 **1433 MAC/像素** 的极低解码复杂度下，达到了接近 HEVC 的性能（BD-Rate 仅差 +2.6%）。
*   **解码独立性**：增加竞争模型的数量 $M$ 可以显著提升压缩率，但**完全不增加解码时间**，仅略微增加解码器的内存占用（参数量依然极小）。

### 7. 优点
*   **非对称性设计**：成功将计算压力转移到编码端（离线训练+在线模式选择），极大地优化了解码效率。
*   **无需迭代优化**：与 Cool-chic 等需要针对每张图进行数千次迭代训练的过拟合模型相比，该方法编码速度极快。
*   **极低开销**：模式图的信令开销几乎可以忽略不计。

### 8. 不足与局限
*   **绝对性能差距**：虽然接近 HEVC，但距离最先进的（SOTA）高复杂度 LIC 模型以及 VVC 标准仍有较大性能差距。
*   **编码复杂度随 M 增长**：编码器需要运行 $M$ 次前向过程来选优，虽然比过拟合模型快，但比单一通用模型慢。
*   **内存占用**：虽然单个模型很小，但解码端需要存储 $M$ 组参数，对于极度内存受限的嵌入式设备可能存在挑战。
*   **空间粒度限制**：目前采用固定大小的块（128x128），可能无法捕捉更细粒度的内容变化。

（完）
