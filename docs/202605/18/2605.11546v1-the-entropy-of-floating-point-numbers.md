---
title: The Entropy of Floating-Point Numbers
title_zh: 浮点数的熵
authors: "Sultan Daniels, Samuel H. D'Ambrosia, Michael R. DeWeese, Anant Sahai"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11546v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 量化随机变量熵的解析近似
tldr: 本研究针对浮点数离散熵的计算问题，提出了一种解析近似方法并给出了误差界限。研究揭示了浮点量化下微分熵与离散熵之间的新型关联，证明了浮点熵在缩放变换下的近似不变性，并为常见分布提供了闭式表达式，为理解数值表示的复杂性提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11546v1/fig-001.webp\", \"caption\": \"TABLE I: Closed-form approximations for the floating-point entropy of common distributions. Here, p is the precision, γe is the Euler-Mascheroni constant, Γ(·) is the gamma function, ψ(·) is the digamma function, B(·, ·) is the beta function, 0d is the d-dimensional zero vector, and λi(Σ) is the i-th eigenvalue of the positive semidefinite covariance matrix Σ. For the multivariate Gaussian case, we assume scalar quantization in which each component of the random vector is independently quantized on distinct floating-point numbers. Note that for all the univariate distributions, the scale parameter cancels (see Theorem 3). Additionally, in the multivariate case, the entropy is invariant under transformation of X by a diagonal matrix (see Corollary 1).\", \"page\": 1, \"index\": 1, \"width\": 470, \"height\": 797}, {\"url\": \"assets/figures/arxiv/2605.11546v1/fig-002.webp\", \"caption\": \"Fig. 1: Floating-point structure, and comparisons between analytic approximation and numerically evaluated exact discrete entropy. (1a) The structure of a floating-point number where each box represents one bit. The true bin size function ∆(x) is plotted on log-log scale for both midpoint (black solid curve) and floor quantization (blue dotted curve) with p = 10 and E = 4 along with the smooth approximation ∆s(x) (red dashed curve). (1b) shows the entropy of Xfp which is a discrete representation of the random variable X ∼ N ( 0, s2 ) that has been clipped and midpoint quantized onto a floatingpoint representation with precision p = 3 and various numbers of exponent bits E. The x-axis shows the standard deviation s of the underlying continuous distribution. The horizontal red line shows the approximate entropy H̃s(p). The vertical dashed lines mark s = 2emin , while the vertical dotted lines mark s = 2emax for each E. Notice that exact entropy closely follows the approximate entropy until s approaches the overflow and underflow boundaries for each E. (1c) Shows the exact discrete entropy of Xfp as p varies for each E when the underlying continuous random variable is X ∼ N (0, 1). The approximation H̃s(p) is also plotted as the dashed red line. Notice that the curves for E ≥ 4 are directly on top of each other, showing that H̃s(p) is close to H(Xfp) when E is large enough to keep the probability of overflows and underflows low. The approximation stays close to the true entropy with a large exponent, even with low precision. (1d) Comparison between exact entropies and Table I approximations for various distributions, in terms of a scale parameter s defined in the figure for each distribution, demonstrating excellent agreement inside the underflow and overflow range specified by 2−(2E−1−1) < s < 22 E−1 .\", \"page\": 2, \"index\": 2, \"width\": 512, \"height\": 578}, {\"url\": \"assets/figures/arxiv/2605.11546v1/fig-003.webp\", \"caption\": \"Fig. 3: Exact midpoint-quantized entropy vs. standard deviation σ. For each precision p ∈ {1, . . . , 8}, the exact discrete entropy H(Xfp) of X ∼ N ( 0, σ2 ) (with µ = 0) is plotted as a function of σ over a wide log-scale range. Each curve corresponds to a distinct value of exponent bits E ∈ {0, 1, . . . , 7}. The vertical dashed lines mark σ = 2emin and the vertical dotted lines mark σ = 2emax for each E, and the horizontal red line shows the approximation. These plots were generated by sweeping σ over 500 log-spaced points and computing the exact entropy via Corollary 2.\", \"page\": 17, \"index\": 3, \"width\": 1042, \"height\": 575}, {\"url\": \"assets/figures/arxiv/2605.11546v1/fig-004.webp\", \"caption\": \"Fig. 2: Clipping and midpoint quantization with K = 3 representable values {u1, u2, u3}. The blue vertical lines represent the midpoints, and the arrows depict the regions of the real line that map to each representable value at a black vertical line.\", \"page\": 16, \"index\": 4, \"width\": 536, \"height\": 242}]"
motivation: 旨在解决浮点量化过程中离散熵难以直接计算的问题，并探索其与微分熵的内在联系。
method: 通过推导浮点数熵的解析近似公式，并分析其在不同分布下的误差界限和缩放特性。
result: 发现了一个连接微分熵与浮点量化熵的新物理量，并证明了浮点熵对缩放操作具有近似鲁棒性。
conclusion: 该研究为常见分布提供了精确的浮点熵闭式表达式，深化了对浮点数表示中信息量变化的理解。
---

## 摘要
本文提出了浮点数熵的解析近似，并给出了该近似误差的界限。众所周知，微分熵与均匀量化随机变量的离散熵紧密相关。我们的近似揭示了一个不同的量，它为浮点量化提供了这种联系。此外，我们证明了浮点量化随机变量的熵在缩放变换下近似保持不变。本文提供了常见分布浮点熵的闭式表达式，并将其与精确结果进行了比较。

## Abstract
Here we present an analytic approximation for the entropy of floating-point numbers, along with bounds on the error of this approximation. It is well-known that the differential entropy is tightly linked to the discrete entropy of a uniformly quantized random variable. Our approximation uncovers a different quantity that provides this link for floating-point quantization. Additionally, we prove that the entropy of a floating-point quantized random variable is approximately unchanged under scaling. Closed-form expressions for the floating-point entropy of common distributions are provided and compared to exact results.

---

## 论文详细总结（自动生成）

这篇论文由加州大学伯克利分校的研究团队撰写，深入探讨了浮点数（Floating-Point Numbers）作为一种非均匀量化器时的信息论特性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现代深度学习和硬件设计中，浮点数格式（如 FP8, BF16）被广泛用于表示连续变量。虽然均匀量化的熵与微分熵之间的关系已广为人知，但**浮点数这种非均匀量化器的输出离散熵如何解析表达**，以及它与微分熵之间存在何种定量联系，此前缺乏深入研究。
*   **研究动机**：随着新型浮点格式不断涌现，设计者需要在精度、动态范围和内存之间寻找平衡。理解浮点表示的熵有助于优化变量率量化器，并为数值表示的复杂性提供理论支撑。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将浮点量化视为一种非均匀量化过程，通过引入“平滑化”的步长函数，建立离散熵与微分熵之间的解析关联。
*   **关键技术细节**：
    *   **量化步长函数 $\Delta(x)$**：浮点数的步长随指数位变化。作者定义了真实步长函数，并提出了一个平滑近似函数 $\Delta_s(x) = \frac{1}{\sqrt{2}}|x| \cdot 2^{1-p}$（其中 $p$ 为精度位）。
    *   **近似公式**：推导出浮点熵的近似表达式 $\tilde{H}_s(X_{fp}) = p - \frac{1}{2} - E[\log(|X|f_X(X))]$。这个公式揭示了“微分浮点熵”的概念。
    *   **误差界限**：利用 KL 散度（Kullback-Leibler divergence）证明了近似值与真实离散熵之间的误差受限于分布的“尖锐度”（Spikiness）以及溢出/欠流区域的概率。
    *   **闭式表达式**：为高斯分布、均匀分布、伽马分布、拉普拉斯分布等 11 种常见分布推导了具体的闭式熵计算公式。

### 3. 实验设计
*   **场景与数据集**：实验主要基于合成数据，通过数值计算验证理论公式。
*   **对比基准（Benchmark）**：将论文提出的**解析近似公式**与**精确离散熵（Exact Discrete Entropy）**进行对比。精确熵是通过对浮点数所有可能的离散取值进行概率求和计算得到的。
*   **对比维度**：
    *   改变分布的缩放参数（如高斯分布的 $\sigma$）。
    *   改变浮点数的精度位 $p$ 和指数位 $E$。
    *   覆盖多种概率分布类型（重尾分布、受限分布等）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或大规模算力资源。
*   **性质分析**：由于这是一篇理论性较强的论文，其实验部分主要涉及概率分布的数值积分和离散求和，通常在普通的 CPU 工作站上使用 Python 或 MATLAB 即可完成，不需要大规模分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：作者对 11 种不同的单变量分布以及多变量高斯分布进行了验证。
*   **充分性**：实验涵盖了从极小标准差到极大标准差（跨越多个数量级）的范围，展示了公式在“溢出”和“欠流”边界处的表现。
*   **客观性**：通过可视化图表（图 1 和图 3）直观展示了近似曲线与真实曲线的重合程度，实验设计逻辑严密，对比公平。

### 6. 主要结论与发现
*   **尺度不变性（Scale-Invariance）**：这是最重要的发现。与均匀量化不同，浮点量化的熵在底层连续变量进行常数缩放时保持近似不变（只要不超出指数范围）。
*   **解析关联**：发现浮点熵等于精度位 $p$ 减去常数，再减去微分熵与一阶对数矩之差。
*   **高精度近似**：在指数位 $E$ 足够大以覆盖分布支撑集的情况下，解析近似与真实熵高度吻合。

### 7. 优点
*   **理论创新**：首次为浮点数熵提供了通用的解析近似框架，填补了非均匀量化信息论的空白。
*   **实用性强**：提供的闭式表达式（如表 I 所示）可以直接用于评估不同数据分布在特定浮点格式下的信息保留能力。
*   **数学严谨**：不仅给出了近似，还通过定理给出了严格的误差上界和下界。

### 8. 不足与局限
*   **简化模型**：研究采用的是简化的浮点模型，**未考虑次正规数（Subnormal numbers）**、零的特殊表示以及 NaN/正负无穷等 IEEE-754 标准中的细节。
*   **分布平滑性要求**：近似精度依赖于概率密度函数的平滑性，对于极度不连续或“多刺”的分布，KL 散度项导致的误差会增大。
*   **应用范围**：目前主要讨论的是标量量化，虽然扩展到了对角协方差的多变量情况，但对于复杂的矢量量化（Vector Quantization）讨论较少。

（完）
