---
title: "PD-4DGS:Progressive Decomposition of 4D Gaussian Splatting for Bandwidth-Adaptive Dynamic Scene Streaming"
title_zh: PD-4DGS：面向带宽自适应动态场景流式传输的 4D 高斯泼溅渐进式分解
authors: "Jiachen Li, Guangzhi Han, Jin Wan, Delong Han, Yuan Gao, Min Li, Mingle Zhou, Gang Li"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11427v1"
tags: ["query:vcodec"]
score: 8.0
evidence: 4D场景的渐进式压缩与带宽自适应传输
tldr: 针对4D高斯泼溅（4DGS）在移动带宽下加载慢、无法流式传输的问题，本文提出了PD-4DGS框架。通过分层变形分解（HDD）将模型拆分为静态脚手架、全局变形和局部细化三层，实现了带宽自适应的渐进式传输。结合熵率失真损失和时间掩码一致性正则化，该方法在保持渲染质量的同时，显著降低了首帧延迟并压缩了码率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11427v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison on Dycheck iPhone [47]. Each cell shows mPSNR / mSSIM / mLPIPS / Stream(MB). Values in red mark the best result and values in green the second-best result for each per-column per-metric entry.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.11427v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison on Dycheck iPhone. Four scenes (apple, spin, paper-windmill, wheel; vertical labels) in a 2×2 grid; within each block: GT, MoDec-GS (18.37 MB), Ours (6.88 MB). Red boxes mark the region of interest, identical across columns. At ∼62% smaller bitstream, Ours matches MoDec-GS on static-leaning/high-frequency-edge cases and inherits the same residual motion artefacts on the hardest fast-rotation case (spin).\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.11427v1/fig-003.webp\", \"caption\": \"Figure 2. PD-4DGS pipeline. (a) Attribute R-DO compresses anchor attributes through a learnable mask mi and a Gaussian entropy model, while temporal mask consistency (TMC) stabilises mi over time; the gated anchor set A′ is the input to (b). (b) HDD factorises the rendering network into three additive bitstreams—Static Scaffold (ss0), +Global Deformation (ss1), +Local Refinement (ss2). (c) A capacity-weighted sampler ℓ ∼ π(ρ), driven online by the mask-activation rate ρ, shifts training budget toward deeper layers on motion-dense scenes; the multiplexer routes the chosen bitstream to the rasteriser, producing Îℓ.\", \"page\": 4, \"index\": 3, \"width\": 826, \"height\": 433}]"
motivation: 现有的4DGS模型必须完整下载后才能渲染，导致在低带宽环境下出现长时间黑屏等待，且不兼容现代自适应比特率传输。
method: 提出分层变形分解（HDD）将运动层次分为三层独立传输，并引入高斯熵率失真损失和时间掩码一致性正则化来优化压缩与渲染。
result: "在Dycheck iPhone基准测试中，PD-4DGS在同等画质下减少了超过60%的码率，并将2Mbps带宽下的首帧延迟从数百秒缩短至约1.7秒。"
conclusion: PD-4DGS首次实现了4DGS的渐进式压缩与按需流式传输，为动态场景的高效分发提供了可行的技术方案。
---

## 摘要
4D 高斯泼溅 (4DGS) 实现了高质量的动态新视角合成，但目前的模型仍为单体比特流，客户端必须完整下载后才能渲染任何帧，这在移动带宽下会导致数十到数百秒的黑屏等待，并使 4DGS 无法兼容现代自适应比特率传输。渐进式 3DGS 压缩缓解了静态场景的这一问题，但它仅作用于空间锚点，无法划分在动态场景体积中占主导地位的时间变形网络。我们提出了 PD-4DGS，这是首个用于 4DGS 渐进式压缩和按需传输的框架。分层变形分解 (HDD) 将 4DGS 中潜在的从粗到细的运动层次结构外化为三个独立传输的层——静态支架、全局变形和局部细化——使得比特流的任何前缀都已具备可渲染性，从而将单次训练转化为可扩展且兼容 DASH/HLS 的比特流。高斯熵属性率失真损失与时间掩码一致性正则化项在缩小基础层规模的同时抑制了低比特率下的闪烁；由学习到的激活率 rho 在线门控的容量加权展开调度，在无需逐场景超参数的情况下防止了变形网络的训练不足。在 Dycheck iPhone 基准测试中，PD-4DGS 在保持相同渲染保真度的前提下，将流式比特流削减了 60% 以上，并在 2 Mbps 链路上将首帧延迟从 73-930 秒降低至约 1.7 秒，唯一实现了 4DGS 真正的按需渐进式流式传输。

## Abstract
4D Gaussian Splatting (4DGS) enables high-quality dynamic novel view synthesis, yet current models remain monolithic bitstreams that clients must download in full before any frame can be rendered, causing black-screen waits of tens to hundreds of seconds on mobile bandwidth and leaving 4DGS incompatible with modern adaptive-bitrate delivery. Progressive 3DGS compression alleviates this for static scenes, but it acts only on spatial anchors and cannot partition the temporal deformation networks that dominate dynamic-scene size. We present PD-4DGS, the first framework for progressive compression and on-demand transmission of 4DGS. Hierarchical Deformation Decomposition (HDD) externalises the coarse-to-fine motion hierarchy already latent in 4DGS into three independently transmittable layers -- a static scaffold, a global deformation, and a local refinement -- so that any prefix of the bitstream is already renderable, turning a single training run into a scalable, DASH/HLS-compatible bitstream. A Gaussian-entropy attribute rate-distortion loss together with a temporal mask consistency regulariser shrink the base layer while suppressing low-bitrate flicker; a capacity-weighted rollout schedule, gated online by a learnt activation rate rho, then prevents deformation-network under-training without any per-scene hyperparameter. On the Dycheck iPhone benchmark, PD-4DGS cuts the streamed bitstream by >60% at matched rendering fidelity and reduces first-frame latency from 73--930 s to ~1.7 s on a 2 Mbps link, uniquely enabling true on-demand progressive streaming for 4DGS.

---

## 论文详细总结（自动生成）

这篇论文提出了 **PD-4DGS**，这是首个针对 4D 高斯泼溅（4DGS）设计的渐进式压缩与按需流式传输框架，旨在解决动态场景模型在移动网络下加载慢、无法自适应带宽的问题。

### 1. 核心问题与研究动机
*   **核心问题**：现有的 4DGS 模型通常是“单体式”比特流，客户端必须完整下载（通常几十 MB）后才能开始渲染。在 2 Mbps 的移动带宽下，这会导致长达数十秒甚至数分钟的黑屏等待。
*   **研究动机**：虽然 3DGS 已有渐进式压缩方案，但它们仅处理空间锚点，无法有效划分占据动态模型体积 40% 以上的“时间变形网络”。因此，需要一种能将变形网络也进行分层、实现“边下边看、由粗到精”的流式传输方案。

### 2. 方法论
PD-4DGS 的核心思想是将渲染管线分解为三个可独立传输的层级，并配合自适应训练策略：
*   **分层变形分解 (HDD)**：
    *   **L0（静态支架）**：仅包含静态锚点和基础 MLP，可立即渲染静态快照。
    *   **L1（全局变形）**：增加轻量级变形网络，捕捉大尺度运动。
    *   **L2（局部细化）**：增加高频残差网络，恢复肌肉、毛发等细节。
*   **属性级率失真优化 (R-DO)**：引入可学习的软掩码 $m_i$ 和高斯熵模型，将比特率直接作为损失函数的一部分，实现可微分的剪枝与压缩。
*   **时间掩码一致性 (TMC)**：通过二值熵惩罚和空间平滑约束，确保掩码在时间轴上稳定，消除低比特率下的闪烁和鬼影。
*   **容量加权渐进式展开训练**：引入学习到的激活率 $\rho$。根据场景的动态复杂程度，在线调整不同层级的训练权重（$\pi(\rho)$），防止变形网络训练不足，确保层级间的质量单调递增。

### 3. 实验设计
*   **数据集**：使用 **Dycheck iPhone** 单目基准测试，包含 Apple、Block、Paper-windmill 等 7 个典型动态场景。
*   **Benchmark 评价指标**：mPSNR、mSSIM、mLPIPS（感知质量）、Stream (MB，传输体积) 以及 t-LPIPS（时间稳定性）。
*   **对比方法**：对比了 SC-GS、Deformable 3DGS、4DGS 以及作为基准的 MoDec-GS。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4070 Super (12 GB VRAM)**。
*   **训练时长**：每个场景进行 30,000 次迭代，耗时约 **1.5 至 2 小时**。
*   **实现**：基于 PyTorch，使用 LZMA 进行最终比特流压缩。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个标准场景的完整对比，并进行了详尽的消融实验。
*   **消融实验**：分别验证了 $\rho$ 自适应采样策略、R-DO 压缩组件、TMC 正则化项以及层级单调性（Monotonicity）。
*   **充分性**：实验设计较为客观，通过模拟不同带宽（2/10/50 Mbps）下的延迟，直观展示了渐进式传输的优势。

### 6. 主要结论与发现
*   **大幅减容**：在保持同等渲染质量的前提下，PD-4DGS 比最强基线（MoDec-GS）减少了 **62.6%** 的传输体积。
*   **极低延迟**：在 2 Mbps 带宽下，首帧（静态快照）加载时间从 73.5 秒缩短至 **1.7 秒**，进入了用户可忍受的 3-5 秒窗口。
*   **层级增益**：验证了 PSNR 随层级（L0 $\to$ L1 $\to$ L2）严格单调增加，证明了分层分解的有效性。

### 7. 优点
*   **首创性**：首次实现了变形网络的渐进式分解，填补了动态场景流式传输的空白。
*   **场景自适应**：通过 $\rho$ 参数自动平衡静态与动态部分的训练权重，无需为每个场景手动调参。
*   **兼容性强**：生成的比特流原生兼容现代流媒体协议（DASH/HLS），具备极高的实用价值。

### 8. 不足与局限
*   **层级固定**：目前采用固定的三层拓扑结构，无法在运行时动态插入更多中间层。
*   **场景覆盖**：主要在单目 Dycheck 数据集上验证，对于大规模室外场景或多视角采集场景的泛化能力仍需进一步研究。
*   **训练开销**：虽然传输快，但每个场景仍需 1.5-2 小时的预训练，不适用于实时生成的动态内容。

（完）
