---
title: "LiVeAction: a Lightweight, Versatile, and Asymmetric Neural Codec Design for Real-time Operation"
title_zh: LiVeAction：一种面向实时操作的轻量级、通用且非对称的神经编解码器设计
authors: "Dan Jacobellis, Neeraja J. Yadwadkar"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06628v1"
tags: ["query:entropy-coding"]
score: 9.5
evidence: 用于实时运行的轻量级非对称神经编解码器设计
tldr: 针对资源受限的传感设备，本文提出LiVeAction，一种轻量级、通用且非对称的神经编解码器。它通过类FFT结构降低编码器复杂度，并利用基于方差的速率惩罚替代对抗损失，实现了跨模态的高效压缩，在保持优异率失真性能的同时，满足低功耗传感器的实时运行需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06628v1/fig-001.webp\", \"caption\": \"Fig. 5: Comparison of Cosmos, LiVeAction, and LiVeAction enhanced using a generative model. Best viewed zoomed in. The image was rescaled before compressing with cosmos to match the rate of two codecs (0.15 bpp).\", \"page\": 7, \"index\": 1, \"width\": 966, \"height\": 683}, {\"url\": \"assets/figures/arxiv/2605.06628v1/fig-002.webp\", \"caption\": \"TABLE V: Additional results and baselines for RGB images. For ImageNet, the reported accuracy is the top-1 accuracy for 1000-way classification. The accuracy without lossy compression is 0.8979.\", \"page\": 7, \"index\": 2, \"width\": 998, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2605.06628v1/fig-003.webp\", \"caption\": \"TABLE I: Rate-distortion-complexity trade-off for each modality. DR is the degree of dimensionality reduction of. CR is the compression ratio. Encoding throughput (Enc.) is measured in megasamples per second (audio), megapixels per second (images), megavoxels per second (hyperspectral) and frames per second (video). The analysis transform is run on GPU (RTX 4090) and the entropy coding is run on CPU (Intel i9 13900k) with the exception of JPEG 2000†, where no GPU acceleration is available. ∗ Encoding the entire video in one pass using Cosmos is not possible due to memory constraints. Instead, we encode chunks of 24 frames with 50 percent overlap, resulting in reduced compression ratio and throughput. If no memory constraints were imposed, the CR would be increased by 4× and the throughput would be increased by 3×.\", \"page\": 5, \"index\": 3, \"width\": 529, \"height\": 421}]"
motivation: 现有的标准编解码器和生成式神经编解码器在处理非传统模态数据时，存在计算开销大、模态受限或率失真性能不佳的问题。
method: 采用非对称设计，通过类FFT结构的轻量级编码器降低复杂度，并利用基于方差的速率惩罚简化训练过程并支持多模态。
result: 该设计在多种信号模态下均优于现有的生成式分词器，且显著降低了编码端的计算资源消耗。
conclusion: LiVeAction为资源受限环境下的实时多模态数据压缩提供了一种高效、通用且易于部署的解决方案。
---

## 摘要
现代传感器产生丰富的高保真数据，但运行在可穿戴或遥感设备上的应用仍受限于带宽和功耗预算。JPEG 和 MPEG 等标准化编解码器在比特率和感知质量之间实现了高效权衡，但它们是为人类感知设计的，限制了其在机器感知任务和非传统模态（如空间音频阵列、高光谱图像和 3D 医学图像）中的适用性。基于标量量化或分辨率降低的通用压缩方案虽然适用范围广，但未能利用固有的信号冗余，导致率失真性能欠佳。最近的生成式神经编解码器（或分词器）能够对复杂的信号依赖关系建模，但通常存在参数冗余、数据需求量大且模态特定等问题，使其在资源受限的环境中难以应用。我们提出了一种轻量级、通用且非对称的神经编解码器架构（LiVeAction），通过两个关键思想解决了这些局限性：(1) 为了降低编码器复杂度以满足执行环境的资源限制，我们引入了类 FFT 结构，并减小了基于神经网络的分析变换的整体规模和深度；(2) 为了支持任意信号模态并简化训练，我们用基于方差的速率惩罚取代了对抗性损失和感知损失。我们的设计所产生的编解码器与最先进的生成式分词器相比，提供了更优越的率失真性能，同时在低功耗传感器上的部署仍具有实用性。我们在 https://github.com/UT-SysML/liveaction 发布了代码、实验和 Python 库。

## Abstract
Modern sensors generate rich, high-fidelity data, yet applications operating on wearable or remote sensing devices remain constrained by bandwidth and power budgets. Standardized codecs such as JPEG and MPEG achieve efficient trade-offs between bitrate and perceptual quality but are designed for human perception, limiting their applicability to machine-perception tasks and non-traditional modalities such as spatial audio arrays, hyperspectral images, and 3D medical images. General-purpose compression schemes based on scalar quantization or resolution reduction are broadly applicable but fail to exploit inherent signal redundancies, resulting in suboptimal rate-distortion performance. Recent generative neural codecs, or tokenizers, model complex signal dependencies but are often over-parameterized, data-hungry, and modality-specific, making them impractical for resource-constrained environments. We introduce a Lightweight, Versatile, and Asymmetric neural codec architecture (LiVeAction), that addresses these limitations through two key ideas. (1) To reduce the complexity of the encoder to meet the resource constraints of the execution environments, we impose an FFT-like structure and reduce the overall size and depth of the neural-network-based analysis transform. (2) To allow arbitrary signal modalities and simplify training, we replace adversarial and perceptual losses with a variance-based rate penalty. Our design produces codecs that deliver superior rate-distortion performance compared to state-of-the-art generative tokenizers, while remaining practical for deployment on low-power sensors. We release our code, experiments, and python library at https://github.com/UT-SysML/liveaction .

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LiVeAction** 的新型神经编解码器设计，旨在解决资源受限环境下（如可穿戴设备、遥感卫星）多模态数据的高效压缩问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代传感器产生海量高保真数据，但边缘设备受限于带宽和功耗。
*   **研究背景**：
    *   **传统编解码器（如 JPEG/MPEG）**：虽计算开销低，但专为人类视觉设计，不适用于机器感知任务或非传统模态（如高光谱、3D 医学图像）。
    *   **通用压缩方案（如标量量化）**：虽通用但忽略了信号冗余，率失真（Rate-Distortion）性能差。
    *   **现有神经编解码器（如生成式分词器）**：性能虽强，但通常参数过度冗余、计算昂贵（尤其是编码端）、且往往针对特定模态定制，难以在低功耗设备上实时运行。
*   **目标**：设计一种**轻量级、通用且非对称**的架构，在保证高压缩率的同时，实现极高的编码效率。

### 2. 方法论：核心思想与技术细节
LiVeAction 的设计围绕三个核心支柱展开：

*   **非对称架构与类 FFT 编码器**：
    *   **非对称性**：将计算压力转移到解码端（云端），使编码端（边缘端）极度轻量。
    *   **结构化卷积**：编码器（分析变换）放弃了深层密集投影，采用受 FFT 启发的**分块对角结构（ShuffleNet/Monarch 风格）**。通过分组卷积交替非线性激活，以极低的计算成本（MACs）实现了多层特征提取。
*   **多模态通用性设计**：
    *   **小波包变换（WPT）**：作为预处理步骤，将时空分辨率转换为频率分辨率，使后续网络能以统一方式处理 1D、2D 或 3D 信号。
    *   **EfficientViT 解码器**：解码端采用线性注意力机制的 EfficientViT，保证了强大的重建能力，同时支持高分辨率训练。
*   **简化的率失真优化目标**：
    *   **方差基速率惩罚**：放弃了复杂的对抗损失和感知损失（这些损失难以跨模态定义且训练不稳定）。
    *   **公式**：训练目标简化为 $L = \log_{10} \|x - \hat{x}\|_2^2 + \lambda \log_2(\text{Var}[\text{latents}])$。假设潜在变量服从指数族分布，通过最小化样本方差的对数来近似最小化比特率。
    *   **量化**：使用有限标量量化（FSQ），结合“先软后硬”的训练策略，确保量化过程可微且高效。

### 3. 实验设计
*   **数据集与场景**：
    *   **音频**：立体声音乐（MUSDB18-HQ）、7 通道空间音频（Aria 麦克风阵列）。
    *   **图像**：RGB 图像（LSDIR 训练，ImageNet/Kodak 测试）。
    *   **视频**：1080p 视频（Vimeo90k 训练，DAVIS 测试）。
    *   **科学/医学**：224 波段高光谱图像（AVIRIS）、3D 医学 CT 卷（MedMNIST 3D）。
*   **Benchmark 与对比方法**：
    *   **传统标准**：JPEG 2000、AVIF、JPEG-LS。
    *   **神经编解码器**：Stable Audio (音频)、EnCodec (音频)、Cosmos (图像/视频)、WaLLoC (通用)。
*   **评估指标**：PSNR、SSIM、DISTS、BD-rate、吞吐量（Throughput）、下游任务准确率（ImageNet Top-1）。

### 4. 资源与算力
*   **硬件环境**：
    *   **训练/GPU 推理**：NVIDIA RTX 4090。
    *   **高性能 CPU 推理**：Intel Core i9-13900k (Raptor Lake)。
    *   **低功耗移动 CPU 推理**：Intel Core Ultra 255U (Arrow Lake)。
*   **训练时长**：文中提到不同模态的训练步数从 120k 到 863k 不等。作者强调，相比 Cosmos 等模型，LiVeAction 仅需数千个样本和极少的 GPU 小时即可完成训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1D 到 3D 的 **6 种不同模态**，这在神经压缩论文中是非常罕见的，充分验证了其“通用性”。
*   **消融实验**：针对“简化速率损失”进行了专门的消融研究，对比了显式速率模型与方差近似模型的性能差异。
*   **客观性**：实验不仅测量了重建质量（PSNR/SSIM），还测量了实际硬件上的吞吐量（Mpix/sec）和下游机器感知准确率，评估维度全面且客观。

### 6. 主要结论与发现
*   **性能领先**：在 RGB 图像上，相比 NVIDIA 的 Cosmos 模型，LiVeAction 在 BD-rate 上提升了 **34%**，且编码速度快了 **10 倍以上**。
*   **极高效率**：在低功耗移动 CPU 上，LiVeAction 达到了最高的编码吞吐量（约 10 Mpix/s），远超 AVIF 和其他神经方法。
*   **机器感知友好**：尽管未在 ImageNet 上训练，但其解码图像在下游分类任务中的表现与 Cosmos 相当，且比特率降低了 48%。
*   **跨模态优越性**：在空间音频、高光谱和 3D 医学图像上均显著优于传统标准（如 JPEG 2000）和现有的神经编解码器。

### 7. 优点与亮点
*   **极致的编码效率**：通过类 FFT 结构和非对称设计，解决了神经编解码器难以在边缘端部署的痛点。
*   **真正的通用性**：一套架构逻辑通吃音频、图像、视频和科学数据，无需针对模态修改损失函数。
*   **训练门槛低**：无需复杂的感知/对抗损失调优，训练稳定且对数据量要求相对较低。
*   **开源贡献**：提供了完整的 Python 库和预训练模型，具有很高的实用价值。

### 8. 不足与局限
*   **感知细节缺失**：由于放弃了生成式对抗损失，解码图像在极低比特率下可能缺乏纹理细节（虽然文中展示了可以用后处理生成模型来弥补，但这增加了整体复杂度）。
*   **固定速率限制**：目前的设计主要针对特定比特率训练，尚未深入探讨单模型变速率（Variable-rate）的实现。
*   **硬件依赖**：虽然在 CPU 上表现优异，但其结构化卷积在某些缺乏分组卷积优化的硬件平台上可能无法发挥最大潜力。

（完）
