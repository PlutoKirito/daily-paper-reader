---
title: Tube-Structured Incremental Semantic HARQ for Generative Video Receivers
title_zh: 面向生成式视频接收器的管状结构增量语义 HARQ
authors: "Xuesong Wang, Xinyan Xie, Runxin Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09897v1"
tags: ["query:vcodec"]
score: 9.0
evidence: 用于从紧凑语义重建视频的生成式语义通信
tldr: 本文针对生成式视频语义通信中错误累积和带宽受限的挑战，提出了一种基于管状结构的增量语义HARQ机制。该方法将时间局部的包作为重传单元，在受限预算下优化AoIS-AUC目标。实验证明，在恶劣信道环境下，该机制能显著降低时间加权恢复成本并加快恢复轨迹的稳定，提升了视频重建的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09897v1/fig-001.webp\", \"caption\": \"TABLE I MAIN EXPERIMENTAL PARAMETERS.\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-002.webp\", \"caption\": \"Fig. 2. PER sweep under the GE packet-erasure channel at K = 8, with absolute AoIS-AUC curves (top) and paired gaps to the two block-based baselines (bottom). Hollow and filled markers denote bc = 2 and bc = 3, respectively.\", \"page\": 4, \"index\": 2, \"width\": 524, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-003.webp\", \"caption\": \"Fig. 3. PER sweep of AoIS-AUC at K = 16.\", \"page\": 4, \"index\": 3, \"width\": 524, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-004.webp\", \"caption\": \"Fig. 4. Protocol audit under PER sweeps at bc = 2: package-transport ratio and average package span for K ∈ {4, 8, 16}.\", \"page\": 5, \"index\": 4, \"width\": 524, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-005.webp\", \"caption\": \"Fig. 5. Recovery-time gap under PER sweeps for K ∈ {8, 16} and bc ∈ {2, 3}. More negative values indicate earlier threshold crossing.\", \"page\": 5, \"index\": 5, \"width\": 524, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-006.webp\", \"caption\": \"Fig. 6. Motion-stratified AoIS-AUC and recovery-time gaps under PER sweeps.\", \"page\": 5, \"index\": 6, \"width\": 524, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2605.09897v1/fig-007.webp\", \"caption\": \"Fig. 1. System overview of receiver-driven semantic HARQ for generative video reconstruction. The receiver requests tube-structured packages, the forward link transports them as atomic HARQ objects, and successful deliveries are committed before diffusion inpainting and the next feedback decision.\", \"page\": 2, \"index\": 7, \"width\": 1066, \"height\": 356}]"
motivation: 现有的生成式语义通信在视频传输中面临错误累积和重传单元设计不合理的问题，难以在有限预算下实现高效的可靠恢复。
method: 提出管状结构的包原生请求机制，将时间局部的包作为信道可见的HARQ对象，进行细粒度的传输、丢弃和提交。
result: 在中等至恶劣的信道条件下，该方法比传统的基于块的基准方案具有更低的时间加权恢复成本，且恢复轨迹更早趋于稳定。
conclusion: 研究证明了重传原语的设计是语义通信系统中的关键变量，管状结构能有效提升生成式视频接收器的鲁棒性和传输效率。
---

## 摘要
生成式语义通信利用接收端的生成先验从紧凑的语义中重建视觉内容，这使其在带宽受限的多媒体传输中极具吸引力。对于视频而言，可靠的恢复仍然具有挑战性，因为误差会随时间累积，有用的证据具有时间相关性，且接收器必须在有限的交互、重传和重建预算下做出决策。现有的生成式语义通信研究主要侧重于表示、压缩或生成式重建，而近期的容错和语义 HARQ 方法在很大程度上仍基于编码器定义的或帧块重传单元进行操作。本文研究了在预算受限的 AoIS-AUC 目标下，面向生成式视频重建的接收器驱动语义 HARQ，并提出重传原语本身就是一个重要的系统设计变量。我们提出了管状结构的包原生请求，其中时间局部的包是信道可见的 HARQ 对象，并以包粒度进行传输、丢弃、接收和提交。在具有匹配骨干网络、预算和信道模型的受控对比协议下，该原语在实际相关的中等至恶劣信道环境下，比具有竞争力的基于块的基准方法产生了更低的时间加权恢复成本，而在接近无噪声的信道中，这种差距自然会缩小。增益主要表现为恢复轨迹的更早稳定，而最终质量终点在大体上保持相当，且即使与感知管状结构的块排序基准相比，这种优势依然存在。

## Abstract
Generative semantic communication uses receiver-side generative priors to reconstruct visual content from compact semantics, making it attractive for bandwidth-limited multimedia delivery. For video, reliable recovery remains difficult because errors accumulate over time, useful evidence is temporally correlated, and the receiver must make decisions under limited interaction, retransmission, and reconstruction budgets. Existing generative semantic communication studies mainly emphasize representation, compression, or generative reconstruction, while recent error-resilient and semantic-HARQ methods still largely operate on encoder-defined or frame-block retransmission units. This paper studies receiver-driven semantic HARQ for generative video reconstruction under a budget-constrained AoIS-AUC objective and argues that the retransmission primitive is itself an important system design variable. We propose tube-structured package-native requests, in which temporally local packages are the channel-visible HARQ objects and are transmitted, dropped, received, and committed at package granularity. Under a controlled comparison protocol with matched backbone, budgets, and channel model, this primitive yields lower time-weighted recovery cost than competitive block-based baselines in practically relevant moderate-to-harsh regimes, while the gap naturally shrinks in near-clean channels. The gain mainly appears as earlier stabilization of the recovery trajectory, while final-quality endpoints remain broadly comparable, and it persists even against a tube-aware block-ranking baseline.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tube-Structured Incremental Semantic HARQ for Generative Video Receivers》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：生成式语义通信（Generative SemCom）利用接收端的生成先验（如扩散模型）从极度压缩的语义中重建视频。
*   **核心挑战**：视频传输中存在时间上的误差累积，且有用的语义证据在时空上高度相关。现有的语义重传机制（HARQ）大多沿用传统的“帧-块”或编码器定义的固定单元，忽略了视频语义的时空连续性，导致接收端在有限的重传预算下难以快速从错误状态恢复。
*   **研究目的**：探讨重传原语（Retransmission Primitive）本身作为设计变量的重要性，旨在优化受限预算下的视频恢复速度和质量。

### 2. 方法论：管状结构增量语义 HARQ
*   **核心思想**：将视频中物体的时空轨迹投影到潜在空间（Latent Space），构建“管状结构包”（Tube-structured Packages）作为信道可见的原子重传对象，而非孤立的图像块。
*   **关键技术细节**：
    *   **管状包构建**：将视频剪辑映射为潜在张量，根据物体区域提取时空潜在块管，并将其切分为具有固定大小约束、时间局部的包。
    *   **AoIS-AUC 目标**：采用“错误语义龄期-曲线下面积”（Age of Incorrect Semantics - Area Under the Curve）作为优化目标，该指标不仅关注最终重建质量，更优先考虑从错误状态转换到可靠状态的速度。
    *   **接收端贪婪请求策略**：接收端根据包的未覆盖比例、平均支持区域和时间跨度对候选包进行评分排序，在每轮 HARQ 中请求得分最高的包。
    *   **扩散修复（Diffusion Inpainting）**：接收端使用预训练的生成模型（如 DiffuEraser），根据已接收的潜在块和掩码进行视频重建。

### 3. 实验设计
*   **数据集与场景**：使用 **DAVIS 2017** 数据集（480p 分辨率），涵盖了不同的运动强度。
*   **信道模型**：采用 Gilbert-Elliott (GE) 突发丢包信道模型，模拟真实的无线环境。
*   **对比方法（Benchmarks）**：
    *   **Greedy Block Requests**：基于块的贪婪请求（传统基准）。
    *   **Tube-Weighted Block Requests**：感知管状信息但仍以块为传输单位的增强基准（用于证明“包”原语本身的增益）。
    *   **Hysteresis Trigger**：带有滞后触发规则的参考方案。
    *   **Offline Planning**：非因果的离线规划参考（理想上限）。
*   **评估指标**：AoIS-AUC、恢复延迟（$t_\alpha$）、PSNR、MS-SSIM、tLPIPS。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **计算成本模型**：论文将计算预算（Compute Budget, $b_c$）作为一个关键变量（设为 2 或 3），指出扩散推理过程是主要的计算开销，并将其建模为系统总耗时的一部分。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了广泛的误码率（PER）扫描实验（从 0.05 到 0.30）。
    *   针对不同的请求预算（K=8, 16）和计算预算（bc=2, 3）进行了多组对比。
    *   进行了运动分层分析（低运动 vs 高运动视频）。
*   **充分性评价**：实验设计较为充分且客观。通过引入“感知管状信息的块请求”作为基准，公平地隔离了“传输原语改变”带来的纯粹增益。实验覆盖了从清洁到恶劣的多种信道环境，验证了方案的鲁棒性。

### 6. 主要结论与发现
*   **恢复速度领先**：在中等至恶劣的信道环境下，管状包原语比基于块的方案能提前多达 **6 轮** 达到恢复阈值。
*   **AoIS-AUC 优化**：该方法显著降低了时间加权恢复成本，使视频重建轨迹更早进入稳定状态。
*   **质量保持**：在提升恢复速度的同时，最终的重建质量（PSNR/SSIM）与基准方案持平，证明增益并非通过牺牲最终精度获得。
*   **信道适应性**：在接近清洁的信道中增益较小，但在实际相关的中高丢包率环境下优势明显。

### 7. 优点与亮点
*   **原语创新**：突破了传统的块传输思维，将视频的时空相干性直接引入 HARQ 的传输单元设计中。
*   **目标导向**：引入 AoIS-AUC 指标，更符合生成式视频应用中用户对“快速消除伪影/错误”的感知需求。
*   **轻量化控制**：请求策略基于简单的评分机制，相比复杂的强化学习方法，在线计算开销极低。

### 8. 不足与局限
*   **依赖预处理**：管状结构的提取依赖于离线的物体分割和投影，对于完全动态、无固定物体的场景可能效果受限。
*   **生成模型依赖**：性能高度依赖于后端扩散修复模型的质量，若模型本身存在偏见或能力不足，传输原语的优化效果会被掩盖。
*   **信道模型单一**：主要在 GE 信道下验证，对于更复杂的多径衰落或干扰环境的适应性有待进一步研究。

（完）
