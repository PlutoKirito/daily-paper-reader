---
title: Rejection Sampling is Optimal for Relative Entropy Coding
title_zh: 拒绝采样对于相对熵编码是最优的
authors: "Spencer Hill, Fady Alajaji, Tamás Linder, Gergely Flamich"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23076v1"
tags: ["query:entropy-coding"]
score: 8.0
evidence: 相对熵编码与功能信息界限
tldr: 本研究探讨了相对熵编码中互信息下界与实际速率之间的对数间隙问题。作者提出了一种基于拒绝采样的“套圈编码”方法，证明了其比特率仅比函数信息量高出常数项，从而证实了拒绝采样在相对熵编码中的最优性。该成果不仅将互信息下界的差距缩小到目前最优水平，还统一了单次与渐近场景下的理论界限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23076v1/fig-001.webp\", \"caption\": \"Fig. 2. A visualization of the acceptance region in rejection sampling for an additive white Gaussian noise channel Y = X+N with X ∼ N (0, 0.75) and N ∼ N (0, 0.25), where X and Y are truncated to a large but finite range to ensure a bounded density ratio. Here, x = −0.5 and the two proposal samples are Y1 = 0.3, Y2 = −0.4 with heights L1 = 1.5 and L2 = 1.1, where we use the shorthand Li ≜ MUi. We see that x ̸∈ S1 but x ∈ S2, meaning Y1 is rejected and Y2 is accepted. The PX -probability of hitting the set Si is exactly wYi (Li).\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 233}]"
motivation: 旨在解决传统互信息量无法准确衡量相对熵编码速率，且存在无法消除的对数间隙的问题。
method: 构造了一种名为“套圈编码”的拒绝采样编码方案，并利用函数信息量作为新的衡量指标。
result: 证明了该方法能以极小的常数差距达到函数信息量下界，并刷新了互信息下界的可实现性记录。
conclusion: 研究表明拒绝采样是相对熵编码的最优策略，且函数信息量是描述该过程速率的本质度量。
---

## 摘要
在相对熵编码中，发送者旨在设计一种随机代码，使得在输入 $X \sim P_X$ 时，接收者可以生成样本 $Y \sim P_{Y \mid X}$。标准结果表明：(1) 这至少需要 $I(X; Y)$ 比特；(2) 该下界可以在对数间隙内实现；(3) 通常情况下，该间隙无法缩小。间隙的必要性表明，互信息并不是量化相对熵编码速率的正确信息度量。Flamich 等人 (2025) 的工作中出现了一个潜在的替代方案，他们证明了一个更紧的下界 $I_F(X \to Y)$，我们称之为函数信息 (functional information)。在本文中，我们通过构建“套圈代码” (ring toss code) 证明了该下界是紧的，这是一种用于拒绝采样的编码方法，最多使用 $I_F(X \to Y) + \log e$ 比特。这证明了拒绝采样对于相对熵编码是最优的。我们的结果意味着，经典的互信息下界在一般情况下可以在 $\log(I(X; Y) + 1) + 2.45$ 比特内实现，在奇异信道下可以在 $1.45$ 比特内实现，这两者都是迄今为止同类界限中最紧的。此外，我们的单次 (one-shot) 结果还恢复了 Sriramu 和 Wagner 关于相对熵代码二阶冗余的渐近结果。

## Abstract
In relative entropy coding, a sender aims to design a stochastic code such that, on input $X \sim P_X$, the receiver can generate a sample $Y \sim P_{Y \mid X}$. It is a standard result that (1) this requires at least $I(X; Y)$ bits, (2) the lower bound is achievable within a logarithmic gap, and (3) this gap cannot be reduced in general. The necessity of the gap suggests that the mutual information is not the correct information measure to quantify the rate of relative entropy coding.   A potential alternative emerged in the work of Flamich et al. (2025), who proved a tighter lower bound of $I_F(X \to Y)$, a quantity we call the functional information. In this paper, we show that this lower bound is tight by constructing the ring toss code, an encoding method for rejection sampling which uses at most $I_F(X \to Y) + \log e$ bits. This demonstrates that rejection sampling is optimal for relative entropy coding. Our result implies that the classical mutual information lower bound is achievable within $\log(I(X; Y) + 1) + 2.45$ bits in general and within $1.45$ bits for singular channels, which are both the tightest bounds of their kind to date. Moreover, our one-shot result also recovers Sriramu and Wagner's asymptotic results on the second-order redundancy of relative entropy codes.

---

## 论文详细总结（自动生成）

这篇论文《Rejection Sampling is Optimal for Relative Entropy Coding》（拒绝采样对于相对熵编码是最优的）是一篇理论信息论论文，旨在解决相对熵编码（Relative Entropy Coding, REC）中长期存在的速率界限不紧凑的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在相对熵编码（又称信道模拟）中，发送者希望通过共享随机性 $Z$ 传输最少的比特，使接收者能从条件分布 $P_{Y|X}$ 中采样。
*   **研究背景**：传统理论认为其下界是互信息 $I(X; Y)$，但实际编码方案总会存在一个无法消除的“对数间隙”（logarithmic gap）。
*   **动机**：这暗示 $I(X; Y)$ 可能不是衡量 REC 速率的最优指标。近期研究提出了“函数信息”（Functional Information, $I_F$）作为更紧的下界。本文的目标是证明这个新下界在单次（one-shot）场景下是可实现的，从而确立拒绝采样在 REC 中的最优地位。

### 2. 方法论
*   **核心思想**：构建了一种名为**“套圈代码”（Ring Toss Code）**的编码方案。该方案本质上是对标准拒绝采样（Rejection Sampling）返回的索引 $K$ 进行了一种改进的条件编码。
*   **关键技术细节**：
    *   **条件编码**：传统的拒绝采样编码直接对索引 $K$ 进行编码，忽略了编码器和解码器共享的随机性 $Z$。套圈代码利用 $Z$ 计算每个候选样本的“宽度函数”（Width Function），从而得到一个更高效的条件分布 $Q_{K|Z}$。
    *   **搜索空间转换**：该方法将 REC 问题从在输出空间 $Y$ 中搜索样本，转化为在输入空间 $X$ 中搜索能使样本被接受的集合。
*   **数学保证**：证明了该编码方案的平均长度满足 $H(K|Z) \leq I_F(X \to Y) + \log e$。其中 $\log e \approx 1.44$ 比特是一个常数开销。

### 3. 实验设计与对比
*   **研究性质**：本文属于**纯理论证明论文**，未涉及大规模数据集的实证实验。
*   **对比场景**：
    *   **奇异信道（Singular Channels）**：如二元擦除信道、加性均匀噪声信道。在这些信道中，证明了 $I_F = I(X; Y)$，即套圈代码能实现常数级的冗余。
    *   **一般信道**：对比了 Poisson 泛函表示、贪婪拒绝采样等现有方案。
*   **Benchmark（基准）**：对比的是 Li 和 El Gamal (2018) 提出的强泛函表示引理中的界限。本文将互信息上方的冗余从 $\log(I+1) + 4$ 降低到了 $\log(I+1) + 2.45$。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU 或大规模算力资源的使用。
*   **原因**：作为信息论的理论研究，其核心贡献在于数学定理的推导和证明，而非算法的工程实现或模型训练。

### 5. 实验数量与充分性
*   **充分性评价**：虽然没有传统意义上的实验组，但论文通过**严谨的数学证明**覆盖了：
    1.  单次编码的速率上界。
    2.  奇异信道的特殊性质分析。
    3.  渐近场景下的二阶冗余恢复。
*   **客观性**：证明过程基于标准的概率论和信息论框架，逻辑严密，结论具有普适性。

### 6. 主要结论与发现
*   **拒绝采样的最优性**：证明了拒绝采样配合适当的索引编码，在 REC 中确实是最优的。
*   **刷新记录**：提出了目前为止最紧的 REC 速率上界：$I(X; Y) + \log(I(X; Y) + 1) + 2.45$ 比特。
*   **理论统一**：该单次结果成功恢复并简化了前人关于渐近冗余的复杂结论，证明了在非奇异信道下，对数冗余的系数确实是 $1/2$。

### 7. 优点
*   **简洁性**：算法逻辑（套圈代码）非常简单，仅通过改变拒绝采样索引的编码方式就达到了理论极限。
*   **理论突破**：解决了 REC 领域关于“互信息是否是正确度量”的长期争论，确立了函数信息 $I_F$ 的核心地位。
*   **通用性**：结果适用于任何具有有界密度比的信道。

### 8. 不足与局限
*   **有界性假设**：算法要求密度比 $dP_{Y|X}/dP_Y$ 必须有一个通用的上界 $M$。如果 $M$ 趋于无穷（如某些无界支撑的连续分布），拒绝采样可能无法在有限时间内终止。
*   **计算复杂度**：虽然通信复杂度（比特数）是最优的，但在某些复杂信道下，计算宽度函数和进行拒绝采样的计算开销可能仍然很高。
*   **缺乏实操指导**：论文侧重于存在性证明和速率界限，对于如何在具有超高维特征的实际机器学习任务中高效部署该编码器提及较少。

（完）
