---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 汽车弱势路面使用者安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:train-trick"]
score: 9.0
evidence: 用于边缘 AI 高效模型训练和量化鲁棒性的知识蒸馏
tldr: 本研究针对自动驾驶中弱势群体（VRU）检测在边缘设备部署的难题，提出了一种知识蒸馏（KD）框架。通过让轻量级的YOLOv8-S学生模型学习大型YOLOv8-L教师模型的特征，实现了3.9倍的模型压缩。实验表明，该方法显著提升了模型在INT8量化下的鲁棒性，在保持高精度的同时大幅减少了误报，为安全关键型的边缘AI部署提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 解决大型目标检测模型在边缘硬件进行INT8量化时精度大幅下降，而小型模型原生性能不足的问题。
method: 采用知识蒸馏框架，训练参数量仅为11.2M的YOLOv8-S学生模型去模仿43.7M参数的YOLOv8-L教师模型。
result: "在BDD100K数据集上，KD学生模型在INT8量化下的精度比直接训练提升了14.5%，且误报率比量化后的教师模型降低了44%。"
conclusion: 知识蒸馏是实现在边缘硬件上部署高精度、高鲁棒性且符合安全要求的VRU检测系统的关键技术。
---

## 摘要
在边缘硬件上部署用于弱势路面使用者（VRU）安全的高精度目标检测，需要在模型容量与计算约束之间取得平衡。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，训练紧凑的 YOLOv8-S 学生模型（11.2M 参数）来模仿 YOLOv8-L 教师模型（43.7M 参数），在实现 3.9 倍压缩的同时保留了量化鲁棒性。我们在全规模 BDD100K 数据集（7万张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 下性能大幅下降（mAP 下降 23%），而经过 KD 的学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，KD 传递的是精度校准而非原始检测能力：在 INT8 下，KD 学生模型的精度达到 0.748，而直接训练的模型仅为 0.653，在相同召回率下提升了 14.5%，且相比性能崩溃的教师模型，误报率降低了 44%。在 INT8 量化下，KD 学生模型的精度（0.748）甚至超过了体积大 3.9 倍的教师模型的 FP32 精度（0.718）。这些发现表明，知识蒸馏是在边缘硬件上部署高精度、安全关键型 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

### 论文总结：通过知识蒸馏实现汽车弱势路面使用者（VRU）安全的可部署边缘检测

#### 1. 核心问题与研究背景
*   **研究动机**：在自动驾驶和高级驾驶辅助系统（ADAS）中，检测弱势路面使用者（VRU，如行人、骑行者）至关重要。
*   **核心矛盾**：大型深度学习模型（如 YOLOv8-L）虽然精度高，但在边缘硬件所需的 **INT8 量化**下会出现严重的性能崩溃；而小型模型（如 YOLOv8-S）虽然计算效率高，但在处理样本较少的类别时精度不足。
*   **研究目标**：探索如何利用**知识蒸馏（KD）**技术，训练出一个既能保持轻量化（易于边缘部署），又能继承大型模型精度和量化鲁棒性的学生模型。

#### 2. 方法论
*   **核心思想**：采用任务阻尼（Task-dampened）知识蒸馏框架，让紧凑的学生模型（YOLOv8-S）模仿大型教师模型（YOLOv8-L）的软输出和中间特征。
*   **关键技术细节**：
    *   **损失函数设计**：总损失 $L_{total} = \alpha \cdot L_{task} + \beta \cdot L_{logit} + \gamma \cdot L_{feat}$。
    *   **Logit 蒸馏**：使用 KL 散度和温度缩放（Temperature Scaling, $T=10$）来软化输出分布，传递类别间的相似性信息。
    *   **特征蒸馏**：通过 L2 距离对齐学生和教师模型骨干网络的中间特征图。
    *   **任务损失阻尼**：将 $\alpha$ 设为 0.5，降低地面真值（Ground Truth）的监督权重，从而增强教师模型“置信度校准”信息的传递。
*   **量化策略**：采用 TensorRT 的训练后量化（PTQ）技术，将模型从 FP32 转换为 INT8 精度。

#### 3. 实验设计
*   **数据集**：使用 **BDD100K**，这是目前最大的驾驶场景数据集，包含 7 万张训练图像和 1 万张验证图像。重点评估行人、自行车骑行者和摩托车骑行者三类 VRU。
*   **基准模型（Benchmark）**：
    *   **Teacher**：直接训练的 YOLOv8-L（43.7M 参数）。
    *   **Direct Student**：直接训练的 YOLOv8-S（11.2M 参数）。
    *   **KD Student**：通过知识蒸馏训练的 YOLOv8-S。
*   **对比维度**：FP32 与 INT8 精度下的 mAP50、mAP50-95、精确率（Precision）、召回率（Recall）以及误报率（FAR）。

#### 4. 资源与算力
*   **训练设备**：使用云端 GPU 基础设施，具体为 **NVIDIA RTX PRO 6000 Blackwell** (96GB VRAM)。
*   **训练时长**：教师模型耗时 7 小时，直接训练的学生模型耗时 2.7 小时，KD 学生模型因需运行教师模型前向传播，耗时 4.9 小时。
*   **推理测试设备**：**NVIDIA RTX 5070** (8GB VRAM)，用于模拟边缘部署环境。

#### 5. 实验数量与充分性
*   **实验规模**：在全规模 BDD100K 数据集上进行，样本量充足。
*   **消融实验**：针对任务损失权重 $\alpha$ 进行了对比实验（$\alpha=0.5$ vs $\alpha=1.0$），验证了“任务阻尼”是实现精度校准传递的关键。
*   **客观性**：实验涵盖了从 FP32 到 INT8 的全流程对比，并针对 VRU 特定类别进行了细分评估，数据对比清晰，具有较强的说服力。

#### 6. 主要结论与发现
*   **量化鲁棒性**：教师模型在 INT8 量化下 mAP 骤降 23.4%，而 KD 学生模型仅下降 5.6%，证明 KD 显著提升了模型的量化稳定性。
*   **精度校准传递**：KD 传递的主要是“精度校准”而非“原始检测能力”。在 INT8 下，KD 学生模型的精确率比直接训练提升了 14.5%，误报率（FAR）比崩溃的教师模型降低了 44%。
*   **部署优势**：KD 学生模型实现了 3.9 倍的参数压缩和 2.37 倍的推理加速，且在 INT8 下的精确率（0.748）甚至超过了教师模型在 FP32 下的表现（0.718）。

#### 7. 优点
*   **实用性强**：直接针对边缘 AI 部署中的 INT8 量化痛点，提供了可落地的技术方案。
*   **安全导向**：重点关注误报率（FAR），这对于提升自动驾驶系统的驾驶员信任度和安全性至关重要。
*   **发现深刻**：揭示了 KD 在量化过程中保留“置信度校准”的独特作用，而非仅仅是提升 mAP。

#### 8. 不足与局限
*   **架构单一**：实验仅基于 YOLOv8 系列，尚未在其他架构（如 Transformer-based 检测器）上验证通用性。
*   **硬件偏差**：虽然使用了 RTX 5070 进行推理测试，但尚未在真正的车载嵌入式硬件（如 NVIDIA Jetson）上进行功耗和热管理分析。
*   **量化方法限制**：仅使用了 PTQ（训练后量化），未探讨量化感知训练（QAT）与 KD 结合可能带来的进一步提升。

（完）
