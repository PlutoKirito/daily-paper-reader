---
title: Accelerating HEVC Intra Partitioning via a CNN-Hierarchical Attention Transformer Hybrid
title_zh: 通过CNN-层次注意力变换器混合架构加速HEVC帧内分区
authors: "Krishna Kumar Sharma, Somdyuti Paul"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.29063v1"
tags: ["query:train-trick"]
score: 9.0
evidence: 直接通过CNN-Transformer混合加速HEVC帧内分区预测，降低编码复杂度
tldr: "针对HEVC帧内编码中递归四叉树划分的高计算开销，现有CNN加速器因局部感受野无法捕获全局语义，而Transformer延迟过高不适用于CPU。提出混合架构HFViT，融合可重参数化深度可分离卷积与层次注意力Transformer，通过载波令牌机制以亚二次复杂度实现全局信息传播，并采用后训练结构融合降低延迟。在JCT-VC序列上，相比ETH-CNN基线，HFViT将VMAF BD-rate惩罚平均降低2.4-7.9个百分点，CPU推理延迟仅增加8%，GPU加速达40%。该工作为HEVC帧内编码加速提供了兼顾质量与速度的实用方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29063v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 217, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 2352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 1790, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 493, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 613, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1862, \"height\": 1306, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1848, \"height\": 2039, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29063v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 482, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 932, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 930, \"height\": 522, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1548, \"height\": 664, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1886, \"height\": 674, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 923, \"height\": 152, \"label\": \"Table\"}]"
motivation: 现有CNN加速器局部感受野不足，Transformer延迟过高，缺乏适合CPU环境的混合架构。
method: 提出HFViT，融合可重参数化深度可分离卷积与层次注意力Transformer，采用载波令牌机制和结构融合。
result: "相比ETH-CNN，HFViT降低VMAF BD-rate惩罚2.4-7.9个百分点，CPU延迟增加8%，GPU加速40%。"
conclusion: HFViT在编码质量与推理速度间取得优异平衡，适合HEVC实时编码场景。
---

## 摘要
高效视频编码(HEVC)中的递归四叉树分区带来了巨大的计算开销，其中CTU分区预测的穷举率失真优化占据了编码时间的绝大部分。尽管通过深度学习进行分区预测已成为一种可行的编码加速方法，但架构上的二分法仍未得到充分解决：CNN计算效率高，但由于其局部有效感受野而空间短视，无法捕捉长距离语义关系和重复纹理；相反，基于变换器的架构更擅长捕捉全局上下文，但会带来不可承受的CPU延迟，这是阻碍部署在主要受限于CPU环境中的关键问题。本文介绍了混合快速视觉变换器(HFViT)，这是一种混合架构，旨在加速HEVC帧内模式分区预测。HFViT融合了重新参数化的深度可分离卷积主干与层次注意力变换器(HAT)机制，利用载波令牌方案以次二次复杂度实现高效的全局信息传播。训练后的结构融合将批归一化合并到前几层，进一步减少延迟。全面评估揭示了HFViT在不同分辨率下加速HEVC帧内编码的有效性。在标准JCT-VC测试序列上，与竞争的ETH-CNN基线相比，HFViT在A、B和E类上的平均VMAF BD-rate惩罚分别降低了2.4、2.6和7.9个百分点，同时保持CPU推理延迟在CNN基线的8%以内，并在GPU上超越40%，为实时编码器集成建立了实际可行性。

## Abstract
The recursive quad-tree partitioning in High Efficiency Video Coding (HEVC) incurs considerable computational overhead, with exhaustive rate-distortion optimization for CTU partition prediction consuming the dominant share of encoding time. Although partition prediction through deep learning has emerged as a viable encoding accelerator, an architectural dichotomy remains largely unaddressed: CNNs are computationally efficient but spatially myopic due to their localized effective receptive fields, failing to capture long range semantic relationships and repetitive textures; conversely, transformer based architectures are better at capturing global context but incur prohibitive CPU latency, a critical liability that impedes deployment which is predominantly CPU-bound. This paper introduces Hybrid Fast Vision Transformer (HFViT), a hybrid architecture designed to accelerate HEVC intra-mode partition prediction. HFViT fuses a reparameterized depthwise-separable convolutional backbone with a Hierarchical Attention Transformer (HAT) mechanism, leveraging a carrier token scheme to enable efficient global information propagation at sub-quadratic complexity. Post-training structural fusion collapses batch normalization into preceding layers to further reduce latency. Comprehensive evaluation reveals the efficacy of HFViT in accelerating HEVC intra-encoding across resolutions. On standard JCT-VC test sequences, HFViT reduces the average VMAF BD-rate penalty by 2.4, 2.6, and 7.9 percentage points on Classes A, B and E, respectively, as compared to the competing ETH-CNN baseline while maintaining CPU inference latency within 8% of the CNN baseline and surpassing it on GPU by 40%, establishing practical viability for real-time encoder integration.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：HEVC（H.265）帧内编码采用递归四叉树分区，其率失真优化（RDO）需要穷举搜索所有可能的分区组合，占编码时间的60%–80%，导致计算量巨大。
- **现有方法局限**：
  - 传统启发式方法（基于纹理、边缘等）仅能获得30%–45%的加速，且带来1.5%–3.0%的BD-rate损失。
  - 深度学习方法（CNN）能实现超过50%的编码时间缩减，但CNN的局部感受野限制其捕捉长距离语义和重复纹理，有效感受野仅为理论感受野的一小部分。
  - Vision Transformer（ViT）能建模全局上下文，但自注意力的二次复杂度导致在CPU上推理延迟过高（是CNN的10倍以上），而HEVC编码器通常部署在CPU受限环境中。
- **研究含义**：需要一种兼顾CNN计算效率与Transformer全局建模能力的混合架构，实现在CPU上接近CNN的延迟，同时提升率失真性能。

## 2. 论文提出的方法论
### 核心思想
- 提出**HFViT（Hybrid Fast Vision Transformer）**，融合可重参数化深度可分离卷积主干与层次注意力Transformer（HAT），通过载波令牌（Carrier Token）实现亚二次复杂度的全局信息传播，并通过后训练BN融合进一步降低CPU延迟。

### 关键技术细节
1. **输入与问题定义**：
   - 输入：64×64 CTU亮度块 I ∈ {0,1,…,255}^(64×64) 和量化参数 q ∈ {0,1,…,51}。
   - 输出：21个二值预测（深度0:1个，深度1:4个，深度2:16个），表示每个CU是否分裂。
2. **架构组成**：
   - **深度可分离卷积主干**：4个下采样阶段，通道数从1增加到32（保守递增[8,16,24,32]），使用深度可分离卷积减少参数；最后一层输出特征图F3 ∈ R^(4×4×32)。
   - **层次注意力Transformer（HAT）**：两个HAT块。每个块先将F3分割为4个2×2窗口，每个窗口生成一个载波令牌（通过自适应平均池化）。在窗口内进行多头自注意力（h=2，d_h=16），结合相对位置偏置。然后通过载波令牌交互层（自注意）实现跨窗口全局信息交换。第二个HAT块使用更新后的载波令牌再次与空间令牌交互。
   - **QP条件分类头**：全局平均池化得到32维特征，与归一化QP（q/51）拼接，经过三层MLP（带BN和Dropout）输出21维sigmoid。
   - **后训练BN融合**：推理时将BN参数吸收到前层卷积或全连接层的权重中，消除BN前向开销。
3. **损失函数**：层次化二元交叉熵，根据父块分裂有效性掩码，仅计算有效CU的损失。

## 3. 实验设计
- **数据集**：
  - 训练集：CPH-Intra数据集，包含从2000张图像下采样得到的四种分辨率的CTU及HM 16.5 RDO真值。
  - 测试集：10个标准测试序列（含不同空间/时间信息），从原始4K分辨率下采样到各目标分辨率；以及JCT-VC标准测试集（Class A/B/E），覆盖2560×1600、1920×1080、1280×720。
- **Benchmark**：
  - 参考：HM 16.5编码器（未修改）。
  - 对比方法：ETH-CNN（CNN基线）、H-FCN（轻量级全卷积网络）。
- **评估指标**：
  - 分区预测准确率（L1/L2/L3/整体）。
  - BD-rate（PSNR、VMAF、MS-SSIM）和编码加速比。
- **实验设置**：
  - 训练：每个分辨率单独训练一个模型，一个模型覆盖4个QP（22,27,32,37）。（注：论文提到四个分辨率各训练一个模型；JCT-VC测试时使用的是对应分辨率模型。）
  - 测试：在10个测试序列及JCT-VC标准序列上逐分辨率评估，并报告95%置信区间。

## 4. 资源与算力
- **训练硬件**：GPU：NVIDIA A40（46GB VRAM）；CPU：2×Intel Xeon Silver 4509Y（32核/64线程）。
- **训练超参数**：优化器AdamW，学习率1e-4，权重衰减0.01，批次大小64，余弦退火调度，最大10000轮，损失函数层次化BCE。
- **推理系统**：CPU AMD Ryzen 5 7235HS (3.2 GHz, 4C/8T)，12 GB DDR4 RAM；GPU：未明确说明，但根据表I推断为与训练相同的A40。
- **训练时长**：论文未明确给出具体训练时长。

## 5. 实验数量与充分性
- **实验规模**：
  - 4种分辨率（4928×3264, 2880×1920, 1536×1024, 768×512）下分别评估。
  - 在每个分辨率上，使用10个测试序列报告平均BD-rate及95%置信区间（表V）。
  - JCT-VC标准测试：3个类、共10个序列（表VI）。
  - 消融实验：1组（表VII），在1536×1024分辨率下比较HFViT、无BN融合、无深度可分离卷积。
  - 分区可视化：2个示例帧（图7）。
  - 推理延迟对比：9种架构的CPU/GPU延迟对比（表I）。
- **充分性评价**：
  - 覆盖了多个分辨率、多种质量指标、标准测试集，并提供了统计置信区间，实验设计较全面。
  - 对比方法选取得当（CNN和轻量级CNN基线），且保证了相似参数量。
  - 消融实验数量偏少（仅1组，且只涉及两个组件），对于更复杂的设计选择（如载波令牌个数、头数）未进行系统消融。
  - 未在更低分辨率（如768×512）上测试JCT-VC序列，可能限制泛化性。

## 6. 主要结论与发现
- **率失真性能**：HFViT在所有分辨率和质量指标上均优于或持平于ETH-CNN和H-FCN。尤其在高分辨率（4928×3264）下，VMAF BD-rate平均仅2.81%（较ETH-CNN降低2.15个百分点），速度提升79.41%。
- **速度优势**：CPU推理延迟0.27ms/CTU（仅比ETH-CNN慢8%），GPU延迟0.047ms/CTU（比ETH-CNN快40%），实现了实际可部署的效率。
- **消融结果**：BN融合无损提升8.71%加速；深度可分离卷积在牺牲少量精度（0.5-0.7% BD-rate）下显著提高速度。
- **层次注意力有效性**：通过可视化（图7）证实HFViT能更准确预测精细的深度-2和深度-3分区边界，尤其在过渡纹理区域。

## 7. 优点
- **架构创新**：巧妙结合深度可分离卷积的低延迟与HAT的全局建模，载波令牌实现亚二次复杂度全局传播。
- **实用导向**：针对CPU受限的真实编码场景设计，通过BN融合和保守通道设计将延迟控制在CNN水平。
- **全面评估**：使用两种测试集（自定义+标准）、多种质量指标（PSNR、VMAF、MS-SSIM）、统计置信区间，实验严谨。
- **开源可复现**：提供代码和预训练模型链接。

## 8. 不足与局限
- **低分辨率性能**：在768×512分辨率下，H-FCN在PSNR和MS-SSIM BD-rate上优于HFViT（2.89% vs 3.35%），说明轻量级模型在低分辨率下仍有优势，HFViT可能过参数化。
- **消融实验不充分**：未对载波令牌数量、注意力头数、窗口大小等超参数进行系统消融，也未验证不同QP条件下的模块贡献。
- **跨分辨率泛化**：模型是针对每个分辨率单独训练的，未尝试单一模型适配所有分辨率，实用性受限。
- **计算资源消耗**：训练需要10000轮且使用A40 GPU，资源要求较高，且未报告训练耗时。
- **假设限制**：仅针对HEVC帧内模式，未来需要扩展至帧间、AV1/VVC等更复杂标准。
- **数据集偏差**：CPH-Intra数据集来源于2000张图像，可能无法完全覆盖自然视频的多样性（尤其是低分辨率场景）。

（完）
