Title: Why Self-Supervised Encoders Want to Be Normal

URL Source: https://arxiv.org/pdf/2604.27743v2

Published Time: Tue, 05 May 2026 01:21:07 GMT

Number of Pages: 32

Markdown Content:
# Why Self-Supervised Encoders Want to Be Normal 

Yuval Domb 

yuval@moonmath.ai 

May 03, 2026 

Abstract 

Self-supervised learning has achieved remarkable empirical success in learning robust repre-sentations without explicit labels, most recently demonstrated within the framework of Joint-Embedding Predictive Architectures (JEPA). However, a fundamental question remains: what analytical principles drive these encoders toward specific distributional states? In this paper, we demonstrate that the preference for normal distributions in self-supervised encoders is a direct consequence of the Information Bottleneck (IB) principle. By recasting the IB objective as a rate-distortion problem over the predictive manifold, we provide a theoretical basis for why optimal, target-neutral, latent representations should tend towards isotropic Gaussian states. Under this framework, we show that latent representations correspond to soft clustering of inputs sharing similar predictive distributions, organized within a natural simplex structure. This perspective unifies a wide range of existing supervised and less-supervised objectives and provides a principled explanation for commonly used regularization schemes. Furthermore, we derive practical loss objectives that approximate this structure and demonstrate their effective-ness on standard benchmarks. Ultimately, our framework offers a geometric lens to understand-ing representation collapse and it establishes a mathematical system for regularization strategies to be used to ensure high-entropy, informative embeddings in modern self-supervised models. 

# 1 Introduction 

Learning compact representations that preserve predictive information is a central problem in machine learning. Given observations X and targets Y , the goal is to learn a latent variable W that compresses X while retaining enough information to predict Y . The IB method [40] formalized this tradeoff by minimizing the mutual information I(X; W ) subject to a constraint on the predictive information I(W ; Y ). This paper develops a geometric and information-theoretic framework for encoder-decoder learn-ing, built on the IB principle. Starting from the rate-distortion equivalence of Harrem¨ oes and Tishby [26], with Kullback-Leibler (KL) divergence as the distortion measure, we characterize the optimal representation geometrically as a soft clustering of the predictive manifold M = {p(Y |x) : x ∈ X } 1.A key outcome is a theoretical justification of the Sketched Isotropic Gaussian Regularization (SI-GReg) method [6] as a relaxation of the maximum entropy principle on the predictive manifold, mediated by a chain of transformations from the flat Dirichlet distribution to the isotropic Gaussian. SIGReg, together with the Conditional Entropy Bottleneck (CEB) decomposition of Fischer [20], yields concrete encoder losses for supervised and semi-supervised settings. In the self-supervised setting, SIGReg serves as the distributional regularizer alongside a view-prediction proxy.        

> 1We use the term “manifold” loosely. Mis the image of x7→p(Y|x) and may have singularities, self-intersections, or varying dimension.

1            

> arXiv:2604.27743v2 [cs.IT] 3 May 2026 YXW
> enc
> min I(X;W)
> max I(W;Y)
> W∈ P K
> W∼ N (0 , I )

Figure 1: Overview from left to right. (a) The IB optimal encoder. (b) Soft clustering in the probability simplex. (c) Max entropy for less supervised settings. The framework applies to both discrete and continuous variables. For discrete Y with K out-comes, the predictive manifold lives in the probability simplex PK . For continuous Y , we introduce the effective predictive dimension and show that the manifold has finite effective dimension bounded by the intrinsic dimension of X.

1.1 Contributions 

1. Geometric framework for optimal IB representations. Building on the rate-distortion equivalence of Harrem¨ oes and Tishby [26] and the tutorial treatment of Goldfeld and Polyan-skiy [23], we assemble known ingredients, including the optimal encoder form, the canonical simplex representation, invariance under prediction-preserving transformations, and the di-mensionality bound dim( W ∗) ≤ K−1 (see Corollary 3), into a unified geometric picture: the optimal encoder at any distortion level performs a soft clustering of the predictive manifold 

M ⊂ P K , with a linear decoder in the canonical parameterization (see Section 3.4). We extend the dimensionality statement to continuous Y via covering numbers (see Proposi-tion 4). The individual results are not new. The contribution is the synthesis, which makes contributions 2 and 3 below concrete. 2. Dirichlet-to-Gaussian chain and characterization of SIGReg. We derive a chain of exact transformations, Dir(1 , . . . , 1) → K i.i.d. Exp(1 /2) → 2K i.i.d. N (0 , 1), that connects the maximum entropy prior on PK to an isotropic Gaussian in Euclidean space, with quanti-fied entropy overhead at each step (see Section 5.3). This identifies SIGReg [6] as a Gaussian relaxation of the flat Dirichlet via the Cram´ er-Wold theorem, with a non-diminishing phase entropy overhead of K log 2 π nats that affects rate accounting but not achievable prediction (see Section 5.4). 3. Non-parametric encoder losses across supervision regimes. Using the CEB decom-position of Fischer [20], which rewrites the IB Lagrangian as conditional rate I(X; W |Y ) plus total rate I(X; W ) (eq. 49), we derive encoder losses for the supervised and semi-supervised settings, estimated via minibatch marginals rather than variational bounds (Sections 6.1– 6.2). In the self-supervised setting, where no labels are available, the CEB conditional rate is replaced by a view-prediction proxy and serves as the distributional regularizer (see Sec-tion 6.3). 

1.2 Prior Work 

The Information Bottleneck. The optimization problem underlying the IB method was first formulated by Witsenhausen and Wyner [43], who studied inf p(w|x): H(X|W )≥α H(Y |W ), in the con-2text of common information [22] for discrete random variables. Tishby, Pereira, and Bialek [40] independently introduced the same problem as a principled framework for extracting relevant in-formation, coining the term “information bottleneck.” The optimal encoder satisfies self-consistent equations solvable by the Blahut-Arimoto algorithm [10, 5], originally developed for rate-distortion computation. The connection between IB and rate-distortion theory was made explicit by Har-rem¨ oes and Tishby [26], who showed that the IB is a rate-distortion function with KL divergence as distortion. We build on this equivalence and develop its geometric consequences. 

IB and deep learning. Goldfeld and Polyanskiy [23] provide a comprehensive tutorial on IB theory and its applications to machine learning, covering the discrete-variable formulation in detail. Shwartz-Ziv and Tishby [38] studied the information plane dynamics of deep networks during training. Saxe et al. [35] showed that the reported compression phase does not hold in general. Achille and Soatto [2] develop an IB-flavoured account of representation learning in which invariance to nuisances emerges from a variational IB objective. Our treatment is complementary: non-variational, and with an explicit geometric characterization of the optimum on the probability simplex. Alemi et al. [3] introduced VIB, using variational bounds (a fixed prior to upper-bound 

I(X; W ) and a learned decoder to lower-bound I(W ; Y )) to make the IB objective tractable for neural networks. Fischer [20] introduced CEB, which rewrites the IB Lagrangian as I(X; W |Y ) −

γ I (W ; Y ) via the chain rule of mutual information (equivalent to our eq. (49) with γ = β − 1), and demonstrated benefits for calibration and adversarial robustness. Fischer used variational bounds in practice. We estimate both CEB terms via minibatch marginals rather than variational surrogates. Subsequent work extended the variational IB approach to nonlinear settings [30] and connections to PAC-Bayes generalization bounds [45]. Strouse and Schwab [39] studied the deterministic IB, which replaces the rate I(X; W ) with the marginal entropy H(W ) and thereby admits deterministic encoders by construction. The resulting frontier is in the ( H(W ), I (W ; Y )) plane rather than on the standard IB plane, i.e. ( I(X; W ), I (W ; Y )). 

Sufficient statistics and minimal representations. The connection between IB and sufficient statistics is classical: the minimal sufficient statistic of X for Y achieves the endpoint of the rate-distortion function at zero distortion [40, 15]. The concept of sufficiency originates with Fisher [21] and was formalized by the Lehmann-Scheff´ e theorem [33]. Our Theorem 1 consolidates these classical facts with a self-contained proof and restates the result in the geometric language of the predictive manifold, serving as the foundation for the soft-clustering interpretation at ε > 0. 

Geometric and information-geometric perspectives. Information geometry [4] studies the Riemannian structure of statistical manifolds equipped with the Fisher metric. Chechik et al. [13] analyzed the Gaussian IB and its geometric properties. Our geometric interpretation of the predictive manifold M ⊂ P K and its soft clustering is complementary: rather than studying the Fisher geometry of the model family, we study the geometry of the set of predictive distributions induced by the data. The manifold hypothesis [18, 34] and effective-dimension constructions [1] provide context for our dimensionality bound. Our effective predictive dimension (see Appendix A) is defined via covering numbers and connects the intrinsic dimension of the input to the complexity of M.

Rate-distortion theory. The rate-distortion function, introduced by Shannon [36] and developed by Berger [7], characterizes the fundamental limits of lossy compression. Our use of KL divergence as distortion connects to the extensive literature on mismatched and logarithmic distortion measures [15]. 

Self-supervised learning and SIGReg. Self-supervised learning methods learn representa-tions without human labels, using pretext tasks or contrastive objectives [14, 25, 12]. Shwartz-Ziv 3and LeCun [37] review self-supervised learning through an information-theoretic lens, introducing a unified framework for the self-supervised information-theoretic learning problem. The Joint-Embedding Predictive Architecture (JEPA) [32] proposes learning by predicting in representation space. LeJEPA [6] implements this paradigm with SIGReg, which enforces an isotropic Gaussian prior on the encoder output via the Cram´ er-Wold theorem and the Epps-Pulley test [16]. Our framework provides a theoretical foundation for this choice: SIGReg implements a Gaussian relax-ation of the maximum entropy principle on the predictive manifold via the Dirichlet-to-Gaussian chain, with a quantified entropy overhead that affects rate but not prediction. 

Maximum entropy and the Dirichlet distribution. The maximum entropy principle [27] selects the least informative distribution consistent with known constraints. On the probability simplex PK , the maximum entropy distribution with respect to the Lebesgue measure is the flat Dirichlet Dir(1 , . . . , 1) [15]. The Dirichlet family is widely used as a prior in Bayesian inference [19] and in variational autoencoders on the simplex [28]. Our use is different: the flat Dirichlet is not a learned prior but a fixed reference encoding complete uncertainty about the predictive structure. 

Semi-supervised learning. Semi-supervised methods leverage unlabeled data to improve learn-ing when labels are scarce [24, 29, 42]. Kingma et al. [29] extend variational autoencoders to the semi-supervised setting. Grandvalet and Bengio [24] use entropy minimization on unlabeled data. Our semi-supervised loss pairs the CEB conditional rate on labeled data with SIGReg on all data, replacing task-specific assumptions with a distributional regularizer grounded in the maximum entropy principle. 

# 2 Problem Statement 

Given a random variable pair ( X, Y ) ∼ p(x, y ), the goal is to approximate p(y|x) via a learned encoder-decoder network, using i.i.d. samples from ( X, Y ). 

2.1 Preliminaries 

X and Y take values in measurable spaces X and Y, respectively, and may be discrete or continuous. We write sums throughout. Continuous variables are handled by replacing sums with integrals and Shannon entropy with differential entropy, under standard regularity conditions. We use standard information-theoretic notation [15]: H(X) denotes the entropy of X, H(Y |X)the conditional entropy, I(X; Y ) = H(Y ) − H(Y |X) the mutual information, I(X; Y |W ) the con-ditional mutual information given W , and DKL (p∥q) = P 

> x

p(x) log p(x) 

> q(x)

the KL divergence between distributions p and q. All logarithms are natural unless stated otherwise. We denote by 

PK := 

(

p ∈ RK : pi ≥ 0,

> K

X

> i=1

pi = 1 

)

(1) the ( K−1)-dimensional probability simplex. Here K is the effective predictive dimension : the dimension of the smallest simplex containing the predictive manifold M := {p(Y |x) : x ∈ X } .

• When Y is discrete then K = |Y| , i.e. p(Y |x) is a K-dimensional probability vector. 

• When Y is continuous, the space of densities is infinite-dimensional, but the predictive man-ifold M has finite effective dimension, and we set K = ⌈deff (M)⌉ + 1, bounded by dX , the intrinsic dimension of X, under Lipschitz regularity of f ∗ : x 7 → p(Y |x) (see Appendix A). 4PK is then the ( K−1)-simplex spanned by K densities whose convex hull contains M (equiv-alently, a sufficient K-bin quantization of Y adapted to M), and all subsequent geometric statements refer to this chart. In both cases, the optimal representation lives in PK and all subsequent results are stated in terms of K.Following the information bottleneck framework [40], we partition the problem into an encoder-decoder pair: the encoder maps X → W and the decoder maps W → ˆY , where ˆY is sampled from 

p(y|w) and W is the latent variable. The system obeys the Markov chain 

Y − X − W − ˆY , (2) which implies p(w|x, y ) = p(w|x): the encoder has no direct access to Y .

2.2 Optimization Problem 

By the chain rule for mutual information, I(X, W ; Y ) = I(X; Y ) + I(W ; Y |X) = I(W ; Y ) + 

I(X; Y |W ). Since I(W ; Y |X) = 0 by (2), we obtain 

I(X; Y ) − I(W ; Y ) = I(X; Y |W ). (3) where I(X; Y |W ) is the residual predictive information between X and Y not captured by W .The goal is to learn an encoder that minimizes the encoding rate R = I(X; W ) subject to an upper bound on this residual: min  

> p(w|x)

I(X; W ) s.t. I(X; Y |W ) ≤ ε. (4) By the data processing inequality [15] applied to (2), I(W ; Y ) ≤ I(X; Y ), so I(X; Y |W ) ≥ 0 and the constraint is always satisfiable. The parameter 0 ≤ ε ≤ I(X; Y ) controls the compression-prediction tradeoff: 

• ε = 0 enforces exact sufficiency, i.e. I(W ; Y ) = I(X; Y ), recovering the minimal sufficient statistic of X with respect to Y [21, 33]. 

• ε > 0 allows approximate sufficiency, permitting greater compression at the cost of some predictive information loss. 

2.3 Lagrangian Formulation 

Introducing β ≥ 0 for the constraint (4) and λ(x) for the normalization of p(w|x), the explicit Lagrangian is 

L(p(w|x), β, λ (x)) := I(X; W ) + β I(X; Y |W ) − ε + X

> x

λ(x) X

> w

p(w|x) − 1

!

. (5) Let R(ε) denote the optimal value of (4) as a function of ε:

R(ε) := inf     

> p(w|x): I(X;Y|W)≤ε

I(X; W ). (6) For finite discrete alphabets, the infimum is attained and “inf” may be replaced by “min”. We use “min” throughout the main text with this implicit assumption. The Lagrange multiplier satisfies 5β = −R′(ε), as shown in Remark 1 below. As we shall show, β parameterizes this curve monoton-ically, so specifying β is equivalent to specifying ε at all smooth points. In practice, β is therefore treated as a hyperparameter that selects the desired compression-prediction tradeoff. Setting the functional derivative of (5) with respect to p(w|x) to zero yields the optimal encoder (Theorem 4 of [40]): it assigns higher probability to latent values w whose predictive distribution 

p(y|w) is close to p(y|x) in KL divergence. The explicit form is derived in the next section. 

Remark 1 (Lagrange multiplier as slope) . From (5) , ∂L/∂ε = −β. At the optimal encoder the constraint is binding, so L = I(X; W ) = R(ε). At smooth points, differentiating with respect to ε

gives R′(ε) = −β [15, Ch. 10]. The multiplier is the negative slope of the rate-distortion function: larger β imposes a steeper rate penalty and drives ε toward zero. 

# 3 IB as Rate Distortion 

3.1 Rate-Distortion Equivalence 

The optimization (4) is equivalent to a classical rate-distortion problem [36, 7] with a task-dependent distortion measure [26]. Define the distortion between input x and latent value w as 

d(x, w ) := DKL 

 p(y|x) ∥ p(y|w). (7) This measures predictive mismatch: d(x, w ) = 0 if and only if p(y|w) = p(y|x). 

Proposition 1 (Rate-distortion equivalence) . Under the Markov chain (2) ,

E[d(X, W )] = H(Y |W ) − H(Y |X) = I(X; Y |W ). (8) 

Therefore the optimization (4) is equivalent to 

min  

> p(w|x)

I(X; W ) s.t. E[d(X, W )] ≤ ε, (9) 

and the Lagrangian (5) takes the classical rate-distortion form [7] 

L = I(X; W ) + β E[d(X, W )] . (10) 

Proof. Expanding the expectation and splitting the logarithm, 

E[d(X, W )] = X

> x,w

p(x, w ) X

> y

p(y|x) log p(y|x)

p(y|w) (11) = X

> x,y

p(x)p(y|x) log p(y|x) − X

> x,w,y

p(x, w )p(y|x) log p(y|w). (12) The first term equals −H(Y |X). For the second, the Markov chain (2) gives p(x, w )p(y|x) = 

p(x, w, y ), so summing over x,

X

> x,w,y

p(x, w, y ) log p(y|w) = X

> w,y

p(w, y ) log p(y|w) = −H(Y |W ). (13) Hence E[d(X, W )] = H(Y |W ) − H(Y |X) = I(X; Y |W ). The distortion (7) measures predictive mismatch rather than reconstruction error: two inputs 

x, x ′ can be merged without distortion if and only if p(y|x) = p(y|x′). By marginalization, 

p(y|w) = E[p(y|X) | W = w] = X

> x

p(x|w) p(y|x), (14) so the decoder sees the average predictive distribution of inputs mapped to w.63.2 The General Solution 

Setting the functional derivative of (10) with respect to p(w|x) to zero yields the optimal encoder 

p(w|x) = p(w)

Z(x, β ) exp  −β D KL 

 p(y|x) ∥ p(y|w) , (15) where Z(x, β ) = P 

> w′

p(w′) exp( −βD KL (p(y|x)∥p(y|w′))) is a normalization function and p(y|w)is the self-consistent decoder (14). Each input x is assigned probabilistically to latent values w,with higher probability given to w whose predictive distribution p(y|w) is close to p(y|x) in KL divergence. The encoder (15), the marginal p(w), and the decoder p(y|w) satisfy the self-consistency equa-tions 

p(w) = X

> x

p(x) p(w|x), (16) 

p(y|w) = 1

p(w)

X

> x

p(x) p(w|x) p(y|x). (17) These can be solved iteratively via the Blahut-Arimoto algorithm [10, 5]: starting from an initial 

p(w) and p(y|w), the algorithm alternates the encoder update (15), the marginal update (16), and the decoder update (17). Convergence to the optimum of (10) for fixed β is guaranteed. 

Remark 2. For ε ∈ (0 , I (X; Y )) , the optimal encoder achieving the rate-distortion function (6) is generally stochastic [39]. A deterministic encoder assigns each x to a fixed point in PK , restricting 

W to a finite subset of the predictive manifold M and typically preventing it from tracing the full rate-distortion curve. At the endpoint ε = 0 , the optimal encoder is deterministic, as we shall show in Theorem 1 in the next section. 

3.3 Minimal Sufficient Statistic: The Zero-Distortion Case 

At ε = 0, the distortion constraint becomes exact sufficiency, i.e. I(X; Y |W ) = 0. 

Theorem 1 (Minimal sufficient statistic) . Define f ∗ : X → P K by f ∗(x) := p(Y |x), and let {Ci}

denote the partition of X into level sets of f ∗:

x ∼ x′ ⇐⇒ f ∗(x) = f ∗(x′), (18) 

where Ci = {x : f ∗(x) = qi} for some qi ∈ P K . Define the random variable 

W ∗ := f ∗(X), (19) 

so that W ∗ takes values in PK . Then: 1. W ∗ is a sufficient statistic of X for Y : I(X; Y |W ∗) = 0 .2. W ∗ is minimal: any encoder W with I(X; Y |W ) = 0 satisfies W ∗ = g(W ) almost surely, where g(w) := p(Y |w) is the self-consistent decoder (17) .Proof. (1) Sufficiency. Since W ∗ = p(Y |X), conditioning on W ∗ = t fixes p(Y |x) = t for every 

x in the preimage {x : p(Y |x) = t}, so p(Y |W ∗ = t) = t. Hence H(Y |W ∗) = H(Y |X), giving 

I(X; Y |W ∗) = 0. 

(2) Minimality. Suppose W satisfies I(X; Y |W ) = 0, equivalently E[d(X, W )] = 0 by Proposi-tion 1. Since d(x, w ) ≥ 0 and its expectation is zero, d(x, w ) = 0 almost surely, so p(y|w) = p(y|x)for every ( x, w ) with p(x, w ) > 0. The decoder g(w) = p(Y |w) therefore satisfies g(w) = p(Y |x) for every x mapped to w, giving W ∗ = g(W ) almost surely. 7Corollary 1 (Minimum rate) . W ∗ achieves the minimum rate among all sufficient encoders: 

I(X; W ∗) = H(W ∗), and any W with I(X; Y |W ) = 0 satisfies I(X; W ) ≥ H(W ∗), with equal-ity if and only if I(X; W |W ∗) = 0 .Proof. By Theorem 1, W ∗ = g(W ), so the data processing inequality applied to X → W → W ∗

gives I(X; W ) ≥ I(X; W ∗). Since W ∗ = f ∗(X) is deterministic, H(W ∗|X) = 0, hence I(X; W ∗) = 

H(W ∗). Equality I(X; W ) = H(W ∗) holds if and only if I(X; W |W ∗) = 0, by the chain rule 

I(X; W ) = H(W ∗) + I(X; W |W ∗). 

Corollary 2 (Hard assignment in the large-β limit) . Let W correspond to the partition {Ci} of Theorem 1, with each cell Ci associated with a distinct latent value wi satisfying p(y|wi) = qi(y),and let xi ∈ Ci be any representative. Then 

DKL 

 p(y|xi) ∥ p(y|wj ) =

(

0 i = j> 0 i̸ = j, (20) 

and the optimal encoder (15) satisfies 

lim  

> β→∞

p(wj |xi) = 

(

1 i = j

0 i̸ = j, (21) 

i.e. the encoder performs hard assignment to the correct partition cell. Proof. Since p(y|wi) = qi(y) = p(y|xi) by construction, DKL (p(y|xi)∥p(y|wi)) = 0. For i̸ = j,

p(y|wj ) = qj (y)̸ = qi(y) = p(y|xi) by definition of the partition, so DKL (p(y|xi)∥p(y|wj )) > 0. As 

β → ∞ , exp( −βD KL ) → 0 for all j̸ = i in (15), so the distribution concentrates on wi.

Corollary 3 (Dimensionality bound) . The intrinsic dimension of the optimal representation W ∗

is at most K − 1.Proof. By (19), W ∗ = p(Y |X) takes values in PK , which has dimension K − 1. For discrete Y

with |Y| = K, each p(Y |x) is a K-dimensional probability vector, so the bound is immediate. For continuous Y , W ∗ = p(Y |X) can always be losslessly reparameterized as W = X, so the intrinsic dimension of the optimal representation is at most dX . When f ∗ is Lipschitz, Appendix A gives the sharper covering-number statement deff (M) ≤ dX via Proposition 4, and hence K − 1 = 

⌈deff (M)⌉ ≤ ⌈ dX ⌉.

Remark 3. The bound dim( W ∗) ≤ K − 1 is a statement about coordinate dimension, distinct from the entropy H(W ∗), which measures the diversity of predictive distributions and can be large even when K is small. In the infinite-data limit, any encoder-decoder network trained for prediction will collapse onto a manifold of dimension at most K − 1, regardless of the ambient dimension of the latent space. 

3.4 Geometric Interpretation 

The optimal encoder at ε = 0 computes W ∗ = p(Y |X), mapping inputs onto the predictive manifold M ⊂ P K . All information in X relevant for predicting Y lives on M. Nuisance variability is discarded. For any ε ≥ 0, the optimal encoder (15) depends on x only through p(Y |x) = W ∗ ∈ P K . The encoder therefore factors as 

X f ∗

−→ W ∗ ∈ P K 

> stochastic kernel p(w|w∗)

−−−−−−−−−−−−−−−→ W. (22) 8The stochastic kernel p(w|w∗) defines a soft clustering of M. The decoder 

p(y|w) = E[W ∗ | W = w] ∈ P K (23) is the center of the cluster associated with w. The decoder law p(y|w) is the only property of 

w relevant for predicting Y , so W takes values in PK without loss of generality. Any two values 

w̸ = w′ with p(y|w) = p(y|w′) carry identical prediction. Merging them leaves the rate and distortion unchanged. The encoder-decoder system therefore takes the canonical form 

X nonlinear encoder 

−−−−−−−−−−−→ W ∈ P K linear decoder 

−−−−−−−−→ ˆY (24) for all ε ≥ 0, concentrating all representational complexity in the encoder. At ε = 0, the stochastic kernel collapses to a point mass: W = W ∗ and the encoder com-putes p(Y |X) exactly. As ε increases, the clusters broaden and overlap, merging nearby predictive distributions at the cost of distortion. At ε = I(X; Y ), all inputs merge into a single cluster, giving W ⊥ X (see Figure 2). In the canonical gauge W ∗ = p(Y |X), the optimal decoder re-duces to indexing into the latent vector, p(y|w) = e⊤ 

> y

w. For ε > 0 the latent need not equal the predictive distribution. The decoder is then the composition of an encoder-determined map 

w 7 → E[W ∗ | W = w] with the same linear readout. 

Remark 4 (Gauge freedom) . Decoder linearity is a property of the canonical representation W =

p(Y |X) ∈ P K . The IB objective is invariant under any invertible reparameterization r : W 7 → r(W ):if W is sufficient for Y , so is r(W ). A loss depending only on mutual information terms does not pin the gauge. The encoder may converge to any informationally equivalent representation, and the decoder is then the generally nonlinear map e⊤ 

> y

◦ r−1. The geometric picture of M ⊂ P K therefore describes an equivalence class : any optimal representation is related to the canonical form by an invertible map.             

> e1e2
> e3
> M
> (a) ε= 0 : exact
> e1e2
> e3
> p(y|w1)
> p(y|w2)
> p(y|w3)
> (b) ε > 0: soft clustering
> e1e2
> e3
> p(y)
> (c) ε→I(X;Y): trivial

Figure 2: Soft clustering of the predictive manifold M ⊂ P 3 at three distortion levels. Each triangle is the probability simplex P3 with vertices e1, e 2, e 3. The red curve is M = {p(Y |x) : x ∈ X } .Blue dots are individual predictive distributions p(Y |xi). (a) At ε = 0, the encoder computes 

W ∗ = p(Y |X) exactly (see Theorem 1). (b) At moderate ε, nearby points merge into soft clusters. Black dots are cluster centers p(y|w) = E[W ∗|W =w] (23). (c) As ε → I(X; Y ), all points merge into a single cluster centered at the marginal p(y), and W ⊥ X.93.5 Invariances of the Optimal Representation 

The optimal encoder discards all variations in X that leave p(Y |x) unchanged, at every distor-tion level. 

Proposition 2 (Invariance) . Let T : X → X satisfy 

p(Y |T (x)) = p(Y |x) ∀ x. (25) 

Then p(w|T (x)) = p(w|x) for all w and all ε ≥ 0. In particular, W ∗(T (x)) = W ∗(x) at ε = 0 .Proof. The optimal encoder depends on x only through p(y|x): at ε = 0 via W ∗ = p(Y |X), and for 

ε > 0 via the self-consistency equation (15). Since p(Y |T (x)) = p(Y |x), the claim follows in both cases. 

Remark 5. Transformations satisfying (25) are nuisance variations: changes in X that carry no information about Y . When they form a group G acting on X , the canonical latent space M ⊂ P K

is the image of the quotient X /G under f ∗, and the encoder collapses G-orbits at every distortion level. This is the input-side counterpart of gauge freedom (see Remark 4): gauge freedom concerns invertible reparameterizations of the output W . The present invariance concerns the collapsing of input X.

Example 1 (Ternary Classification on the Simplex) . Let X be uniform on {1, 2, 3, 4, 5, 6} and 

Y ∈ { 1, 2, 3} with 

p(Y |X=1) = p(Y |X=2) = (0 .8, 0.1, 0.1) ,p(Y |X=3) = p(Y |X=4) = (0 .1, 0.8, 0.1) ,p(Y |X=5) = p(Y |X=6) = (0 .1, 0.1, 0.8) . (26) 

The minimal sufficient statistic W ∗ = p(Y |X) takes three values w1 = (0 .8, 0.1, 0.1) , w2 = (0 .1, 0.8, 0.1) ,

w3 = (0 .1, 0.1, 0.8) , each with probability 1/3. The predictive manifold M = {w1, w 2, w 3} is this finite set of three points, each lying near a vertex of P3 (see Figure 3). The marginal p(Y ) = (1 /3, 1/3, 1/3) is the centroid of that triangle. At ε = 0 , the encoder maps each x to its cluster center wi. As ε increases, the clusters broaden and the representation coarsens toward the single point p(Y ).

e1 = (1 , 0, 0) e2 = (0 , 1, 0) 

e3 = (0 , 0, 1)   

> w1w2
> w3
> p(Y)

Figure 3: Example (26) inside P3. Blue points: M = {w1, w 2, w 3}, the predictive manifold. Dashed triangle: convex hull of M (visual aid). Grey dot: marginal p(Y ) = (1 /3, 1/3, 1/3). 10 4 The Information Plane 

4.1 The Achievable Region 

The achievable region is the set of all pairs ( I(X; W ), I (W ; Y )) attainable by some encoder 

p(w|x) satisfying the Markov chain (2), for a fixed joint distribution p(x, y ). 

Upper bound (converse). The data processing inequality [15] applied to the two ends of Y −

X − W gives two bounds: 

I(W ; Y ) ≤ I(X; Y ), (27) 

I(W ; Y ) ≤ I(X; W ). (28) No encoder extracts more information about Y than X contains. Nor can it convey more about Y

than about X. These bounds confine the achievable region to 

C := (R, ∆) : 0 ≤ ∆ ≤ min( R, I (X; Y )) , (29) where R := I(X; W ) and ∆ := I(W ; Y ). The boundary of C consists of the diagonal ∆ = R for 

R ≤ I(X; Y ) and the horizontal line ∆ = I(X; Y ) for R ≥ I(X; Y ). 

Achievable endpoints. Two operating points are always achievable: 

• Trivial encoder (W ⊥ X): gives ( R, ∆) = (0 , 0). 

• Minimal sufficient statistic (W = W ∗ = p(Y |X)): gives ( R, ∆) = ( H(W ∗), I(X; Y )), by Theorem 1, i.e. I(X; W ∗) = H(W ∗) and I(W ∗; Y ) = I(X; Y ). 

The gap. The endpoint ( H(W ∗), I (X; Y )) lies on the diagonal (28) if and only if H(W ∗) = 

I(X; Y ), equivalently H(W ∗|Y ) = 0. In general, 

H(W ∗) = I(X; Y ) + H(W ∗|Y ), (30) since H(W ∗) = I(W ∗; Y )+ H(W ∗|Y ) = I(X; Y )+ H(W ∗|Y ). The gap H(W ∗|Y ) ≥ 0 is the entropy overhead that the optimal encoder spends on predictive distributions that are indistinguishable from their outputs alone. When H(W ∗|Y ) > 0 the endpoint lies strictly below the diagonal. 

Time sharing lower bound. Mixing W ∗ with the trivial encoder with probability λ ∈ [0 , 1], independently of ( X, Y ), achieves every point 

 λH (W ∗), λI (X; Y ), λ ∈ [0 , 1] . (31) This traces a line from (0 , 0) to ( H(W ∗), I (X; Y )) with slope 

I(X; Y )

H(W ∗) = I(X; Y )

I(X; Y ) + H(W ∗|Y ) ≤ 1, (32) with equality if and only if H(W ∗|Y ) = 0. When the slope is strictly below 1, the time-sharing line lies below the diagonal. A triangular gap opens between the converse boundary C and the time-sharing line. 

The IB curve (see Section 4.2) . The time-sharing line (31) is a lower bound on the IB curve. The converse boundary C is an upper bound. The gap between them depends on H(W ∗|Y ). By Theorem 2, the IB curve is concave and lies within this gap. Its exact shape depends on p(x, y ). Figure 4 summarises the information plane structure: the converse boundary C, the time-sharing lower bound, the IB curve (concave, connecting (0 , 0) to ( H(W ∗), I (X; Y ))), and the triangular gap that opens when H(W ∗|Y ) > 0. 11 R = I(X; W )

∆ = I(W ; Y )

I(W ; Y ) = I(X; W )

I(W ; Y ) = I(X; Y )

> IB curve

(0 , 0) 

I(X; Y )

H(W ∗)I(X; Y ) H(X)

W ∗

> gap
> achievable region

Figure 4: The achievable region (light blue trapezoid) in the ( I(X; W ), I (W ; Y )) plane, bounded by the converse boundary C (gray dashed: diagonal and horizontal data processing inequality bounds), the R-axis, and R = H(X). The blue line is the time-sharing lower bound on the IB curve. The darker shaded triangle is the gap region between the converse boundary and the time-sharing line. The true IB curve (red) is concave, connects (0 , 0) to ( H(W ∗), I (X; Y )), and is flat at I(X; Y ) for 

R ∈ [H(W ∗), H (X)]. When H(W ∗|Y ) = 0, the gap triangle collapses and the IB curve coincides with the converse boundary up to R = I(X; Y ) = H(W ∗). 

Example 2 (Binary Classification and the Information Plane) . Let X be uniform on {1, 2, 3, 4}

and Y ∈ { 0, 1} with 

p(Y |X=1) = p(Y |X=2) = (0 .1, 0.9) ,p(Y |X=3) = p(Y |X=4) = (0 .9, 0.1) . (33) 

The minimal sufficient statistic W ∗ = p(Y |X) takes two values w1 = (0 .1, 0.9) and w2 = (0 .9, 0.1) ,each with probability 1/2, so H(W ∗) = log 2 ≈ 0.693 nats. The marginal satisfies p(Y =1) = 0 .5,so H(Y ) = log 2 and H(Y |X) = Hb(0 .9) ≈ 0.325 nats, where Hb(p) = −p log p − (1 − p) log(1 − p),so I(X; Y ) ≈ 0.368 nats. The endpoint (H(W ∗), I (X; Y )) ≈ (0 .693 , 0.368) lies strictly below the diagonal. The gap is 

H(W ∗|Y ) = H(W ∗)−I(X; Y ) ≈ 0.325 nats: given Y = 1 , the posterior p(W ∗=w1|Y =1) = 0 .9̸ = 1 ,so observing Y does not determine which predictive distribution generated it. The time-sharing line has slope I(X; Y )/H (W ∗) ≈ 0.531 , and the triangular gap has vertices 

(0 , 0) , (0 .368 , 0.368) , and (0 .693 , 0.368) . The IB curve lies in this gap. 

4.2 The IB Curve 

Define the IB curve as the upper boundary of the achievable region: for each rate R ≥ 0, ∆∗(R) := max    

> p(w|x): I(X;W)≤R

I(W ; Y ). (34) Equivalently, in terms of rate-distortion, ∆∗(R) = I(X; Y ) − ε∗(R), (35) where ε∗(R) is the minimum distortion achievable at rate R (inverse of (6)). 12 Theorem 2 (Concavity of the IB curve) . The IB curve ∆∗(R) is concave and non-decreasing in R.Equivalently, the rate-distortion function R(ε) (6) is convex and non-increasing in ε ∈ [0 , I (X; Y )] .Proof. See Appendix B. 

Bounds of the IB curve. Combined with the bounds of Section 4.1, Theorem 2 gives the following characterization: 

• The IB curve starts at (0 , 0) and ends at ( H(W ∗), I (X; Y )). 

• It is concave and lies weakly above the achievable time-sharing line (31). 

• It lies weakly below the converse boundary C (29). 

• For R ≥ H(W ∗), the curve is flat at ∆ ∗(R) = I(X; Y ): one can augment W ∗ with additional information about X to increase the rate without affecting the predictive information (see Appendix C). 

• The curve lies on the diagonal if and only if H(W ∗|Y ) = 0. The gap then vanishes and 

H(W ∗) = I(X; Y ). The exact shape of the IB curve within the gap region depends on p(x, y ) through the geometry of the predictive manifold M ⊂ P K and has no closed form in general. It can be computed iteratively via the Blahut-Arimoto algorithm [10, 5]. 

Slope of the IB curve. The normalization term in (5) vanishes at any feasible p(w|x). Using 

I(X; Y |W ) = I(X; Y ) − I(W ; Y ), the remaining terms give 

L = R − β ∆ + β I(X; Y ) − ε. (36) Assuming the minimum in (6) is attained, each ε ∈ [0 , I (X; Y )] determines a unique Lagrange multiplier β(ε) := −R′(ε) ≥ 0 (see Remark 1). The level sets of R − β∆ in (36) are lines of slope 1/β in the ( R, ∆) plane. The optimal encoder for distortion ε is the point where such a line is tangent to the IB curve. Denoting this tangent line ∆ ε(R), ∆ε(R) = Rβ(ε) + c(ε), c(ε) := I(X; Y ) − ε − R(ε)

β(ε) , (37) where c(ε) is the ∆-axis intercept (see Figure 5). 

The critical β. Monotonicity alone does not imply that every β > 0 yields a non-trivial operating point. Since the curve is concave (see Theorem 2), its slope s(R) = ∆ ∗′ (R) is largest at the origin and decreases as R grows. Write s0 := ∆ ∗′ (0 +) for the slope at the origin. By the data-processing inequality (28), the curve lies weakly below the diagonal ∆ ≤ R, so 

s0 ≤ 1. (38) If 1 /β > s 0, the iso-cost lines are flatter than the curve at the origin, no interior tangent exists, and the trivial encoder W ⊥ X is optimal. If 1 /β < s 0, an interior tangent point exists. As β grows it slides further along the curve. The critical value is therefore 

βc := 1

s0

≥ 1, (39) 13 R = I(X; W )

∆ = I(W ; Y )

> IB curve

I(X; Y )

H(W ∗) 

> slope 1
> β
> c(R0)
> R0
> ∆∗(R0)

Figure 5: Tangent to the IB curve at operating point ( R0, ∆∗(R0)). The slope of the tangent is 1 /β 

and its ∆-axis intercept is c(R0) = ∆ ∗(R0) − R0/β . As R0 increases, β grows, the slope flattens, and c(R0) rises from 0 to I(X; Y ). with equality βc = 1 when H(W ∗|Y ) = 0, in which case the IB curve is tangent to the diagonal at the origin and coincides with it up to R = I(X; Y ). For any β < β c we have R(β) = 0 and 

ε(β) = I(X; Y ). The interior of the IB curve is traced only by β ∈ (βc, ∞). Stochastic tasks typically have βc > 1. The exact value depends on p(x, y ) through the slope of the IB curve at the origin, but the qualitative picture is universal: phase transition at βc ≥ 1, non-trivial representation above it, and collapse below. 

Proposition 3 (Monotonicity in β). For each β ≥ 0, let pβ (w|x) denote the optimal encoder minimizing I(X; W ) + β I (X; Y |W ), and let ε(β) := I(X; Y |W ) and R(β) := I(X; W ) denote the distortion and rate achieved by pβ . Then ε(β) is non-increasing in β and R(β) is non-decreasing in β.Proof. See Appendix D. 

Endpoint values. The two extremes of the IB curve are: Encoder ε R(ε) β(ε) Slope c(ε)Trivial I(X; Y ) 0 βc 1/β c 0Sufficient 0 H(W ∗) ∞ 0 I(X; Y )

Intercept c(ε) is non-decreasing. As ε decreases (lower distortion), R(ε) increases and β(ε)increases by Proposition 3. Writing β for β(ε) and viewing c as a function of R (using dR/dε = −β

and hence dε/dR = −1/β ), 

dc dR = 1

β − ddR 

 Rβ



= 1

β − β − R dβ/dR β2 = Rβ2

dβ dR ≥ 0, (40) since dβ/dR ≥ 0 (see Proposition 3) and R ≥ 0. 

IB curve regimes. The IB curve can be divided into three regimes: 

• 0 ≤ β ≤ βc: the trivial encoder W ⊥ X is optimal, giving the origin ( R, ∆) = (0 , 0) and distortion ε = I(X; Y ). 14 • βc < β < ∞: non-trivial stochastic soft clustering. The operating point ( R(β), ∆( β)) is determined by the self-consistency equations (16)–(17) and traces the interior of the IB curve. 

• β → ∞ : hard assignment to the partition of Theorem 1, recovering W ∗ = p(Y |X) at the endpoint ( H(W ∗), I (X; Y )) by Corollary 2. 

4.3 The Gap and Representation Regimes 

When H(W ∗|Y ) > 0, the endpoint lies strictly below the diagonal: the encoder uses H(W ∗) nats but only I(X; Y ) < H (W ∗) are predictive of Y . The gap H(W ∗|Y ) is information in W ∗ about which equivalence class x belongs to that is not deducible from Y alone. It vanishes whenever distinct equivalence classes Ci̸ = Cj assign probability to disjoint subsets of Y — equivalently, whenever the map from Y to W ∗ is deterministic. Otherwise the gap is strictly positive, an intrinsic cost of the task’s stochasticity. 

Deterministic regime. If Y is a deterministic function of X, then H(Y |X) = 0 and W ∗ = p(Y |X)takes values at the vertices of PK . Thus H(W ∗|Y ) = 0, H(W ∗) = H(Y ) = I(X; Y ), and the endpoint lies on the diagonal. 

Example 3 (Deterministic binary task) . Let X be uniform on {1, 2, 3, 4} with Y = 1 for X ∈ { 1, 2}

and Y = 0 for X ∈ { 3, 4}. W ∗ takes two values e0 = (1 , 0) and e1 = (0 , 1) , each with probability 

1/2. Since H(Y |X) = 0 , I(X; Y ) = H(Y ) = log 2 ≈ 0.693 nats and H(W ∗) = log 2 . The endpoint 

(log 2 , log 2) lies on the diagonal. By contrast, Example 2 has H(W ∗|Y ) ≈ 0.325 nats and the IB curve lies strictly below the converse boundary. 

Near-deterministic regime. If p(Y |X) is nearly deterministic, H(Y |X) ≈ 0 and most inputs map to predictive distributions near the vertices of PK . Thus I(X; Y ) ≈ H(Y ), the map from Y

to W ∗ is nearly deterministic, and H(W ∗|Y ) ≈ 0, so H(W ∗) ≈ I(X; Y ). The gap is small and the IB curve nearly touches the converse boundary. 

Ambiguous regime. If p(Y |X) varies smoothly across the interior of the simplex, W ∗ = p(Y |X)takes values spread continuously inside PK . H(W ∗) can then be much larger than I(X; Y ), the gap H(W ∗|Y ) is large, and the IB curve bows significantly below the diagonal. The geometry of the optimal representation is most clearly described at ε = 0, where the magnitude of the gap determines the structure. In all regimes, for ε > 0 the operating point moves along the IB curve toward the origin, with the encoder progressively merging nearby regions of M

into coarser clusters. 

# 5 Learning with Less Supervision 

5.1 Beyond Supervised Learning 

In the fully supervised setting, the encoder is trained on i.i.d. samples from the true joint dis-tribution p(x, y ), directly optimizing for sufficiency with respect to p(Y |X). In the semi-supervised setting [42], labeled pairs ( x, y ) are available but scarce or biased: they may come from multiple sources, cover only a subset of the input space, or reflect a task not representative of the full predictive structure of X. In the self-supervised setting [6], no human labels are available during encoder training. The target Y is replaced by a self-generated signal, typically the embedding of another view of the same input. Human labels are used only for downstream decoder training. In both semi-supervised and self-supervised settings, directly optimizing for sufficiency with respect 15 to the available labeling risks overfitting to the observed signal. A distributional regularizer on the encoder output is therefore needed. 

5.2 Maximum Entropy on the Predictive Manifold 

The canonical representation W ∈ P K induces a measure on the simplex determined jointly by p(X) and the stochastic clustering kernel (see Section 3.4). When the labeling is biased, this measure reflects the bias of the observed ( x, y ) pairs rather than the true structure of p(Y |X). Maximum entropy on the induced measure over the simplex is the natural response: it assumes no preference for any region of the predictive manifold. All predictive distributions are treated equally. This principle applies to the semi-supervised and self-supervised settings, where labeling is absent or biased. In the fully supervised setting, the conditional rate I(X; W |Y ) already constrains the encoder toward the correct predictive structure (see Section 6.1). The maximum entropy distribution on PK with respect to the Lebesgue measure is the flat Dirichlet [15]: 

W ∼ Dir(1 , . . . , 1) , (41) which assigns uniform density to all points in PK , reflecting complete uncertainty about the labeling structure. Direct optimization on the simplex is numerically problematic: gradients vanish or explode near the boundary, and the reparameterization trick is less stable than in Euclidean spaces. A Euclidean relaxation of (41), trading a bounded entropy overhead for numerical tractability, is therefore preferred. 

5.3 From Simplex to Euclidean Space 

The flat Dirichlet can be constructed from simpler distributions via the following chain of transformations, each trading exact simplex geometry for a looser but more tractable representation. 

Step 1: Exponential construction of the flat Dirichlet. If X1, . . . , X K iid 

∼ Exp(1 /2), then the normalized vector 

D = (X1, . . . , X K )

PKk=1 Xk

(42) follows Dir(1 , . . . , 1) exactly [19]. This construction replaces the ( K − 1)-dimensional simplex constraint with K non-negative dimensions. The only extra degree of freedom introduced is the total scale S = P 

> k

Xk, which is divided out by the normalization in (42). 

Step 2: Gaussian construction of the exponential. If Zk, 1, Z k, 2 iid 

∼ N (0 , 1), then 

Xk := Z2 

> k, 1

+ Z2 

> k, 2

∼ χ2(2) = Exp(1 /2) . (43) Thus the flat Dirichlet can be constructed from 2 K i.i.d. standard Gaussians. Each Gaussian pair ( Zk, 1, Z k, 2) encodes two degrees of freedom: the power Xk = Z2 

> k, 1

+ Z2

> k, 2

, which enters the construction, and the phase θk = arctan( Zk, 2/Z k, 1), which does not. The map to Xk depends only on the power and is phase-invariant. The three parameterizations are summarized in Table 1 and illustrated in Figure 6. Each trades exactness for tractability: the flat Dirichlet is exact but numerically unstable near the simplex boundary [28], the exponential parameterization introduces a scale overhead of 12 log(2 πeK ) nats, vanishing relative to the total entropy K as K → ∞ , and the Gaussian parameterization incurs a non-diminishing phase entropy overhead of K log 2 π nats but provides an unconstrained, fully reparameterizable optimization landscape. 16 Prior Dimensions Extra DoF Entropy overhead 

Dir(1 , . . . , 1) K − 1 None None 

K i.i.d. Exp(1 /2) K 1 scale 12 log(2 πeK ) nats 2K i.i.d. N (0 , 1) 2K 1 scale + K phases 12 log(2 πeK ) + K log(2 π) nats Table 1: Parameterizations of the flat Dirichlet and their entropy overheads. 2K i.i.d. 

Zk,j ∼ N (0 , 1) 

> dim = 2 K

K i.i.d. 

Xk ∼ Exp(1 /2)  

> dim = K

Flat Dirichlet 

D ∼ Dir(1 , . . . , 1)    

> dim = K−1
> unconstrained, smooth non-negative simplex-constrained

Figure 6: The chain of transformations connecting the isotropic Gaussian to the flat Dirichlet on 

PK . Reading right to left: the flat Dirichlet is the maximum entropy prior on PK . The exponential parameterization embeds it in RK 

> +

with a diminishing scale overhead. The Gaussian parameteriza-tion embeds it in R2K with a non-diminishing phase overhead of K log 2 π nats in I(X; W ) that is invariant under the simplex map, but provides the smoothest optimization landscape. 

5.4 Connection to SIGReg 

SIGReg [6] is a Gaussian relaxation of the maximum entropy principle on the predictive mani-fold. SIGReg enforces full distributional matching of the encoder marginal to N (0 , I ), not merely moment matching. Given a minibatch of N encoder outputs w1, . . . , w N ∈ R2K , SIGReg projects them along M random unit directions a1, . . . , a M and applies the Epps-Pulley characteristic-function test [16] to each set of one-dimensional projections {a⊤

> m

wn}Nn=1 , then averages the test statistics: 

LSIGReg (q(w)) := 1

M

> M

X

> m=1

TEP 

 {a⊤

> m

wn}Nn=1 

, (44) where q(w) := Ep(x)[q(w|x)] is the encoder marginal estimated from the minibatch, and TEP is the Epps-Pulley statistic testing normality of a univariate sample. By the Cram´ er-Wold theorem [8], matching all one-dimensional projections to a standard Gaussian is equivalent to matching the full distribution to N (0 , I ). This is strictly stronger than moment matching: matching any finite number of moments does not determine the distribution in general [6], but characteristic-function matching does. SIGReg enforces the 2 K Gaussian parameterization of the flat Dirichlet. The encoder outputs live in R2K , not on the simplex. The simplex and the Gaussian space are alternative representations of the same predictive information, related by the chain of Section 5.3. No explicit inverse to the simplex is needed in practice: the decoder maps directly from the Gaussian representation to predictions. This parameterization introduces a non-diminishing phase entropy overhead of 

K log 2 π nats relative to the exact flat Dirichlet. This overhead is a cost in rate accounting only ,not in achievable prediction. By the construction of Section 5.3, the map π : R2K → P K defined by 

π(w)k := ( w2

> k, 1

+w2

> k, 2

)/ P 

> j

(w2

> j, 1

+w2

> j, 2

) depends on w only through the squared radii r2 

> k

= w2

> k, 1

+w2

> k, 2

.It is therefore invariant under the action of SO (2) K on the K phase planes. The phases are nuisance coordinates of the simplex map: they carry no predictive content. 17 Concretely, applying the data processing inequality to the chain X → W → π(W ) gives 

I(π(W ); Y ) ≤ I(W ; Y ) ≤ I(X; Y ), (45) Any decoder that depends on w only through π(w) achieves I(π(W ); Y ) = I(W ; Y ). Equivalently, when W is generated by the Dirichlet–Gaussian chain the Bayes-optimal predictive distribution 

p(y|w) is itself phase-invariant. The K log 2 π phase entropy is therefore rate that the encoder can 

spend on phase-equivalent distinctions contributing nothing to I(W ; Y ): a horizontal shift of the operating point in the ( R, ∆) plane, increasing I(X; W ) without reducing I(W ; Y ). The exponential parameterization eliminates the phase slack, reducing the overhead to the vanishing 12 log(2 πeK ) scale term, at the cost of non-negativity constraints and less stable gradients near the simplex boundary [28]. The Gaussian parameterization is preferred when β is a free hyperparameter: the phase overhead is then absorbed into β without affecting predictions. The exponential parameterization is more appropriate when the exact value of I(X; W ) matters, for example when β is fixed by a constraint or when estimating the IB curve quantitatively. SIGReg has three consequences within the IB framework. First, it prevents representational collapse: the encoder cannot map all inputs to a constant, since this would violate the isotropic Gaussian constraint. Second, since the marginal p(W ) is constrained to approximate N (0 , I ), the marginal entropy H(W ) is approximately constant. The encoding rate 

I(X; W ) = H(W ) − H(W |X) (46) is controlled primarily via H(W |X): compression is achieved by increasing the conditional entropy of W given X, rather than by collapsing the marginal. Third, the encoder can be trained indepen-dently of the decoder and of the specific labeling: (44) requires no labels and can be applied to all available data, labeled or not. 

# 6 Practical Learning using IB 

Optimizing (4) directly requires access to true mutual information quantities, which are in-tractable in general. Three learning settings are considered. In the supervised case, the IB La-grangian is estimated from minibatches via the CEB decomposition. In the semi-supervised case, labels are scarce: the conditional rate is restricted to labeled data and SIGReg regularizes the encoder on all data. In the self-supervised case, no labels are available. The CEB conditional rate is replaced by a view-prediction proxy and SIGReg serves as the distributional regularizer. In all three cases, the encoder is the primary object of interest. Training proceeds in two stages. 

Stage 1: Encoder training. The encoder qϕ(w|x) is trained by minimizing 

Lenc (ϕ) := Lpred (ϕ) + Lshape (ϕ), (47) where Lpred measures how well the representation predicts the target, and Lshape regularizes the encoder distribution qϕ(w|x). The specific forms of both terms, and the justification for each choice, are given in the subsections below. 

Stage 2: Decoder training. With the encoder frozen, the decoder qψ(y|w) is trained by mini-mizing the cross-entropy loss on labeled data: 

Ldec (ψ) := −Ep(x,y )Eqϕ(w|x)[log qψ(y|w)] . (48) Minimizing this over ψ drives qψ(y|w) toward the optimal decoder p(y|w): by the standard cross-entropy decomposition [9], Ldec (ψ) = Eqϕ(w)[DKL (p(y|w)∥qψ(y|w))] + H(Y |W ), so the unique min-imizer is qψ(y|w) = p(y|w). 18 6.1 Supervised Learning 

In the fully supervised setting, labeled pairs ( x, y ) are available for all training examples. Two formulations of the IB objective (4) are considered: the CEB of Fischer [20] and the VIB of Alemi et al. [3]. Both use variational bounds for practical optimization in their original papers. Here the CEB objective is estimated via minibatch class-conditional marginals rather than a learned backward encoder, avoiding an additional variational approximation. Standard cross-entropy training of the encoder optimizes Lpred alone, which upper-bounds the distortion I(X; Y |W ) [3], with no Lshape 

term. The encoder is not penalized for retaining redundant information about X.

6.1.1 CEB: Conditional Entropy Bottleneck 

By the chain rule for mutual information (see Appendix E), the IB Lagrangian of (4) is equiv-alent, up to the constant β I (X; Y ), to 

Lceb  

> enc

(ϕ) := β I (X; W |Y )

| {z }

> Lpred

+ (1 − β) I(X; W )

| {z }

> Lshape

. (49) 

Lpred = β I (X; W |Y ) is the conditional rate [44, 20]: the information W retains about X beyond what Y already reveals, minimized when W is a sufficient statistic for Y . Lshape = (1 − β) I(X; W )is the encoding rate: for β ≥ βc ≥ 1 its coefficient is negative, so it acts as a regularizer that maximizes the encoding rate, driving the encoder away from the trivial solution while the prediction term enforces sufficiency. The objective is bounded since I(X; W ) ≤ H(X). Fischer [20] optimizes (49) via variational bounds, introducing a learned backward encoder 

b(w|y) to approximate p(w|y). Here both terms are instead estimated from minibatch class-conditional marginals, requiring no additional learned approximation. The conditional rate de-composes as 

I(X; W |Y ) = Ep(x,y )

DKL 

 qϕ(w|x) ∥ qϕ(w|y) , (50) where 

qϕ(w|y) := Ep(x|y)[qϕ(w|x)] (51) is the class-conditional marginal of the encoder, estimated per minibatch as ˆqϕ(w|y) := 1

Ny

X 

> xi:yi=y

qϕ(w|xi), (52) where Ny is the number of labeled examples with label y in the minibatch. This is an unbiased empirical estimate of (51) under the i.i.d. training data assumption. The total rate satisfies 

I(X; W ) = Ep(x)

DKL 

 qϕ(w|x) ∥ qϕ(w) , (53) where qϕ(w) := Ep(x)[qϕ(w|x)] is estimated from the minibatch as a mixture. Evaluating the KL requires computing Eqϕ(w|x)[log qϕ(w)]. Since qϕ(w) is a finite mixture, this expectation has no closed form [9] and is estimated by drawing samples w ∼ qϕ(w|x) and evaluating log qϕ(w) at each sample. The CEB supervised encoder loss is therefore 

Lceb  

> enc

(ϕ) = β Ep(x,y )

DKL 

 qϕ(w|x) ∥ qϕ(w|y) | {z }

> Lpred

+ (1 − β) Ep(x)

DKL 

 qϕ(w|x) ∥ qϕ(w) | {z }

> Lshape

. (54) 19 6.1.2 VIB: Variational Information Bottleneck 

VIB [3] uses a Gaussian encoder qϕ(w|x) = N (μϕ(x), diag σ2

> ϕ

(x)) with W ∈ Rd and a fixed prior 

a(w) := N (0 , I ) to upper-bound I(X; W ) as 

I(X; W ) ≤ Ep(x)

DKL (qϕ(w|x) ∥ a(w)) . (55) The VIB encoder and decoder are trained jointly using the loss 2

Lvib (ϕ, ψ ) := β(− Ep(x,y )Eqϕ(w|x)[log qψ(y|w)]) 

| {z }

> Lpred

+ Ep(x)

DKL (qϕ(w|x) ∥ a(w)) | {z }

> Lshape

, (56) where Lpred takes the same form as the cross-entropy decoder loss (48). CEB and VIB differ in how they estimate the same IB objective. CEB uses minibatch marginals, targeting the true mutual information without a fixed prior. The cost is sampling to evaluate the mixture KL. VIB uses closed-form KL bounds, but the fixed N (0 , I ) prior biases the encoder, making Lshape an upper bound on I(X; W ) with the gap acting as an uncontrolled regularization beyond the IB objective. Tschannen et al. [41] show that such prior-induced regularization can dominate performance, making empirical gains from VIB difficult to attribute to compression alone. 

6.2 Semi-Supervised Learning with SIGReg 

In the semi-supervised setting [24, 29], labeled pairs ( x, y ) are scarce while unlabeled data is abundant. Two failure modes arise: training on labeled data alone yields a biased encoder, as the labeled subset rarely covers the full support of p(Y |X). Training on unlabeled data without a sufficiency constraint yields the trivial solution. Meaningful training on all available data therefore requires a regularizer that prevents collapse while remaining agnostic to the missing labels. In the absence of label information, the least biased choice is the maximum entropy distribution on PK (41), which imposes no preference over predictive classes. SIGReg (44) implements this prior as a distributional regularizer on the encoder output (see Section 5.4). The semi-supervised encoder loss is 

Lsemi enc (ϕ) := Eq(x,y )

DKL 

 qϕ(w|x) ∥ qϕ(w|y) | {z } 

> Lpred (labeled data only)

+ λ LSIGReg (qϕ(w)) 

| {z }

> Lshape

. (57) The first term targets the conditional rate I(X; W |Y ), estimated from the labeled distribution 

q(x, y ), and encourages the encoder to discard non-predictive information. The second term applies SIGReg to all data, preventing collapse. The trade-off coefficient λ ≥ 0 balances the two: too small and the encoder overfits the labeled subset, too large and the distributional prior dominates, washing out class-discriminative structure. 

6.3 Self-Supervised Learning with SIGReg 

In the self-supervised setting, no labels are available during encoder training. Without any sufficiency signal, minimizing I(X; W ) alone yields the trivial solution. The sufficiency signal is instead provided by a view-prediction loss [11, 14]: given augmented views {xv} of the same input, the encoder is trained so that the embeddings {fϕ(xv)} are close to a joint global view, replacing the conditional rate I(X; W |Y ) with a proxy based on shared semantic content [6].       

> 2We follow the convention of [40], with βon the prediction term. [3] place βon the KL term, related by
> βAlemi = 1 /β .

20 The self-supervised encoder loss is 

Lself enc (ϕ) := Ep(x)

DV({qϕ(w|xv)})| {z }

> Lpred

+ λ LSIGReg (qϕ(w)) 

| {z }

> Lshape

, (58) where DV penalizes spread among the view embeddings [6], and LSIGReg prevents collapse by en-forcing the distributional prior on all data (see Section 5.4). The trade-off coefficient λ ≥ 0 balances prediction against regularization: too small and the encoder ignores distributional structure. Too large and the prior dominates, washing out view-discriminative content. Since no labels are used, the encoder training is arguably task-agnostic. 

# 7 Experiments 

Four experiments are presented. The first two are toy problems where the ground-truth predic-tive manifold M = {p(Y |x) : x ∈ X } can be visualised on the probability simplex P3. The encoder is a Dirichlet distribution W ∼ Dir( α(x)) and β is swept to trace the IB curve. The third applies the minibatch-marginal CEB estimator to FashionMNIST and compares it with the standard VIB prior bound. The fourth ablates over the simplex dimension K, confirming that it is the only structural parameter and is easy to set. Every β sweep uses the identical grid β ∈ { 0.5, 5, 10 , 25 , 50 , 100 , 250 },with β = 0 .5 included as a deliberate sub-critical probe: by the phase transition (39), β < 1 lies at or below βc and should drive the encoder toward the trivial solution, which is verified empirically in all three settings. All reported accuracy, rate, and distortion values in Sections 7.3–7.4 are from a single training run per configuration (seed 42). 

7.1 Continuous input 

The input is an angle X = θ ∼ Uniform[0 , 2π) and Y ∈ { 0, 1, 2}. The ground-truth conditional 

p(Y = y|x) ∝ exp(cos( x−2πy/ 3)) traces a smooth loop through the simplex (see Figure 7). Because 

p(Y |x) is injective and x is continuous, the rate I(X; W ∗) = ∞, so the IB curve extends to infinite rate. The standard IB Lagrangian L = I(X; W )+ β I (X; Y |W ) is minimised using the known p(Y |X). As β increases, the Dirichlet means progressively recover the full predictive loop (see Figure 8). The rate–distortion trade-off is shown in Figure 9. 

Figure 7: Continuous predictive manifold: p(Y |x) traces a loop through P3 as θ varies over [0 , 2π). 21 Figure 8: Continuous experiment: Dirichlet means (blue) vs. ground-truth p(Y |x) (black circles) for increasing β.

Figure 9: Continuous experiment: Information Plane, Rate–Distortion curve, and ε(β). 

7.2 Discrete input 

Here X ∈ { 0, . . . , 19 } is uniform, with pairs of consecutive values sharing the same conditional (Y = ⌊X/ 2⌋ gives 10 clusters). The 10 distributions p(Y |x) are placed at equally-spaced angles on the same circular contour used in Section 7.1 (see Figure 10), giving H(X) = log 20 ≈ 3.00 but 

H(W ∗) = log 10 ≈ 2.30. The CEB Lagrangian L = (1 −β) I(X; W ) + β I (X; W |Y ) is estimated non-parametrically via leave-one-out (LOO) log-mixture estimators. No knowledge of p(Y |X) enters the loss. As β grows the encoder separates the 10 clusters (see Figure 11). The rate saturates near H(W ∗) = log 10, well below H(X) (see Figure 12): the encoder discards the redundant bit that distinguishes the two X-values within each cluster. The learned Dirichlet means do not coincide with the ground-truth simplex points. They form a different partition of the simplex that is nonetheless equivalent for prediction. The IB objective only requires W to be a sufficient statistic for Y . Any injective map from the 10 clusters into P3

achieves I(X; Y |W ) = 0 and is therefore a global optimum. The particular embedding is a gauge freedom of the loss (see Remark 4), so different random seeds (or β values) land on different but equally valid configurations. 

7.3 VIB vs. CEB 

The standard VIB [3] is compared with the CEB formulation on FashionMNIST (28 ×28 grayscale, 10 classes), sweeping β ∈ { 0.5, 5, 10 , 25 , 50 , 100 , 250 }. Both methods share the same convolutional backbone. They differ in the latent space, the loss, and the assumptions required. 

VIB uses a Gaussian encoder qϕ(w|x) = N (μϕ(x), diag σ2

> ϕ

(x)) with W ∈ R16 and a learned 22 Figure 10: Discrete predictive manifold: 10 distinct p(Y |x) points on the simplex loop. 

Figure 11: Discrete CEB experiment: Dirichlet means vs. ground-truth for increasing β.multilayer perceptron (MLP) decoder qψ(y|w). The loss is 

Lvib (ϕ) = β − Ep(x,y )Eqϕ(w|x)[log qψ(y|w)] | {z }

> Lpred

+ Ep(x)

DKL 

 qϕ(w|x) a(w) | {z }

> Lshape

, (59) where a(w) = N (0 , I ), cf. (56). 

CEB uses a Dirichlet encoder qϕ(w|x) = Dir( αϕ(x)) with W ∈ P 10 , the 10-class probability simplex. The loss is (54), 

Lceb  

> enc

(ϕ) = β Ep(x,y )

DKL 

 qϕ(w|x) ∥ qϕ(w|y) | {z }

> Lpred

+ (1 −β) Ep(x)

DKL 

 qϕ(w|x) ∥ qϕ(w) | {z }

> Lshape

, (60) where qϕ(w|y) and qϕ(w) are the class-conditional and marginal distributions estimated from mini-batch LOO log-mixture estimators. Unlike VIB, this requires no fixed prior, no decoder during training, and no variational bound. The loss targets the true mutual information directly. The 

only free parameter beyond the shared backbone is the number of components of W (here K=10, matching the number of classes). Because the CEB loss is purely information-theoretic, the encoder exhibits gauge freedom (see Remark 4): the learned simplex coordinates need not align with class labels. Classification accuracy is evaluated by training a small post-hoc readout MLP (10 → 256 → 10) on the frozen encoder means, separating representation learning from label alignment. Figure 13 shows the results. CEB matches or exceeds VIB at every operating point from 

β=5 onward, peaking at ∼91 .4% accuracy ( β=25) versus VIB’s best of ∼90 .7% ( β=250), while 23 Figure 12: Discrete CEB experiment: Information Plane, Rate–Distortion curve, and ε(β). Dashed lines mark H(X) and H(W ∗). maintaining a consistently lower rate. For a fair rate comparison, I(X; W ) is reported for both methods using the same minibatch LOO log-mixture estimator applied to each encoder’s own distribution family (Dirichlet for CEB, Gaussian for VIB). VIB’s variational KL-to-prior upper bound is not used, as it is not comparable to CEB’s non-parametric estimate. 

Relation to the phase transition. The sweep β ∈ { 0.5, 5, 10 , 25 , 50 , 100 , 250 } is designed to span the critical value βc of (39). FashionMNIST is a near-deterministic classification task: Y is almost a function of X, so H(W ∗|Y ) ≈ 0 and βc ≈ 1. Consistent with this, β = 0 .5 lies at or below the phase transition and CEB performs poorly there. β ≥ 5 sits comfortably above βc and yields the non-trivial operating points reported above. Two mechanisms prevent the minibatch-marginal estimator from collapsing at usable β. First, the LOO log-mixture estimator of I(X; W ) uses an ( N −1)-component mixture rather than the true marginal, biasing the estimated compression cost and softening the pull toward the trivial point. Second, the stochastic Dirichlet encoder acts as implicit regularization, keeping H(W |X) bounded away from zero and preventing degenerate deterministic solutions. 

7.4 Ablation over the simplex dimension K

The CEB encoder has a single structural parameter: the simplex dimension K. For a determin-istic classification task such as FashionMNIST, the true predictive manifold M = {p(Y |x) : x ∈ X } 

consists of |Y| = 10 point masses (the simplex vertices e0, . . . , e 9), so the effective degrees of freedom equal the number of classes. We therefore expect the encoder to need at least K = |Y| components and to gain little from K > |Y| .

β = 25 (the best CEB operating point from Section 7.3) is fixed and K ∈ { 3, 5, 10 , 15 , 20 } is swept. All other settings match Section 7.3: the same convolutional backbone, batch size (2048), optimizer (Adam, lr=10 −3), encoder training (90 epochs), and post-hoc readout (30 epochs, same architecture). K is the only quantity that varies. Figure 14 shows the results. Accuracy rises from 

K=3 (88 .7%) to K=10 (90 .9%) and plateaus for K > 10 (91 .2% at K=15 and K=20). Distortion 

I(X; Y |W ) continues to decrease with K, reflecting the additional capacity, but the predictive benefit saturates once K reaches the number of classes. K = |Y| is a natural default: sufficient and near-optimal. 

Behaviour for K < |Y| . The only sub-|Y| point in the sweep is K=3, which gives 88 .7% accuracy, a noticeable drop of roughly 2 pp from the K=10 value of 90 .9%, but not a total collapse. This is consistent with our framing: once K falls below the number of classes, multiple predictive clusters must share simplex vertices and some class information is necessarily lost, but the remaining K

24 Figure 13: FashionMNIST: VIB (Gaussian, R16 ) vs. CEB (Dirichlet, P10 ). Top-left: error rate vs. 

β. Top-right: information plane I(W ; Y ) vs. I(X; W ) (CEB only). Bottom-left: rate–distortion curve I(X; Y |W ) vs. I(X; W ) (CEB only). Bottom-right: distortion ε = I(X; Y |W ) vs. β (CEB only). CEB achieves higher accuracy at lower rate with no parametric assumptions beyond the dimension of W .vertices can still carry most of the label signal, so the degradation is gradual rather than catas-trophic. K=1 and K=2 are excluded from the sweep as their behaviour is determined a priori: P1

is a single point, so W ⊥ X by construction and accuracy is exactly chance (10%). P2 supports at most one non-trivial split and cannot separate ten classes. The trend K=3 → 5 → 10 and the plateau at K > |Y| together confirm that K = |Y| is the natural default. 

# 8 Conclusion 

We presented a geometric framework for representation learning grounded in the Information Bottleneck, showing that the optimal encoder at any distortion level performs a soft clustering of the predictive manifold M ⊂ P K . The Dirichlet-to-Gaussian chain connects the maximum entropy prior on the simplex to the isotropic Gaussian enforced by SIGReg, identifying it as a Gaussian relaxation with a non-diminishing entropy overhead that affects rate accounting but not achievable prediction. This relaxation provides a principled distributional regularizer for semi-supervised 25 Figure 14: CEB ablation over simplex dimension K at β = 25. Accuracy saturates at K=|Y| =10. Further increasing K reduces distortion but does not improve classification. and self-supervised learning. The Conditional Entropy Bottleneck decomposition yields practical encoder losses for supervised and semi-supervised settings. In the self-supervised setting, SIGReg serves as the regularizer alongside a view-prediction proxy. Several directions remain open. The continuous-Y bound (see Proposition 4) relies on asymp-totic covering-number dimension. Replacing this with a more operational notion would strengthen the result. The exponential parameterization offers a near-exact alternative to the Gaussian with a vanishing overhead, and its practical viability deserves empirical investigation. Finally, scaling the non-parametric CEB estimator to larger datasets and higher-dimensional tasks would test the limits of the minibatch marginal approach. 

# References 

[1] Aqeel Abbas and Marcus Hutter. A scale-dependent notion of effective dimension. arXiv preprint arXiv:2001.10872 , 2020. [2] Alessandro Achille and Stefano Soatto. Emergence of invariance and disentanglement in deep representations. Journal of Machine Learning Research , 19(50):1–34, 2018. 26 [3] Alexander A Alemi, Ian Fischer, Joshua V Dillon, and Kevin Murphy. Deep variational information bottleneck. In International Conference on Learning Representations , 2017. [4] Shun-ichi Amari. Information Geometry and Its Applications . Springer, 2016. [5] Suguru Arimoto. An algorithm for computing the capacity of arbitrary discrete memoryless channels. IEEE Transactions on Information Theory , 18(1):14–20, 1972. [6] Randall Balestriero and Yann LeCun. LeJEPA: Provable and scalable self-supervised learning without the heuristics. arXiv preprint arXiv:2511.08544 , 2025. [7] Toby Berger. Rate Distortion Theory: A Mathematical Basis for Data Compression . Prentice-Hall, 1971. [8] Patrick Billingsley. Probability and Measure . Wiley, 3 edition, 1995. [9] Christopher M. Bishop. Pattern Recognition and Machine Learning . Springer, 2006. [10] Richard Blahut. Computation of channel capacity and rate-distortion functions. IEEE Trans-actions on Information Theory , 18(4):460–473, 1972. [11] Avrim Blum and Tom Mitchell. Combining labeled and unlabeled data with co-training. In Proceedings of the Eleventh Annual Conference on Computational Learning Theory , pages 92–100. ACM, 1998. [12] Mathilde Caron, Hugo Touvron, Ishan Misra, Herv´ e J´ egou, Julien Mairal, Piotr Bojanowski, and Armand Joulin. Emerging properties in self-supervised vision transformers. In Interna-tional Conference on Computer Vision , pages 9650–9660, 2021. [13] Gal Chechik, Amir Globerson, Naftali Tishby, and Yair Weiss. Information bottleneck for Gaussian variables. Journal of Machine Learning Research , 6:165–188, 2005. [14] Ting Chen, Simon Kornblith, Mohammad Norouzi, and Geoffrey Hinton. A simple framework for contrastive learning of visual representations. In International Conference on Machine Learning , pages 1597–1607, 2020. [15] Thomas M Cover and Joy A Thomas. Elements of Information Theory . Wiley-Interscience, 2nd edition, 2006. [16] Thomas W Epps and Lawrence B Pulley. A test for normality based on the empirical charac-teristic function. Biometrika , 70(3):723–726, 1983. [17] Kenneth Falconer. Fractal Geometry: Mathematical Foundations and Applications . John Wiley & Sons, 2nd edition, 2003. [18] Charles Fefferman, Sanjoy Mitter, and Hariharan Narayanan. Testing the manifold hypothesis. 

Journal of the American Mathematical Society , 29(4):983–1049, 2016. [19] Thomas S Ferguson. A Bayesian analysis of some nonparametric problems. The Annals of Statistics , 1(2):209–230, 1973. [20] Ian Fischer. The conditional entropy bottleneck. Entropy , 22(9):999, 2020. [21] Ronald A Fisher. On the mathematical foundations of theoretical statistics. Philosophical Transactions of the Royal Society of London A , 222:309–368, 1922. 27 [22] Peter G´ acs and J´ anos K¨ orner. Common information is far less than mutual information. 

Problems of Control and Information Theory , 2(2):149–162, 1973. [23] Ziv Goldfeld and Yury Polyanskiy. The information bottleneck problem and its applications in machine learning. IEEE Journal on Selected Areas in Information Theory , 1(1):99–129, 2020. [24] Yves Grandvalet and Yoshua Bengio. Semi-supervised learning by entropy minimization. In 

Advances in Neural Information Processing Systems , volume 17, 2004. [25] Jean-Bastien Grill, Florian Strub, Florent Altch´ e, Corentin Tallec, Pierre Richemond, Elena Buchatskaya, Carl Doersch, Bernardo Avila Pires, Zhaohan Chen, Michal Valko, et al. Boot-strap your own latent: A new approach to self-supervised learning. In Advances in Neural Information Processing Systems , 2020. [26] Peter Harrem¨ oes and Naftali Tishby. The information bottleneck revisited or how to choose a good distortion measure. In IEEE International Symposium on Information Theory , pages 566–570, 2007. [27] Edwin T. Jaynes. Information theory and statistical mechanics. Physical Review , 106(4):620– 630, 1957. [28] Weonyoung Joo, Wonsung Lee, Sungrae Park, and Il-Chul Moon. Dirichlet variational autoen-coder. Pattern Recognition , 107:107514, 2020. [29] Diederik P. Kingma, Shakir Mohamed, Danilo Jimenez Rezende, and Max Welling. Semi-supervised learning with deep generative models. In Advances in Neural Information Processing Systems , volume 27, 2014. [30] Artemy Kolchinsky, Brendan D Tracey, and Steven Van Kuyk. Nonlinear information bottle-neck. Entropy , 21(12):1181, 2019. [31] Andrei N Kolmogorov and Vladimir M Tikhomirov. ε-entropy and ε-capacity of sets in func-tional spaces. American Mathematical Society Translations , 17:277–364, 1961. [32] Yann LeCun. A path towards autonomous machine intelligence. Technical report, Meta AI, 2022. [33] Erich L Lehmann and Henry Scheff´ e. Completeness, similar regions, and unbiased estimation: Part I. Sankhy¯ a, 10(4):305–340, 1950. [34] Phillip Pope, Chen Zhu, Ahmed Abdelkader, Micah Goldblum, and Tom Goldstein. The intrinsic dimension of images and its impact on learning. In International Conference on Learning Representations , 2021. [35] Andrew M Saxe, Yamini Bansal, Joel Dapello, Madhu Advani, Artemy Kolchinsky, Bren-dan D Tracey, and David D Cox. On the information bottleneck theory of deep learning. In 

International Conference on Learning Representations , 2018. [36] Claude E Shannon. Coding theorems for a discrete source with a fidelity criterion. IRE National Convention Record , 7(4):142–163, 1959. [37] Ravid Shwartz-Ziv and Yann LeCun. To compress or not to compress—self-supervised learning and information theory: A review. Entropy , 26(3):252, 2024. 28 [38] Ravid Shwartz-Ziv and Naftali Tishby. Opening the black box of deep neural networks via information. arXiv preprint arXiv:1703.00810 , 2017. [39] DJ Strouse and David J Schwab. The deterministic information bottleneck. Neural Computa-tion , 29(6):1611–1630, 2017. [40] Naftali Tishby, Fernando C Pereira, and William Bialek. The information bottleneck method. In Proceedings of the 37th Annual Allerton Conference on Communication, Control, and Com-puting , pages 368–377, 1999. [41] Michael Tschannen, Josip Djolonga, Paul K Rubenstein, Sylvain Gelly, and Mario Lucic. On mutual information maximization for representation learning. In International Conference on Learning Representations , 2020. [42] Jesper E van Engelen and Holger H Hoos. A survey on semi-supervised learning. Machine Learning , 109(2):373–440, 2020. [43] Hans S Witsenhausen and Aaron D Wyner. A conditional entropy bound for a pair of discrete random variables. IEEE Transactions on Information Theory , 21(5):493–501, 1975. [44] Aaron D. Wyner and Jacob Ziv. The rate-distortion function for source coding with side information at the decoder. IEEE Transactions on Information Theory , 22(1):1–10, 1976. [45] Aolin Xu and Maxim Raginsky. Information-theoretic analysis of generalization capability of learning algorithms. In Advances in Neural Information Processing Systems , 2017. 

# Appendices A Effective Dimension in the Continuous Case 

When Y is continuous, p(Y |x) is a density and the predictive manifold M = {p(Y |x) : x ∈ X } 

is a subset of an infinite-dimensional function space. 3 The effective dimension of M is finite under mild regularity conditions on f ∗ : x 7 → p(Y |x) and the input space X .Let ( S, d ) be a metric space, where d : S ×S → R≥0 is a distance function. The covering number 

N (δ, S, d ) is the smallest number of d-balls of radius δ needed to cover S. The effective dimension 

of S [31] is the exponent governing the growth of the covering number, 4

deff (S) := lim 

> δ→0

log N (δ, S, d )log(1 /δ ) , (61) when this limit exists (or the corresponding lim sup). 

Proposition 4 (Geometric bound [17, §3.2]) . Suppose the support of p(X) has intrinsic dimension 

dX , meaning log N (δ, supp( pX ), ∥ · ∥ ) = Θ( dX log(1 /δ )) . If f ∗ is L-Lipschitz with respect to a metric ρ on the space of distributions (e.g. Hellinger distance dH or total variation), 

ρ p(Y |x1), p (Y |x2) ≤ L ∥x1 − x2∥, (62) 

then deff (M) ≤ dX .            

> 3We continue to call Ma “manifold” loosely: it is the image of x7→p(Y|x) and may have singularities, self-intersections, or varying dimension. Only the metric-space structure ( M, d ) is used in what follows.
> 4The term effective dimension has been used in several disjoint senses in the literature. Here we use it as our name for this covering-number-based variant (equivalently, the upper box-counting / Minkowski dimension), in the spirit of Kolmogorov–Tikhomirov’s ε-entropy [31].

29 Proof. Any ( δ/L )-cover of supp( pX ) maps under f ∗ to a δ-cover of M in ρ. Hence log N (δ, M, ρ ) ≤ log N δ/L, supp( pX ), ∥ · ∥  = Θ  dX log(1 /δ ), (63) so deff (M) ≤ dX .The intrinsic dimension dX reflects the manifold hypothesis [18, 34]: even when X lives in a high-dimensional ambient space, p(X) typically concentrates near a lower-dimensional submanifold. Lipschitz regularity of f ∗ ensures this low dimension transfers to the predictive manifold M.

Example 4 (Scalar Gaussian channel) . Let Y = X + Z with Z ∼ N (0 , σ 2 

> Z

) independent of X, so 

p(Y |x) = N (x, σ 2 

> Z

). The predictive manifold is M = {N (x, σ 2 

> Z

) : x ∈ R}, a one-parameter family (varying mean, fixed variance). Since X is scalar, dX = 1 . To verify the Lipschitz condition, note that d2 

> H

(p(Y |x1), p (Y |x2)) = 1 −exp( −(x1−x2)2/8σ2 

> Z

) ≤ (x1−x2)2/8σ2 

> Z

, so dH (p(Y |x1), p (Y |x2)) ≤|x1−x2|/(2 √2 σZ ): f ∗ is Lipschitz with constant L = 1 /(2 √2 σZ ). Proposition 4 gives deff (M) ≤

dX = 1 , hence K = ⌈deff (M)⌉ + 1 = 2 and P2 suffices. Since p(Y |x) is injective, M is a genuine 1D curve and P1 (a single point) is insufficient: K = 2 is tight. 

Remark 6. For discrete Y , K = |Y| exactly. The joint bound is K ≤ min  |Y| , ⌈dX ⌉ + 1 . For continuous Y , the optimal representation admits a reparameterization of dimension at most dX in general, since X itself is sufficient. The stronger statement deff (M) ≤ dX , and hence K ≤ ⌈ dX ⌉ + 1 

as defined, requires Lipschitz regularity of f ∗ (see Proposition 4). 

# B Proof of Theorem 2 

Proof. The argument is the standard time-sharing construction for convexity of the rate-distortion function [15, Theorem 10.2.1], adapted to the IB distortion measure. Let ε1, ε 2 ∈ [0 , I (X; Y )] and 

λ ∈ [0 , 1]. For any δ > 0, let p1(w|x) be a feasible encoder with I1(X; Y |W ) ≤ ε1 and I1(X; W ) ≤

R(ε1) + δ, and let p2(w|x) be a feasible encoder with I2(X; Y |W ) ≤ ε2 and I2(X; W ) ≤ R(ε2) + δ.Such encoders exist. For finite alphabets the minimum is achieved, so one may take δ = 0. Let Q ∼ Bernoulli( λ) be independent of ( X, Y ), and define 

W := 

(

(1 , W 1) if Q = 1 ,

(0 , W 2) if Q = 0 , (64) where Wi ∼ pi(w|x). Since each pi(w|x) satisfies Y − X − Wi and Q ⊥ (X, Y ), the combined encoder satisfies Y − X − W : for w = ( q, w q), 

p(w|x, y ) = p(q|x, y ) pq(wq|q, x, y ) = p(q|x) pq(wq|q, x ) = p(w|x). (65) 

Rate decomposition. Since Q ⊥ X,

I(X; W ) = I(X; Q, W Q) = I(X; Q) + I(X; WQ|Q)= 0 + λ I 1(X; W1) + (1 − λ) I2(X; W2)

≤ λ (R(ε1) + δ) + (1 − λ) ( R(ε2) + δ)= λ R (ε1) + (1 − λ) R(ε2) + δ. (66) 

Distortion decomposition. Since W = ( Q, W Q), 

I(X; Y |W ) = I(X; Y |Q, W Q)= λ I 1(X; Y |W1) + (1 − λ) I2(X; Y |W2)

≤ λ ε 1 + (1 − λ) ε2. (67) 30 The time-shared encoder is feasible at distortion ¯ ε = λε 1 + (1 − λ)ε2. Its rate is at most 

λR (ε1) + (1 − λ)R(ε2) + δ. Since R(¯ ε) is the minimum rate at distortion ¯ ε,

R λε 1 + (1 − λ)ε2

 ≤ λ R (ε1) + (1 − λ) R(ε2) + δ. (68) Taking δ → 0 gives 

R λε 1 + (1 − λ)ε2

 ≤ λ R (ε1) + (1 − λ) R(ε2). (69) 

Monotonicity. The feasible set {p(w|x) : I(X; Y |W ) ≤ ε} grows with ε, so the minimum rate cannot increase. 

Concavity of ∆∗(R). Write ∆ ∗(R) = I(X; Y ) − ε∗(R), where ε∗(R) is the inverse of R(ε). Since 

R(ε) is convex and non-increasing, ε∗(R) is convex and non-increasing, so ∆ ∗(R) is concave and non-decreasing. 

# C Flat Portion of the IB Curve 

For any R ∈ [H(W ∗), H (X)], set W = ( W ∗, U ) with U = g(X) for a suitably chosen function 

g. Since W ∗ = f ∗(X) is a function of X, the Markov chain Y − X − W is preserved. The rate 

I(X; W ) ≥ H(W ∗) increases with the information g retains about X, reaching I(X; W ) = H(X)at g(X) = X.For the predictive information, the data processing inequality on W → W ∗ gives I(W ; Y ) ≥

I(W ∗; Y ) = I(X; Y ). The data processing inequality on Y − X − W gives I(W ; Y ) ≤ I(X; Y ), so 

I(W ; Y ) = I(X; Y ). The IB curve is therefore flat at I(X; Y ) for all R ∈ [H(W ∗), H (X)]. 

# D Proof of Proposition 3 

Proof. Let β1 < β 2 and let p1, p 2 denote the corresponding optimal encoders achieving ( R1, ε 1) and (R2, ε 2) respectively. By optimality of p1 at β1,

R1 + β1 ε1 ≤ R2 + β1 ε2. (70) By optimality of p2 at β2,

R2 + β2 ε2 ≤ R1 + β2 ε1. (71) Adding these two inequalities gives (β2 − β1) ε2 ≤ (β2 − β1) ε1. (72) Since β2 > β 1, dividing by β2 − β1 > 0 yields ε2 ≤ ε1, so ε(β) is non-increasing. For the rate, the first optimality inequality gives 

R1 − R2 ≤ β1 (ε2 − ε1), (73) and the second gives 

R2 − R1 ≤ β2 (ε1 − ε2), (74) equivalently R1 − R2 ≥ β2 (ε2 − ε1). Since ε2 − ε1 ≤ 0 and β1 ≥ 0, the first inequality gives 

R1 − R2 ≤ β1(ε2 − ε1) ≤ 0, so R1 ≤ R2 and R(β) is non-decreasing. 31 E Derivation of the Equivalent Lagrangian 

Expanding I(X; W, Y ) = I(X; W ) + I(X; Y |W ) = I(X; Y ) + I(X; W |Y ) in two ways gives the chain-rule identity 

I(X; Y |W ) + I(X; W ) = I(X; W |Y ) + I(X; Y ). (75) Since I(X; Y ) is constant with respect to the encoder, the Lagrangian L = I(X; W ) + β I (X; Y |W )can be rewritten as 

L = I(X; W ) + β I (X; Y |W )= I(X; W ) + β I(X; W |Y ) + I(X; Y ) − I(X; W )

= (1 − β) I(X; W ) + β I (X; W |Y ) + β I (X; Y ). (76) Dropping the constant β I (X; Y ) yields the equivalent Lagrangian (49). 32