---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 针对汽车弱势路面使用者安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:train-trick"]
score: 8.0
evidence: 用于模型压缩和量化鲁棒性的知识蒸馏
tldr: "本研究针对自动驾驶中弱势路人（VRU）检测在边缘设备部署的难题，提出了一种知识蒸馏（KD）框架。通过让轻量级的YOLOv8-S学生模型学习大型YOLOv8-L教师模型，实现了3.9倍的模型压缩。实验表明，该方法显著提升了模型在INT8量化下的鲁棒性，不仅解决了大型模型量化后精度大幅下降的问题，还在保持高召回率的同时将误报率降低了44%，为安全关键的边缘AI部署提供了有效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 旨在解决大型目标检测模型在边缘硬件进行INT8量化时精度剧降，而小型模型原生精度不足的矛盾。
method: 采用知识蒸馏框架，训练参数量仅为11.2M的YOLOv8-S学生模型去模仿43.7M参数的YOLOv8-L教师模型。
result: "在BDD100K数据集上，KD学生模型在INT8量化下的精度甚至超过了教师模型的FP32精度，且误报率比量化后的教师模型降低了44%。"
conclusion: 知识蒸馏是实现在边缘硬件上部署高精度、高鲁棒性且符合安全要求的VRU检测系统的关键技术。
---

## 摘要
在边缘硬件上部署用于弱势路面使用者（VRU）安全的高精度目标检测，需要在模型容量与计算约束之间取得平衡。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，训练紧凑的 YOLOv8-S 学生模型（1120万参数）来模仿 YOLOv8-L 教师模型（4370万参数），在实现 3.9 倍压缩的同时保留了量化鲁棒性。我们在全规模 BDD100K 数据集（7万张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 下性能大幅下降（mAP 下降 23%），而经过知识蒸馏的学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，知识蒸馏传递的是精度校准而非原始检测能力：在 INT8 下，知识蒸馏学生模型的精度达到 0.748，而直接训练的模型仅为 0.653，在等效召回率下提升了 14.5%，且相比性能崩溃的教师模型，虚警率降低了 44%。在 INT8 量化下，知识蒸馏学生模型的精度（0.748）甚至超过了体积大 3.9 倍的教师模型的 FP32 精度（0.718）。这些发现表明，知识蒸馏是在边缘硬件上部署高精度、安全关键型 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在计算资源受限的边缘硬件（如车载 AI 加速器）上，既保证弱势路面使用者（VRU，如行人、骑行者）检测的高精度，又满足实时性和低功耗的 INT8 量化部署要求？
*   **研究动机**：
    *   **量化困境**：大型检测模型（如 YOLOv8-L）在 FP32 下精度高，但在边缘部署必备的 INT8 量化下会出现“精度崩塌”（mAP 下降超过 20%）。
    *   **性能瓶颈**：小型模型（如 YOLOv8-S）虽然量化鲁棒性好，但原生检测精度（尤其是对少数类别的召回率）不足。
    *   **安全需求**：自动驾驶系统对误报（False Alarms）极其敏感，误报会侵蚀驾驶员信任并导致系统频繁误触发。

### 2. 论文提出的方法论
*   **核心思想**：采用**知识蒸馏（Knowledge Distillation, KD）**框架，训练一个紧凑的“学生模型”（YOLOv8-S）去模仿一个强大的“教师模型”（YOLOv8-L）的软输出和中间特征，从而将教师模型的“精度校准能力”转移给学生模型。
*   **关键技术细节**：
    *   **损失函数设计**：总损失 $L_{total} = \alpha \cdot L_{task} + \beta \cdot L_{logit} + \gamma \cdot L_{feat}$。
        *   $L_{task}$：标准检测损失（分类+定位）。
        *   $L_{logit}$：基于 KL 散度的逻辑值蒸馏，引入温度系数 $T=10$ 来平滑概率分布。
        *   $L_{feat}$：基于 L2 距离的中间特征图对齐。
    *   **任务损失衰减（Task-Dampening）**：这是本文的关键发现。通过将 $\alpha$ 设为 0.5（降低地面真值标签的权重），强迫学生模型更多地学习教师模型的软分布，从而获得更好的置信度校准。
*   **量化策略**：采用 TensorRT 的训练后量化（PTQ）技术，将模型从 FP32 压缩至 INT8。

### 3. 实验设计
*   **数据集**：使用 **BDD100K**，这是目前最大的自动驾驶公开数据集之一。实验涵盖了 70,000 张训练图像和 10,000 张验证图像。
*   **检测目标**：专注于 VRU 类别，包括行人（Pedestrian）、骑行者（Cyclist）和摩托车手（Motorcyclist）。
*   **Benchmark 与对比方法**：
    *   **Teacher (L)**：直接训练的 YOLOv8-L（43.7M 参数）。
    *   **Direct Student (S)**：直接使用 Ground Truth 训练的 YOLOv8-S（11.2M 参数）。
    *   **KD Student (S)**：通过本文蒸馏框架训练的 YOLOv8-S。
    *   **对比维度**：FP32 vs INT8 精度、mAP50、精确率（Precision）、召回率（Recall）、虚警率（FAR）及推理速度（FPS）。

### 4. 资源与算力
*   **训练算力**：使用云端 GPU 基础设施，具体型号为 **NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)**。
*   **训练时长**：
    *   教师模型：7 小时。
    *   直接训练学生模型：2.7 小时。
    *   知识蒸馏学生模型：4.9 小时（增加的时长源于教师模型的前向推理开销）。
*   **推理/量化测试**：在 **NVIDIA RTX 5070 (8GB VRAM)** 上进行，以模拟接近边缘端的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：在全规模 BDD100K 数据集上运行，而非简化版，保证了结果的统计说服力。
*   **消融实验**：专门针对任务损失权重 $\alpha$ 做了对比实验（$\alpha=0.5$ vs $\alpha=1.0$），验证了“任务衰减”对精度校准的关键作用。
*   **客观性**：实验对比了相同架构在不同训练策略下的表现，并详细记录了量化前后的性能变化，评价指标全面（不仅看 mAP，还深入分析了 Precision 和 FAR），实验设计较为公平、客观。

### 6. 论文的主要结论与发现
*   **KD 提升量化鲁棒性**：在 INT8 量化下，教师模型 mAP 下降了 23.4%，而 KD 学生模型仅下降 5.6%。
*   **精度校准转移**：KD 转移的不是原始检测能力（召回率基本持平），而是**精度校准（Precision Calibration）**。在 INT8 下，KD 学生模型的精确率比直接训练的学生模型高出 14.5%。
*   **大幅降低误报**：在 INT8 部署时，KD 学生模型比性能崩溃的教师模型减少了 **44% 的虚警（误报）**。
*   **性能超越**：KD 学生模型（11.2M）在 INT8 下的精确率（0.748）甚至超过了其教师模型在 FP32 下的精确率（0.718）。

### 7. 优点
*   **实用性强**：直接针对边缘 AI 部署中的量化痛点，提供了可落地的技术路径。
*   **深入的机理分析**：不仅展示了结果，还通过消融实验揭示了知识蒸馏如何通过影响置信度校准来提升量化后的表现。
*   **显著的压缩比**：实现了 3.9 倍的参数压缩和 2.37 倍的推理加速，同时保持了极高的安全性指标。

### 8. 不足与局限
*   **架构局限性**：实验仅限于 YOLOv8 系列，尚未验证该结论在其他架构（如 Transformer-based 检测器）或其他 YOLO 版本（如 YOLOv10/11）上的普适性。
*   **硬件偏差**：虽然使用了 RTX 5070 进行测试，但仍属于桌面级 GPU，未在真正的嵌入式车载芯片（如 NVIDIA Jetson Orin 或专用 ASIC）上进行功耗和热管理分析。
*   **量化方法单一**：仅使用了 PTQ（训练后量化），未探讨 QAT（量化感知训练）与 KD 结合是否能进一步压榨性能。
*   **场景覆盖**：未针对雨天、雾天或夜间等极端天气条件进行专门的鲁棒性分层评估。

（完）
