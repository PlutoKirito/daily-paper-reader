---
title: "JLT: Clean-Latent Prediction in Latent Diffusion Transformers"
title_zh: JLT：潜扩散Transformer中的干净潜预测
authors: "Funing Fu, Tenghui Wang, Guanyu Zhou, Junyong Cen, Qichao Zhu"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27102v2"
tags: ["query:vcodec"]
score: 8.0
evidence: 研究潜在扩散Transformer中的纯净潜在预测，直接针对潜在扩散技术
tldr: 针对潜在扩散模型中预测目标选择问题，本文提出JLT模型，采用干净潜在预测而非速度预测。通过局部高斯分析揭示速度预测会放大低方差潜在方向，而干净预测则抑制该效应。在FLUX.2 VAE潜在空间上训练的130M参数DiT，在ImageNet 256×256上FID-50K达2.50，显著优于速度预测。结果表明预测目标需考虑表示依赖性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27102v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1396, \"height\": 702, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27102v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27102v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27102v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 265, \"label\": \"Table\"}]"
motivation: 验证干净数据预测原则在已压缩的潜在空间中是否仍然有效，并探究不同预测目标对生成质量的影响。
method: 基于FLUX.2 VAE编码的潜在空间，训练130M参数DiT，对比干净潜在预测与速度预测，并引入局部高斯分析解释差异。
result: JLT-B/1模型在ImageNet 256×256上FID-50K为2.50，相比速度预测有显著优势。
conclusion: 潜在扩散中的预测目标是表示依赖的几何选择，干净潜在预测优于速度预测。
---

## 摘要
针对干净数据预测的流匹配表明，回归干净点比预测环境噪声量更能有效利用低维结构。我们探究了这一原则在图像映射到学习到的潜空间后是否仍然有效，其中压缩已经消除了原始像素的大部分可变性。我们提出了JLT，一个在冻结的FLUX.2 VAE编码上的130M潜扩散Transformer，并在相同的表示、主干网络和训练设置下，将干净潜预测与匹配的速度预测DiT进行比较。尽管对于固定的损坏时间，三个变量x、ε和v是线性可转换的，但局部高斯分析表明，速度回归继承了各向同性的目标协方差下限，并放大了低方差的潜方向，而干净预测则抑制了它们。在ImageNet 256×256上，JLT-B/1在无分类器引导下获得了FID-50K 2.50，与速度预测相比存在较大的匹配目标差距。这些结果表明，潜扩散中的预测目标是依赖于表示的几何选择，而非可互换的代数参数化。

## Abstract
Flow matching with clean-data prediction has shown that regressing the clean point can exploit low-dimensional structure more effectively than predicting an ambient noised quantity. We ask whether this principle remains useful after images are mapped into a learned latent space, where compression has already removed much of the raw pixel variability. We introduce JLT, a 130M latent diffusion Transformer over frozen FLUX.2 VAE codes, and compare clean-latent prediction with a matched velocity-prediction DiT under the same representation, backbone, and training settings. Although the three variables x, epsilon, and v are linearly convertible for a fixed corruption time, a local Gaussian analysis shows that velocity regression inherits an isotropic target-covariance floor and amplifies low-variance latent directions, while clean prediction damps them. On ImageNet 256 x 256, JLT-B/1 obtains FID-50K 2.50 with classifier-free guidance, with a large matched-target gap over velocity prediction. These results suggest that prediction targets in latent diffusion are representation-dependent geometric choices, rather than interchangeable algebraic parameterizations.

---

## 论文详细总结（自动生成）

# 论文总结：JLT: Clean-Latent Prediction in Latent Diffusion Transformers

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：扩散模型中的预测目标（如预测干净样本 x、噪声 ε 或速度 v）虽然代数上可线性转换，但在有限容量网络下，不同目标会导致不同的回归难易度。已有工作（JiT）在像素空间证明干净预测能利用低维流形结构，优于噪声和速度预测。
- **核心问题**：当图像被压缩到潜在空间（如 VAE 编码）后，压缩已消除大量像素可变性，此时干净预测原则是否仍然有效？即目标选择是否仍为几何选择而非简单代数重参数化？
- **整体含义**：验证潜在扩散中预测目标的表示依赖性，强调目标参数化在潜在空间仍是重要的建模选择，会显著影响生成质量。

## 2. 方法论核心思想与关键技术细节

- **核心思想**：在固定 FLUX.2 VAE 潜在空间中，比较干净潜在预测（直接预测干净潜变量 x）与速度预测（预测 v = x - ε）的生成性能，并给出局部高斯分析解释差异。
- **关键技术细节**：
  - 使用线性损坏路径：\( z_t = t x + (1-t) \epsilon \)，其中 t ∈ [0,1]。
  - 模型输出：JLT 预测 x，DiT（对照）预测 v。
  - 训练损失：JLT 实际使用 readout 后的速度损失（即预测 x 后通过公式转为 v 再计算损失），与直接预测 v 的 DiT 仅在输出参数化上不同。
  - 局部高斯分析：假设局部数据服从 \( x \sim N(0,\Sigma) \)，噪声 \( \epsilon \sim N(0,I) \)。目标协方差：Cov(y_x) = Σ，Cov(y_v) = Σ + I，即速度预测为所有方向添加单位方差下限。条件后验方差：Var(v_i|z_i) 比 Var(x_i|z_i) 大 1/(1-t)^2 倍，且当 λ_i → 0 时，干净预测系数趋于 0，速度预测系数趋于 -1/(1-t)，放大低方差方向。
- **架构**：Base-scale Transformer，12 个 Transformer 块，隐藏维度 768，12 注意力头，128 维瓶颈 patch 嵌入，130M 参数。使用冻结 FLUX.2 VAE（latent tokenizer），patch 分 /1（1×1 latent grid）和 /2（2×2 聚合）两种变体。

## 3. 实验设计

- **数据集**：ImageNet 256×256，class-conditional 生成。
- **评测指标**：FID-50K（越低越好）、IS（越高越好）。
- **对比方法**：
  - 主要消融：JLT-B/1 与 DiT-B/1（速度预测），JLT-B/2 与 DiT-B/2，保持训练设置、表示、架构相同，仅改变预测目标。
  - 代表性基线：JiT-L/16（像素级干净预测，459M 参数）、LDM（潜在扩散）、JiT-B/16（像素级，131M 参数）。
- **实验数量**：核心为两组消融（/1 和 /2），另有一组最终 JLT-B/1 的完整结果（使用 CFG 和无 CFG），并比较了代表性基线。附录中给出了额外诊断建议但未做。

## 4. 资源与算力

- 论文明确提到：训练了 250K 步（200 个 epoch），有效 batch size 1024，使用 AdamW 优化器，学习率 2×10⁻⁴（基学习率 5×10⁻⁵ 经 batch 缩放后）。未明确说明 GPU 型号、数量或训练时长。仅指出训练设置为 JiT-B 设置，并保持固定。

## 5. 实验数量与充分性

- **实验数量**：主要消融实验 2 组（/1 和 /2），每组两个目标对比，共 4 个模型；最终 JLT-B/1 额外给出 CFG 和 no CFG 结果；对比基线 3 个。实验数量适中，但聚焦于控制变量。
- **充分性**：设计公平——固定表示、架构、优化、采样，仅改变目标。训练曲线（图 2）显示全程优势。但局限：仅测试 ImageNet 256×256 一个数据集，未扩展其他尺度或数据集；未进行不同噪声调度、损失加权、采样器等的系统消融；无跨 tokenizer（如其他 VAE）验证。因此实验充分但有限。

## 6. 主要结论与发现

- 干净潜在预测在匹配设置下显著优于速度预测：/1 变体 FID 从 6.56 降至 2.56（CFG），/2 变体从 28.71 降至 14.81。
- 局部高斯分析提供机制：速度预测带来各向同性协方差地板，并放大低方差潜在方向；干净预测抑制这些方向，降低回归难度。
- 结论：潜扩散中的预测目标是表示依赖的几何选择，而非代数互换；即使经过压缩，目标选择仍然重要。

## 7. 优点

- **控制变量严谨**：严格固定表示、主干、训练、采样，仅改变直接输出参数化，隔离目标几何效应。
- **理论解释简洁**：通过局部线性高斯模型给出协方差和条件方差推导，直观说明目标差异来源。
- **公开代码**：提供 GitHub 仓库，可复现。
- **实验设置清晰**：明确区分引导和无引导结果，排除辅助分类损失和重复类标记拼接等混淆因素。
- **结果差异大**：FID 差距达 4 分（/1），有力证明目标选择的重要性。

## 8. 不足与局限

- **数据集单一**：仅在 ImageNet 256×256 上验证，未涉及其他分辨率、其他类别数据集或文本-图像生成。
- **模型规模有限**：仅使用 130M Base-scale，未在更大模型（XL 等）上验证结论的泛化性。
- **架构固定**：仅使用 DiT 风格 Transformer，未在其他架构（U-Net 等）上验证。
- **Tokenizer 单一**：仅用 FLUX.2 VAE，未比较其他潜在编码器（如 LDM 的 VQ-VAE、其他 VAE）。
- **噪声调度固定**：仅使用线性损坏路径，未试验其他调度（如 EDM 的连续噪声）。
- **理论简化**：局部高斯假设不严格成立，推导仅作为机制解释，非全局最优证明；未提供非参数验证（附录 D 建议但未做）。
- **未消融损失加权**：JLT 使用 readout 后的速度损失，与直接速度预测相比权重不同，可能引入混淆（但作者认为符合 JiT 惯例）。
- **训练步数较短**：250K 步（200 epoch），可能未完全收敛，但对比公平。

（完）
