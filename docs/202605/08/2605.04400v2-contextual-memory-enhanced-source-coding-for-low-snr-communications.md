---
title: Contextual Memory-Enhanced Source Coding for Low-SNR Communications
title_zh: 低信噪比通信中基于上下文记忆增强的信源编码
authors: "Ziqiong Wang, Rongpeng Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04400v2"
tags: ["query:entropy-coding"]
score: 8.5
evidence: 算术编码依赖于基于大语言模型的概率估计
tldr: 针对低信噪比下分离信源信道编码（SSCC）中自回归解码易受残余误码干扰的问题，本文提出了一种增强上下文记忆的信源编码方案（MASC）。该方案通过在收发两端共享参数化上下文记忆（PCM）和混合记忆专家路由（MMER），将上下文模式内化到信源模型中，从而优化概率估计并提高解码鲁棒性。实验证明该方法在瑞利衰落和高斯白噪声信道下均优于现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04400v2/fig-001.webp\", \"caption\": \"Fig. 1. Framework of the Proposed MASC-Based SSCC System.\", \"page\": 2, \"index\": 1, \"width\": 806, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2605.04400v2/fig-002.webp\", \"caption\": \"Fig. 2. The MASC Architecture.\", \"page\": 3, \"index\": 2, \"width\": 968, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2605.04400v2/fig-003.webp\", \"caption\": \"Fig. 4. Overall system-level performance under AWGN channels.\", \"page\": 6, \"index\": 3, \"width\": 1063, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2605.04400v2/fig-004.webp\", \"caption\": \"Fig. 3. Overall system-level performance under Rayleigh channels.\", \"page\": 6, \"index\": 4, \"width\": 1060, \"height\": 285}]"
motivation: 解决低信噪比环境下，分离信源信道编码中自回归信源解码对残余比特错误极度敏感且易崩溃的问题。
method: 提出MASC方案，利用共享的参数化上下文记忆编码n-gram模式，并通过混合记忆专家路由实现稀疏的隐藏状态依赖路由。
result: 在瑞利衰落和AWGN信道上的实验表明，该方案能有效缩短平均码长并显著提升对信道误差的容忍度。
conclusion: 通过将上下文信息内化至信源建模过程，MASC显著增强了低信噪比通信中信源编码的效率与鲁棒性。
---

## 摘要
虽然分离信源信道编码（SSCC）保留了模块化系统设计的实际优势，但其在噪声文本传输中的有效性从根本上受到自回归信源解码脆弱性的限制。在低信噪比（SNR）环境下，即使是信道解码后少量的残留比特错误也可能导致后续的无损重建过程崩溃，特别是当算术编码（AC）依赖于基于大语言模型（LLM）的概率估计时。现有的补救措施要么仅基于信道观测来加强信道解码，要么仅在接收端引入上下文信息进行事后纠正，但两者都未能充分解决残留信道错误下信源概率建模的脆弱性。为此，本文提出了一种用于鲁棒 SSCC 传输的记忆增强信源编码（MASC）方案。MASC 并非将上下文视为外部辅助信息，而是将上下文模式内化到发送端信源编码器和接收端信源解码器共享的信源模型中。具体而言，MASC 采用共享的参数化上下文记忆（PCM）来编码多阶 n-gram 模式，并进一步引入记忆专家混合路由（MMER），在自回归信源建模过程中对记忆专家执行基于隐藏状态的稀疏路由。通过在每个编码步骤自适应地仅激活最相关的记忆，MASC 细化了信源概率估计，缩短了平均码长，并减轻了信源解码对残留信道错误的敏感性。在瑞利衰落信道和加性高斯白噪声（AWGN）信道上的广泛实验证明了该方案与现有先进方法相比的有效性。

## Abstract
While Separate Source-Channel Coding (SSCC) retains the practical benefits of modular system design, its effectiveness in noisy text transmission is fundamentally constrained by the fragility of autoregressive source decoding. In low-SNR regimes, even a small number of residual bit errors after channel decoding may derail the subsequent lossless reconstruction process, especially when Arithmetic Coding (AC) relies on Large Language Model (LLM)-based probability estimation. Existing remedies either strengthen channel decoding based solely on channel observations or introduce contextual information only at the receiver for post-hoc correction, yet neither fully addresses the fragility of source probability modeling under residual channel errors. To this end, this paper proposes a Memory-Augmented Source Coding (MASC) scheme for robust SSCC-based transmission. Rather than treating context as external side information, MASC internalizes contextual patterns into a source model shared by both the transmitter-side source encoder and the receiver-side source decoder. Specifically, MASC employs a shared Parameterized Contextual Memory (PCM) to encode multi-order $n$-gram patterns, and further introduces a Mixture-of-Memory-Experts Router (MMER) to perform sparse, hidden-state-dependent routing over memory experts during autoregressive source modeling. By adaptively activating only the most relevant memories at each coding step, MASC refines source probability estimation, shortens average codelength, and mitigates the sensitivity of source decoding to residual channel errors. Extensive experiments over Rayleigh fading and AWGN channels demonstrate the effectiveness of the proposed scheme compared with state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：低信噪比通信中基于上下文记忆增强的信源编码 (MASC)

#### 1. 核心问题与研究动机
*   **核心问题**：在低信噪比（Low-SNR）环境下，传统的“分离信源信道编码”（SSCC）存在严重的“悬崖效应”（Cliff Effect）。由于自回归信源解码（如基于LLM的算术编码）具有极强的序列依赖性，信道解码后的微小残余比特错误会导致后续解码彻底崩溃。
*   **研究动机**：现有的改进方案多集中在增强信道纠错能力或在接收端进行事后纠正，但未能从根本上解决信源概率建模在噪声环境下的脆弱性。本文旨在通过在收发两端共享上下文记忆，提高概率估计的准确性，从而缩短码长并增强系统对信道噪声的鲁棒性。

#### 2. 方法论
*   **核心思想**：提出**记忆增强信源编码（MASC）**框架。它不将上下文视为外部辅助信息，而是将其内化为收发双方共享的参数化模型，通过精准的概率预测减少传输比特数，间接提升抗噪能力。
*   **关键技术细节**：
    *   **参数化上下文记忆 (PCM)**：采用类似 Engram 的架构，利用哈希表存储多阶（2-gram 到 5-gram）的局部上下文模式。
    *   **混合记忆专家路由 (MMER)**：引入稀疏路由机制。根据当前 Transformer 层的隐藏状态，动态选择最相关的 $n$-gram 专家（Top-K 激活），避免全量计算，提高效率。
    *   **上下文感知门控融合**：将检索到的记忆表示通过门控机制（Sigmoid 激活）与 Transformer 的隐藏状态融合，并利用一维因果卷积（Conv1D）进一步提取局部非线性特征。
    *   **训练目标**：联合优化交叉熵损失（Next-token prediction）和辅助负载均衡损失（防止路由退化，确保各专家被均匀利用）。

#### 3. 实验设计
*   **数据集与场景**：使用 **Europarl** 英语语料库，涵盖瑞利衰落（Rayleigh）和加性高斯白噪声（AWGN）两种典型无线信道场景。
*   **Benchmark 与对比方法**：
    *   **SSCC 类**：Huffman 编码、基于 GPT-2 的算术编码（LLM-AC）、引入纠错 Transformer（ECCT）的方案、以及接收端上下文解码（ICD）。
    *   **JSCC 类**：DeepSC、通用 Transformer (UT) 及其量化版本。
*   **评估指标**：BLEU-1、BLEU-4（词法保真度）和基于 BERT 的语义相似度（语义保持度）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及训练时长。
*   **模型规模**：提到使用了预训练的 GPT-2 作为基础模型，并配置了 31 位精度的算术编码器，这暗示了实验具有一定的计算开销，但属于常规科研工作站可负担范围。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 7 种不同的基准方法。
    *   覆盖了从 0dB 到 15dB 的多个信噪比点。
    *   测试了两种主流信道模型。
*   **充分性评价**：实验设计较为充分，涵盖了从传统方法到前沿语义通信方法的横向对比。通过统一能量预算（Unified SNR）进行比较，确保了不同有效载荷长度下对比的公平性。但论文提到这是一个初步版本，更深入的消融实验（如不同 $n$ 阶记忆的影响）尚在进行中。

#### 6. 主要结论与发现
*   **性能领先**：MASC 在所有测试的信噪比范围内均优于传统的 SSCC 和 JSCC 方案，尤其在低 SNR 区域优势显著。
*   **缓解悬崖效应**：通过更精准的概率建模，MASC 缩短了平均码长，使得在相同信道条件下受错误干扰的概率降低，有效推迟了性能崩溃点的出现。
*   **自包含优势**：相比于依赖接收端外部信息的 ICD 方案，MASC 通过收发端共享记忆实现了更稳健的自包含传输。

#### 7. 优点
*   **架构创新**：将混合专家路由（MoE）思想引入信源编码的记忆管理，实现了上下文信息的稀疏激活。
*   **模块化兼容**：作为 SSCC 框架的改进，它保留了与现有工业标准（如 LDPC 码）无缝集成的潜力。
*   **鲁棒性强**：通过“内化”上下文模式，从信源端主动降低了对信道质量的极端依赖。

#### 8. 不足与局限
*   **计算复杂度**：虽然引入了稀疏路由，但维护多阶 $n$-gram 哈希表和 Transformer 骨干网对于资源受限的移动终端仍有挑战。
*   **数据依赖性**：记忆模块的效果高度依赖于训练数据与实际传输内容的分布一致性，对于完全陌生的领域（Out-of-distribution），增益可能受限。
*   **多模态扩展**：目前仅验证了文本传输，在图像、视频等更复杂的离散信源上的有效性尚待验证。

（完）
